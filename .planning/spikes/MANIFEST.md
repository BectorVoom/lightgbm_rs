# Spike Manifest

## Idea

Optimise lightgbm_rs **learning (train) speed**, both ends of the row-count curve:
- **High rows (GPU track, spike 001):** the ROCm path is launch-bound and ~100× slower
  on small data; prove where it crosses below the CPU anchor at scale. → ~700k rows.
- **Low rows (CPU track, spike 002):** the CPU anchor is ~2–3× off C++ even where it's
  closest; localize the fixed per-iteration gap via a per-phase A/B. → histogram build.
See `.planning/notes/gpu-track-goal-crossover-at-scale.md` and
`.planning/notes/low-row-gap-fixed-per-iteration-overhead.md`.

## Requirements

- Backend stays **compile-time switched** (`--features rocm`); the CPU f64 anchor
  remains the default and the bit-exact merge gate. Speed work must not touch parity.
- "GPU is faster" is only claimed in the regime the data supports — never route
  small/medium datasets to the GPU.
- Crossover claims are wall-clock facts; the ~1e-6 GPU-vs-CPU parity contract at large
  shapes is a separate, still-open gate.
- **`Vec<Vec<T>>` is not categorically a pessimization** (spikes 010/011). The
  discriminator is *usage*: a `vec![template; n]`-style structure rebuilt on the hot
  path is waste → flatten (010, shipped); a per-thread private accumulator written
  once + merged once is already optimal → keep it, flattening it forces shared-buffer
  false sharing (011, regressed 13–21%). Do not "tidy" the parallel build's
  per-feature `Vec<Vec<f64>>` in `build_histograms_into` — a NOTE marks it load-bearing.
- **GPU large-data attribution is measured, not assumed** (spike-014). At the wide
  shape (1M×500) GPU train wall-clock is ≈ ⅓ redundant resident-bin re-upload, ⅓
  growth-phases (scan-fused kernel + partition), ⅙ binning (bench artifact), ⅙ loop
  overhead. The histogram kernel is NOT the dominant cost. Profiling needs the
  `LGBM_PHASE_PROF=1` whole-train BUDGET (not just the growth-phase split, which folds
  the GPU kernel into "scan" via resident fusion and covers <½ the clock). Pending
  build lever: **hoist `upload_resident_bins` (learner.rs:826) out of the per-tree
  `train_inner` to once-per-train** — the bins are immutable, so re-uploading 2GB every
  tree is pure waste (≈−31% wall-clock at 1M×500, parity-neutral). [SHIPPED quick-p9v.]
- **Post-p9v, the wide-shape (×500) GPU bottleneck is the f32-atomic histogram BUILD**
  (spike-015), NOT the kernel-of-record, the upload, or the scan round-trip. The
  `phase_prof` "scan=96%" is an ASYNC ARTIFACT: the build launches un-synced, so its
  device-compute materializes inside the scan's `read_one_unchecked`. A forced pre-scan
  drain (`LGBM_SCAN_DRAIN=1`) re-attributes it: **build 86%→92% (GROWS with rows)**, scan
  launch+readback 14%→8% (shrinks), marshal+upload ~0%. Build is atomic-contention-bound
  (~820 Mr/s), ALREADY f32-atomic + LDS-privatized; spike-007 row-partition is inactive
  at wide (`768/500=1` → P=1). **Measured DEAD levers — do NOT pursue: scan-round-trip
  batching, constant-array hoisting, f32 build switch (already done).** At 250k×500 the
  CPU anchor (1.80s) beats the GPU (7.14s) ~4× ⇒ **wide-many-feature shapes route to
  CPU.** Only untried build lever = finer (per-warp) LDS sub-histogram privatization.

## Spikes

| # | Name | Type | Validates | Verdict | Tags |
|---|------|------|-----------|---------|------|
| 001 | gpu-cpu-crossover | standard | GPU train wall-clock crosses below the CPU anchor as rows scale up | ✅ VALIDATED — crossover ≈ 700k rows (feat=50/bins=255/31 leaves); GPU wins ≳1M, widening to 1.45× at 2M | gpu, rocm, performance, crossover, benchmark |
| 002 | lowrow-phase-ab | standard | Per-phase A/B vs C++ localizes the low-row fixed-overhead gap | ✅ VALIDATED — gap is ~entirely histogram BUILD: Rust 232µs/iter vs C++ 44.5 (5.2×) = 187µs of the ~188µs/iter gap; scan & partition near parity. Fix = R3 columnar storage + scratch reuse | performance, cpu, histogram, profiling, low-rows, a-b |
| 003 | columnar-hist-build | standard | Hoisting the redundant per-feature grad/hess gather to once-per-leaf speeds the build at both scales, bit-exactly | ✅ VALIDATED + SHIPPED — build −33% small / −39% large; train −16–18% small / −32–33% large; bit-exact gate green. The real R3 lever (quick-260614-p0n chased alloc and failed; the cost was redundant gather memory traffic) | performance, cpu, histogram, gather, bit-exact |
| 003b/r4o | fused-branchless-build | follow-on | Fold direct from the bin column into reused hot scratch, branchless (validation relocated upstream) | ✅ VALIDATED + SHIPPED (quick 260614-r4o) — train −17% small / −6.6% large on top of spike-003; bit-exact; per-element safety checks serialize the fold (relocated to once-per-train) | performance, cpu, histogram, branchless |
| 004 | columnar-u8-bins | standard | Narrowing the bin column u32→u8 speeds the gather+fold via cache density | ✅ VALIDATED + SHIPPED (quick 260614-ruz) — micro-bench isolated −58% u8; full impl (BinColumn u8/u16/u32 in lgbm-compute, per-width match fold) gave large train −49% (2.74→1.40s) / build −53%, small no-regression, bit-exact. Cumulative w/ 003+r4o: large −67% | performance, cpu, histogram, cache, bin-width |
| 005 | feature-parallel-build | standard | Parallelize the per-feature build across cores above a leaf-size threshold | ✅ VALIDATED + SHIPPED (inline) — rayon over features, gated at leaf_rows≥16384 (LGBM_PAR_THRESHOLD). Large train −26–31% (16 cores), small/medium NO regression (serial below threshold; unconditional parallel regressed small 5×). Bit-exact incl forced-parallel oracle 29/0 + permanent parallel==serial test. R4. Makes CPU anchor multi-threaded (reframes 1-core-vs-C++ basis + pushes spike-001 GPU crossover up) | performance, cpu, parallelism, rayon, R4 |
| 006 | gpu-u8-bins | standard | Narrow device bins u32→u8 to speed the GPU build (analog of the CPU u8 win) | ❌ INVALIDATED (gfx1100 micro-bench) — u8 vs u32 device-bin-read ≈0% (−0.6 to +1.7%). The GPU build is atomic-contention/scattered-read-LATENCY bound (234 Mreads/s, slow), NOT bin-bandwidth bound — CPU's L2-density mechanism doesn't apply. Cheap probe saved a multi-kernel plumbing dud. Bonus: Array<u8> proven to work on HIP | performance, gpu, rocm, bin-width, negative-result |
| 009 | multifeature-per-cube | standard | Does packing many small-bin features into one cube beat one-cube-per-feature at MATCHED occupancy? | ❌ INVALIDATED (gfx1100, 1M×128×32) — null/slight regression: packed/per-feature = 0.91–1.00× at matched 768 cubes (correctness 7.2e-6). Per-cube overhead was never the bottleneck; row-partitioning (007) already supplies occupancy, so packing has nothing to amortize and its extra LDS/loops slightly regress. Keep one-cube-per-feature × P. Closes the "wastes a CU on small-bin features" gap with evidence. | performance, gpu, packing, negative-result |
| 008 | 16bit-discretized-hist | standard | Can LightGBM's int16 discretized histogram (Lever 3) meet the exact-parity contract, or is it approximate-only? | ❌ INVALIDATED for exact parity (CPU probe) — quantization drift is irreducible: even at FULL int16 (65536 levels) rel err ≈ 3.2e-4, ~30× over the 1e-5 f32 gate / ~300× over the ~1e-6 contract; at LightGBM's default (4 bins) it's 200%+. It is `use_quantized_grad` (default FALSE) — an opt-in APPROXIMATE mode by construction. Can only ever be a separate opt-in mode, never a drop-in for the exact build. Cheap CPU probe killed a large packed-int-atomic kernel before plumbing. | performance, gpu, quantization, parity, negative-result |
| 007 | row-partitioned-histogram-build | standard | Split a feature's rows across P cubes (LightGBM grid_dim_y analog) to raise occupancy on large leaves | ✅ VALIDATED (gfx1100 micro-bench, 1M×50×256) — production kernel launches only 50 cubes (one/feature) on a 96-CU GPU = starved. Row-partition to P=16 (~800 cubes, ~8 wkgrps/CU) gives a stable **1.30–1.39×** build speedup (6/6 rounds across 3 process runs). Confirms spike-006's "latency-bound" was partly starved OCCUPANCY. P=32 over-partitions → regresses to ~1.0× (tune to ~8 wkgrps/CU, don't maximize). Win is modest + still atomic-bound (~820 Mr/s ≪ bandwidth ceiling) → pair with register-row-batching for the residual intra-cube contention. PARITY: P≥2 widens GPU-vs-P=1 f32 divergence 4e-7→~2e-5 rel (independent partial-sum trees); CPU f64 anchor untouched (merge gate safe) | performance, gpu, rocm, histogram, occupancy, row-partition |
| 010 | histogram-pool-arena | standard | Flatten the per-tree `HistogramPool.buffers: Vec<Vec<f64>>` → one flat `Vec<f64>` arena (slot = `[s*hist_len..]`) — faster + bit-exact? | ✅ VALIDATED + SHIPPED (Variant A) — the old `vec![vec![0.0; hist_len]; cache_size]` clone-memcpys a zeroed template `cache_size−1×` (~3MB traffic on large) + does N allocs, **per tree** (`learner.rs:816`). Flat arena = 1 alloc, no clones, contiguous subtraction-trick slots; public slot API unchanged ⇒ bit-exact (76 treelearner + 41 lgbm + oracle `raw_bin_train_parity` green). Isolated ceiling 8–22% of per-tree on med/large; **end-to-end −4.3% large / −1.6% medium / null small** (allocator amortizes per-tree realloc → below ceiling). R3 alloc-traffic win (cf 003/004). FOLLOW-ON: reuse pool across trees (learner-field hoist) targets the full 8–22% ceiling. | performance, cpu, histogram, allocation, arena, bit-exact, R3 |
| 012 | reuse-pool-across-trees | standard | Hoist `HistogramPool` from a per-tree local to a learner field, alloc once/train + `reset_map` per tree (the explicit 010 follow-on) — faster + bit-exact? | ✅ VALIDATED + SHIPPED (modest) — `hist_pool: Option<HistogramPool>` `take`/reset/store-back in `train_inner`; every slot is fully overwritten before read (zero+fill or copy_from_slice) so reuse = within-tree reuse = bit-exact (76 treelearner + 41 lgbm + oracle **raw_bin_train_matches_cpp_golden** green). End-to-end **~−3% large incremental on top of 010** (clusters 406–410 vs 392–400, non-overlapping), medium ~−1.5% noisy, small null. Well below the 8–22% isolated ceiling: the allocator already returned the same hot pre-faulted block on 010's per-tree realloc, so 010 captured most of it — reuse removes the residual. Combined 010+012 ≈ ~7% large vs original Vec<Vec>. | performance, cpu, histogram, allocation, reuse, bit-exact, R3 |
| 013 | feature-splittable-arena | standard | Flatten/reuse the per-tree `feature_splittable = vec![vec![true; nf]; nl]` bool matrix (the last `vec![template;n]` in the learner) — worth it? | ❌ INVALIDATED (not worth it; isolated microbench, 2 runs) — construction is **0.005–0.25% of per-tree time** (small 0.25%, medium 0.02%, large 0.005%): real `vec![template;n]` pattern but ~1.5KB vs the pool's MBs, so the magnitude is sub-noise. Literal flatten would change the `[leaf][feature]` access type at multiple sites for ≤0.25% best case → keep `Vec<Vec<bool>>` as-is, NO production change. Closes the "in learning" Vec<Vec<T>> sweep (branch_features/bynode_selected conditional-empty, interaction_constraints config-time). Confirms the rule: flatten a per-tree `vec![template;n]` only when MB-scale. | performance, cpu, allocation, negative-result, not-worth-it |
| 011 | parallel-build-scatter | standard | Replace the rayon build's per-feature `Vec<Vec<f64>>` accumulators + reassembly copy with a scatter that folds each feature straight into disjoint `&mut out` slots — faster + byte-identical? | ❌ INVALIDATED (CPU microbench, 16-core; 3 process runs) — the `Vec<Vec<f64>>` intermediate is **load-bearing, not a wart**. Scatter regressed the threshold-sized leaves the parallel path actually runs on (16k–131k rows) by **13–21%** (0.79–0.87× live/scatter), null only on huge leaves (≥500k = 1.00×). Thread-private cache-hot accumulators + one sequential memcpy beat 16 threads scattering into a shared 200KB `out` (cache-coherence/false-sharing > the alloc+copy removed). End-to-end bench too noisy (build ≪ train at these shapes) → isolated in-process microbench per CONVENTIONS. Build stays gather-bound (002/003/006). Production reverted + NOTE left at call site; permanent `#[ignore]` regression-evidence microbench in-crate. | performance, cpu, histogram, parallelism, rayon, false-sharing, negative-result |
| 014a | coarse-phase-attribution | standard | Given a 1M×500 GPU train, when the wired `phase_prof` dump runs, then one phase (build/scan/partition) is shown to dominate wall-clock — settling whether the histogram kernel is even the bottleneck at the wide shape | ⚠️ PARTIAL — coarse attribution obtained, **overturns the framing**. At 500 feat (≥`unified_bfs_threshold`=100) the GPU build+fix+scan fuse into ONE `build_fix_scan_resident` launch timed under SCAN, so `build=0` is a fusion artifact and "scan=93%" CONTAINS the kernel. Worse, `phase_prof` covers only 31–45% of train wall-clock: the **uninstrumented remainder GROWS with rows (55%→62%→69% at 250k/500k/1M)**. iters A/B (250k): per-iter 2.07s, fixed binning-setup ~1.56s (~18%, a bench artifact); ~HALF of every iteration is uninstrumented (boosting loop / per-tree GPU transfer). ⇒ kernel ≤31% of wall-clock, NOT shown to be the bottleneck; existing tooling structurally inadequate → 014b retargeted to a whole-train budget + inside-resident-launch timers | performance, gpu, rocm, profiling, attribution, wide-shape |
| 014b | gpu-launch-vs-compute-split | standard | Given the dominant phase from 014a, when launch / device-compute / readback are separated with new GPU timers, then we learn whether the cost is slow device-compute (occupancy/atomics) vs the sync round-trip — pointing to the next real lever | ✅ VALIDATED — whole-train budget (binning/grad/learner/score + UPLOAD drill-down) names the uninstrumented half: at 1M×500 GPU train splits ~31% **redundant resident-bin re-upload** / ~31% growth-phases (scan-fused kernel+partition) / ~18% binning (bench artifact) / ~17% loop overhead. `in_learner_other` is GPU-only (16× CPU A/B) and **93% is the resident-bin upload**: `learner.rs:826` re-allocs 2×2GB + re-uploads the IMMUTABLE bin matrix to device EVERY tree (`train_inner` is per-tree; comment says "per-train" — stale). **NEXT LEVER: hoist `upload_resident_bins` to once-per-train ⇒ ≈−31% wall-clock at 1M×500, parity-neutral.** Original "kernel is THE bottleneck" overturned: kernel ≈ redundant upload ≈ ⅓ each. Bit-exact gate green (instrumentation behavior-neutral) | performance, gpu, rocm, profiling, redundant-upload, next-lever |
| 015 | scan-roundtrip-decomposition | standard | Given the post-p9v wide GPU train where `phase_prof` shows scan≈96%, when the per-leaf scan round-trip is decomposed (marshal/upload/launch+readback) and a forced build-drain separates build-compute from scan, then the true dominant cost is named | ⚠️ PARTIAL — **premise invalidated, bottleneck definitively located.** Began as "replace seq-f64 build with parallel-f32"; reading the code killed it twice (fused-f64 path is OFF by default `FUSED_MAX_NUM_DATA=-1`; the wide build ALREADY runs parallel f32-atomic). New `LGBM_SCAN_PROF` timers: marshal 0% + upload 0.1% + launch+readback 99.8% — but that's an ASYNC ARTIFACT (build launches un-synced; its compute drains inside the scan's `read_one_unchecked`). `LGBM_SCAN_DRAIN=1` re-attributes: **f32-atomic BUILD 86%→92% (GROWS with rows)**, scan launch+readback 14%→8%, marshal+upload ~0%. Build is atomic-contention-bound (~820 Mr/s), already LDS-privatized; row-partition inactive at wide (`768/500=1`→P=1). **DEAD levers (measured): scan-round-trip batching, array-hoisting, f32 build switch.** CPU anchor beats GPU ~4× at 250k×500 (1.80 vs 7.14s) ⇒ route wide shapes to CPU. Only untried build lever = finer per-warp LDS sub-hist privatization. Instrumentation kept (env-gated, parity-neutral, 76/76 treelearner green) | performance, gpu, rocm, scan, build, atomic-bound, async-artifact, wide-shape, premise-invalidated |
| 016 | parallel-scan-reorder-parity | standard | Given the within-feature best-split scan, when its f64 prefix-sums are REORDERED (as a parallel scan would), then does the chosen split stay within the hip ~1e-6 gate or does the argmax flip → tree divergence? | ⚠️ PARTIAL — **partition parity-safe; default-direction flag is the open residual.** Cheap HOST f64 probe (no GPU, spike-008 method, `examples/spike016_scan_reorder_probe.rs`) replicated the reverse+forward scan calling the REAL `get_split_gains`, comparing SEQUENTIAL vs PAIRWISE-TREE prefix-sums over 200k representative histograms. **best_threshold flips = 0/200k on realistic data** (threshold never flips ⇒ partition of present data ALWAYS identical; gains diverge only ~1e-12 ≪ 1e-6 gate) — the core fear (reorder → different split → tree divergence) is NOT realized; flips need ~1e-9 near-ties that don't occur naturally (10.6% only under artificial clustering). **BUT default_left flips ~34%** at equal-gain reverse-vs-forward ties (same threshold, only the missing/default-direction flag differs — the def-hip-split tie-aware phenomenon). LIMITATION: the probe doesn't model the kernel's `most_freq_bin`/default-bin semantics, so whether a dleft flip is cosmetic (missing-only) or a real default-bin partition diff is UNRESOLVED by a host probe → needs a GPU prototype vs `kernel_parity_split_within_tol_on_hip`. **Recommendation: DEFER wiring** — parity risk acceptable-pending-GPU-confirmation, but ROI is low (scan=11% device, already cross-feature parallel, GPU loses to CPU on this 8-CU APU). Revisit only if discrete-GPU perf matters. | performance, gpu, rocm, scan, parity, argmax, reorder, host-probe, defer |
| 017 | perwarp-lds-replication | standard | Given the wide-shape f32-atomic BUILD (one cube/feature, P=1), when each WARP gets its own LDS sub-hist replica (R=warps/cube) instead of 256 threads sharing one, then per-cube atomic contention drops and BUILD device-time falls | ✅ VALIDATED (modest) — **first POSITIVE GPU build-kernel lever** (006/008/009 null, 007 wide-inactive, p93 warp-aggregation null). R8 (=1 replica/warp) sign-stable **~1.1× device-time** (1.06–1.29× across bins, both process runs); R2/R4 NULL. Mechanism: CUBE_DIM 256 / PLANE_DIM 32 = 8 warps, so only R8 removes inter-warp same-address LDS-atomic serialization — partial replication leaves cross-warp contention intact (all-or-nothing at the warp boundary). Win does NOT shrink at high bins ⇒ serialization-latency-bound, not collision-count-bound. Parity within ~1e-5 gate (R8 vs anchor 5.9e-7–1.1e-5; at 16 bins R8 CLOSER to anchor than R1). **DISPOSITION: keep `examples/gpu_lds_replication.rs` as rocm-gated evidence, do NOT wire** — modest + APU-only (occupancy tradeoff of 8× LDS unvalidatable on 8-CU APU), and CPU still beats GPU ~4× at wide (routing unchanged). Wire only if discrete-gfx110x perf becomes a goal (needs oracle parity re-pin). Closes the MANIFEST's "only untried build lever" with a measured modest-positive | performance, gpu, rocm, histogram, lds, privatization, replication, contention, device-time-proxy, wide-shape |
| 018 | fixedpoint-int-atomics | standard | Given the wide f32-atomic BUILD, when grad/hess are accumulated as wide fixed-point i64 (S=2^30) via INTEGER LDS atomics instead of f32, then it stays within ~1e-6 of the f64 anchor, is deterministic, AND is not slower — resolving research Q2 / finding #3 | ✅ VALIDATED (STRONG) — **strongest GPU build lever found; triple-aligned with the parity-first contract.** 018a CPU parity GATE PASS (opposite of spike-008's int16 null): i64@S=2^30 within ~1e-6, **~10^3-10^4× MORE accurate than f32** (2.5e-8/exact/exact vs f32 7e-4/8e-5/1e-3), EXACT in the cancelling regime, deterministic; i32 overflows (i64 required). 018b GPU (gfx1152 APU, single-cube=wide P=1): feasibility YES via `Atomic<u64>` two's-complement (cubecl-hip 0.10 `Atomic<i64>::store`→`atomicExch(long long*)` is a codegen gap — HIP only has u64 atomicExch); accuracy 5.9e-9 vs f32 2.2e-5; **speed i64/f32 = 1.80×/2.06× both SEP-WIN** (sign-stable, 2 process runs). **Reconciles W5 null** (packed-int P=16 = low contention): under high per-cube contention (wide P=1) f32 atomicAdd is a CAS retry loop (ds_cmpst) that explodes, integer ds_add_u64 is native single-instr — both right per regime, 018b's IS the wide bottleneck. **DISPOSITION: pending human decision** — robust multi-axis win worth wiring, but a large change (new u64 kernel, i64 buffers 2× bytes, oracle parity RE-PIN; def-f8u-01). Caveats: APU-only microbench, overflow guard for extreme leaves, P-sweep would confirm the contention-regime hypothesis. Evidence: `examples/fixedpoint_parity_probe.rs` + `examples/gpu_fixedpoint_i64.rs` | performance, gpu, rocm, histogram, fixed-point, integer-atomics, determinism, parity, finding-3, wide-shape |
