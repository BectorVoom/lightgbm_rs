---
milestone: "1.0"
audited: 2026-06-21
status: passed
scope:
  phases_in_milestone: [1, 2, 3, 4, 5, 6, 7, 8]
  out_of_scope_on_disk: ["09-gpu-hist-build-perf", "10-quantized-training"]  # never roadmapped into v1.0; post-milestone work
scores:
  requirements: 69/69
  phases: 8/8
  integration: PASS
  flows: 3/3
gaps: {}        # no critical blockers
tech_debt:
  - phase: cross-cutting
    items:
      - "REQUIREMENTS.md traceability table line 179: TRL-02 status column reads 'In Progress (gap closure 05-07)' but the requirement is satisfied (checkbox [x], 05-07 SUMMARY requirements_completed lists TRL-02, ROADMAP marks 05-07 WR-01/WR-02 CLOSED, Phase 5 VERIFICATION=passed). Cosmetic stale label — will be normalized when complete-milestone marks all v1 requirements complete in the archive."
  - phase: nyquist
    items:
      - "6 of 8 phases have VALIDATION.md with nyquist_compliant:false (01,02,03,06,08) or wave_0_complete:false (05). Discovery-only; not a merge gate. Phases 04 and 07 are fully compliant. Optional: /gsd-validate-phase <N> to retro-fill."
  - phase: 06-gbdt-spine + 07-parity-variants
    items:
      - "bagging_by_query=true over the DenseCorpus facade returns a typed error (booster.rs:1183-1192) — the dense facade carries no query metadata. The query-grouped bagging draw itself IS implemented and RNG-replay-tested at the rank_parity harness level. Documented surface limit, not a broken wire."
nyquist:
  compliant_phases: [4, 7]
  partial_phases: [1, 2, 3, 5, 6, 8]
  missing_phases: []
  overall: partial
---

# Milestone v1.0 — Audit Report

**Status: ✅ PASSED.** All 69 v1 requirements satisfied, all 8 in-scope phases verified `passed`, cross-phase integration PASS, all 3 E2E pipelines complete. No critical blockers. Minor, non-blocking tech debt noted below.

## Scope

Milestone v1.0 = **Phases 1–8** per `ROADMAP.md` (the full single-machine C++ parity spine + Python bindings). Definition of done: reproduce C++ LightGBM outputs within ~1e-6 (f32) on CPU and ROCm, exposed via a Rust-native API and Python bindings.

> **Note:** Phase directories `09-gpu-hist-build-perf` and `10-quantized-training` exist on disk but were **never roadmapped into v1.0** — they are post-milestone work (the GPU/CPU perf campaign run as quick tasks, and the v2 `QNT-01` quantized-training spike). They have no VERIFICATION.md and are correctly excluded from this audit. (Phase 10 maps to the deferred **v2** requirement QNT-01.)

## Requirements Coverage — 69/69 satisfied

3-source cross-reference (REQUIREMENTS.md checkbox + phase VERIFICATION.md status + SUMMARY.md `requirements_completed` frontmatter) for every milestone REQ-ID:

| Phase | REQ-IDs | Verdict |
|-------|---------|---------|
| 1 | FND-01..04, CFG-01..03, ORA-01, ORA-02 (9) | ✅ all satisfied (VERIFICATION verified; 01-03 closed CFG-02/03) |
| 2 | DAT-01..07, ORA-03 (8) | ✅ all satisfied (VERIFICATION passed; 02-06/07 closed DAT-01/07 at Construct level) |
| 3 | DAT-08, DAT-09, PRD-01..03, PRD-06 (6) | ✅ all satisfied |
| 4 | CMP-01..05, ORA-04 (6) | ✅ all satisfied (cpu bit-exact hard gate + hip ~1e-6 separate gate, D-03a) |
| 5 | TRL-01..05, TRL-07..09 (8) | ✅ all satisfied (bit-exact vs real lib_lightgbm 4.6 on both corpora; 05-09 closed final 2-ULP) |
| 6 | BST-01..03, BST-07, OBJ-01..03, MET-01, MET-02, API-01 (10) | ✅ all satisfied |
| 7 | BST-04..06, TRL-06, OBJ-04..06, MET-03, MET-04, PRD-04, PRD-05, ADV-01..07 (18) | ✅ all satisfied |
| 8 | PYB-01..04 (4) | ✅ all satisfied |

**Orphan check:** none. Every REQ-ID in the traceability table is declared in a phase plan and verified.

**TRL-02 note:** the traceability table's status *column* (line 179) still reads "In Progress (gap closure 05-07)". This is a **stale label**, not an unsatisfied requirement — the checkbox is `[x]`, the requirement body says "COMPLETE (05-07)", 05-03 and 05-07 SUMMARYs list TRL-02 in `requirements_completed`, ROADMAP marks 05-07's outcome "WR-01/WR-02 CLOSED", and Phase 5 VERIFICATION is `passed`. Classified **satisfied** by the status matrix (passed + listed + `[x]`). Recorded as a cosmetic doc fix.

## Phase Verifications — 8/8 passed

| Phase | VERIFICATION status | Notes |
|-------|---------------------|-------|
| 1 | verified (was gaps_found → closed by 01-03) | CFG-02/03 blockers closed |
| 2 | passed (was gaps_found → closed by 02-07) | faithful Dataset::Construct |
| 3 | passed | predict + model-text round-trip |
| 4 | passed | cpu bit-exact + hip best-effort |
| 5 | passed (was gaps_found → closed 05-05..09) | bit-exact vs real binary, both corpora |
| 6 | passed (was gaps_found → closed by 06-06) | end-to-end train→predict |
| 7 | passed | all parity variants |
| 8 | passed | PyO3 + sklearn bindings |

## Cross-Phase Integration — PASS

Independent integration checker verified every seam is wired through the **production call graph** (not dead code / test-only bypass), with file:line evidence:

- facade `lgbm::Booster` → BinMapper → SerialTreeLearner → `lgbm-compute::Backend` (live `subtract_histograms`/`build_resident_leaf`/`find_best_splits_batched`/`data_partition` calls) → Gbdt loop → GbdtModel → predict.
- `lgbm-python` `_core` → facade `train_raw`/`predict`; sklearn wrappers → engine → `_core`.
- **CMP-01 switchability holds:** no crate above `lgbm-compute` names a CubeCL runtime; CPU-only `cargo build --workspace --exclude lgbm-python` links with no ROCm toolchain.

## E2E Flows — 3/3 complete

- **Rust:** raw matrix → Dataset(bin) → train → predict (raw/sigmoid/leaf) → model-text write → reload → predict. ✅ (`public_api_train_predict_round_trip`, `booster_facade_model_text_round_trips`; 41/41 facade tests).
- **Python:** numpy/scipy/polars → PyO3 Booster → train → predict → sklearn → refit + custom callbacks. ✅ links / delegation chain statically consistent (cargo check passes; not maturin-built in this audit).
- **Oracle:** kernel_parity (6) / learner_parity (29) / boosting_parity (75) / predict_parity (5) all drive the wired production path, not a reimplementation. ✅

## Nyquist Coverage (discovery-only, non-blocking)

| Phase | VALIDATION.md | Compliant | Suggested action |
|-------|---------------|-----------|------------------|
| 1 | exists | false | optional `/gsd-validate-phase 1` |
| 2 | exists | false | optional `/gsd-validate-phase 2` |
| 3 | exists | false | optional `/gsd-validate-phase 3` |
| 4 | exists | true | — |
| 5 | exists | true (wave_0 incomplete) | optional `/gsd-validate-phase 5` |
| 6 | exists | false | optional `/gsd-validate-phase 6` |
| 7 | exists | true | — |
| 8 | exists | false | optional `/gsd-validate-phase 8` |

These are validation-coverage markers, not test failures — the phases verified `passed` on their own VERIFICATION.md. Retro-filling is optional and does not block milestone close.

## Verdict

**v1.0 is cleanly closeable.** Requirements 69/69, phases 8/8 passed, integration PASS, flows 3/3. The tech-debt items are cosmetic (one stale traceability label) or optional (Nyquist retro-fill), and the one functional deferral (`bagging_by_query` over the dense facade) is a documented, RNG-tested surface limit. Proceed to `/gsd-complete-milestone 1.0`.
