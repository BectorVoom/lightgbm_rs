---
phase: 05-tree-learner-split-finding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/lgbm-treelearner/Cargo.toml
  - crates/lgbm-treelearner/src/lib.rs
  - crates/lgbm-treelearner/src/error.rs
  - crates/lgbm-treelearner/src/split_info.rs
  - crates/lgbm-model/src/tree.rs
  - xtask/cpp/learner_capture.cpp
  - xtask/src/main.rs
  - crates/oracle-harness/tests/learner_parity.rs
  - REFERENCE_MANIFEST.md
autonomous: true
requirements: [TRL-01, TRL-04]
must_haves:
  truths:
    - "A new lgbm-treelearner crate builds as a workspace member and exposes a TreeLearnerError thiserror boundary"
    - "lgbm-model::Tree gains a split() mutation method + growth-time arrays and grows a 2-leaf tree whose to_string() is byte-stable"
    - "learner_parity.rs exists and SKIPs gracefully when the learner golden is absent (failing end-to-end harness in place)"
    - "D-01: the header-only C++ transcription capture harness (learner-capture xtask + learner_capture.cpp) is the reference-tree/per-split golden strategy — no real lib_lightgbm build, goldens committed and replayable with no C++ toolchain at test time"
  artifacts:
    - path: "crates/lgbm-treelearner/Cargo.toml"
      provides: "new workspace member crate; path deps lgbm-core/dataset/compute/model + thiserror; NO cubecl dep (CMP-01)"
      contains: "lgbm-compute"
    - path: "crates/lgbm-treelearner/src/error.rs"
      provides: "TreeLearnerError variants (LengthMismatch, InvalidLeafHessian, Compute #[from])"
      contains: "TreeLearnerError"
    - path: "crates/lgbm-treelearner/src/split_info.rs"
      provides: "re-export of lgbm_compute::gain::SplitInfo + split_gt tie-break helper"
      contains: "split_gt"
    - path: "crates/lgbm-model/src/tree.rs"
      provides: "Tree::split mutation + leaf_depth/leaf_parent/split_feature_inner/threshold_in_bin growth arrays"
      contains: "fn split"
    - path: "crates/oracle-harness/tests/learner_parity.rs"
      provides: "per-split + per-tree golden replay harness with graceful SKIP"
      exports: []
  key_links:
    - from: "Cargo.toml"
      to: "crates/lgbm-treelearner"
      via: "workspace members entry"
      pattern: "lgbm-treelearner"
    - from: "crates/lgbm-treelearner/src/split_info.rs"
      to: "crates/lgbm-compute/src/gain.rs"
      via: "re-export SplitInfo (no duplicate struct)"
      pattern: "lgbm_compute::gain::SplitInfo"
    - from: "crates/oracle-harness/tests/learner_parity.rs"
      to: "tests/fixtures/learner"
      via: "committed golden path, never LightGBM/"
      pattern: "fixtures/learner"
---

<objective>
The enabling vertical slice (MVP first-task discipline): stand up the `lgbm-treelearner` crate skeleton with its `thiserror` boundary, add the `Tree::split` mutation + growth-time arrays to `lgbm-model` (so the grown tree can be serialized via the Phase-3 `%.17g` machinery for D-07), reuse `lgbm_compute::gain::SplitInfo` (NOT a duplicate struct, per RESEARCH Standard Stack) and add only the C++ `SplitInfo::operator>` tie-break helper, and scaffold the `learner-capture` xtask subcommand + `learner_capture.cpp` emitter + `learner_parity.rs` replay harness so a FAILING end-to-end test harness exists before the spine learner is written.

Purpose: This is the "thinnest end-to-end skeleton" task — after it, `cargo test -p oracle-harness --test learner_parity` runs and SKIPs gracefully (golden absent), `lgbm-model::Tree::split` can grow a node, and the crate compiles. Every subsequent plan implements against these contracts (interface-first ordering) instead of exploring the codebase.

Output: a new workspace-member crate, a `Tree::split` method proven by a unit test that grows a root → 2-leaf tree and asserts the serialized form is byte-stable, the split_info tie-break helper with a unit test for the gain-tie smaller-feature rule, and the capture+parity scaffolding (graceful-SKIP harness, manifest entry).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tree-learner-split-finding/05-RESEARCH.md
@.planning/phases/05-tree-learner-split-finding/05-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New crate + symbols:
- New crate `lgbm-treelearner` (workspace member; path deps lgbm-core/dataset/compute/model + thiserror; dev-dep oracle-harness).
- `lgbm_treelearner::error::TreeLearnerError` (variants: `LengthMismatch { expected, actual }`, `InvalidLeafHessian { value }`, `Compute(#[from] lgbm_compute::ComputeError)`, plus a `BinIndexOutOfRange`/`InvalidNumLeaves` variant as the V5 boundary needs).
- `lgbm_treelearner::split_info` module: re-export `pub use lgbm_compute::gain::SplitInfo;` + `pub fn split_gt(a, a_feat, b, b_feat) -> bool` (gain, then smaller feature index, -1 → i32::MAX).
- `lgbm_treelearner::lib` facade (module declarations + re-exports; the `SerialTreeLearner` type itself is introduced in Plan 03 — this plan may declare an empty `learner` module or omit it).
- `lgbm_model::Tree::split(...)` — new mutation method.
- `lgbm_model::Tree` new growth-time-only fields: `leaf_depth: Vec<i32>`, `leaf_parent: Vec<i32>`, `split_feature_inner: Vec<i32>`, `threshold_in_bin: Vec<u32>` (kept out of the serialized `to_string()` form, which already matches C++).
- `xtask` `learner-capture` subcommand + `xtask/cpp/learner_capture.cpp` (scaffold: emits the record-format header + at least a trivial fixture so the harness has a target; full learner transcription content lands in Plan 03/04).
- `crates/oracle-harness/tests/learner_parity.rs` — new replay harness (graceful SKIP).
- `tests/fixtures/learner/` — committed golden directory (created here, populated by capture in Plan 03/04).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: lgbm-model::Tree::split mutation + growth-time arrays (D-07 enabler)</name>
  <files>crates/lgbm-model/src/tree.rs</files>
  <read_first>
    - crates/lgbm-model/src/tree.rs (the existing parallel-array Tree fields ~60-98; the CATEGORICAL_MASK/DEFAULT_LEFT_MASK/MISSING_ZERO/MISSING_NAN decision_type bit constants ~45-53; the byte-exact to_string() ~238-262; the GetDecisionType helper ~100)
    - .planning/phases/05-tree-learner-split-finding/05-RESEARCH.md (Code Examples §"Tree growth mutation" citing tree.h:543-585 + tree.cpp:61-75; the field-existence note)
    - .planning/phases/05-tree-learner-split-finding/05-PATTERNS.md (§"crates/lgbm-model/src/tree.rs — EXTEND")
  </read_first>
  <action>
    Add four growth-time-only parallel-array fields to `Tree`: `leaf_depth: Vec<i32>`, `leaf_parent: Vec<i32>`, `split_feature_inner: Vec<i32>`, `threshold_in_bin: Vec<u32>`, each documented with the C++ field-name correspondence (`leaf_depth_`, `leaf_parent_`, `split_feature_inner_`, `threshold_in_bin_`). These are growth-bookkeeping only and MUST NOT appear in `to_string()` (the serialized form already matches C++; verify to_string() is unchanged). Add a `split(...)` mutation method transcribing the structural part of `tree.h:543-585` + the public `tree.cpp:61-75` additions: parameters mirror the C++ `Tree::Split` (leaf index, split_feature (original) + split_feature_inner, threshold_in_bin (u32) + threshold (f64 real value), left_value/right_value/left_weight/right_weight/left_count/right_count (f64/i32), split_gain (f32), default_left + missing_type for the decision_type bit-packing). Mechanics: `new_node = num_leaves - 1`; rewire the parent's child pointer to this node; set `split_feature_inner_[node]`/`split_feature_[node]`/`split_gain_[node]`/`threshold_in_bin_[node]`/`threshold_[node]`; `left_child[node] = ~leaf`, `right_child[node] = ~num_leaves`; `internal_value[node] = leaf_value[leaf]`; `leaf_value[leaf] = left_value`, `leaf_value[num_leaves] = right_value` (same for weight/count); `leaf_depth[num_leaves] = leaf_depth[leaf] + 1`, `leaf_depth[leaf] += 1`; `leaf_parent` updates; `decision_type[node]` packed via the existing DEFAULT_LEFT_MASK + MISSING_* constants; `++num_leaves`. Do NOT implement SplitCategorical (TRL-06 deferred to Phase 7). Leaf outputs are passed in (computed by the learner via gain::calculate_splitted_leaf_output) — Tree::split stores them, it does not compute gain.
  </action>
  <verify>
    <automated>cargo test -p lgbm-model tree_split</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn split' crates/lgbm-model/src/tree.rs` >= 1
    - `grep -c 'leaf_depth\|split_feature_inner\|threshold_in_bin' crates/lgbm-model/src/tree.rs` >= 3 (growth arrays added)
    - A new unit test grows a single-node Tree (root leaf → split → 2 leaves), asserts `num_leaves == 2`, `left_child[0] == !0` (or the C++ `~leaf` encoding), `internal_value[0] == old root leaf_value`, and `leaf_depth == [1, 1]`
    - The same test asserts `to_string()` of the 2-leaf tree is byte-identical across two calls (serialization stable; growth arrays excluded)
    - `cargo test -p lgbm-model` exits 0 (existing Phase-3 load/predict/write tests unaffected)
  </acceptance_criteria>
  <done>Tree::split grows a node + 2 leaves with the C++ rewiring, growth arrays track depth/parent/inner-feature/bin-threshold, and the serialized form is byte-stable and unchanged from Phase 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: lgbm-treelearner crate scaffold + TreeLearnerError + split_info tie-break</name>
  <files>Cargo.toml, crates/lgbm-treelearner/Cargo.toml, crates/lgbm-treelearner/src/lib.rs, crates/lgbm-treelearner/src/error.rs, crates/lgbm-treelearner/src/split_info.rs</files>
  <read_first>
    - Cargo.toml (the [workspace] members list — add the new crate)
    - crates/lgbm-compute/Cargo.toml (the member-crate shape: edition.workspace/rust-version.workspace, path deps, thiserror = { workspace = true })
    - crates/lgbm-compute/src/error.rs (the #[derive(Debug, Error, Clone, PartialEq)] V5 boundary idiom + #[from] wrapping)
    - crates/lgbm-compute/src/gain.rs (the existing SplitInfo struct + none() sentinel ~254-312 — reuse, do not duplicate)
    - .planning/phases/05-tree-learner-split-finding/05-PATTERNS.md (§Cargo.toml, §lib.rs, §split_info.rs — the split_gt helper shape and "prefer re-export" decision)
    - .planning/phases/05-tree-learner-split-finding/05-RESEARCH.md (§Security V5 — the boundary checks the learner adds)
  </read_first>
  <action>
    Create `crates/lgbm-treelearner/Cargo.toml` mirroring `lgbm-compute/Cargo.toml`: `edition.workspace = true`, `rust-version.workspace = true`, `[dependencies]` path deps `lgbm-core`, `lgbm-dataset`, `lgbm-compute`, `lgbm-model`, `thiserror = { workspace = true }`, and `[dev-dependencies] oracle-harness = { path = "../oracle-harness" }`. Do NOT add a `cubecl` dependency (CMP-01 — the learner takes a `&impl Backend`, never names a runtime). Add `"crates/lgbm-treelearner"` to the workspace `members` list in the root `Cargo.toml`. Create `src/error.rs` with `TreeLearnerError` (`#[derive(Debug, Error, Clone, PartialEq)]`): variants `LengthMismatch { expected: usize, actual: usize }`, `InvalidLeafHessian { value: f64 }` (guards the `cnt_factor = num_data / sum_hessian` division with a `!(sum_hessian > 0.0)` NaN-catching check per RESEARCH §Security V5), `BinIndexOutOfRange { index: u32, num_bin: u32 }`, `InvalidNumLeaves { value: i32 }`, and `Compute(#[from] lgbm_compute::ComputeError)` (wrap upstream errors; never re-validate the kernel boundary). Create `src/split_info.rs`: `pub use lgbm_compute::gain::SplitInfo;` (reuse, no duplicate) plus `pub fn split_gt(a: &SplitInfo, a_feat: i32, b: &SplitInfo, b_feat: i32) -> bool` transcribing `split_info.hpp:138-165` — compare `a.gain != b.gain ⇒ a.gain > b.gain`, else smaller feature index wins with `-1 → i32::MAX`. Create `src/lib.rs` with module declarations (`pub mod error; pub mod split_info;`) + re-exports (`pub use error::TreeLearnerError; pub use split_info::{SplitInfo, split_gt};`) and a crate-level doc comment naming the C++ port target. The `learner` module is declared/implemented in Plan 03.
  </action>
  <verify>
    <automated>cargo test -p lgbm-treelearner</automated>
  </verify>
  <acceptance_criteria>
    - `crates/lgbm-treelearner` appears in the root `Cargo.toml` workspace members and `cargo build -p lgbm-treelearner` exits 0
    - `grep -c 'cubecl' crates/lgbm-treelearner/Cargo.toml` == 0 (CMP-01 containment honored)
    - `grep -c '#\[from\] lgbm_compute::ComputeError\|#\[from\]' crates/lgbm-treelearner/src/error.rs` >= 1
    - `grep -c 'pub use lgbm_compute::gain::SplitInfo' crates/lgbm-treelearner/src/split_info.rs` == 1 (no duplicate struct)
    - A unit test for `split_gt` asserts: higher gain wins; on equal gain the smaller feature index wins; feature `-1` is treated as `i32::MAX` (loses ties to any real feature)
    - The existing CMP-01 containment guard test in `lgbm-compute` still passes (`cargo test -p lgbm-compute cmp01` or the workspace run)
  </acceptance_criteria>
  <done>The lgbm-treelearner crate builds with a thiserror boundary, reuses (not duplicates) SplitInfo, and the gain/smaller-feature tie-break helper is unit-proven.</done>
</task>

<task type="auto">
  <name>Task 3: learner-capture xtask + learner_capture.cpp scaffold + learner_parity.rs harness (Wave 0 infra)</name>
  <files>xtask/cpp/learner_capture.cpp, xtask/src/main.rs, crates/oracle-harness/tests/learner_parity.rs, REFERENCE_MANIFEST.md</files>
  <read_first>
    - xtask/src/main.rs (the dispatch arms ~75-78; the `kernel_capture()` configure→build→run→assert sequence; the byte-idempotent recorded-seed doc; the REFERENCE_MANIFEST.md refresh pattern)
    - xtask/cpp/kernel_capture.cpp (the verbatim-transcription header doc ~1-40; the raw little-endian bit serialization helpers; the record-format-as-doc-block convention)
    - crates/oracle-harness/tests/kernel_parity.rs (fixture_path/kernels_dir ~37-43; graceful-SKIP-when-absent doc ~13-15; the raw-bit parse helpers ~45-99; compare_exact_f64_bits usage ~32-35)
    - crates/oracle-harness/src/comparator.rs (compare_exact_f64_bits / compare_exact_u32 signatures — reuse as-is)
  </read_first>
  <action>
    Add a `Some("learner-capture") => learner_capture()` dispatch arm + a `learner_capture()` fn to `xtask/src/main.rs` mirroring `kernel_capture()`: cmake-configure a standalone header-only build for `xtask/cpp/learner_capture.cpp`, build the `learner_capture` target, run it to write goldens under `tests/fixtures/learner/`, assert the `.txt` files were written, and refresh `REFERENCE_MANIFEST.md`. Record a fixed `LEARNER_MASTER_SEED` so re-running is byte-idempotent. Create `xtask/cpp/learner_capture.cpp` as a SCAFFOLD: the verbatim-transcription header doc (WHY header-only — `external_libs` unvendored, `LightGBM/` untracked), the raw-bit serialization helpers copied from kernel_capture.cpp, a documented record format for two golden kinds — per-split snapshot (`PSPLIT split=<i> feature=<f> num_bin=<n> gains=<bits...> winner=<bits>`) and per-tree (`PTREE name=<...>` followed by the Tree::to_string() text) — and emit ONE trivial placeholder fixture so the harness has a target. (The full learner transcription content — leaf-wise loop, subtraction trick, partition — lands in Plan 03; this task establishes the format + pipeline only.) Create `crates/oracle-harness/tests/learner_parity.rs` mirroring `kernel_parity.rs`: a `CARGO_MANIFEST_DIR`-rooted `tests/fixtures/learner/` path (NEVER the untracked `LightGBM/` tree), graceful SKIP when a golden is absent (pre-capture), the raw-bit parse helpers, and one stub test `learner_parity_scaffold` that loads the placeholder fixture (or SKIPs) — the per-split (`compare_exact_f64_bits` on per-bin gain arrays) and per-tree (`String` equality on `Tree::to_string()`) assertions are filled in by Plan 03/04. Update `REFERENCE_MANIFEST.md` with a "Learner Golden Set" section pinning the fixture set + capture config placeholders (D-04 row/col, D-03 g/h source — to be finalized in Plan 03/04).
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test learner_parity</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Some("learner-capture")' xtask/src/main.rs` == 1 (subcommand wired)
    - `xtask/cpp/learner_capture.cpp` exists and contains the header-only transcription rationale doc + the PSPLIT and PTREE record-format docs
    - `crates/oracle-harness/tests/learner_parity.rs` exists; `cargo test -p oracle-harness --test learner_parity` exits 0 and SKIPs (or passes the scaffold) WITHOUT referencing any path under `LightGBM/`
    - `grep -c 'LightGBM/' crates/oracle-harness/tests/learner_parity.rs` == 0 (untracked tree never referenced)
    - `REFERENCE_MANIFEST.md` contains a "Learner Golden Set" section
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>The learner-capture pipeline + learner_parity replay harness exist with the per-split/per-tree record formats defined; the harness SKIPs gracefully (failing end-to-end test in place) and never touches the untracked LightGBM/ tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `TreeLearnerError` boundary | g/h slices, bin indices, num_leaves, sum_hessian cross from the (eventual) caller into the learner; must be validated to typed errors, never panic |
| capture toolchain → committed golden | the C++-transcribed emitter writes raw-bit fixtures; absent/malformed golden must SKIP, never silently pass; `LightGBM/` must never be `git add`-ed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering / DoS | TreeLearnerError boundary | mitigate | `InvalidLeafHessian` rejects non-positive/NaN sum_hessian before any `cnt_factor` division; `LengthMismatch`/`BinIndexOutOfRange` typed before any kernel launch (Phase-4 V5 precedent); `Compute(#[from])` wraps, never re-derives |
| T-05-02-02 | Information disclosure (silent wrong answer) | learner_parity harness | mitigate | graceful SKIP when golden absent; never silently pass; assertion is bit-exact when present |
| T-05-02-03 | Tampering | git tracking of LightGBM/ | accept→mitigate | harness paths are `CARGO_MANIFEST_DIR`-rooted under `tests/fixtures/`; a grep gate asserts no `LightGBM/` reference; `.gitignore` already excludes the untracked tree (memory: lightgbm-ref-tree-untracked) |
| T-05-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages (RESEARCH Package Legitimacy Audit: zero new dependency surface); only an in-workspace path crate is added |
</threat_model>

<verification>
- `cargo build -p lgbm-treelearner` and `cargo test -p lgbm-treelearner` exit 0.
- `cargo test -p lgbm-model` exits 0 (Tree::split unit test green; Phase-3 tests unaffected).
- `cargo test -p oracle-harness --test learner_parity` exits 0 (SKIP or scaffold pass).
- `cargo test --workspace` exits 0; CMP-01 containment guard still green.
</verification>

<success_criteria>
- New `lgbm-treelearner` workspace member compiles with a `thiserror` boundary and reuses `lgbm_compute::gain::SplitInfo` (no duplicate).
- `Tree::split` grows a node + 2 leaves with the C++ rewiring; serialized form byte-stable.
- `split_gt` tie-break (gain, then smaller feature, -1→i32::MAX) is unit-proven.
- `learner-capture` + `learner_parity.rs` scaffolding exists with per-split/per-tree record formats; harness SKIPs gracefully and never touches `LightGBM/`.
</success_criteria>

<output>
Create `.planning/phases/05-tree-learner-split-finding/05-02-SUMMARY.md` when done.
</output>
