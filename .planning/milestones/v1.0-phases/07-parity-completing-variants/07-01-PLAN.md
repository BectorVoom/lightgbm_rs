---
phase: 07-parity-completing-variants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - xtask/src/main.rs
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/oracle-harness/tests/fixtures/determinism/.gitkeep
  - .planning/phases/07-parity-completing-variants/07-D05-DECISION.md
  - .planning/REFERENCE_MANIFEST.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "The bagged-subset split-gain knife-edge (DEF-06-01 + regression_l1+bagging) has a recorded root-cause and a SETTLED D-05 outcome branch before any bagging-dependent wave starts"
    - "A real-lib_lightgbm-4.6 FP trace of binary_bag1_es0_bfa1 tree-0 subset histogram (per-bin sum_gradient/sum_hessian) and per-split current_gain/min_gain_shift is captured or capture-gated"
    - "The Rust subset histogram + gain is compared cell-for-cell against the captured trace, localizing the divergence to bin-order vs init-score-timing vs genuine f32 accumulation"
    - "If a faithful fix exists, regression_l1+bagging is un-deferred (the BoostingError::UnsupportedConfig typed-reject removed) and DEF-06-01 cleared; otherwise a bounded known-divergence with a hard struct_divergent<=1 cap is documented"
  artifacts:
    - path: ".planning/phases/07-parity-completing-variants/07-D05-DECISION.md"
      provides: "The recorded D-05 branch decision (faithful-fix vs bounded-cap) with the cell-for-cell evidence"
    - path: "xtask/src/main.rs"
      provides: "A new subset-FP-trace capture subcommand"
  key_links:
    - from: "xtask subset-trace subcommand"
      to: "crates/oracle-harness/tests/boosting_parity.rs"
      via: "committed determinism fixture replayed in a diagnostic test"
      pattern: "fixtures/determinism"
---

<objective>
Wave 0 (D-05): settle the bagged-subset split-gain determinism BEFORE any bagging-dependent wave (GOSS W4, RF W6) builds on it, and before deciding whether to un-defer `regression_l1 + bagging`. This is a diagnostic + decision plan, not a feature plan — it owns no requirement ID. It carries the Phase-5 real-binary FP-trace technique forward to root-cause the DEF-06-01 leaf-structure flip (`binary_bag1_es0_bfa1` tree 0 → rust 2 vs cpp 4 leaves; `regression_l1_bag1_es0_bfa0` tree 0 → rust:0.0 vs cpp:11.0).

Purpose: every downstream bagging variant (GOSS/RF must bag; quantile/mape renew on bagged subsets) inherits a SETTLED answer instead of an open question (D-05, D-02 step 1).
Output: a new subset-FP-trace xtask subcommand, a diagnostic test in boosting_parity, and `07-D05-DECISION.md` recording the branch decision with cell-for-cell evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/deferred-items.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-06-SUMMARY.md
@.planning/codebase/CONCERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Subset-FP-trace xtask capture subcommand + determinism fixture scaffold</name>
  <files>xtask/src/main.rs, crates/oracle-harness/tests/fixtures/determinism/.gitkeep, .planning/REFERENCE_MANIFEST.md</files>
  <read_first>
    - xtask/src/main.rs (current capture subcommands — model the new one on `boosting_oracle_capture` registered at main.rs:115 and the version-pinned pattern main.rs:84-97 that asserts `lightgbm==4.6.0` before emitting)
    - crates/oracle-harness/tests/boosting_parity.rs (the corpus-builder + cell_builder(objective,bfa) cross-product boosting_parity.rs:44-176, and the read_golden skip-pass boosting_parity.rs:316-330)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Pitfall 1" (the C++ subset-build path: tmp_subset_->CopySubrow then ConstructHistogram, goss.hpp:58-59 / bagging.hpp:120-121; cnt_factor = num_data/sum_hessian, per-bin cnt = RoundInt(hess*cnt_factor), feature_histogram.hpp:872-873,1122; gain compare current_gain <= min_gain_shift at feature_histogram.hpp:1169,1281,562,643)
    - .planning/STATE.md (Phase-5 05-09 real-binary FP-trace technique that closed the 2-ULP; Phase-6 06-05 bagging_rands_ build-once fix)
  </read_first>
  <action>
    Add an `xtask` subcommand (e.g. `subset-determinism-capture`) that trains the `binary_bag1_es0_bfa1` cell AND the `regression_l1_bag1_es0_bfa0` cell on real `lightgbm==4.6.0` with the pinned deterministic config (`deterministic=true force_row_wise=true num_threads=1`, fixed seed, `bagging_fraction=0.7 bagging_freq=1`), version-asserting the wheel FIRST (mirror main.rs:84-97 — a wrong version must never emit a golden). Emit a tree-0 FP trace fixture into `crates/oracle-harness/tests/fixtures/determinism/`: per-bin subset histogram `sum_gradient`/`sum_hessian` and per-candidate-split `current_gain`/`min_gain_shift` at `%.17g`, plus the resulting leaf count. Add a `.gitkeep` so the dir is committed. Record the new fixtures + capture command in `.planning/REFERENCE_MANIFEST.md`. The capture itself is gated by the wheel (human checkpoint Task 3); this task only lands the emitter + scaffold and must compile + leave the workspace green.
  </action>
  <verify>
    <automated>cargo build -p xtask</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p xtask` succeeds with the new `subset-determinism-capture` subcommand registered in xtask/src/main.rs
    - The subcommand asserts the installed `lightgbm` version == 4.6.0 before any training (grep: the version-assert string is present in the new subcommand body)
    - `crates/oracle-harness/tests/fixtures/determinism/.gitkeep` exists and the dir is committed
    - `.planning/REFERENCE_MANIFEST.md` lists the new determinism fixtures + capture command
    - `cargo test --workspace` remains GREEN (no fixture enforced yet — skip-pass)
  </acceptance_criteria>
  <done>The subset-FP-trace emitter compiles, is version-pinned, and the determinism fixture dir + manifest entry exist; workspace green.</done>
</task>

<task type="auto">
  <name>Task 2: Diagnostic test + cell-for-cell Rust-vs-trace comparison + branch localization</name>
  <files>crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - crates/oracle-harness/tests/boosting_parity.rs (assert_scores/assert_gradients helpers boosting_parity.rs:199-301, read_golden skip-pass boosting_parity.rs:316-330)
    - crates/lgbm-treelearner/src/learner.rs (the subset path learner.rs:305-336 that re-gathers f.bins[r] per in-bag row and folds f32 grad/hess in in-bag order; per_bin_gains cnt_factor at learner.rs:1090-1093)
    - crates/lgbm-boosting/src/sample_strategy.rs (BaggingSampleStrategy::bagging draw order + the build-once bagging_rands_ pattern :113-118,187-216)
    - crates/oracle-harness/tests/comparator.rs (compare_exact_f64_bits, compare_within, ORACLE_TOL)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Pitfall 1 → How to investigate" (steps 1-4: capture trace, compare cell-for-cell, localize bin-order vs init-score-timing vs genuine f32, branch per D-05)
  </read_first>
  <action>
    Add a diagnostic test `subset_determinism_diagnostic` in boosting_parity.rs that, when the determinism fixture is present, drives the Rust subset histogram + per-split gain for `binary_bag1_es0_bfa1` tree 0 and compares cell-for-cell against the captured trace via `compare_exact_f64_bits` (with a localizing breakdown: assert the per-bin subset `sum_hessian` first, then `cnt_factor`, then `current_gain`/`min_gain_shift`, then the leaf count). Use the skip-pass `read_golden` shape so the test is green until the fixture is captured. The test's purpose is to LOCALIZE: which of (a) in-bag row fold ORDER vs C++ CopySubrow order, (b) boost_from_average init-score application timing, (c) genuine f32 histogram accumulation is the first cell to diverge. Do NOT weaken any existing assertion; this is a new diagnostic alongside the existing matrix.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test boosting_parity subset_determinism_diagnostic</automated>
  </verify>
  <acceptance_criteria>
    - `subset_determinism_diagnostic` exists in boosting_parity.rs and skip-passes cleanly when the determinism fixture is absent (no panic, no false green that hides a missing fixture — uses the read_golden Option shape)
    - The test asserts in localizing order: per-bin subset sum_hessian → cnt_factor → current_gain/min_gain_shift → leaf count (each a distinct assertion so the FIRST divergent cell is identifiable)
    - `cargo test --workspace` GREEN; no existing boosting_parity assertion changed
  </acceptance_criteria>
  <done>The diagnostic test exists, localizes the divergence cell-for-cell, and skip-passes until capture; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Capture the real-binary determinism trace + record the D-05 branch decision</name>
  <what-built>
    The version-pinned `subset-determinism-capture` xtask subcommand (Task 1) and the localizing `subset_determinism_diagnostic` test (Task 2). Capture is human-gated because `lightgbm==4.6.0` is not installed here (RESEARCH Environment / A5), exactly the Phase-5/6 posture.
  </what-built>
  <how-to-verify>
    1. Install the pinned reference: `pip install lightgbm==4.6.0` (or build lib_lightgbm 4.6 CPU-only single-thread from source per the Phase-5 05-09 technique if a finer FP trace is needed — `external_libs` are fetchable, memory: lightgbm-ref-tree-untracked; never `git add LightGBM/`).
    2. Run the capture: `cargo run -p xtask -- subset-determinism-capture` — confirm it asserts version 4.6.0 and writes the fixtures under `crates/oracle-harness/tests/fixtures/determinism/`.
    3. Re-run to confirm byte-idempotent regen (no diff on second run).
    4. Run `cargo test -p oracle-harness --test boosting_parity subset_determinism_diagnostic` and read the localizing output — note the FIRST divergent cell (sum_hessian / cnt_factor / gain / leaf count).
    5. Based on the localized cell, decide the D-05 branch and write `.planning/phases/07-parity-completing-variants/07-D05-DECISION.md`:
       - **Faithful-fix branch** (divergence is fold-ORDER or init-score-timing, not pure f32): record the fix, then in this same plan remove the `BoostingError::UnsupportedConfig` typed-reject for `regression_l1 + bagging` from `lgbm-boosting` (06-06 Task 2b), re-point the 4 `regression_l1_bag1_*` matrix cells from asserting the typed error to asserting parity, and clear DEF-06-01 in `06.../deferred-items.md`.
       - **Bounded-cap branch** (genuine f32 accumulation, no faithful fix): document the bounded known-divergence and keep the hard `struct_divergent <= 1` cap (carry the Phase-6 posture) so a GROWING divergence still fails as a regression.
  </how-to-verify>
  <resume-signal>Type "approved: faithful-fix" or "approved: bounded-cap" (with the localized first-divergent-cell noted), or describe issues. The chosen branch is then applied as the closing edit of this plan.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a diagnostic/decision plan over the offline numerical library and the read-only C++ reference. No network, auth, session, PII, or untrusted-input surface is introduced. The only inputs are developer-supplied config and the committed deterministic fixtures.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-SC | Tampering | `pip install lightgbm==4.6.0` (capture step) | mitigate | Version-asserted in the xtask subcommand before any golden is emitted (mirror main.rs:84-97); capture is a blocking-human checkpoint; `LightGBM/` never git-added |

No other security-relevant surface: this plan adds an emitter + a diagnostic test and records a decision; it parses no untrusted input.
</threat_model>

<verification>
- `cargo build -p xtask` succeeds (new subcommand)
- `cargo test --workspace` GREEN (diagnostic skip-passes until capture)
- After capture (Task 3): the trace is byte-idempotent and `07-D05-DECISION.md` records the branch with the localized first-divergent cell
- If faithful-fix: the 4 `regression_l1_bag1_*` cells assert parity (not the typed error) and DEF-06-01 is cleared
</verification>

<success_criteria>
The D-05 determinism posture is SETTLED and recorded before W4 (GOSS) / W6 (RF) start: either regression_l1+bagging is un-deferred with a faithful fix and DEF-06-01 cleared, or a bounded known-divergence with a hard struct_divergent<=1 cap is documented. The diagnostic test localizes any future bagged-subset divergence cell-for-cell.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-01)

New symbols/files created by this plan:
- `xtask/src/main.rs` — new `subset-determinism-capture` subcommand (function + clap registration)
- `crates/oracle-harness/tests/boosting_parity.rs` — new `subset_determinism_diagnostic` test fn
- `crates/oracle-harness/tests/fixtures/determinism/` — new fixture dir (`.gitkeep` + captured `*_subset_trace.txt`)
- `.planning/phases/07-parity-completing-variants/07-D05-DECISION.md` — D-05 branch decision record
- `.planning/REFERENCE_MANIFEST.md` — extended with determinism capture command + fixtures
- (faithful-fix branch only) removal of `BoostingError::UnsupportedConfig` reject for `regression_l1 + bagging` in `crates/lgbm-boosting`
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-01-SUMMARY.md` when done
</output>
