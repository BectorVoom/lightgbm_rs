---
phase: 07-parity-completing-variants
plan: 10
type: execute
wave: 10
depends_on: ["07-08"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-model/src/tree.rs
  - crates/lgbm-model/src/predict.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/predict_parity.rs
  - crates/oracle-harness/tests/fixtures/predict_modes/.gitkeep
autonomous: false
requirements: ["PRD-04", "PRD-05"]
must_haves:
  truths:
    - "A user can request predict_contrib (TreeSHAP) and get per-feature contributions whose sum plus the expected-value base equals the raw margin, matching real lib_lightgbm 4.6"
    - "TreeSHAP recurses the node structure using leaf_count/internal_count/leaf_weight cover, over numeric, categorical, and multiclass trees"
    - "Prediction early stopping (pred_early_stop/_freq/_margin) evaluates the running accumulator every freq iters and stops on margin, matching the reference iters-evaluated count + final score"
  artifacts:
    - path: "crates/lgbm-model/src/predict.rs"
      provides: "TreeSHAP predict_contrib + pred-early-stop driver"
      contains: "predict_contrib"
    - path: "crates/oracle-harness/tests/predict_parity.rs"
      provides: "new predict-mode parity test file (contrib + early_stop), capture-gated"
  key_links:
    - from: "crates/lgbm-model/src/predict.rs (predict_contrib)"
      to: "crates/lgbm-model/src/tree.rs (leaf_count/internal_count/leaf_weight cover + ExpectedValue)"
      via: "TreeSHAP path-weighting by cover"
      pattern: "leaf_count|internal_count|ExpectedValue"
---

<objective>
W9 (PRD-04 + PRD-05): add the two prediction modes — TreeSHAP feature contributions (`predict_contrib`) and prediction early stopping (`pred_early_stop`/`_freq`/`_margin`). Vertical slice: Config params (mostly present) → TreeSHAP recursion + ExpectedValue (PRD-04) + early-stop accumulator hook (PRD-05) → builder → real-binary parity (sum(contrib)+base == raw; iters-evaluated + final score).

Purpose: prediction-mode additions on the locked predict path (D-02 step 6); TreeSHAP is one of the two genuinely-new algorithms (no analog). Runs after 07-08 (shares tree.rs; SHAP must cover categorical trees too).
Output: predict_contrib + pred-early-stop wired with a new predict_parity.rs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TreeSHAP predict_contrib (ExpectedValue + path recursion over cover) + ExpectedValue</name>
  <files>crates/lgbm-model/src/tree.rs, crates/lgbm-model/src/predict.rs</files>
  <read_first>
    - crates/lgbm-model/src/predict.rs (predict_row_transformed :328+; the per-row f64 accumulator; the check_cols V5 gate :42; the driver wrapper pattern :75-129)
    - crates/lgbm-model/src/tree.rs (the parallel-array node structure left_child/right_child/split_feature/threshold; get_leaf/predict :212-237; the cover fields leaf_count/internal_count/leaf_weight :81-89 — already present; categorical_decision :180-198 for cat trees; find_in_bitset :143-150)
    - C++ refs (authoritative): LightGBM/include/LightGBM/tree.h:668-727 (PredictContrib: output[num_features]+=ExpectedValue(); if num_leaves>1 build unique_path_data of size max_path_len*(max_path_len+1)/2, call TreeSHAP(feature_values, output, 0,0, unique_path_data, 1,1,-1)); the TreeSHAP recursion + PathElement (the genuinely-new algorithm — port directly), ExpectedValue weights leaves by cover
  </read_first>
  <behavior>
    - ExpectedValue: cover-weighted average of leaf outputs (weights from leaf_count/internal_count/leaf_weight)
    - predict_contrib: output[num_features] += ExpectedValue(); then TreeSHAP recursion fills output[0..num_features] per-feature contributions; INVARIANT sum(output[0..num_features]) + output[num_features] == raw margin
    - works over numeric, categorical (uses find_in_bitset on the path), and multiclass (per-class stride) trees
    - max_path_len = max_depth+1; unique_path buffer size max_path_len*(max_path_len+1)/2
  </behavior>
  <action>
    Add `ExpectedValue` to Tree (cover-weighted leaf average using leaf_count/internal_count/leaf_weight per tree.h ExpectedValue). Port the TreeSHAP recursion + PathElement into predict.rs::predict_contrib per `<behavior>` from tree.h:668-727 (this is net-new — no Rust analog; model the driver wrapper on predict.rs:75-129 with the check_cols V5 gate). Handle numeric, categorical (find_in_bitset on the split path), and multiclass (per-class stride) trees. Assert the load-bearing invariant in a unit test: sum(per-feature contrib) + ExpectedValue base == raw margin for a known small tree.
  </action>
  <verify>
    <automated>cargo test -p lgbm-model</automated>
  </verify>
  <acceptance_criteria>
    - `Tree::expected_value` (cover-weighted) + `predict.rs::predict_contrib` (TreeSHAP recursion) exist (grep)
    - the SHAP invariant sum(contrib[0..num_features]) + contrib[num_features] == raw margin holds on a known tree (unit test, within f64 epsilon)
    - predict_contrib works on a categorical tree (uses find_in_bitset) and a multiclass tree (per-class stride) (unit tests)
    - `cargo test -p lgbm-model` GREEN
  </acceptance_criteria>
  <done>TreeSHAP predict_contrib + ExpectedValue produce cover-weighted contributions whose sum + base equals the raw margin across numeric/categorical/multiclass trees.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prediction early stopping (pred_early_stop/_freq/_margin) accumulator hook + config</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-model/src/predict.rs</files>
  <read_first>
    - crates/lgbm-model/src/predict.rs (predict_raw_mat_range accumulation loop :89-129 — the per-row f64 accumulator pred-early-stop hooks into)
    - crates/lgbm-core/src/config/set.rs (re-grep pred_early_stop/pred_early_stop_freq/pred_early_stop_margin — RESEARCH marks ADD(verify))
    - C++ refs (authoritative): LightGBM/src/application/predictor.hpp + prediction_early_stop.cpp (the margin check every pred_early_stop_freq iters on the running accumulator); config.h:871 pred_early_stop, :876 pred_early_stop_freq (10), :881 pred_early_stop_margin (10.0)
  </read_first>
  <behavior>
    - every pred_early_stop_freq accumulated iterations, evaluate the running per-row score against pred_early_stop_margin; if the margin condition is met, stop accumulating further trees for that row
    - returns the same final score + an iters-evaluated count matching the reference
  </behavior>
  <action>
    Re-grep set.rs FIRST; add missing params with config.h values: pred_early_stop (false), pred_early_stop_freq (10), pred_early_stop_margin (10.0). Add the early-stop margin check to the predict accumulation loop (predict.rs:89-129) per `<behavior>` from prediction_early_stop.cpp: evaluate the running accumulator every freq iters, stop on margin. Keep the default (pred_early_stop=false) path byte-identical to the existing predict (regression-test the existing predict goldens stay green).
  </action>
  <verify>
    <automated>cargo test -p lgbm-model && cargo test -p lgbm-core</automated>
  </verify>
  <acceptance_criteria>
    - pred_early_stop/_freq/_margin resolve with config.h defaults (unit test)
    - the early-stop margin check fires every freq iters on the running accumulator (unit test)
    - pred_early_stop=false leaves the existing predict path byte-identical (existing predict goldens still green)
    - `cargo test -p lgbm-model`, `cargo test -p lgbm-core` GREEN
  </acceptance_criteria>
  <done>Prediction early stopping hooks the accumulator with the margin check; the default path is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Builder + predict-mode capture + new predict_parity.rs (capture-gated)</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/predict_parity.rs, crates/oracle-harness/tests/fixtures/predict_modes/.gitkeep</files>
  <read_first>
    - crates/lgbm/src/builder.rs (bool setters reg_sqrt/boost_from_average :108-124 — pred_early_stop bool setter; freq/margin numeric setters)
    - xtask/src/main.rs (model-capture / boosting capture; version-pin :84-97 — capture real-binary predict_contrib + pred-early-stop outputs)
    - crates/oracle-harness/tests/boosting_parity.rs (capture-gated read_golden :316-330 shape to mirror)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Oracle Axis Matrix → Prediction modes" (SHAP: numeric × categorical × multiclass tree, per-feature contrib + ExpectedValue base, sum+base==raw; pred early stop: freq∈{10,1} × margin∈{10.0,2.0}, iters-evaluated count + final score)
  </read_first>
  <action>
    Add builder setters: `predict_contrib`-mode selection + `pred_early_stop(bool)`/`pred_early_stop_freq`/`pred_early_stop_margin`. Create the NEW `crates/oracle-harness/tests/predict_parity.rs`, modeled on the capture-gated shape. Extend the capture xtask to emit real-binary predict_contrib outputs (numeric/categorical/multiclass tree) + pred-early-stop outputs (freq×margin: iters-evaluated count + final score); version-pin 4.6.0. Add capture-gated `contrib_*` and `early_stop_*` cells comparing via compare_within(ORACLE_TOL); the contrib cells additionally assert the sum+base==raw invariant. Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test predict_parity</automated>
  </verify>
  <acceptance_criteria>
    - builder setters predict_contrib/pred_early_stop/_freq/_margin route into Config (test)
    - `crates/oracle-harness/tests/predict_parity.rs` exists with `contrib_*` (numeric/cat/multiclass) + `early_stop_*` (freq×margin) cells; each skip-passes absent the golden
    - contrib cells assert sum+base==raw
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>Builder + predict-mode capture + predict_parity.rs cells are in place; cells skip-pass; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Capture the predict-mode real-binary goldens</name>
  <what-built>TreeSHAP predict_contrib (Task 1), pred-early-stop (Task 2), builder/capture/predict_parity.rs (Task 3). Human-gated capture (wheel absent).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the predict-mode capture subcommand; confirm version-assert + fixtures (contrib for numeric/cat/multiclass + early-stop freq×margin) under `crates/oracle-harness/tests/fixtures/predict_modes/`.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test predict_parity` — confirm cells flip to GREEN within ORACLE_TOL; contrib cells satisfy sum+base==raw.
  </how-to-verify>
  <resume-signal>Type "approved" once contrib + early-stop cells are green, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline prediction over a loaded model + developer input matrices. The model is produced/loaded via the Phase-3-validated path; input matrices cross the crate boundary as typed slices.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-01 | Tampering | predict_contrib with mismatched column count / OOB feature index | mitigate | check_cols V5 gate (predict.rs:42) before recursion; bounds-check feature indices |
| T-07-10-02 | DoS | TreeSHAP unique_path buffer sizing on a deep tree | accept | buffer sized from max_depth+1 (bounded by config max_depth CHECK); no untrusted depth — model depth is from the trained/loaded model, itself bounds-validated in Phase-3 |
| T-07-10-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-model`, `cargo test -p lgbm-core` GREEN
- `cargo test --workspace` GREEN (predict_parity skip-passes until capture; existing predict goldens unregressed)
- After capture: contrib (sum+base==raw) + early-stop cells green within ORACLE_TOL
</verification>

<success_criteria>
SHAP/predict_contrib (PRD-04) over numeric/categorical/multiclass trees with sum+base==raw, and prediction early stopping (PRD-05) with matching iters-evaluated + final score, both match real lib_lightgbm 4.6.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-10)

- `Tree::expected_value` (cover-weighted) (crates/lgbm-model/src/tree.rs)
- `predict.rs::predict_contrib` (TreeSHAP recursion + PathElement) + pred-early-stop accumulator hook (crates/lgbm-model/src/predict.rs)
- Config params pred_early_stop/pred_early_stop_freq/pred_early_stop_margin (lgbm-core/src/config/set.rs)
- Builder setters predict_contrib/pred_early_stop/_freq/_margin (crates/lgbm/src/builder.rs)
- `crates/oracle-harness/tests/predict_parity.rs` (new file) — `contrib_*`/`early_stop_*` cells + `tests/fixtures/predict_modes/`
- xtask predict-mode capture subcommand
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-10-SUMMARY.md` when done
</output>
