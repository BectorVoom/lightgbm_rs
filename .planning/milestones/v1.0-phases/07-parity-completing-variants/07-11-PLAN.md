---
phase: 07-parity-completing-variants
plan: 11
type: execute
wave: 11
depends_on: ["07-08"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-core/src/config/alias.rs
  - crates/lgbm-treelearner/src/learner.rs
  - crates/lgbm-treelearner/src/monotone_constraints.rs
  - crates/lgbm-treelearner/src/cost_effective_gradient_boosting.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/learner_parity.rs
  - crates/oracle-harness/tests/fixtures/constraints/.gitkeep
autonomous: false
requirements: ["ADV-01", "ADV-02", "ADV-03", "ADV-04", "ADV-05"]
must_haves:
  truths:
    - "Monotone constraints (basic/intermediate/advanced + monotone_penalty) constrain split selection matching real lib_lightgbm 4.6"
    - "Interaction constraints restrict the allowed-feature set per node; forced splits/bins drive the forced node feature/threshold from JSON; extra-trees picks randomized thresholds via extra_seed; CEGB penalizes per-split gain"
    - "All five constraints are additive gates on the per-feature loop / cross-feature argmax; the numeric-spine and categorical goldens stay green"
  artifacts:
    - path: "crates/lgbm-treelearner/src/monotone_constraints.rs"
      provides: "monotone basic/intermediate/advanced + monotone_penalty gate"
      contains: "monotone"
    - path: "crates/lgbm-treelearner/src/cost_effective_gradient_boosting.rs"
      provides: "CEGB penalized gain"
      contains: "cegb"
  key_links:
    - from: "crates/lgbm-treelearner/src/learner.rs (per-feature loop / split_gt)"
      to: "monotone / interaction / forced / extra / cegb gates"
      via: "additive constraint gates on split selection"
      pattern: "monotone|interaction|forced|extra|cegb"
---

<objective>
W10 (ADV-01..05): add the advanced learner-side constraints — monotone (ADV-01), interaction (ADV-02), forced splits/bins (ADV-03), extra trees (ADV-04), CEGB (ADV-05) — as additive gates on the per-feature loop / cross-feature argmax in the serial learner. Each constraint alters WHICH split is chosen; all are additive above the numeric + categorical split paths.

Purpose: advanced constraints are leaf nodes in the DAG (nothing depends on them) and reorder by risk (D-02 step 7). Runs after 07-08 (shares learner.rs). Each constraint crosses its OWN params only (NOT loop axes), per D-04.
Output: 5 constraints wired with per-axis parity + the extra-trees RNG-replay candidate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Monotone (ADV-01) + interaction (ADV-02) + extra-trees (ADV-04) gates on the per-feature loop</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-treelearner/src/learner.rs, crates/lgbm-treelearner/src/monotone_constraints.rs</files>
  <read_first>
    - crates/lgbm-treelearner/src/learner.rs (the per-feature loop + cross-feature argmax :1004-1061; the used_features ColSampler gate :1008-1012 — interaction reuses this; split_gt direction check — monotone alters this; the count-reconstruction at :1090-1093)
    - crates/lgbm-treelearner/src/col_sampler.rs (the per-node feature mask pattern interaction mirrors)
    - crates/lgbm-core/src/config/set.rs + alias.rs (re-grep monotone_constraints/monotone_constraints_method/monotone_penalty/extra_trees/extra_seed/interaction_constraints — RESEARCH: monotone_constraints_method/monotone_penalty/extra_trees present; others ADD)
    - C++ refs (authoritative): LightGBM/src/treelearner/monotone_constraints.hpp + serial_tree_learner.cpp (basic/intermediate/advanced + monotone_penalty; GetSplitGains already has the monotone_constraint arg + left_output/right_output direction check at feature_histogram.hpp:788-790); serial_tree_learner.cpp feature-allowed gate for interaction_constraints (interaction_constraints_vector in config :1153); the USE_RAND template branch in FindBestThresholdSequentially (feature_histogram.hpp:895,1151, categorical :187,268) for extra-trees randomized thresholds (meta.rand.next_int per feature)
  </read_first>
  <behavior>
    - monotone: monotone_constraints_method ∈ {basic, intermediate, advanced}; the constraint vector (+1/-1/0 per feature) + monotone_penalty alter split_gt / the left_output<=right_output (or >=) direction check; reject a split that violates the constraint
    - interaction: interaction_constraints restrict the allowed-feature set per node (reuse the used_features mask gate at :1008-1012)
    - extra-trees: extra_trees=true picks a RANDOM threshold per feature via meta.rand.next_int (extra_seed), not the best — the USE_RAND branch; RNG-replay candidate
  </behavior>
  <action>
    Re-grep set.rs/alias.rs FIRST; add missing params with config.h values: interaction_constraints (vector), extra_seed (6), and any monotone aliases. Add a new module monotone_constraints.rs porting the basic/intermediate/advanced constraint + monotone_penalty per monotone_constraints.hpp, hooking the split_gt / left_output/right_output direction check (feature_histogram.hpp:788-790). Add the interaction-allowed gate to the per-feature loop reusing the used_features mask (learner.rs:1008-1012). Add the extra-trees randomized-threshold branch (meta.rand.next_int per feature, extra_seed) mirroring the USE_RAND branch. Each gate is ADDITIVE — the unconstrained numeric + categorical paths stay byte-identical when the constraint is inactive.
  </action>
  <verify>
    <automated>cargo test -p lgbm-treelearner && cargo test -p oracle-harness --test learner_parity spine_real</automated>
  </verify>
  <acceptance_criteria>
    - monotone basic/intermediate/advanced + monotone_penalty gate exists and rejects constraint-violating splits (unit test on a +1 constraint forcing left_output<=right_output)
    - interaction constraints restrict the per-node allowed-feature set (unit test)
    - extra-trees picks a random threshold via extra_seed (unit test; RNG-replay candidate fixture)
    - with all constraints inactive, spine_real replays BIT-EXACT (no-regression)
    - `cargo test -p lgbm-treelearner` GREEN
  </acceptance_criteria>
  <done>Monotone + interaction + extra-trees are additive split-selection gates; the unconstrained spine stays bit-exact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Forced splits/bins (ADV-03, JSON) + CEGB (ADV-05) penalized gain</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-core/src/config/alias.rs, crates/lgbm-treelearner/src/learner.rs, crates/lgbm-treelearner/src/cost_effective_gradient_boosting.rs</files>
  <read_first>
    - crates/lgbm-treelearner/src/learner.rs (the per-feature loop + split selection — ForceSplits hooks before the argmax; CEGB penalizes the gain in the argmax)
    - crates/lgbm-core/src/config/set.rs (re-grep forced_splits_filename/cegb_tradeoff/cegb_penalty_split/cegb_penalty_feature_lazy/cegb_penalty_feature_coupled — RESEARCH marks ADD)
    - C++ refs (authoritative): LightGBM/src/treelearner/serial_tree_learner.cpp ForceSplits + GatherInfoForThreshold* (feature_histogram.hpp:474); LightGBM/src/treelearner/cost_effective_gradient_boosting.hpp (cegb_tradeoff penalties: penalty_split, penalty_feature_coupled/_lazy); config.h:541 forced_splits_filename (alias fs/forced_splits), :557 cegb_tradeoff, :561 cegb_penalty_split, :567/:573 feature penalties
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Package Legitimacy Audit" (forced-splits JSON: hand-roll the small schema or reuse a workspace dep — verify before adding; if a new crate is proposed, run the Package Legitimacy Gate)
  </read_first>
  <behavior>
    - forced splits: parse the forced-splits JSON (single forced split, nested left/right); drive the forced node feature/threshold via GatherInfoForThreshold; validate the schema + feature/threshold ranges (typed error on malformed)
    - CEGB: penalize the per-split gain by cegb_tradeoff*(penalty_split + feature coupled/lazy penalties); the penalized gain feeds the argmax
  </behavior>
  <action>
    Re-grep set.rs FIRST; add params with config.h values: forced_splits_filename (alias fs/forced_splits), cegb_tradeoff (1.0, >=0), cegb_penalty_split (0.0, >=0), cegb_penalty_feature_lazy ([]), cegb_penalty_feature_coupled ([]). For forced splits (ADV-03): add a forced-splits JSON parser — hand-roll the small schema (the C++ uses vendored json11) OR reuse serde_json ONLY if already a workspace dep (verify via `cargo tree`/Cargo.toml first; if a NEW crate is needed, STOP and surface it for the Package Legitimacy Gate — do NOT add silently). Wire ForceSplits into learner.rs before the argmax, validating the schema + feature/threshold ranges as typed errors. For CEGB (ADV-05): add cost_effective_gradient_boosting.rs porting the penalized gain per cegb_*.hpp; subtract the penalty in the argmax gain comparison.
  </action>
  <verify>
    <automated>cargo test -p lgbm-treelearner && cargo test -p lgbm-core</automated>
  </verify>
  <acceptance_criteria>
    - forced_splits_filename (+ aliases) + cegb_* params resolve with config.h defaults (unit test)
    - a forced-splits JSON drives the forced node feature/threshold (unit test); a malformed JSON returns a typed error (no panic)
    - CEGB penalizes the per-split gain by cegb_tradeoff*penalty (unit test: a higher penalty changes the chosen split)
    - NO new external crate added without the Package Legitimacy Gate (grep Cargo.toml diff)
    - `cargo test -p lgbm-treelearner`, `cargo test -p lgbm-core` GREEN
  </acceptance_criteria>
  <done>Forced splits (validated JSON) + CEGB penalized gain are additive; malformed JSON is typed-rejected; no un-gated dependency added.</done>
</task>

<task type="auto">
  <name>Task 3: Builder + constraints capture + capture-gated learner_parity cells (per-axis) + no-regression</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/learner_parity.rs, crates/oracle-harness/tests/fixtures/constraints/.gitkeep</files>
  <read_first>
    - crates/lgbm/src/builder.rs (chained + bool setters)
    - xtask/src/main.rs (learner/boosting capture; version-pin :84-97)
    - crates/oracle-harness/tests/learner_parity.rs (the assert helpers + the D-06 spine_real/mfb_pos_real/growth_path_subtract gates — must stay green)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Oracle Axis Matrix → Monotone / Other advanced" (monotone: method∈{basic,intermediate,advanced} × penalty∈{0,5} × constraint{+1,-1,mixed}; interaction: {one group, two groups}; forced: {single, nested}; extra: extra_seed∈{6,9} RNG-replay; CEGB: cegb_tradeoff∈{1.0,0.5} × penalties)
  </read_first>
  <action>
    Add builder setters: monotone_constraints/monotone_constraints_method/monotone_penalty/interaction_constraints/forced_splits_filename/extra_trees(bool)/extra_seed/cegb_tradeoff/cegb_penalty_split/cegb_penalty_feature_lazy/cegb_penalty_feature_coupled. Extend the xtask capture to emit per-axis goldens for each constraint (monotone method×penalty×constraint-vector; interaction allowed-set per node; forced node threshold/feature + GatherInfoForThreshold gain; extra-trees random threshold per feature; CEGB penalized gain per split); version-pin 4.6.0. Add capture-gated cells in learner_parity.rs per constraint axis + the extra-trees RNG-replay golden. CONFIRM the D-06 spine_real/mfb_pos_real/growth_path_subtract + the 07-08 categorical goldens stay green. Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - builder setters for all 5 constraints route into Config (test)
    - learner_parity.rs has per-axis cells for monotone/interaction/forced/extra/cegb + the extra-trees RNG-replay; each skip-passes absent the golden
    - D-06 no-regression: spine_real/mfb_pos_real/growth_path_subtract + categorical goldens GREEN
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>Builder + constraints capture + per-axis cells + extra-trees RNG-replay are in place; spine + categorical goldens unregressed; cells skip-pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Capture the constraints real-binary goldens (per-axis + extra-trees RNG-replay)</name>
  <what-built>Monotone/interaction/extra-trees (Task 1), forced/CEGB (Task 2), builder/capture/per-axis cells (Task 3). Human-gated capture (wheel absent).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the constraints capture subcommand; confirm version-assert + per-axis fixtures + the extra-trees RNG-replay under `crates/oracle-harness/tests/fixtures/constraints/`.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test learner_parity` — confirm the per-axis cells + extra-trees RNG-replay flip to GREEN.
    5. Confirm spine_real/mfb_pos_real/growth_path_subtract + categorical goldens STILL green.
  </how-to-verify>
  <resume-signal>Type "approved" once constraint cells + extra-trees RNG-replay are green AND spine/categorical goldens confirmed unregressed, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline split-selection constraints over developer config. **ADV-03 forced splits parses untrusted JSON (`forced_splits_filename`)** — a real Tampering surface that crosses the crate boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Tampering/DoS | untrusted forced-splits JSON (malformed schema, OOB feature/threshold, deeply nested) | mitigate | validate the JSON schema + feature/threshold ranges before use; typed error on malformed; bounded nesting depth |
| T-07-11-02 | Tampering | monotone_constraints / cegb penalty vectors with wrong length vs num_features | mitigate | length-validate constraint/penalty vectors against num_features (typed error) |
| T-07-11-SC | Tampering | a NEW JSON crate for ADV-03 | mitigate | hand-roll or reuse an existing workspace dep; ANY new crate STOPS for the Package Legitimacy Gate (slopcheck + registry verify + blocking-human) before install |
| T-07-11-SC2 | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-treelearner`, `cargo test -p lgbm-core` GREEN
- D-06: spine_real/mfb_pos_real/growth_path_subtract + categorical goldens GREEN
- `cargo test --workspace` GREEN (constraint cells skip-pass until capture)
- After capture: per-axis + extra-trees RNG-replay green
</verification>

<success_criteria>
Monotone (ADV-01), interaction (ADV-02), forced splits/bins (ADV-03), extra trees (ADV-04), and CEGB (ADV-05) each reproduce the C++ behavior per their own axes; forced-splits JSON is validated/typed-rejected when malformed; the numeric-spine + categorical goldens stay green.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-11)

- `crates/lgbm-treelearner/src/monotone_constraints.rs` (new) — basic/intermediate/advanced + monotone_penalty gate
- `crates/lgbm-treelearner/src/cost_effective_gradient_boosting.rs` (new) — CEGB penalized gain
- interaction-allowed gate + extra-trees randomized-threshold branch + ForceSplits hook in crates/lgbm-treelearner/src/learner.rs
- forced-splits JSON parser (hand-rolled or existing workspace dep)
- Config params interaction_constraints/extra_seed/forced_splits_filename(+aliases)/cegb_tradeoff/cegb_penalty_split/cegb_penalty_feature_lazy/cegb_penalty_feature_coupled (lgbm-core/src/config/set.rs, alias.rs)
- Builder setters for all 5 constraints (crates/lgbm/src/builder.rs)
- per-axis learner_parity cells + extra-trees RNG-replay golden + `tests/fixtures/constraints/`
- xtask constraints capture subcommand
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-11-SUMMARY.md` when done
</output>
