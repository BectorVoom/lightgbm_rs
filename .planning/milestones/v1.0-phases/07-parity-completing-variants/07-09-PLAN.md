---
phase: 07-parity-completing-variants
plan: 09
type: execute
wave: 9
depends_on: ["07-04", "07-08"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-core/src/config/alias.rs
  - crates/lgbm-metric/src/dcg_calculator.rs
  - crates/lgbm-metric/src/rank.rs
  - crates/lgbm-metric/src/lib.rs
  - crates/lgbm-objective/src/rank.rs
  - crates/lgbm-objective/src/lib.rs
  - crates/lgbm-boosting/src/sample_strategy.rs
  - crates/lgbm-boosting/src/objective.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/rank_parity.rs
  - crates/oracle-harness/tests/fixtures/rank/.gitkeep
autonomous: false
requirements: ["OBJ-06", "MET-04", "bagging_by_query"]
must_haves:
  truths:
    - "A user can train lambdarank/rank_xendcg with query boundaries and evaluate ndcg/map per-query within parity of real lib_lightgbm 4.6"
    - "DCGCalculator initializes its gain/discount tables ONCE and is shared by OBJ-06 and MET-04"
    - "bagging_by_query samples QUERIES (not rows) via the per-block Random and expands to row ranges; the typed BaggingByQueryDeferred reject is removed; a dedicated RNG-replay golden proves sampled query indices + expanded rows bit-exact"
  artifacts:
    - path: "crates/lgbm-metric/src/dcg_calculator.rs"
      provides: "DCGCalculator static gain/discount tables (init-once), shared by rank objective + metric"
      contains: "DcgCalculator"
    - path: "crates/lgbm-objective/src/rank.rs"
      provides: "lambdarank/rank_xendcg pairwise lambdas + sigmoid table + objective_seed"
      contains: "lambdarank"
    - path: "crates/oracle-harness/tests/rank_parity.rs"
      provides: "new per-query ranking parity test file + RNG-replay cells"
  key_links:
    - from: "crates/lgbm-objective/src/rank.rs"
      to: "crates/lgbm-metric/src/dcg_calculator.rs"
      via: "shared DCGCalculator discounts/gains"
      pattern: "DcgCalculator|dcg"
    - from: "crates/lgbm-boosting/src/sample_strategy.rs (bagging_by_query branch)"
      to: "crates/lgbm-dataset::metadata::query_boundaries"
      via: "query-grouped sampling expanded to sampled_query_boundaries"
      pattern: "query_boundaries|by_query"
---

<objective>
W8 (OBJ-06 + MET-04 + bagging_by_query): land the ranking stack as one coherent group sharing query infrastructure (D-02 step 5, D-03). Ranking objectives (lambdarank/rank_xendcg) + ranking metrics (ndcg/map) + the shared DCGCalculator + query-grouped bagging all land together. Vertical slice: Config params → DCGCalculator + rank objective/metric math → bagging_by_query branch (remove the Phase-6 deferral reject) → builder → real-binary per-query parity + two RNG-replay goldens (by_query, rank_xendcg objective_seed).

Purpose: ranking couples objectives + metrics + DCGCalculator + query boundaries + the one query-grouped bagging path; shared infra lands together. Depends on the metric seam (07-04) and runs after the learner re-open (07-08); the sample_strategy.rs edit follows 07-05 GOSS.
Output: ranking objectives + ndcg/map + bagging_by_query wired end-to-end with per-query parity + RNG-replay goldens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DCGCalculator (init-once tables) + lambdarank/rank_xendcg objective + ndcg/map metric + config</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-core/src/config/alias.rs, crates/lgbm-metric/src/dcg_calculator.rs, crates/lgbm-metric/src/rank.rs, crates/lgbm-metric/src/lib.rs, crates/lgbm-objective/src/rank.rs, crates/lgbm-objective/src/lib.rs, crates/lgbm-boosting/src/objective.rs</files>
  <read_first>
    - crates/lgbm-objective/src/multiclass.rs (MulticlassSoftmax :55-205 — the strided per-group dispatch + construction-captures-metadata :76 that ranking mirrors over query_boundaries instead of class strides)
    - crates/lgbm-objective/src/binary.rs (sigmoid f64→f32 :38-96 — the lambda sigmoid table models on this)
    - crates/lgbm-metric/src/regression.rs (enum Metric + parse + factor_to_bigger_better + eval ordered fold :27-111 — ndcg/map add arms; per-query iterate query_boundaries, factor +1)
    - crates/lgbm-dataset/src/metadata.rs (query_boundaries :58 — already prefix-summed, present since DAT-06)
    - crates/lgbm-core/src/config/set.rs + alias.rs (re-grep objective_seed/eval_at/lambdarank_truncation_level/lambdarank_norm/label_gain/sigmoid — RESEARCH A4: verify defaults at config.h; eval_at aliases ndcg_eval_at/ndcg_at/map_eval_at/map_at)
    - C++ refs (authoritative): LightGBM/src/metric/dcg_calculator.cpp (DCGCalculator::Init static gain/discount tables — init ONCE, never recompute per query); LightGBM/src/objective/rank_objective.hpp:25-292 (lambdarank/rank_xendcg pairwise lambdas, DCG usage, objective_seed per-query random gamma for rank_xendcg, sigmoid lookup table _sigmoid_bins=1024 :269-292, lambdarank_truncation_level/lambdarank_norm/label_gain); LightGBM/src/metric/rank_metric.hpp (ndcg/map, eval_at/ndcg_eval_at, per-query); config.h:920 (objective_seed default 5), :1057-1060 (eval_at aliases)
  </read_first>
  <behavior>
    - DcgCalculator: Init builds the gain table (label_gain) + discount table ONCE; provides DCGAtK / CalDCGAtK per-query; shared by objective + metric
    - lambdarank: per-query pairwise lambdas over query_boundaries using the sigmoid lookup table (_sigmoid_bins=1024) + DCG delta; lambdarank_truncation_level/lambdarank_norm/label_gain
    - rank_xendcg: per-query random gamma draw seeded by objective_seed (RNG-replay)
    - ndcg/map metric: per-query DCG / average-precision averaged over queries; factor_to_bigger_better=+1; eval_at controls the @k cutoffs
    - label range guard: rank label>=0 (LabelOutOfRange typed)
  </behavior>
  <action>
    Re-grep set.rs/alias.rs FIRST; add missing params with config.h values (verify per A4): objective_seed (5), eval_at (+ aliases ndcg_eval_at/ndcg_at/map_eval_at/map_at), lambdarank_truncation_level, lambdarank_norm, label_gain, sigmoid (verify present). Create `crates/lgbm-metric/src/dcg_calculator.rs` porting DCGCalculator::Init (gain + discount tables built ONCE — do NOT recompute per query). Create `crates/lgbm-objective/src/rank.rs` with lambdarank + rank_xendcg mirroring multiclass.rs's per-group dispatch over query_boundaries (captured at construction) + binary.rs's sigmoid table; port pairwise lambdas + objective_seed per-query gamma per `<behavior>` from rank_objective.hpp. Create `crates/lgbm-metric/src/rank.rs` with ndcg/map per-query eval (factor +1) using the DcgCalculator. Add the rank label>=0 typed guard. Wire BoostObjective rank variants + dispatch. Re-export in both lib.rs files.
  </action>
  <verify>
    <automated>cargo test -p lgbm-metric && cargo test -p lgbm-objective && cargo test -p lgbm-core</automated>
  </verify>
  <acceptance_criteria>
    - `DcgCalculator` exists in lgbm-metric and builds its tables once (grep: init-once, no per-query recompute) and is consumed by BOTH lgbm-objective rank + lgbm-metric rank
    - `lambdarank`/`rank_xendcg` objectives + `ndcg`/`map` metrics parse with config_auto.cpp aliases (grep)
    - ndcg/map `factor_to_bigger_better()` == +1 (unit test)
    - objective_seed + eval_at (+ aliases) resolve with config.h defaults (unit test)
    - a negative rank label returns a typed LabelOutOfRange error (unit test)
    - `cargo test -p lgbm-metric`, `cargo test -p lgbm-objective`, `cargo test -p lgbm-core` GREEN
  </acceptance_criteria>
  <done>DCGCalculator (init-once) is shared by lambdarank/rank_xendcg and ndcg/map; per-query math + objective_seed + eval_at wired; unit tests prove parse + sign + guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bagging_by_query branch (remove deferral reject) + query RNG-replay golden</name>
  <files>crates/lgbm-boosting/src/sample_strategy.rs, crates/lgbm-core/src/config/set.rs</files>
  <read_first>
    - crates/lgbm-boosting/src/sample_strategy.rs (BaggingSampleStrategy::bagging per-block RNG loop :113-118,187-216; the typed BaggingByQueryDeferred reject at :83-85 — REMOVE it; the bag_indices_match_rng_replay_golden test :272-340)
    - crates/lgbm-dataset/src/metadata.rs (query_boundaries :58)
    - C++ refs (authoritative): LightGBM/src/boosting/bagging.hpp:52-104 (the query path: BaggingHelper over num_queries_ then expand each in-bag query to its row range, build sampled_query_boundaries), :178-181 (per-block Random(bagging_seed+i) block 1024)
  </read_first>
  <behavior>
    - bagging_by_query=true: sample QUERIES (not rows) via the per-block Random(bagging_seed+i) block 1024 over num_queries_, then expand each in-bag query to its [query_boundaries[q], query_boundaries[q+1]) row range; build sampled_query_boundaries
    - build-once rands, advance across draws (same CRITICAL discipline as bagging)
    - golden bagging_by_query_seed{S}.txt asserts sampled query indices + expanded row indices + sampled_query_boundaries bit-exact
  </behavior>
  <action>
    Remove the typed `BoostingError::BaggingByQueryDeferred` reject (sample_strategy.rs:83-85 / the corresponding set.rs gate at set.rs:478-481). Add the query-grouped branch in BaggingSampleStrategy per `<behavior>` from bagging.hpp:52-104: BaggingHelper over num_queries_ using the build-once per-block rands, expand in-bag queries to row ranges, build sampled_query_boundaries. Add the RNG-replay golden `bagging_by_query_seed{S}.txt` (modeled on bag_indices_match_rng_replay_golden) asserting sampled query indices + expanded rows + sampled_query_boundaries bit-exact.
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting && cargo test -p oracle-harness --test rank_parity bagging_by_query_rng</automated>
  </verify>
  <acceptance_criteria>
    - the BaggingByQueryDeferred reject is GONE (grep: no BaggingByQueryDeferred remains)
    - bagging_by_query samples queries then expands to row ranges using the build-once rands (grep / unit test)
    - the bagging_by_query RNG-replay golden asserts query indices + expanded rows + sampled_query_boundaries bit-exact; skip-passes absent the fixture
    - `cargo test -p lgbm-boosting` GREEN
  </acceptance_criteria>
  <done>bagging_by_query is un-deferred and faithfully query-samples via the proven RNG; the RNG-replay golden is in place.</done>
</task>

<task type="auto">
  <name>Task 3: Builder + ranking corpus capture + new rank_parity.rs (per-query + RNG-replay, capture-gated)</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/rank_parity.rs, crates/oracle-harness/tests/fixtures/rank/.gitkeep</files>
  <read_first>
    - crates/lgbm/src/builder.rs (objective/metric/bagging setters — add objective_seed/eval_at/lambdarank_* + bagging_by_query setters)
    - xtask/src/main.rs (boosting_oracle_capture :115; version-pin :84-97 — the capture must build a QUERY corpus with group boundaries)
    - crates/oracle-harness/tests/boosting_parity.rs (capture-gated read_golden :316-330 + layered shape to mirror in rank_parity.rs)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Oracle Axis Matrix → Objectives → lambdarank/rank_xendcg" ({bag(by_query) on/off} × {es on/off}, bfa N/A; lambdarank_truncation_level/lambdarank_norm/label_gain/sigmoid; rank_xendcg objective_seed∈{5,7} RNG-replay)
  </read_first>
  <action>
    Add builder setters: objective_seed/eval_at/lambdarank_truncation_level/lambdarank_norm/label_gain/sigmoid + bagging_by_query(bool) + objective("lambdarank"/"rank_xendcg") + metric("ndcg"/"map"). Create the NEW `crates/oracle-harness/tests/rank_parity.rs` (VALIDATION Wave-0 gap), modeled on the capture-gated layered shape. Extend the capture xtask to train a QUERY corpus (group boundaries) on real lightgbm==4.6.0 for lambdarank/rank_xendcg across {bag(by_query)}×{es} + the objective-specific params, emitting per-query ndcg/map + per-row lambdas + final model; version-pin 4.6.0. Add capture-gated per-query parity cells + the rank_xendcg objective_seed RNG-replay golden. Document any horizon cap (rank uses sigmoid/exp — Pitfall 5). Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test rank_parity</automated>
  </verify>
  <acceptance_criteria>
    - builder setters for objective_seed/eval_at/lambdarank_*/sigmoid/bagging_by_query route into Config (test)
    - `crates/oracle-harness/tests/rank_parity.rs` exists with per-query parity cells + the rank_xendcg objective_seed RNG-replay; each skip-passes absent the golden
    - rank cells document any sigmoid/exp horizon cap (no weakened tolerance hiding a flip)
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>Builder + ranking corpus capture + rank_parity.rs per-query cells + RNG-replay are in place; cells skip-pass; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Capture the ranking real-binary goldens (per-query parity + RNG-replays)</name>
  <what-built>DCGCalculator + rank objective/metric (Task 1), bagging_by_query + its RNG-replay (Task 2), builder/capture/rank_parity.rs (Task 3). Human-gated capture (wheel absent).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the ranking corpus capture subcommand; confirm version-assert + the query-corpus fixtures (per-query ndcg/map, per-row lambdas, model, the bagging_by_query RNG-replay, the rank_xendcg objective_seed RNG-replay) under `crates/oracle-harness/tests/fixtures/rank/`.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test rank_parity` — confirm per-query parity + both RNG-replays flip to GREEN (bit-exact where the algorithm permits; ORACLE_TOL on sigmoid/exp horizon).
  </how-to-verify>
  <resume-signal>Type "approved" once ranking per-query cells + both RNG-replays are green, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline ranking objectives/metrics + query-grouped sampling over developer config + query boundaries. No network/auth/session/PII. RNG is the deterministic FND-01 LCG.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-01 | Tampering→NaN | non-monotonic / out-of-bounds query_boundaries fed to per-query iteration | mitigate | validate query_boundaries monotonic + in-bounds (typed error) before per-query iteration |
| T-07-09-02 | Tampering→NaN | out-of-range rank labels feeding DCG/sigmoid | mitigate | rank label>=0 LabelOutOfRange typed guard |
| T-07-09-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-metric`, `cargo test -p lgbm-objective`, `cargo test -p lgbm-boosting`, `cargo test -p lgbm-core` GREEN
- `cargo test --workspace` GREEN (rank_parity skip-passes until capture)
- After capture: per-query parity + bagging_by_query RNG-replay + rank_xendcg seed RNG-replay green
</verification>

<success_criteria>
lambdarank/rank_xendcg (OBJ-06) + ndcg/map (MET-04) match real lib_lightgbm 4.6 per-query with a shared init-once DCGCalculator; bagging_by_query is un-deferred and proven bit-exact via RNG-replay.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-09)

- `crates/lgbm-metric/src/dcg_calculator.rs` (new file) — `DcgCalculator` static tables
- `crates/lgbm-objective/src/rank.rs` (new file) — `lambdarank`/`rank_xendcg` + sigmoid table
- `crates/lgbm-metric/src/rank.rs` (new file) — `ndcg`/`map` per-query metrics
- BoostObjective rank variants (lgbm-boosting/src/objective.rs)
- bagging_by_query branch in sample_strategy.rs; removal of `BoostingError::BaggingByQueryDeferred`
- Config params objective_seed/eval_at(+aliases)/lambdarank_truncation_level/lambdarank_norm/label_gain/sigmoid (lgbm-core/src/config/set.rs, alias.rs)
- Builder setters objective_seed/eval_at/lambdarank_*/sigmoid/bagging_by_query (crates/lgbm/src/builder.rs)
- `crates/oracle-harness/tests/rank_parity.rs` (new file) + RNG-replay goldens `bagging_by_query_seed{S}.txt`, rank_xendcg seed golden + `tests/fixtures/rank/`
- xtask ranking corpus capture subcommand
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-09-SUMMARY.md` when done
</output>
