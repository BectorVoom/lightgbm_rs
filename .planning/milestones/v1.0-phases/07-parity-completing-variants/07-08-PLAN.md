---
phase: 07-parity-completing-variants
plan: 08
type: execute
wave: 8
depends_on: ["07-07"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-treelearner/src/learner.rs
  - crates/lgbm-treelearner/src/feature_histogram_categorical.rs
  - crates/lgbm-model/src/tree.rs
  - crates/lgbm-model/src/model_text.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/learner_parity.rs
  - crates/oracle-harness/tests/fixtures/categorical/.gitkeep
autonomous: false
requirements: ["TRL-06"]
must_haves:
  truths:
    - "The serial learner grows categorical splits (one-hot below max_cat_to_onehot, many-vs-many above) whose per-category gains, chosen category bitset, and decision_type match real lib_lightgbm 4.6"
    - "A grown categorical tree round-trips through model-text with the num_cat/cat_boundaries/cat_threshold lines and the ||-separated category set"
    - "HARD INVARIANT (D-06): spine_real.txt, mfb_pos_real.txt, and the growth-path/subtract goldens still replay BIT-EXACT after the categorical re-open"
  artifacts:
    - path: "crates/lgbm-treelearner/src/feature_histogram_categorical.rs"
      provides: "find_best_threshold_categorical mirroring feature_histogram.cpp:144-382"
      contains: "find_best_threshold_categorical"
    - path: "crates/lgbm-model/src/tree.rs"
      provides: "Tree::split_categorical constructor + grown-tree categorical model-text emit"
      contains: "split_categorical"
  key_links:
    - from: "crates/lgbm-treelearner/src/learner.rs (bin_type dispatch)"
      to: "crates/lgbm-treelearner/src/feature_histogram_categorical.rs"
      via: "Categorical branch routed at the TOP of the per-feature find-best loop; Numerical byte-untouched"
      pattern: "bin_type|Categorical"
---

<objective>
W7 (TRL-06, D-06/D-07): add categorical splits as an ADDITIVE branch in the bit-exact Phase-5 serial learner. The numeric spine stays byte-untouched and bit-exact (D-06 HARD INVARIANT). Vertical slice: Config params → bin_type dispatch + find_best_threshold_categorical (one-hot + many-vs-many gain math) → Tree::split_categorical + model-text emit → builder → real-binary categorical corpus parity + layered diagnostics + the no-regression numeric-spine gate.

Purpose: the ONLY Phase-7 item that re-opens the bit-exact learner; the highest-FP-risk net-new gain math. Runs after the spine is otherwise stable so the no-regression gate is meaningful (D-02 step 4).
Output: categorical split finding + node + model-text, validated against a real-binary categorical corpus, with the spine goldens proven bit-exact after the re-open.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
@.planning/phases/05-tree-learner-split-finding/05-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config params + bin_type dispatch + find_best_threshold_categorical (one-hot + many-vs-many) — numeric spine byte-untouched</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-treelearner/src/learner.rs, crates/lgbm-treelearner/src/feature_histogram_categorical.rs</files>
  <read_first>
    - crates/lgbm-treelearner/src/learner.rs (scan_leaf_histogram :974-1063 + per_bin_gains :1068-1200; the per-feature loop + cross-feature argmax :1004-1061; the ColSampler used_features gate :1008-1012; the count-reconstruction cnt_factor=num_data/(sum_hessian+2*eps), RoundInt(x)=(x+0.5f32 as f64) as i32 at :1090-1093 — reuse EXACTLY for the cat_smooth filter)
    - crates/lgbm-core/src/config/set.rs (re-grep min_data_per_group/max_cat_threshold/cat_l2/cat_smooth/max_cat_to_onehot — RESEARCH marks several ADD(verify); cat_smooth present)
    - C++ refs (authoritative): LightGBM/src/treelearner/serial_tree_learner.cpp:779-781 (is_numerical_split from bin_type — the dispatch hook), :807-843 (categorical else branch builds cat_bitset via Common::ConstructBitset, calls SplitCategorical + data_partition_->Split); LightGBM/src/treelearner/feature_histogram.cpp:144-382 (the cat gain algorithm: gain_shift uses ORIGINAL l2 for min_gain_to_split but per-category gain uses l2+=cat_l2 :163-168,248; use_onehot=num_bin<=max_cat_to_onehot :179; one-hot path :184-238; many-vs-many path :239-339: filter RoundInt(hess*cnt_factor)>=cat_smooth into sorted_idx, stable_sort by ctr=sum_grad/(sum_hess+cat_smooth) ascending :250-257, both directions :259-262, max_num_cat=min(max_cat_threshold,(used_bin+1)/2) :263-264, accumulate groups respecting min_data_per_group :276-313, best cat_threshold = first best_threshold+1 of sorted_idx fwd or sorted_idx[used_bin-1-i] bwd :364-378)
  </read_first>
  <behavior>
    - dispatch at the TOP of the per-feature loop: f.bin_type==Numerical -> existing byte-untouched backend.find_best_split (D-06); f.bin_type==Categorical -> find_best_threshold_categorical
    - one-hot (num_bin<=max_cat_to_onehot): each bin as one-vs-rest, gate min_data_in_leaf/min_sum_hessian_in_leaf, gain via the existing GetSplitGains, best -> cat_threshold=[best_bin+offset], num_cat_threshold=1
    - many-vs-many (else): cat_smooth filter via RoundInt(hess*cnt_factor)>=cat_smooth; l2+=cat_l2 (ONLY in per-category gain, not gain_shift); stable_sort ascending by ctr=g/(h+cat_smooth); both directions; max_num_cat cap; accumulate groups respecting min_data_per_group; best bitset
    - gain_shift (min_gain_to_split baseline) uses the ORIGINAL l2 (the deliberate asymmetry)
  </behavior>
  <action>
    Re-grep set.rs FIRST; add missing params with exact config.h values: min_data_per_group (100, >0), max_cat_threshold (32, >0), cat_l2 (10.0, >=0), max_cat_to_onehot (4, >0) — cat_smooth (10.0) likely present. Add a `bin_type` dispatch at the TOP of learner.rs's per-feature loop (mirror serial_tree_learner.cpp:779): Numerical routes to the EXISTING byte-untouched scan (D-06 — do not perturb loop bounds/offset/kEpsilon position), Categorical routes to a new `find_best_threshold_categorical` in the new module feature_histogram_categorical.rs. Port the one-hot + many-vs-many gain algorithm per `<behavior>` from feature_histogram.cpp:144-382, reusing the existing cnt_factor/RoundInt (learner.rs:1090-1093) for the cat_smooth filter and the existing GetSplitGains for the per-category gain. Keep the l2+=cat_l2 asymmetry (cat_l2 only in per-category gain, original l2 in gain_shift).
  </action>
  <verify>
    <automated>cargo test -p lgbm-treelearner && cargo test -p oracle-harness --test learner_parity spine_real mfb_pos</automated>
  </verify>
  <acceptance_criteria>
    - `find_best_threshold_categorical` exists in feature_histogram_categorical.rs with both one-hot and many-vs-many paths (grep)
    - the bin_type dispatch routes Numerical to the existing scan UNCHANGED (grep: the numeric find_best_split call site is byte-identical to pre-change — diff shows only the added dispatch wrapper)
    - **D-06 HARD INVARIANT: `spine_real.txt` and `mfb_pos_real.txt` replay BIT-EXACT** (`cargo test -p oracle-harness --test learner_parity spine_real mfb_pos` GREEN, no tolerance)
    - min_data_per_group/max_cat_threshold/cat_l2/max_cat_to_onehot resolve with config.h defaults (unit test)
    - the l2+=cat_l2 asymmetry is present (grep / unit test: gain_shift uses original l2)
    - `cargo test -p lgbm-treelearner` GREEN
  </acceptance_criteria>
  <done>Categorical split finding (one-hot + many-vs-many) is an additive bin_type branch; the numeric spine goldens replay bit-exact unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tree::split_categorical + grown-tree categorical model-text emit + round-trip</name>
  <files>crates/lgbm-model/src/tree.rs, crates/lgbm-model/src/model_text.rs</files>
  <read_first>
    - crates/lgbm-model/src/tree.rs (Tree::split numeric growth :276-350; the ALREADY-PRESENT categorical fields num_cat/cat_boundaries/cat_threshold :64-93, categorical_decision :180-198, find_in_bitset :143-150; CATEGORICAL_MASK bit :46; the to_string num_cat>0 branch :363-381 emitting cat_boundaries=/cat_threshold=; the parse round-trip :533-553; format_g17/format_g6 :43)
    - C++ refs (authoritative): LightGBM/include/LightGBM/tree.h:86 (SplitCategorical), LightGBM/src/io/tree.cpp:62-95 (SplitCategorical sets the categorical decision bit, threshold_in_bin_=threshold_=num_cat_, pushes cat_threshold_ real + cat_threshold_inner_ bin, appends cat_boundaries_/cat_boundaries_inner_, ++num_cat_), :346,371-378 (num_cat=/cat_boundaries=/cat_threshold= model-text lines), :550-557 (CategoricalDecisionIfElse ||-separated set)
  </read_first>
  <behavior>
    - Tree::split_categorical(leaf, feature, cat_bitset_real, cat_bitset_inner, ...) mirrors Tree::split but sets CATEGORICAL_MASK in decision_type, threshold=num_cat as f64, pushes the bitset into cat_threshold + offset into cat_boundaries, num_cat+=1
    - to_string emits num_cat/cat_boundaries/cat_threshold for a GROWN cat tree (the loaded-tree path already works from Phase-3)
    - round-trip: grow -> to_string -> parse -> identical tree (incl. ||-separated category set in the if-else)
  </behavior>
  <action>
    Add a `Tree::split_categorical` constructor mirroring Tree::split (:276-350) but setting the CATEGORICAL_MASK bit, threshold=num_cat as f64, pushing the real + inner bitsets into cat_threshold/cat_threshold_inner and the offsets into cat_boundaries/cat_boundaries_inner, ++num_cat — per tree.cpp:62-95. Wire the GROWN categorical tree (from Task 1's split finder) through the existing to_string num_cat>0 branch (:363-381) and confirm the parse round-trip (:533-553) reproduces it. Use format_g17/format_g6, never a fresh formatter. The prediction side (categorical_decision/find_in_bitset) already works from Phase-3 DAT-08 — do not touch it.
  </action>
  <verify>
    <automated>cargo test -p lgbm-model</automated>
  </verify>
  <acceptance_criteria>
    - `Tree::split_categorical` exists and sets the CATEGORICAL_MASK + threshold=num_cat (grep)
    - a grown categorical tree round-trips grow->to_string->parse->identical (unit test, byte-stable model-text)
    - the model-text uses format_g17/format_g6 (grep: no fresh formatter)
    - `cargo test -p lgbm-model` GREEN
  </acceptance_criteria>
  <done>Tree::split_categorical grows categorical nodes that round-trip byte-stably through the existing categorical model-text path.</done>
</task>

<task type="auto">
  <name>Task 3: Builder + categorical corpus capture + layered learner_parity cells + no-regression gate</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/learner_parity.rs, crates/oracle-harness/tests/fixtures/categorical/.gitkeep</files>
  <read_first>
    - crates/lgbm/src/builder.rs (chained setters — add cat param setters)
    - xtask/src/main.rs (learner-oracle-capture / boosting capture; version-pin :84-97 — model the cat corpus capture on the Phase-5 learner-oracle-capture that produced spine_real/mfb_pos_real)
    - crates/oracle-harness/tests/learner_parity.rs (the assert_real_tree_parity byte-exact helper; spine_real/mfb_pos_real/growth_path_subtract gates — the D-06 must-not-regress goldens)
    - crates/lgbm-dataset (the bit-exact categorical binning path from Phase-2, reused for the cat corpus)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Oracle Axis Matrix → Categorical" (max_cat_to_onehot∈{4,1} × cat_smooth∈{10.0,0.0} × cat_l2∈{10.0,0.0} × min_data_per_group∈{100,1} × max_cat_threshold∈{32,2}; layered diagnostics: per-category gain array, chosen cat_threshold bitset, decision_type categorical bit, model-text || round-trip)
  </read_first>
  <action>
    Add builder setters cat_l2/min_data_per_group/max_cat_threshold/max_cat_to_onehot (cat_smooth likely present). Extend the xtask capture to train a SYNTHETIC categorical corpus on real lightgbm==4.6.0 (reusing the Phase-2 bit-exact categorical binning) across the axis matrix, emitting D-07 layered diagnostics: per-category gain arrays, chosen cat_threshold bitset, decision_type categorical bit, and the full categorical model-text. Add capture-gated `categorical_*` cells in learner_parity.rs asserting each diagnostic layer bit-exact. ALSO add (or confirm) an explicit no-regression assertion in this wave that spine_real/mfb_pos_real/growth_path_subtract are GREEN. Add the categorical fixture dir. Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - builder setters cat_l2/min_data_per_group/max_cat_threshold/max_cat_to_onehot route into Config (test)
    - learner_parity.rs has `categorical_*` cells across the axis matrix with the 4 layered diagnostics; each skip-passes absent the golden
    - **D-06 no-regression gate: `cargo test --workspace` GREEN with spine_real/mfb_pos_real/growth_path_subtract bit-exact**
    - `cargo build -p xtask` succeeds
  </acceptance_criteria>
  <done>The categorical corpus capture + layered diagnostics + builder setters are in place; the numeric spine no-regression gate is green; cells skip-pass until capture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Capture the categorical real-binary corpus (layered) + confirm no-regression</name>
  <what-built>Categorical split finding (Task 1), Tree::split_categorical + model-text (Task 2), builder/capture/layered cells + no-regression gate (Task 3). Human-gated capture (wheel absent).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the categorical corpus capture subcommand; confirm version-assert + the synthetic categorical corpus fixtures (per-category gains, cat_threshold bitset, decision_type, model-text) under `crates/oracle-harness/tests/fixtures/categorical/`.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test learner_parity categorical` — confirm the layered cells flip to GREEN bit-exact.
    5. Run `cargo test --workspace` and CONFIRM spine_real/mfb_pos_real/growth_path_subtract are STILL bit-exact (D-06).
  </how-to-verify>
  <resume-signal>Type "approved" once the categorical layered cells are green AND the numeric-spine goldens are confirmed still bit-exact, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline split finding over developer config + the binned (already-validated) categorical dataset, plus model-text load/save. The model-text parse path is a Tampering surface (already validated in Phase-3 tree.rs).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-01 | Tampering | malformed model-text (cat_threshold/cat_boundaries OOB) on load | mitigate | bounds-check cat_boundaries/cat_threshold before bitset index (tree.rs already validates from Phase-3; confirm the grown-tree emit path produces in-bounds offsets) |
| T-07-08-02 | Tampering | cat_l2/max_cat_threshold/min_data_per_group out of range | mitigate | config.h CHECK surfaced as typed Result |
| T-07-08-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-treelearner`, `cargo test -p lgbm-model` GREEN
- **D-06: `cargo test --workspace` GREEN with spine_real/mfb_pos_real/growth_path_subtract bit-exact after every categorical commit**
- After capture: categorical layered cells green bit-exact; byte-idempotent
</verification>

<success_criteria>
Categorical splits (TRL-06) produce matching category bitsets, per-category gains, decision_type, and model-text round-trip vs real lib_lightgbm 4.6; the numeric-spine real-binary goldens remain BIT-EXACT after the additive re-open (D-06 HARD INVARIANT).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-08)

- `crates/lgbm-treelearner/src/feature_histogram_categorical.rs` (new file) — `find_best_threshold_categorical` (one-hot + many-vs-many)
- bin_type dispatch in crates/lgbm-treelearner/src/learner.rs (Numerical byte-untouched)
- `Tree::split_categorical` constructor + grown-tree categorical model-text emit (crates/lgbm-model/src/tree.rs, model_text.rs)
- Config params min_data_per_group/max_cat_threshold/cat_l2/max_cat_to_onehot (lgbm-core/src/config/set.rs)
- Builder setters cat_l2/min_data_per_group/max_cat_threshold/max_cat_to_onehot (crates/lgbm/src/builder.rs)
- `categorical_*` layered cells (learner_parity.rs) + `tests/fixtures/categorical/`
- xtask categorical corpus capture subcommand
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-08-SUMMARY.md` when done
</output>
