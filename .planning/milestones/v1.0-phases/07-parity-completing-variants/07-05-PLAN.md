---
phase: 07-parity-completing-variants
plan: 05
type: execute
wave: 5
depends_on: ["07-01"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-boosting/src/sample_strategy.rs
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/oracle-harness/tests/fixtures/goss/.gitkeep
autonomous: false
requirements: ["BST-04"]
must_haves:
  truths:
    - "A user can select data_sample_strategy=goss and train a model whose sampled+amplified rows and final model match real lib_lightgbm 4.6"
    - "GOSS reuses the build-once / advance-across-draws bagging_rands_ block-1024 RNG; draw order is bit-exact in a dedicated RNG-replay golden"
    - "GOSS amplifies grad AND hess by (cnt-top_k)/other_k for kept non-top rows, using ArgMaxAtK (not a full sort) for the top-k threshold"
  artifacts:
    - path: "crates/lgbm-boosting/src/sample_strategy.rs"
      provides: "GossSampleStrategy mirroring goss.hpp sampling+amplification"
      contains: "Goss"
    - path: "crates/oracle-harness/tests/fixtures/goss/.gitkeep"
      provides: "goss RNG-replay + parity fixture dir"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs"
      to: "crates/lgbm-boosting/src/sample_strategy.rs::GossSampleStrategy"
      via: "data_sample_strategy==goss selection inside train_one_iter, before the learner"
      pattern: "goss"
---

<objective>
W4 (BST-04): add the GOSS sample strategy as a `BaggingSampleStrategy` sibling. Vertical slice: confirm Config params → GossSampleStrategy math (grad-magnitude sort + amplification) → Gbdt selection on `data_sample_strategy=goss` → builder → real-binary parity + a dedicated RNG-replay golden.

Purpose: GOSS subsamples (modifies grad/hess in place), so it depends on the 07-01 W0 determinism posture. Ordered in the boosting-variant chain (D-02 step 3).
Output: GossSampleStrategy wired end-to-end with an RNG-replay golden + layered parity cells.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
@.planning/phases/07-parity-completing-variants/07-D05-DECISION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GossSampleStrategy (grad-magnitude sort + ArgMaxAtK + amplification) + Gbdt selection + RNG-replay golden</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-boosting/src/sample_strategy.rs, crates/lgbm-boosting/src/gbdt.rs, crates/oracle-harness/tests/boosting_parity.rs, crates/oracle-harness/tests/fixtures/goss/.gitkeep</files>
  <read_first>
    - crates/lgbm-boosting/src/sample_strategy.rs (BaggingSampleStrategy: the build-once bagging_rands_ block array :113-118,187-216; the draw-every-row-in-order loop; the reference_bag + bag_indices_match_rng_replay_golden test :272-340 — GOSS reuses this exact RNG discipline)
    - crates/lgbm-boosting/src/gbdt.rs (train_one_iter :209+; sample-strategy selection point — GOSS runs INSIDE train_one_iter after get_gradients, before the learner, because IsHessianChange)
    - crates/lgbm-core/src/config/set.rs (re-grep top_rate/other_rate — RESEARCH notes present; verify defaults 0.2/0.1 + CHECK top+other<=1; the goss alias-expansion at set.rs:472-476)
    - C++ refs (authoritative): LightGBM/src/boosting/goss.hpp:33 (skip subsampling for iter<1/learning_rate), :87-89 (GOSS forbids bagging), :95-98 (per-block Random(bagging_seed+i) block 1024), :127-162 (Helper: top_k=max(1,cnt*top_rate), other_k=cnt*other_rate, ArgMaxAtK for threshold, per-row keep + amplify grad AND hess by (cnt-top_k)/other_k), :152 (prob=rest_need/rest_all running)
    - .planning/phases/07-parity-completing-variants/07-D05-DECISION.md (the settled subset-determinism posture GOSS inherits)
  </read_first>
  <behavior>
    - skip subsampling when iter < 1/learning_rate (returns full set, grad/hess unmodified)
    - top_k = max(1, (data_size_t)(cnt*top_rate)); other_k = (data_size_t)(cnt*other_rate)
    - threshold via ArgMaxAtK(top_k-1) over |grad*hess| (NOT a full sort — tie behavior differs)
    - per row in order: if |g*h| >= threshold keep (top); else if rands[block].next_float() < prob (running rest_need/rest_all) keep AND grad*=multiply, hess*=multiply where multiply=(cnt-top_k)/other_k
    - RNG-replay golden goss_sampled_seed{S}_top{T}_other{O}.txt asserts in-bag indices + which rows were amplified, bit-exact
  </behavior>
  <action>
    Verify/add top_rate (0.2) / other_rate (0.1, CHECK top+other<=1) in set.rs; confirm the goss alias-expansion (boosting==goss → gbdt + data_sample_strategy=goss) at set.rs:472-476. Add `GossSampleStrategy` in sample_strategy.rs reusing the build-once bagging_rands_ block-1024 array (do NOT re-seed per draw — STATE.md 06-05 CRITICAL fix). Port the Helper per the `<behavior>` block from goss.hpp:127-162: ArgMaxAtK top-k threshold, per-row keep/amplify with the running prob, amplify grad AND hess by multiply. Skip subsampling for iter<1/learning_rate. Select GossSampleStrategy in gbdt.rs::train_one_iter when data_sample_strategy==goss, running it after get_gradients and before the learner (IsHessianChange). Add the goss fixture dir + a dedicated RNG-replay golden `goss_sampled_seed{S}_top{T}_other{O}.txt` (modeled on bag_indices_match_rng_replay_golden) asserting in-bag indices + amplified rows bit-exact via compare_exact.
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting && cargo test -p oracle-harness --test boosting_parity goss_rng</automated>
  </verify>
  <acceptance_criteria>
    - `GossSampleStrategy` exists and reuses the build-once bagging_rands_ block array (grep: no per-draw re-seed)
    - threshold uses ArgMaxAtK semantics, not a full sort (grep / unit test on tie ordering)
    - amplification multiplies BOTH grad and hess by (cnt-top_k)/other_k (unit test)
    - subsampling is skipped for iter < 1/learning_rate (unit test)
    - the goss RNG-replay golden asserts in-bag indices + amplified rows bit-exact; skip-passes absent the fixture
    - `cargo test -p lgbm-boosting` GREEN
  </acceptance_criteria>
  <done>GossSampleStrategy faithfully samples+amplifies with the proven RNG discipline, is selected on data_sample_strategy=goss, and the RNG-replay golden is in place.</done>
</task>

<task type="auto">
  <name>Task 2: Builder wiring + capture emitter + capture-gated GOSS parity cells</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - crates/lgbm/src/builder.rs (objective/bagging_fraction setters :52-55,152-156 — add top_rate/other_rate/data_sample_strategy setters in this shape)
    - xtask/src/main.rs (boosting_oracle_capture :115; version-pin :84-97)
    - crates/oracle-harness/tests/boosting_parity.rs (cell_builder :44-176; layered asserts :199-301; read_golden :316-330)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Oracle Axis Matrix → Boosting variants" (GOSS: top_rate∈{0.2,0.1} × other_rate∈{0.1,0.05}, top+other<=0.5 for subset path; × {es on/off}; × {bfa on/off}; GOSS forbids bagging)
  </read_first>
  <action>
    Add builder setters `top_rate(f64)`, `other_rate(f64)`, `data_sample_strategy(&str)` (or `goss()` convenience) routing into params. Extend the capture xtask to emit layered L1-L5 goldens for GOSS across `top_rate×other_rate × {es} × {bfa}` (NO bagging axis — GOSS forbids it). Add capture-gated parity cells `goss_*` in boosting_parity.rs. The grad/hess golden must reflect the amplified values. Document any collapsed cell. Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test boosting_parity goss</automated>
  </verify>
  <acceptance_criteria>
    - builder setters `top_rate`/`other_rate`/`data_sample_strategy` route into Config (test)
    - boosting_parity.rs has `goss_*` cells over top_rate×other_rate×{es}×{bfa} (no bag axis); each skip-passes absent the golden
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>Builder setters + capture emitter cover the GOSS axis matrix; cells skip-pass; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Capture the GOSS real-binary goldens (parity + RNG-replay)</name>
  <what-built>GossSampleStrategy + RNG-replay golden (Task 1) + builder/capture/cells (Task 2). Human-gated capture (wheel absent).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the GOSS capture subcommand; confirm version-assert + fixtures (both the parity cells and the RNG-replay golden) written under `crates/oracle-harness/tests/fixtures/goss/`.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test boosting_parity goss` — confirm parity cells AND the RNG-replay golden flip to GREEN (in-bag indices bit-exact; amplified grad/hess bit-exact where the algorithm permits).
  </how-to-verify>
  <resume-signal>Type "approved" once captures are committed and GOSS cells + RNG-replay are green, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline sampling over developer config + grad/hess buffers. No network/auth/session/PII. RNG is the deterministic FND-01 LCG (never cryptographic).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | top_rate/other_rate out of range (top+other>1) | mitigate | config.h CHECK ([0,1], top+other<=1) surfaced as typed Result |
| T-07-05-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-boosting` GREEN
- `cargo test --workspace` GREEN (goss cells + RNG-replay skip-pass until capture)
- After capture: goss parity + RNG-replay green; byte-idempotent
</verification>

<success_criteria>
GOSS (BST-04) trains via data_sample_strategy=goss and matches real lib_lightgbm 4.6 across top_rate×other_rate×{es}×{bfa}; the RNG-replay golden proves sampled+amplified indices bit-exact.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-05)

- `GossSampleStrategy` struct (crates/lgbm-boosting/src/sample_strategy.rs)
- data_sample_strategy=goss selection branch in `Gbdt::train_one_iter` (crates/lgbm-boosting/src/gbdt.rs)
- Config params top_rate/other_rate confirmed/added (lgbm-core/src/config/set.rs)
- Builder setters `top_rate`/`other_rate`/`data_sample_strategy` (crates/lgbm/src/builder.rs)
- RNG-replay golden `goss_sampled_seed{S}_top{T}_other{O}.txt` + `goss_*` parity cells (boosting_parity.rs) + `tests/fixtures/goss/`
- xtask GOSS capture subcommand
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-05-SUMMARY.md` when done
</output>
