---
phase: 07-parity-completing-variants
plan: 07
type: execute
wave: 7
depends_on: ["07-01", "07-03", "07-06"]
files_modified:
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm-model/src/ensemble.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/oracle-harness/tests/fixtures/rf/.gitkeep
autonomous: false
requirements: ["BST-06"]
must_haves:
  truths:
    - "A user can select boosting=rf and train an averaged-tree model (no shrinkage accumulation, mandatory bagging) matching real lib_lightgbm 4.6"
    - "RF uses average_output + MultiplyScore running-average rescale and per-tree RenewTreeOutput with a label-pred residual getter"
    - "RF requires objective != null and rejects a config with neither bagging nor feature_fraction active"
  artifacts:
    - path: "crates/lgbm-boosting/src/gbdt.rs"
      provides: "BoostingVariant::Rf branch mirroring rf.hpp (averaged trees, mandatory bagging, no shrinkage)"
      contains: "Rf"
    - path: "crates/oracle-harness/tests/fixtures/rf/.gitkeep"
      provides: "rf parity fixture dir"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs (BoostingVariant::Rf)"
      to: "crates/lgbm-boosting/src/sample_strategy.rs::BaggingSampleStrategy"
      via: "mandatory bagging inherited from the existing bagging RNG golden"
      pattern: "bagging"
---

<objective>
W6 (BST-06): add Random Forest as a `BoostingVariant::Rf` branch — averaged (not accumulated) trees, mandatory bagging, no shrinkage accumulation. Vertical slice: RF branch math (MultiplyScore running average, per-tree renew) → CHECKs (objective!=null, bagging-or-feature_fraction) → builder → real-binary parity (inherits the bagging RNG golden).

Purpose: RF MUST bag, so it depends on the 07-01 W0 determinism posture and reuses the proven BaggingSampleStrategy; renew reuses the objective renew from 07-03; uses the BoostingVariant enum introduced in 07-06. Ordered after 07-06 (shared gbdt.rs/builder.rs/ensemble.rs).
Output: RF wired end-to-end with parity cells over mandatory-bagging × single/multiclass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
@.planning/phases/07-parity-completing-variants/07-D05-DECISION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BoostingVariant::Rf branch (averaged trees, MultiplyScore rescale, per-tree renew, CHECKs)</name>
  <files>crates/lgbm-boosting/src/gbdt.rs, crates/lgbm-model/src/ensemble.rs</files>
  <read_first>
    - crates/lgbm-boosting/src/gbdt.rs (the BoostingVariant enum + variant field from 07-06; train_one_iter :209+; the ScoreUpdater interaction)
    - crates/lgbm-boosting/src/score_updater.rs (the f64 ScoreUpdater — RF rescales via MultiplyScore-analog)
    - crates/lgbm-boosting/src/sample_strategy.rs (BaggingSampleStrategy — RF mandatory bagging reuses it; the D-13 bagging RNG golden)
    - crates/lgbm-model/src/ensemble.rs (average_output / per-tree accumulation — RF averages)
    - C++ refs (authoritative): LightGBM/src/boosting/rf.hpp:35-40 (CHECK bagging OR feature_fraction active), :90-109 (Boosting() re-derives grad/hess from a constant init-score buffer once), :91-93 (RF requires objective != null), :150-152 (RenewTreeOutput with residual_getter = label-pred), :157-159 (per-tree MultiplyScore(cur_tree_id, iter+num_init); UpdateScore; MultiplyScore(1/(iter+num_init+1)) running average); average_output_=true; shrinkage_rate_=1.0
  </read_first>
  <behavior>
    - RF: average_output=true, shrinkage_rate=1.0 (no shrinkage accumulation)
    - per iter per class: MultiplyScore(cur_tree_id, iter+num_init); UpdateScore; MultiplyScore(1/(iter+num_init+1)) — the running average
    - Boosting() re-derives grad/hess from a constant init-score buffer ONCE (not per accumulated score)
    - RenewTreeOutput residual_getter = label - pred (per-tree)
    - CHECK: objective != null -> typed error if absent; CHECK: bagging active OR feature_fraction<1 -> typed error otherwise
  </behavior>
  <action>
    Add the `BoostingVariant::Rf` branch in gbdt.rs::train_one_iter per `<behavior>` from rf.hpp:90-159: average_output + shrinkage 1.0, the per-tree MultiplyScore running-average rescale via the ScoreUpdater, the once-derived grad/hess from the constant init-score buffer, and per-tree RenewTreeOutput with the label-pred residual getter (reuse the objective renew machinery). Add the two CHECKs (objective!=null; bagging-or-feature_fraction active) as typed errors at the top of the RF path (before any tree grows), mirroring rf.hpp:35-40,91-93. Wire average_output into ensemble.rs predict so RF predictions average tree outputs. RF mandatory bagging reuses the existing BaggingSampleStrategy (no new RNG — inherits the D-13 bagging golden).
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting</automated>
  </verify>
  <acceptance_criteria>
    - `BoostingVariant::Rf` branch exists; selected via boosting=rf (grep)
    - RF uses shrinkage 1.0 + average_output (grep / unit test on the running-average rescale)
    - RF with no objective returns a typed error; RF with neither bagging nor feature_fraction<1 returns a typed error (unit tests)
    - RF predict averages tree outputs (integration test)
    - `cargo test -p lgbm-boosting` GREEN
  </acceptance_criteria>
  <done>BoostingVariant::Rf faithfully averages trees with mandatory bagging + per-tree renew + the two CHECKs; unit tests prove the rejects and the averaging.</done>
</task>

<task type="auto">
  <name>Task 2: Builder wiring + capture emitter + capture-gated RF parity cells</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/boosting_parity.rs, crates/oracle-harness/tests/fixtures/rf/.gitkeep</files>
  <read_first>
    - crates/lgbm/src/builder.rs (boosting/objective/bagging_fraction setters)
    - xtask/src/main.rs (boosting_oracle_capture :115; version-pin :84-97)
    - crates/oracle-harness/tests/boosting_parity.rs (cell_builder :44-176; layered asserts :199-301; read_golden :316-330)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Oracle Axis Matrix → Boosting variants → RF" (mandatory bagging bagging_fraction∈{0.7} × bagging_freq∈{1} OR feature_fraction<1; × {single vs multiclass}; inherits the bagging RNG golden)
    - .planning/phases/07-parity-completing-variants/07-D05-DECISION.md (RF averaged-tree leaf structure on bagged subsets inherits this posture)
  </read_first>
  <action>
    Add the builder `boosting("rf")` selection (and confirm bagging_fraction/bagging_freq/feature_fraction setters exist). Extend the capture xtask to emit layered goldens for RF over mandatory-bagging × {single-output vs multiclass}; version-pin 4.6.0. Add capture-gated `rf_*` parity cells. RF averaged-tree leaf structure on the bagged subset inherits the 07-01 D-05 posture (assert parity or bounded-cap accordingly; cite the decision in a comment). Add the rf fixture dir. Document any collapsed cell. Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test boosting_parity rf</automated>
  </verify>
  <acceptance_criteria>
    - builder `boosting("rf")` selects RF (test)
    - boosting_parity.rs has `rf_*` cells over mandatory-bagging × {single,multiclass}; each skip-passes absent the golden; bagged-subset cells cite the 07-01 D-05 decision
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>RF builder + capture emitter + cells cover mandatory-bagging × single/multiclass with the D-05 posture; cells skip-pass; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Capture the RF real-binary goldens</name>
  <what-built>BoostingVariant::Rf branch (Task 1) + builder/capture/cells (Task 2). Human-gated capture (wheel absent).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the RF capture subcommand; confirm version-assert + fixtures under `crates/oracle-harness/tests/fixtures/rf/`.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test boosting_parity rf` — confirm cells flip to GREEN (averaged trees within parity; leaf structure per the D-05 posture).
  </how-to-verify>
  <resume-signal>Type "approved" once captures are committed and RF cells are green, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline boosting variant over developer config + in-memory ensemble. No network/auth/session/PII. RNG is the deterministic FND-01 LCG (mandatory bagging reuses the proven BaggingSampleStrategy).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Tampering | RF config with no objective or no bagging/feature_fraction | mitigate | typed-error CHECKs at the top of the RF path (rf.hpp:35-40,91-93), before any tree grows |
| T-07-07-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-boosting` GREEN
- `cargo test --workspace` GREEN (rf cells skip-pass until capture)
- After capture: rf cells green per the D-05 posture; byte-idempotent
</verification>

<success_criteria>
RF (BST-06) trains via boosting=rf with averaged trees + mandatory bagging + no shrinkage, matches real lib_lightgbm 4.6 over mandatory-bagging × single/multiclass, and enforces the objective!=null + bagging-or-feature_fraction CHECKs.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-07)

- `BoostingVariant::Rf` branch (averaged trees, MultiplyScore rescale, per-tree renew, 2 CHECKs) in crates/lgbm-boosting/src/gbdt.rs
- average_output predict path in crates/lgbm-model/src/ensemble.rs
- Builder `boosting("rf")` selection (crates/lgbm/src/builder.rs)
- `rf_*` parity cells (boosting_parity.rs) + `tests/fixtures/rf/`
- xtask RF capture subcommand
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-07-SUMMARY.md` when done
</output>
