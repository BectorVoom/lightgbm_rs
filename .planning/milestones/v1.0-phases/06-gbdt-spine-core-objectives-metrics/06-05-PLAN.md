---
phase: 06-gbdt-spine-core-objectives-metrics
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - crates/lgbm-boosting/src/sample_strategy.rs
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm-boosting/src/score_updater.rs
  - crates/lgbm-boosting/src/early_stopping.rs
  - crates/lgbm-boosting/src/lib.rs
  - crates/lgbm-metric/src/lib.rs
  - crates/lgbm/src/builder.rs
  - crates/lgbm/src/booster.rs
  - xtask/py/boosting_oracle_capture.py
  - xtask/src/main.rs
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md
autonomous: true
requirements: [BST-03, BST-07, MET-02]
must_haves:
  truths:
    - "the bagging draw reproduces the exact C++ Random NextFloat per-row sequence: per-block Random(bagging_seed+i) at block size 1024, draw for EVERY row in order, in-bag appended left / OOB filled right, then OOB tail reversed"
    - "the full bag_data_indices array (in-bag asc ++ OOB desc) matches the C++ Random draw-sequence golden (RNG-replay, D-13 Option A)"
    - "OOB rows still get the tree prediction added each iteration (score update on out-of-bag rows)"
    - "bagging_by_query is an explicit, documented Phase-7 deferral (BST-03 scope note, 2026-06-07): Phase 6 ships pos/neg row bagging only and rejects bagging_by_query=true with a typed error rather than silently reducing scope"
    - "early stopping fires at the same iteration as the reference: factor_to_bigger_better*score vs best + min_delta, first_metric_only honored, best_iteration recorded"
    - "metric infrastructure: multi-metric lists, metric_freq cadence, is_provide_training_metric, training-metric eval match the reference"
    - "the full ~40-cell D-07 cross-product (5 objectives x {bagging} x {early_stop} x {bfa}) replays vs the real binary, with the one allowed collapse documented"
  artifacts:
    - path: "crates/lgbm-boosting/src/sample_strategy.rs"
      provides: "BaggingSampleStrategy (RNG draw + bag_data_indices)"
      contains: "next_float"
    - path: "crates/lgbm-boosting/src/early_stopping.rs"
      provides: "early-stopping decision (BST-07)"
      contains: "early_stopping"
    - path: "crates/oracle-harness/tests/fixtures/boosting/bag_indices_seed3_frac0.7.txt"
      provides: "RNG-replay bagged-index golden (D-13)"
      contains: ""
  key_links:
    - from: "crates/lgbm-boosting/src/sample_strategy.rs"
      to: "lgbm_core::Random"
      via: "per-block Random(bagging_seed+i).next_float() draw loop"
      pattern: "Random|next_float"
    - from: "crates/lgbm-boosting/src/gbdt.rs"
      to: "BaggingSampleStrategy + OOB score update"
      via: "Bagging(iter) then UpdateScore over in-bag + OOB rows"
      pattern: "bag_data_indices|oob"
    - from: "crates/lgbm-boosting/src/early_stopping.rs"
      to: "Booster.best_iteration / eval history"
      via: "factor_to_bigger_better metric comparison"
      pattern: "best_iteration"
---

<objective>
Add the final two axes (D-17 steps 3+4): bagging / row subsampling (BST-03, with the dedicated D-13 bagged-index RNG-replay golden) and early stopping (BST-07), plus the metric infrastructure (MET-02: multi-metric lists, `metric_freq`, `is_provide_training_metric`, training-metric eval). Then capture + replay the full ~40-cell D-07 cross-product (5 objectives × {bagging on/off} × {early_stop on/off} × {bfa on/off}) against the real `lib_lightgbm` 4.6 oracle, completing the maximal-fidelity validation matrix.

Scope note (BST-03, 2026-06-07 user decision): Phase 6 ships pos/neg ROW bagging only. `bagging_by_query` (the query-grouped draw) is an EXPLICIT, decision-backed deferral to Phase 7 — it only affects ranking/query objectives (OBJ-04/05/06), none of which exist in Phase 6 — and ships there alongside them (CONTEXT.md BST-03 scope note + Deferred Ideas). Phase 6 guards `bagging_by_query=false` and errors if set true; it does NOT silently drop the feature.

Purpose: Bagging is the single most RNG-order-sensitive path; D-13 makes it a dedicated golden so a wrong-but-similar bag cannot hide an RNG mismatch behind a near-matching model. Early stopping closes BST-07. The cross-product matrix is the phase's exhaustive end-to-end gate (D-07). After this plan, all 5 ROADMAP success criteria are demonstrated and all 10 requirement IDs are satisfied.
Output: BaggingSampleStrategy (RNG-replay golden) + OOB score update; early-stopping decision + Booster best_iteration/eval history; metric infrastructure; the ~40-cell matrix replaying; manifest documenting every cell + the one allowed collapse.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-CONTEXT.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-RESEARCH.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-PATTERNS.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BaggingSampleStrategy (RNG draw + OOB score update) + D-13 RNG-replay golden</name>
  <files>crates/lgbm-boosting/src/{sample_strategy.rs,gbdt.rs,score_updater.rs,lib.rs}, crates/lgbm-core/src/random.rs (read), xtask/src/main.rs, xtask/py/boosting_oracle_capture.py, crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - `crates/lgbm-core/src/random.rs` — `Random::new(seed)` + `next_float()` (:75, bit-exact FND-01) + the `Random::sample` per-element draw idiom (:87-120) to mirror; do NOT re-port the PRNG
    - `crates/lgbm-boosting/src/gbdt.rs` — the `Bagging(iter)` call site + `UpdateScore` to add OOB handling to
    - `crates/lgbm-boosting/src/score_updater.rs` — training-path add; OOB rows use the tree predict-side add
    - `xtask/py/` + any existing `rng_capture` / `rng_sequence.txt` fixture (FND-01 precedent) — the `Random.NextFloat` sequence dump format to reuse for the D-13 RNG-replay golden
    - `LightGBM/src/boosting/bagging.hpp` — `ResetSampleConfig` (:177-181 per-block `Random(bagging_seed+i)`, block 1024), when-fires (:33 `iter%bagging_freq==0 || need_re_bagging_`), `bag_data_cnt_` target (:161 `bagging_fraction*num_data` truncated), `BaggingHelper` (:230-246 the draw loop), `BalancedBaggingHelper` (:248-274 pos/neg)
    - `LightGBM/src/boosting/sample_strategy.h` — `bagging_rand_block_=1024` (:75)
    - `LightGBM/include/LightGBM/utils/threading.h` — one-buffer reverse (:152-155, OOB tail reversed)
    - `LightGBM/src/boosting/gbdt.cpp` — OOB score update (:499-509), valid updaters (:517-519), UpdateScore use_subset branch (:491-520)
    - 06-RESEARCH.md Pattern 3 + §Code Examples (bagging draw) + Pitfall 4 + §Validation Architecture D-13 Option A (RNG-replay, NOT a bag capture)
  </read_first>
  <behavior>
    - Test: per-block seeding constructs `ceil(num_data/1024)` `Random` instances seeded `bagging_seed+i`.
    - Test: the draw loop draws `next_float()` for EVERY row in order (incl. OOB), in-bag appended left / OOB filled from right, then the OOB tail `[bag_cnt..cnt)` is reversed → `bag_data_indices = in-bag asc ++ OOB desc`.
    - Test: bagging fires on iters 0, k, 2k, ... for `bagging_freq=k`; `bag_data_cnt = trunc(bagging_fraction*num_data)`.
    - Test: balanced pos/neg bagging draws against pos/neg fraction by `label>0` (verbatim loop).
    - Test (D-13): the full `bag_data_indices` array bit-matches a value derived from a committed C++ `Random.NextFloat` sequence golden.
    - Test: OOB rows receive the tree prediction added to their score each iteration (not skipped).
    - Test (explicit scope boundary): `bagging_by_query=true` is rejected with a typed `BoostingError` (deferred to Phase 7), NOT silently treated as row bagging; `bagging_by_query=false` (default) takes the pos/neg row-bagging path.
  </behavior>
  <action>
    Implement `BaggingSampleStrategy` in `sample_strategy.rs` mirroring `bagging.hpp` + `threading.h` verbatim (RESEARCH Pattern 3): `reset_sample_config` builds per-block `lgbm_core::Random::new(bagging_seed + i)` (block size 1024 const), `need_re_bagging` true once so iter 0 bags; `bagging(iter)` fires when `bag_data_cnt < num_data && iter % bagging_freq == 0 || need_re_bagging`; `bag_data_cnt = (bagging_fraction * num_data) as data_size_t` (truncate). The draw: `for i in 0..cnt { if bagging_rands[i/1024].next_float() < bagging_fraction { buf[left]=i; left+=1 } else { right-=1; buf[right]=i } }` then `buf[left..cnt].reverse()` (one-buffer OOB-tail reverse) — draw per row IN ORDER incl. OOB (Pitfall 4). Add `BalancedBaggingHelper` (pos/neg fraction by `label>0`, :248-274). REUSE `lgbm-core::Random` (Don't Hand-Roll — FND-01 bit-exact). In `gbdt.rs` wire `Bagging(iter)` before the tree loop, compact bagged g/h to the front per gbdt.cpp:388-396, and in `UpdateScore` handle the use_subset / OOB branches: when `bag_data_cnt < num_data`, in-bag rows via the train-path scatter + OOB rows via the tree predict-side add (gbdt.cpp:499-509) — OOB rows STILL get scored (Pitfall 4 / Anti-Pattern). For the D-13 golden: extend the xtask capture (or reuse the FND-01 `rng_capture`) to dump `ceil(num_data/1024)` `Random(bagging_seed+i)` first-`num_data` NextFloats to `bag_indices_seed<S>_frac<F>.txt`; the Rust test reproduces `BaggingHelper` over `lgbm-core::Random` and asserts the derived `bag_data_indices` against that committed sequence (Option A — no internal-bag capture needed; bit-exact via FND-01). If the executor judges Option A insufficient for a cell, STOP and flag a checkpoint:human-verify (Option B = source-built debug bag dump) rather than inventing a capture path. EXPLICIT SCOPE BOUNDARY — `bagging_by_query` (BST-03): per the 2026-06-07 user decision (CONTEXT.md BST-03 scope note + Deferred Ideas), the query-grouped draw (`num_sampled_queries`/`sampled_query_indices`, mirroring `gbdt.cpp:227` + the `bagging.hpp` query branch) is DEFERRED to Phase 7 and ships there alongside the Phase-7 ranking objectives (OBJ-04/05/06) — the only objectives it affects; Phase 6 ships none. Phase 6's BST-03 surface is `bagging_fraction`/`bagging_freq`/`bagging_seed` + pos/neg row bagging + RNG-replay parity (D-13). Implement this deferral EXPLICITLY, NOT as a silent reduction: (a) add a doc-comment in `sample_strategy.rs` AND at the `Bagging(iter)` call site in `gbdt.rs` marking the query-grouped path as an explicit Phase-7 deferral and citing the CONTEXT.md decision; (b) guard `bagging_by_query == false` for Phase 6 — if a caller sets `bagging_by_query=true`, return a typed `BoostingError` (or assert) that states it is deferred to Phase 7, rather than silently falling through to row bagging. The plan text must read as a documented, decision-backed deferral — `bagging_by_query` is not dropped, it is scheduled for Phase 7.
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting bagging_rng && cargo test -p oracle-harness --test boosting_parity bagging</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-boosting bagging_rng` exits 0: the full `bag_data_indices` (in-bag asc ++ OOB desc) matches the RNG-replay sequence golden bit-exact (`compare_exact_u32`); balanced + plain variants; fires on the right iters.
    - The OOB-score-update test passes (OOB rows scored each iter).
    - `grep -q 'next_float' crates/lgbm-boosting/src/sample_strategy.rs` and the PRNG is `lgbm_core::Random` (not re-rolled).
    - `bagging_by_query` is an explicit, decision-backed Phase-7 deferral: a doc-comment in `sample_strategy.rs` references the CONTEXT.md BST-03 scope note, and `bagging_by_query=true` returns a typed error/asserts (test-proven) rather than silently row-bagging — `grep -qi 'bagging_by_query' crates/lgbm-boosting/src/sample_strategy.rs`.
    - `git diff --stat` empty after a second capture of the RNG sequence golden (idempotent).
  </acceptance_criteria>
  <done>Bagging reproduces the exact C++ RNG draw/call sequence with the full bagged-index ordering proven against an RNG-replay golden, and OOB rows are scored; D-13 satisfied explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Early stopping (BST-07) + metric infrastructure (MET-02)</name>
  <files>crates/lgbm-boosting/src/{early_stopping.rs,gbdt.rs,lib.rs}, crates/lgbm-metric/src/lib.rs, crates/lgbm/src/{builder.rs,booster.rs}</files>
  <read_first>
    - `crates/lgbm-boosting/src/gbdt.rs` — the loop's per-iter metric eval / OutputMetric call site
    - `crates/lgbm/src/booster.rs` — the D-05 `best_iteration` + eval-history fields (06-02 stubs) to populate
    - `crates/lgbm-metric/src/lib.rs` — `factor_to_bigger_better()` per metric; the metric enum for multi-metric lists
    - `crates/lgbm-core/src/config/` — `early_stopping_round`, `first_metric_only`, `early_stopping_min_delta`, `metric_freq`, `is_provide_training_metric` defaults/semantics (D-02)
    - `LightGBM/src/boosting/gbdt.cpp` — `OutputMetric`/eval cadence (:551-609), early-stop decision (:591-608: `cur=factor*score.back()`; `if cur-best>min_delta { best=cur; best_iter=iter } else if iter-best_iter>=round { STOP }`), `kMinScore` init (:215, gbdt.cpp:484 pop trailing trees), `metric_freq` gate, training-metric list
    - 06-RESEARCH.md §"Early Stopping" + §Code Examples (early-stop decision) + §Metric Infrastructure + D-12 (per-round metric snapshot)
  </read_first>
  <behavior>
    - Test: for each (valid_set, metric), `best_score` inits to `kMinScore`; improvement when `factor*score - best > min_delta` (updates best + best_iter); STOP when `iter - best_iter >= early_stopping_round`.
    - Test: `first_metric_only` evaluates only metric[0] for the stop decision.
    - Test: on stop, `best_iteration` is recorded and the trailing `early_stopping_round*num_class` trees are popped (prediction defaults to best_iteration).
    - Test: `metric_freq=k` evaluates metrics only on iters where `iter%k==0`; `is_provide_training_metric` adds the training set to the eval list.
    - Test: multi-metric list evaluates all listed metrics per round.
  </behavior>
  <action>
    Implement `early_stopping.rs` in `lgbm-boosting` mirroring gbdt.cpp:591-608 verbatim: per (valid_set i, metric j) track `best_score[i][j]` (init `kMinScore`, gbdt.cpp:215) + `best_iter[i][j]`; each eval round `cur = metric.factor_to_bigger_better() * score.last()`; if `cur - best_score > early_stopping_min_delta` → update best + best_iter (improvement); else if `iter - best_iter >= early_stopping_round` → STOP, set `best_iteration` and pop the trailing `early_stopping_round * num_class` trees (gbdt.cpp:484). Honor `first_metric_only` (skip `j>0`). Wire the metric-eval cadence into the loop (`OutputMetric`, gbdt.cpp:551-609): `metric_freq` gate, the multi-metric list (MET-02), `is_provide_training_metric` adds the training set; emit per-round metric snapshots (D-12 L3 golden). Populate the Booster D-05 fields: `best_iteration` + per-valid-set/per-metric eval history (mirror Python `best_iteration_`/`best_score_`/`record_evaluation`). Wire `early_stopping_*`/`metric`/`metric_freq`/`is_provide_training_metric` through the builder→Config (D-02/D-03). Single-threaded deterministic.
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting early_stopping metric_infra</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-boosting early_stopping` exits 0: improvement/stop decision matches the verbatim formula incl. `min_delta`, `first_metric_only`, kMinScore init, trailing-tree pop, `best_iteration`.
    - `cargo test -p lgbm-boosting metric_infra` exits 0: `metric_freq` cadence, multi-metric list, `is_provide_training_metric`.
    - `grep -q 'factor_to_bigger_better' crates/lgbm-boosting/src/early_stopping.rs` (the stop input).
    - `grep -q 'best_iteration' crates/lgbm/src/booster.rs` (D-05 field populated).
  </acceptance_criteria>
  <done>Early stopping fires per the verbatim decision with best_iteration/eval-history surfaced on the Booster, and the metric infrastructure (metric_freq, multi-metric, training-metric) matches the reference.</done>
</task>

<task type="auto">
  <name>Task 3: Full ~40-cell D-07 cross-product capture + replay + collapse documentation</name>
  <files>xtask/py/boosting_oracle_capture.py, xtask/src/main.rs, crates/oracle-harness/tests/boosting_parity.rs, crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md</files>
  <read_first>
    - `xtask/py/boosting_oracle_capture.py` — the per-objective spine-cell capture (06-02..06-04) to cross over the 3 config axes
    - `xtask/src/main.rs` — the `boosting-oracle-capture` body
    - `crates/oracle-harness/tests/boosting_parity.rs` — the parametrized per-objective replay to extend to the full matrix
    - `crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md` — the Phase-6 section to complete with all cells + the one allowed collapse
    - 06-CONTEXT.md D-06/D-07/D-09 (full cross-product, ~10-20 iters; collapse only if provably byte-identical, documented, never silent)
    - 06-RESEARCH.md §"Cross-Product Collapse Analysis" — the ONLY safe automatic collapse is (1) spine == bagging-off/es-off/bfa-on; early-stop cells must genuinely FIRE (valid set + plateauing corpus); bfa cells must have `|init|>kEps` (avoid label-mean-zero corpora)
  </read_first>
  <action>
    Extend the capture to the full D-07 cross-product: for each of the 5 core objectives, cross `{bagging on/off} × {early_stopping on/off} × {boost_from_average on/off}` = 8 cells/objective (~40 total) + the custom run, each end-to-end (model text `%.17g` + predictions) via the real `lib_lightgbm 4.6` wheel. Per RESEARCH §Cross-Product Collapse Analysis: the ONLY safe automatic collapse is cell (1) (`bagging off + es off + bfa on` IS the per-objective spine golden from 06-02..06-04 — reference it, do NOT re-capture); construct early-stop cells with a valid set + a corpus where the metric plateaus so stopping GENUINELY FIRES (else the cell is byte-identical to its es-off sibling — document + collapse with the note, but PREFER firing); choose corpora where `|init_score|>kEps` so the bfa axis is exercised (avoid label-mean-zero). Document EVERY cell + every collapse with its byte-identity proof in the manifest Phase-6 section — never silently drop a cell. Extend `boosting_parity.rs` to replay each committed cell (model text bit-exact `compare_exact_f64_bits`; predictions within ORACLE_TOL or bit-exact per the 06-02 L2 contract). Version-assert the wheel; idempotent regen (`git diff` empty after re-capture). If any cell cannot fit the corpus/iteration budget to genuinely exercise its axis, surface it in the SUMMARY (do NOT silently truncate). NEVER `git add LightGBM/`.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test boosting_parity && cargo test --workspace 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness --test boosting_parity` exits 0: every committed D-07 cell (≈39 distinct + the spine reference + custom) replays vs the real binary.
    - The manifest Phase-6 section enumerates all ~40 cells; every collapse carries a byte-identity proof note (RESEARCH allows only the spine collapse automatically).
    - At least one early-stop cell per objective is constructed to genuinely fire (best_iteration < num_iterations), proven by the replay.
    - `git diff --stat crates/oracle-harness/tests/fixtures/boosting/` empty after a second `cargo run -p xtask -- boosting-oracle-capture` (idempotent).
    - `cargo test --workspace` exits 0.
  </acceptance_criteria>
  <done>The full maximal-fidelity D-07 cross-product replays bit-exact / ~1e-6 vs the real lib_lightgbm 4.6 oracle with every cell + collapse documented; the phase's exhaustive end-to-end gate is green.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (06-05) produces:
- `BaggingSampleStrategy` (`sample_strategy.rs`) — RNG draw + bag_data_indices + balanced pos/neg, over `lgbm-core::Random`.
- OOB score-update + use_subset branch in `GBDT::UpdateScore`.
- `early_stopping.rs` — the early-stopping decision (BST-07) + trailing-tree pop.
- Booster `best_iteration` + eval-history fields populated (D-05); metric infrastructure (multi-metric, metric_freq, is_provide_training_metric).
- Goldens: `bag_indices_seed<S>_frac<F>.txt` (D-13 RNG-replay) + the full ~40-cell D-07 cross-product `<obj>_<cell>_model.txt`/`<obj>_<cell>_pred.txt` + per-round metric (L3) cells.
- The completed `REFERENCE_MANIFEST.md` Phase-6 section documenting every cell + collapse.
- All 10 Phase-6 requirement IDs satisfied; all 5 ROADMAP success criteria demonstrated.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder bagging/early-stop params → engine | `bagging_fraction`, `bagging_freq`, `bagging_seed`, `early_stopping_round`, etc. cross the API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering/DoS | `bagging_fraction` outside (0,1], `bagging_freq<0`, or `early_stopping_round<0` | mitigate | Builder routes through `lgbm-core::Config` CHECK (CFG-03, D-02): `0<bagging_fraction<=1` etc. → typed `Result`, never panic (Security V5). |
| T-06-05-02 | Tampering | empty valid set with early_stopping_round>0 | mitigate | Validate a valid set exists when early stopping is requested → `BoostingError`, not a panic on `score.last()` of an empty vec. |
| T-06-05-04 | Tampering | `bagging_by_query=true` requested in Phase 6 (no ranking objective) | mitigate | Explicit Phase-7 deferral (CONTEXT.md BST-03): reject `bagging_by_query=true` with a typed `BoostingError` rather than silently row-bagging — no silent scope reduction, no wrong-bag divergence. |
| T-06-05-03 | Information Disclosure | `Random` LCG misused as a secure RNG | accept | The LCG is a determinism tool, NOT cryptographic (RESEARCH §Security V6); documented; no security claim made. |
| T-06-05-SC | Tampering | pip wheel at capture (~40 cells) | accept | Version-asserted per capture; test-time oracle only; idempotent/human-gated (as prior plans). |
</threat_model>

<verification>
- `cargo test --workspace` green (bagging + early-stop + metric infra live; all prior slices unregressed).
- D-13 bagged-index RNG-replay golden + the full ~40-cell D-07 matrix replay green; capture idempotent.
- Every D-07 cell + collapse documented in the manifest.
- CMP-01 holds; `LightGBM/` never `git add`ed.
</verification>

<success_criteria>
- ROADMAP SC#5 (bagging selects the same rows via RNG-matching sequence) demonstrated by the D-13 golden.
- SC#4 complete (early stopping fires identically; metric infrastructure matches).
- All 5 ROADMAP success criteria demonstrated; BST-03, BST-07, MET-02 satisfied; the full maximal-fidelity matrix is the exhaustive end-to-end gate.
</success_criteria>

<output>
Create `.planning/phases/06-gbdt-spine-core-objectives-metrics/06-05-SUMMARY.md` when done.
</output>
