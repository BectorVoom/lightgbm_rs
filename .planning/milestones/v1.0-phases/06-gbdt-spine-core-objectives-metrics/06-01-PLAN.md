---
phase: 06-gbdt-spine-core-objectives-metrics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/lgbm-objective/Cargo.toml
  - crates/lgbm-objective/src/lib.rs
  - crates/lgbm-objective/src/error.rs
  - crates/lgbm-metric/Cargo.toml
  - crates/lgbm-metric/src/lib.rs
  - crates/lgbm-metric/src/error.rs
  - crates/lgbm-boosting/Cargo.toml
  - crates/lgbm-boosting/src/lib.rs
  - crates/lgbm-boosting/src/error.rs
  - crates/lgbm/Cargo.toml
  - crates/lgbm/src/lib.rs
  - crates/lgbm-model/src/tree.rs
  - crates/lgbm-treelearner/src/learner.rs
  - crates/oracle-harness/tests/boosting_parity.rs
autonomous: true
requirements: [BST-01, BST-02, OBJ-03]
must_haves:
  truths:
    - "The four new crates (lgbm-objective, lgbm-metric, lgbm-boosting, lgbm) build under cargo build --workspace"
    - "Tree::shrinkage(rate) and Tree::add_bias(val) exist with MaybeRoundToZero, mirroring C++ tree.h:188/213"
    - "SerialTreeLearner exposes add_prediction_to_score(&Tree, &mut [f64]) and a renew_tree_output hook"
    - "A failing (or #[ignore]d) boosting_parity end-to-end test scaffold exists naming the spine golden it will replay"
  artifacts:
    - path: "crates/lgbm-objective/src/error.rs"
      provides: "ObjectiveError thiserror enum"
      contains: "pub enum ObjectiveError"
    - path: "crates/lgbm-metric/src/error.rs"
      provides: "MetricError thiserror enum"
      contains: "pub enum MetricError"
    - path: "crates/lgbm-boosting/src/error.rs"
      provides: "BoostingError thiserror enum with #[from] wrapping"
      contains: "pub enum BoostingError"
    - path: "crates/oracle-harness/tests/boosting_parity.rs"
      provides: "Wave-0 failing end-to-end replay scaffold"
      contains: "boosting"
  key_links:
    - from: "Cargo.toml"
      to: "crates/lgbm-objective, lgbm-metric, lgbm-boosting, lgbm"
      via: "workspace members list"
      pattern: "lgbm-boosting"
    - from: "crates/lgbm-boosting/src/score_updater.rs (later wave)"
      to: "SerialTreeLearner::add_prediction_to_score"
      via: "per-leaf scatter over data_partition"
      pattern: "add_prediction_to_score"
---

<objective>
Lay the Wave-0 foundation for Phase 6: scaffold the four new crates (`lgbm-objective`, `lgbm-metric`, `lgbm-boosting`, `lgbm`), register them in the workspace, add the per-crate `thiserror` error enums, extend `lgbm-model::Tree` with `shrinkage`/`add_bias` methods (with `MaybeRoundToZero`), extend the Phase-5 `SerialTreeLearner` with the `add_prediction_to_score` score-scatter and the `renew_tree_output` hook, and stand up the failing end-to-end `boosting_parity` test scaffold. This unblocks the spine slice (06-02) and every later wave.

Purpose: Per RESEARCH §"Wave 0 Gaps" and PATTERNS, every Phase-6 file has an existing analog except the public builder. This plan creates the empty-but-compiling skeletons and the two existing-crate extensions so subsequent slices are pure fill-in against compiling contracts (interface-first ordering). It does NOT implement any objective/metric/loop math.
Output: Four new compiling crates with error boundaries; `Tree::shrinkage`/`add_bias`; learner `add_prediction_to_score`/`renew_tree_output`; a Wave-0 failing boosting_parity scaffold. `cargo build --workspace` green; `cargo test --workspace` green except the intentionally-`#[ignore]`d boosting scaffold.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-CONTEXT.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-RESEARCH.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-PATTERNS.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the four new crates + workspace registration + error enums</name>
  <files>Cargo.toml, crates/lgbm-objective/{Cargo.toml,src/lib.rs,src/error.rs}, crates/lgbm-metric/{Cargo.toml,src/lib.rs,src/error.rs}, crates/lgbm-boosting/{Cargo.toml,src/lib.rs,src/error.rs}, crates/lgbm/{Cargo.toml,src/lib.rs}</files>
  <read_first>
    - `Cargo.toml` (root) — the `members` list + `[workspace.dependencies]` (thiserror 2.0.18, anyhow 1.0.102) to inherit
    - `crates/lgbm-treelearner/Cargo.toml` — the multi-dep + `[dev-dependencies] oracle-harness` scaffold to copy (PATTERNS §crate scaffolds)
    - `crates/lgbm-model/Cargo.toml` — the lean-dep scaffold variant
    - `crates/lgbm-treelearner/src/error.rs` — the thiserror idiom incl. `#[from]` upstream-error wrapping (PATTERNS §error.rs analog)
    - `crates/lgbm-core/src/error.rs` — the base thiserror idiom
    - `crates/lgbm-model/src/lib.rs` — the facade `pub mod` + `pub use` re-export shape for `lgbm/src/lib.rs`
    - CLAUDE.md — FND-04 mandate: thiserror derive only, never hand-roll `impl Error`; map C++ CHECK/Log::Fatal to Result, never panic
  </read_first>
  <action>
    Create the four crate directories under `crates/` and add them to the root `Cargo.toml` `members` list (net-additions, no rename — RESEARCH Runtime State Inventory). Each `Cargo.toml` inherits `edition.workspace=true`, `rust-version.workspace=true`, uses `{ path = "../…" }` intra-workspace deps and `thiserror = { workspace = true }` (PATTERNS §"Workspace-inherited Cargo metadata"). Dep map per PATTERNS:
    - `lgbm-objective`: deps `lgbm-core`, `lgbm-dataset`, `lgbm-model`, `thiserror`; dev-dep `oracle-harness`.
    - `lgbm-metric`: deps `lgbm-core`, `lgbm-dataset`, `lgbm-model`, `thiserror`; dev-dep `oracle-harness`.
    - `lgbm-boosting`: deps `lgbm-core`, `lgbm-dataset`, `lgbm-model`, `lgbm-treelearner`, `lgbm-objective`, `lgbm-metric`, `thiserror`; dev-dep `oracle-harness`. MUST NOT list `cubecl` or `lgbm-compute` as a dependency (CMP-01 containment — mirror the `lgbm-treelearner/Cargo.toml` comment that the compute seam stays below).
    - `lgbm` (facade): deps all four new crates + `lgbm-core`, `lgbm-dataset`, `lgbm-model`, `lgbm-treelearner`, `thiserror`.
    Add `error.rs` to each new crate: `ObjectiveError`, `MetricError`, `BoostingError` as `#[derive(Debug, Error, Clone, PartialEq)]` enums mirroring `treelearner/error.rs` (struct-style variants, `#[error("…{field}…")]`). Seed each with at minimum a `LengthMismatch { expected, actual }` variant. `ObjectiveError` gets `LabelOutOfRange { label, num_class }` (Security V5 — multiclass_objective.hpp:62 `Log::Fatal` → typed Result, never panic). `BoostingError` `#[from]`-wraps `TreeLearnerError`, `ObjectiveError`, `MetricError` (mirror the `#[from] lgbm_compute::ComputeError` in treelearner/error.rs). Each `lib.rs` is a stub with `pub mod error;` + `pub use error::*;` and a module doc-comment citing the C++ port target dir (`src/objective`, `src/metric`, `src/boosting`). `lgbm/src/lib.rs` follows the `lgbm-model/src/lib.rs` facade re-export shape (curated `pub use`), initially re-exporting only what compiles.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace` exits 0 with all four new crates compiling.
    - `grep -c 'lgbm-boosting' Cargo.toml` ≥ 1 (registered in members).
    - `grep -q 'pub enum ObjectiveError' crates/lgbm-objective/src/error.rs && grep -q 'pub enum MetricError' crates/lgbm-metric/src/error.rs && grep -q 'pub enum BoostingError' crates/lgbm-boosting/src/error.rs` all succeed.
    - `grep -q 'cubecl' crates/lgbm-boosting/Cargo.toml` returns NOTHING (CMP-01: boosting names no cubecl/compute runtime).
    - No `panic!`/`unwrap()` in the new `error.rs`/`lib.rs` stubs (Security V5).
  </acceptance_criteria>
  <done>The four crates build, are workspace-registered, and expose their thiserror error boundaries; the boosting crate is provably cubecl-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Tree (shrinkage/add_bias) + learner (add_prediction_to_score/renew_tree_output)</name>
  <files>crates/lgbm-model/src/tree.rs, crates/lgbm-treelearner/src/learner.rs</files>
  <read_first>
    - `crates/lgbm-model/src/tree.rs` — `Tree` struct (has `shrinkage: f64` field at :95, `leaf_value: Vec<f64>` at :79, `internal_value`); confirms there is NO `shrinkage(rate)`/`add_bias(val)` METHOD yet (PATTERNS §Tree extension)
    - `crates/lgbm-core/src/types.rs` — `K_ZERO_THRESHOLD` (:35) for the `MaybeRoundToZero` test; `K_EPSILON` (:31)
    - `LightGBM/include/LightGBM/tree.h` — `Shrinkage` (:188, multiply leaf+internal values, set shrinkage_), `AddBias` (:213, add to leaf+internal values), `MaybeRoundToZero` (:258, `IsZero(fval) ? 0 : fval`)
    - `crates/lgbm-treelearner/src/data_partition.rs` — `indices_in_leaf(leaf) -> &[u32]` (:83), the per-leaf row accessor for the scatter
    - `crates/lgbm-treelearner/src/learner.rs` — `train` (:265) + `train_with_snapshots` (:278) driver shape to mirror; the owned `DataPartition`
    - `LightGBM/src/treelearner/serial_tree_learner.h` — `AddPredictionToScore` (:100-118, per-leaf scatter) + `RenewTreeOutput` interface
  </read_first>
  <behavior>
    - Test: `Tree::shrinkage(0.1)` multiplies every `leaf_value` and `internal_value` by 0.1 and sets `self.shrinkage = 0.1`.
    - Test: `Tree::add_bias(b)` adds `b` to every `leaf_value` and `internal_value`.
    - Test: a value v with `|v| < K_ZERO_THRESHOLD` after shrinkage/add_bias snaps to `+0.0` (MaybeRoundToZero; signed-zero normalized).
    - Test: `add_prediction_to_score(&tree, &mut score)` over a 2-leaf tree adds `leaf_value[leaf]` to each row's score per the data partition's `indices_in_leaf`, accumulating in f64.
  </behavior>
  <action>
    Add `impl Tree` methods `shrinkage(&mut self, rate: f64)` and `add_bias(&mut self, val: f64)` mirroring C++ `tree.h:188`/`:213`: `shrinkage` multiplies all `leaf_value` + `internal_value` entries by `rate` then applies `MaybeRoundToZero` (use `K_ZERO_THRESHOLD` from `lgbm_core::types`, normalizing `-0.0`→`+0.0`) and sets `self.shrinkage = rate`; `add_bias` adds `val` to all `leaf_value` + `internal_value` (model-text only — does NOT touch any score buffer, RESEARCH Pitfall 5). Reuse any existing `MaybeRoundToZero` helper from Phase 5 if present (RESEARCH State-of-the-Art notes it may be partly present); otherwise add a small private fn. Cite `tree.h:188`/`:213`/`:258` in doc-comments. In `lgbm-treelearner/src/learner.rs` add `pub fn add_prediction_to_score(&self, tree: &Tree, out_score: &mut [f64])` performing the per-leaf scatter over the learner's owned `DataPartition` (`for leaf in 0..tree.num_leaves() { let v = tree.leaf_value[leaf]; for &row in data_partition.indices_in_leaf(leaf) { out_score[row as usize] += v; } }`, early-return if `num_leaves<=1`) — verbatim `serial_tree_learner.h:100-115` (RESEARCH §Code Examples). Add a `renew_tree_output` hook stub: `pub fn renew_tree_output(&self, tree: &mut Tree, obj: &impl ...)` signature placeholder that is a no-op for objectives whose `IsRenewTreeOutput()==false` (the real median-residual body lands in 06-03 with regression_l1) — define it now as the seam the loop calls so the loop contract is stable. Do NOT modify the learner's split-finding/growth.
  </action>
  <verify>
    <automated>cargo test -p lgbm-model tree && cargo test -p lgbm-treelearner add_prediction_to_score</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-model tree` exits 0 incl. a new unit test asserting `shrinkage` + `add_bias` + MaybeRoundToZero snap.
    - `cargo test -p lgbm-treelearner add_prediction_to_score` exits 0 with the f64 per-leaf scatter test.
    - `grep -q 'pub fn add_prediction_to_score' crates/lgbm-treelearner/src/learner.rs` and `grep -q 'pub fn shrinkage' crates/lgbm-model/src/tree.rs` and `grep -q 'pub fn add_bias' crates/lgbm-model/src/tree.rs` all succeed.
    - Phase-5 learner_parity suite still green: `cargo test -p oracle-harness --test learner_parity` exits 0 (no growth regression).
  </acceptance_criteria>
  <done>Tree has shrinkage/add_bias with MaybeRoundToZero; the learner exposes the bit-exact score-scatter and the renew hook seam; Phase-5 parity unregressed.</done>
</task>

<task type="auto">
  <name>Task 3: Wave-0 failing boosting_parity scaffold + manifest section + capture subcommand stub</name>
  <files>crates/oracle-harness/tests/boosting_parity.rs, crates/oracle-harness/Cargo.toml, xtask/src/main.rs, crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md</files>
  <read_first>
    - `crates/oracle-harness/tests/learner_parity.rs` — the layered golden-replay test structure + `#[ignore]`-with-honest-reason convention to mirror
    - `crates/oracle-harness/src/comparator.rs` — `compare_exact_f64_bits` (:125), `compare_exact_u32`, `compare_within(.., ORACLE_TOL)` (:15), `Mismatch` first-divergence reporting
    - `crates/oracle-harness/tests/fixtures/REFERENCE_MANIFEST.md` — the per-set section structure (Learner Golden Set at :277, REAL Learner Oracle Set at :381) to append a Phase-6 section to
    - `xtask/src/main.rs` — the subcommand `match` dispatch (:96-108) + `learner_oracle_capture` capture-fn skeleton (:739-810): `$LGBM_CAPTURE_PYTHON` resolve, version-assert, write under tracked `crates/oracle-harness/tests/fixtures/`, idempotent manifest refresh
    - `xtask/py/learner_oracle_capture.py` — the pip-wheel capture script to model the new boosting capture on
    - 06-RESEARCH.md §"Validation Architecture" — the L1–L5 layer table + D-13 Option A (RNG-replay) recommendation
  </read_first>
  <action>
    Add `crates/oracle-harness/tests/boosting_parity.rs` containing the Wave-0 failing/`#[ignore]`d end-to-end scaffold: a `#[test]` named `spine_end_to_end` (and stubs `score_accumulation`, `gradients`, `early_stopping`, `custom_objective`, `bagging_rng`) that reference the spine golden path `crates/oracle-harness/tests/fixtures/boosting/regression_spine_model.txt` and panic with `MISSING — implemented in wave 2 (06-02)` until the slice lands (mirror the Phase-5 `#[ignore]`-with-reason idiom; the Nyquist scaffold). Add the `lgbm-boosting`, `lgbm-objective`, `lgbm-metric`, `lgbm` crates as `[dev-dependencies]` of `oracle-harness/Cargo.toml` (same path-dep idiom as the existing `lgbm-treelearner` dev-dep; keeps the library crate cubecl-free). Add a `boosting-oracle-capture` arm to the xtask subcommand `match` (a stub fn that prints "not yet implemented — wave 2+" and exits 0; the real capture body fills in across 06-02..06-05). Append a "Boosting / Objective / Metric Golden Set (Phase 6)" section header to `REFERENCE_MANIFEST.md` documenting the planned L1–L5 layers + the one allowed D-07 collapse (spine == bagging-off/es-off/bfa-on, per RESEARCH §Cross-Product Collapse Analysis) — the section is populated as goldens are committed in later waves. Create the empty `crates/oracle-harness/tests/fixtures/boosting/` directory (with a `.gitkeep` or a README noting "populated by boosting-oracle-capture").
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test boosting_parity 2>&1 | grep -E 'ignored|test result'</automated>
  </verify>
  <acceptance_criteria>
    - `boosting_parity.rs` compiles; its tests are `#[ignore]`d (or fail with the explicit `MISSING — wave 2` message) — `cargo test -p oracle-harness --test boosting_parity` reports them ignored, not silently absent (Nyquist: the scaffold exists).
    - `grep -q 'lgbm-boosting' crates/oracle-harness/Cargo.toml` succeeds (dev-dep wired).
    - `grep -q 'boosting-oracle-capture' xtask/src/main.rs` succeeds.
    - `grep -q 'Boosting' crates/oracle-harness/tests/fixtures/REFERENCE_MANIFEST.md` succeeds (Phase-6 section header present).
    - `cargo test --workspace` exits 0 (the boosting scaffold ignored, everything else green).
  </acceptance_criteria>
  <done>The failing end-to-end Nyquist scaffold, capture subcommand stub, manifest section, and fixtures dir exist; subsequent slices fill them in.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (06-01) produces:
- New crates: `lgbm-objective`, `lgbm-metric`, `lgbm-boosting`, `lgbm` (scaffolds + error boundaries only).
- New types: `ObjectiveError`, `MetricError`, `BoostingError` (thiserror enums; `BoostingError` `#[from]`-wraps the three upstream errors).
- New methods: `Tree::shrinkage(rate)`, `Tree::add_bias(val)` (with `MaybeRoundToZero`); `SerialTreeLearner::add_prediction_to_score(&Tree, &mut [f64])`, `SerialTreeLearner::renew_tree_output(...)` hook seam.
- New test file: `crates/oracle-harness/tests/boosting_parity.rs` (Wave-0 scaffold).
- New xtask subcommand: `boosting-oracle-capture` (stub).
- New fixtures dir: `crates/oracle-harness/tests/fixtures/boosting/` + `REFERENCE_MANIFEST.md` Phase-6 section.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public API surface (future) → engine | user training data + Config cross into the boosting/objective layers; this plan only creates the typed error channel for it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Denial of Service | new crate error boundaries | mitigate | Every error variant is a typed `Result` (thiserror); no `panic!`/`unwrap` in the boundary code. C++ `CHECK`/`Log::Fatal` map to `Result` variants (Security V5). |
| T-06-01-02 | Tampering | length-mismatched grad/hess/score slices | mitigate | `LengthMismatch { expected, actual }` variant seeded in each error enum; later waves validate at boundaries before FP work. |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan — only in-tree path-dep crates added to the workspace (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `cargo build --workspace` green (four new crates compile).
- `cargo test --workspace` green (boosting scaffold `#[ignore]`d; Phase-5 learner_parity + all prior suites unregressed).
- CMP-01 holds: `grep cubecl crates/lgbm-boosting/Cargo.toml` empty.
- `LightGBM/` never `git add`ed.
</verification>

<success_criteria>
- Four new crates registered + compiling with thiserror boundaries.
- `Tree::shrinkage`/`add_bias` (MaybeRoundToZero) + learner `add_prediction_to_score`/`renew_tree_output` seam exist and tested.
- Wave-0 failing boosting_parity scaffold + capture subcommand stub + manifest section in place.
</success_criteria>

<output>
Create `.planning/phases/06-gbdt-spine-core-objectives-metrics/06-01-SUMMARY.md` when done.
</output>
