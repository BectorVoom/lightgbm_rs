---
phase: 06-gbdt-spine-core-objectives-metrics
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - crates/lgbm-objective/src/lib.rs
  - crates/lgbm-objective/src/regression.rs
  - crates/lgbm-objective/src/binary.rs
  - crates/lgbm-objective/src/custom.rs
  - crates/lgbm-objective/src/percentile.rs
  - crates/lgbm-metric/src/lib.rs
  - crates/lgbm-metric/src/binary.rs
  - crates/lgbm-treelearner/src/learner.rs
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm/src/builder.rs
  - xtask/py/boosting_oracle_capture.py
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md
autonomous: true
requirements: [OBJ-01, OBJ-02, OBJ-03, MET-01]
must_haves:
  truths:
    - "regression_l1 GetGradients (grad=Sign(diff), hess=1), BoostFromScore (median), and RenewTreeOutput (median residual per leaf) match the reference"
    - "binary GetGradients (sigmoid response), BoostFromScore (logit of base rate), ConvertOutput (sigmoid) match the reference"
    - "the custom objective closure Fn(&[f32], &Dataset)->(Vec<f32>,Vec<f32>) is called in place of GetGradients, with boost_from_average forced off, matching a Python fobj reference run AND (OBJ-02 cross-anchor) the custom run's model text matches the native regression(L2) cell (same g/h ⇒ same trees)"
    - "binary_logloss, binary_error, and auc metrics match the per-round reference"
    - "PercentileFun is a faithful port of common.h, validated on the l1 BoostFromScore + per-leaf renew goldens"
  artifacts:
    - path: "crates/lgbm-objective/src/binary.rs"
      provides: "binary sigmoid grad/hess + BoostFromScore + ConvertOutput (training side)"
      contains: "sigmoid"
    - path: "crates/lgbm-objective/src/percentile.rs"
      provides: "PercentileFun / WeightedPercentileFun verbatim port"
      contains: "percentile"
    - path: "crates/lgbm-objective/src/custom.rs"
      provides: "Custom(closure) objective variant (D-04)"
      contains: "Fn"
    - path: "crates/lgbm-metric/src/binary.rs"
      provides: "binary_logloss / binary_error / auc"
      contains: "auc"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs"
      to: "SerialTreeLearner::renew_tree_output"
      via: "median-residual leaf overwrite for regression_l1 (IsRenewTreeOutput)"
      pattern: "renew_tree_output"
    - from: "crates/lgbm-metric/src/binary.rs"
      to: "lgbm_model::ObjectiveKind::convert_output"
      via: "prob-space transform before logloss/error"
      pattern: "convert_output"
    - from: "crates/lgbm-boosting/src/gbdt.rs"
      to: "custom objective closure"
      via: "called in place of GetGradients; bfa forced off"
      pattern: "custom"
---

<objective>
Widen the proven single-output spine along the objective + metric axis (D-17 step 1, one axis at a time): add `regression_l1` (with its `RenewTreeOutput` median-residual leaf overwrite + `PercentileFun`), `binary` (sigmoid grad/hess + logit BoostFromScore + sigmoid ConvertOutput), the `custom` objective closure (D-04 / OBJ-02), and the binary-side metrics (`binary_logloss`, `binary_error`, `auc`). All as thin, individually-validated additions on the 06-02 loop — the loop itself does not change shape, only gains the `renew_tree_output` body and the custom-objective branch.

Purpose: Complete the single-output objective/metric breadth (regression, regression_l1, binary + their metrics + custom) before the structural multiclass addition (06-04). Per RESEARCH this is where the highest new-numerical-code risk lives (Q3: `PercentileFun` for regression_l1's median residual) — read `common.h` verbatim and capture a per-leaf renew golden.
Output: 3 of 5 single-output objectives complete + custom; binary metrics + auc; the `renew_tree_output` body wired; per-objective L1–L5 goldens for regression_l1/binary + the custom run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-CONTEXT.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-RESEARCH.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-PATTERNS.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: regression_l1 (Sign grad + median BoostFromScore + RenewTreeOutput) + PercentileFun</name>
  <files>crates/lgbm-objective/src/{regression.rs,percentile.rs,lib.rs}, crates/lgbm-treelearner/src/learner.rs, crates/lgbm-boosting/src/gbdt.rs</files>
  <read_first>
    - `crates/lgbm-objective/src/regression.rs` — the 06-02 L2 variant + `Sign` helper to extend
    - `crates/lgbm-treelearner/src/learner.rs` — the `renew_tree_output` hook seam (06-01 stub) + `data_partition.indices_in_leaf`; do NOT modify growth
    - `crates/lgbm-boosting/src/gbdt.rs` — the loop's `renew_tree_output` call site (no-op for L2; active here) between Train and Shrinkage
    - `LightGBM/src/objective/regression_objective.hpp` — regression_l1 `GetGradients` (:207-288: `diff=score-label`, `grad=(score_t)Sign(diff)`, `hess=1.0f`), `BoostFromScore` (:236-249: weighted/unweighted percentile alpha=0.5 = median, NOT mean), `IsRenewTreeOutput=true` + `RenewTreeOutput` (:251-283 median residual per leaf)
    - `LightGBM/include/LightGBM/utils/common.h` — `PercentileFun` + `WeightedPercentileFun` macros VERBATIM (the exact sort + index/interpolation convention; Q3 highest-risk — read line-by-line)
    - `LightGBM/src/treelearner/serial_tree_learner.cpp` — `RenewTreeOutput` driving loop (:920-940)
    - 06-RESEARCH.md Pitfall 2 (l1 leaf values are NOT the learner Newton output) + Pitfall 3 (percentile determinism)
  </read_first>
  <behavior>
    - Test: `PercentileFun([sorted residuals], alpha=0.5)` returns the C++ median for odd AND even counts (exact index/interpolation), matching hand-computed values from the verbatim algorithm.
    - Test: regression_l1 `get_gradients` → `grad[i]=Sign(score[i]-label[i]) as f32`, `hess[i]=1.0f32`.
    - Test: regression_l1 `boost_from_score(labels)` = median(labels) via PercentileFun (NOT mean).
    - Test: `renew_tree_output` overwrites each leaf's value with the median residual (`label-score`) of that leaf's rows.
  </behavior>
  <action>
    Add `percentile.rs` to `lgbm-objective`: port `PercentileFun` and `WeightedPercentileFun` VERBATIM from `common.h` (the exact `std::sort`/`nth_element` choice, index formula, interpolation) with a doc-header citing the common.h lines (Q3 — read before implementing; this is the highest-risk new numerical code). Add the `regression` enum's L1 form (or a `RegressionL1` variant): `get_gradients` per regression_objective.hpp:207-288 (`grad=(score_t)Sign(diff)`, `hess=1.0f`), `boost_from_score` = `PercentileFun(labels, 0.5)` weighted/unweighted (:236-249), and mark `is_renew_tree_output()==true`. Implement the `renew_tree_output` BODY in `lgbm-treelearner/src/learner.rs` (filling the 06-01 seam): for each leaf, gather the leaf's rows via `data_partition.indices_in_leaf`, compute the median residual (`label[row]-score[row]`) via the objective's percentile fn, and overwrite `tree.leaf_value[leaf]` — mirroring serial_tree_learner.cpp:920-940; for objectives with `is_renew_tree_output()==false` it stays a no-op (early return). Wire the loop's existing `renew_tree_output` call (06-02) to pass the objective + residual getter (`label[i]-train_score[offset+i]`) per RESEARCH Pattern 1 step. Note: renew runs AFTER Train, BEFORE Shrinkage (Pitfall 2). Cite all C++ lines.
  </action>
  <verify>
    <automated>cargo test -p lgbm-objective percentile gradients_l1 && cargo test -p lgbm-treelearner renew_tree_output</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-objective percentile` exits 0: PercentileFun median matches hand-computed for odd + even counts.
    - `cargo test -p lgbm-objective gradients_l1` exits 0: Sign grad + median BoostFromScore.
    - `cargo test -p lgbm-treelearner renew_tree_output` exits 0: leaf median-residual overwrite; no-op for L2/binary.
    - `grep -q 'common.h' crates/lgbm-objective/src/percentile.rs` (verbatim citation).
    - Phase-5 learner_parity still green (renew is additive; L2 renew is a no-op so the spine is unregressed): `cargo test -p oracle-harness --test learner_parity` exits 0.
  </acceptance_criteria>
  <done>regression_l1 (Sign grad, median init, median-residual renew) + faithful PercentileFun implemented and unit-validated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: binary objective + custom closure (OBJ-02) + binary metrics + auc</name>
  <files>crates/lgbm-objective/src/{binary.rs,custom.rs,lib.rs}, crates/lgbm-metric/src/binary.rs, crates/lgbm-boosting/src/gbdt.rs, crates/lgbm/src/builder.rs</files>
  <read_first>
    - `crates/lgbm-objective/src/lib.rs` + `error.rs` — the `Objective` enum + `LabelOutOfRange` / `LengthMismatch` variants
    - `crates/lgbm-model/src/objective.rs` — `ObjectiveKind::Binary { sigmoid }` + `convert_output` sigmoid (:198) to reuse for predict + prob-space metrics
    - `crates/lgbm-core/src/types.rs` — `K_EPSILON` (:31) for the pavg clamp + logloss floor
    - `crates/lgbm-dataset/` — label/weights accessors the custom closure receives via `&Dataset` (D-04)
    - `crates/lgbm-boosting/src/gbdt.rs` — the `Boosting`/GetGradients step (06-02) to add the custom branch to
    - `LightGBM/src/objective/binary_objective.hpp` — grad/hess (:105-177: `response=-label_val*sigmoid_/(1+exp(label_val*sigmoid_*score))`, `grad=(score_t)(response*label_weight)`, `hess=(score_t)(abs_resp*(sigmoid_-abs_resp)*label_weight)`), `BoostFromScore` (:139-165: `pavg=Σis_pos/num_data` clamped `[kEps,1-kEps]`, return `log(pavg/(1-pavg))/sigmoid_`), `ConvertOutput` (:175), `ClassNeedTrain` (:80-84)
    - `LightGBM/src/metric/binary_metric.hpp` — `binary_logloss` (:119-130), `binary_error` (:143-149), AUC (:194-251, grouped accumulation); prob-space calls ConvertOutput first (:80-83)
    - `LightGBM/src/objective/objective_function.cpp` — `custom`⇒nullptr; `LightGBM/src/boosting/gbdt.cpp:355-372` custom branch (supplied g/h used directly; BoostFromAverage skipped when obj==null)
    - 06-RESEARCH.md §"Custom objective" D-04 mapping + Pitfall 1 (AUC unstable-sort tie-invariance is bit-safe) + §Security V5
  </read_first>
  <behavior>
    - Test: binary `get_gradients` for is_pos/is_neg rows matches the verbatim response formula bit-exact f32.
    - Test: binary `boost_from_score` = `log(pavg/(1-pavg))/sigmoid_` with pavg clamped to `[kEps,1-kEps]`.
    - Test: `binary_logloss`/`binary_error` over (scores,labels) match hand-computed (ConvertOutput applied first); `auc` on a tied-score corpus matches regardless of (unstable) sort order.
    - Test: a custom closure returning fixed (grad,hess) is invoked in place of GetGradients; wrong-length return → `ObjectiveError::LengthMismatch` (not panic).
  </behavior>
  <action>
    Add `binary.rs` to `lgbm-objective`: `Binary { sigmoid }` training side per binary_objective.hpp:105-177 verbatim (label_val∈{-1,+1}, label_weight from is_unbalance/scale_pos_weight defaulting 1.0, the f64 response, f32-cast grad/hess); `boost_from_score` per :139-165 (pavg clamp via `K_EPSILON`); reuse `ObjectiveKind::convert_output` (sigmoid) for predict; honor `ClassNeedTrain==false` for single-class (push constant tree — but full degenerate handling is exercised in 06-04 multiclass). Add `custom.rs`: a `Custom` variant holding the D-04 closure `Box<dyn Fn(&[f32], &Dataset) -> (Vec<f32>, Vec<f32>)>` (ownership/borrow shape Claude's discretion bounded by the Python `fobj` contract); validate the returned lengths == `num_data*num_class`, NaN-check optional → return `ObjectiveError::LengthMismatch` on mismatch (Security V5, never panic). In `gbdt.rs` add the custom branch to step (2): when objective is `Custom`, call the closure with `score_` cast to f32 (mirror Python f32 preds) IN PLACE of `get_gradients`, and force `boost_from_average` off (mirror C++ `obj==null` skipping BoostFromAverage — RESEARCH §Custom). Add `binary.rs` to `lgbm-metric`: `BinaryLogloss`, `BinaryError`, `Auc` per binary_metric.hpp (prob-space metrics call `ObjectiveKind::convert_output` first — do NOT duplicate; AUC uses `slice::sort_by(|a,b| score[b].partial_cmp(score[a]))` + the grouped accumulation loop, unstable sort is tie-safe per Pitfall 1; `factor_to_bigger_better`: -1 for losses, +1 for auc). Cite all C++ lines. Expose `objective`/`metric` setters in the builder (D-03) for binary + custom.
  </action>
  <verify>
    <automated>cargo test -p lgbm-objective binary custom && cargo test -p lgbm-metric binary auc</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-objective binary` exits 0: sigmoid grad/hess + logit BoostFromScore bit-exact f32.
    - `cargo test -p lgbm-objective custom` exits 0: closure invoked in place of GetGradients; wrong-length return → `ObjectiveError::LengthMismatch` (asserted, not a panic).
    - `cargo test -p lgbm-metric binary auc` exits 0: logloss/error within ORACLE_TOL; auc bit-identical across two sort orders on a tied-score corpus.
    - `grep -q 'binary_objective.hpp' crates/lgbm-objective/src/binary.rs` (citation).
  </acceptance_criteria>
  <done>binary objective + custom closure + binary_logloss/binary_error/auc implemented, unit-validated, with the custom branch wired into the loop and bfa forced off.</done>
</task>

<task type="auto">
  <name>Task 3: Capture + replay regression_l1 / binary / custom layered goldens (L1–L5 spine cells)</name>
  <files>xtask/py/boosting_oracle_capture.py, crates/oracle-harness/tests/boosting_parity.rs, crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md</files>
  <read_first>
    - `xtask/py/boosting_oracle_capture.py` — the 06-02 spine-cell capture to extend per objective
    - `xtask/src/main.rs` — the `boosting-oracle-capture` body (06-02)
    - `crates/oracle-harness/tests/boosting_parity.rs` — the spine replay tests (06-02) to parametrize per objective
    - `crates/oracle-harness/src/comparator.rs` — `compare_exact_f64_bits` (model text/scores), `compare_within(ORACLE_TOL)` (g/h/metrics), `compare_exact_u32`
    - `crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md` — the Phase-6 section to populate
    - 06-RESEARCH.md §Validation Architecture (L1 score-derivation route, L2 predict raw_score, L3 record_evaluation, L5 save_model+predict) + the custom run validated against a Python `fobj` reference (no distinct C++ objective)
    - 06-CONTEXT.md D-06/D-08/D-09 (per-objective synthetic corpora, ~10-20 iters, low num_leaves)
  </read_first>
  <action>
    Extend `boosting_oracle_capture.py` + the xtask body to capture the per-objective spine cells (bagging off, early-stop off, bfa on — the D-07 spine cells) for `regression_l1` and `binary`, each with its objective-appropriate synthetic corpus (D-08: continuous labels for l1, 0/1 for binary; ~10-20 iters, low num_leaves per D-09) reusing the Phase-2 binning path: emit L1 `<obj>_gh_iter1.txt`/`<obj>_gh_iterN.txt`, L2 `<obj>_scores.txt`, L3 `<obj>_metrics.txt` (l1/l2/rmse for l1; binary_logloss/binary_error/auc for binary), L5 `<obj>_spine_model.txt`/`<obj>_spine_pred.txt`. For `custom`: run a Python `fobj` reference that replicates the L2 objective's grad/hess (`grad=score-label`, `hess=1`) — chosen DELIBERATELY so the custom run is cross-anchorable to a real native objective — and capture its model/predict + per-row g/h; the Rust custom run uses the equivalent closure and asserts parity against THAT reference. ADDITIONALLY assert the cross-anchor (OBJ-02 anchored to the real binary): the custom run's end-to-end model text bit-matches the native `regression`(L2) spine cell's model text (`regression_spine_model.txt` from 06-02) — same g/h ⇒ same trees ⇒ same model — since `boost_from_average` is forced off for custom, use the L2 cell captured with `boost_from_average=false` (or subtract the bfa init) so the two are genuinely comparable; document the exact comparison basis in the manifest. Add `crates/oracle-harness/tests/fixtures/boosting/custom_*.txt`. Parametrize `boosting_parity.rs`: per-objective `spine_end_to_end`/`gradients`/`score_accumulation`/`metrics` + a `custom_objective` test replaying the custom golden. For regression_l1 add a dedicated per-leaf renew golden assertion (Q3/Pitfall 2/3: l1 leaf values are the median residual, NOT Newton). Use the L2-precision contract resolved in 06-02 (bit-exact if Open-Q2 resolved bit-exact, else ~1e-6 leaning on L5). Version-assert the wheel; idempotent regen; populate the manifest Phase-6 section with these cells. NEVER `git add LightGBM/`.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test boosting_parity regression_l1 binary custom_objective</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness --test boosting_parity` exits 0 for the `regression_l1`, `binary`, and `custom_objective` cells (L1 g/h, L2 scores, L3 metrics, L5 model+predict).
    - regression_l1 per-leaf renew golden replays bit-exact (median residual, not Newton) — proves Pitfall 2/3 handled.
    - `git diff --stat crates/oracle-harness/tests/fixtures/boosting/` empty after a second capture (idempotent).
    - The custom run's end-to-end model text bit-matches the native `regression`(L2) cell's model text (OBJ-02 cross-anchored to the real binary: same g/h ⇒ same trees), the comparison basis (bfa-off / init-subtracted) noted, and the equivalence RECORDED in the `REFERENCE_MANIFEST.md` / golden manifest.
    - The manifest Phase-6 section documents the regression_l1/binary/custom cells.
  </acceptance_criteria>
  <done>regression_l1, binary, and custom each pass the layered L1–L5 (or L1–L3+L5) golden replay against the real binary / Python fobj reference; custom is additionally cross-anchored to the native regression(L2) model text (OBJ-02 → real binary) with the equivalence recorded in the manifest; the l1 median-residual renew is proven; capture idempotent.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (06-03) produces:
- `Objective::RegressionL1` (Sign grad, median BoostFromScore, RenewTreeOutput), `Objective::Binary { sigmoid }`, `Objective::Custom(closure)` (D-04 `Box<dyn Fn(&[f32], &Dataset)->(Vec<f32>,Vec<f32>)>`) in `lgbm-objective`.
- `PercentileFun`/`WeightedPercentileFun` (`percentile.rs`) verbatim port.
- `Metric::{BinaryLogloss, BinaryError, Auc}` in `lgbm-metric`.
- `SerialTreeLearner::renew_tree_output` median-residual body (filling the 06-01 seam).
- The custom-objective branch in the GBDT loop (bfa forced off).
- Goldens: `regression_l1_*`, `binary_*`, `custom_*` (L1/L2/L3/L5) + l1 per-leaf renew golden.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user custom closure → loop | OBJ-02: a user-supplied closure returns grad/hess each iteration — untrusted length/content |
| training labels → binary/l1 objectives | label values cross into objective math |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering/DoS | custom closure returning wrong-length or NaN grad/hess | mitigate | Validate returned `(grad,hess)` lengths == `num_data*num_class` at the loop boundary → `ObjectiveError::LengthMismatch`, never index OOB / panic (Security V5). |
| T-06-03-02 | Denial of Service | percentile sort on adversarial/degenerate residuals (empty leaf, all-equal) | mitigate | PercentileFun guards empty/odd/even counts per the verbatim common.h algorithm; renew early-returns for `num_leaves<=1`. |
| T-06-03-03 | Tampering | binary label outside {0,1} | accept | binary treats `label>0` as positive (verbatim); no OOB index risk (no array indexed by label) — matches C++. |
| T-06-03-SC | Tampering | pip wheel at capture | accept | Version-asserted; test-time oracle only (as 06-02). |
</threat_model>

<verification>
- `cargo test --workspace` green (3 single-output objectives + custom + binary metrics live; spine + Phase-5 unregressed).
- regression_l1/binary/custom layered goldens replay; l1 renew proven; auc tie-safe.
- CMP-01 holds; `LightGBM/` never `git add`ed.
</verification>

<success_criteria>
- ROADMAP SC#3 advanced: regression_l1 + binary GetGradients/ConvertOutput/BoostFromScore + custom pass-through within ~1e-6.
- SC#4 advanced: binary_logloss/binary_error/auc match the reference.
- Single-output objective/metric breadth complete (regression, regression_l1, binary + their metrics + custom) before multiclass.
</success_criteria>

<output>
Create `.planning/phases/06-gbdt-spine-core-objectives-metrics/06-03-SUMMARY.md` when done.
</output>
