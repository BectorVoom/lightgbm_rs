---
quick_id: 260609-c2l
type: investigation
title: Investigate whether we can minimize host-device transfers for kernels in the leaf-estimation part
date: 2026-06-09
mode: quick (investigation-only, no code changes)
---

# Quick Task 260609-c2l — Plan

## Task

Technical investigation: determine whether host↔device transfers can be minimized
for the kernels in LightGBM's **leaf-estimation** path.

## Approach

Investigation-only (no code changes). Map the leaf-estimation code path and its
transfer points, then assess against the established profiling baseline
(launch-bound, not transfer-bound).

1. **Map the leaf-estimation code path** — locate where leaf output values are
   computed (Newton step + renewal), and whether any of it runs on a GPU/cubecl
   kernel or is host scalar math.
2. **Inventory host↔device transfer points** in `lgbm-compute` (RocmBackend) around
   the tree-learner / leaf path; identify what is resident vs re-transferred.
3. **Assess** whether any transfer in the leaf-estimation part is reducible, in
   light of the prior finding that the GPU path is launch-bound, not transfer-bound.
4. **Write findings** to `260609-c2l-FINDINGS.md` with an explicit verdict + evidence.

## Deliverable

`260609-c2l-FINDINGS.md` — structured investigation report with verdict, code map,
transfer inventory, and recommendation. No source files modified.
