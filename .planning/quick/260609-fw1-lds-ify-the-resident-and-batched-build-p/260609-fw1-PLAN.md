---
quick_id: 260609-fw1
type: implementation
title: LDS-ify the resident/batched build path
date: 2026-06-09
mode: quick
---

# Quick Task 260609-fw1 — Plan

## Task

LDS-privatize the GPU histogram BUILD hot path (resident + batched), the eo5
Finding #2 follow-up that makes the LDS win reach training. User chose (AskUserQuestion):
fix the flaky gate first, then LDS-ify the build path AND wire it live.

## Steps

1. Fix DEF-f8u-01 (`learner_parity_{resident,fused}_equals_host_tree_on_hip` flaky):
   pin both GPU trees to the deterministic cpu f64 anchor — structure bit-exact, leaf
   values within an f32-accumulation-aware tol. Verify reliably green over many runs.
2. Implement `construct_leaf_hist_{resident,batched}_lds_kernel` (one cube per feature,
   ≤2 KiB LDS, sentinel slot_off, ≤256-bin cap + naive fallback).
3. Wire via a shared `resident_raw_build_into` helper into BOTH the resident-pool chain
   and the host path; same branch for batched.
4. Validate: kernel_parity build oracles (now LDS) + learner_parity end-to-end + repeat
   for stability.
5. Benchmark LDS vs naive build.

## Done when

- DEF-f8u-01 reliably green.
- LDS build wired live; default + hip gates GREEN; end-to-end stable.
- Benchmark shows the win.

## Outcome

Both parts done. Gate 12/12; LDS build wired live, 3.5–9× faster, learner_parity 31/31
(10/10 stable). Commits d82611b + b878eb5.
