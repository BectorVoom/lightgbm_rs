---
quick_id: 260609-f8u
type: implementation
title: Parallelize the histogram with LDS sub-histograms
date: 2026-06-09
mode: quick
---

# Quick Task 260609-f8u — Plan

## Task

Implement eo5 Finding #2: LDS-privatized sub-histograms for the GPU histogram, to
relieve global-atomic contention (the classic GPU histogram optimization LightGBM's
own OpenCL kernels use; absent in this port).

## Approach

1. Research cubecl 0.10 LDS API feasibility (SharedMemory, shared atomics, sync_cube,
   comptime size). [DONE — feasible.]
2. Implement a new rocm-gated kernel + launcher: per-cube LDS sub-histogram → global
   merge. Handle the comptime-size constraint (fixed 256-bin cap + runtime length).
   Do NOT touch the cpu f64 anchor.
3. Validate on gfx1100: no-lost-updates under contention, exact-vs-naive on integers,
   <1e-5 vs the f64 anchor.
4. Benchmark LDS vs naive global-atomic across contention regimes.
5. If a strict win + parity holds, wire RocmBackend::construct_histograms to it; else
   land as an available primitive and surface why.
6. Full gate (default + rocm).

## Done when

- LDS kernel correct (parity tests green on gfx1100) + benchmarked.
- Default merge gate + hip parity GREEN.
- Wiring decision made and justified; production parity not regressed.

## Outcome

Kernel built, proven, 4–4.6× faster under contention. NOT wired (pre-existing flaky
gating test DEF-f8u-01 + needs resident-path unification). Landed as primitive.
See FINDINGS.
