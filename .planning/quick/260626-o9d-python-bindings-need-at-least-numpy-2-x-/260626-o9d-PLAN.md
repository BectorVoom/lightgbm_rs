---
quick_id: 260626-o9d
description: Python bindings need at least numpy 2.x.x (and require Python >=3.11)
status: complete
---

# Quick Task 260626-o9d: Raise Python-binding floors (numpy 2.x, Python 3.11)

## Goal

The `lightgbm-rs` Python bindings must require numpy ≥ 2.x and Python ≥ 3.11.

## Decisions

- numpy floor raised to `>=2.0.0` ("at least numpy 2.x.x").
- `requires-python` raised to `>=3.11` (per user instruction).
- abi3 stable-ABI floor aligned to `abi3-py311` (user-confirmed) so the wheel's
  ABI tag (`cp311-abi3`) matches the declared Python floor — keeps design
  decision D-13 internally consistent rather than leaving `abi3-py39` below the
  Python floor.

## Tasks

1. `crates/lgbm-python/pyproject.toml`
   - `dependencies`: `numpy>=1.17.0` → `numpy>=2.0.0`
   - `requires-python`: `>=3.9` → `>=3.11`
   - Update the D-13 floor comment (`abi3-py39 ... 3.9+` → `abi3-py311 ... 3.11+`)
   - verify: `git diff`; valid TOML
   - done: both floors raised

2. `crates/lgbm-python/Cargo.toml`
   - `[dependencies] pyo3` feature `abi3-py39` → `abi3-py311`
   - `[dev-dependencies] pyo3` feature `abi3-py39` → `abi3-py311`
   - verify: `cargo verify-project` succeeds; `cargo metadata` confirms
     `abi3-py311` is a real feature of pyo3 0.27.2
   - done: ABI floor aligned to 3.11

## Verification

- `crates/lgbm-python/pyproject.toml:20` = `numpy>=2.0.0`; `:10` = `requires-python = ">=3.11"`.
- `crates/lgbm-python/Cargo.toml` pyo3 dep + dev-dep both `abi3-py311`.
- `abi3-py311` confirmed present in resolved pyo3 0.27.2 feature set.
- Project `.venv` is Python 3.12 — satisfies `>=3.11`.
