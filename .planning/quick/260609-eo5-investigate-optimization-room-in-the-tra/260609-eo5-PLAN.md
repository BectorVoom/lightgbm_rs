---
quick_id: 260609-eo5
type: investigation
title: Investigate optimization room in the traversal algorithm within the kernels
date: 2026-06-09
mode: quick (investigation-only, no code changes)
---

# Quick Task 260609-eo5 — Plan

## Task

Technical investigation: is there room to optimize the traversal algorithm(s) inside
the compute kernels?

## Approach

Investigation-only (no code changes).

1. Enumerate the `#[cube]` kernels and their inner traversal loops
   (`histogram.rs`, `split.rs`, `partition.rs`, `subtract.rs`); confirm there is no
   tree-walk/predict kernel.
2. For each kernel, classify the traversal: sequential single-owner (bit-exact
   anchor) vs parallel (one-unit-per-row/feature), and identify the GPU dispatch via
   `RocmBackend` (lib.rs).
3. Cross-check against the established profiling baseline (GPU launch-bound) and the
   bit-exact constraint to separate real levers from non-viable ones.
4. Write findings with ranked opportunities + risk to `260609-eo5-FINDINGS.md`.

## Deliverable

`260609-eo5-FINDINGS.md` — structured report with verdict, per-kernel traversal
classification, ranked opportunities, and recommendation. No source files modified.
