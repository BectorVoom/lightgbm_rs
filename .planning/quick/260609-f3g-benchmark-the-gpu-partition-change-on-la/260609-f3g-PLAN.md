---
quick_id: 260609-f3g
type: benchmark
title: Benchmark the GPU partition change on large data
date: 2026-06-09
mode: quick (measurement-only, throwaway instrumentation reverted)
---

# Quick Task 260609-f3g — Plan

## Task

Benchmark the 260609-eu9 partition-kernel parallelization (single-unit → one-unit-per-row)
on the real gfx1100 at large n, to attach real numbers to the "win on large data" claim.

## Approach

1. Add temporary timing buckets (upload / GPU launch+sync+readback / host gather) inside
   `data_partition_on`, gated by `LGBM_PART_BENCH`.
2. Add a throwaway rocm-gated test looping `data_partition_on` over n = 100k/1M/5M
   (warmup + 15 timed calls, median).
3. Measure parallel (HEAD); swap kernel body + launch config back to single-unit; measure.
4. Revert all instrumentation (`git checkout` partition.rs + delete the throwaway test).
5. Record results in FINDINGS/SUMMARY + STATE + memory.

## Deliverable

`260609-f3g-FINDINGS.md` with the A/B table + interpretation + caveats. No code shipped.
