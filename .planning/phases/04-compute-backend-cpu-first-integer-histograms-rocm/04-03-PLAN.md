---
phase: 04-compute-backend-cpu-first-integer-histograms-rocm
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - crates/lgbm-compute/src/lib.rs
  - crates/lgbm-compute/src/gain.rs
  - crates/lgbm-compute/src/kernels/mod.rs
  - crates/lgbm-compute/src/kernels/split.rs
  - crates/lgbm-compute/src/kernels/partition.rs
  - crates/lgbm-compute/src/kernels/subtract.rs
  - xtask/src/main.rs
  - xtask/cpp/kernel_capture.cpp
  - crates/oracle-harness/tests/kernel_parity.rs
  - crates/oracle-harness/tests/fixtures/kernels/split.txt
  - crates/oracle-harness/tests/fixtures/kernels/partition.txt
  - crates/oracle-harness/tests/fixtures/kernels/subtract.txt
autonomous: true
requirements: [CMP-04, CMP-05, ORA-04]

must_haves:
  truths:
    - "Backend::find_best_split carries the gain math inside the kernel (ThresholdL1/GetLeafGain/GetSplitGains/CalculateSplittedLeafOutput) transcribed verbatim from feature_histogram.hpp:711-845, with the exact kEpsilon/2*kEpsilon placements"
    - "The split scan preserves the SKIP_DEFAULT_BIN continue, the offset arithmetic, the t-1+offset (REVERSE)/t+offset (forward) threshold recording, and the exact min_data_in_leaf/min_sum_hessian_in_leaf/min_gain_shift gate order"
    - "Backend::subtract_histograms reproduces the C++ FeatureHistogram::Subtract math (the kernel-layer subtraction; which child is Phase-5 orchestration)"
    - "Backend::data_partition mirrors the C++ stable row->{left,right} reorder; Phase-5 owns leaf_begin_/leaf_count_ bookkeeping"
    - "cubecl-cpu split/partition/subtract outputs are bit-exact vs committed C++ goldens; the capability gate selects Sequential on cpu"
  artifacts:
    - path: "crates/lgbm-compute/src/gain.rs"
      provides: "ThresholdL1/GetLeafGain/GetSplitGains/CalculateSplittedLeafOutput + GainConfig + SplitInfo (verbatim from feature_histogram.hpp)"
      contains: "ThresholdL1"
    - path: "crates/lgbm-compute/src/kernels/split.rs"
      provides: "#[cube] find_best_split_kernel — scan + gain math, default-bin skip, kEpsilon seeds"
      contains: "#[cube"
    - path: "crates/lgbm-compute/src/kernels/subtract.rs"
      provides: "#[cube] subtract_hist_kernel — FeatureHistogram::Subtract math"
      contains: "#[cube"
    - path: "crates/oracle-harness/tests/fixtures/kernels/split.txt"
      provides: "committed best-split golden (per-candidate gains + winner)"
      min_lines: 5
  key_links:
    - from: "crates/lgbm-compute/src/kernels/split.rs"
      to: "crates/lgbm-compute/src/gain.rs"
      via: "GetSplitGains/GetLeafGain inside the scan"
      pattern: "GetSplitGains|GetLeafGain|gain"
    - from: "crates/oracle-harness/tests/kernel_parity.rs"
      to: "lgbm_compute::Backend::find_best_split"
      via: "drive on split golden inputs, compare SplitInfo + per-candidate gains bit-exact"
      pattern: "find_best_split"
---

<objective>
Complete the kernel suite: `find_best_split` (D-01 whole-kernel — the gain formula lives INSIDE the kernel, transcribed verbatim from `feature_histogram.hpp:711-1000`), `data_partition` (row→leaf routing), and `subtract_histograms` (the histogram-subtraction MATH — resolving RESEARCH open question A3: the subtract op IS in scope at the kernel layer since it is histogram-layer math; the Phase-5 learner only orchestrates WHICH child is subtracted). Each is wired Backend method → cubecl-cpu kernel → committed C++ golden → bit-exact parity, reusing the 04-02 capture + parity machinery. This implements Phase-5 TRL-04's gain math early per D-01a (Phase 5 consumes, does not re-derive).

Purpose: Land the remaining two-and-a-half kernels on the proven cpu anchor so the full `Backend` op set (`construct_histograms`/`find_best_split`/`data_partition`/`subtract_histograms`) exists for Phase 5. The split scan is where `kEpsilon`/`2*kEpsilon` placement, default-bin skip, and gate order are load-bearing — faithful 1:1 transcription, no restructuring.
Output: `gain.rs`, `kernels/{split,partition,subtract}.rs`, the three Backend methods, the extended `kernel_capture.cpp` + goldens, and the split/partition/subtract parity layers in `kernel_parity.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-CONTEXT.md
@.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-RESEARCH.md
@.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-PATTERNS.md
@.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See the full phase-wide symbol registry in 04-01-PLAN.md. Symbols introduced/finalized in THIS plan:
- `Backend::find_best_split` — takes the f64 histogram (from `construct_histograms`), a `GainConfig`, `num_bin`, `offset`/`default_bin`/`most_freq_bin` (from the Phase-2 `FeatureGroup`/`Bin`), total sum_grad/sum_hess/num_data; returns `SplitInfo`
- `Backend::data_partition` — takes the binned feature column + a threshold + missing/default routing; returns reordered left/right row indices (stable, mirroring C++ `DataPartition::Split`)
- `Backend::subtract_histograms` — takes parent + (smaller) child f64 histograms; returns the derived (larger) child histogram (FeatureHistogram::Subtract math)
- `gain.rs`: `ThresholdL1`, `get_leaf_gain`, `get_split_gains`, `calculate_splitted_leaf_output` (verbatim from `feature_histogram.hpp`), `GainConfig` struct (from `lgbm-core::Config`: `min_data_in_leaf`, `min_sum_hessian_in_leaf`, `max_delta_step`, `lambda_l1`, `lambda_l2`, `min_gain_to_split`, `path_smooth`), `SplitInfo` result struct (best_threshold, best_gain, left/right output+count+sum, default-left flag)
- `find_best_split_kernel`, `data_partition_kernel`, `subtract_hist_kernel` (`#[cube]`)
- goldens: `split.txt`, `partition.txt`, `subtract.txt` (committed)
- `kernel_capture.cpp` extended with FindBestThreshold/Split/Subtract transcriptions
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: gain.rs — verbatim ThresholdL1/GetLeafGain/GetSplitGains + GainConfig/SplitInfo, and the find_best_split scan kernel</name>
  <files>crates/lgbm-compute/src/gain.rs, crates/lgbm-compute/src/kernels/split.rs, crates/lgbm-compute/src/kernels/mod.rs, crates/lgbm-compute/src/lib.rs</files>
  <read_first>
    - LightGBM/src/treelearner/feature_histogram.hpp:711-845 (ThresholdL1, GetLeafGain USE_L1/else, GetSplitGains, CalculateSplittedLeafOutput — the verbatim gain math)
    - LightGBM/src/treelearner/feature_histogram.hpp:845-1000 (FindBestThreshold numerical scan — REVERSE branch 862-934, forward 935+; the gate order + threshold recording)
    - LightGBM/src/treelearner/feature_histogram.hpp:172 (the `2*kEpsilon` hessian bump at scan entry) + :862/:935 (the `sum_*_hessian = kEpsilon` scan seeds)
    - LightGBM/include/LightGBM/bin.h:180-258 (offset/default_bin_/most_freq_bin_ semantics driving SKIP_DEFAULT_BIN + the threshold offset arithmetic)
    - LightGBM/include/LightGBM/meta.h:54-56 (kEpsilon=1e-15f, kZeroThreshold=1e-35f)
    - crates/lgbm-core/src/types.rs:28-35 (K_EPSILON, K_ZERO_THRESHOLD — REUSE, do not redefine; K_EPSILON is f32 1e-15 — preserve the f32→f64 widening)
    - crates/lgbm-core/src/config (the Config fields backing GainConfig: lambda_l1/l2, min_gain_to_split, min_sum_hessian_in_leaf, min_data_in_leaf, max_delta_step, path_smooth — REUSE, do not redefine)
    - .planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-RESEARCH.md §Pitfall 4 (epsilon placements) + §Pitfall 5 (default-bin skip + offset) + §Code Examples (gain math + scan gates) + §Pattern 3 (whole-kernel gain math)
    - .planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-PATTERNS.md §gain.rs (constants reuse + gain-config surface) + §kernels split scan body
  </read_first>
  <action>
    Create `crates/lgbm-compute/src/gain.rs`. Transcribe verbatim (cite source lines in comments): `ThresholdL1(s, l1) = Sign(s) * max(0.0, |s| - l1)`; `get_leaf_gain`: `USE_L1 -> ThresholdL1(g,l1)^2 / (h + l2)` else `g*g / (h + l2)`; `get_split_gains = get_leaf_gain(left) + get_leaf_gain(right)`; `calculate_splitted_leaf_output`: `USE_L1 -> -ThresholdL1(g,l1)/(h+l2)` else `-g/(h+l2)`. Define `GainConfig` extracting the seven fields from `lgbm-core::Config` (do NOT pass `&Config` into the kernel — keep the surface minimal/comptime-friendly) and `SplitInfo` (best_threshold, best_gain, left/right sum_grad/sum_hess/count/output, default-left flag). Reuse `K_EPSILON`/`K_ZERO_THRESHOLD` from `lgbm-core::types`. Compute the math in f64 (matching C++ `double` gain), preserving the f32→f64 widening of the 1e-15f literal.

    Create `crates/lgbm-compute/src/kernels/split.rs` with a `#[cube]` `find_best_split_kernel` transcribing the `feature_histogram.hpp:862-934` REVERSE scan (and the forward branch): add `2*kEpsilon` to `sum_hessian` at entry (`:172`); seed `sum_right_hessian = kEpsilon` (REVERSE); per bin high→low: skip `(t+offset)==default_bin` when SKIP_DEFAULT_BIN, accumulate, then the EXACT gate order — `if (right_count < min_data_in_leaf || sum_right_hessian < min_sum_hessian_in_leaf) continue;` `left_count = num_data - right_count;` `if (left_count < min_data_in_leaf) break;` `if (sum_left_hessian < min_sum_hessian_in_leaf) break;` `current_gain = get_split_gains(...);` `if (current_gain <= min_gain_shift) continue;` `if (current_gain > best_gain) { best_threshold = t-1+offset; best_gain = current_gain; }` (`min_gain_shift = gain_shift + min_gain_to_split`). Do NOT restructure the loop or reorder the gates (Pitfall 5). Use the single-owner ordered fold (`CubeDim::new_1d(1)`) — the cpu anchor; the scan is inherently sequential.

    Add `pub mod split;` to `kernels/mod.rs` and `Backend::find_best_split` to `lib.rs`: takes the f64 histogram, `GainConfig`, `num_bin`, `offset`/`default_bin`/`most_freq_bin`, total `sum_grad`/`sum_hess`/`num_data`; returns `Result<SplitInfo, ComputeError>`. V5-validate inputs (lengths, num_bin) before launch.
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute && cargo test -p lgbm-compute</automated>
  </verify>
  <acceptance_criteria>
    - `gain.rs` defines `ThresholdL1`, `get_leaf_gain`, `get_split_gains`, `calculate_splitted_leaf_output`, `GainConfig`, `SplitInfo`; reuses `K_EPSILON`/`K_ZERO_THRESHOLD` (grep: no local redefinition of `1e-15`/`1e-35`)
    - `split.rs` adds `2*kEpsilon` at entry and seeds `kEpsilon` (grep for `K_EPSILON` usage in both positions) and contains the SKIP_DEFAULT_BIN continue + the `t - 1 + offset` threshold recording
    - the gain math computes in f64 (no f32 gain accumulation)
    - `Backend::find_best_split` exists returning `Result<SplitInfo, ComputeError>` with V5 validation
    - `cargo test -p lgbm-compute` exits 0 (determinism_spike + capability + any new unit tests)
  </acceptance_criteria>
  <done>The gain formula lives inside `find_best_split` verbatim from C++ with exact epsilon placements, default-bin skip, and gate order — TRL-04's math implemented early (D-01a) for Phase 5 to consume.</done>
</task>

<task type="auto">
  <name>Task 2: data_partition + subtract_histograms kernels + Backend methods (A3 resolution)</name>
  <files>crates/lgbm-compute/src/kernels/partition.rs, crates/lgbm-compute/src/kernels/subtract.rs, crates/lgbm-compute/src/kernels/mod.rs, crates/lgbm-compute/src/lib.rs</files>
  <read_first>
    - LightGBM/src/treelearner/data_partition.hpp:101 (DataPartition::Split — the in-place indices reorder + leaf_begin_/leaf_count_ tracking; the stable row->{left,right} routing to mirror)
    - LightGBM/src/treelearner/serial_tree_learner.cpp (the row->leaf routing context; Dataset::Split per-row left/right decision)
    - LightGBM/src/treelearner/feature_histogram.hpp:99 (FeatureHistogram::Subtract — the kernel-layer subtraction math; A3 resolution: subtract MATH is in-scope here, which-child orchestration is Phase 5)
    - .planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-RESEARCH.md §Pitfall 6 (subtraction trick — math here, orchestration Phase 5; A3 recommends the subtract op IS in scope) + §Open Questions Q3 (data_partition output shape: mirror the C++ stable row->{left,right} reorder, Phase-5 owns leaf_begin_/leaf_count_)
    - crates/lgbm-compute/src/kernels/histogram.rs (the established `#[cube]` + single-owner-fold + V5-validation idiom to mirror)
    - .planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-PATTERNS.md §kernels/{partition} + §Kernel I/O shape
  </read_first>
  <action>
    Create `crates/lgbm-compute/src/kernels/partition.rs` with a `#[cube]` `data_partition_kernel` mirroring C++ `DataPartition::Split` (`data_partition.hpp:101`): given the binned feature column for the rows in a leaf, a split threshold, and the missing/default routing, partition rows into left/right with STABLE order (RESEARCH Open Q3 recommendation). Output reordered left/right row indices (or a stable left-then-right index array + a split point); the Phase-5 learner owns `leaf_begin_`/`leaf_count_` bookkeeping, so this op returns just the partition, not the leaf-tree state. Add `Backend::data_partition` to `lib.rs` returning the reordered indices in a `Result<_, ComputeError>` with V5 validation.

    Create `crates/lgbm-compute/src/kernels/subtract.rs` with a `#[cube]` `subtract_hist_kernel` transcribing `FeatureHistogram::Subtract` (`feature_histogram.hpp:99`): derived[i] = parent[i] - child[i] over the f64 stride-2 cells (both grad and hess). Add `Backend::subtract_histograms(parent, child) -> Result<Vec<f64>, ComputeError>` to `lib.rs` (A3: this is the histogram-layer subtraction math; which child is subtracted is Phase-5 orchestration). Validate the two histograms have equal length `2*num_bin` (V5). Use the single-owner ordered fold on cpu (no reduction nondeterminism — it is element-wise, but keep the cpu launch shape consistent).

    Add `pub mod partition;` and `pub mod subtract;` to `kernels/mod.rs`.
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute && cargo test -p lgbm-compute</automated>
  </verify>
  <acceptance_criteria>
    - `Backend::data_partition` exists returning stable-ordered left/right row indices (`Result<_, ComputeError>`); the partition mirrors `data_partition.hpp:101` (no leaf_begin_/leaf_count_ state in the return — that is Phase 5)
    - `Backend::subtract_histograms` exists computing `parent - child` over f64 stride-2 cells; validates equal `2*num_bin` lengths → `ComputeError::LengthMismatch` (V5)
    - `subtract.rs` transcribes `FeatureHistogram::Subtract` (cite line in a comment) and operates on f64 cells
    - `cargo test -p lgbm-compute` exits 0
  </acceptance_criteria>
  <done>The full Backend op set is present: data_partition (stable C++ row routing) and subtract_histograms (kernel-layer Subtract math, A3 resolved in-scope) join construct_histograms + find_best_split.</done>
</task>

<task type="auto">
  <name>Task 3: Extend kernel-capture + kernel_parity for split/partition/subtract — bit-exact on cpu</name>
  <files>xtask/cpp/kernel_capture.cpp, xtask/src/main.rs, crates/oracle-harness/tests/kernel_parity.rs, crates/oracle-harness/tests/fixtures/kernels/split.txt, crates/oracle-harness/tests/fixtures/kernels/partition.txt, crates/oracle-harness/tests/fixtures/kernels/subtract.txt</files>
  <read_first>
    - xtask/cpp/kernel_capture.cpp (current state from 04-02 — the histogram transcription + F64Bits/F32Bits serializers + CaseGen idiom to extend)
    - xtask/src/main.rs (the `kernel_capture()` fn from 04-02 — extend its output-path asserts to cover the three new fixture files)
    - LightGBM/src/treelearner/feature_histogram.hpp:711-1000 (FindBestThreshold/Split — transcribe to emit per-candidate gains + winner), :99 (Subtract)
    - LightGBM/src/treelearner/data_partition.hpp:101 (the partition routing to emit golden left/right indices)
    - crates/oracle-harness/tests/kernel_parity.rs (the histogram layer from 04-02 — add split/partition/subtract layers using the same parse/compare helpers)
    - crates/lgbm-dataset/tests/bin_storage_layout.rs (parse helpers: parse_u32_list, parse_f64_bits_list, field)
    - .planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-PATTERNS.md §kernel_parity.rs (comparator import via full module path; bit-exact cpu vs ~1e-6 hip split) + §Specifics (layered goldens so a failure localizes to accumulate vs gain-scan vs partition)
  </read_first>
  <action>
    Extend `xtask/cpp/kernel_capture.cpp`: VERBATIM-transcribe `FindBestThreshold`/`GetSplitGains`/`GetLeafGain`/`ThresholdL1` (`feature_histogram.hpp:711-1000`), the `DataPartition::Split` routing (`data_partition.hpp:101`), and `FeatureHistogram::Subtract` (`:99`), reusing the histogram inputs already generated in 04-02. Emit three LAYERED goldens (Specifics — diagnostic separation): `split.txt` (inputs: histogram cells + GainConfig fields + offset/default_bin + totals; outputs: per-candidate gains as F64Bits AND the winning SplitInfo fields — so a failure points at the gain scan, not just the winner), `partition.txt` (inputs: bins + threshold + routing; outputs: stable left/right index lists), `subtract.txt` (inputs: parent + child cells; output: derived cells). Cover the D-02a paths including default-bin skip and missing/zero routing in the split + partition cases. All emitted under `KERNEL_MASTER_SEED` for idempotency.

    Extend `xtask/src/main.rs` `kernel_capture()` to assert all four fixture files (histogram + split + partition + subtract) are written and to record them in the manifest section.

    Extend `crates/oracle-harness/tests/kernel_parity.rs` with three new layers reusing the 04-02 parse/SKIP helpers and the full-path `compare_exact_f64_bits` import: drive `find_best_split` and assert per-candidate gains AND the `SplitInfo` fields bit-exact vs `split.txt`; drive `data_partition` and assert the left/right index lists exactly match `partition.txt`; drive `subtract_histograms` and assert derived cells bit-exact vs `subtract.txt`. Label each layer so a failure localizes to gain-scan vs partition vs subtract. Keep each layer green-when-fixture-absent (SKIP).
  </action>
  <verify>
    <automated>cargo run -p xtask -- kernel-capture && git diff --exit-code crates/oracle-harness/tests/fixtures/kernels/ && cargo test -p oracle-harness --test kernel_parity && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p xtask -- kernel-capture` writes non-empty `split.txt`, `partition.txt`, `subtract.txt`; a second run yields an empty `git diff` over `tests/fixtures/kernels/` (idempotent)
    - `cargo test -p oracle-harness --test kernel_parity` exits 0; `compare_exact_f64_bits` reports 0 mismatches for the split per-candidate gains + winner, and the subtract cells; the partition index lists match exactly
    - `split.txt` records PER-CANDIDATE gains (not just the winner) so the layer is diagnostic
    - `cargo test --workspace` exits 0
    - the capability gate selects `ReducePath::Sequential` on the cpu path for these kernels (no Plane dependency on cpu)
  </acceptance_criteria>
  <done>find_best_split, data_partition, and subtract_histograms are all bit-exact vs committed C++ goldens on the cubecl-cpu anchor — the full CMP-05 kernel set is closed on the hard cpu gate (ORA-04 cpu), with the capability gate (CMP-04) selecting Sequential on cpu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-5 learner → `find_best_split`/`data_partition`/`subtract_histograms` | histogram lengths, gain config, threshold, and bin indices cross into kernels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/DoS | split/partition/subtract Backend boundary | mitigate | V5 validation: histogram length `== 2*num_bin`, equal parent/child lengths for subtract, bin indices in range, threshold within `[0,num_bin)` → typed `ComputeError`, never panic |
| T-04-06 | Tampering | gain-math FP correctness (epsilon placement) | mitigate | Transcribe `kEpsilon`/`2*kEpsilon` positions verbatim; per-candidate-gain golden catches any drift in the scan, not just the winner |
| T-04-02 | Tampering | `unsafe` kernel launch | mitigate | All `unsafe` confined to lgbm-compute with documented handle/length safety invariants per launch site |
| T-04-SC | Tampering | cargo installs | accept | No new external packages (RESEARCH audit — all Approved) |
</threat_model>

<verification>
- `cargo test --workspace` green (histogram + split + partition + subtract bit-exact on cpu)
- `cargo run -p xtask -- kernel-capture` idempotent across all four goldens
- Split golden carries per-candidate gains (diagnostic localization)
</verification>

<success_criteria>
- Full CMP-05 kernel set (`construct_histograms`/`find_best_split`/`data_partition`/`subtract_histograms`) bit-exact on cubecl-cpu (ORA-04 cpu hard gate)
- Gain math + scan + epsilon placements faithful to C++ (D-01a / TRL-04 early)
- A3 resolved: subtract_histograms in-scope at the kernel layer
- CMP-04 capability gate selects Sequential on cpu for all kernels
</success_criteria>

<output>
Create `.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-03-SUMMARY.md` when done.
</output>
