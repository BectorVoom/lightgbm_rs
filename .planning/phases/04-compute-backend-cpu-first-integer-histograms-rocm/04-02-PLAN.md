---
phase: 04-compute-backend-cpu-first-integer-histograms-rocm
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - crates/lgbm-compute/src/lib.rs
  - crates/lgbm-compute/src/kernels/histogram.rs
  - xtask/src/main.rs
  - xtask/cpp/kernel_capture.cpp
  - xtask/cpp/CMakeLists.txt
  - crates/oracle-harness/Cargo.toml
  - crates/oracle-harness/tests/kernel_parity.rs
  - crates/oracle-harness/tests/fixtures/kernels/histogram.txt
  - crates/oracle-harness/fixtures/REFERENCE_MANIFEST.md
autonomous: true
requirements: [CMP-01, CMP-02, CMP-05, ORA-04]

must_haves:
  truths:
    - "Backend::construct_histograms is a coarse whole-kernel op taking the Phase-2 binned store + f32 ordered grad/hess and returning f64 [grad,hess] stride-2 histogram cells"
    - "An xtask kernel-capture subcommand emits a committed histogram golden by header-only verbatim transcription of C++ dense_bin.hpp/sparse_bin.hpp ConstructHistogram (no C++ toolchain at test time)"
    - "The cubecl-cpu construct_histograms output is bit-exact (compare_exact_f64_bits) vs the committed C++ histogram golden across the D-02a synthetic-input path coverage"
    - "kernel-capture regenerates the golden byte-identically (idempotent — empty git diff on re-run)"
    - "The histogram golden covers dense + sparse bin layouts, most-frequent/default-bin skip, missing/zero routing, multiple bit widths, and grad/hess sign/magnitude spread"
  artifacts:
    - path: "xtask/cpp/kernel_capture.cpp"
      provides: "header-only C++ transcription of ConstructHistogram emitting f64-bit histogram goldens"
      contains: "ConstructHistogram"
    - path: "crates/oracle-harness/tests/kernel_parity.rs"
      provides: "replay of the committed histogram golden, bit-exact on cubecl-cpu"
      contains: "compare_exact_f64_bits"
    - path: "crates/oracle-harness/tests/fixtures/kernels/histogram.txt"
      provides: "committed synthetic-input histogram golden (D-02a coverage)"
      min_lines: 5
  key_links:
    - from: "crates/oracle-harness/tests/kernel_parity.rs"
      to: "lgbm_compute::Backend::construct_histograms"
      via: "drive kernel on golden inputs, compare to golden output"
      pattern: "construct_histograms"
    - from: "crates/oracle-harness/tests/kernel_parity.rs"
      to: "oracle_harness::comparator::compare_exact_f64_bits"
      via: "full module path import (lib.rs re-export gap)"
      pattern: "comparator::compare_exact_f64_bits"
    - from: "xtask/src/main.rs"
      to: "xtask/cpp/kernel_capture.cpp"
      via: "kernel-capture subcommand cmake build + run"
      pattern: "kernel-capture|kernel_capture"
---

<objective>
Deliver the FIRST complete vertical slice of the compute backend: a real `construct_histograms` whole-kernel op (D-01) wired end-to-end — `Backend` trait method → cubecl-cpu kernel (f64 accumulation, single-owner ordered fold proven in 04-01) → committed C++-transcription golden via a new `xtask kernel-capture` subcommand → bit-exact parity replay in `kernel_parity.rs`. After this slice, a real consumer (the Phase-5 learner, simulated by the parity test) can construct a histogram from the Phase-2 binned store and get C++-bit-identical f64 cells on the deterministic cpu anchor.

Purpose: Prove the histogram path — the GPU-relevant inner loop and the most FP-sensitive accumulation — at bit-exactness against C++ before fanning out to split/partition (04-03) and ROCm (04-04). Establishes the golden-capture + parity machinery the next two plans reuse.
Output: finalized `construct_histograms` Backend method, the `kernel-capture` xtask subcommand + `kernel_capture.cpp`, the committed histogram golden, and `kernel_parity.rs` (histogram layer).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-CONTEXT.md
@.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-RESEARCH.md
@.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-PATTERNS.md
@.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
See the full phase-wide symbol registry in 04-01-PLAN.md `<artifacts_this_phase_produces>`. Symbols finalized/introduced in THIS plan:
- `Backend::construct_histograms` — coarse whole-kernel method: inputs = a `lgbm-dataset` bin column (`Bin::data(idx)->u32`, `num_data()`) + f32 ordered gradient/hessian slices + `num_bin`; output = f64 histogram `Vec` / buffer laid out `[g0,h0,g1,h1,...]` (stride-2, `bin<<1`)
- `construct_hist_kernel` — finalized from the 04-01 minimal form
- `kernel-capture` xtask subcommand + `KERNEL_MASTER_SEED` const
- `xtask/cpp/kernel_capture.cpp` — header-only transcription harness (histogram layer this plan; split/partition added in 04-03)
- `crates/oracle-harness/tests/fixtures/kernels/histogram.txt` — committed golden
- `crates/oracle-harness/tests/kernel_parity.rs` — parity test (histogram layer this plan)
- oracle-harness dev-deps: `lgbm-compute`, `lgbm-dataset` added (test-only — CMP-01 boundary intact)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Finalize Backend::construct_histograms whole-kernel op over the Phase-2 binned store</name>
  <files>crates/lgbm-compute/src/lib.rs, crates/lgbm-compute/src/kernels/histogram.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/histogram.rs (current state — the 04-01 minimal `construct_hist_kernel` + single-owner fold + V5 validation)
    - crates/lgbm-compute/src/lib.rs (current state — `Backend` trait with `type Runtime: cubecl::Runtime`)
    - crates/lgbm-dataset/src/bin/mod.rs (the `Bin` trait — `data(idx)->u32`, `num_data()`; the kernel input contract, do NOT re-bin)
    - LightGBM/src/io/dense_bin.hpp:99-141 (ConstructHistogramInner — stride-2 `[grad,hess]`, `ti = bin<<1`, f64 accumulate; the authoritative body)
    - LightGBM/src/io/sparse_bin.hpp (ConstructHistogram for the sparse iterator path — the sparse layout the golden must also cover, D-02a)
    - .planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-PATTERNS.md §Kernel I/O shape (D-01 whole-kernel signature: f32 ordered grad/hess in, f64 cells out)
    - .planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-RESEARCH.md §Pitfall 3 (hist_t=double) + §Pattern 1 (host launch API)
  </read_first>
  <action>
    Add `fn construct_histograms` to the `Backend` trait in `lib.rs`. Signature (D-01 whole-kernel, faithful to `dense_bin.hpp`): takes the per-row bin indices for a feature column (sourced from a `lgbm-dataset` `Bin` via `data(idx)`), the f32 `ordered_gradients` and `ordered_hessians` slices, and `num_bin`; returns `Result<Vec<f64>, ComputeError>` where the output is the stride-2 `[grad,hess]` histogram of length `2*num_bin` indexed `ti = bin << 1` (`out[ti] += grad`, `out[ti+1] += hess`). Provide a default impl (or a concrete cpu impl) that performs the host-side V5 validation from 04-01, allocates the f64 `out` buffer (`2*num_bin` cells, threat T-04-03: confirm no overflow), launches `construct_hist_kernel` with `CubeDim::new_1d(1)` (single-owner ordered fold — the bit-exact anchor proven in 04-01), reads back via `client.read_one_unchecked`, and returns the f64 cells. Keep f64 accumulation (hist_t=double, Pitfall 3); do NOT accumulate in f32 on the cpu path. The sparse layout is driven by the same per-row bin indices the `Bin` iterator yields (reuse the Phase-2 store, do not re-bin). Confine all cubecl names to this crate (CMP-01).
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute && cargo test -p lgbm-compute determinism_spike</automated>
  </verify>
  <acceptance_criteria>
    - `Backend::construct_histograms` exists with the documented signature returning `Result<Vec<f64>, ComputeError>` (f64 cells, length `2*num_bin`)
    - the impl launches `construct_hist_kernel` with `CubeDim::new_1d(1)` and accumulates into f64 (grep confirms `Array<f64>` / `f64` accumulation, no f32 cell)
    - the 04-01 `determinism_spike` still passes against the finalized kernel (no regression of the bit-exact fold)
    - host-side validation (`bin < num_bin`, length consistency) precedes launch and returns `ComputeError` (V5)
  </acceptance_criteria>
  <done>`construct_histograms` is a callable coarse whole-kernel Backend op producing f64 stride-2 histogram cells from the Phase-2 binned store + f32 grad/hess, on the bit-exact cubecl-cpu fold.</done>
</task>

<task type="auto">
  <name>Task 2: xtask kernel-capture subcommand + header-only ConstructHistogram transcription emitting the committed golden</name>
  <files>xtask/src/main.rs, xtask/cpp/kernel_capture.cpp, xtask/cpp/CMakeLists.txt, crates/oracle-harness/tests/fixtures/kernels/histogram.txt, crates/oracle-harness/fixtures/REFERENCE_MANIFEST.md</files>
  <read_first>
    - xtask/src/main.rs (the `bin_capture()` fn ~lines 201-333 — the EXACT structure to copy: subcommand match arm, master-seed const, workspace_root → verify_toolchain → header-exists assert → cmake configure + build target → locate_exe → run → assert outputs → write_manifest; and the `write_manifest` fn ~549-779)
    - xtask/cpp/bin_capture.cpp (lines 9-37 header-only rationale; 1061-1071 F64Bits/F32Bits serializers; 1073-1095 CaseGen/CaseSpec/EmitCase; the existing IBin/DenseBin/SparseBin/BinMapper transcriptions ~588-714, 110-147 to REUSE as the bin-storage side of the histogram input)
    - xtask/cpp/CMakeLists.txt (the `bin_capture` target block to copy verbatim for a `kernel_capture` target)
    - LightGBM/src/io/dense_bin.hpp:99-141 + LightGBM/src/io/sparse_bin.hpp (ConstructHistogram bodies to transcribe verbatim)
    - LightGBM/include/LightGBM/bin.h:180-258 (offset/default_bin_/most_freq_bin_ semantics that drive most-frequent/default-bin skip — D-02a path coverage)
    - .planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-PATTERNS.md §xtask/cpp/kernel_capture.cpp + §xtask/src/main.rs (the step-for-step copy guide + the fixture output dir `crates/oracle-harness/tests/fixtures/kernels`)
  </read_first>
  <action>
    Create `xtask/cpp/kernel_capture.cpp` mirroring `bin_capture.cpp` discipline: copy the top-of-file header-only rationale (retargeted: `feature_histogram.cpp`/`dense_bin.hpp` pull `common.h` → unvendored `external_libs`, so transcribe header-only against `std` + the header-only `LightGBM::Random`). REUSE the `IBin`/`DenseBin`/`SparseBin`/`BinMapper` transcriptions already in `bin_capture.cpp` as the bin-storage input. VERBATIM-transcribe `ConstructHistogram` from `dense_bin.hpp:99-141` (and the `sparse_bin.hpp` iterator variant) with cited source line ranges in comments: `ti = static_cast<uint32_t>(data(idx)) << 1; grad[ti] += ordered_gradients[i]; hess[ti] += ordered_hessians[i];` accumulating into `double` (hist_t). Generate synthetic inputs per D-02a: dense + sparse bin layouts, most-frequent/default-bin skip cases, missing/zero routing, multiple bit widths (u8/u16/u32 paths), and a grad/hess sign/magnitude spread that stresses the f64 reduction. Emit a LAYERED `histogram.txt` golden: one `#`-comment-delimited record per case carrying the inputs (bins, grad/hess as F32Bits) and the f64 histogram cells as F64Bits raw u64 (copy `F64Bits`/`F32Bits` from `bin_capture.cpp:1061-1071`). Single `KERNEL_MASTER_SEED` source of randomness for idempotency.

    Modify `xtask/cpp/CMakeLists.txt`: add `add_executable(kernel_capture kernel_capture.cpp)` + `target_include_directories(kernel_capture PRIVATE "${LIGHTGBM_DIR}/include")` mirroring the `bin_capture` block (same 3.28/C++11/`-DLIGHTGBM_DIR` guard; no external_libs include).

    Modify `xtask/src/main.rs`: add `Some("kernel-capture") => kernel_capture(),` to the match (and the usage strings); add `const KERNEL_MASTER_SEED` next to `BIN_MASTER_SEED`; write `fn kernel_capture()` copying `bin_capture()` step-for-step — `workspace_root()` → `verify_toolchain()` → assert `LightGBM/include/LightGBM/utils/random.h` exists → cmake configure + `cmake --build --target kernel_capture` (reuse `target/xtask-cpp-build`) → `locate_exe(&build_dir, "kernel_capture")` → run with the fixture output path (`root.join("crates/oracle-harness/tests/fixtures/kernels")`, `create_dir_all`) + the seed arg → assert `histogram.txt` written → `write_manifest()` → print the idempotency-check reminder. Extend `write_manifest` with a "## Kernel Golden Set (Phase 4)" section in `REFERENCE_MANIFEST.md` recording `KERNEL_MASTER_SEED`, the D-02a path coverage, and the capture command (pure function of recorded constants — idempotent). Run the subcommand once to emit + commit `histogram.txt`.
  </action>
  <verify>
    <automated>cargo run -p xtask -- kernel-capture && git diff --exit-code crates/oracle-harness/tests/fixtures/kernels/histogram.txt && test -s crates/oracle-harness/tests/fixtures/kernels/histogram.txt</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p xtask -- kernel-capture` exits 0 and writes a non-empty `crates/oracle-harness/tests/fixtures/kernels/histogram.txt`
    - a SECOND `cargo run -p xtask -- kernel-capture` produces an empty `git diff` on `histogram.txt` (idempotent regen)
    - `kernel_capture.cpp` transcribes `ConstructHistogram` accumulating into `double` (grep for `double` cells; grep that no `external_libs` include is added)
    - the golden contains >=1 dense AND >=1 sparse case and a most-frequent/default-bin-skip case (D-02a coverage — verify via the case-spec comments)
    - `REFERENCE_MANIFEST.md` gains a "Kernel Golden Set (Phase 4)" section recording `KERNEL_MASTER_SEED`
  </acceptance_criteria>
  <done>The histogram golden is C++-bit-faithfully captured by header-only transcription, committed under the oracle-harness crate, and regenerates idempotently with no C++ toolchain needed at test time.</done>
</task>

<task type="auto">
  <name>Task 3: kernel_parity.rs — bit-exact histogram replay on the cubecl-cpu anchor</name>
  <files>crates/oracle-harness/Cargo.toml, crates/oracle-harness/tests/kernel_parity.rs</files>
  <read_first>
    - crates/oracle-harness/Cargo.toml (current — only `lgbm-core` dev-dep; the `oracle-harness` LIBRARY must stay cubecl-free, dev-deps are test-only)
    - crates/oracle-harness/src/comparator.rs (the re-export gap: `lib.rs:10` re-exports only `compare_within`/`abs_diff_within`/`Mismatch`/`ORACLE_TOL`; `compare_exact_f64_bits` is at `comparator.rs:150` and must be imported via the full module path)
    - crates/lgbm-dataset/tests/bin_storage_layout.rs (lines ~30-81 — the EXACT parse/replay/compare structure: `fixture_path()` via `env!("CARGO_MANIFEST_DIR")`, `field()`/`parse_u64`/`parse_f64_bits_list` (f64 via `from_bits`), and the `use oracle_harness::comparator::compare_exact_f64_bits` full-path import at line ~30)
    - crates/oracle-harness/tests/rng_parity.rs (lines ~52-61 — the graceful-SKIP-when-fixture-absent idiom keeping `cargo test` green pre-capture)
    - crates/lgbm-compute/src/lib.rs (the `Backend::construct_histograms` method to drive)
  </read_first>
  <action>
    Add `lgbm-compute = { path = "../lgbm-compute" }` and `lgbm-dataset = { path = "../lgbm-dataset" }` to `crates/oracle-harness/Cargo.toml` `[dev-dependencies]` (test-only; the library crate stays cubecl-free so CMP-01 holds). Create `crates/oracle-harness/tests/kernel_parity.rs`. Copy the `fixture_path()` (`env!("CARGO_MANIFEST_DIR").join("tests/fixtures/kernels/histogram.txt")`), the line-parse helpers (`field`, `parse_u32_list`, `parse_f64_bits_list` via `f64::from_bits`), and the graceful-SKIP idiom from `bin_storage_layout.rs`/`rng_parity.rs` verbatim. Import the exact comparator via the full module path: `use oracle_harness::comparator::compare_exact_f64_bits;` (NOT the re-export — it is not re-exported). For each golden record: parse the inputs (bins + f32 grad/hess from bit-lists), drive `lgbm_compute`'s cubecl-cpu `construct_histograms` over those inputs, and assert the resulting f64 cells are BIT-EXACT vs the golden f64 cells via `compare_exact_f64_bits` (the D-04 hard cpu gate). Report the first divergence with its case label so a failure localizes to a specific D-02a path. Keep the test green-when-fixture-absent (SKIP with a "run kernel-capture" message), exactly as `rng_parity.rs`.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test kernel_parity && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness --test kernel_parity` exits 0 with the committed `histogram.txt` present; `compare_exact_f64_bits` reports 0 mismatches vs the golden for every case
    - the test imports `oracle_harness::comparator::compare_exact_f64_bits` via the full module path (grep confirms — not the re-export)
    - `cargo test --workspace` exits 0 (no regression of prior-phase tests)
    - `oracle-harness/Cargo.toml` adds `lgbm-compute`/`lgbm-dataset` only under `[dev-dependencies]` (the library `[dependencies]` stays cubecl-free — CMP-01)
    - deleting/renaming the fixture makes the test SKIP (green), not panic
  </acceptance_criteria>
  <done>The cubecl-cpu histogram kernel is proven bit-exact vs the committed C++ golden across the D-02a path coverage — the first full vertical slice is closed and ORA-04's cpu hard gate covers the histogram layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parity test / Phase-5 learner → `construct_histograms` | bin indices + grad/hess slice lengths cross into the kernel launch |
| committed fixture → test | trusted (capture-time C++); never references the untracked LightGBM/ tree at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering/DoS | `construct_histograms` Backend boundary | mitigate | V5 validation from 04-01 retained: `bin < num_bin`, `grad.len()==hess.len()==binned.len()`, `out.len()==2*num_bin` → typed `ComputeError` before launch |
| T-04-03 | Tampering | `2*num_bin` `out` allocation + `bin<<1` index math | mitigate | u32 widening as C++; verify `2*num_bin` allocation cannot overflow / under-size the histogram buffer |
| T-04-05 | Tampering | golden provenance | mitigate | Goldens are C++-transcription-captured and COMMITTED under a tracked crate dir; never `git add` the untracked LightGBM/ tree; idempotent regen guards silent fixture drift |
| T-04-SC | Tampering | cargo installs | accept | No new external packages; lgbm-compute/lgbm-dataset are workspace path dev-deps only (RESEARCH audit — all Approved) |
</threat_model>

<verification>
- `cargo test --workspace` green (histogram parity bit-exact on cpu)
- `cargo run -p xtask -- kernel-capture` idempotent (empty git diff on re-run)
- The histogram golden covers the D-02a paths (dense + sparse + default-bin skip + width variants + grad/hess spread)
</verification>

<success_criteria>
- `construct_histograms` whole-kernel op callable end-to-end (CMP-05 histogram layer)
- cubecl-cpu histogram output bit-exact vs committed C++ golden (CMP-02, ORA-04 cpu gate)
- kernel-capture harness + parity machinery established for 04-03/04-04 reuse
- CMP-01 boundary intact (oracle-harness library stays cubecl-free)
</success_criteria>

<output>
Create `.planning/phases/04-compute-backend-cpu-first-integer-histograms-rocm/04-02-SUMMARY.md` when done.
</output>
