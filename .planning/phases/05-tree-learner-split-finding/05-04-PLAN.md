---
phase: 05-tree-learner-split-finding
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - crates/lgbm-treelearner/src/lib.rs
  - crates/lgbm-treelearner/src/learner.rs
  - crates/lgbm-treelearner/src/col_sampler.rs
  - xtask/cpp/learner_capture.cpp
  - xtask/src/main.rs
  - crates/oracle-harness/tests/learner_parity.rs
  - crates/oracle-harness/tests/fixtures/learner/col_wise.txt
  - crates/oracle-harness/tests/fixtures/learner/col_sampler.txt
  - crates/oracle-harness/tests/fixtures/learner/real_gh.txt
  - REFERENCE_MANIFEST.md
autonomous: true
requirements: [TRL-08, TRL-09]
must_haves:
  truths:
    - "force_col_wise and force_row_wise produce bit-identical trees to each other AND to the C++ reference on the deterministic anchor"
    - "Per-tree and per-node feature subsampling (feature_fraction, feature_fraction_bynode, feature_fraction_seed) selects the same features via Random::Sample call-sequence parity"
    - "The learner grows the same tree as C++ on captured real first-iteration g/h from a real dataset (realistic distribution)"
  artifacts:
    - path: "crates/lgbm-treelearner/src/col_sampler.rs"
      provides: "ColSampler ResetByTree/GetByNode reproducing the C++ Random::Sample draw sequence + call order"
      contains: "ColSampler"
    - path: "crates/oracle-harness/tests/fixtures/learner/col_wise.txt"
      provides: "force_col_wise golden proving tree-equality with force_row_wise + C++"
    - path: "crates/oracle-harness/tests/fixtures/learner/col_sampler.txt"
      provides: "feature-subsampling RNG parity golden (selected feature indices per tree/node)"
    - path: "crates/oracle-harness/tests/fixtures/learner/real_gh.txt"
      provides: "captured real first-iteration g/h corpus + reference tree (D-03)"
  key_links:
    - from: "crates/lgbm-treelearner/src/col_sampler.rs"
      to: "lgbm_core::Random"
      via: "sample(n,k) + call-sequence parity"
      pattern: "Random|\\.sample\\("
    - from: "crates/lgbm-treelearner/src/learner.rs"
      to: "crates/lgbm-treelearner/src/col_sampler.rs"
      via: "is_feature_used_bytree + GetByNode per-node draws"
      pattern: "col_sampler|ColSampler"
    - from: "crates/oracle-harness/tests/learner_parity.rs"
      to: "crates/oracle-harness/tests/fixtures/learner/real_gh.txt"
      via: "captured-g/h full-tree replay"
      pattern: "real_gh\\.txt"
---

<objective>
Layer the two parity additions onto the proven Phase-5 spine (D-04: spine first, then additions): `force_col_wise` (TRL-09) and per-tree/per-node feature subsampling (TRL-08), and add the D-03 captured-real-g/h corpus that proves the learner grows the same tree as C++ under a REALISTIC gradient/hessian distribution (not just hand-crafted synthetic).

Purpose: TRL-09 asserts `force_row_wise` and `force_col_wise` produce BIT-IDENTICAL trees (the two strategies differ only in histogram-build ORDER, not result — Pitfall 5; A1 predicts they are observationally identical at the single-thread Backend layer, which this plan empirically confirms and documents). TRL-08 reproduces the C++ `ColSampler` RNG draw SEQUENCE (the PRNG itself is already bit-exact, FND-01 — the risk is call order: ResetByTree once per tree, GetByNode once per node for the smaller leaf then the larger). D-03 adds the captured first-iteration g/h (regression-l2 `grad=score-label`, `hess=1`, `boost_from_average=false` recommended; plus a binary-logloss case for sign/magnitude spread) on the committed Phase-2/3 example matrices, avoiding NaN-missing columns (ties to A5).

Output: `col_sampler.rs` + the learner wiring for both strategies and subsampling; the `col_wise.txt`/`col_sampler.txt`/`real_gh.txt` goldens; and the parity tests (row==col tree equality, RNG selection parity, captured-g/h full-tree). The `force_col_wise` finding (whether it needs a distinct compute path or is a config-flag no-op at the Backend layer) is documented in the SUMMARY + manifest (Open Q2 resolution).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tree-learner-split-finding/05-RESEARCH.md
@.planning/phases/05-tree-learner-split-finding/05-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

New symbols (parity additions):
- `lgbm_treelearner::col_sampler::ColSampler` — `reset_by_tree()`, `get_by_node()`, `is_feature_used_bytree()`, `get_cnt(total, fraction)` (transcribing col_sampler.hpp:33-37,74-89,91-179) using `lgbm_core::Random::sample`.
- `SerialTreeLearner` extension: a histogram-build strategy field/flag (`force_row_wise`/`force_col_wise`) and `ColSampler` integration in `find_best_splits` (per-tree + per-node draws in the exact C++ order: smaller leaf then larger leaf).
- New golden files: `tests/fixtures/learner/col_wise.txt`, `col_sampler.txt`, `real_gh.txt`.
- New parity tests in `learner_parity.rs`: `learner_parity_row_vs_col` (tree equality both strategies + vs C++), `learner_parity_col_sampler_rng` (selected feature indices per tree/node), `learner_parity_real_gh_full_tree` (captured-g/h D-07 full tree).
- `xtask/cpp/learner_capture.cpp` extension: emits the col_wise / col_sampler / real_gh fixtures (the real_gh g/h captured via the `$LGBM_CAPTURE_PYTHON` pip path OR a header-only regression/binary objective transcription producing the same iteration-1 g/h).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ColSampler + force_col_wise wiring (TRL-08, TRL-09)</name>
  <files>crates/lgbm-treelearner/src/col_sampler.rs, crates/lgbm-treelearner/src/learner.rs, crates/lgbm-treelearner/src/lib.rs</files>
  <read_first>
    - crates/lgbm-treelearner/src/learner.rs (the Plan-03 find_best_splits + the loop — the is_feature_used gate + the build-strategy branch point)
    - crates/lgbm-core/src/random.rs (Random::sample ~87-120, next_short/next_int/next_float — the bit-exact PRNG to drive ColSampler)
    - .planning/phases/05-tree-learner-split-finding/05-RESEARCH.md (Code Examples §ColSampler col_sampler.hpp:74-89 ResetByTree, :33-37 GetCnt, :91-179 GetByNode; draw order serial_tree_learner.cpp:479,487; Pitfall 5 force_col_wise==force_row_wise; A1; Open Q2)
    - .planning/phases/05-tree-learner-split-finding/05-PATTERNS.md (§col_sampler.rs — call-sequence parity is the risk, not the PRNG)
    - crates/lgbm-compute/src/lib.rs (construct_histograms — to confirm whether force_col_wise routes identically at the Backend layer per A1)
  </read_first>
  <action>
    Create `col_sampler.rs` transcribing the C++ `ColSampler`: `get_cnt(total, fraction) = max(round_int(total*fraction), min(1,total))` (col_sampler.hpp:33-37); `reset_by_tree()` drawing `used_feature_indices_ = random_.sample(valid_feature_indices_.len(), used_cnt_bytree)` ONLY when `feature_fraction < 1.0` (col_sampler.hpp:74-89); `get_by_node()` drawing AGAIN with `random_.sample` for `feature_fraction_bynode < 1.0` (col_sampler.hpp:91-179); and `is_feature_used_bytree()`. Drive ALL randomness through `lgbm_core::Random` seeded by `feature_fraction_seed` — the parity risk is the CALL SEQUENCE (RESEARCH §ColSampler / Pitfall): `reset_by_tree` once per tree (BeforeTrain), `get_by_node` once per node for the SMALLER leaf then the LARGER leaf (serial_tree_learner.cpp:479,487) — reproduce that exact order. Wire `ColSampler` into the learner's `find_best_splits`: `is_feature_used[f] = col_sampler.is_feature_used_bytree(f) && bynode_selected && splittable`. For `force_col_wise` (TRL-09): add the build-strategy flag to `SerialTreeLearner`; per A1/Open Q2, the Phase-4 `construct_histograms` whole-kernel op likely routes both strategies identically on the deterministic single-thread anchor — implement `force_col_wise` so it produces the SAME f64 histogram cells (a config flag, not a new kernel, if A1 holds). If the Backend layer turns out to require a distinct column-major accumulation to match (A1 wrong), that is a Phase-4-boundary re-open — STOP and flag it in the SUMMARY rather than silently shipping a divergent tree. Default config (feature_fraction=1.0) must leave the spine behavior from Plan 03 unchanged (no draw, all features used).
  </action>
  <verify>
    <automated>cargo test -p lgbm-treelearner col_sampler</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ColSampler' crates/lgbm-treelearner/src/col_sampler.rs` >= 1 and it exposes `reset_by_tree`/`get_by_node`/`get_cnt`
    - A unit test asserts `get_cnt(total, fraction) == max(round_int(total*fraction), min(1,total))` on hand-computed cases (incl. the `min(1,total)` floor)
    - A unit test asserts `feature_fraction == 1.0` draws NOTHING (no RNG advance — the spine path) and `feature_fraction < 1.0` advances the RNG exactly once per ResetByTree
    - A unit test asserts the GetByNode draw order: smaller-leaf draw precedes larger-leaf draw within one node (call-sequence parity)
    - `cargo test -p lgbm-treelearner` exits 0; the Plan-03 spine tests still pass (feature_fraction=1.0 unchanged)
  </acceptance_criteria>
  <done>ColSampler reproduces the C++ GetCnt + ResetByTree/GetByNode draw sequence via the bit-exact Random, the learner gates features on it, force_col_wise is wired to produce identical histogram cells (or the A1-violation is flagged), and the default no-subsampling spine is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: col_wise + col_sampler + real_gh goldens + parity tests (D-03, TRL-08/09 validation)</name>
  <files>xtask/cpp/learner_capture.cpp, xtask/src/main.rs, crates/oracle-harness/tests/learner_parity.rs, crates/oracle-harness/tests/fixtures/learner/col_wise.txt, crates/oracle-harness/tests/fixtures/learner/col_sampler.txt, crates/oracle-harness/tests/fixtures/learner/real_gh.txt, REFERENCE_MANIFEST.md</files>
  <read_first>
    - xtask/cpp/learner_capture.cpp (the Plan-03 full-learner transcription to extend with col_wise/col_sampler/real_gh cases)
    - xtask/src/main.rs (learner_capture() + the $LGBM_CAPTURE_PYTHON model-capture path used in Phase 3 for the real-g/h capture; the byte-idempotent seed discipline)
    - crates/oracle-harness/tests/learner_parity.rs (the Plan-03 tests + parse helpers to extend; compare_exact_f64_bits + Tree::to_string String equality)
    - .planning/phases/05-tree-learner-split-finding/05-RESEARCH.md (D-03 captured-g/h recommendation: regression-l2 iter-1 grad=score-label hess=1 boost_from_average=false on the committed regression example matrix + a binary-logloss case; Open Q3; A4 pip-vs-header-only; Pitfall 5 row==col)
    - tests/fixtures/examples (the committed Phase-2/3 example matrices reused as the real dataset; confirm columns avoid NaN-missing per A5)
  </read_first>
  <action>
    Extend `learner_capture.cpp` to emit three goldens: (1) `col_wise.txt` — the SAME synthetic corpus from Plan 03 grown under `force_col_wise=true`, emitting the grown tree's `to_string()` (for the row==col equality assert). (2) `col_sampler.txt` — a `feature_fraction<1.0` + `feature_fraction_bynode<1.0` config with a fixed `feature_fraction_seed`, emitting the SELECTED feature indices per tree (ResetByTree) and per node (GetByNode smaller then larger) as the RNG-parity golden. (3) `real_gh.txt` — captured real iteration-1 g/h: regression-l2 (`grad=score-label`, `hess=1`, `boost_from_average=false`) on the committed regression example matrix, plus a binary-logloss case for sign/magnitude spread, choosing columns WITHOUT NaN-missing (A5). Capture the g/h via the existing `$LGBM_CAPTURE_PYTHON` pip path (Phase-3 precedent, A4) OR a header-only objective transcription that produces the same deterministic iteration-1 g/h — pick one and record it in the manifest; emit the captured g/h + the C++-reference grown tree `to_string()`. Record all seeds for byte-idempotent regen; refresh `REFERENCE_MANIFEST.md` "Learner Golden Set" with the D-04 row/col + D-03 g/h source + feature_fraction_seed. In `learner_parity.rs` add: `learner_parity_row_vs_col` (grow the corpus under BOTH strategies via `SerialTreeLearner`, assert `row_tree.to_string() == col_tree.to_string()` AND both == the committed golden — String equality, Pitfall 5); `learner_parity_col_sampler_rng` (assert the selected feature indices per tree/node match `col_sampler.txt` exactly — the draw-sequence parity); `learner_parity_real_gh_full_tree` (grow from the captured g/h, assert the full tree `to_string()` is byte-identical to the `real_gh.txt` reference tree — D-07 under realistic distribution). Graceful SKIP when a golden/`$LGBM_CAPTURE_PYTHON` is absent; never reference `LightGBM/`.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test learner_parity</automated>
  </verify>
  <acceptance_criteria>
    - `crates/oracle-harness/tests/fixtures/learner/{col_wise,col_sampler,real_gh}.txt` all exist
    - `cargo test -p oracle-harness --test learner_parity learner_parity_row_vs_col` exits 0 (force_row_wise tree == force_col_wise tree == C++, String equality — TRL-09)
    - `cargo test -p oracle-harness --test learner_parity learner_parity_col_sampler_rng` exits 0 (selected feature indices per tree/node match — TRL-08 RNG call-sequence parity)
    - `cargo test -p oracle-harness --test learner_parity learner_parity_real_gh_full_tree` exits 0 (captured-g/h full tree byte-identical — D-03 + D-07 under realistic distribution)
    - `grep -c 'LightGBM/' crates/oracle-harness/tests/learner_parity.rs` == 0
    - Re-running `cargo run -p xtask -- learner-capture` (toolchain/pip present) yields an empty `git diff` on the three new goldens; tests SKIP gracefully when absent
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>force_row_wise and force_col_wise produce bit-identical trees to each other and to C++; feature subsampling selects the same features via Random::Sample call-sequence parity; and the learner grows the same tree as C++ on captured real iteration-1 g/h — all replayed bit-exact from committed goldens that never touch LightGBM/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| learner → `ColSampler` / `Random` | feature_fraction_seed + draw counts cross into the PRNG; a wrong call sequence silently selects different features (parity bug, not a security breach) |
| capture toolchain / `$LGBM_CAPTURE_PYTHON` → committed golden | the pip/header-only emitter writes raw-bit fixtures; absent toolchain SKIPs, never silently passes; LightGBM/ never git-add-ed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Information disclosure (silent wrong tree) | ColSampler draw sequence | mitigate | col_sampler.txt asserts the exact selected feature indices per tree/node; a wrong draw order fails the parity gate rather than silently shipping a divergent tree |
| T-05-04-02 | Tampering (config drift) | force_col_wise build path | mitigate | row==col tree-equality gate; if A1 is violated (force_col_wise needs a distinct compute path), the task STOPS and flags a Phase-4 boundary re-open rather than shipping a divergent tree |
| T-05-04-03 | Information disclosure (silent wrong answer) | committed goldens + $LGBM_CAPTURE_PYTHON | mitigate | graceful SKIP only when truly absent; bit-exact assertion when present; never silently pass on a missing fixture |
| T-05-04-04 | Tampering | git tracking of LightGBM/ | mitigate | harness paths CARGO_MANIFEST_DIR-rooted; grep gate asserts no LightGBM/ reference; goldens committed under tests/fixtures/ |
| T-05-04-SC | Tampering | npm/pip/cargo installs | mitigate | No new Rust packages (RESEARCH Package Legitimacy Audit: zero new dependency surface); pip-lightgbm is a Phase-3 capture-time-only tool, not a runtime/test dependency |
</threat_model>

<verification>
- `cargo test -p lgbm-treelearner` exits 0 (col_sampler unit tests; spine unchanged at feature_fraction=1.0).
- `cargo test -p oracle-harness --test learner_parity` exits 0 (row==col, col_sampler RNG, real-g/h full tree).
- `cargo test --workspace` exits 0; CMP-01 containment guard still green.
- The `force_col_wise` finding (config-flag no-op vs distinct path, Open Q2) is recorded in the SUMMARY + REFERENCE_MANIFEST.md.
</verification>

<success_criteria>
- `force_col_wise` == `force_row_wise` == C++ tree (TRL-09), bit-identical via `%.17g` String equality.
- Per-tree/per-node feature subsampling selects the same features via `Random::Sample` call-sequence parity (TRL-08).
- The learner grows the same tree as C++ on captured real first-iteration g/h (D-03, realistic distribution, D-07 full tree).
- Default config (feature_fraction=1.0, force_row_wise) leaves the Plan-03 spine behavior unchanged.
</success_criteria>

<output>
Create `.planning/phases/05-tree-learner-split-finding/05-04-SUMMARY.md` when done.
</output>
