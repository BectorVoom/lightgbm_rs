---
phase: 18-on-device-data-partition-tree-mutation-prediction
plan: 02
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - crates/lgbm-compute/src/kernels/data_partition.rs
  - crates/lgbm-compute/src/kernels/histogram_arena.rs
  - crates/lgbm-compute/src/kernels/mod.rs
  - crates/oracle-harness/tests/partition_parity.rs
autonomous: true
requirements: [ODL-13]

must_haves:
  truths:
    - "The cpu f64 anchor partitions a leaf's index slice as a plain single-owner STABLE partition (left-keepers first in original order, then right-keepers in original order), order-equivalent to §9's block-tiled scatter (D-04 CONFIRMED)"
    - "The device mark→prefix-sum→scatter (GenDataToLeftBitVector + PrepareOffset + AggregateBlockOffset + SplitInner + CopyDataIndices) reproduces that exact permutation — never sorting (§17)"
    - "The full comptime flag fan-out (MIN_IS_MAX, MISSING_IS_ZERO, MISSING_IS_NA, MFB_IS_ZERO, MFB_IS_NA, MAX_TO_LEFT, USE_MIN_BIN, BIN_TYPE) routes missing/default rows to the reference direction (D-02)"
    - "Categorical membership routing (GenDataToLeftBitVector_Categorical via FindInBitset on bin−min_bin+mfb_offset) matches the PCAT golden (D-03)"
    - "The 16-int SplitTreeStructure child-stats packet fields match the PPACKET golden (D-08)"
    - "The HistArena leaf-indexed pool swap assigns the larger child the parent buffer and the smaller child a fresh slot, so the subtraction-trick reuse is correct (D-09, §17)"
  prohibitions:
    - "Never sort to partition — strictly mark → prefix-sum → scatter (§17)"
    - "No GPU-vs-GPU comparison — anchor only to the cpu f64 fold (D-12/def-f8u-01)"
    - "No f64-typed row-indexed arrays in the mark/scatter per-row loops — integer/native-width only (D-14)"
    - "No per-split device allocation — scatter scratch, per-block count buffers, and the 16-int packet are pre-allocated once outside the hot loop (D-15)"
    - "Do NOT extend or rebuild the host-gather partition.rs (D-01) or rebuild the HistArena rotation (D-09)"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/data_partition.rs"
      provides: "§9 device mark→prefix-sum→scatter partition (numeric + categorical) + 16-int packet + cpu f64 stable-partition anchor + shared route #[cube] fn"
      min_lines: 200
    - path: "crates/lgbm-compute/src/kernels/histogram_arena.rs"
      provides: "leaf-indexed whole-pool swap() alongside the existing rotate()"
      contains: "fn swap"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/data_partition.rs"
      to: "crates/lgbm-compute/src/kernels/primitives.rs"
      via: "u16 inclusive PrepareOffset scan + u32 exclusive AggregateBlockOffset scan (from 18-01)"
      pattern: "prefix_sum"
    - from: "crates/lgbm-compute/src/kernels/data_partition.rs"
      to: "crates/lgbm-compute/src/kernels/split_info.rs"
      via: "SplitScalars = CUDASplitInfo read for the 16-int packet + default_left routing"
      pattern: "SplitScalars"
    - from: "crates/oracle-harness/tests/partition_parity.rs"
      to: "crates/oracle-harness/tests/fixtures/kernels/partition.txt"
      via: "PORDER / PCAT / PPACKET golden replay vs the cpu fold"
      pattern: "PORDER"
---

<objective>
Build the NEW §9-faithful on-device data-partition path in `data_partition.rs` (parallel to the shipped
host-gather `partition.rs`, D-01): the shared comptime route decision, the `mark → prefix-sum → scatter`
row permutation with the full flag fan-out (D-02), categorical membership routing (D-03), the 16-int
`SplitTreeStructure` child-stats packet (D-08), the cpu f64 stable-partition anchor (D-04 CONFIRMED), and
the `HistArena` leaf-indexed whole-pool swap for the subtraction-trick reuse (D-09). Un-ignore the
`partition_parity` order/cat/packet cells + the `histogram_arena::swap` unit test.

Purpose: ODL-13 — the row router that eliminates the host partition round-trip. Row order fixes per-leaf
f32 accumulation order (§17), so the post-scatter permutation must match the reference bit-for-bit.
Output: `data_partition.rs`, extended `histogram_arena.rs`, un-ignored `partition_parity` cells.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-RESEARCH.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-PATTERNS.md
@crates/lgbm-compute/src/kernels/partition.rs
@crates/lgbm-compute/src/kernels/primitives.rs
@crates/lgbm-compute/src/kernels/histogram_arena.rs
@crates/lgbm-compute/src/kernels/split_info.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared comptime route #[cube] + GenDataToLeftBitVector numeric mark + PrepareOffset block scan + cpu f64 stable-partition anchor</name>
  <files>crates/lgbm-compute/src/kernels/data_partition.rs, crates/lgbm-compute/src/kernels/mod.rs, crates/oracle-harness/tests/partition_parity.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/partition.rs — `data_partition_kernel<B: Int>` route (:44-94, the MissingType::None numeric base case: `th = threshold + min_bin`, `default_to_right = most_freq_bin > threshold`), the branchless `select` route (:88-92, SP-2), the native-width dispatch macro (:371-393), and `gather_route` (:290-304) — the EXACT stable-partition anchor shape to reuse for D-04.
    - RESEARCH "GenDataToLeftBitVector Flag Fan-Out → CubeCL comptime" table + the shared-decision note (:160, the `(missing_type==1 && bin==default_bin) || (missing_type==2 && bin==max_bin) → default_left ? left : right` else `bin<=threshold` form) — transcribe ONCE as a `#[cube]` inline fn taking the comptime flags.
    - RESEARCH "D-04 Gate: Order-Equivalence RESOLVED — CONFIRMED" (:62-78) + the cpu-anchor code example (:355-366) — the anchor is a plain exact stable partition.
    - crates/lgbm-compute/src/kernels/primitives.rs — the u16 inclusive scan driver added in 18-01 (PrepareOffset per-block).
    - PATTERNS SP-1/SP-2/SP-4 + the data_partition.rs section (:94-135).
    - LightGBM-release-4.6.0.99/src/treelearner/cuda/cuda_data_partition.cu:290 (GenDataToLeftBitVector), :52 (PrepareOffset).
  </read_first>
  <behavior>
    - Route: a numeric bin at/below threshold routes left, above routes right; an out-of-[min,max] bin folds to the default/most-freq direction; a missing/NA bin (per the comptime MISSING flags) routes by default_left — identical logic to the predict tree-walk (one shared fn).
    - cpu anchor: over a leaf's index slice, left-keepers appear first in original relative order, then right-keepers in original relative order; `to_left_total == count(route_left)`.
    - PrepareOffset: each thread's inclusive left-rank is written to the per-block offset buffer; per-block left/right totals are emitted.
  </behavior>
  <action>
    Create `data_partition.rs`. Transcribe the shared route decision ONCE as a `pub(crate) #[cube] fn`
    taking the eight comptime flags (MIN_IS_MAX, MISSING_IS_ZERO, MISSING_IS_NA, MFB_IS_ZERO, MFB_IS_NA,
    MAX_TO_LEFT, USE_MIN_BIN as comptime bools; BIN_TYPE as the comptime column type) plus the runtime
    threshold/min_bin/max_bin/default_bin/most_freq_bin — using branchless `select` stores only (SP-2,
    cubecl-cpu MLIR constraint). This fn is the single source both this mark kernel and the 18-04 predict
    walk call (Pitfall 4).

    Implement the `GenDataToLeftBitVector` mark kernel: per row read the native-width bin (u8/u16/u32
    dispatch macro copied from partition.rs:371-393), compute `to_left` via the shared route fn, then run
    the PrepareOffset per-block inclusive scan (the 18-01 u16 driver) to produce `block_to_left_offset` +
    per-block left/right totals. Pre-allocate the offset/count buffers once (SP-3/D-15).

    Implement the cpu f64 anchor as the plain stable partition over `data_indices[leaf_start..leaf_end]`
    (D-04 CONFIRMED) — reuse the `gather_route` two-pass shape from partition.rs:290-304. Validate inputs at
    the host boundary (num_bin>0, threshold<num_bin, every bin<num_bin; SP-4) before any `launch_unchecked`.

    Un-ignore the `partition_parity` `order` cell: parse PCASE/PBINS/PORDER, drive the cpu-fold stable
    partition, assert the resulting index array equals PORDER exactly.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness partition_parity::order -- --nocapture && cargo test --workspace 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness partition_parity::order` passes: the cpu-fold post-partition index array equals every PORDER golden exactly (bit-exact integer array), across all flag-fan-out PCASE cases.
    - The shared route `#[cube] fn` is `pub(crate)` and uses only branchless `select` stores (grep confirms no nested-if mutation chains in the body).
    - The mark kernel dispatches u8/u16/u32 via the copied native-width macro and uses the 18-01 u16 scan for PrepareOffset.
    - `cargo test --workspace` GREEN with `LGBM_CUDA_ON_DEVICE` unset; clippy clean on new code.
  </acceptance_criteria>
  <done>The numeric mark + PrepareOffset + cpu stable-partition anchor land; partition row-order parity is green for the full flag fan-out.</done>
</task>

<task type="auto">
  <name>Task 2: AggregateBlockOffset + SplitInner scatter + CopyDataIndices + UpdateDataIndexToLeafIndex + 16-int SplitTreeStructure packet</name>
  <files>crates/lgbm-compute/src/kernels/data_partition.rs, crates/oracle-harness/tests/partition_parity.rs</files>
  <read_first>
    - RESEARCH "Kernel Inventory Map" rows AggregateBlockOffsetKernel0 (:131), AggregateBlockOffsetKernel1 (:132), SplitInnerKernel (:133), SplitTreeStructureKernel (:134), CopyDataIndicesKernel (:135), UpdateDataIndexToLeafIndex (:129).
    - RESEARCH "Per-Split Readback Packets (research target #5)" — the exact 16-int packet field table (indices 0..8 ints + 8..16 as double[4]: left_sum_hessians/right_sum_hessians/left_sum_gradients/right_sum_gradients).
    - RESEARCH Code Examples "Reference scatter (the D-04 order-equivalence core)" (:330-340) — the exclusive left rank `= block_to_left_offset[tid-1]`, right dest `= tid - rank`; Pitfall 2 (inclusive vs exclusive).
    - crates/lgbm-compute/src/kernels/primitives.rs — the u32 exclusive block-totals scan driver added in 18-01 (AggregateBlockOffset) + the 3-launch host driver shape (:268-326).
    - crates/lgbm-compute/src/kernels/split_info.rs — `SplitScalars` per-side sum_gradients/sum_hessians (f64) + default_left the packet reads.
    - LightGBM-release-4.6.0.99/src/treelearner/cuda/cuda_data_partition.cu:681/745 (AggregateBlockOffset), :909-937 (SplitInner + CopyDataIndices), :787-825 (SplitTreeStructure packet).
  </read_first>
  <action>
    Implement `AggregateBlockOffset` (start with the two faithful kernels — Kernel0 large-grid serial
    chunk-scan + Kernel1 one-shot warp scan, matching the reference launchers per Open-Q3; collapse later
    if desired, parity-neutral) using the 18-01 u32 exclusive scan: block `b`'s left base = Σ left counts
    of blocks 0..b-1, right base shifted by `to_left_total`. Implement the `SplitInner` scatter as a NEW
    `#[cube]`: recover the exclusive in-block left rank from `block_to_left_offset[tid-1]` (inclusive→
    exclusive, Pitfall 2), write left rows to `out[left_base+rank]` and right rows to
    `out[right_base+(tid−rank)]` in the pre-allocated scratch `cuda_out_data_indices_in_leaf_` (D-15);
    preserve the `global_thread_index < num_data_in_leaf` bounds guard (T-18-01). `CopyDataIndices` copies
    the reordered scratch back over the parent slice. `UpdateDataIndexToLeafIndex` writes the destination
    leaf id into the row→leaf map (independent scatter-free write; takes the right_leaf_index from 18-03's
    SplitKernel as an input param — the Split-before-partition ordering invariant, §1/§10).

    Implement `SplitTreeStructureKernel`'s scalar fan-out packing the 16-int packet (8 ints:
    left/right leaf index, num_data, data_start, smaller/larger child leaf index; + 4 f64 in [8..16]:
    left/right sum_hessians, left/right sum_gradients) into the pre-allocated packet buffer, reading
    per-side sums from `SplitScalars`. Un-ignore `partition_parity::packet` (assert the packet fields vs
    PPACKET). Extend the `order` replay to run the FULL device scatter path where a device client is
    available (still cpu-fold-anchored, never GPU-vs-GPU).
  </action>
  <verify>
    <automated>cargo test -p oracle-harness partition_parity::packet partition_parity::order -- --nocapture && cargo test --workspace 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness partition_parity::packet` passes: all 16 packet fields (8 ints + 4 f64 bit-exact) match PPACKET.
    - The scatter preserves the `global_thread_index < num_data_in_leaf` guard (grep + test); the exclusive in-block rank is derived from the inclusive scan `[tid-1]` (Pitfall 2 avoided).
    - Scatter scratch, per-block count buffers, and the 16-int packet are allocated once in the driver `new`/setup, not per split (grep: single `client.empty` alloc site, D-15).
    - `UpdateDataIndexToLeafIndex` consumes `right_leaf_index` as an input (ordering invariant honored).
    - `cargo test --workspace` GREEN with the env unset.
  </acceptance_criteria>
  <done>The mark→prefix-sum→scatter permutation + row→leaf map + 16-int packet land and match the goldens; two readbacks per split (8-int + 16-int) are the only device→host transfers.</done>
</task>

<task type="auto">
  <name>Task 3: Categorical partition routing (FindInBitset) + HistArena leaf-indexed pool swap (D-03, D-09)</name>
  <files>crates/lgbm-compute/src/kernels/data_partition.rs, crates/lgbm-compute/src/kernels/histogram_arena.rs, crates/oracle-harness/tests/partition_parity.rs</files>
  <read_first>
    - RESEARCH "HistArena Pool-Swap Integration (research target #3, D-09)" (:177-184) — the leaf-indexed whole-pool swap driven by `num_data[left] < num_data[right]`: left-smaller → `pool[right]=parent_ptr`, `pool[left]=fresh`; mirror for right-smaller; larger child inherits parent for `parent − smaller`.
    - crates/lgbm-compute/src/kernels/histogram_arena.rs — `rotate()` (:236-267, the per-split {parent,smaller,larger} index reassignment — do NOT rebuild), the counted-alloc discipline (:120-135), the no-alias assert style (:257-264), and the `rotate_subtract_lands_in_larger_parent_slot` round-trip test (:402-456) to mirror.
    - RESEARCH "Kernel Inventory Map" rows GenDataToLeftBitVectorKernel_Categorical (:128), UpdateDataIndexToLeafIndexKernel_Categorical (:129), FindInBitsetCUDA (:142); the categorical route via `FindInBitset(bitset, len, bin−min_bin+mfb_offset)`.
    - PATTERNS "FindInBitsetCUDA<T> helper" note + "leaf-indexed whole-pool swap()" (No-Analog table) — a tiny NEW `pub(crate) #[cube]` helper (`bits[pos/32] >> (pos%32) & 1`, guard `pos >= n → false`, T-18 bitset OOB) SHARED with 18-04 predict.
    - LightGBM-release-4.6.0.99/src/treelearner/cuda/cuda_data_partition.cu:557/582 (categorical kernels), :76 (FindInBitset n-guard), :827-906 (pool swap branch).
  </read_first>
  <action>
    Add the categorical route to `data_partition.rs`: a shared `pub(crate) #[cube] fn find_in_bitset<T>`
    (word index `pos/32`, bit `pos%32`, guard `pos >= n → false` — preserve the n-bound check, T-18
    bitset OOB) used by `GenDataToLeftBitVectorKernel_Categorical` (membership via bin−min_bin+mfb_offset)
    and `UpdateDataIndexToLeafIndexKernel_Categorical`. Reuse the same PrepareOffset/AggregateBlockOffset/
    SplitInner machinery from Tasks 1-2 — only the per-row decision changes. Un-ignore
    `partition_parity::cat` (assert the PCAT membership routing vs golden).

    Extend `histogram_arena.rs` with a leaf-index → slot-handle table and a
    `swap(left_leaf, right_leaf, smaller_is_left)` that reassigns handles EXACTLY as
    `SplitTreeStructureKernel` does (left-smaller → larger/right inherits parent, smaller/left takes a
    fresh non-aliasing slot; mirror for right-smaller) — reassigns INDICES only, zero `client.empty`,
    `device_allocations` frozen (do NOT rebuild `rotate()`, D-09). Add a `histogram_arena::swap` unit test
    in the same `#[cfg(test)] mod`, mirroring the `rotate_subtract_lands_in_larger_parent_slot` round-trip:
    swap → drive `subtract_hist_kernel` → assert the parent−smaller result lands in the larger child's
    slot on the cpu f64 anchor (never GPU-vs-GPU).
  </action>
  <verify>
    <automated>cargo test -p oracle-harness partition_parity::cat -- --nocapture && cargo test -p lgbm-compute histogram_arena::swap -- --nocapture && cargo test --workspace 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness partition_parity::cat` passes: categorical membership routing matches the PCAT golden across the cat_onehot / cat_manyvsmany cases.
    - `cargo test -p lgbm-compute histogram_arena::swap` passes: after the leaf-indexed swap, the `parent − smaller` subtraction result lands in the larger child's slot; the smaller child holds a fresh non-aliasing slot (no-alias assert).
    - `find_in_bitset` preserves the `pos >= n → false` guard (T-18 bitset OOB) and is `pub(crate)` for 18-04 reuse.
    - `histogram_arena.rs` adds `swap()` WITHOUT rebuilding `rotate()` and WITHOUT any new `client.empty` (device_allocations unchanged).
    - `cargo test --workspace` GREEN with the env unset.
  </acceptance_criteria>
  <done>Categorical membership routing + the subtraction-trick pool swap land; all three `partition_parity` cells + `histogram_arena::swap` are green. ODL-13 device path complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → device scatter | untrusted `num_data_in_leaf` / bin values cross into the scatter + membership kernels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Tampering | `SplitInnerKernel` scatter device write | mitigate | Preserve the `global_thread_index < num_data_in_leaf` guard (reference cuda_data_partition.cu:925); size the scratch `cuda_out_data_indices_in_leaf_` to full leaf `num_data` once (D-15) |
| T-18-02 | Tampering/DoS | prefix-sum offsets (u16 per-block ≤1024, u32 global) | mitigate | Reuse `validate_scan_inputs`; widths match the reference; assert `num_data` fits `i32` (`data_size_t`) at the host boundary |
| T-18-03 | Tampering | `find_in_bitset` (categorical membership) | mitigate | Preserve the `pos >= n → false` bound check (reference cuda_data_partition.cu:78) |
| T-18-SC | Tampering | package installs | accept | No packages installed this phase — no supply-chain surface |
</threat_model>

<verification>
- `cargo test -p oracle-harness partition_parity` (order + cat + packet) all green vs the goldens.
- `cargo test -p lgbm-compute histogram_arena::swap` green (subtraction reuse correct).
- `cargo test --workspace` GREEN with `LGBM_CUDA_ON_DEVICE` unset (ODL-19 merge gate).
- Grep: single `client.empty` alloc site in the partition driver (D-15); no f64-typed row-indexed arrays in the mark/scatter per-row loops (D-14 — f64 confined to the packet's scalar sum fields); `partition.rs` and `HistArena::rotate` untouched/unrebuilt (D-01/D-09).
- hip parity deferred to 18-04's gate; cpu f64 fold is the anchor here (never GPU-vs-GPU).
</verification>

<success_criteria>
ODL-13 device data-partition is complete: the §9 mark→prefix-sum→scatter reproduces the reference stable
partition bit-for-bit across the full numeric flag fan-out and categorical membership routing; the 16-int
packet matches; the HistArena leaf-indexed swap makes the subtraction-trick reuse correct; the merge gate
is green with the env unset.
</success_criteria>

## Artifacts this phase produces

New symbols/files introduced by this plan (source-grounding exclusion list for downstream review):
- `data_partition.rs` (NEW): `pub(crate) #[cube]` shared route fn (8-flag comptime), `GenDataToLeftBitVector` numeric + `_Categorical` mark kernels, `PrepareOffset` per-block scan wiring, `AggregateBlockOffset` (Kernel0/Kernel1), `SplitInner` scatter, `CopyDataIndices`, `UpdateDataIndexToLeafIndex` (+ `_Categorical`), `SplitTreeStructure` 16-int packet packer, `find_in_bitset<T>` shared helper, the cpu f64 stable-partition anchor, the host driver with pre-allocated scratch/count/packet buffers.
- `histogram_arena.rs`: new leaf-index→slot-handle table + `swap(left_leaf, right_leaf, smaller_is_left)` + `histogram_arena::swap` unit test.
- `partition_parity.rs`: `order`, `cat`, `packet` cells un-ignored (driven live against the cpu fold).

<output>
Create `.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-02-SUMMARY.md` when done
</output>
