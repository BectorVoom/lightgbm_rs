---
phase: 18-on-device-data-partition-tree-mutation-prediction
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/lgbm-compute/src/kernels/primitives.rs
  - crates/lgbm-compute/src/kernels/mod.rs
  - crates/lgbm-compute/src/kernels/data_partition.rs
  - crates/lgbm-compute/src/kernels/tree.rs
  - crates/lgbm-compute/src/kernels/predict.rs
  - xtask/cpp/kernel_capture.cpp
  - crates/oracle-harness/tests/fixtures/kernels/partition.txt
  - crates/oracle-harness/tests/fixtures/kernels/predict.txt
  - crates/oracle-harness/tests/partition_parity.rs
  - crates/oracle-harness/tests/tree_mutation_parity.rs
  - crates/oracle-harness/tests/predict_parity.rs
autonomous: true
requirements: [ODL-13, ODL-14, ODL-15]

must_haves:
  truths:
    - "A u16 (or u32-widened fallback) and a u32 integer block prefix-sum lower and match a serial Rust scan bit-for-bit on cubecl-cpu"
    - "The golden fixtures carry the full GenDataToLeftBitVector flag fan-out cases, categorical membership routing, the 16-int child-stats packet, and numeric+categorical tree-walk predict goldens (D-11)"
    - "partition_parity.rs and tree_mutation_parity.rs compile and are #[ignore = \"Wave-0 scaffold\"] so cargo test --workspace stays green with LGBM_CUDA_ON_DEVICE unset"
    - "predict_parity.rs carries an #[ignore] on_device scaffold cell that compiles against the extended fixture"
  prohibitions:
    - "No new external crate is added to any Cargo.toml (cubecl 0.10 is already the workspace backend; Package Legitimacy Audit is N/A)"
    - "No scaffold test runs live (all are #[ignore]) — the merge gate must not turn red on a fresh checkout"
    - "The int-scan bounds guard validate_scan_inputs is reused, not re-implemented"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/primitives.rs"
      provides: "u16/u32 (or u32-widened) inclusive+exclusive block-prefix-sum launch wrappers + host drivers"
      contains: "prefix_sum"
    - path: "xtask/cpp/kernel_capture.cpp"
      provides: "extended partition + predict golden emitters (flag fan-out, categorical, 16-int packet, tree-walk)"
    - path: "crates/oracle-harness/tests/partition_parity.rs"
      provides: "Wave-0 golden-replay scaffold for ODL-13"
    - path: "crates/oracle-harness/tests/tree_mutation_parity.rs"
      provides: "Wave-0 golden-replay scaffold for ODL-14"
  key_links:
    - from: "crates/oracle-harness/tests/partition_parity.rs"
      to: "crates/oracle-harness/tests/fixtures/kernels/partition.txt"
      via: "CARGO_MANIFEST_DIR fixture path + raw-f64-bits parse (mirror best_split_parity.rs)"
      pattern: "CARGO_MANIFEST_DIR"
    - from: "crates/lgbm-compute/src/kernels/primitives.rs"
      to: "validate_scan_inputs"
      via: "reused block-count bounds guard for the integer scans"
      pattern: "validate_scan_inputs"
---

<objective>
Lay the Wave-0 foundation for Phase 18: (1) de-risk the one genuine primitive gap — integer-typed
(u16/u32) block prefix-sum launchers on cubecl (Open Q1 / A1); (2) extend the C++ golden capture and
regenerate the fixtures with the full partition flag fan-out, categorical routing, the 16-int
child-stats packet, and numeric+categorical tree-walk predict goldens (D-11); (3) create the
`#[ignore]` Nyquist scaffold tests for ODL-13/14/15 so the Wave-1/2 kernel work has a failing target
to turn green.

Purpose: The scatter (18-02), tree mutation (18-03), and predict (18-04) kernels all consume these
scan launchers, fixtures, and scaffolds. Landing them first keeps the merge gate green and removes the
single MEDIUM-risk unknown (u16 lowering on hip) before committing the scatter to the generic scan body.
Output: extended `primitives.rs`, extended `kernel_capture.cpp`, regenerated `partition.txt` + new
`predict.txt`, new `partition_parity.rs` / `tree_mutation_parity.rs`, extended `predict_parity.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-RESEARCH.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-PATTERNS.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-VALIDATION.md
@crates/lgbm-compute/src/kernels/primitives.rs
@crates/oracle-harness/tests/best_split_parity.rs
@crates/oracle-harness/tests/fixtures/kernels/partition.txt
@xtask/cpp/kernel_capture.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: u16/u32 integer block prefix-sum launchers + scan-lowering parity spike (ODL-13, Open Q1/A1) + new-module stubs</name>
  <files>crates/lgbm-compute/src/kernels/primitives.rs, crates/lgbm-compute/src/kernels/mod.rs, crates/lgbm-compute/src/kernels/data_partition.rs, crates/lgbm-compute/src/kernels/tree.rs, crates/lgbm-compute/src/kernels/predict.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/primitives.rs — the existing `N: Numeric` bodies `block_scan_body` (:82-112), `scan_block_totals_body` (:117-130), `add_base_body` (:135), the f64/f32 launch wrappers (:159-211), the f64/f32 host drivers (:245-326), and `validate_scan_inputs` (:216-234). Copy the wrapper+driver pair, swapping the cell type only.
    - RESEARCH "Resident Dataset + Prefix-Sum Primitives (research target #4)" + "Open Questions #1" — §9 uses inclusive scan (PrepareOffset per-block, u16) and exclusive block-totals scan (AggregateBlockOffset, u32); the inclusive/exclusive distinction is load-bearing (§17).
    - PATTERNS SP-1 (one body + thin per-type wrappers) and the primitives.rs EXTEND section (:196-215).
    - LightGBM-release-4.6.0.99/include/LightGBM/cuda/cuda_algorithms.hpp:33 (inclusive) / :64 (exclusive) — the reference scan semantics.
  </read_first>
  <action>
    Add integer instantiations of the EXISTING generic scan bodies — instantiation, not a rewrite.
    Add launch wrappers and host drivers for a u16 inclusive block scan (the PrepareOffset per-block path)
    and a u32 exclusive block-totals scan (the AggregateBlockOffset path), modeled byte-for-byte on the
    existing f64 inclusive driver, swapping only the cell type. Reuse `validate_scan_inputs` unchanged for
    the block-count bounds (rejects block_size==0, num_blocks>1024). Re-export the new drivers from
    `mod.rs` if the module re-exports scan entry points.

    A1 fallback (parity-neutral): if cubecl `Numeric` does not admit u16 on cubecl-hip 0.10 (the plane
    intrinsic does not lower), route the per-block scan through a u32-widened instantiation instead and
    document the widening in a code comment citing Open Q1 — u16 vs u32-widen produce identical scan
    output, so this is parity-neutral. Decide u16-vs-widen by the spike test result below.

    ALSO (wave-conflict avoidance): declare the three new kernel modules in `mod.rs`
    (`pub mod data_partition; pub mod tree; pub mod predict;`) and create them as EMPTY compiling stub
    files now, so the Wave-1/2 plans (18-02/03/04) fill in their own module WITHOUT re-editing the shared
    `mod.rs` (keeps the two Wave-1 plans on disjoint files). Each stub is an empty `.rs` (module-doc comment
    only) that compiles clean; the Wave-1/2 plans replace the stub body.

    Add a `#[cfg(test)] mod` unit test `int_scan` that drives both the u16 (or widened) inclusive scan and
    the u32 exclusive scan on the cpu client over a fixed input vector and asserts each equals a serial
    Rust reference scan element-for-element (integer equality — no rounding). Include a boundary case at
    the 1024-block single-tile cap and an empty/zero-length input rejected by `validate_scan_inputs`.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute primitives::int_scan -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-compute primitives::int_scan` passes: u16/widened inclusive scan and u32 exclusive scan match a serial Rust scan element-for-element on the cpu client.
    - `validate_scan_inputs` is CALLED by the new drivers (grep shows the reuse), not re-implemented.
    - `mod.rs` declares `pub mod data_partition; pub mod tree; pub mod predict;` and the three empty stub files compile clean (so Wave-1/2 plans never touch `mod.rs`).
    - `cargo build -p lgbm-compute --tests` exits 0; clippy clean on the edited region.
    - If u16 does not lower on hip, the code comment records the u32-widen fallback and cites Open Q1 (parity-neutral).
  </acceptance_criteria>
  <done>Integer block prefix-sum launchers exist and are proven bit-exact vs a serial scan; the u16-lowering unknown is resolved (u16 or u32-widen), unblocking the 18-02 scatter.</done>
</task>

<task type="auto">
  <name>Task 2: Extend kernel_capture.cpp + regenerate partition/predict goldens (D-11)</name>
  <files>xtask/cpp/kernel_capture.cpp, crates/oracle-harness/tests/fixtures/kernels/partition.txt, crates/oracle-harness/tests/fixtures/kernels/predict.txt</files>
  <read_first>
    - xtask/cpp/kernel_capture.cpp — `PCaseSpec` (:957), `EmitPCase` (:968 → PCASE/PBINS/PORDER/PSPLIT), `BuildPartitionCorpus` (:1009), the file-write block (:1161-1177), and the argv driver layout (:1102-1111: hist_out, master_seed, split_out, partition_out, subtract_out).
    - crates/oracle-harness/tests/fixtures/kernels/partition.txt — the existing KERNEL_MASTER_SEED / COUNTS partition / PCASE/PBINS/PORDER/PSPLIT format (Phase-4, MissingType::None only).
    - RESEARCH "Fixture Strategy (research target #7, D-11)" + "Per-Split Readback Packets (research target #5)" — the 16-int packet field table (indices 0..8 ints + 8..16 as double[4]); A4/Open-Q2 recommends a HOST-reconstructed packet golden (compute the fields from the C++ Tree/partition on CPU), not an instrumented CUDA build.
    - RESEARCH "GenDataToLeftBitVector Flag Fan-Out" table — the flag meanings that define which cases to emit.
    - RESEARCH "Prediction Tree-Walk — Verified Transcription" (:162-175) — the numeric+categorical walk to emit predict goldens for.
    - crates/oracle-harness/tests/fixtures/categorical/cat_onehot.txt + cat_manyvsmany.* — existing categorical fixture models to drive the categorical predict + membership goldens.
  </read_first>
  <action>
    Extend `PCaseSpec`/`EmitPCase`/`BuildPartitionCorpus` to emit, in the SAME partition.txt format
    family: (a) the full flag fan-out cases — a case each for missing-is-zero, missing-is-NA, default_left
    true and false, most-freq-bin coincident with the zero bin and with the NA bin, max-to-left, and the
    degenerate min_bin==max_bin case; (b) categorical membership routing cases (bitset + bin → left/right
    via the reference bitset-membership logic on bin−min_bin+mfb_offset), tagged with a `PCAT` marker so
    the Rust replay can distinguish them; (c) a `PPACKET` block per split carrying the 16-int child-stats
    packet fields (8 ints + 4 f64 as raw little-endian f64-bit decimal-u64, matching the best_split.txt
    bit-hex convention) — HOST-reconstruct the packet fields from the C++ Tree/partition per A4/Open-Q2,
    not from an instrumented device build.

    Add a NEW predict.txt fixture (new argv output path) emitting numeric AND categorical tree-walk predict
    goldens: for a small numeric model and the cat_onehot / cat_manyvsmany models, emit the input matrix
    (f64-bit rows) plus the per-row raw-margin score the reference tree-walk produces (pre-inverse-link —
    inverse-link stays host-side this phase). Wire the new output paths into the argv layout at :1102-1111
    and the file-write block at :1161-1177. Regenerate both fixtures by running the capture; document the
    capture command in the SUMMARY. The untracked LightGBM/ tree is never git-added (build lib_lightgbm 4.6
    in-tree per MEMORY lightgbm-ref-tree-untracked).
  </action>
  <verify>
    <automated>grep -cE 'PCAT|PPACKET' crates/oracle-harness/tests/fixtures/kernels/partition.txt && test -f crates/oracle-harness/tests/fixtures/kernels/predict.txt && echo FIXTURES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `partition.txt` contains PCAT (categorical) and PPACKET (16-int packet) blocks in addition to the flag-fan-out PCASE cases; the file still parses as the existing format family.
    - `predict.txt` exists with a numeric model block and both categorical (cat_onehot, cat_manyvsmany) blocks, each carrying input rows + reference raw-margin scores as f64-bit decimal-u64.
    - `kernel_capture.cpp` compiles under `xtask/cpp/CMakeLists.txt`; the argv driver accepts the new output path(s).
    - The regenerated fixtures are committed; the untracked LightGBM/ tree is not git-added.
  </acceptance_criteria>
  <done>The golden fixtures carry every case the Wave-1/2 kernels are anchored against; the packet golden is host-reconstructed (A4).</done>
</task>

<task type="auto">
  <name>Task 3: Nyquist scaffold tests — partition_parity.rs, tree_mutation_parity.rs, predict_parity.rs on_device cell (#[ignore])</name>
  <files>crates/oracle-harness/tests/partition_parity.rs, crates/oracle-harness/tests/tree_mutation_parity.rs, crates/oracle-harness/tests/predict_parity.rs</files>
  <read_first>
    - crates/oracle-harness/tests/best_split_parity.rs — the EXACT idioms to mirror: `kernels_dir()` CARGO_MANIFEST_DIR fixture path (:49-52), `parse_f64_bits` (:57), the `field`/`parse_i64`/`parse_u32` token helpers (:59-81), the `#[ignore = "Wave-0 scaffold; un-ignore when NN-NN lands"]` gate (:17-19), graceful SKIP when the fixture is absent, and `oracle_harness::comparator::compare_exact_f64_bits`.
    - crates/oracle-harness/tests/predict_parity.rs — `predict_modes_dir()` (:30), `read_golden` graceful SKIP (:38-51), `parse_matrix` (:55), `load_corpus` (:90), `compare_within(.., ORACLE_TOL)` (:122).
    - PATTERNS "partition_parity.rs (NEW)" / "tree_mutation_parity.rs (NEW)" / "predict_parity.rs (EXTEND)" sections + the Golden-harness idioms row in the Shared Patterns Summary Table.
    - VALIDATION Per-Task Verification Map (rows 18-01-01..18-03-02) — the exact module/submodule names each test must expose (`partition_parity`, `partition_parity::cat`, `partition_parity::packet`, `tree_mutation_parity`, `predict_parity::on_device`, `predict_parity::cat`).
  </read_first>
  <action>
    Create `partition_parity.rs` covering ODL-13: a `order` cell (parse PCASE/PBINS/PORDER, assert the
    post-partition index array equals the golden), a `cat` submodule cell (PCAT membership routing), and a
    `packet` submodule cell (PPACKET 16-int child-stats fields). Create `tree_mutation_parity.rs` covering
    ODL-14: a `SplitKernel` field-write cell (reuse the split.txt cases per the RESEARCH test map) plus a
    Split-before-partition ordering assertion cell (the returned right_leaf_index feeds the partition
    launch — Pitfall 3). Extend `predict_parity.rs` with an `on_device` cell (numeric tree-walk vs the
    predict.txt golden) and a `cat` cell (categorical membership predict).

    Every new/extended cell parses its fixture with the raw-f64-bits helpers, SKIPs gracefully when the
    fixture is absent, and drives NOTHING live yet — each primary assertion is marked
    `#[ignore = "Wave-0 scaffold; un-ignore when 18-02/18-03/18-04 lands"]` so `cargo test --workspace`
    stays GREEN with LGBM_CUDA_ON_DEVICE unset (D-13/ODL-19). The bodies must reference the eventual
    cpu-fold entry points by name in a comment so the un-ignore in Wave 1/2 is a one-line change, matching
    the Phase-17 best_split_parity scaffold pattern.
  </action>
  <verify>
    <automated>cargo test --workspace --no-run 2>&1 | tail -3 && cargo test -p oracle-harness partition_parity tree_mutation_parity predict_parity 2>&1 | grep -qE 'ignored|0 passed; 0 failed' && echo SCAFFOLDS_COMPILE</automated>
  </verify>
  <acceptance_criteria>
    - `partition_parity.rs`, `tree_mutation_parity.rs`, and the `predict_parity.rs` on_device/cat cells compile as `[[test]]` targets.
    - The submodule/cell names match the VALIDATION map exactly (`partition_parity`, `::cat`, `::packet`, `tree_mutation_parity`, `predict_parity::on_device`, `predict_parity::cat`).
    - Every primary assertion carries `#[ignore = "Wave-0 scaffold; ..."]`; `cargo test --workspace` is GREEN (env unset) — no scaffold runs live.
    - Each cell SKIPs (does not panic) when its golden fixture is absent.
  </acceptance_criteria>
  <done>ODL-13/14/15 each have a compiling, ignored Nyquist scaffold ready for a one-line un-ignore in Waves 1-2; the merge gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → device scan | untrusted `num_data`/`num_blocks`/`block_size` sizes cross into the launch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Tampering/DoS | integer prefix-sum offsets (u16 per-block ≤1024, u32 global) | mitigate | Reuse `validate_scan_inputs` (rejects block_size==0, num_blocks>1024); u16/u32 widths match the reference; assert `num_data` fits `i32` (`data_size_t`) |
| T-18-SC | Tampering | package installs | accept | NO packages installed this phase (cubecl 0.10 already vendored; Package Legitimacy Audit N/A) — no supply-chain surface |
</threat_model>

<verification>
- `cargo test -p lgbm-compute primitives::int_scan` passes (u16/u32 or widened scans bit-exact vs serial).
- Fixtures regenerated: `partition.txt` has PCAT + PPACKET; `predict.txt` exists (numeric + categorical).
- `cargo test --workspace` GREEN with `LGBM_CUDA_ON_DEVICE` unset (all scaffolds `#[ignore]`).
- No new crate in any Cargo.toml; the untracked `LightGBM/` tree is not git-added.
</verification>

<success_criteria>
The integer-scan unknown is resolved and proven; the golden fixtures cover the full flag fan-out,
categorical routing, 16-int packet, and numeric+categorical predict; three `#[ignore]` scaffolds exist
for ODL-13/14/15; the merge gate is green. Waves 1-2 can now transcribe kernels against a concrete
failing target.
</success_criteria>

## Artifacts this phase produces

New symbols/files introduced by this plan (source-grounding exclusion list for downstream review):
- `primitives.rs`: `prefix_sum_inclusive_u16_on` / `prefix_sum_exclusive_u32_on` (or u32-widened equivalents) launch wrappers + host drivers; `#[cfg(test)] mod int_scan`.
- `mod.rs`: `pub mod data_partition; pub mod tree; pub mod predict;` declarations + three empty compiling stub files `data_partition.rs`, `tree.rs`, `predict.rs` (filled by Waves 1-2).
- `xtask/cpp/kernel_capture.cpp`: extended `PCaseSpec`/`EmitPCase`/`BuildPartitionCorpus` with `PCAT` + `PPACKET` emitters and a predict-golden emitter.
- Fixtures: extended `tests/fixtures/kernels/partition.txt` (PCAT/PPACKET blocks); new `tests/fixtures/kernels/predict.txt`.
- Tests: new `partition_parity.rs` (`order`, `cat`, `packet` cells), new `tree_mutation_parity.rs` (SplitKernel + ordering cells), extended `predict_parity.rs` (`on_device`, `cat` cells) — all `#[ignore]` Wave-0 scaffolds.

<output>
Create `.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-01-SUMMARY.md` when done
</output>
