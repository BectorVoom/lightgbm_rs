---
phase: 18-on-device-data-partition-tree-mutation-prediction
plan: 04
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - crates/lgbm-compute/src/kernels/predict.rs
  - crates/oracle-harness/tests/predict_parity.rs
  - crates/oracle-harness/tests/kernel_parity.rs
autonomous: true
requirements: [ODL-15]

must_haves:
  truths:
    - "The tree-walk AddPredictionToScoreKernel<USE_INDICES> walks from node 0 over the §13 columnar dataset (8/16/32 width dispatch), applies numeric threshold + missing/default_left routing and categorical bitset membership, and adds leaf_value[~node] into a double* score accumulator (D-05, §10)"
    - "The numeric route reuses the SAME shared route #[cube] fn as the partition mark (one transcription, Pitfall 4); categorical reuses the shared find_in_bitset helper from 18-02"
    - "The §9 AddPredictionToScoreKernel<USE_BAGGING> per-row leaf-map gather-add is included this phase (D-06)"
    - "Numeric predict (8/16/32) and categorical membership predict (cat_onehot/cat_manyvsmany) match the predict.txt golden within ORACLE_TOL vs the cpu f64 anchor + cross-check lib_lightgbm"
    - "hip f32 is within ~1e-6 of the cpu f64 anchor (tie-aware); never GPU-vs-GPU"
  prohibitions:
    - "No f64-typed per-row bin reads in the walk — bin reads are native-width integer; f64 only in the score accumulator + scalar leaf-value/gain math (D-14/spike-052)"
    - "No GPU-vs-GPU comparison — hip f32 is compared only to the cpu f64 anchor (D-12/def-f8u-01)"
    - "Do NOT re-transcribe the numeric route decision — call the shared 18-02 route fn (Pitfall 4)"
    - "Do NOT apply objective inverse-link on device — inverse-link stays host-side at the readback boundary this phase (Phase-19 boundary)"
    - "on_device_growth_supported() stays false — these kernels are exercised by parity tests only, no live grow-loop"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/predict.rs"
      provides: "AddPredictionToScore tree-walk (numeric + categorical, 8/16/32) + §9 leaf-map gather-add"
      min_lines: 120
  key_links:
    - from: "crates/lgbm-compute/src/kernels/predict.rs"
      to: "crates/lgbm-compute/src/kernels/data_partition.rs"
      via: "shared route #[cube] fn + find_in_bitset helper (one transcription, 18-02)"
      pattern: "find_in_bitset"
    - from: "crates/lgbm-compute/src/kernels/predict.rs"
      to: "crates/lgbm-compute/src/kernels/column_data.rs"
      via: "ColumnFeatureMeta 8/16/32 dispatch + bin remap (bin∈[min,max]?bin-min+offset:most_freq_bin)"
      pattern: "ColumnFeatureMeta"
    - from: "crates/oracle-harness/tests/predict_parity.rs"
      to: "crates/oracle-harness/tests/fixtures/kernels/predict.txt"
      via: "numeric + categorical tree-walk golden replay within ORACLE_TOL"
      pattern: "on_device"
---

<objective>
Build the §10 on-device prediction tree-walk in `predict.rs` (D-05): `AddPredictionToScoreKernel<USE_INDICES>`
walking from node 0 over the §13 columnar dataset (8/16/32 width dispatch), numeric threshold +
missing/`default_left` routing (reusing the shared 18-02 route fn — one transcription, Pitfall 4) and
categorical bitset membership (reusing the shared `find_in_bitset` helper), adding `leaf_value[~node]`
into a `double*` score accumulator (D-14). Include §9's `AddPredictionToScoreKernel<USE_BAGGING>` per-row
leaf-map gather-add (D-06). Un-ignore the `predict_parity::on_device` + `::cat` cells and add the hip
parity gate. Objective inverse-link stays host-side at readback (Phase-19 boundary).

Purpose: ODL-15 — model-consuming prediction on device, the last leg of the per-split chain. Categorical
membership is fully built + anchored here (it is model-consuming, independent of how a cat split was grown
— the cat split-FINDER is Phase 22).
Output: `predict.rs`, un-ignored `predict_parity` cells, hip parity module.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-RESEARCH.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-PATTERNS.md
@crates/lgbm-compute/src/kernels/column_data.rs
@crates/lgbm-compute/src/kernels/partition.rs
@crates/oracle-harness/tests/predict_parity.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Numeric tree-walk AddPredictionToScoreKernel<USE_INDICES> (8/16/32) + §9 leaf-map gather-add (USE_BAGGING)</name>
  <files>crates/lgbm-compute/src/kernels/predict.rs, crates/oracle-harness/tests/predict_parity.rs</files>
  <read_first>
    - RESEARCH "Prediction Tree-Walk — Verified Transcription (research target, D-05)" (:162-175) — the full line-by-line walk: data_index resolution, node=0 loop, feature→column, 8/16/32 dispatch, bin remap (bin∈[min,max]?bin-min+offset:most_freq_bin), missing/default branch, leaf reached → `score[data_index] += leaf_value[~node]` (f64 accumulator).
    - RESEARCH Code Examples "Reference predict route (share with partition mark)" (:342-352) — identical to the partition decision; call the shared fn.
    - crates/lgbm-compute/src/kernels/column_data.rs — `ColumnFeatureMeta` (:31-52: bit_type∈{8,16,32}, feature_min_bin/max_bin, offset, most_freq_bin, default_bin, missing_is_zero/na, feature_to_column) — read this struct, do not re-derive meta.
    - crates/lgbm-compute/src/kernels/data_partition.rs (from 18-02) — the shared route `#[cube] fn` + `find_in_bitset` helper to call.
    - crates/lgbm-compute/src/kernels/partition.rs — the per-row native-width read via `u32::cast_from` (:86, SP-5) — bin reads stay integer.
    - crates/oracle-harness/tests/predict_parity.rs — `parse_matrix` (:55), `read_golden` SKIP (:38-51), `compare_within(.., ORACLE_TOL)` (:122); the `on_device` scaffold cell from 18-01.
    - PATTERNS predict.rs section (:170-192) + SP-5 (f64 accumulator only).
    - LightGBM-release-4.6.0.99/src/io/cuda/cuda_tree.cu:317-396 (AddPredictionToScore tree-walk).
  </read_first>
  <behavior>
    - For each row: walk from node 0; at each internal node read the row's native-width bin for the split feature, remap into the feature offset range (else most_freq_bin), route left/right by the shared numeric decision (threshold + missing/default_left); on reaching a leaf (node<0) add leaf_value[~node] to the row's f64 score.
    - USE_INDICES=true walks a used-index subset; USE_INDICES=false walks all rows (identity).
    - §9 USE_BAGGING gather-add: add each row's leaf output via the data-index→leaf map into the f64 score.
    - Numeric predict over an 8-, 16-, and 32-bit column model each match the golden within ORACLE_TOL.
  </behavior>
  <action>
    Fill the `predict.rs` stub created in 18-01 (the module is already declared in `mod.rs` — do NOT
    re-edit `mod.rs`). Implement `AddPredictionToScoreKernel<USE_INDICES>` as a `#[cube]` tree-walk:
    resolve `data_index` (USE_INDICES comptime bool), loop `node=0` while `node >= 0`, read `ColumnFeatureMeta`
    for the split feature, dispatch the native-width bin read (8/16/32 macro from partition.rs:371-393),
    remap the bin (bin∈[min,max]?bin-min+offset:most_freq_bin), and route via the SHARED 18-02 numeric route
    `#[cube] fn` (one transcription, Pitfall 4). On `node < 0`, add `leaf_value[~node]` into the `double*`
    score (f64 accumulator, SP-5/D-14 — the ONLY f64 in the loop). Add the §9
    `AddPredictionToScoreKernel<USE_BAGGING>` per-row leaf-map gather-add (D-06) as a sibling `#[cube]`.
    Validate inputs at the host boundary (SP-4) before the confined `unsafe` launch.

    Un-ignore `predict_parity::on_device`: load the numeric predict.txt golden (8/16/32 model cases), drive
    the cpu-fold tree-walk, assert the per-row raw-margin score matches within ORACLE_TOL AND cross-checks
    the lib_lightgbm golden. Objective inverse-link is applied HOST-side at the readback boundary (Phase-19
    boundary) — the on-device kernel emits raw margin only.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness predict_parity::on_device -- --nocapture && cargo test --workspace 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness predict_parity::on_device` passes: numeric tree-walk raw-margin scores match the predict.txt golden within ORACLE_TOL for 8/16/32-bit column models, cross-checked vs lib_lightgbm.
    - The numeric route CALLS the shared 18-02 route fn (grep: no duplicate route transcription in predict.rs, Pitfall 4).
    - Per-row bin reads are native-width integer (`u32::cast_from`); f64 appears only in the score accumulator (D-14 — grep the walk loop body).
    - The §9 USE_BAGGING leaf-map gather-add is present.
    - `cargo test --workspace` GREEN with `LGBM_CUDA_ON_DEVICE` unset; `on_device_growth_supported()` still false.
  </acceptance_criteria>
  <done>The numeric tree-walk + §9 leaf-map add land and are anchor-green + lib_lightgbm-cross-checked; the route decision is shared with the partition mark.</done>
</task>

<task type="auto">
  <name>Task 2: Categorical membership predict + hip f32 parity gate (~1e-6, tie-aware) + ODL-19 merge-gate discipline</name>
  <files>crates/lgbm-compute/src/kernels/predict.rs, crates/oracle-harness/tests/predict_parity.rs, crates/oracle-harness/tests/kernel_parity.rs</files>
  <read_first>
    - RESEARCH "Prediction Tree-Walk" categorical branch (:171: cat_idx = threshold_in_bin[node]; find_in_bitset(bitset_inner + cat_boundaries_inner[cat_idx], len, bin) → left else right).
    - crates/lgbm-compute/src/kernels/data_partition.rs (18-02) — the shared `find_in_bitset<T>` helper (reuse, do not re-port).
    - crates/oracle-harness/tests/fixtures/categorical/cat_onehot.* + cat_manyvsmany.* — the categorical fixture models.
    - crates/oracle-harness/tests/kernel_parity.rs — the existing hip parity module idiom (`--features hip`, tie-aware assert vs the cpu f64 anchor); MEMORY hip-split-parity-preexisting-defect (tie-aware default_left is an f32-vs-f64 near-tie, assert tie-aware) + def-f8u-01 (never GPU-vs-GPU).
    - RESEARCH "Validation Architecture" test map (hip row :416) + PATTERNS predict_parity.rs EXTEND section (:276-291).
  </read_first>
  <action>
    Add the categorical branch to the `predict.rs` tree-walk: when the decision type has kCategoricalMask,
    read `cat_idx = threshold_in_bin[node]` and route via the SHARED `find_in_bitset` helper over
    `bitset_inner + cat_boundaries_inner[cat_idx]` with length `cat_boundaries_inner[cat_idx+1] − [cat_idx]`
    — left on membership, right otherwise (preserve the `pos >= n → false` guard). Un-ignore
    `predict_parity::cat`: drive the cat_onehot / cat_manyvsmany models, assert within ORACLE_TOL vs the
    golden.

    Add the hip parity cell to `kernel_parity.rs` (behind `--features hip`): run the numeric + categorical
    tree-walk on cubecl-hip f32 and assert within ~1e-6 of the cpu f64 anchor, tie-aware on default_left
    near-ties (the hip-split-parity pattern) — NEVER comparing two GPU f32 paths (def-f8u-01). Confirm the
    ODL-19 merge-gate discipline: `cargo test --workspace` is green with `LGBM_CUDA_ON_DEVICE` unset, and a
    scoped review of the new `predict.rs`/`data_partition.rs`/`tree.rs` per-row loop bodies confirms f64
    appears only in the score accumulator and scalar math, never as a row-indexed array element (D-14).
  </action>
  <verify>
    <automated>cargo test -p oracle-harness predict_parity::cat -- --nocapture && cargo test --workspace 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness predict_parity::cat` passes: categorical membership predict matches the golden within ORACLE_TOL for cat_onehot + cat_manyvsmany.
    - The categorical branch REUSES the shared `find_in_bitset` helper (grep: no duplicate bitset impl); the `pos >= n → false` guard is preserved.
    - The hip parity cell (`--features hip`) asserts within ~1e-6 of the cpu f64 anchor, tie-aware, never GPU-vs-GPU (verified on the local ROCm box per the manual-verification note; skips cleanly when no GPU).
    - `cargo test --workspace` GREEN with `LGBM_CUDA_ON_DEVICE` unset; a scoped review confirms no f64 row-indexed arrays in the walk/mark/scatter per-row loops (D-14).
  </acceptance_criteria>
  <done>Categorical membership predict + the hip f32 parity gate land; ODL-15 is complete and the ODL-19 merge-gate discipline holds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → device predict | untrusted row bins + tree node/bitset indices cross into the tree-walk kernel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | Tampering | `find_in_bitset` in the categorical walk | mitigate | Reuse the shared 18-02 helper with the `pos >= n → false` bound check (reference cuda_data_partition.cu:78) |
| T-18-06 | Tampering | leaf-map gather-add + score index | mitigate | `data_index`/`~node` bounded by dataset `num_data` and the pre-allocated tree; score accumulator sized to num_data once (D-15); native-width bin reads bounded by column meta |
| T-18-SC | Tampering | package installs | accept | No packages installed this phase — no supply-chain surface |
</threat_model>

<verification>
- `cargo test -p oracle-harness predict_parity::on_device predict_parity::cat` green vs the goldens + lib_lightgbm cross-check.
- hip parity cell (`--features hip`) within ~1e-6 of the cpu f64 anchor, tie-aware, never GPU-vs-GPU (local ROCm box).
- `cargo test --workspace` GREEN with `LGBM_CUDA_ON_DEVICE` unset (ODL-19 merge gate); `on_device_growth_supported()` still false.
- Grep: numeric route + bitset membership shared (no duplicate transcription); per-row loop bodies use f64 only in the score accumulator/scalar math (D-14); inverse-link stays host-side (no on-device ConvertOutput).
</verification>

<success_criteria>
ODL-15 on-device prediction is complete: the tree-walk over the §13 columnar dataset (8/16/32) with
numeric + categorical routing matches the cpu f64 anchor within ORACLE_TOL and cross-checks lib_lightgbm;
hip f32 is within ~1e-6 tie-aware; the route decision and bitset membership are single transcriptions
shared with the partition; the merge gate is green with the env unset.
</success_criteria>

## Artifacts this phase produces

New symbols/files introduced by this plan (source-grounding exclusion list for downstream review):
- `predict.rs` (NEW): `AddPredictionToScoreKernel<USE_INDICES>` numeric+categorical tree-walk `#[cube]` + host driver, §9 `AddPredictionToScoreKernel<USE_BAGGING>` leaf-map gather-add, the categorical branch reusing the shared `find_in_bitset` helper.
- `predict_parity.rs`: `on_device` (numeric 8/16/32) + `cat` (cat_onehot/cat_manyvsmany) cells un-ignored.
- `kernel_parity.rs`: new hip f32 tree-walk parity cell (`--features hip`, tie-aware vs cpu f64 anchor).

<output>
Create `.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-04-SUMMARY.md` when done
</output>
