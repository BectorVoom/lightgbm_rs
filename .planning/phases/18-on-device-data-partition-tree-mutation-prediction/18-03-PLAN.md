---
phase: 18-on-device-data-partition-tree-mutation-prediction
plan: 03
type: execute
wave: 1
depends_on: [18-01]
files_modified:
  - crates/lgbm-compute/src/kernels/tree.rs
  - crates/oracle-harness/tests/tree_mutation_parity.rs
autonomous: true
requirements: [ODL-14]

must_haves:
  truths:
    - "A device-resident flat CUDATree (SoA: cuda_leaf_value_, cuda_left_child_/right_child_, cuda_decision_type_, thresholds/counts/depth, cat_boundaries/bitsets) is pre-allocated once and mutated by SplitKernel (D-07)"
    - "SplitKernel writes the 14 numeric tree fields from SplitScalars (NaN→0), rewires parent/child links, and returns right_leaf_index — running BEFORE the partition step (§1/§10 hard ordering invariant)"
    - "SplitCategoricalKernel does the same fan-out plus kCategoricalMask/num_cat and appends bitset lengths to cat_boundaries/_inner"
    - "ShrinkageKernel (leaf_value *= rate) and AddBiasKernel (leaf_value += val) are elementwise, f64-scalar math (D-14)"
    - "The reconstructed host lgbm_model::Tree matches the cpu f64 anchor structure bit-exact"
  prohibitions:
    - "No f64-typed per-row hot loop — the tree kernels are scalar/elementwise; f64 only in leaf-value/gain scalar math (D-14)"
    - "No GPU-vs-GPU comparison — anchor to the cpu f64 fold (D-12/def-f8u-01)"
    - "No per-split device allocation — the flat CUDATree buffers are pre-allocated once (D-15)"
    - "Do NOT reorder Split after partition — SplitKernel must produce right_leaf_index before the partition consumes it (Pitfall 3)"
    - "Do NOT redefine SplitScalars — read the existing split_info.rs record"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/tree.rs"
      provides: "device flat CUDATree SoA + SplitKernel / SplitCategoricalKernel / ShrinkageKernel / AddBiasKernel + host Tree reconstruction"
      min_lines: 150
  key_links:
    - from: "crates/lgbm-compute/src/kernels/tree.rs"
      to: "crates/lgbm-compute/src/kernels/split_info.rs"
      via: "SplitScalars (CUDASplitInfo) read for the 14 field writes + default_left"
      pattern: "SplitScalars"
    - from: "crates/oracle-harness/tests/tree_mutation_parity.rs"
      to: "crates/oracle-harness/tests/fixtures/kernels/split.txt"
      via: "SplitKernel field-write golden replay + Split-before-partition ordering assert"
      pattern: "SplitKernel"
---

<objective>
Build the §10 on-device tree mutation in `tree.rs`: a device-resident flat `CUDATree` (D-07) mutated by
`SplitKernel` (14 field writes from `SplitScalars`, NaN→0, returns `right_leaf_index`) running BEFORE the
partition step (§1/§10 ordering), plus `SplitCategoricalKernel`, `ShrinkageKernel`, and `AddBiasKernel`,
all anchor-pinned to the host tree structure. Un-ignore `tree_mutation_parity` (field writes + ordering)
and add a `tree::shrinkage` unit test.

Purpose: ODL-14 — the tree-mutation half of the per-split chain. The Split-before-partition invariant is
load-bearing: a partition running before the mutation would consume a stale leaf id (Pitfall 3).
Output: `tree.rs`, un-ignored `tree_mutation_parity.rs`, `tree::shrinkage` unit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-RESEARCH.md
@.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-PATTERNS.md
@crates/lgbm-compute/src/kernels/split.rs
@crates/lgbm-compute/src/kernels/split_info.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Device flat CUDATree SoA + SplitKernel (14 field writes, NaN→0, returns right_leaf_index; Split-before-partition ordering)</name>
  <files>crates/lgbm-compute/src/kernels/tree.rs, crates/oracle-harness/tests/tree_mutation_parity.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/split_info.rs — `SplitScalars` field list (:80-126: default_left, per-side sum_gradients/sum_hessians f64, left/right_value f64, threshold u32, inner_feature_index, num_cat_threshold + reserved cat slabs :230-233), `DeviceSplitInfo::new` counted-alloc SoA (:267-357, NUM_FIELD_BUFFERS=21), `copy_slot` (:522-555). Read — do NOT redefine SplitScalars.
    - crates/lgbm-compute/src/kernels/split.rs — the `#[cube(launch)]` scalar-write kernel shape (:393), the branchless `select(...)` field-write / `round_int` convention (:246-292, SP-2) — the cubecl-cpu-safe way to do NaN→0 conditional writes.
    - RESEARCH "Kernel Inventory Map" SplitKernel row (:137) + "Split-Before-Partition Ordering (research target #6)" (:216-218) — SplitKernel mutates the flat device tree and the host launch returns the new leaf id the partition consumes.
    - RESEARCH "Architectural Responsibility Map" Tree-mutation row (:89) + PATTERNS tree.rs section (:139-166: `Set/GetDecisionTypeCUDA` bit helpers, kDefaultLeftMask).
    - LightGBM-release-4.6.0.99/src/io/cuda/cuda_tree.cu:48-138 (SplitKernel).
  </read_first>
  <behavior>
    - After SplitKernel: the parent node's decision fields (split_feature, threshold, decision_type with default_left/missing bits, left/right child links) are written; two new leaf slots carry left_value/right_value; internal node count/leaf count advance; NaN inputs are coerced to 0.
    - The host launch returns `right_leaf_index` (the new leaf slot id).
    - Ordering: a test asserts the returned right_leaf_index is the value the partition step consumes (Pitfall 3).
  </behavior>
  <action>
    Fill the `tree.rs` stub created in 18-01 (the module is already declared in `mod.rs` — do NOT re-edit
    `mod.rs`). Model the device flat `CUDATree` as counted-`client.empty` SoA handles exactly like
    `DeviceSplitInfo::new` (one buffer per field: `cuda_leaf_value_`, `cuda_left_child_`/`cuda_right_child_`,
    `cuda_decision_type_`, `cuda_threshold_`, `cuda_split_feature_`, counts, depth, plus `cat_boundaries`/
    bitset slabs) — allocated ONCE (D-15). Add comptime bit helpers `set/get_decision_type` and
    `set/get_missing_type` (kDefaultLeftMask, kCategoricalMask) as SP-2 select-based packers.

    Implement `SplitKernel` as a `#[cube(launch)]` scalar-write kernel reading `SplitScalars`: rewire the
    parent/child links and write the 14 numeric fields, coercing NaN→0 via branchless `select` (mirror
    split.rs:246-292). The host driver returns `right_leaf_index`. Reconstruct the host `lgbm_model::Tree`
    from the flat arrays at the (per-tree) end for the anchor compare (D-07, discretion on the exact point).

    Un-ignore `tree_mutation_parity` (the SplitKernel field-write cell): reuse the `split.txt` cases per
    the RESEARCH test map, drive the cpu-fold SplitKernel, assert every tree field is bit-exact vs the
    golden. Add the ordering-invariant assertion cell: the returned right_leaf_index equals the leaf id the
    partition launch would consume.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness tree_mutation_parity -- --nocapture && cargo test --workspace 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness tree_mutation_parity` passes: all SplitKernel tree-field writes are bit-exact vs the split.txt golden; the ordering assert confirms right_leaf_index is produced before/for the partition consumer.
    - The flat CUDATree is allocated once (single `client.empty` site, D-15); NaN→0 uses branchless `select` (no nested-if in the `#[cube]` body).
    - `SplitScalars` is READ, not redefined (grep: no duplicate struct).
    - `cargo test --workspace` GREEN with `LGBM_CUDA_ON_DEVICE` unset; clippy clean.
  </acceptance_criteria>
  <done>The device flat CUDATree + SplitKernel land, structure bit-exact to the anchor; the Split-before-partition ordering invariant is asserted.</done>
</task>

<task type="auto">
  <name>Task 2: SplitCategoricalKernel + ShrinkageKernel + AddBiasKernel (D-14 f64 scalar math)</name>
  <files>crates/lgbm-compute/src/kernels/tree.rs, crates/oracle-harness/tests/tree_mutation_parity.rs</files>
  <read_first>
    - RESEARCH "Kernel Inventory Map" rows SplitCategoricalKernel (:138: same fan-out + kCategoricalMask, num_cat, extend cat_boundaries/_inner by bitset lengths), ShrinkageKernel (:139: leaf_value *= rate), AddBiasKernel (:140: leaf_value += val).
    - crates/lgbm-compute/src/kernels/split_info.rs — the reserved cat slabs (:230-233, MAX_CAT_PER_SPLIT=32) the categorical split appends to.
    - PATTERNS SP-1 (body + thin wrappers) + SP-5 (f64 scalar-only, no f64 per-row loop) + the tree.rs section note on Shrinkage/AddBias elementwise bodies (:156-157).
    - LightGBM-release-4.6.0.99/src/io/cuda/cuda_tree.cu:160 (SplitCategorical), :290 (Shrinkage), :303 (AddBias).
  </read_first>
  <action>
    Implement `SplitCategoricalKernel` reusing the Task-1 SplitKernel fan-out plus: set kCategoricalMask
    in the decision type, write `num_cat`, and append the split's bitset lengths to `cat_boundaries`/
    `cat_boundaries_inner` (the reserved slabs from split_info.rs). Implement `ShrinkageKernel`
    (`leaf_value *= rate`) and `AddBiasKernel` (`leaf_value += val`) as elementwise `#[cube]` bodies via the
    SP-1 body+wrapper shape; the scalar math stays f64 (SP-5/D-14 — these are per-leaf, not per-row).

    Extend `tree_mutation_parity` with a categorical-split field-write cell (vs the appropriate golden) and
    add a `tree::shrinkage` unit test in `lgbm-compute` asserting `leaf_value *= rate` and `+= bias` match a
    serial f64 reference over a small leaf-value vector on the cpu client.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute tree::shrinkage -- --nocapture && cargo test -p oracle-harness tree_mutation_parity -- --nocapture && cargo test --workspace 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-compute tree::shrinkage` passes: Shrinkage (`*= rate`) and AddBias (`+= val`) match a serial f64 reference element-for-element.
    - The categorical-split field-write cell in `tree_mutation_parity` is green (kCategoricalMask/num_cat/cat_boundaries append correct).
    - Shrinkage/AddBias use the SP-1 body+wrapper shape; scalar math is f64, no f64 per-row loop (D-14).
    - `cargo test --workspace` GREEN with the env unset.
  </acceptance_criteria>
  <done>Categorical split mutation + Shrinkage + AddBias land and are anchor-green. ODL-14 tree mutation complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → device tree write | untrusted leaf/node index + SplitScalars fields cross into the scalar tree kernels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Tampering | SplitKernel node/leaf index writes | mitigate | Node/leaf indices bounded by the pre-allocated `num_leaves`-sized flat CUDATree; NaN→0 coercion on numeric fields; scalar fan-out matches the reference `<<<3,5>>>`/`<<<3,6>>>` thread counts |
| T-18-05 | Tampering | cat_boundaries/bitset slab append | mitigate | Append bounded by the reserved `MAX_CAT_PER_SPLIT=32` slabs (split_info.rs:230-233); no per-split device alloc (D-15) |
| T-18-SC | Tampering | package installs | accept | No packages installed this phase — no supply-chain surface |
</threat_model>

<verification>
- `cargo test -p oracle-harness tree_mutation_parity` (numeric + categorical field writes + ordering) green.
- `cargo test -p lgbm-compute tree::shrinkage` green.
- `cargo test --workspace` GREEN with `LGBM_CUDA_ON_DEVICE` unset (ODL-19 merge gate).
- Grep: single `client.empty` site for the flat CUDATree (D-15); tree kernels are scalar/elementwise with f64 confined to leaf-value/gain math, no f64 per-row loop (D-14); `SplitScalars` not redefined.
- cpu f64 fold is the anchor (never GPU-vs-GPU); hip parity is exercised in 18-04's gate.
</verification>

<success_criteria>
ODL-14 on-device tree mutation is complete: SplitKernel writes the flat CUDATree structure bit-exact to
the anchor and returns right_leaf_index before the partition consumes it; SplitCategorical/Shrinkage/
AddBias are anchor-green; the merge gate is green with the env unset.
</success_criteria>

## Artifacts this phase produces

New symbols/files introduced by this plan (source-grounding exclusion list for downstream review):
- `tree.rs` (NEW): device flat `CUDATree` SoA + counted-alloc `new`, `set/get_decision_type` + `set/get_missing_type` comptime bit helpers, `SplitKernel` (`#[cube(launch)]` + host driver returning `right_leaf_index`), `SplitCategoricalKernel`, `ShrinkageKernel`, `AddBiasKernel`, host `lgbm_model::Tree` reconstruction.
- `tree_mutation_parity.rs`: SplitKernel field-write cell + Split-before-partition ordering cell + categorical-split cell un-ignored.
- `#[cfg(test)] mod` `tree::shrinkage` unit in `lgbm-compute`.

<output>
Create `.planning/phases/18-on-device-data-partition-tree-mutation-prediction/18-03-SUMMARY.md` when done
</output>
