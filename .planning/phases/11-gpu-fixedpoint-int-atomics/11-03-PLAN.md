---
phase: 11-gpu-fixedpoint-int-atomics
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - crates/lgbm-compute/examples/gpu_fixedpoint_resident_ab.rs
autonomous: true
requirements: [SPEC-3]
must_haves:
  truths:
    - "A device-time A/B harness measures the LIVE resident u64 fixed-point build vs an f32 twin in the wide regime"
    - "The f32 arm is the build_f32_rp TWIN (the production f32 resident launcher is removed by Plan 01); both arms share the build_rp layout so the ratio isolates atomic-type cost, but the f32 arm is NOT the literal production f32 path"
    - "The A/B uses the spike measurement discipline: interleaved median+p25/p75, 2 process runs, SEP-WIN bar, accumulate-launches-then-one-read (compute-throughput, not launch-bound)"
    - "The result confirms the integer build is NOT slower in the wide regime (expect ~1.3-1.7x on large leaves; null-but-not-regressed on small leaves is acceptable)"
  artifacts:
    - path: "crates/lgbm-compute/examples/gpu_fixedpoint_resident_ab.rs"
      provides: "device-time A/B of the resident u64 vs f32-twin build in the wide large-leaf regime"
      contains: "SEP"
  key_links:
    - from: "gpu_fixedpoint_resident_ab.rs"
      to: "the live resident u64 build path"
      via: "drives the production u64 resident kernel + an f32 twin in the realistic resident layout"
      pattern: "median|p25|p75|SEP"
---

<objective>
Add a device-time A/B harness that confirms the LIVE resident u64 fixed-point build (Plan 01)
is not slower than the f32 path in the wide large-leaf regime, using the spike-018/019
measurement discipline. This is the SPEC item-3 confirmation: ~1.3-1.7x expected on large
leaves (the wide root); null-but-not-regressed acceptable on small leaves.

Purpose: lock in the perf claim with a reproducible, regime-correct A/B. Measurement is the
CubeCL device-time proxy on the 8-CU APU (wall-clock unvalidatable here). Discrete-gfx110x
confirmation is ideal but NOT blocking (option-ii, user-accepted).

> F32-ARM EXPECTATION (executor MUST honor): Plan 01 switches `resident_raw_build_into` to
> dispatch the u64 kernel, so the production f32 resident launcher is REMOVED from the live
> path. The A/B's f32 arm is therefore the `build_f32_rp` TWIN (the example-local f32 launcher
> from `gpu_int_vs_f32_psweep.rs`), NOT the literal production f32 resident path. Both arms
> share the SAME `build_rp` resident layout (`CubeCount = (num_features, P)`, double
> indirection through `leaf_rows` + resident column) and differ ONLY in cell/atomic type, so
> the device-time RATIO validly isolates the atomic-type cost -- this is exactly the spike-018b
> / spike-019 methodology. State this in the example header so no one mistakes the f32 arm for a
> production launcher.

Output: a runnable `--features rocm` example that prints the interleaved median+p25/p75 and a
SEP-WIN/overlap verdict per regime.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-gpu-fixedpoint-int-atomics/11-CONTEXT.md
@.planning/phases/11-gpu-fixedpoint-int-atomics/11-PATTERNS.md
@.planning/spikes/018-fixedpoint-int-atomics/README.md
@.planning/spikes/019-int-atomic-contention-regime/README.md
@.claude/skills/spike-findings-lightgbm_rs/SKILL.md

# The Plan-01 SUMMARY names the live u64 resident kernel/launcher to drive:
@.planning/phases/11-gpu-fixedpoint-int-atomics/11-01-SUMMARY.md

# The validated harness shape to copy (median/p25/p75, 2 runs, SEP, accumulate-then-read):
@crates/lgbm-compute/examples/gpu_int_vs_f32_psweep.rs
@crates/lgbm-compute/examples/gpu_fixedpoint_i64.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Device-time A/B example for the resident u64 vs f32-twin build in the wide regime</name>
  <files>crates/lgbm-compute/examples/gpu_fixedpoint_resident_ab.rs</files>
  <read_first>
    - examples/gpu_int_vs_f32_psweep.rs:130-268 -- the full harness: `build_f32_rp`/`build_u64_rp` twins, the `bench_f32`/`bench_u64` accumulate-LAUNCHES-then-one-read closures, the interleaved 9-rep `median[p25..p75]` loop, the `SEP-WIN` decision (`u75 < f25`), and the regime config table (16x1M = the heavy wide regime where the win is ~1.3-1.7x; 16x200k = the null light-load regime). `build_f32_rp` here IS the f32 twin the A/B's f32 arm uses (the production f32 resident launcher is gone after Plan 01).
    - examples/gpu_fixedpoint_i64.rs:198-264 -- the compute-throughput bench rationale (accumulate launches into ONE reused buffer, single readback forces device sync; NOT per-launch readback which is launch-bound) + the 2-process-run sign-stability note.
    - spikes/019-int-atomic-contention-regime/README.md:24-60 -- the regime table + "win lands in heavy atomic load (wide root/large leaves)" mapping; use it to choose the A/B configs.
    - The Plan-01 SUMMARY -- the live u64 resident kernel/launcher names so the A/B's u64 arm drives the PRODUCTION path (`construct_leaf_hist_resident_lds_kernel_u64`); confirms the production f32 resident launcher is no longer on the live path (hence the f32 arm is the `build_f32_rp` twin).
  </read_first>
  <action>
    Create `examples/gpu_fixedpoint_resident_ab.rs`, `#[cfg(feature = "rocm")]`, modeled on `gpu_int_vs_f32_psweep.rs`. Drive the LIVE production resident u64 build (the Plan-01 `construct_leaf_hist_resident_lds_kernel_u64` kernel / `resident_raw_build_into` dispatch) as the "u64" arm. For the "f32" arm use the `build_f32_rp` TWIN (the example-local f32 resident launcher from `gpu_int_vs_f32_psweep.rs`) -- NOT the production f32 resident path (Plan 01 removed it from the live path). Both arms run the realistic resident `build_rp` layout (`CubeCount = (num_features, P)`, double indirection through `leaf_rows` + resident column) and differ ONLY in atomic/cell type, so the ratio isolates atomic-type cost (the spike-018b/019 methodology). Add a header comment stating explicitly that the f32 arm is the `build_f32_rp` twin sharing the `build_rp` layout, NOT a production launcher.

    Use the spike measurement discipline verbatim: warm-up reps, then interleaved `REPS = 9..11` reps computing `median` + `p25`/`p75` for each arm; `ratio = f32_median / u64_median`; print `SEP-WIN` when `u64_p75 < f32_p25` else `overlap`. Accumulate `LAUNCHES` launches into ONE reused buffer per timed sample and read back ONCE (compute-throughput, not launch-bound). Add a header comment instructing the operator to run the whole process >=2x and check sign-stability (the spike CONVENTIONS).

    Cover at least the two decisive regimes from spike-019: the HEAVY wide regime (e.g. 16 features x 1M rows -- where the win is ~1.3-1.7x SEP) and the LIGHT regime (e.g. 16 x 200k -- where the result is null/overlap, which is acceptable). Optionally sweep row-partition P (spike-019 showed the win composes with P up to 16). Print a per-regime verdict line and a closing diagnosis. Do NOT assert pass/fail in the example (it is an operator-run device-time proxy, wall-clock unvalidatable here); the verdict is the printed SEP-WIN/overlap + ratio. Record the captured numbers in the SUMMARY.

    Keep a `#[cfg(not(feature = "rocm"))] fn main()` stub printing "requires --features rocm" (mirror the spike examples) so the example compiles in CPU-only builds.
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute --features rocm --example gpu_fixedpoint_resident_ab 2>&1 | tail -5 && cargo build -p lgbm-compute --example gpu_fixedpoint_resident_ab 2>&1 | tail -3</automated>
  </verify>
  <done>`examples/gpu_fixedpoint_resident_ab.rs` builds under `--features rocm` and CPU-only; running it under rocm prints per-regime median[p25..p75] for the u64 (live) vs f32-twin resident build with a SEP-WIN/overlap verdict and ratio, using the accumulate-then-one-read compute-throughput method. The header states the f32 arm is the `build_f32_rp` twin (not production). The heavy wide regime shows the integer build is not slower (expected SEP-WIN ~1.3-1.7x); the light regime overlap is acceptable.</done>
  <acceptance_criteria>
    - Example is `#[cfg(feature = "rocm")]` with a CPU-only stub `main`; both build configs compile.
    - The u64 arm drives the LIVE production resident u64 kernel/launcher; the f32 arm is the `build_f32_rp` twin (header comment states it is NOT a production launcher).
    - The two arms differ ONLY in cell/atomic type and use the realistic resident `build_rp` layout (double indirection).
    - Uses interleaved median+p25/p75, accumulate-LAUNCHES-then-ONE-read, SEP-WIN (`u64_p75 < f32_p25`) decision, and a 2-process-run instruction.
    - Covers >=2 regimes (heavy wide + light); prints a per-regime ratio + verdict.
    - No pass/fail assert (device-time proxy); captured numbers go in the SUMMARY.
  </acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- **`crates/lgbm-compute/examples/gpu_fixedpoint_resident_ab.rs`** -- a new `--features rocm` device-time A/B example. Drives the live resident u64 fixed-point build (u64 arm) vs the `build_f32_rp` f32 TWIN (f32 arm -- the production f32 resident launcher is removed by Plan 01) in the realistic shared `build_rp` layout; prints interleaved `median[p25..p75]`, `ratio = f32/u64`, and a `SEP-WIN`/`overlap` verdict per regime (heavy wide + light). Header states the f32 arm is a twin, not production. Operator runs >=2 processes for sign-stability. CPU-only stub `main` for non-rocm builds.

No new production symbols, env flags, kernels, or tests. The captured device-time numbers (per-regime ratio + SEP verdict) are recorded in the SUMMARY.
</artifacts_produced>

<verification>
- `cargo build -p lgbm-compute --features rocm --example gpu_fixedpoint_resident_ab` succeeds.
- `cargo build -p lgbm-compute --example gpu_fixedpoint_resident_ab` (CPU-only) succeeds (stub main).
- Running under rocm prints per-regime median[p25..p75] + ratio + SEP/overlap; the heavy wide regime is not slower than the f32 twin (SUMMARY records the numbers).
</verification>

<success_criteria>
- A device-time A/B (spike harness shape) confirms the integer resident build is not slower than the f32 twin in the wide regime; ~1.3-1.7x expected on large leaves; null-but-not-regressed on small leaves acceptable (SPEC item 3).
- The f32 arm is correctly the `build_f32_rp` twin sharing the `build_rp` layout (ratio isolates atomic-type cost), explicitly documented as not-production.
- Measurement uses the spike discipline (median+p25/p75, 2 runs, SEP, compute-throughput).
</success_criteria>

<output>
Create `.planning/phases/11-gpu-fixedpoint-int-atomics/11-03-SUMMARY.md` when done, recording: the example path, the per-regime ratios + SEP verdicts captured across 2 process runs, confirmation the f32 arm is the `build_f32_rp` twin (not production), and the confirmation that the wide regime is not-slower.
</output>
