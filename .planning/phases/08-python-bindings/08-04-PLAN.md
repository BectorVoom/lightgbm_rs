---
phase: 08-python-bindings
plan: 04
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - crates/lgbm-python/src/marshal.rs
  - crates/lgbm-python/src/dataset.rs
  - crates/lgbm-python/src/lib.rs
  - crates/lgbm-python/python/lightgbm_rs/__init__.py
  - crates/lgbm-python/python/tests/test_polars_input.py
autonomous: true
requirements: [PYB-02]
must_haves:
  truths:
    - "A Python user can pass a polars DataFrame as training/predict input, ingested zero-copy via Arrow (pyo3-polars), no numpy round-trip (D-03)"
    - "polars Categorical/Enum/string columns auto-route to LightGBM categorical features; numeric columns route to numeric bins (D-04, reusing TRL-06 categorical splits)"
    - "An explicit categorical_feature override (list of column names/indices) takes precedence over dtype auto-routing"
    - "polars-fed training A/B-matches an equivalent numpy/pandas run within ~1e-6"
  artifacts:
    - path: "crates/lgbm-python/src/marshal.rs"
      provides: "pyo3-polars PyDataFrame → per-column Arrow extraction + dtype→feature-kind routing"
      contains: "PyDataFrame"
    - path: "crates/lgbm-python/python/tests/test_polars_input.py"
      provides: "polars numeric + Categorical routing parity test"
      contains: "Categorical"
  key_links:
    - from: "crates/lgbm-python/src/marshal.rs"
      to: "crates/lgbm/src/booster.rs"
      via: "categorical column indices → RawCorpus.categorical_features → BinMapper categorical"
      pattern: "categorical"
---

<objective>
Add polars DataFrame input, ingested zero-copy via Arrow (pyo3-polars), with
dtype-driven routing: Categorical/Enum/string → LightGBM categorical features
(the Phase-7 TRL-06 categorical splits, end-to-end from a DataFrame), numeric →
numeric bins (D-03/D-04). Honor an explicit `categorical_feature` override.

Purpose: polars is the user's explicitly named first-class modern input
(CONTEXT specifics). This slice delivers the DataFrame→categorical-split path a
user can actually exercise, distinct from the numpy/sparse numeric paths.
Output: a zero-copy polars ingest path feeding the same binning/train pipeline,
with a parity test over numeric + categorical columns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-python-bindings/08-RESEARCH.md
@.planning/phases/08-python-bindings/08-PATTERNS.md
@crates/lgbm/src/booster.rs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan:
- `crates/lgbm-python/src/marshal.rs`: `polars_df_to_corpus` (PyDataFrame → owned RawCorpus rows + per-column feature-kind classification), `classify_polars_dtype` (Categorical/Enum/Utf8 → categorical; numeric → numeric).
- `crates/lgbm-python/src/dataset.rs`: `Dataset` constructor accepting a polars DataFrame (via pyo3-polars `PyDataFrame`) + an optional `categorical_feature` override param.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: polars DataFrame zero-copy ingest + dtype→feature-kind routing (D-03/D-04)</name>
  <files>crates/lgbm-python/src/marshal.rs, crates/lgbm-python/src/dataset.rs, crates/lgbm-python/src/lib.rs</files>
  <read_first>
    - crates/lgbm-python/src/marshal.rs (08-02/08-03 numpy + sparse paths — the ingest patterns to extend)
    - crates/lgbm-python/src/dataset.rs (Dataset constructor)
    - crates/lgbm/src/booster.rs (RawCorpus.categorical_features field from 08-01 — the routing target; build_feature_columns_from_raw routes those indices to BinMapper categorical)
    - crates/lgbm-dataset/src/bin_mapper.rs (find_bin_categorical :416 — the categorical path TRL-06 splits consume)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (D-03 polars zero-copy via Arrow; D-04 dtype auto-routing; Pattern: pyo3-polars PyDataFrame/PySeries; Anti-Pattern: numpy round-trip erases Categorical/Enum; Open Question 1 categorical_feature override)
    - .planning/phases/08-python-bindings/08-PATTERNS.md (marshal.rs polars section: pyo3-polars Arrow extraction, dtype-route; No Analog Found — pyo3-polars genuinely new surface)
    - LightGBM/python-package/lightgbm/basic.py (READ-ONLY: how official handles pandas Categorical → categorical_feature='auto')
  </read_first>
  <action>
    In `marshal.rs`, add `polars_df_to_corpus(df: pyo3_polars::PyDataFrame, categorical_override: Option<Vec<String|usize>>) -> PyResult<(rows, feature_names, categorical_indices)>`. Consume the polars DataFrame's Arrow-backed columns DIRECTLY in Rust (pyo3-polars `PyDataFrame` → `polars::DataFrame`), iterating columns: classify each via `classify_polars_dtype` — `DataType::Categorical`/`Enum`/`Utf8(String)` → categorical feature; numeric (`Int*`/`Float*`/`Boolean`) → numeric. Extract numeric columns to f64 rows; for categorical columns, extract the physical/category codes (the integer category indices polars already stores) as the raw values BinMapper's categorical path consumes — NO numpy round-trip (Anti-Pattern: erases dtype). Build the categorical-feature index set; if `categorical_override` is provided (list of names or indices), it takes PRECEDENCE over dtype routing (D-04 / Open-Q1; default `'auto'` = dtype routing). Feed the result into a `RawCorpus` with `categorical_features` set so `build_feature_columns_from_raw` (08-01) routes those columns through `find_bin_categorical`. Validate column lengths equal across the frame → `PyValueError`. No panic.
  </action>
  <verify>
    <automated>cargo build -p lgbm-python 2>&1 | tail -20 && cargo clippy -p lgbm-python 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p lgbm-python` compiles with pyo3-polars wired (proves the 0.26 ↔ pyo3 0.27 ABI resolves with numpy 0.27 in the same tree — Pitfall 1 re-confirmed).
    - `grep -n "PyDataFrame" crates/lgbm-python/src/marshal.rs` matches; `grep -n "Categorical\|Enum\|Utf8" crates/lgbm-python/src/marshal.rs` shows dtype routing.
    - No numpy round-trip for polars (grep: the polars path does not convert to PyReadonlyArray).
    - categorical_override precedence covered by a unit test or the Task 2 pytest.
    - No `unwrap`/`panic!`; clippy clean.
  </acceptance_criteria>
  <done>polars DataFrames ingest zero-copy via Arrow with dtype-driven categorical/numeric routing and an explicit override, feeding the same binning/train pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: polars input parity pytest (numeric + Categorical, D-04 routing)</name>
  <files>crates/lgbm-python/python/tests/test_polars_input.py, crates/lgbm-python/python/lightgbm_rs/__init__.py</files>
  <read_first>
    - crates/lgbm-python/python/tests/test_numpy_sparse_parity.py (A/B harness shape from 08-03)
    - crates/oracle-harness/tests/fixtures/REFERENCE_MANIFEST.md (deterministic settings)
    - .planning/phases/08-python-bindings/08-VALIDATION.md (Per-Task Verification Map PYB-02 polars row: `pytest .../test_polars_input.py`; Threat T-08 V5)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (Environment Availability — polars new dep, version floor A1; Validation A/B shape)
  </read_first>
  <action>
    Create `crates/lgbm-python/python/tests/test_polars_input.py`. `test_polars_numeric_matches_numpy`: build a polars DataFrame of numeric columns and the equivalent numpy array; train both via `lightgbm_rs`; assert identical predictions (the polars path must equal the numpy path on pure-numeric data). `test_polars_categorical_routes`: build a DataFrame with a `Categorical`/`Enum` column; train via `lightgbm_rs`; assert it A/B-matches real `lightgbm` fed the equivalent pandas Categorical (or a numpy + `categorical_feature` list) within `atol=1e-6` — proving Categorical columns drive the TRL-06 categorical splits. `test_categorical_override`: assert an explicit `categorical_feature=[...]` overrides dtype routing. `pytest.importorskip("polars")` + `importorskip("lightgbm")` with printed reasons (SKIP cleanly, never silently pass). Document `maturin develop` rebuild. Do NOT git-add `LightGBM/`.
  </action>
  <verify>
    <automated>cd crates/lgbm-python && (command -v maturin >/dev/null && maturin develop 2>&1 | tail -3 || true); python -m pytest python/tests/test_polars_input.py 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest test_polars_input.py` passes (or SKIPs with printed reason if polars/lightgbm absent).
    - `test_polars_numeric_matches_numpy` asserts polars numeric == numpy numeric predictions (identical).
    - `test_polars_categorical_routes` asserts ~1e-6 A/B parity with a Categorical column driving categorical splits.
    - `test_categorical_override` asserts the override precedence.
    - `cargo test --workspace` GREEN; `LightGBM/` not git-added.
  </acceptance_criteria>
  <done>polars numeric input equals the numpy path, Categorical/Enum columns route to TRL-06 categorical splits matching real lightgbm, and the explicit override works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python polars → PyO3 (Arrow FFI) | untrusted DataFrame columns (dtype, length) cross the Arrow C-data-interface boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | uneven column lengths / unexpected dtype | mitigate | Validate equal column lengths + classify dtype explicitly; unknown/unsupported dtype → `PyValueError` before binning (Security V5) |
| T-08-04-02 | Tampering (silent corruption) | numpy round-trip erasing Categorical/Enum | mitigate | Consume Arrow columns directly (no numpy round-trip) so dtype routing is faithful (D-03/D-04 Anti-Pattern) |
| T-08-04-03 | DoS | malformed Arrow buffer | mitigate | pyo3-polars handles the Arrow C-data-interface contract; all paths return `PyResult`, no panic across FFI |
| T-08-04-SC | Tampering | cargo/pip installs | mitigate | pyo3-polars 0.26.0 already pinned + audited (RESEARCH § Package Legitimacy, Approved); polars PyPI is an official-parity optional dep (A1 floor to confirm against polars 0.53 ABI at build) |
</threat_model>

<verification>
- `cargo build -p lgbm-python` compiles with pyo3-polars (triangle re-confirmed in one tree).
- pytest: polars numeric == numpy; Categorical routes to categorical splits matching real lightgbm ~1e-6; override works (or SKIP-marked).
- `cargo test --workspace` GREEN; clippy clean; no panic across FFI; `LightGBM/` not git-added.
</verification>

<success_criteria>
- polars DataFrames ingest zero-copy via Arrow with no numpy round-trip (D-03).
- Categorical/Enum/string columns auto-route to categorical features; numeric to numeric bins; explicit override honored (D-04).
- polars-fed training matches the equivalent numpy/pandas run within ~1e-6 — PYB-02 polars satisfied.
</success_criteria>

<output>
Create `.planning/phases/08-python-bindings/08-04-SUMMARY.md` when done.
</output>
