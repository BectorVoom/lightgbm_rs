---
phase: 08-python-bindings
plan: 07
type: execute
wave: 7
depends_on: ["08-06"]
files_modified:
  - crates/lgbm-python/python/lightgbm_rs/sklearn.py
  - crates/lgbm-python/python/lightgbm_rs/callback.py
  - crates/lgbm-python/python/lightgbm_rs/engine.py
  - crates/lgbm-python/python/lightgbm_rs/plotting.py
  - crates/lgbm-python/python/lightgbm_rs/__init__.py
  - crates/lgbm-python/python/tests/test_sklearn_parity.py
  - crates/lgbm-python/python/tests/test_callbacks_cv.py
autonomous: true
requirements: [PYB-03]
must_haves:
  truths:
    - "A Python user can use LGBMClassifier/LGBMRegressor/LGBMRanker with fit/predict/predict_proba matching the official sklearn wrappers' semantics (PYB-03)"
    - "The training-callback list protocol works: callbacks=[early_stopping(), log_evaluation(), record_evaluation(), reset_parameter()] (D-09)"
    - "lgb.cv k-fold cross-validation runs as pure-Python orchestration over train() (D-09)"
    - "Feature importance is exposed and plot_importance/plot_tree/plot_metric render (matplotlib/graphviz optional deps) (D-09)"
  artifacts:
    - path: "crates/lgbm-python/python/lightgbm_rs/sklearn.py"
      provides: "LGBMClassifier/LGBMRegressor/LGBMRanker"
      contains: "class LGBMClassifier"
    - path: "crates/lgbm-python/python/lightgbm_rs/callback.py"
      provides: "early_stopping/log_evaluation/record_evaluation/reset_parameter"
      contains: "def early_stopping"
    - path: "crates/lgbm-python/python/lightgbm_rs/engine.py"
      provides: "cv()"
      contains: "def cv"
  key_links:
    - from: "crates/lgbm-python/python/lightgbm_rs/sklearn.py"
      to: "crates/lgbm-python/python/lightgbm_rs/__init__.py"
      via: "uses compiled _core train/Booster"
      pattern: "train|Booster"
---

<objective>
Add the thin pure-Python wrapper layer (no new Rust): sklearn-style estimators
`LGBMClassifier`/`LGBMRegressor`/`LGBMRanker` (PYB-03), the training-callback list
protocol, `lgb.cv`, and feature-importance plotting — all mirroring the official
package and orchestrating the compiled `_core` engine (D-09).

Purpose: These are the high-level conveniences users expect for drop-in parity with
official lightgbm. They are pure-Python orchestration over the validated `_core`
train/predict/eval surface — no new numerical code.
Output: the `python/lightgbm_rs/{sklearn,callback,engine,plotting}.py` modules +
their parity tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-python-bindings/08-RESEARCH.md
@.planning/phases/08-python-bindings/08-PATTERNS.md
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan (all pure-Python, in `python/lightgbm_rs/`):
- `sklearn.py`: `LGBMClassifier`, `LGBMRegressor`, `LGBMRanker`, `LGBMModel` (base), `_ObjectiveFunctionWrapper`, `_EvalFunctionWrapper`.
- `callback.py`: `early_stopping`, `log_evaluation`, `record_evaluation`, `reset_parameter`, `CallbackEnv`.
- `engine.py`: `cv`, `train` (the public Python `train` wrapper that applies the callback list over the `_core` loop).
- `plotting.py`: `plot_importance`, `plot_tree`, `plot_metric`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Training-callback list protocol + lgb.cv (engine.py + callback.py, D-09)</name>
  <files>crates/lgbm-python/python/lightgbm_rs/callback.py, crates/lgbm-python/python/lightgbm_rs/engine.py, crates/lgbm-python/python/lightgbm_rs/__init__.py</files>
  <read_first>
    - crates/lgbm-python/python/lightgbm_rs/__init__.py (the compiled _core re-exports: train/Booster/Dataset)
    - crates/lgbm-python/src/booster.rs (the _core train #[pyfn] signature: params, dataset, num_boost_round, fobj/feval, valid sets, eval history return shape)
    - LightGBM/python-package/lightgbm/callback.py (READ-ONLY: early_stopping/log_evaluation/record_evaluation/reset_parameter signatures, CallbackEnv fields, before_iteration + order attributes)
    - LightGBM/python-package/lightgbm/engine.py (READ-ONLY: train() callback application + cv() k-fold orchestration, _make_n_folds, stratified handling)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (D-09 callback protocol + cv pure-Python over train; sklearn _EvalFunctionWrapper)
    - .planning/phases/08-python-bindings/08-PATTERNS.md (python package semantic-ref table: callback.py/engine.py)
  </read_first>
  <action>
    Create `crates/lgbm-python/python/lightgbm_rs/callback.py` mirroring the official `callback.py`: a `CallbackEnv` dataclass/namedtuple (model, params, iteration, begin_iteration, end_iteration, evaluation_result_list); `early_stopping(stopping_rounds, first_metric_only=False, min_delta=...)`, `log_evaluation(period=1)`, `record_evaluation(eval_result)`, `reset_parameter(**kwargs)` — each returns a callable carrying `order`/`before_iteration` attributes per the official protocol. Create `crates/lgbm-python/python/lightgbm_rs/engine.py` with a Python `train(params, train_set, num_boost_round, valid_sets=..., callbacks=..., fobj=..., feval=..., ...)` that drives the compiled `_core` per-iteration loop (or `_core.train` returning eval history) and APPLIES the callback list — sort by `order`, honor `before_iteration`, raise the official early-stop signal to break the loop. Add `cv(params, train_set, num_boost_round, folds=None, nfold=5, stratified=True, callbacks=..., ...)`: pure-Python k-fold split mirroring `_make_n_folds`, train per fold over `_core`, aggregate per-iteration mean/std eval, return the official cv-results dict. No new Rust. Re-export `train`/`cv`/the four callbacks from `__init__.py`.
  </action>
  <verify>
    <automated>cd crates/lgbm-python && (command -v maturin >/dev/null && maturin develop 2>&1 | tail -3 || true); python -c "import lightgbm_rs; print(lightgbm_rs.cv, lightgbm_rs.early_stopping, lightgbm_rs.log_evaluation, lightgbm_rs.record_evaluation, lightgbm_rs.reset_parameter)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import lightgbm_rs; lightgbm_rs.cv; lightgbm_rs.early_stopping"` resolves all five symbols.
    - `grep -n "def early_stopping\|def log_evaluation\|def record_evaluation\|def reset_parameter" crates/lgbm-python/python/lightgbm_rs/callback.py` shows all four.
    - `grep -n "def cv\|def train" crates/lgbm-python/python/lightgbm_rs/engine.py` shows both.
    - The callback application sorts by `order` and honors `before_iteration` (grep or test in Task 3).
    - No new Rust files modified (this task is pure-Python).
  </acceptance_criteria>
  <done>The training-callback list protocol (early_stopping/log_evaluation/record_evaluation/reset_parameter) and lgb.cv work as pure-Python orchestration over the _core engine.</done>
</task>

<task type="auto">
  <name>Task 2: sklearn wrappers + plotting (sklearn.py + plotting.py, PYB-03 / D-09)</name>
  <files>crates/lgbm-python/python/lightgbm_rs/sklearn.py, crates/lgbm-python/python/lightgbm_rs/plotting.py, crates/lgbm-python/python/lightgbm_rs/__init__.py</files>
  <read_first>
    - crates/lgbm-python/python/lightgbm_rs/engine.py (the Python train/cv from Task 1 — the estimators call these)
    - crates/lgbm-python/python/lightgbm_rs/__init__.py (Booster surface: predict, feature_importance, model_to_string)
    - LightGBM/python-package/lightgbm/sklearn.py (READ-ONLY: LGBMModel base, LGBMClassifier/Regressor/Ranker, fit/predict/predict_proba, _ObjectiveFunctionWrapper/_EvalFunctionWrapper, sklearn tags, n_features_in_/classes_ attributes)
    - LightGBM/python-package/lightgbm/plotting.py (READ-ONLY: plot_importance/plot_tree/plot_metric signatures, matplotlib/graphviz usage)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (PYB-03 sklearn wrappers; D-09 plotting matplotlib/graphviz optional; Supporting deps scikit-learn>=0.24.2)
    - .planning/phases/08-python-bindings/08-PATTERNS.md (python package semantic-ref: sklearn.py/plotting.py)
  </read_first>
  <action>
    Create `crates/lgbm-python/python/lightgbm_rs/sklearn.py` mirroring the official class hierarchy: `LGBMModel` (base: stores params, `fit(X, y, sample_weight, eval_set, eval_metric, callbacks, ...)` building a `_core` Dataset + calling the Python `train` from engine.py; `predict(X)`; `feature_importances_` property; `n_features_in_`, `booster_`), `LGBMRegressor` (regression objective default), `LGBMClassifier` (binary/multiclass; `predict_proba`, `classes_`, label encoding), `LGBMRanker` (group/query support, lambdarank default). Reuse `_ObjectiveFunctionWrapper`/`_EvalFunctionWrapper` to bridge sklearn-style custom obj/metric to the `_core` fobj/feval contract (PYB-04 path from 08-06). Use scikit-learn base classes/tags where present (optional import; degrade gracefully if scikit-learn absent). Create `crates/lgbm-python/python/lightgbm_rs/plotting.py`: `plot_importance(booster, importance_type='split'|'gain', ...)`, `plot_tree(booster, tree_index, ...)`, `plot_metric(eval_result, ...)` — import matplotlib/graphviz lazily inside the functions (optional deps; raise an informative ImportError if missing). Re-export the estimators + plotting from `__init__.py`. No new Rust.
  </action>
  <verify>
    <automated>cd crates/lgbm-python && (command -v maturin >/dev/null && maturin develop 2>&1 | tail -3 || true); python -c "import lightgbm_rs; print(lightgbm_rs.LGBMClassifier, lightgbm_rs.LGBMRegressor, lightgbm_rs.LGBMRanker, lightgbm_rs.plot_importance)"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import lightgbm_rs; lightgbm_rs.LGBMClassifier; lightgbm_rs.LGBMRegressor; lightgbm_rs.LGBMRanker; lightgbm_rs.plot_importance"` resolves all.
    - `grep -n "class LGBMClassifier\|class LGBMRegressor\|class LGBMRanker\|class LGBMModel" crates/lgbm-python/python/lightgbm_rs/sklearn.py` shows all four.
    - `LGBMClassifier` exposes `predict_proba` and `classes_` (grep).
    - `plotting.py` imports matplotlib/graphviz lazily inside functions (grep: imports inside `def`, not module top).
    - Covered by Task 3 parity tests.
  </acceptance_criteria>
  <done>LGBMClassifier/LGBMRegressor/LGBMRanker mirror the official sklearn API (fit/predict/predict_proba/feature_importances_) and plotting helpers render via optional matplotlib/graphviz.</done>
</task>

<task type="auto">
  <name>Task 3: sklearn + callbacks/cv A/B parity pytest (PYB-03 / D-09)</name>
  <files>crates/lgbm-python/python/tests/test_sklearn_parity.py, crates/lgbm-python/python/tests/test_callbacks_cv.py</files>
  <read_first>
    - crates/lgbm-python/python/tests/test_booster_parity.py (A/B harness shape)
    - crates/oracle-harness/tests/fixtures/REFERENCE_MANIFEST.md (deterministic settings to pin both sides)
    - .planning/phases/08-python-bindings/08-VALIDATION.md (Per-Task Verification Map PYB-03 row: `pytest .../test_sklearn_parity.py`; D-09 callbacks/cv)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (A/B harness; scikit-learn optional dep)
  </read_first>
  <action>
    Create `crates/lgbm-python/python/tests/test_sklearn_parity.py`: for each of `LGBMRegressor`/`LGBMClassifier`/`LGBMRanker`, fit on the same data/params (deterministic settings) via BOTH real `lightgbm` and `lightgbm_rs`, assert `np.testing.assert_allclose(rs.predict(Xt), ref.predict(Xt), atol=1e-6)` (and `predict_proba` for the classifier). Create `crates/lgbm-python/python/tests/test_callbacks_cv.py`: `test_early_stopping_callback` (callbacks=[early_stopping(...)] stops at the same iteration as real lightgbm), `test_record_evaluation` (eval history dict matches), `test_cv` (`lightgbm_rs.cv` results dict mean/std matches real `lightgbm.cv` within atol=1e-6). `importorskip("sklearn")`, `importorskip("lightgbm")`, `importorskip("lightgbm_rs")` with printed reasons (SKIP cleanly). Document `maturin develop`. Do NOT git-add `LightGBM/`.
  </action>
  <verify>
    <automated>cd crates/lgbm-python && (command -v maturin >/dev/null && maturin develop 2>&1 | tail -3 || true); python -m pytest python/tests/test_sklearn_parity.py python/tests/test_callbacks_cv.py 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest test_sklearn_parity.py` passes (or SKIPs with printed reason): all three estimators within `atol=1e-6` vs real lightgbm, classifier `predict_proba` matches.
    - `pytest test_callbacks_cv.py` passes: early_stopping stops at the same iteration, record_evaluation matches, `cv` results match within `atol=1e-6`.
    - `cargo test --workspace` still GREEN (no Rust regression); `LightGBM/` not git-added.
  </acceptance_criteria>
  <done>The sklearn wrappers, callback list protocol, and lgb.cv all A/B-match the official package within ~1e-6 — PYB-03 + the D-09 high-level surface satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python user → pure-Python wrappers | wrappers marshal user X/y/params into _core Dataset/train; no NEW FFI boundary (delegates to the already-validated _core) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-01 | Tampering | sklearn fit input shapes | mitigate | Validation happens at the _core Dataset boundary (08-02/03 V5 checks); wrappers surface those errors as-is (no new unchecked indexing in Python) |
| T-08-07-02 | DoS | plotting optional-dep import | accept | matplotlib/graphviz imported lazily; missing dep raises a clear ImportError (no security impact; visual-only, optional) |
| T-08-07-03 | Tampering | cv fold-split logic | mitigate | Pure-Python k-fold over numpy indices; no unchecked native indexing; degrades to _core's validated train |
| T-08-07-SC | Tampering | pip installs (scikit-learn/matplotlib/graphviz) | mitigate | All are official-parity PyPI deps cited from LightGBM/python-package/pyproject.toml (RESEARCH § Package Legitimacy); no NEW Rust crates this plan |
</threat_model>

<verification>
- `import lightgbm_rs` resolves the estimators, callbacks, cv, and plotting symbols.
- pytest: sklearn estimators + callbacks + cv A/B-match real lightgbm within ~1e-6 (or SKIP-marked).
- `cargo test --workspace` GREEN (no Rust change); `LightGBM/` not git-added.
</verification>

<success_criteria>
- LGBMClassifier/LGBMRegressor/LGBMRanker match the official sklearn wrappers' semantics (PYB-03).
- The callback list protocol, lgb.cv, and feature-importance plotting work and match the official package (D-09).
</success_criteria>

<output>
Create `.planning/phases/08-python-bindings/08-07-SUMMARY.md` when done.
</output>
