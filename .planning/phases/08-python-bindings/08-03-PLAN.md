---
phase: 08-python-bindings
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - crates/lgbm-python/src/marshal.rs
  - crates/lgbm-python/src/dataset.rs
  - crates/lgbm-python/src/lib.rs
  - crates/lgbm-python/python/lightgbm_rs/__init__.py
  - crates/lgbm-python/python/tests/test_numpy_sparse_parity.py
autonomous: true
requirements: [PYB-02]
must_haves:
  truths:
    - "A Python user can pass BOTH f32 and f64 dense numpy arrays and get results matching the official package for either width (PYB-02, SC#2)"
    - "scipy CSR and CSC sparse matrices are accepted and routed through the existing bit-exact ingest::from_csr/from_csc (D-05)"
    - "Non-contiguous / wrong-dtype arrays are handled explicitly (contiguity repair, dtype dispatch, single f32→f64 widen site)"
    - "Malformed CSR/CSC indptr/indices are rejected with a ValueError before any indexing (Security V5)"
  artifacts:
    - path: "crates/lgbm-python/src/marshal.rs"
      provides: "f32+f64 dense dispatch + scipy CSR/CSC slice extraction"
      contains: "from_csr"
    - path: "crates/lgbm-python/python/tests/test_numpy_sparse_parity.py"
      provides: "A/B parity over f32/f64/csr/csc"
      contains: "csr"
  key_links:
    - from: "crates/lgbm-python/src/marshal.rs"
      to: "crates/lgbm-dataset/src/ingest.rs"
      via: "from_csr/from_csc delegation"
      pattern: "from_csr|from_csc"
---

<objective>
Widen the Python input surface to satisfy PYB-02 fully: accept f32 AND f64 dense
numpy arrays (matching official results for either width) and scipy CSR/CSC sparse
matrices routed through the existing bit-exact `ingest::from_csr`/`from_csc` (D-05).

Purpose: After 08-02 a user can train on f64 dense only. This slice makes the
Python package accept the real spectrum of array inputs the official package
supports — the next vertical increment a user can actually do.
Output: dtype-dispatched dense marshalling + scipy sparse ingest behind the same
`Dataset` constructor, with an A/B parity test over f32/f64/csr/csc.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-python-bindings/08-RESEARCH.md
@.planning/phases/08-python-bindings/08-PATTERNS.md
@crates/lgbm-dataset/src/ingest.rs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan:
- `crates/lgbm-python/src/marshal.rs`: `numpy_dense_f32_to_rows` (or a runtime dtype-dispatch path), `scipy_csr_to_finished`/`scipy_csc_to_finished` (CSR/CSC raw-slice → FinishedDataset via existing ingest + the shared FinishedDataset→Vec<FeatureColumn> extractor per RESEARCH Open-Q2).
- `crates/lgbm-python/src/dataset.rs`: `Dataset` overloads / classmethods `from_csr`/`from_csc` (mirroring official) accepting the three scipy arrays.
- Possibly a shared `finished_dataset_to_feature_columns` extractor in `crates/lgbm/src/booster.rs` IF sparse uses Route B (only if not already added in 08-01; if added, note it as an 08-01 artifact instead).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: f32 + f64 dense dtype dispatch with explicit contiguity (PYB-02 SC#2)</name>
  <files>crates/lgbm-python/src/marshal.rs, crates/lgbm-python/src/dataset.rs</files>
  <read_first>
    - crates/lgbm-python/src/marshal.rs (08-02 f64 path — the template to generalize)
    - crates/lgbm-python/src/dataset.rs (Dataset constructor from 08-02)
    - crates/lgbm-dataset/src/ingest.rs (from_mat :217 — wants &[f32]; the single f32→f64 widen site lives here)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (Pattern 3 numpy dense f32/f64; Pitfall 3 non-contiguous/wrong-dtype; official widens custom grad/hess to f32 + asserts c_contiguous)
    - .planning/phases/08-python-bindings/08-PATTERNS.md (marshal.rs section: PyReadonlyArray2<f32|f64>, is_standard_layout, dtype dispatch, widen at single site)
  </read_first>
  <action>
    Generalize `marshal.rs` to accept both widths. Add a dtype-dispatching `Dataset` constructor path: inspect the incoming numpy array dtype and route to `PyReadonlyArray2<'_, f64>` or `PyReadonlyArray2<'_, f32>` (either via a runtime `extract`/downcast on `&Bound<PyAny>` checking `.dtype()`, or two typed `#[pymethods]` overloads). For each: `.as_array()`, explicit `is_standard_layout` check + `.to_owned()`/(row,col) iteration on non-C-contiguous input (Pitfall 3), then produce owned Rust rows. Route both widths to the SAME downstream `RawCorpus`/binning so the single f32→f64 widen happens at one site (consistent with `ingest::from_mat`'s widen). Ensure f32 and f64 inputs of the same data yield identical bins (the BinMapper consumes f64 after the single widen). Validate shape before indexing → `PyValueError`. No panic.
  </action>
  <verify>
    <automated>cargo build -p lgbm-python 2>&1 | tail -15 && cargo clippy -p lgbm-python 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p lgbm-python` compiles with both f32 and f64 dense paths.
    - `grep -n "PyReadonlyArray2<.*f32\|f32" crates/lgbm-python/src/marshal.rs` shows an f32 path added.
    - `grep -c "is_standard_layout" crates/lgbm-python/src/marshal.rs` ≥ 1 (contiguity handled for both widths).
    - A Rust unit test (or the pytest in Task 3) proves f32 and f64 of the same data train to identical models.
    - No `unwrap`/`panic!`; `cargo clippy -p lgbm-python` clean.
  </acceptance_criteria>
  <done>Both f32 and f64 dense numpy arrays are accepted with explicit contiguity/dtype handling and route through one widen site, matching the official package for either width.</done>
</task>

<task type="auto">
  <name>Task 2: scipy CSR/CSC sparse ingest via existing bit-exact ingest (D-05)</name>
  <files>crates/lgbm-python/src/marshal.rs, crates/lgbm-python/src/dataset.rs, crates/lgbm-python/src/lib.rs</files>
  <read_first>
    - crates/lgbm-dataset/src/ingest.rs (from_csr :301 signature `indptr:&[i64], indices:&[i32], values:&[f32], num_rows, num_cols, cfg, metadata`; from_csc :364; validate_indptr + indices<num_cols checks)
    - crates/lgbm-python/src/dataset.rs (Dataset to extend with from_csr/from_csc)
    - crates/lgbm/src/booster.rs (if a FinishedDataset→Vec<FeatureColumn> extractor was added in 08-01, reuse it; else add it here per RESEARCH Open-Q2 Route B for sparse)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (Pattern 4 scipy CSR/CSC; A5 dtype coercion indptr→i64/indices→i32/values→f32; Open-Q2 — sparse cleaner through Route B since from_csr returns FinishedDataset; Pitfall 4 EFB bundling on extraction)
    - .planning/phases/08-python-bindings/08-PATTERNS.md (marshal.rs delegation-target signatures + shape-validation; Security V5 surface DatasetError as ValueError)
  </read_first>
  <action>
    Add `#[pymethods]`/classmethod `Dataset::from_csr` and `from_csc` accepting the three scipy arrays as numpy readonly arrays (`indptr`, `indices`, `data`) plus `shape` and `params`. In `marshal.rs`, extract them as raw slices, coercing to the ingest-expected widths (indptr→i64, indices→i32, values→f32; RESEARCH A5 — do the coercion at the boundary or document the Python-side `.astype`). Delegate to `lgbm_dataset::ingest::from_csr`/`from_csc` (NO new sparse algorithm) → `FinishedDataset`. Convert the `FinishedDataset` to the `Vec<FeatureColumn>` the train path consumes via the shared extractor (Route B); if EFB bundling is active, unbundle/disable so per-feature bins are correct (Pitfall 4 — prefer enable_bundle off for the extraction, or map group-relative bins back per sub-feature). Let `from_csr`/`from_csc`'s existing `validate_indptr` + `indices[k] < num_cols` checks fire; surface their `DatasetError` as `PyValueError` (Security V5 — never index first). Register the methods. No panic.
  </action>
  <verify>
    <automated>cargo build -p lgbm-python 2>&1 | tail -15 && cargo test -p lgbm sparse 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p lgbm-python` compiles with `from_csr`/`from_csc` on `Dataset`.
    - `grep -n "from_csr\|from_csc" crates/lgbm-python/src/marshal.rs` shows delegation to ingest.
    - A test (Rust or pytest Task 3) proves a sparse matrix and its dense equivalent train to the same model (CSR/CSC↔dense equivalence inherits the Phase-2 guarantee).
    - Malformed indptr (e.g. non-monotone) surfaces as a Python `ValueError`, not a panic (covered in Task 3 pytest or a Rust unit test).
    - No `unwrap`/`panic!`; clippy clean.
  </acceptance_criteria>
  <done>scipy CSR and CSC sparse input is accepted and routed through the existing bit-exact ingest, with indptr/indices bounds validated and surfaced as ValueError — PYB-02 "dense AND sparse" satisfied.</done>
</task>

<task type="auto">
  <name>Task 3: A/B parity pytest over f32/f64/csr/csc (PYB-02)</name>
  <files>crates/lgbm-python/python/tests/test_numpy_sparse_parity.py, crates/lgbm-python/python/lightgbm_rs/__init__.py</files>
  <read_first>
    - crates/lgbm-python/python/tests/test_booster_parity.py (08-02 A/B harness shape to extend)
    - crates/oracle-harness/tests/fixtures/REFERENCE_MANIFEST.md (deterministic settings to pin both sides)
    - .planning/phases/08-python-bindings/08-VALIDATION.md (Per-Task Verification Map PYB-02 row: `pytest .../test_numpy_sparse_parity.py -k 'f32 or f64 or csr or csc'`; Threat T-08 V5)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (A/B harness shape; Environment Availability — scipy required for sparse tests)
  </read_first>
  <action>
    Create `crates/lgbm-python/python/tests/test_numpy_sparse_parity.py` parametrized over `{f32 dense, f64 dense, csr, csc}`. For each case, feed the SAME logical data + params (pinned to REFERENCE_MANIFEST deterministic settings) to BOTH real `lightgbm` and `lightgbm_rs`, and `np.testing.assert_allclose(rs.predict(Xt), ref.predict(Xt), atol=1e-6)`. Include a `test_f32_f64_self_consistency` asserting `lightgbm_rs` produces identical predictions for the f32 and f64 versions of the same data (single-widen guarantee). Include a `test_malformed_csr_raises` asserting a non-monotone indptr raises `ValueError` (Security V5). `pytest.importorskip("scipy")` and `importorskip("lightgbm")` with printed reasons — SKIP cleanly, never silently pass. Document `maturin develop` rebuild before running. Do NOT git-add `LightGBM/`.
  </action>
  <verify>
    <automated>cd crates/lgbm-python && (command -v maturin >/dev/null && maturin develop 2>&1 | tail -3 || true); python -m pytest python/tests/test_numpy_sparse_parity.py -k 'f32 or f64 or csr or csc or malformed or self_consistency' 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest test_numpy_sparse_parity.py` passes (or SKIPs with a printed reason if real lightgbm/scipy absent) for all four input kinds at `atol=1e-6`.
    - `test_f32_f64_self_consistency` asserts identical lightgbm_rs predictions for f32 vs f64 inputs.
    - `test_malformed_csr_raises` asserts a `ValueError` (no panic / no abort).
    - `cargo test --workspace` GREEN; `LightGBM/` not git-added.
  </acceptance_criteria>
  <done>An A/B parity suite confirms f32/f64 dense and CSR/CSC sparse all match real lightgbm within ~1e-6, f32/f64 are self-consistent, and malformed sparse input raises cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python numpy/scipy → PyO3 | untrusted dense arrays (dtype/contiguity) + CSR/CSC indptr/indices/data cross the boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Tampering/DoS | CSR/CSC indptr/indices | mitigate | Existing `validate_indptr` + per-index `< num_cols` in `ingest.rs` fire; `DatasetError` → `PyValueError`, never index first (Security V5) |
| T-08-03-02 | Tampering (silent corruption) | f32/f64 non-contiguous dense | mitigate | Explicit `is_standard_layout` check + copy for both widths (Pattern 3 / SC#2) |
| T-08-03-03 | Tampering | f32/f64 width divergence | mitigate | Single f32→f64 widen site; self-consistency test asserts identical models for either width |
| T-08-03-SC | Tampering | cargo/pip installs | mitigate | No NEW crates this plan (numpy/pyo3-polars already pinned + audited in 08-02); scipy is an official-parity PyPI dep cited from LightGBM pyproject.toml |
</threat_model>

<verification>
- `cargo build -p lgbm-python` compiles with f32+f64 dense and CSR/CSC sparse.
- pytest A/B parity over all four input kinds within ~1e-6 (or SKIP-marked); f32/f64 self-consistent; malformed CSR raises ValueError.
- `cargo test --workspace` GREEN; clippy clean; no panic across FFI; `LightGBM/` not git-added.
</verification>

<success_criteria>
- f32 and f64 dense numpy inputs both work and match the official package for either width (SC#2).
- scipy CSR and CSC sparse inputs work via the existing bit-exact ingest (D-05) — PYB-02 satisfied.
- All input paths validate shape/dtype/contiguity/indptr at the boundary and surface ValueError, never panic.
</success_criteria>

<output>
Create `.planning/phases/08-python-bindings/08-03-SUMMARY.md` when done.
</output>
