---
phase: 08-python-bindings
plan: 08
type: execute
wave: 8
depends_on: ["08-07"]
files_modified:
  - crates/lgbm-python/src/booster.rs
  - crates/lgbm-python/python/lightgbm_rs/__init__.py
  - crates/lgbm-python/python/lightgbm_rs/sklearn.py
  - crates/lgbm-python/python/tests/test_persistence.py
autonomous: true
requirements: [PYB-01]
must_haves:
  truths:
    - "A Python user can save_model(path) / model_to_string() / load via model_str= or model_file= on the Phase-3 C++-compatible text format; a C++-trained model loads and predicts identically and vice-versa (D-10)"
    - "Python pickle works (__getstate__/__setstate__ over the model string) for sklearn-pipeline use (D-10)"
    - "A model trained by real lightgbm 4.6 (saved to text) loads in lightgbm_rs and predicts within ~1e-6 (cross-format parity)"
    - "model_from_string on untrusted text parses via the validated lgbm-model loader and raises a typed exception on malformed input (Security V5)"
  artifacts:
    - path: "crates/lgbm-python/src/booster.rs"
      provides: "#[pymethods] save_model/model_to_string + model_from_string/model_file constructor + __getstate__/__setstate__"
      contains: "model_to_string"
    - path: "crates/lgbm-python/python/tests/test_persistence.py"
      provides: "C++-text round-trip + cross-format load + pickle test"
      contains: "pickle"
  key_links:
    - from: "crates/lgbm-python/src/booster.rs"
      to: "crates/lgbm/src/booster.rs"
      via: "Booster::model_to_string / model_from_string / save_model (08-01 facade methods)"
      pattern: "model_to_string|model_from_string|save_model"
---

<objective>
Complete the phase with persistence: expose the Phase-3 C++-compatible model text
I/O to Python (`save_model`/`model_to_string`/load via `model_str=`/`model_file=`)
plus Python `pickle` support (`__getstate__`/`__setstate__` over the model string),
and prove cross-format parity — a real-lightgbm-trained text model loads in
`lightgbm_rs` and predicts identically (D-10).

Purpose: Persistence + pickle is the final user-facing surface (sklearn pipelines
require pickle; interop with real lightgbm requires the shared text format). It is
thin wiring over the 08-01 facade text-I/O methods.
Output: `Booster` persistence `#[pymethods]` + pickle dunders + a cross-format
persistence parity test. This closes Phase 8.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-python-bindings/08-RESEARCH.md
@.planning/phases/08-python-bindings/08-PATTERNS.md
@crates/lgbm/src/booster.rs
</context>

<artifacts_this_phase_produces>
NEW symbols introduced by this plan:
- `crates/lgbm-python/src/booster.rs`: `#[pymethods]` `save_model(path)`, `model_to_string()`, a `model_from_string`/`model_file` constructor (classmethod or `#[new]` with `model_str=`/`model_file=`), `__getstate__`/`__setstate__` (pickle over the model string).
- `crates/lgbm-python/python/lightgbm_rs/sklearn.py`: `LGBMModel.__getstate__`/`__setstate__` (delegating to the booster string) for sklearn-pipeline pickling.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Persistence #[pymethods] + pickle dunders (D-10)</name>
  <files>crates/lgbm-python/src/booster.rs</files>
  <read_first>
    - crates/lgbm/src/booster.rs (08-01 facade methods: model_to_string, model_from_string, save_model — the delegation targets)
    - crates/lgbm-python/src/booster.rs (#[pyclass] Booster from 08-02 — add persistence methods + dunders)
    - crates/lgbm-python/src/error.rs (ModelError → LightGBMError mapping from 08-02)
    - crates/lgbm-model/src/model_text.rs (load :53 typed ModelError; save :216 — the C++-compatible text format, Phase 3)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (D-10 persistence text + pickle; Security: untrusted model text parsed via validated lgbm-model loader → typed error; pickle-only-as-trusted-as-source caveat)
    - LightGBM/python-package/lightgbm/basic.py (READ-ONLY: Booster save_model/model_to_string/__init__ model_str=/model_file=; __getstate__/__setstate__ over the model handle)
  </read_first>
  <action>
    Add `#[pymethods]` to `#[pyclass] Booster` in `crates/lgbm-python/src/booster.rs`: `model_to_string(&self) -> PyResult<String>` → `self.inner.model_to_string()`; `save_model(&self, filename: &str, ...) -> PyResult<()>` → `self.inner.save_model(Path::new(filename)).map_err(...)`; a constructor path accepting `model_str=`/`model_file=` (a classmethod `from_model_string`/`from_model_file`, or extend `#[new]`) → read file if needed, `lgbm::Booster::model_from_string(text).map_err(...)`, wrap. Implement `__getstate__(&self) -> PyResult<String>` returning `model_to_string()` and `__setstate__(&mut self, state: String) -> PyResult<()>` rebuilding `inner` from `model_from_string` — enabling `pickle` over the model string (D-10). Map `ModelError` (malformed text) → the `LightGBMError`/`PyValueError` via the existing `From<LgbmError>` (Security V5 — parse via the validated loader, never index raw text). No panic. Re-export nothing new (methods are on the existing class).
  </action>
  <verify>
    <automated>cargo build -p lgbm-python 2>&1 | tail -15 && cargo clippy -p lgbm-python 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p lgbm-python` compiles with persistence methods + pickle dunders.
    - `grep -n "fn model_to_string\|fn save_model\|fn from_model_string\|fn from_model_file\|__getstate__\|__setstate__" crates/lgbm-python/src/booster.rs` shows all.
    - The text path delegates to the 08-01 facade methods (grep for `model_to_string`/`model_from_string`/`save_model` on `self.inner`).
    - Malformed model text → typed exception (ModelError mapped), no panic.
    - No `unwrap`/`panic!`; clippy clean.
  </acceptance_criteria>
  <done>Booster exposes save_model/model_to_string/model_str=/model_file= over the C++-compatible text format and supports pickle via __getstate__/__setstate__.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-format persistence parity + pickle pytest (D-10)</name>
  <files>crates/lgbm-python/python/tests/test_persistence.py, crates/lgbm-python/python/lightgbm_rs/sklearn.py, crates/lgbm-python/python/lightgbm_rs/__init__.py</files>
  <read_first>
    - crates/lgbm-python/python/tests/test_booster_parity.py (A/B harness shape)
    - crates/lgbm-python/python/lightgbm_rs/sklearn.py (add LGBMModel __getstate__/__setstate__ delegating to the booster string)
    - crates/oracle-harness/tests/fixtures/REFERENCE_MANIFEST.md (deterministic settings)
    - .planning/phases/08-python-bindings/08-VALIDATION.md (Per-Task Verification Map D-10 row: `pytest .../test_persistence.py`; Threat T-08 V5)
    - .planning/phases/08-python-bindings/08-RESEARCH.md (D-10; cross-format load — C++-trained model loads + predicts identically)
  </read_first>
  <action>
    Add `__getstate__`/`__setstate__` to `LGBMModel` in `sklearn.py` (delegate to the booster's model string) so fitted estimators pickle in sklearn pipelines. Create `crates/lgbm-python/python/tests/test_persistence.py`: `test_text_roundtrip` (train in lightgbm_rs, `model_to_string` → `from_model_string` → predictions byte-identical); `test_save_load_file` (`save_model` to a tmp path → load via `model_file=` → identical predictions); `test_cross_format_load` (train a model in REAL `lightgbm` 4.6, `save_model` to text, load that text in `lightgbm_rs`, assert predictions match within `atol=1e-6` — the C++-compatibility proof); `test_pickle_roundtrip` (`pickle.dumps`/`loads` a trained `Booster` AND a fitted `LGBMClassifier`, assert identical predictions after unpickle); `test_malformed_model_text_raises` (garbage text → `ValueError`/`LightGBMError`, not a panic). `importorskip("lightgbm")` + `importorskip("lightgbm_rs")` with printed reasons. Document `maturin develop`. Do NOT git-add `LightGBM/`.
  </action>
  <verify>
    <automated>cd crates/lgbm-python && (command -v maturin >/dev/null && maturin develop 2>&1 | tail -3 || true); python -m pytest python/tests/test_persistence.py 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `pytest test_persistence.py` passes (or SKIPs with printed reason): text round-trip identical, file save/load identical, cross-format load within `atol=1e-6`, pickle round-trip identical (Booster + estimator), malformed text raises.
    - `test_cross_format_load` proves a real-lightgbm-trained text model predicts identically in lightgbm_rs (the D-10 C++-compatibility contract).
    - `test_malformed_model_text_raises` confirms a typed exception, not a panic (Security V5).
    - `cargo test --workspace` GREEN; `LightGBM/` not git-added.
  </acceptance_criteria>
  <done>Model persistence round-trips on the C++-compatible text format, cross-loads real-lightgbm models within ~1e-6, and pickles for sklearn pipelines — D-10 satisfied and Phase 8 complete.</done>
</task>

</tasks>

<threat_model>
ASVS L1; block_on: high

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted model text / pickle → PyO3 | `model_str=`/`model_file=`/unpickle feed untrusted serialized model text into the loader |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08-01 | Tampering | untrusted model text parse | mitigate | Parse via the validated `lgbm-model` `model_text::load` (typed `ModelError` → exception); never index raw text (Security V5) |
| T-08-08-02 | Tampering | malformed model_file path | mitigate | File read errors → typed `PyErr`; no panic; validate before parse |
| T-08-08-03 | Tampering | pickle of model string | accept | Pickle of a model string is only as trusted as its source (standard pickle caveat); documented in the method docstring — no code mitigation possible for the pickle threat model |
| T-08-08-SC | Tampering | cargo/pip installs | mitigate | No NEW crates this plan (uses already-pinned/audited deps) |
</threat_model>

<verification>
- `cargo build -p lgbm-python` compiles with persistence + pickle.
- pytest: text round-trip, file save/load, cross-format load (~1e-6), pickle round-trip, malformed-text-raises (or SKIP-marked).
- `cargo test --workspace` GREEN; clippy clean; no panic across FFI; `LightGBM/` not git-added.
</verification>

<success_criteria>
- save_model/model_to_string/model_str=/model_file= round-trip on the C++-compatible text format; a real-lightgbm model cross-loads within ~1e-6 (D-10).
- Python pickle works for Booster and fitted estimators; malformed model text raises a typed exception.
</success_criteria>

<output>
Create `.planning/phases/08-python-bindings/08-08-SUMMARY.md` when done.
</output>
