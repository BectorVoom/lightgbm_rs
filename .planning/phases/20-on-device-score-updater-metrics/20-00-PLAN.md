---
phase: 20-on-device-score-updater-metrics
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - xtask/py/metric_oracle_capture.py
  - crates/oracle-harness/tests/fixtures/metric/rmse_labels.txt
  - crates/oracle-harness/tests/fixtures/metric/rmse_scores.txt
  - crates/oracle-harness/tests/fixtures/metric/rmse_value.txt
  - crates/oracle-harness/tests/fixtures/metric/l2_labels.txt
  - crates/oracle-harness/tests/fixtures/metric/l2_scores.txt
  - crates/oracle-harness/tests/fixtures/metric/l2_value.txt
  - crates/oracle-harness/tests/fixtures/metric/l1_labels.txt
  - crates/oracle-harness/tests/fixtures/metric/l1_scores.txt
  - crates/oracle-harness/tests/fixtures/metric/l1_value.txt
  - crates/oracle-harness/tests/fixtures/metric/binary_logloss_labels.txt
  - crates/oracle-harness/tests/fixtures/metric/binary_logloss_scores.txt
  - crates/oracle-harness/tests/fixtures/metric/binary_logloss_value.txt
  - crates/lgbm-compute/src/device_metric.rs
  - crates/lgbm-compute/src/lib.rs
  - crates/lgbm-compute/src/kernels/mod.rs
  - crates/lgbm-compute/src/kernels/score_updater.rs
  - crates/lgbm-compute/src/kernels/metric_pointwise.rs
autonomous: true
requirements: [ODL-17]
must_haves:
  truths:
    - "The 4 missing device-supported metric goldens (rmse, l2, l1, binary_logloss) exist on disk as real lib_lightgbm 4.6 triplets, OR are documented as cpu-f64-fold-anchored when the venv is unavailable"
    - "metric_supported(name) returns true for exactly the 12 pointwise losses and false for AUC/AUC-mu/NDCG/MAP/multi_error/multi_logloss/xentropy/xentlambda/KullbackLeibler"
    - "The three new module files are registered and the workspace builds green with LGBM_CUDA_ON_DEVICE unset (byte-unchanged host path)"
  artifacts:
    - path: "crates/lgbm-compute/src/device_metric.rs"
      provides: "metric_supported discriminator + unit test"
      contains: "pub fn metric_supported"
    - path: "crates/lgbm-compute/src/kernels/score_updater.rs"
      provides: "registered empty module stub for Plan 01"
    - path: "crates/lgbm-compute/src/kernels/metric_pointwise.rs"
      provides: "registered empty module stub for Plan 02"
    - path: "crates/oracle-harness/tests/fixtures/metric/binary_logloss_value.txt"
      provides: "the binary_logloss golden (Pitfall 1 gap #4)"
  key_links:
    - from: "crates/lgbm-compute/src/lib.rs"
      to: "crates/lgbm-compute/src/device_metric.rs"
      via: "pub mod device_metric declaration"
      pattern: "pub mod device_metric"
    - from: "crates/lgbm-compute/src/kernels/mod.rs"
      to: "crates/lgbm-compute/src/kernels/score_updater.rs"
      via: "pub mod score_updater declaration"
      pattern: "pub mod score_updater"
---

<objective>
Front-load the Wave-0 scaffolding the two kernel plans (01 score updater, 02 metric evaluator)
depend on: (1) capture the 4 device-supported metric goldens that do NOT exist on disk
(rmse / l2 / l1 / binary_logloss — Pitfall 1; the other 8 already exist), (2) register the
three new module files as empty stubs so Plans 01/02 fill them without touching `mod.rs`
(avoiding a same-wave `kernels/mod.rs` conflict), and (3) implement the metric-supported
discriminator in full (it is a pure classifier with no kernel dependency, so it lands now).

Purpose: without the 4 goldens, those metric parity cells SKIP forever and silently
under-test ODL-17 (Pitfall 1). Registering the stubs unblocks parallel Wave-2 kernel work.
The discriminator (D-05) gates the host fallback for the unsupported metric set.
Output: 4 golden triplets on disk (or documented cpu-fold anchor), `device_metric.rs` with
`metric_supported` + tests, and two registered empty kernel stub modules.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-on-device-score-updater-metrics/20-CONTEXT.md
@.planning/phases/20-on-device-score-updater-metrics/20-RESEARCH.md
@.planning/phases/20-on-device-score-updater-metrics/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the 4 missing device-supported metric goldens (rmse/l2/l1/binary_logloss)</name>
  <files>xtask/py/metric_oracle_capture.py, crates/oracle-harness/tests/fixtures/metric/rmse_value.txt, crates/oracle-harness/tests/fixtures/metric/l2_value.txt, crates/oracle-harness/tests/fixtures/metric/l1_value.txt, crates/oracle-harness/tests/fixtures/metric/binary_logloss_value.txt</files>
  <read_first>
    - xtask/py/metric_oracle_capture.py (the `train_and_capture(out_dir, name, seed, objective, metric, params)` helper at lines ~210-238 — the self-extend analog; it currently captures only the extended/xentropy/multiclass families, never the base regression/binary losses — Pitfall 1)
    - crates/oracle-harness/tests/fixtures/metric/poisson_{labels,scores,value}.txt (the on-disk triplet SHAPE to reproduce: one value per line labels+scores, single scalar in value.txt)
    - .planning/phases/20-on-device-score-updater-metrics/20-RESEARCH.md (Pitfall 1 + Assumption A1 — the cpu-f64-fold fallback when the pinned lightgbm 4.6 venv at repo root is unavailable)
    - MEMORY: phase8-python-venv (the uv `.venv` at repo root is the maturin + lightgbm==4.6 toolchain)
  </read_first>
  <action>
    Extend `metric_oracle_capture.py` to call `train_and_capture` for the 4 base losses that
    have no golden: `rmse`, `l2`, `l1` with objective `"regression"` and the matching metric
    name, and `binary_logloss` with objective `"binary"`. Reuse the existing seed + regression
    params convention already in the script (do NOT introduce a new capture idiom). Then run
    `cargo run -p xtask -- metric-oracle-capture` (or `python xtask/py/metric_oracle_capture.py`)
    against the repo-root uv `.venv` (pinned lightgbm 4.6) to write the triplets into
    `crates/oracle-harness/tests/fixtures/metric/`. FALLBACK (A1): if the venv/toolchain is
    unavailable at execution time, skip the 4 real captures and instead record in the SUMMARY
    that rmse/l2/l1/binary_logloss will anchor to the cpu-f64 fold in Plan 02 (weaker
    reference-fidelity proof, documented) — do NOT fabricate golden values by hand. Never
    git-add anything under `LightGBM/`; goldens live only under the tracked oracle-harness dir.
  </action>
  <verify>
    <automated>ls crates/oracle-harness/tests/fixtures/metric/rmse_value.txt crates/oracle-harness/tests/fixtures/metric/l2_value.txt crates/oracle-harness/tests/fixtures/metric/l1_value.txt crates/oracle-harness/tests/fixtures/metric/binary_logloss_value.txt</automated>
  </verify>
  <acceptance_criteria>
    - Either all 4 triplets (`{name}_labels.txt`, `{name}_scores.txt`, `{name}_value.txt` for rmse/l2/l1/binary_logloss) exist on disk with the same one-value-per-line shape as `poisson_*.txt`, OR the SUMMARY documents the cpu-f64-fold fallback per A1 with the reason the venv was unavailable.
    - `git status` shows no files added under `LightGBM/`.
    - The `binary_logloss` capture used objective `"binary"` (not `"regression"`) so its golden reflects the sigmoid-probability input path.
  </acceptance_criteria>
  <done>The 4 previously-missing metric goldens exist (or the cpu-fold fallback is documented), matching the on-disk triplet shape, with nothing added under LightGBM/.</done>
</task>

<task type="auto">
  <name>Task 2: Register the 3 new module files + implement the metric_supported discriminator (D-05)</name>
  <files>crates/lgbm-compute/src/device_metric.rs, crates/lgbm-compute/src/lib.rs, crates/lgbm-compute/src/kernels/mod.rs, crates/lgbm-compute/src/kernels/score_updater.rs, crates/lgbm-compute/src/kernels/metric_pointwise.rs</files>
  <read_first>
    - crates/lgbm-compute/src/device_objective.rs (lines 104-160 — `device_objective_supported(name)` + `DeviceObjectiveKind::from_name` alias-table + the `unsupported_*_are_rejected` / `supported_*_are_accepted` test structure; the EXACT pattern to mirror, D-05)
    - crates/lgbm-compute/src/lib.rs (lines 14-19 — the top-level `pub mod` block where `device_objective` is declared; add `pub mod device_metric;` here)
    - crates/lgbm-compute/src/kernels/mod.rs (lines 59-72 — the Phase-18/19 `pub mod` block; add `pub mod score_updater;` and `pub mod metric_pointwise;`)
    - .planning/phases/20-on-device-score-updater-metrics/20-PATTERNS.md (device_metric.rs section — the CRITICAL asymmetry: MAPE/Gamma/Gamma-deviance/Tweedie are metric-supported even though their OBJECTIVES return None from device_objective_supported)
  </read_first>
  <action>
    Create `crates/lgbm-compute/src/kernels/score_updater.rs` and
    `crates/lgbm-compute/src/kernels/metric_pointwise.rs` as EMPTY module stubs (module-level
    doc comment only, no functions) so Plans 01 and 02 fill them without editing `mod.rs`
    (prevents a same-wave conflict). Add `pub mod score_updater;` and `pub mod metric_pointwise;`
    to `kernels/mod.rs`. Create `crates/lgbm-compute/src/device_metric.rs` implementing
    `pub fn metric_supported(name: &str) -> bool` returning `true` for exactly the 12 pointwise
    losses — rmse, l2 (and the `mean_squared_error`/`regression` aliases as appropriate),
    l1 (`mean_absolute_error`), quantile, huber, fair, poisson, mape, gamma, gamma_deviance,
    tweedie, binary_logloss (`binary`) — and `false` for auc, auc_mu, ndcg, map,
    average_precision, multi_error, multi_logloss, cross_entropy/xentropy,
    cross_entropy_lambda/xentlambda, kullback_leibler, and any unknown name. Key the match on
    the METRIC name list, NOT the objective-support list (D-05 — do NOT call
    `device_objective_supported`). Add `pub mod device_metric;` to `lib.rs`. Include a
    `#[cfg(test)]` module mirroring `device_objective.rs`'s two tests: one asserting every
    unsupported metric routes to host, one asserting all 12 pointwise losses are supported,
    with an explicit sub-assertion that MAPE/Gamma/Gamma-deviance/Tweedie are supported here
    even though `device_objective_supported` rejects them.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute metric_supported</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-compute metric_supported` passes, including the asymmetry assertion `assert!(metric_supported("gamma") && !device_objective_supported("gamma"))`.
    - `cargo build -p lgbm-compute` is green with the two empty kernel stubs registered.
    - `grep -n 'pub mod score_updater' crates/lgbm-compute/src/kernels/mod.rs` and `grep -n 'pub mod metric_pointwise' crates/lgbm-compute/src/kernels/mod.rs` and `grep -n 'pub mod device_metric' crates/lgbm-compute/src/lib.rs` each return a match.
    - `metric_supported` does not delegate to `device_objective_supported` (independent list, D-05).
  </acceptance_criteria>
  <done>The discriminator classifies exactly the 12 supported / 9-plus unsupported metrics with the objective/metric asymmetry proven; the two kernel stub modules and device_metric are registered and the crate builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture script → committed fixtures | Python-generated golden text written into the tracked oracle-harness dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-00-01 | Tampering | metric golden capture writing outside the fixtures dir | mitigate | Capture writes only under `crates/oracle-harness/tests/fixtures/metric/`; verify `git status` adds nothing under `LightGBM/` (never git-add the reference tree) |
| T-20-00-02 | Tampering | discriminator misclassification silently routing an unsupported metric on-device | mitigate | Unit tests assert the full unsupported set returns `false`; discriminator is a pure classifier with no device dispatch of its own |
| T-20-00-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; capture runs against the pre-provisioned pinned lightgbm 4.6 venv (no registry fetch) |
</threat_model>

<verification>
- `cargo test -p lgbm-compute metric_supported` green.
- `cargo build --workspace` green with LGBM_CUDA_ON_DEVICE unset (byte-unchanged host path).
- 4 golden triplets present on disk (or cpu-fold fallback documented in SUMMARY).
</verification>

<success_criteria>
The 4 missing goldens exist (or fallback documented), the metric_supported discriminator is
correct with the D-05 asymmetry proven, and the two kernel stub modules are registered so
Plans 01 and 02 run in parallel.
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces
- New file `crates/lgbm-compute/src/device_metric.rs` with `pub fn metric_supported(name: &str) -> bool` + `#[cfg(test)]` unit tests.
- New empty stub modules `crates/lgbm-compute/src/kernels/score_updater.rs` and `crates/lgbm-compute/src/kernels/metric_pointwise.rs` (filled by Plans 01/02).
- Module registrations: `pub mod device_metric;` in `lib.rs`; `pub mod score_updater;` + `pub mod metric_pointwise;` in `kernels/mod.rs`.
- 4 metric golden triplets on disk: `{rmse,l2,l1,binary_logloss}_{labels,scores,value}.txt` under `crates/oracle-harness/tests/fixtures/metric/` (or documented cpu-fold fallback).
- Extended `xtask/py/metric_oracle_capture.py` capturing the 4 base losses.
- No CLI/env behavior change.
</artifacts_produced>

<output>
Create `.planning/phases/20-on-device-score-updater-metrics/20-00-SUMMARY.md` when done.
</output>
