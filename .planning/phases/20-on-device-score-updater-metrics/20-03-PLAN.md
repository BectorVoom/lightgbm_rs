---
phase: 20-on-device-score-updater-metrics
plan: 03
type: execute
wave: 3
depends_on: [20-00, 20-01, 20-02]
files_modified:
  - crates/lgbm-compute/src/lib.rs
  - crates/oracle-harness/tests/learner_parity.rs
autonomous: true
requirements: [ODL-18, ODL-19]
must_haves:
  truths:
    - "grow_tree_on_device sequences the Phase-16/17/18 kernels into a real per-leaf grow loop: root init -> build smaller + subtract larger -> best-split -> break if best_leaf == -1 -> CUDATree::Split (before partition) -> DataPartition::Split, up to num_leaves-1, returning (Tree, LeafPartitionLayout)"
    - "on_device_growth_supported() returns cuda_on_device_enabled() on GpuBackend<R> so with LGBM_CUDA_ON_DEVICE unset it stays false and ROCm/CPU/host-CUDA paths are byte-unchanged (Pitfall 2, D-09)"
    - "The grown on-device tree is STRUCTURE bit-exact to the cpu f64 anchor (tie-aware default_left), leaf values within ROCM_LEAF_VALUE_TOL, via the existing assert_on_device_tree_matches_cpu_anchor oracle — never GPU-vs-GPU"
    - "Every new kernel in the driver keeps f32 + u64 fixed-point build with no f64 per-row grow/build hot loop (ODL-19)"
  artifacts:
    - path: "crates/lgbm-compute/src/lib.rs"
      provides: "grow_tree_on_device driver body + gated on_device_growth_supported() flip"
      contains: "fn grow_tree_on_device"
    - path: "crates/oracle-harness/tests/learner_parity.rs"
      provides: "activated STRUCTURE gate cell with a real on-device tree"
  key_links:
    - from: "crates/lgbm-compute/src/lib.rs"
      to: "crates/lgbm-compute/src/kernels/histogram.rs"
      via: "driver step build/subtract composes the Phase-16 build+fix+subtract kernels"
      pattern: "histogram|subtract"
    - from: "crates/lgbm-compute/src/lib.rs"
      to: "crates/lgbm-treelearner/src/learner.rs"
      via: "the dormant on_device_eligible fork consumes grow_tree_on_device once the discriminator flips"
      pattern: "grow_tree_on_device"
    - from: "crates/oracle-harness/tests/learner_parity.rs"
      to: "crates/lgbm-compute/src/lib.rs"
      via: "assert_on_device_tree_matches_cpu_anchor over a real grow_tree_on_device tree"
      pattern: "assert_on_device_tree_matches_cpu_anchor"
---

<objective>
Implement the pulled-forward on-device single-GPU tree-learner driver (D-01, ODL-18/ODL-19):
fill the currently-`Ok(None)` `Backend::grow_tree_on_device` body by sequencing the existing
Phase-16 (histogram build/fix/subtract), Phase-17 (best-split), and Phase-18 (tree mutation +
data partition) kernels into the per-leaf grow loop (§6/§16), flip `on_device_growth_supported()`
true GATED behind `cuda_on_device_enabled()` (Pitfall 2), and activate the STRUCTURE-bit-exact
gate against the cpu f64 anchor. Resolve the data->leaf map Handle alias-vs-double-buffer
question (Pitfall 3) at implementation time with an A/B against the anchor.

Purpose: this is the load-bearing integration D-01 absorbed from Phase 21 — it activates the
dormant learner fork so a full tree grows on device. Scope the proving slice to continuous
features + L2 (no RenewTreeOutput refit — Pitfall 4).
Output: the driver body + discriminator flip in `lib.rs`, and the activated STRUCTURE gate cell.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-on-device-score-updater-metrics/20-CONTEXT.md
@.planning/phases/20-on-device-score-updater-metrics/20-RESEARCH.md
@.planning/phases/20-on-device-score-updater-metrics/20-PATTERNS.md
@crates/lgbm-compute/src/kernels/histogram.rs
@crates/lgbm-compute/src/kernels/best_split.rs
@crates/lgbm-compute/src/kernels/data_partition.rs
@crates/lgbm-compute/src/kernels/tree.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill grow_tree_on_device driver body + gated on_device_growth_supported() flip</name>
  <files>crates/lgbm-compute/src/lib.rs</files>
  <read_first>
    - crates/lgbm-compute/src/lib.rs (lines 1233-1317 — the seam: the `on_device_growth_supported()` default-false discriminator to flip, the `Ok(None)` `grow_tree_on_device` body to fill, the cubecl-0.10 checklist at 1264-1279, and `cuda_on_device_enabled()` at 1313; the shared-`GpuBackend<R>` warning at 1238-1240 — Pitfall 2)
    - crates/lgbm-compute/src/kernels/histogram.rs (Phase-16 build + FixHistogram + SubtractHistogram via hist_t** rotation — driver step build-smaller/subtract-larger; the 8aed100-class ordering guard: parent fully built before any child subtract reads it)
    - crates/lgbm-compute/src/kernels/best_split.rs (Phase-17 stage-1 per-feature eval + stage-2/3 cross-feature/cross-leaf argmax → the 8-int chosen-split readback; tie-aware default_left)
    - crates/lgbm-compute/src/kernels/tree.rs (Phase-18 CUDATree::Split writing the device tree arrays — runs BEFORE partition, returns right_leaf_index)
    - crates/lgbm-compute/src/kernels/data_partition.rs (Phase-18 mark->prefix-sum->scatter row routing + the data-index->leaf map; the alias question, Pitfall 3)
    - crates/lgbm-compute/src/kernels/{split_info,random}.rs (Phase-14 device split/leaf-split structs + LCG for root init §6.1)
    - crates/lgbm-dataset/src/dataset.rs (lines 84-100 — `LeafPartitionLayout` the driver returns; do NOT name treelearner's DataPartition here, crate cycle)
    - MEMORY: phase18-wr01-histarena-swap-aliasing (the latent HistArena::swap slot-aliasing bug that "will bite the Phase-21 multi-leaf on-device grow loop" — this IS that loop; verify the data->leaf map buffer strategy)
    - MEMORY: def-f8u-01 (never compare two nondeterministic GPU f32 paths) + spike-052 (no f64 per-row hot loops; keep u64 fixed-point build)
  </read_first>
  <action>
    Implement the `grow_tree_on_device` body in `lib.rs`: root init (§6.1, reuse Phase-14
    split_info/random), then a per-leaf best-first loop up to `num_leaves - 1` that (a) builds
    the smaller child histogram and derives the larger by subtraction (Phase-16, enforcing the
    parent-built-before-child-subtract ordering), (b) runs the best-split finder (Phase-17) and
    reads back the chosen split, (c) breaks when `best_leaf == -1`, (d) calls `CUDATree::Split`
    (Phase-18) BEFORE partition and consumes its `right_leaf_index`, (e) runs
    `DataPartition::Split` (Phase-18) updating the data-index->leaf map. Return
    `(lgbm_model::Tree, lgbm_dataset::LeafPartitionLayout)`. Resolve the data->leaf map Handle
    strategy (Pitfall 3) with a tiny A/B (in-place alias vs ping-pong double-buffer) against the
    cpu anchor at `num_leaves > 2`; PREFER double-buffer unless alias is proven safe, and record
    the decision + evidence in the SUMMARY. Flip `on_device_growth_supported()` on
    `GpuBackend<R>` to return `cuda_on_device_enabled()` (NOT a bare `true`) so ROCm/CUDA/WGPU
    with the env unset stays false and byte-unchanged (Pitfall 2, D-09). Keep every new kernel
    f32 + u64 fixed-point with no f64 per-row grow/build hot loop (ODL-19); f64 only in the
    scalar/gain math where the reference uses it. Bounds-guard every launcher; confine `unsafe`
    to the launch site.
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` is green with `LGBM_CUDA_ON_DEVICE` unset, proving CPU/ROCm/host-CUDA paths are byte-unchanged (ODL-19 hard merge gate); on-device growth is unreachable with the env unset.
    - `on_device_growth_supported()` on `GpuBackend<R>` returns `cuda_on_device_enabled()` (verified by inspection); with the env unset it is `false`.
    - The driver body composes the Phase-16/17/18 kernels (no re-implemented histogram/split/partition); `grep -n 'grow_tree_on_device' crates/lgbm-compute/src/lib.rs` shows the body is no longer `Ok(None)`.
    - No f64 appears in a per-row grow/build hot loop of the new driver kernels (manual grep review of the driver's build/partition kernels; u64 fixed-point build retained); the data->leaf map buffer strategy (alias vs double-buffer) is decided with an anchor A/B and documented.
  </acceptance_criteria>
  <done>grow_tree_on_device grows a full tree on device by sequencing the existing kernels with the correct §16 ordering and the resolved buffer strategy; the discriminator flip is gated so the env-unset path is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Activate the STRUCTURE bit-exact gate against the cpu f64 anchor (ODL-18) + no-f64 review (ODL-19)</name>
  <files>crates/oracle-harness/tests/learner_parity.rs</files>
  <read_first>
    - crates/oracle-harness/tests/learner_parity.rs (lines 2185-2272 — `assert_on_device_tree_matches_cpu_anchor` (tie-aware default_left acceptance at 2217-2233, ROCM_LEAF_VALUE_TOL, SPLIT_GAIN_TIE_TOL); `cpu_anchor_tree` at 2245; the Slice-0 host-fallback stand-in at ~2260/2445 that must be REPLACED by a real on-device tree, and must NOT leak into production)
    - crates/lgbm-treelearner/src/learner.rs (lines 485-505 the `on_device_eligible = on_device_growth_supported() && cuda_on_device_env()` cache; 696-724 the fork that now calls the real driver)
    - .planning/phases/20-on-device-score-updater-metrics/20-PATTERNS.md (learner_parity.rs section — activate the existing oracle with a REAL on-device tree; anchor is ALWAYS cpu_anchor_tree, never a second GPU path; Pitfall 3 buffer note)
    - .planning/phases/20-on-device-score-updater-metrics/20-VALIDATION.md (the on-device gated run command + the D-06 three-layer gate; layer 3 = this structure gate)
  </read_first>
  <action>
    In `learner_parity.rs`, activate the STRUCTURE gate cell: with `LGBM_CUDA_ON_DEVICE=1`, grow a
    continuous-feature L2 tree through the real driver (the learner fork now reaches
    `grow_tree_on_device`), build the `cpu_anchor_tree` for the same inputs, and call
    `assert_on_device_tree_matches_cpu_anchor(&on_device_tree, &cpu_anchor_tree(..), "on-device")`.
    The anchor is ALWAYS the cubecl-cpu f64 fold — never a second GPU f32 path (D-07/def-f8u-01);
    replace the Slice-0 `host_grow` stand-in so the cell exercises a genuine on-device tree.
    Assert structure fields bit-exact, leaf values within `ROCM_LEAF_VALUE_TOL`, and default_left
    flips accepted ONLY on a genuine f32-vs-f64 split_gain near-tie (identical threshold + child
    row-counts). Add/record the ODL-19 no-f64 review: a grep-based inspection over the driver's
    new build/partition kernels confirming no f64 per-row grow/build hot loop was introduced
    (f64 permitted only in the reference-blessed scalar/gain math) — capture the reviewed kernel
    list + finding in the SUMMARY. Keep the gate behind the env so a fresh `cargo test --workspace`
    (env unset) is unchanged.
  </action>
  <verify>
    <automated>LGBM_CUDA_ON_DEVICE=1 cargo test -p oracle-harness --test learner_parity on_device</automated>
  </verify>
  <acceptance_criteria>
    - `LGBM_CUDA_ON_DEVICE=1 cargo test -p oracle-harness --test learner_parity on_device` passes; the on-device tree is STRUCTURE bit-exact to `cpu_anchor_tree` with leaf values within `ROCM_LEAF_VALUE_TOL`.
    - The test compares the on-device tree ONLY against the cpu f64 anchor (no GPU-vs-GPU); the `host_grow` stand-in no longer stands in for the on-device tree.
    - Any default_left flip is corroborated by identical threshold + identical child row-counts + a split_gain near-tie (a non-tie flip hard-fails).
    - The SUMMARY records the reviewed driver kernels and confirms no f64 per-row grow/build hot loop was added (ODL-19); `cargo test --workspace` (env unset) stays green.
  </acceptance_criteria>
  <done>The STRUCTURE gate asserts a real on-device tree bit-exact to the cpu f64 anchor with tie-aware default_left, and the ODL-19 no-f64 review is captured; the env-unset merge gate is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host learner → device grow driver | gradients/hessians + growth params cross to the resident device grow loop; the driver reads/writes device index/histogram buffers across kernel launches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-01 | Tampering | out-of-bounds device read/write across the sequenced driver kernels | mitigate | Bounds-guard every launcher (`i < len`); size histogram/index/partition buffers exactly; keep `unsafe` confined to each `launch_unchecked` site (existing Phase-16/17/18 launchers already do this) |
| T-20-03-02 | Tampering | data->leaf map buffer aliasing corrupting partition results at num_leaves>2 | mitigate | Resolve alias-vs-double-buffer with an anchor A/B (Pitfall 3); prefer double-buffer; the STRUCTURE gate at num_leaves>2 catches corruption |
| T-20-03-03 | Tampering | discriminator over-claim mutating the ROCm/host-CUDA path with env unset | mitigate | Gate the flip behind `cuda_on_device_enabled()` (returns false when env unset); `cargo test --workspace` env-unset proves byte-unchanged (D-09) |
| T-20-03-04 | Tampering | integer overflow in per-leaf offset / leaf-index arithmetic | mitigate | Use the existing `usize`/`i32` bounds-checked index patterns from data_partition.rs; validate leaf indices against `num_leaves` |
| T-20-03-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this plan |
</threat_model>

<verification>
- `cargo test --workspace` green with LGBM_CUDA_ON_DEVICE unset (byte-unchanged merge gate).
- `LGBM_CUDA_ON_DEVICE=1 cargo test -p oracle-harness --test learner_parity on_device` green (STRUCTURE bit-exact).
- No f64 per-row grow/build hot loop introduced (reviewed + recorded in SUMMARY); u64 fixed-point build retained.
</verification>

<success_criteria>
grow_tree_on_device grows a full continuous-feature L2 tree on device by composing the existing
Phase-16/17/18 kernels with the correct §16 ordering; the discriminator flip is gated so the
env-unset path is byte-unchanged; and the grown tree is STRUCTURE bit-exact to the cpu f64 anchor
with tie-aware default_left, with the no-f64 constraint verified.
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces
- `crates/lgbm-compute/src/lib.rs`: real `grow_tree_on_device` driver body (root init → per-leaf build/subtract → best-split → break on `best_leaf == -1` → CUDATree::Split → DataPartition::Split, returning `(Tree, LeafPartitionLayout)`); `on_device_growth_supported()` on `GpuBackend<R>` flipped to return `cuda_on_device_enabled()` (gated).
- Resolved data→leaf map buffer strategy (alias vs double-buffer) with anchor A/B evidence recorded in SUMMARY.
- `crates/oracle-harness/tests/learner_parity.rs`: activated STRUCTURE gate cell running a real on-device tree through `assert_on_device_tree_matches_cpu_anchor`; Slice-0 host-fallback stand-in replaced.
- ODL-19 no-f64 review record (reviewed driver kernel list) in SUMMARY.
- Env behavior: on-device growth reachable only when `LGBM_CUDA_ON_DEVICE=1`; env unset = byte-unchanged CPU/ROCm/host-CUDA.
</artifacts_produced>

<output>
Create `.planning/phases/20-on-device-score-updater-metrics/20-03-SUMMARY.md` when done.
</output>
