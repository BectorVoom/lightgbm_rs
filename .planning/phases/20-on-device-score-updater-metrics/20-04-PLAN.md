---
phase: 20-on-device-score-updater-metrics
plan: 04
type: execute
wave: 5
depends_on: [20-01, 20-02, 20-03b]
files_modified:
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/oracle-harness/tests/resident_score_ab.rs
autonomous: true
requirements: [ODL-16, ODL-19]
must_haves:
  truths:
    - "With LGBM_CUDA_ON_DEVICE=1 the resident cuda_score_ stays on device across the whole multi-iteration train, fed by the on-device grow loop, following the §16 order: Shrinkage -> UpdateScore(§11) -> (optional RenewTreeOutput) -> Metric.Eval(§12)"
    - "After a full multi-iteration run the resident cuda_score_ matches the host score_ vector (bit-for-bit on the cpu anchor where the algorithm permits, else within the f32 envelope) — the D-06 layer-2 residency A/B"
    - "With LGBM_CUDA_ON_DEVICE unset the GBDT TrainOneIter score/eval path is byte-unchanged and the merge gate is green (D-09/ODL-19)"
  artifacts:
    - path: "crates/lgbm-boosting/src/gbdt.rs"
      provides: "resident-loop wiring behind LGBM_CUDA_ON_DEVICE (§16 sequencing, L2 proving slice)"
    - path: "crates/oracle-harness/tests/resident_score_ab.rs"
      provides: "the D-06 layer-2 resident-score A/B test (new file)"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs"
      to: "crates/lgbm-boosting/src/score_updater.rs"
      via: "TrainOneIter routes the score update through the resident boosting_on_cuda_ toggle"
      pattern: "boosting_on_cuda_|add_tree_train_path"
    - from: "crates/oracle-harness/tests/resident_score_ab.rs"
      to: "crates/lgbm-boosting/src/gbdt.rs"
      via: "multi-iteration run asserts resident cuda_score_ equals host score_"
      pattern: "LGBM_CUDA_ON_DEVICE|resident"
---

<objective>
Wire the resident cross-iteration score loop (ODL-16 residency half, D-01/D-06 layer 2): behind
`LGBM_CUDA_ON_DEVICE`, `GBDT::TrainOneIter` keeps `cuda_score_` resident across the whole train,
fed by the 20-03b on-device grow loop, following the §16 order (Shrinkage → UpdateScore(§11) →
optional RenewTreeOutput(§5.1) → Metric.Eval(§12)). Prove residency correctness with the D-06
layer-2 A/B: after a full multi-iteration run the resident `cuda_score_` matches the host `score_`.

Note (re-scope): this plan chains off the corrected Wave-3+ tail — it depends on 20-03b (the real
on-device grow driver), not the defective original 20-03. 20-04 touches only `gbdt.rs` (lgbm-boosting,
above every compute crate) + the new `resident_score_ab.rs` (oracle-harness), so it does NOT inherit
the signature/crate-cycle defect that blocked the driver; it consumes the driver + Plan-01 score
updater + Plan-02 metric through the already-acyclic boosting seam.

Purpose: this closes ODL-16 — the score never leaves device across the train — and validates the
composition of Plan 01 (score updater), Plan 02 (metric), and Plan 03 (driver). Scope the proving
slice to L2 continuous features (no RenewTreeOutput refit — Pitfall 4); document the ordering
contract for L1/quantile follow-up.
Output: the resident-loop wiring in `gbdt.rs` + the new `resident_score_ab.rs` test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-on-device-score-updater-metrics/20-CONTEXT.md
@.planning/phases/20-on-device-score-updater-metrics/20-RESEARCH.md
@.planning/phases/20-on-device-score-updater-metrics/20-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the resident score loop into GBDT::TrainOneIter (§16 order, L2 slice)</name>
  <files>crates/lgbm-boosting/src/gbdt.rs</files>
  <read_first>
    - crates/lgbm-boosting/src/gbdt.rs (the existing `TrainOneIter` score-update + `Metric::Eval` call sites; the `boosting_on_cuda_` seam — the wiring target)
    - crates/lgbm-boosting/src/score_updater.rs (the Plan-01 boosting_on_cuda_ toggle: `add_constant`/`multiply_score`/`add_tree_train_path` now route to resident device ops when active)
    - crates/lgbm-compute/src/lib.rs (the `cuda_on_device_enabled()` gate + the 20-03b `grow_tree_on_device` driver body the learner fork now drives; the 20-03a gated `on_device_growth_supported()` flip)
    - .planning/phases/20-on-device-score-updater-metrics/20-PATTERNS.md (gbdt.rs EDIT section — §16 sequencing Shrinkage → UpdateScore → optional RenewTreeOutput → Metric.Eval; Pitfall 4 scope first slice to L2, document the ordering contract)
    - .planning/phases/20-on-device-score-updater-metrics/20-RESEARCH.md (Pitfall 4 RenewTreeOutput ordering; Pitfall 5 DART/RF per-row-predict paths stay host or explicitly deferred)
  </read_first>
  <action>
    Wire `GBDT::TrainOneIter` ADDITIVELY behind `cuda_on_device_enabled()`: when active, keep
    `cuda_score_` resident across iterations — apply `MultiplyScoreConstant` for shrinkage, the
    per-leaf `AddScore` (Plan-01 delegate to `add_prediction_to_score_on_device`) for the grown
    tree, then `Metric::Eval` over the resident score composed with ConvertOutput (Plan-02),
    following the §16 order exactly. Scope the proving slice to L2 continuous features (no
    RenewTreeOutput refit) and document the ordering contract for the L1/quantile follow-up
    (Pitfall 4). Leave the DART/RF per-row-predict paths (`add_tree_predict_path`/
    `add_tree_scaled_all`) on the host path (Pitfall 5) with a comment. With the env unset the
    existing host score/eval path must be byte-unchanged (D-09).
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` is green with `LGBM_CUDA_ON_DEVICE` unset — the GBDT score/eval path is byte-unchanged (D-09/ODL-19).
    - The resident-loop branch follows the §16 order (Shrinkage → UpdateScore → optional RenewTreeOutput → Metric.Eval); the L2 slice applies no RenewTreeOutput refit and the ordering contract for L1/quantile is documented in a code comment + SUMMARY.
    - The DART/RF per-row-predict paths remain on the host path (documented as out-of-slice).
  </acceptance_criteria>
  <done>TrainOneIter keeps the score resident across the train behind the env gate with the correct §16 ordering; the env-unset path is byte-unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Resident-score A/B test — resident cuda_score_ vs host score_ (D-06 layer 2)</name>
  <files>crates/oracle-harness/tests/resident_score_ab.rs</files>
  <read_first>
    - crates/lgbm-boosting/src/score_updater.rs (lines 70-80 — `scores()` / `class_scores(cur_tree_id)` read-only views used to compare the mirrored host vector)
    - crates/oracle-harness/tests/learner_parity.rs (lines 2245 `cpu_anchor_tree` + the ROCM_LEAF_VALUE_TOL / envelope constants — reuse the same anchor discipline for the score comparison)
    - .planning/phases/20-on-device-score-updater-metrics/20-VALIDATION.md (D-06 layer 2 — after a full multi-iter run with the env set, resident cuda_score_ matches host score_ bit-for-bit where the algorithm permits else f32 envelope)
    - MEMORY: def-f8u-01 (never compare two nondeterministic GPU f32 paths — anchor to the cpu f64 fold / host accumulation)
  </read_first>
  <action>
    Author `crates/oracle-harness/tests/resident_score_ab.rs`: run a full multi-iteration train
    (a handful of L2 continuous-feature trees) twice on the cpu backend — once with the resident
    path (`LGBM_CUDA_ON_DEVICE=1`) and once host-only — and assert the resident/mirrored
    `cuda_score_` equals the host `score_` bit-for-bit on the cpu f64 anchor, and within the ROCm
    ~1e-6 f32 envelope on the gpu backend. Anchor ONLY to the host/cpu-fold accumulation — never
    compare two GPU f32 paths (D-07/def-f8u-01). Keep the on-device arm behind the env so a fresh
    `cargo test --workspace` (env unset) is unchanged.
  </action>
  <verify>
    <automated>LGBM_CUDA_ON_DEVICE=1 cargo test -p oracle-harness --test resident_score_ab</automated>
  </verify>
  <acceptance_criteria>
    - `LGBM_CUDA_ON_DEVICE=1 cargo test -p oracle-harness --test resident_score_ab` passes; after the multi-iteration run the resident `cuda_score_` equals the host `score_` bit-for-bit on the cpu f64 backend.
    - The gpu-backend assertion uses the ~1e-6 f32 envelope and compares to the host/cpu-anchor accumulation, never a second GPU path.
    - `cargo test --workspace` with the env unset is green and unchanged (the A/B arm is env-gated).
  </acceptance_criteria>
  <done>The resident-score A/B proves cuda_score_ residency is correct across the whole train, anchored to the host/cpu-fold accumulation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GBDT loop → resident device score | the cumulative f64 score stays on device across iterations; the host mirror is read by non-resident consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-01 | Tampering | stale host mirror diverging from the resident score across iterations | mitigate | The resident-score A/B asserts the mirrored host vector equals the host accumulation after a full run; mirror-on-read via the Plan-01 toggle |
| T-20-04-02 | Tampering | env-gate misconfiguration mutating the default GBDT score/eval path | mitigate | Resident branch gated on `cuda_on_device_enabled()`; `cargo test --workspace` env-unset proves byte-unchanged (D-09) |
| T-20-04-03 | Tampering | integer overflow in the class-major `offset = num_data * tree_id` across iterations | mitigate | Reuse the `usize` offset arithmetic (score_updater.rs:64); validate `tree_id >= 0` |
| T-20-04-SC | Tampering | npm/pip/cargo installs | accept | No new package installs this plan |
</threat_model>

<verification>
- `cargo test --workspace` green with LGBM_CUDA_ON_DEVICE unset (byte-unchanged GBDT path).
- `LGBM_CUDA_ON_DEVICE=1 cargo test -p oracle-harness --test resident_score_ab` green (residency A/B).
- The resident loop follows §16 order; DART/RF per-row-predict paths stay host (documented).
</verification>

<success_criteria>
The resident cuda_score_ stays on device across the whole multi-iteration train with the correct
§16 sequencing, matches the host score_ after a full run (D-06 layer 2), and leaves the env-unset
GBDT path byte-unchanged — closing ODL-16's residency half.
</success_criteria>

<artifacts_produced>
## Artifacts This Plan Produces
- `crates/lgbm-boosting/src/gbdt.rs`: additive resident-loop wiring in `TrainOneIter` behind `cuda_on_device_enabled()` following §16 order (Shrinkage → UpdateScore → optional RenewTreeOutput → Metric.Eval); L2 continuous-feature proving slice; documented ordering contract for L1/quantile follow-up; DART/RF per-row-predict paths left host-side.
- New test file `crates/oracle-harness/tests/resident_score_ab.rs` (D-06 layer-2 residency A/B: resident `cuda_score_` vs host `score_` after a full multi-iteration run, anchored to host/cpu-fold).
- Env behavior: resident cross-iteration loop active only when `LGBM_CUDA_ON_DEVICE=1`; env unset = byte-unchanged GBDT score/eval path.
</artifacts_produced>

<output>
Create `.planning/phases/20-on-device-score-updater-metrics/20-04-SUMMARY.md` when done.
</output>
