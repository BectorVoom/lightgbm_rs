---
phase: 23-perf-validation-default-on-rollout-dod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/lgbm-compute/src/lib.rs
  - crates/lgbm-treelearner/src/learner.rs
  - crates/lgbm-compute/tests/cuda_on_device.rs
autonomous: true
requirements: [ODL-21]
must_haves:
  truths:
    - "LGBM_CUDA_ON_DEVICE is tri-state: unset resolves to the device default, \"0\" forces OFF (the off-switch fallback), \"1\" forces ON (D-01)"
    - "on_device_default() returns false on every backend in this plan (pre-verdict safe state, D-09) — no behavior change vs today"
    - "The learner reads a SINGLE source of truth for on-device eligibility; the duplicate learner.rs parse is gone (L-2)"
    - "The env-unset merge gate is byte-unchanged on cpu/rocm builds (SC-4)"
  artifacts:
    - path: "crates/lgbm-compute/src/lib.rs"
      provides: "Tri-state resolver (cuda_on_device_override + on_device_default + cuda_on_device_enabled) preserving OnceLock read-once semantics"
      contains: "cuda_on_device_override"
    - path: "crates/lgbm-treelearner/src/learner.rs"
      provides: "Single-source-of-truth on-device eligibility (duplicate cuda_on_device_env removed)"
    - path: "crates/lgbm-compute/tests/cuda_on_device.rs"
      provides: "Unit tests for tri-state resolver + on_device_default per-feature"
  key_links:
    - from: "crates/lgbm-treelearner/src/learner.rs"
      to: "crates/lgbm-compute/src/lib.rs"
      via: "on_device_eligible base gate calls backend.on_device_growth_supported() (which returns cuda_on_device_enabled()) — no second parse"
      pattern: "on_device_growth_supported"
    - from: "crates/lgbm-boosting/src/score_updater.rs"
      to: "crates/lgbm-compute/src/lib.rs"
      via: "boosting_on_cuda inherits the resolver unchanged (name/signature preserved)"
      pattern: "cuda_on_device_enabled"
---

<objective>
Convert `LGBM_CUDA_ON_DEVICE` from a binary parse to a tri-state resolver and add a compile-time CUDA-only device-default helper — WITHOUT flipping the default on yet (D-09: the flip is a separate verdict-gated commit in plan 23-04). Reconcile the duplicate env parse in the learner (L-2) so all three toggle sites (compute, learner, boosting) agree on one source of truth.

Purpose: Land the routing machinery for the default-on rollout (ODL-21) while keeping every backend byte-unchanged (SC-4). `on_device_default()` returns `false` here (pre-verdict); plan 23-04 flips its body to `cfg!(feature="cuda")` only after the Kaggle A/B PASS verdict.
Output: Tri-state resolver + `on_device_default()` stub in `lgbm-compute`, single-source eligibility in the learner, unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-CONTEXT.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-RESEARCH.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-PATTERNS.md
</context>

## Artifacts this plan produces

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `cuda_on_device_override() -> Option<bool>` (lib.rs) | new private fn | OnceLock<Option<bool>>: `"1"`→Some(true), `"0"`→Some(false), else→None (D-01) |
| `on_device_default() -> bool` (lib.rs) | new private fn | Returns `false` in this plan (pre-verdict, D-09). Flipped to `cfg!(feature="cuda")` in 23-04 |
| `cuda_on_device_enabled() -> bool` (lib.rs) | REPLACED body, SAME name+signature | `cuda_on_device_override().unwrap_or_else(on_device_default)` |
| `LGBM_CUDA_ON_DEVICE` env semantics | CHANGED contract | binary → tri-state; `"0"` is now a first-class force-off |
| `crates/lgbm-compute/tests/cuda_on_device.rs` | new test file | tri-state + default per-feature unit tests |
| `cuda_on_device_env()` (learner.rs) | REMOVED | folded into the single compute-layer resolver (L-2) |

<tasks>

<task type="auto">
  <name>Task 1: Tri-state resolver + on_device_default() stub in lgbm-compute</name>
  <read_first>
    - crates/lgbm-compute/src/lib.rs §1305-1340 — the current binary `cuda_on_device_enabled()` (:1324, REPLACE its body) and the `split_2lane_enabled()` OnceLock sibling idiom (:1336) to mirror.
    - crates/lgbm-compute/src/runtime.rs §137-208 — the `#[cfg(feature="cuda")]` type-alias precedent for D-03 ("runtime binding = cargo feature").
    - 23-RESEARCH.md § Pattern 1 (tri-state override cache + compile-time default helper) and § Pitfall P-3 (dual-feature build).
  </read_first>
  <files>crates/lgbm-compute/src/lib.rs</files>
  <action>
    Replace the binary `cuda_on_device_enabled()` body at lib.rs:1324 with a tri-state design, keeping the `pub fn cuda_on_device_enabled() -> bool` name and signature UNCHANGED so `score_updater.rs` and both `on_device_growth_supported()` impls inherit the new semantics with no call-site edit (D-02, Pattern 3).
    Add a private `cuda_on_device_override() -> Option<bool>` backed by `static OnceLock<Option<bool>>`, resolving `LGBM_CUDA_ON_DEVICE` via exact-string match: `Ok("1")` => Some(true), `Ok("0")` => Some(false), everything else (unset/empty/malformed) => None (D-01, V5 exact-match closed enum). Preserve read-once OnceLock caching (P-1).
    Add a private `#[must_use] on_device_default() -> bool` that returns the literal `false` in THIS plan. Add a doc comment stating it is the pre-verdict safe state per D-09 and that plan 23-04 flips the body to `cfg!(feature="cuda")` after the Kaggle A/B PASS verdict; also note the mono-feature Pitfall P-3 caveat for the future flip. Do NOT bake any CUDA default into the env parse (D-02 anti-pattern).
    Make `cuda_on_device_enabled()` return `cuda_on_device_override().unwrap_or_else(on_device_default)`.
    The resolver must NOT be `#[cfg(feature="cpu")]`-gated (unlike `split_2lane_enabled`) since cuda/rocm builds call it.
    Update the surrounding doc comment on `cuda_on_device_enabled` to describe the tri-state contract (replace the "\"1\" ⇒ on, else off" wording).
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn cuda_on_device_override' crates/lgbm-compute/src/lib.rs` returns a match.
    - `grep -n 'fn on_device_default' crates/lgbm-compute/src/lib.rs` returns a match whose body returns `false` in this plan.
    - `pub fn cuda_on_device_enabled() -> bool` still exists with unchanged signature (grep confirms).
    - `cargo build -p lgbm-compute` exits 0.
  </acceptance_criteria>
  <done>The tri-state resolver + `false` default stub compile under the default cpu feature; the public resolver name/signature is unchanged so downstream call sites need no edit.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for tri-state resolver + on_device_default per-feature</name>
  <read_first>
    - crates/lgbm-compute/tests/capability.rs and crates/lgbm-compute/tests/split_info.rs — the integration-test file layout under crates/lgbm-compute/tests/ to mirror.
    - 23-RESEARCH.md § Validation Architecture (Test Map rows D-01, D-02/D-03) and § Pitfall P-1 (OnceLock read-once: cannot flip env mid-process).
    - crates/oracle-harness/tests/resident_score_ab.rs:168 — the precedent for skipping the default-check when the env is set.
  </read_first>
  <files>crates/lgbm-compute/tests/cuda_on_device.rs</files>
  <action>
    Create `crates/lgbm-compute/tests/cuda_on_device.rs`. Because the resolver is OnceLock-cached (read once per process, P-1), the override parse cannot be exercised across multiple env values in one process via the public fn. Test the tri-state MAPPING as a pure closed-enum decision that does not depend on the cache: expose a testable pure helper (e.g. a `#[doc(hidden)] pub fn cuda_on_device_override_from(s: Option<&str>) -> Option<bool>` in lib.rs that the OnceLock initializer also calls) and assert: `Some("1")`→Some(true), `Some("0")`→Some(false), `None`→None, `Some("")`→None, `Some("2")`→None, `Some("true")`→None. (Add that thin pure helper in Task 1's file if needed — it keeps the mapping unit-testable without fighting the OnceLock.)
    Assert `on_device_default()` semantics via a `#[cfg(not(feature = "cuda"))]` test that the resolved default is OFF: with the env unset in a fresh process the resolver returns `false` on the cpu build (SC-4 anchor). Name the tests so `cargo test -p lgbm-compute cuda_on_device` selects them (module/file already matches).
    Do NOT write a test that flips `LGBM_CUDA_ON_DEVICE` between two reads in one process (P-1 warning sign).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute cuda_on_device</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-compute cuda_on_device` runs and passes (not zero tests collected).
    - A test asserts the malformed/empty/unset string all map to `None` (follow device default).
    - A test asserts `"0"`→force-off and `"1"`→force-on at the mapping level.
    - The default-off assertion runs under the cpu build (no `-F cuda`).
  </acceptance_criteria>
  <done>The tri-state mapping (`unset`/`"0"`/`"1"`/malformed) and the cpu-build default-off are locked by passing unit tests selectable with `cargo test -p lgbm-compute cuda_on_device`.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the learner's duplicate parse (L-2, single source of truth)</name>
  <read_first>
    - crates/lgbm-treelearner/src/learner.rs:471-473 (`cuda_on_device_env()` — the duplicate binary parse to delete) and :539 (the `on_device_eligible: backend.on_device_growth_supported() && cuda_on_device_env()` double-gate) and the mirror in `refresh_on_device_eligibility` (~:3844).
    - 23-PATTERNS.md § "reconcile the duplicate parse (L-2)" and 23-RESEARCH.md § Pattern 2.
    - crates/lgbm-compute/src/lib.rs:1358 / :2288 — confirmation that both `on_device_growth_supported()` impls already return the resolved `cuda_on_device_enabled()`.
  </read_first>
  <files>crates/lgbm-treelearner/src/learner.rs</files>
  <action>
    Delete `fn cuda_on_device_env()` (learner.rs:471). At the `on_device_eligible` construction site (:539) drop the `&& cuda_on_device_env()` clause so the base gate is `backend.on_device_growth_supported()` alone — that method already returns the resolved tri-state `cuda_on_device_enabled()`, so the second parse is redundant and would disagree with the new `"0"` off-switch. Apply the identical simplification at the `refresh_on_device_eligibility` mirror (~:3844). Leave `on_device_eligible_gate()` (the categorical+quantized D-06 negation, :486-492) unchanged. Update any doc comment on :463-470 that references the now-removed duplicate parse.
    This makes `lgbm_compute::cuda_on_device_enabled()` the single source of truth shared by the compute discriminator, the learner eligibility gate, and the boosting `boosting_on_cuda` toggle.
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn cuda_on_device_env' crates/lgbm-treelearner/src/learner.rs` returns NO match (duplicate removed).
    - `grep -n 'cuda_on_device_env()' crates/lgbm-treelearner/src/learner.rs` returns NO match (both call sites simplified).
    - `cargo test --workspace` is green — the env-unset merge gate (`learner_parity`, `score_updater_parity`, `resident_score_ab`) is byte-unchanged (SC-4).
  </acceptance_criteria>
  <done>The learner has no independent env parse; all three toggle sites resolve through `lgbm_compute::cuda_on_device_enabled()`, and the full workspace merge gate is green with the env unset.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → routing code path | `LGBM_CUDA_ON_DEVICE` (untrusted-ish operator input) selects the tree-learner path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Tampering | `cuda_on_device_override()` env parse | mitigate | Exact-string closed-enum match (`Some(true)`/`Some(false)`/`None`); no eval/exec, no path/format interpretation of the value (ASVS V5). |
| T-23-01-SC | Tampering (safety invariant) | `on_device_default()` default resolution | mitigate | Returns `false` in this plan (D-09 pre-verdict) so cpu/rocm/cuda builds are all byte-unchanged; SC-4 upheld by the green `cargo test --workspace` merge gate. |
</threat_model>

<verification>
- `cargo build -p lgbm-compute` (Task 1)
- `cargo test -p lgbm-compute cuda_on_device` (Task 2 — tri-state mapping + cpu default-off)
- `cargo test --workspace` (Task 3 — full CPU f64 merge gate, SC-4 byte-unchanged)

**W4 decision — ROCm SC-4 (recorded):** ROCm byte-unchanged is COMPILE-TIME-GUARANTEED and accepted WITHOUT a runtime test lane. Rationale: `on_device_default()` (in this plan) returns the literal `false`, and after the 23-04 flip returns `cfg!(feature="cuda")` which is statically `false` under a `-F rocm` (cubecl-hip) build — so no ROCm binary can resolve on-device-default true (D-03). The spoofed local "gfx1100" is an 8-CU APU (not a real discrete ROCm target), so a runtime ROCm assertion would add no fidelity. SC-4 for ROCm is validated by `cargo build -p lgbm-compute --no-default-features -F rocm` compiling with `on_device_default()` statically `false` (23-04 Task 1 acceptance), plus code-comment inspection — no `#[cfg(feature="rocm")]` runtime assertion is added.
</verification>

<success_criteria>
- Tri-state resolver + `on_device_default()==false` stub landed; public `cuda_on_device_enabled()` signature unchanged.
- Learner duplicate parse removed; single source of truth.
- Full workspace test suite green with `LGBM_CUDA_ON_DEVICE` unset (SC-4).
</success_criteria>

<output>
Create `.planning/phases/23-perf-validation-default-on-rollout-dod/23-01-SUMMARY.md` when done.
</output>
