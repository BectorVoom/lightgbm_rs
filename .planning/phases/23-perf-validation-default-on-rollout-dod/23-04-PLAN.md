---
phase: 23-perf-validation-default-on-rollout-dod
plan: 04
type: execute
wave: 3
depends_on: [23-03]
files_modified:
  - crates/lgbm-compute/src/lib.rs
  - crates/lgbm-compute/tests/cuda_on_device.rs
autonomous: false
requirements: [ODL-21]
must_haves:
  truths:
    - "The default-on flip is a SEPARATE commit gated on the committed results artifact showing PASS (<=5% on BOTH shapes AND ~1e-6 parity) — never auto-engaged before proof (D-09)"
    - "After the flip, on_device_default() resolves to true ONLY under a cubecl-cuda binding; cpu/rocm builds stay false (D-02/D-03, SC-4)"
    - "LGBM_CUDA_ON_DEVICE=0 remains the off-switch fallback under the new default (ODL-21)"
    - "Unsupported configs (Ok(None) / use_quantized_grad) still silently fall back to host-CUDA with no log noise (D-10)"
    - "If the A/B FAILS, this plan is NOT committed; the default stays OFF and the phase is still DoD-complete with documented numbers + a follow-up note (D-09)"
  artifacts:
    - path: "crates/lgbm-compute/src/lib.rs"
      provides: "on_device_default() flipped to cfg!(feature=\"cuda\") (verdict-gated)"
      contains: "cfg!(feature = \"cuda\")"
    - path: "crates/lgbm-compute/tests/cuda_on_device.rs"
      provides: "cuda-arm default-on assertion + reaffirmed cpu/rocm default-off"
  key_links:
    - from: "crates/lgbm-compute/src/lib.rs"
      to: "crates/lgbm-compute/src/runtime.rs"
      via: "on_device_default() keyed on the compiled cubecl binding (cargo feature), mirroring ActiveRuntime aliasing (D-03)"
      pattern: "cfg!\\(feature = \"cuda\"\\)"
---

<objective>
Flip the CUDA-only on-device default ON — as a SEPARATE, verdict-gated commit (D-09). This plan is executed ONLY after the committed Kaggle A/B evidence artifact (`results.md` from plan 23-03) shows PASS: on-device median wall-clock `<= 5%` slower than host-CUDA on BOTH shapes (D-04/D-05) AND the real-CUDA end-to-end ~1e-6 parity assertion (D-11) green. It changes `on_device_default()` from the pre-verdict `false` to `cfg!(feature="cuda")`, making CUDA builds default to the on-device learner while cpu/rocm stay byte-unchanged (D-02/D-03, SC-4).

Purpose: Deliver ODL-21 (the on-device learner becomes the DEFAULT CUDA tree-learner path) with the audit-before-wire guarantee — never engaged before proof.
Output: The flipped `on_device_default()` body + the cuda-arm default-on assertion. If the A/B FAILS, this plan is intentionally left uncommitted (D-09) and the phase remains DoD-complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-CONTEXT.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-RESEARCH.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-PATTERNS.md
</context>

## Artifacts this plan produces

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `on_device_default()` body flip | CHANGED | `false` → `cfg!(feature="cuda")` (D-02/D-03); verdict-gated commit citing results.md |
| cuda-arm default-on test | new/updated assertion | `cfg(feature="cuda")` ⇒ default-on; `cfg(not(cuda))` ⇒ default-off reaffirmed |
| commit message | evidence citation | cites the `results.md` PASS verdict (audit-before-wire) |

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: A/B verdict gate before the default-on flip (D-09)</name>
  <action>Do not proceed to Task 1 until a human confirms the committed `results.md` evidence artifact shows a PASS verdict. This is the audit-before-wire gate: the behavior-changing flip is made ONLY on proof.</action>
  <what-built>Plans 23-01..23-03 landed the tri-state resolver, L-1 launch instrumentation, and the committed Kaggle A/B harness. Per the tracked "Manual inter-wave step (Wave 2 → Wave 3)" checklist in 23-03, the harness has been run on Kaggle (`boomvector`), captured via `poll_kaggle.sh`, and the evidence artifact `results.{md,json}` is committed under the phase dir (D-07). This checkpoint audits that committed artifact — do NOT proceed if the inter-wave checklist has not been completed and committed.</what-built>
  <how-to-verify>
    1. Open `.planning/phases/23-perf-validation-default-on-rollout-dod/results.md`.
    2. Confirm the verdict is PASS: on-device median wall-clock `<= 1.05x` host-CUDA on BOTH shapes (500k x 50 AND 100k x 500) — D-04/D-05.
    3. Confirm the D-11 real-CUDA parity assertion passed: `max(|p_on - p_host|) <= 1e-6`.
    4. Confirm `device_launches/tree` for the on-device arm is below the 85.7 (8,570/100-trees) baseline — SC-2.
    - If PASS on all: type "approved" to proceed with the flip.
    - If FAIL on any shape or parity: type "fail" — do NOT flip. The default stays OFF (opt-in via `LGBM_CUDA_ON_DEVICE=1`); record a follow-up note in the SUMMARY. The phase is still DoD-complete (D-09).
  </how-to-verify>
  <resume-signal>Type "approved" (A/B PASS — proceed to flip) or "fail" (A/B FAIL — skip the flip, close the phase DoD-complete with documented numbers).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Flip on_device_default() to cfg!(feature="cuda") (verdict-gated)</name>
  <read_first>
    - crates/lgbm-compute/src/lib.rs — the `on_device_default()` stub authored in plan 23-01 (returns `false`), and the surrounding `cuda_on_device_override()` / `cuda_on_device_enabled()` resolver.
    - crates/lgbm-compute/src/runtime.rs §137-208 — the `#[cfg(feature="cuda")]` type-alias precedent (D-03: runtime binding = cargo feature).
    - 23-RESEARCH.md § Pitfall P-3 (dual-feature `-F cuda,rocm` build) + § Alternatives Considered (per-runtime sealed const) — surface the mono-feature-vs-dual-feature choice, do not silently drop it.
    - .planning/phases/23-perf-validation-default-on-rollout-dod/results.md — the PASS verdict this commit cites.
  </read_first>
  <files>crates/lgbm-compute/src/lib.rs</files>
  <action>
    Change the body of `on_device_default()` from `false` to `cfg!(feature = "cuda")` (D-02/D-03). Update its doc comment: the default is now ON under a `cubecl-cuda` binding, OFF under cpu/rocm/wgpu; cite that this flip is gated on the `results.md` PASS verdict (D-09) and that `LGBM_CUDA_ON_DEVICE=0` remains the off-switch (ODL-21). Keep the tri-state override precedence: an explicit `"0"`/`"1"` still overrides the default (`cuda_on_device_override().unwrap_or_else(on_device_default)` is unchanged).
    Address Pitfall P-3 explicitly: EITHER (a) keep the `cfg!(feature="cuda")` helper and add a code comment asserting mono-feature builds (the real Kaggle `-F cuda` / ROCm `-F rocm` case), noting a `-F cuda,rocm` build would mis-default ROCm on; OR (b) if the dual-feature guarantee is wanted, implement the per-runtime sealed-trait const (`trait DeviceDefault { const ON_DEVICE_DEFAULT: bool; }` on `CudaRuntime=true`, `RocmRuntime`/`WgpuRuntime`/cpu`=false`, keyed on the concrete `R` in the generic `GpuBackend<R>` impl). Choose (a) unless the project builds dual-feature; record the choice + rationale in the SUMMARY.
    Do NOT touch the D-10 fallback path — `on_device_eligible_gate()` + the driver's `Ok(None)` already route unsupported configs to host-CUDA silently.
    W4 (recorded decision): ROCm SC-4 byte-unchanged is COMPILE-TIME-GUARANTEED and accepted without a runtime lane — `cfg!(feature="cuda")` is statically `false` under a `-F rocm` (cubecl-hip) build, so no ROCm binary can default-on. Validate ROCm SC-4 by the `--no-default-features -F rocm` compile + code-comment inspection in the acceptance criteria below; do NOT add a `#[cfg(feature="rocm")]` runtime assertion (the local "gfx1100" is a spoofed 8-CU APU, so a runtime lane adds no fidelity). Task 2's per-feature tests cover cpu (default-off) and cuda (default-on) arms; ROCm's default-off is the static `cfg!` fact, not a test.
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'cfg!(feature = "cuda")' crates/lgbm-compute/src/lib.rs` matches inside `on_device_default()`.
    - `cargo test --workspace` (default cpu build) is green — `on_device_default()` is `false` on cpu so the env-unset merge gate is byte-unchanged (SC-4).
    - `cargo build -p lgbm-compute --no-default-features -F rocm` compiles and (by inspection/comment) `on_device_default()` resolves false under rocm (or the per-runtime const enforces it).
    - The commit message cites the `results.md` PASS verdict (audit-before-wire, D-09).
  </acceptance_criteria>
  <done>`on_device_default()` returns `cfg!(feature="cuda")`; CUDA builds default to the on-device learner, cpu/rocm stay off (SC-4), the off-switch and D-10 fallback are intact, and the merge gate is green.</done>
</task>

<task type="auto">
  <name>Task 2: cuda-arm default-on assertion + reaffirm cpu/rocm default-off</name>
  <read_first>
    - crates/lgbm-compute/tests/cuda_on_device.rs — the unit tests from plan 23-01 (cpu default-off) to extend.
    - 23-RESEARCH.md § Validation Architecture (Test Map row D-02/D-03: cpu/rocm ⇒ false, cuda ⇒ true; the cuda arm needs a `-F cuda` build).
  </read_first>
  <files>crates/lgbm-compute/tests/cuda_on_device.rs</files>
  <action>
    Add a `#[cfg(feature = "cuda")]` test asserting `on_device_default()` (or the resolved `cuda_on_device_enabled()` with the env unset) is `true` under the cuda build, and reaffirm the `#[cfg(not(feature = "cuda"))]` default-off assertion. Because `on_device_default()` may be private, expose it for test via a `#[doc(hidden)] pub` accessor or `#[cfg(test)]` re-export if not already reachable. Document that the cuda-arm test compiles/runs only under `cargo test -p lgbm-compute --no-default-features -F cuda` (which compiles locally without a GPU; the growth itself needs the device, but the default-resolution assertion does not).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute cuda_on_device</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'cfg(feature = "cuda")' crates/lgbm-compute/tests/cuda_on_device.rs` shows a cuda-arm test.
    - `cargo test -p lgbm-compute cuda_on_device` (cpu build) passes and still asserts default-off.
    - `cargo test -p lgbm-compute --no-default-features -F cuda cuda_on_device` compiles and the cuda-arm test asserts default-on.
  </acceptance_criteria>
  <done>The per-feature default resolution is locked by tests: default-off on cpu/rocm, default-on on cuda.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| compiled cargo feature → routing default | the on-device default is decided at compile time by the cubecl binding |
| evidence artifact → rollout decision | `results.md` verdict gates whether this behavior-changing commit is made |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-SC | Tampering (safety invariant) | `on_device_default()` flip | mitigate | Keyed on `cfg!(feature="cuda")` so cpu/rocm builds are byte-unchanged (SC-4); merge gate green. P-3 dual-feature caveat documented (or per-runtime const). |
| T-23-04-V | Repudiation / process | the rollout decision | mitigate | Blocking human-verify checkpoint requires the committed `results.md` PASS verdict before the flip; FAIL leaves the plan uncommitted (D-09). Commit message cites the evidence. |
| T-23-04-D10 | Denial of service (correctness) | unsupported-config fallback | accept | `Ok(None)` / `on_device_eligible_gate` already route quantized/categorical-quantized to host silently (D-10); no change here. Verified by the green merge gate. |
</threat_model>

<verification>
- Blocking checkpoint: `results.md` shows PASS (both shapes <=5% + ~1e-6 parity + launches/tree < 85.7) — else skip the flip (D-09).
- `cargo test --workspace` (Task 1 — cpu default byte-unchanged, SC-4)
- `cargo test -p lgbm-compute cuda_on_device` (cpu) + `--no-default-features -F cuda` (Task 2 — per-feature default resolution)
</verification>

<success_criteria>
- If A/B PASS: `on_device_default()==cfg!(feature="cuda")`, committed as a separate verdict-citing commit; CUDA defaults on, cpu/rocm off, off-switch + D-10 fallback intact, merge gate green (ODL-21 delivered).
- If A/B FAIL: plan uncommitted, default stays OFF, phase DoD-complete with documented numbers + follow-up note (D-09).
</success_criteria>

<output>
Create `.planning/phases/23-perf-validation-default-on-rollout-dod/23-04-SUMMARY.md` when done (including the flip/no-flip decision and the P-3 mechanism choice).
</output>
