---
phase: 23-perf-validation-default-on-rollout-dod
plan: 03
type: execute
wave: 2
depends_on: [23-01, 23-02]
files_modified:
  - .planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py
  - .planning/phases/23-perf-validation-default-on-rollout-dod/fixtures/sample_phase_prof.stderr
  - .planning/phases/23-perf-validation-default-on-rollout-dod/test_ab_harness_parse.py
autonomous: true
requirements: [ODL-20]
must_haves:
  truths:
    - "A committed, reusable Kaggle A/B script measures device_launches/tree + wall-clock medians + ratios across 3 runs x 2 shapes (500k x 50 AND 100k x 500) x 2 paths (on-device vs host-CUDA) (ODL-20, D-05, D-06)"
    - "The host arm sets LGBM_CUDA_ON_DEVICE=0 (the new tri-state off-switch) and the on-device arm sets =1; both set LGBM_PHASE_PROF=1"
    - "The harness embeds the D-11 real-CUDA end-to-end ~1e-6 parity CHECK (on-device preds vs host-CUDA) as a captured max_abs folded into the verdict — NOT a bare inline assert — with a documented near-tie envelope and the cpu-f64 structural anchor as the primary parity proof (def-f8u-01)"
    - "The harness emits a structured results.{md,json} evidence artifact UNCONDITIONALLY (finally/before-exit path, even on parity/run failure — D-09) with a PASS/FAIL verdict per the D-04 (<=5%) / D-05 (both shapes) + parity_ok rule, then exits non-zero on FAIL"
    - "The COUNTS-line parser is verifiable LOCALLY via a dry-run parse over a committed stderr fixture (no Kaggle needed to test the parser)"
  artifacts:
    - path: ".planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py"
      provides: "Committed Kaggle A/B harness extending the continue_benchmark.py family; parse + median + verdict + parity assertion + results emit"
      contains: "device_launches"
    - path: ".planning/phases/23-perf-validation-default-on-rollout-dod/test_ab_harness_parse.py"
      provides: "Local dry-run test of the COUNTS regex + median/verdict logic over a fixture"
    - path: ".planning/phases/23-perf-validation-default-on-rollout-dod/fixtures/sample_phase_prof.stderr"
      provides: "Captured stderr fixture with a device_launches= COUNTS line for the local parse test"
  key_links:
    - from: ".planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py"
      to: "crates/lgbm-treelearner/src/phase_prof.rs"
      via: "regex parse of the [phase_prof:train] COUNTS: device_launches=N line (23-02 makes it non-zero on-device)"
      pattern: "device_launches="
    - from: ".planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py"
      to: "crates/lgbm-compute/src/lib.rs"
      via: "host arm uses LGBM_CUDA_ON_DEVICE=0 off-switch (23-01 tri-state)"
      pattern: "LGBM_CUDA_ON_DEVICE"
---

<objective>
Build the committed, reusable Kaggle A/B harness (ODL-20) that measures the on-device path's `device_launches/tree` and wall-clock ratios on real discrete CUDA, emits the first-class DoD evidence artifact (`results.{md,json}`, D-07), and embeds the D-11 real-CUDA end-to-end ~1e-6 parity assertion. The harness ALWAYS lands (D-09) — it is the phase's core value regardless of the verdict.

Purpose: Produce the empirical evidence (device-launch collapse below the 8,570/100-trees baseline + not-slower-than-host-CUDA verdict + real-CUDA parity) that plan 23-04 gates the default-on flip on.
Output: `ab_harness.py` (Kaggle-run), a captured stderr fixture, and a LOCAL parse test proving the parser/verdict logic without Kaggle.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-CONTEXT.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-RESEARCH.md
@.planning/phases/23-perf-validation-default-on-rollout-dod/23-PATTERNS.md
</context>

Note: this plan runs on the shipped Python-wheel train path where `phase_prof::dump("train")` is emitted (`crates/lgbm/src/booster.rs:1480`). The on-device `device_launches` line only becomes non-zero because of plan 23-02 (L-1), and the `"0"` host off-switch only exists because of plan 23-01 — hence `depends_on: [23-01, 23-02]`.

## Artifacts this plan produces

| Symbol / file | Kind | Notes |
|---------------|------|-------|
| `ab_harness.py` | new committed script | extends `continue_benchmark.py`; 3x2x2 matrix, medians, ratios, parity, results emit |
| `results.{md,json}` schema | defined by the harness | raw runs + medians + ratios + launches + parity max_abs + PASS/FAIL verdict (emitted on Kaggle, not committed by this plan) |
| `fixtures/sample_phase_prof.stderr` | new fixture | a captured stderr block containing a `device_launches=` COUNTS line |
| `test_ab_harness_parse.py` | new local test | dry-run parse + median + verdict over the fixture (no Kaggle) |

<tasks>

<task type="auto">
  <name>Task 1: Committed Kaggle A/B harness (matrix + parse + parity + results emit)</name>
  <read_first>
    - continue_benchmark.py (repo root) — the clone -> maturin `-F cuda` -> install -> official-lightgbm -> `make_classification` -> timed A/B flow (authenticated as `boomvector`) to EXTEND, not rewrite (setup ~:11-38, timed arm ~:61-76).
    - benchmark.py, poll_kaggle.sh (repo root) — the push/poll analog.
    - 23-RESEARCH.md § Code Examples (the `COUNTS` regex; the D-05 matrix skeleton; the D-11 parity assertion) and § Security Domain (no embedded tokens; pin repo; official lgb `--no-binary`).
    - 23-CONTEXT.md D-04..D-08, D-11 — the exact pass rule, matrix, shapes, comparison set, parity check.
  </read_first>
  <files>.planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py</files>
  <action>
    Author `ab_harness.py` extending the `continue_benchmark.py` build flow (clone `BectorVoom/lightgbm_rs`, `maturin build --release -F cuda`, install the wheel, build official lightgbm from source with USE_CUDA=ON for the context ratio, `--no-binary`). Do NOT embed any Kaggle token or credential — auth stays out-of-band in the CLI config (T-23-03).
    Run the D-05 matrix: 3 runs x 2 shapes {(500000,50),(100000,500)} (D-06) x 2 paths, `n_estimators=100` (D-08 baseline convention). Set per-arm env: host arm `LGBM_CUDA_ON_DEVICE=0` (the 23-01 tri-state off-switch), on-device arm `LGBM_CUDA_ON_DEVICE=1`; BOTH arms `LGBM_PHASE_PROF=1`. Capture stderr per arm.
    Parse the COUNTS line with the SHORT total-only regex `re.compile(r"\[phase_prof:\w+\] COUNTS: device_launches=(?P<launches>\d+)")` — this consumer deliberately uses the total-only form, NOT the full paren-terminated regex quoted in 23-PATTERNS.md § Code Examples (`device_launches=… \(build_resident=… fused=…\)`). The short form matches because 23-02 keeps `device_launches=<total>` as the FIRST COUNTS field and folds `on_device=` INSIDE the parenthesized breakdown (W3 producer/consumer contract). Optionally also capture the `on_device=` sub-field with a separate non-anchored regex. Compute `device_launches/tree = launches / 100`.
    Take the MEDIAN of the 3 wall-clocks per (shape,path). Compute the verdict: PASS iff for BOTH shapes `median(on_device) <= 1.05 * median(host_cuda)` (D-04 <=5% "within noise", D-05 both-shapes) AND the D-11 parity gate below is green.
    D-11 real-CUDA parity gate (crash-safe, W1): after training the on-device and host-CUDA arms on the SAME (X,y), COMPUTE `max_abs_on_host = max(|p_on - p_host|)` on `raw_score=True` predictions over the actual dataset and STORE it into the results structure BEFORE any threshold comparison. Do NOT `assert` inline (a bare assert would raise and abort BEFORE results are written — D-09 requires the artifact to land unconditionally). Instead fold the parity result into the PASS/FAIL verdict as a boolean `parity_ok`.
    Near-tie / nondeterminism envelope (W2, def-f8u-01 — never compare two nondeterministic GPU f32 paths to each other at a bare 1e-6): the primary parity proof is the CPU f64 structural anchor (the on-device tree is bit-exact on the cubecl-cpu lane, already green in the merge gate + per-phase gates 14-22, D-11); the Kaggle real-CUDA `max_abs_on_host` is CORROBORATING evidence, not the sole veto. Define `parity_ok = max_abs_on_host <= (atol + rtol * max(|p_host|))` with a documented small envelope `atol=1e-6, rtol=1e-6` so a legitimate f32-vs-f32 near-tie cannot produce a false-negative that wrongly vetoes the ODL-21 flip. Record BOTH the raw `max_abs_on_host` and the envelope-adjusted `parity_ok` in results; also report `max(|p_on - p_official|)` as context only (never a same-tol gate — f32 reference). Add an explicit note in `results.md` stating the cpu-f64 anchor is the primary parity proof and the real-CUDA check is corroboration under the documented envelope.
    Emit `results.json` (raw runs + medians + ratios + launches + parity `max_abs_on_host` + `parity_ok` + PASS/FAIL verdict) and `results.md` (a verdict table: on-device vs host-CUDA per shape, launches/tree vs baseline, official context ratio, parity max_abs + envelope note) UNCONDITIONALLY — wrap the whole matrix/parity flow so the results emit runs on a `finally`/before-exit path even when parity or a run fails, so the committed evidence artifact ALWAYS lands (D-09). AFTER emitting results, if the overall verdict is FAIL (either shape >5% OR `parity_ok` false), exit non-zero. Factor the parse + median + verdict (incl. `parity_ok` envelope) logic into importable pure functions (so Task 3 can test them locally).
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('.planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py').read()); print('parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'LGBM_CUDA_ON_DEVICE' .planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py` shows BOTH `=0` (host) and `=1` (on-device) arms.
    - `grep -n 'device_launches=(?P<launches>' .planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py` shows the SHORT total-only COUNTS regex (not the full paren-terminated form).
    - `grep -nE '1\.05|<= *1e-6|1e-6' .planning/phases/...ab_harness.py` shows the D-04 (<=5%) verdict and the D-11 (<=1e-6) parity envelope.
    - `grep -n 'max_abs_on_host\|parity_ok' ...ab_harness.py` shows parity is CAPTURED into a variable and folded into the verdict (not a bare inline `assert`); `grep -c 'finally' ...ab_harness.py` >= 1 (results emit on the before-exit path).
    - `grep -n 'atol\|rtol\|cpu.*f64.*anchor\|corroborat' ...ab_harness.py` shows the documented near-tie envelope + the cpu-f64-anchor-is-primary note (W2, def-f8u-01).
    - `grep -n '500000|100000\|100000, *500\|500000, *50' ...` shows both shapes; `n_estimators` = 100.
    - No credential literal in the file (grep for `token`, `KAGGLE_KEY`, `api_key` returns nothing sensitive).
    - `python -c "ast.parse(...)"` reports `parse-ok` (valid Python).
  </acceptance_criteria>
  <done>A committed, credential-free `ab_harness.py` runs the 3x2x2 matrix with the tri-state off-switch/on-device arms, parses `device_launches` via the short total-only regex, computes the D-04/D-05 verdict FOLDING IN the D-11 parity `parity_ok` (near-tie envelope, cpu-f64 anchor primary), emits `results.{md,json}` UNCONDITIONALLY on a finally/before-exit path (D-09), then exits non-zero on FAIL. Parse/median/verdict logic is importable for local testing.</done>
</task>

<task type="auto">
  <name>Task 2: Capture a phase_prof stderr fixture for local parser verification</name>
  <read_first>
    - crates/lgbm-treelearner/src/phase_prof.rs:196-198 — the exact COUNTS format string (post-23-02, with `on_device=`) to reproduce faithfully in the fixture.
    - kaggle_out/ (repo root, untracked) — real captured stderr examples of the phase_prof lines, if present, to copy the exact shape.
  </read_first>
  <files>.planning/phases/23-perf-validation-default-on-rollout-dod/fixtures/sample_phase_prof.stderr</files>
  <action>
    Create a small `fixtures/sample_phase_prof.stderr` containing a realistic on-device `[phase_prof:train] COUNTS: device_launches=<N> (build_resident=0 subtract_resident=0 scan_resident=0 fused=0 on_device=<N>) | scan_roundtrips(syncs)=0` line (a plausible collapsed N, e.g. a few thousand for 100 trees) plus a host-path COUNTS line with a large `device_launches` (~8570) so the parser test can distinguish the two arms. Match the EXACT format emitted by phase_prof.rs post-23-02 (field order, spacing, parentheses) so the fixture is a faithful parse target — critically, `device_launches=<total>` MUST be the first field and `on_device=` MUST sit INSIDE the parentheses so the harness SHORT total-only regex (W3) matches the fixture exactly as it will match real Kaggle stderr.
  </action>
  <verify>
    <automated>grep -c 'device_launches=' .planning/phases/23-perf-validation-default-on-rollout-dod/fixtures/sample_phase_prof.stderr</automated>
  </verify>
  <acceptance_criteria>
    - The fixture contains at least two `device_launches=` COUNTS lines (one collapsed on-device, one ~8570 host).
    - The line format matches the phase_prof.rs emit (same field order incl. `on_device=`).
    - `grep -c 'device_launches=' ...fixtures/sample_phase_prof.stderr` >= 2.
  </acceptance_criteria>
  <done>A committed stderr fixture faithfully mirrors the phase_prof COUNTS output for both an on-device (collapsed) and a host (~8570) arm, giving the local parser a real target.</done>
</task>

<task type="auto">
  <name>Task 3: Local dry-run parse + verdict test over the fixture</name>
  <read_first>
    - .planning/phases/23-perf-validation-default-on-rollout-dod/ab_harness.py — the importable parse/median/verdict functions authored in Task 1.
    - .planning/phases/23-perf-validation-default-on-rollout-dod/fixtures/sample_phase_prof.stderr — the fixture from Task 2.
    - 23-VALIDATION.md § Wave 0 Requirements (committed harness validated locally by a dry-run parse over a captured stderr fixture).
  </read_first>
  <files>.planning/phases/23-perf-validation-default-on-rollout-dod/test_ab_harness_parse.py</files>
  <action>
    Author `test_ab_harness_parse.py` (a plain-Python assertion script; runnable with `python test_ab_harness_parse.py`, no pytest dependency required). Import the parse function from `ab_harness.py`, read the fixture, and assert: (a) the on-device line parses to the expected collapsed `device_launches` integer via the SHORT total-only regex, (b) the host line parses to ~8570, (c) `device_launches/tree` = launches/100 is computed correctly, (d) the median helper returns the middle of 3 synthetic wall-clocks, (e) the verdict helper returns PASS when both shapes' on-device medians are `<= 1.05x` host AND `parity_ok` is true, and FAIL when either shape exceeds the 1.05x bar OR `parity_ok` is false (feed synthetic medians + parity values for all branches), (f) the `parity_ok` envelope helper returns true for a near-tie just inside `atol+rtol*|p_host|` and false for a real divergence beyond it (W2). Print a clear pass/fail summary and exit non-zero on any assertion failure.
    This proves the parser + verdict logic locally (the harness itself runs only on Kaggle).
  </action>
  <verify>
    <automated>python .planning/phases/23-perf-validation-default-on-rollout-dod/test_ab_harness_parse.py</automated>
  </verify>
  <acceptance_criteria>
    - `python .planning/phases/23-perf-validation-default-on-rollout-dod/test_ab_harness_parse.py` exits 0 and prints a pass summary.
    - The test covers the collapsed on-device parse, the ~8570 host parse, launches/tree, the median helper, BOTH verdict branches (PASS and FAIL), and the `parity_ok` envelope (near-tie true / divergence false).
    - The test imports the real functions from `ab_harness.py` (not a reimplementation).
  </acceptance_criteria>
  <done>The COUNTS parser and the D-04/D-05 verdict logic are locally proven over a faithful fixture — the Kaggle-only measurement is de-risked, and the parser cannot silently regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle CI ↔ public repo | harness clones + builds `BectorVoom/lightgbm_rs` from source on Kaggle |
| harness ↔ Kaggle credentials | Kaggle CLI auth (`boomvector`) is out-of-band; must not enter the committed script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | Information disclosure | committed `ab_harness.py` | mitigate | No Kaggle token/credential literal in the script; auth stays in the CLI's out-of-band config. Grep-verified in acceptance criteria. |
| T-23-03-SC | Tampering / supply chain | Kaggle clone + build | mitigate | Pin the repo (`BectorVoom/lightgbm_rs`); build lgb_rs from source via maturin (no third-party binary wheel); official `lightgbm` from source (`--no-binary`). No new package enters the shipped crate. |
| T-23-03-I | Information disclosure | `results.{md,json}` artifact | mitigate | Artifact carries only aggregate timings/launch counts/ratios/parity numbers — no dataset content, no tokens. |
</threat_model>

<verification>
- `python -c "ast.parse(...)"` on `ab_harness.py` (Task 1 — valid Python, credential-free)
- `grep -c 'device_launches=' fixtures/sample_phase_prof.stderr` (Task 2)
- `python test_ab_harness_parse.py` (Task 3 — parser + verdict logic proven locally)
- Manual / Kaggle-only (documented in 23-VALIDATION.md § Manual-Only): the real-CUDA A/B run + `results.{md,json}` capture via `poll_kaggle.sh` — NOT locally runnable (local GPU is a spoofed 8-CU APU).
</verification>

<success_criteria>
- Committed `ab_harness.py` runs the 3x2x2 matrix with tri-state arms, parses device_launches, computes the D-04/D-05 verdict, folds in D-11 parity (crash-safe), emits `results.{md,json}` unconditionally.
- The COUNTS parser + verdict logic pass a local dry-run test over a faithful fixture.
- No credentials committed; supply-chain pinned.
- (Kaggle, manual) real-CUDA `device_launches/tree` < 85.7 baseline + not-slower verdict + ~1e-6 parity captured into the committed evidence artifact.
</success_criteria>

## Manual inter-wave step (Wave 2 → Wave 3, D-07 evidence commit) — W5

**This is an explicit, tracked out-of-band step between this plan (Wave 2) and 23-04 (Wave 3). It is NOT auto-runnable — the local GPU is a spoofed 8-CU APU; only Kaggle gives real discrete CUDA numbers.** After 23-03 lands the committed harness + local parse test, and BEFORE the 23-04 blocking checkpoint, a human performs and records:

1. [ ] Push/run `ab_harness.py` on Kaggle authenticated as `boomvector` (extends the `continue_benchmark.py` push flow; kernel e.g. `boomvector/lgb-rs-cuda-bench`).
2. [ ] Poll for completion with `poll_kaggle.sh` and pull the kernel output (`kaggle kernels output ... -p kaggle_out`).
3. [ ] Extract the harness-emitted `results.{md,json}` from the Kaggle output and place them under `.planning/phases/23-perf-validation-default-on-rollout-dod/`.
4. [ ] Commit `results.{md,json}` under the phase dir (the D-07 first-class DoD evidence artifact) — this is the artifact the 23-04 checkpoint audits. Commit even on a FAIL verdict (D-09: the phase is DoD-complete with documented numbers either way).

This checklist makes the D-07 evidence commit a tracked hand-off, not an assumed precondition of 23-04.

<output>
Create `.planning/phases/23-perf-validation-default-on-rollout-dod/23-03-SUMMARY.md` when done.
</output>
