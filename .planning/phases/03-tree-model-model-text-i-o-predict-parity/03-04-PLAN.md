---
phase: 03-tree-model-model-text-i-o-predict-parity
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - crates/lgbm-model/src/ensemble.rs
  - crates/lgbm-model/src/predict.rs
  - crates/lgbm-model/tests/predict_subrange.rs
autonomous: true
requirements: [PRD-06, PRD-01]

must_haves:
  truths:
    - "Sub-range prediction returns the C++-matching slice of the ensemble for representative `(start_iteration, num_iteration)` pairs, including `num_iteration == -1` (== all) and `start_iteration > 0` (PRD-06)."
    - "`InitPredict` sub-range math (`start = clamp(start_iteration, 0, total)`, `num = if num_iteration>0 { min(num_iteration, total-start) } else { total-start }`) matches C++ `gbdt.h:426`."
    - "Sub-range raw scores are within ~1e-6 (f32) of the C++ reference (PRD-06 over PRD-01 accumulation)."
  artifacts:
    - path: "crates/lgbm-model/src/ensemble.rs"
      provides: "InitPredict sub-range slice math wired into predict_raw start/num_iteration parameters (parity-asserted)"
      contains: "init_predict"
    - path: "crates/lgbm-model/src/predict.rs"
      provides: "predict_raw batch entry threading start_iteration/num_iteration through to the ensemble slice"
      contains: "start_iteration"
    - path: "crates/lgbm-model/tests/predict_subrange.rs"
      provides: "D-06 layer 5 — compare_within on sub-range raw scores for representative slices incl. -1==all"
  key_links:
    - from: "crates/lgbm-model/src/predict.rs"
      to: "crates/lgbm-model/src/ensemble.rs predict_raw init_predict"
      via: "start_iteration/num_iteration threaded into the slice loop"
      pattern: "init_predict"
    - from: "crates/lgbm-model/tests/predict_subrange.rs"
      to: "crates/lgbm-model/tests/fixtures/models/subrange"
      via: "compare_within over committed (start,num) slice goldens"
      pattern: "compare_within"
---

<objective>
Final vertical slice: sub-range prediction (`start_iteration` / `num_iteration`) returning the C++-matching slice of the ensemble — closing PRD-06 and completing the Phase-3 prediction surface.

Purpose: 03-02 introduced the `predict_raw(features, start_iteration, num_iteration)` signature and the `init_predict` helper but only exercised the full-range path (`start=0, num=-1`). This slice proves the slice math for representative `(start_iteration, num_iteration)` pairs — including `num_iteration == -1` (all), a bounded count, and a non-zero start — against committed C++ sub-range goldens (D-06 layer 5). It is the smallest remaining slice: the accumulation loop already exists; this adds the parity assertion for the clamping/slicing semantics (`-1 == all`, `gbdt.h:426`).

Output: parity-asserted `init_predict` sub-range math in `ensemble.rs`, the `start_iteration`/`num_iteration` parameters threaded through the `predict.rs` batch entry, and the D-06 layer-5 sub-range parity test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-CONTEXT.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-RESEARCH.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-PATTERNS.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-VALIDATION.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-03-SUMMARY.md

# Workspace analogs
@crates/lgbm-model/src/ensemble.rs
@crates/lgbm-model/src/predict.rs
@crates/lgbm-model/tests/predict_raw_parity.rs
@crates/lgbm-model/tests/golden/mod.rs
@crates/lgbm-dataset/tests/example_dataset_parity.rs
</context>

<artifacts_this_phase_produces>
**This plan (03-04) creates these NEW symbols** (excluded from cross-AI source-drift verification):

- `crates/lgbm-model/src/ensemble.rs` (extended/asserted): the `init_predict(start_iteration: i32, num_iteration: i32) -> (start, num)` sub-range helper (introduced in 03-02) now has parity tests; `predict_raw` honors the clamped slice.
- `crates/lgbm-model/src/predict.rs` (extended): the batch `predict_raw` public entry accepts/threads `start_iteration`/`num_iteration` (default full-range `0`/`-1`) to the ensemble slice for all input forms.
- One integration test: `predict_subrange.rs` (D-06 layer 5).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: InitPredict sub-range parity + thread start_iteration/num_iteration through the predict driver</name>
  <read_first>
    - `crates/lgbm-model/src/ensemble.rs` (the `predict_raw` + `init_predict` from 03-02 — assert + finalize the slice math)
    - `crates/lgbm-model/src/predict.rs` (the batch dense/CSR/CSC driver — thread the two params through)
    - 03-RESEARCH.md § Ensemble predict loop + InitPredict (lines 248-261 — the exact clamp/slice math), § Phase Requirements PRD-06 (gbdt.h:426 `InitPredict` sets start_iteration_for_pred_/num_iteration_for_pred_; `-1 == all`)
    - `LightGBM/src/boosting/gbdt.h` `InitPredict` (426-436), `LightGBM/include/LightGBM/boosting.h` (start_iteration/num_iteration semantics, `-1 == all`)
    - `crates/lgbm-core/src/config/mod.rs` (`start_iteration_predict` default 0, `num_iteration_predict` default -1 — the param semantics to mirror)
  </read_first>
  <behavior>
    Given a model with `total = trees.len()/num_tree_per_iteration` iterations:
    - Test 1 (`-1 == all`): `init_predict(0, -1)` → `(start=0, num=total)`.
    - Test 2 (bounded count): `init_predict(0, 3)` → `(0, min(3, total))`.
    - Test 3 (non-zero start): `init_predict(2, -1)` → `(2, total-2)`; `init_predict(2, 3)` → `(2, min(3, total-2))`.
    - Test 4 (clamp): `init_predict(total+5, -1)` → `(start=total, num=0)` (start clamped to total; empty slice).
    - Test 5 (slice accumulation): `predict_raw(row, 1, 2)` accumulates ONLY `trees[i*ntpi+k]` for `i in 1..3` — assert it equals the manual sum of exactly those trees, and differs from the full-range result.
  </behavior>
  <action>
    In `crates/lgbm-model/src/ensemble.rs`, finalize `init_predict(start_iteration, num_iteration) -> (usize, usize)` as a faithful port of `gbdt.h:426-436`: `total = trees.len() / num_tree_per_iteration`; `start = start_iteration.clamp(0, total)` (in usize/i32-safe arithmetic); `num = if num_iteration > 0 { min(num_iteration as usize, total - start) } else { total - start }` (so `-1` and `0` both mean "all from start" per the C++ `> 0` test — confirm against the C++; `num_iteration <= 0` → remaining). Ensure `predict_raw` loops exactly `i in start..start+num`, `k in 0..ntpi`, accumulating in f64. Document the math with the C++ source line.
    In `crates/lgbm-model/src/predict.rs`, ensure the batch `predict_raw` public entry (and csr/csc variants) accept `start_iteration: i32` and `num_iteration: i32` and thread them into the ensemble call for every row (default full-range `0`/`-1` for the 03-02/03-03 callers — keep those green). Validate that `start_iteration`/`num_iteration` are sane (no panic on extreme values — the clamp handles it).
    Add the `<behavior>` tests inline as `#[cfg(test)] mod tests` in ensemble.rs.
  </action>
  <verify>
    <automated>cargo test -p lgbm-model ensemble:: && cargo test -p lgbm-model --test predict_raw_parity --test predict_transform --test predict_leaf_parity</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-model ensemble::` passes all `<behavior>` cases incl. `-1==all`, bounded count, non-zero start, and over-range clamp.
    - `init_predict(0, -1)` returns `(0, total)` and `init_predict(total+5, -1)` returns `(total, 0)` (asserted).
    - The existing `predict_raw_parity` / `predict_transform` / `predict_leaf_parity` tests STILL pass with the threaded signature (full-range default unchanged) — no regression.
    - No panic on extreme `start_iteration`/`num_iteration` (clamp covers it).
  </acceptance_criteria>
  <done>Sub-range slice math is a faithful `InitPredict` port, threaded through the driver, with the full-range callers still green.</done>
</task>

<task type="auto">
  <name>Task 2: D-06 layer-5 sub-range parity test</name>
  <read_first>
    - `crates/lgbm-model/tests/predict_raw_parity.rs` (the layer-2 raw-score replay from 03-02 — the closest test analog)
    - `crates/lgbm-model/tests/golden/mod.rs` (the golden loader; the sub-range golden encodes multiple `(start, num)` slices)
    - `crates/lgbm-dataset/tests/example_dataset_parity.rs` (committed-golden replay + graceful SKIP + localizing-assert idiom)
    - 03-VALIDATION.md layer 5 (the sub-range corpus + `compare_within`)
    - 03-RESEARCH.md § Validation (D-06 layer 5 — sub-range raw scores for representative `(start_iteration, num_iteration)` incl. `-1==all`)
    - `crates/oracle-harness/src/comparator.rs` (`compare_within`, `ORACLE_TOL`)
  </read_first>
  <action>
    Create `crates/lgbm-model/tests/predict_subrange.rs` (D-06 layer 5). Resolve the `subrange` fixture model + its sub-range golden via `CARGO_MANIFEST_DIR`/tests/fixtures/models/subrange (graceful SKIP + regen eprintln if absent). The sub-range golden (emitted by `xtask model-capture` in 03-01) contains raw-score vectors for several representative `(start_iteration, num_iteration)` pairs — at minimum: `(0, -1)` (all), a bounded count e.g. `(0, k)`, and a non-zero start e.g. `(s, -1)` and `(s, k)`. For each pair, load the model, call the batch `predict_raw` with those params on the committed input matrix, and assert `compare_within(&rust_raw, &cpp_subrange_golden[pair], ORACLE_TOL)` is Ok. Localizing assert messages naming the fixture + `(start, num)` pair + row index. Keep the dense input form (CSR/CSC raw parity already covered in 03-02 — sub-range is orthogonal to input form).
    If the 03-01 capture did not emit the exact `(start, num)` pairs this test needs, this task may extend `xtask model_capture`'s subrange-golden emission to include them and re-run capture (must remain byte-idempotent); prefer reusing whatever pairs the manifest already records.
  </action>
  <verify>
    <automated>cargo test -p lgbm-model --test predict_subrange && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-model --test predict_subrange` passes: raw scores for each `(start_iteration, num_iteration)` slice — including `-1==all`, a bounded count, and a non-zero start — are within `ORACLE_TOL` of the C++ sub-range golden (`compare_within` Ok) — PRD-06.
    - The test exercises at least 3 distinct `(start, num)` pairs (asserted by iterating the golden's recorded pairs).
    - If capture was extended, `cargo run -p xtask -- model-capture` remains byte-idempotent (empty `git diff` on re-run).
    - `cargo test --workspace` green — full Phase-3 suite (layers 1-5) passes with no regression.
  </acceptance_criteria>
  <done>Sub-range raw scores match C++ within ~1e-6 across representative slices — PRD-06 closed and the full D-06 layered battery (1-5) is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `start_iteration`/`num_iteration` params → ensemble slice | Caller-supplied iteration bounds cross into the slice loop; extreme/negative values must clamp, not panic or index OOB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | DoS | `init_predict` OOB / overflow on extreme `start_iteration`/`num_iteration` | mitigate | `start = clamp(start_iteration, 0, total)`; `num` derived as `total - start` floor; checked/saturating arithmetic — never index past `trees.len()`, never panic (Task 1 Test 4). |
| T-03-13 | Tampering | sub-range golden provenance | accept | Goldens are the committed, human-approved, byte-idempotent capture from 03-01 (T-03-01 control applies); no new untrusted surface here. |
</threat_model>

<verification>
- `cargo test -p lgbm-model ensemble::` — InitPredict slice math (Task 1).
- `cargo test -p lgbm-model --test predict_subrange` — D-06 layer 5 sub-range parity (Task 2).
- `cargo test -p lgbm-model --test predict_raw_parity --test predict_transform --test predict_leaf_parity` — no regression from the threaded signature.
- `cargo test --workspace` green — full Phase-3 layered battery (1-5).
</verification>

<success_criteria>
- Sub-range prediction returns the C++-matching ensemble slice for representative `(start_iteration, num_iteration)` pairs incl. `-1==all` and non-zero start (PRD-06).
- The `InitPredict` clamp/slice math is a faithful port and never panics/indexes OOB on extreme inputs.
- The full D-06 layered parity battery (layers 1-5) is green with no regression.
</success_criteria>

<output>
Create `.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-04-SUMMARY.md` when done.
</output>
