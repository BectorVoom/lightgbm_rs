---
phase: 03-tree-model-model-text-i-o-predict-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/lgbm-model/Cargo.toml
  - crates/lgbm-model/src/lib.rs
  - crates/lgbm-model/src/error.rs
  - crates/lgbm-model/src/format.rs
  - xtask/Cargo.toml
  - xtask/src/main.rs
  - crates/lgbm-model/tests/fixtures/models/.gitkeep
  - crates/oracle-harness/fixtures/REFERENCE_MANIFEST.md
autonomous: false
requirements: [DAT-08, DAT-09]
user_setup:
  - service: lightgbm (pip)
    why: "Golden-capture path B (RESEARCH Open Q2): train the D-05 model corpus and emit reference .txt models + predict-vector goldens. The full lib_lightgbm is unbuildable here (external_libs/{fmt,fast_double_parser} are empty) and no lightgbm is installed/no CLI — confirmed at plan time. A pip-installed lightgbm ships a prebuilt lib with fmt baked in, so its .txt output IS the authoritative version=v4 format with correct %.17g."
    env_vars: []
    dashboard_config:
      - task: "Approve and run the model-capture path (pip lightgbm install + train, or transcription fallback A); record exact lightgbm version + train params in REFERENCE_MANIFEST.md"
        location: "Task 3 checkpoint:decision in this plan"

must_haves:
  truths:
    - "The crate `lgbm-model` exists in the workspace and `cargo build -p lgbm-model` compiles."
    - "A user can format any f64 with the `%.17g` formatter and the bytes match C++ fmt `{:.17g}` on a battery of doubles (0.1, 1e300, 1e-300, subnormals, integers-as-double, 17-sig-digit cases)."
    - "A user can format an f64 with the `{:g}` (6-sig-digit) formatter matching printf `%g` precision 6."
    - "Running `cargo run -p xtask -- model-capture` produces committed `.txt` model fixtures + predict-vector goldens for all 5 D-05 corpora under `crates/lgbm-model/tests/fixtures/models/`, and re-running it is byte-idempotent (empty `git diff`)."
    - "REFERENCE_MANIFEST.md records the exact lightgbm version + train params + capture-path resolution for the model corpus."
  artifacts:
    - path: "crates/lgbm-model/src/format.rs"
      provides: "%.17g and {:g} printf-faithful float formatters + inline parity test battery"
      contains: "fn format_g17"
    - path: "crates/lgbm-model/src/error.rs"
      provides: "ModelError thiserror enum (MalformedModel, ShapeMismatch)"
      contains: "enum ModelError"
    - path: "crates/lgbm-model/src/lib.rs"
      provides: "crate root with pub mod + re-exports"
      contains: "pub mod format"
    - path: "crates/lgbm-model/Cargo.toml"
      provides: "lgbm-model crate manifest (deps: lgbm-core, lgbm-dataset; dev: oracle-harness)"
      contains: "name = \"lgbm-model\""
    - path: "xtask/src/main.rs"
      provides: "model-capture subcommand emitting committed model + predict goldens"
      contains: "model-capture"
    - path: "crates/lgbm-model/tests/fixtures/models"
      provides: "committed C++ .txt models + raw/transformed/leaf/subrange golden vectors for regression/binary/multiclass/categorical/subrange"
  key_links:
    - from: "xtask/src/main.rs model_capture"
      to: "crates/lgbm-model/tests/fixtures/models/"
      via: "writes committed fixtures from a pip-lightgbm-trained reference (path B) or transcription (path A)"
      pattern: "tests/fixtures/models"
    - from: "root Cargo.toml [workspace].members"
      to: "crates/lgbm-model"
      via: "workspace membership"
      pattern: "crates/lgbm-model"
---

<objective>
Build the Wave-0 enabling slice for Phase 3: the `lgbm-model` crate skeleton, the `%.17g`/`{:g}` float formatter (the byte-exact serialization linchpin, built and verified FIRST per RESEARCH), and the `xtask model-capture` golden-capture pipeline that produces the committed reference `.txt` models + predict-vector goldens that every later parity slice replays.

Purpose: Per RESEARCH (line 519-523) and 03-VALIDATION.md Wave 0, nothing in Phase 3 can be verified until (a) the crate exists, (b) the `%.17g` formatter is proven byte-faithful — it is THE serialization-parity risk (DAT-09) and must be de-risked before the tree writer is written, and (c) the golden corpus exists. The golden-capture path is the FIRST planning gate (RESEARCH Open Q2): the full `lib_lightgbm` is unbuildable here (`external_libs/{fmt,fast_double_parser}` empty — confirmed at plan time) and no `lightgbm` is installed, so a human decision on the capture path (pip `lightgbm` train+dump = path B, recommended; vs verbatim transcription = path A) is required and the produced fixtures need one-time human approval (03-VALIDATION.md Manual-Only Verifications).

Output: `crates/lgbm-model/` (Cargo.toml, lib.rs, error.rs, format.rs); root `Cargo.toml` workspace membership; `xtask model-capture` subcommand; committed fixtures under `crates/lgbm-model/tests/fixtures/models/{regression,binary,multiclass,categorical,subrange}/`; extended REFERENCE_MANIFEST.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-CONTEXT.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-RESEARCH.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-PATTERNS.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-VALIDATION.md

# Workspace analogs (read the ones named in each task's <read_first>)
@Cargo.toml
@crates/lgbm-dataset/Cargo.toml
@crates/lgbm-dataset/src/lib.rs
@crates/lgbm-dataset/src/error.rs
@crates/lgbm-core/src/error.rs
@xtask/src/main.rs
</context>

<artifacts_this_phase_produces>
**This plan (03-01) creates these NEW symbols** (excluded from any cross-AI source-drift verification — they do not exist before this phase):

- Crate `lgbm-model` (new workspace member).
- `crates/lgbm-model/src/format.rs`: `pub fn format_g17(value: f64) -> String` (`%.17g`, printf `%g` precision 17), `pub fn format_g6(value: f64) -> String` (`{:g}`, precision 6), and any private helpers they need. (Exact fn names are Claude's discretion; `format_g17`/`format_g6` are the recommended names.)
- `crates/lgbm-model/src/error.rs`: `pub enum ModelError` with at minimum variants `MalformedModel { detail: String }` and `ShapeMismatch { detail: String }` (thiserror).
- `crates/lgbm-model/src/lib.rs`: crate root, `pub mod {format, error, ...}` + `pub use error::ModelError`.
- `xtask` `model-capture` subcommand: `fn model_capture() -> Result<()>` + `Some("model-capture") => model_capture(),` dispatch arm + `MODEL_*` recorded constants (lightgbm version, train seed/params).
- Committed fixtures: `crates/lgbm-model/tests/fixtures/models/{regression,binary,multiclass,categorical,subrange}/model.txt` + per-corpus golden vector files (`raw.txt`, `transformed.txt`, `leaf.txt`, `subrange.txt` as applicable — exact filenames Claude's discretion, recorded in the manifest).
- New REFERENCE_MANIFEST.md section: "Model / Predict Golden Set" (model corpus provenance + lightgbm version + train params).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the lgbm-model crate + workspace membership + error boundary</name>
  <read_first>
    - root `Cargo.toml` (the `[workspace].members` array — append target)
    - `crates/lgbm-dataset/Cargo.toml` (exact dependency-shape analog; note this crate has NO `src/bin/` so `autobins = false` is NOT needed — 03-PATTERNS.md line 46)
    - `crates/lgbm-dataset/src/lib.rs` (doc-comment + `pub mod` + flat `pub use` re-export idiom)
    - `crates/lgbm-dataset/src/error.rs` (whole file — `#[derive(Debug, Error, Clone, PartialEq, Eq)]` enum + one-variant-per-V5-input-class + `#[cfg(test)] mod tests` Display-assertion idiom)
    - `crates/lgbm-core/src/error.rs` (lines 18-58 — derive header + per-variant doc idiom)
    - 03-RESEARCH.md § Security Domain (lines 535-544 — the exact C++ Log::Fatal sites to mirror as ModelError variants)
  </read_first>
  <action>
    Create `crates/lgbm-model/Cargo.toml` mirroring `lgbm-dataset/Cargo.toml`: `name = "lgbm-model"`, `edition.workspace`/`rust-version.workspace`, `[dependencies] lgbm-core = { path = "../lgbm-core" }`, `lgbm-dataset = { path = "../lgbm-dataset" }`, `thiserror.workspace = true`; `[dev-dependencies] anyhow.workspace = true`, `oracle-harness = { path = "../oracle-harness" }`. Do NOT add `autobins = false` (no `src/bin/` dir here).
    Append `"crates/lgbm-model",` to the root `Cargo.toml` `[workspace].members` array (keep alphabetical-ish ordering consistent with the existing block).
    Create `crates/lgbm-model/src/lib.rs` with a doc-comment naming the faithful-mirror targets (`tree.h`/`tree.cpp`, `gbdt_model_text.cpp`, `gbdt_prediction.cpp`, D-03/D-04) and declaring `pub mod error;` + `pub mod format;` (the only two modules this plan creates; later plans add `tree`, `ensemble`, `model_text`, `predict`). Re-export `pub use error::ModelError;`.
    Create `crates/lgbm-model/src/error.rs` with `pub enum ModelError` deriving `Debug, Error, Clone, PartialEq, Eq` (thiserror is a CLAUDE.md mandate — never hand-roll `impl Error`). Variants (one per V5 input-validation class from RESEARCH Security, lines 535-544): `MalformedModel { detail: String }` (missing required key / parsed-array-length inconsistency / OOB node index — mirrors gbdt_model_text.cpp:494,514 + tree.cpp Log::Fatal), `ShapeMismatch { detail: String }` (predict-input feature count != max_feature_idx+1). Each variant gets a `///` doc citing its C++ source. Copy the `#[cfg(test)] mod tests` Display-assertion idiom verbatim from the dataset analog (construct each variant, assert `!to_string().is_empty()` and that it contains the detail substring).
    Create `crates/lgbm-model/tests/fixtures/models/.gitkeep` (empty) so the fixtures dir is tracked before capture lands.
  </action>
  <verify>
    <automated>cargo build -p lgbm-model && cargo test -p lgbm-model error:: 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p lgbm-model` compiles with zero errors.
    - `crates/lgbm-model` appears in root `Cargo.toml` `[workspace].members` (assert via `grep -q 'crates/lgbm-model' Cargo.toml`).
    - `cargo test -p lgbm-model error::` runs the Display tests and they pass.
    - `ModelError` derives `Clone, PartialEq, Eq` (compile-checked by the inline tests using `assert_eq!`).
    - No `panic!`/`unwrap`/unchecked `[]` introduced in error.rs.
  </acceptance_criteria>
  <done>The crate builds, is a workspace member, and `ModelError` exists with passing Display tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: %.17g / {:g} printf-faithful float formatter (the DAT-09 linchpin) — TDD, built FIRST</name>
  <read_first>
    - 03-RESEARCH.md Pitfall 1 (lines 295-299) + Open Q4 (lines 461-463) — the THREE formatting modes and why `ryu`/`{}`/`{:.17e}` are all wrong
    - 03-RESEARCH.md § Tree::ToString (lines 372-394) — which fields use `{:.17g}` (threshold/leaf_value/leaf_weight) vs `{:g}` (split_gain/internal_value/internal_weight) vs ostream-default (shrinkage)
    - 03-PATTERNS.md "format.rs … NO ANALOG" (lines 290-306) — the inline `#[cfg(test)] mod tests` battery idiom from `comparator.rs:191-226`
    - `LightGBM/include/LightGBM/utils/common.h` `CommonC::ArrayToString` + `__TToStringHelper<T,true,true>` → `fmt::format_to_n(buf, len, "{:.17g}", value)` (lines ~1210-1256) and `__TToStringHelper<T,true,false>` → `{:g}` — the authoritative formatter spec
    - `crates/oracle-harness/src/comparator.rs` (lines 191-226 — the inline-test battery idiom)
  </read_first>
  <behavior>
    Define expected outputs as a test battery BEFORE implementing. `format_g17` must reproduce printf `%.17g` / fmt `{:.17g}` semantics: 17 SIGNIFICANT digits (not fractional), strip trailing zeros, use exponent form only when the decimal exponent `< -4` or `>= 17`, C locale (`.` decimal point, lowercase `e`, exponent sign always present, min 2 exponent digits as fmt emits). `format_g6` is the same with precision 6.
    Test cases (compare against captured C++ output for these exact doubles — capture them in Task 3's pipeline OR transcribe the C++ fmt output for this fixed battery and commit it as a small `format_golden.txt`):
    - Test 1: `0.1` → `0.10000000000000001` (17 sig digits, NOT `0.1`).
    - Test 2: `1.0` → `1` (trailing zeros stripped).
    - Test 3: `1e300` → exponent form (`1e+300`).
    - Test 4: `1e-300` → exponent form (`1e-300`).
    - Test 5: a subnormal (e.g. `5e-324`) → exponent form, no panic.
    - Test 6: an integer-as-double (e.g. `42.0`) → `42`.
    - Test 7: a value needing exactly 17 digits to round-trip (e.g. `0.30000000000000004`).
    - Test 8: `format_g6(123456.789)` → 6-sig-digit `%g` form.
    - Test 9: `0.0`, `-0.0` → `0` / `-0` (match C++).
    - Test 10: round-trip property — `f64::from_str(&format_g17(x)).unwrap() == x` (bit-exact) for every case (Rust parse is correctly-rounded, RESEARCH Pitfall 6).
  </behavior>
  <action>
    Create `crates/lgbm-model/src/format.rs`. Implement `pub fn format_g17(value: f64) -> String` and `pub fn format_g6(value: f64) -> String` reproducing printf `%g` semantics at precision 17 and 6 respectively. Do NOT use `ryu`, `f64::to_string()`, `{}`, or `{:.17e}` — they emit shortest-round-trip or always-exponential, NOT 17-significant-digit `%g`. Hand-roll the `%g` algorithm: compute the decimal exponent, choose fixed vs scientific per the `%g` rule (scientific iff exp `< -4` or `>= precision`), emit `precision` significant digits, then strip trailing zeros (and a trailing `.`), C locale. Match fmt's exponent formatting (lowercase `e`, explicit sign, min 2 digits). Handle `0.0`/`-0.0`, NaN/inf defensively (they will not appear in valid model floats but must not panic). Add a `shrinkage`-mode note in the module doc: `shrinkage` uses C++ ostream-default (`std::defaultfloat`, precision 6) which is `{:g}`-equivalent for typical values — `format_g6` covers it pending the byte-exact golden as arbiter (RESEARCH Open Q4). Add the `#[cfg(test)] mod tests` battery from `<behavior>`, asserting against a committed `format_golden.txt` (Task 3 emits it, or transcribe the fmt output for this fixed battery). No fenced code in production — directive only.
  </action>
  <verify>
    <automated>cargo test -p lgbm-model format::</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-model format::` passes ALL battery cases including the 10 listed.
    - `format_g17(0.1) == "0.10000000000000001"` (asserted) — NOT `"0.1"`.
    - The round-trip property holds bit-exact for every battery value (`f64::from_str(&format_g17(x)) == Ok(x)`).
    - No use of `ryu` / `to_string()` / `{:.17e}` for the numeric formatting (assert via `grep -L 'ryu\|to_string()\|:.17e' crates/lgbm-model/src/format.rs` or code review).
    - No panic on subnormal / 1e300 / 1e-300 / 0.0 / -0.0.
  </acceptance_criteria>
  <done>Both formatters reproduce C++ `%g` byte-for-byte on the battery and round-trip bit-exact; the DAT-09 linchpin is de-risked before any tree writer exists.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 3: Golden-capture path decision (RESEARCH Open Q2 — FIRST planning gate)</name>
  <action>Pause for the user to choose the golden-capture path before Task 4 runs. Present the decision/options/resume-signal below. This is a blocking-human checkpoint (never auto-approvable): the chosen path determines how Task 4 produces the committed fixtures, and the produced fixtures require one-time human approval that they are numerically identical to lib_lightgbm.</action>
  <verify><human-check>User has selected path-b-pip or path-a-transcribe and (if pip) confirmed the install is permitted and approved recording the lightgbm version in REFERENCE_MANIFEST.md.</human-check></verify>
  <done>The capture path is selected and approved; Task 4 can proceed with the chosen path.</done>
  <decision>Golden-capture path for the Phase-3 model corpus (RESEARCH Open Q2 — the FIRST planning gate)</decision>
  <context>
    The committed reference `.txt` models + predict-vector goldens (D-05/D-07) require a C++-trained LightGBM model as input. VERIFIED at plan time: the full `lib_lightgbm` is UNBUILDABLE here (`LightGBM/external_libs/fmt/include/fmt/format.h` does not exist — empty submodules), and no `lightgbm` is installed (`python3 -c "import lightgbm"` fails) and there is no `lightgbm` CLI. This is the same blocker Phases 1 (RNG) and 2 (binning) hit. The `LightGBM/` tree is git-UNTRACKED and may NEVER be a runtime/test dependency (project memory). Whichever path is chosen, the produced fixtures need one-time human approval that they are numerically identical to `lib_lightgbm` (03-VALIDATION.md Manual-Only Verifications), and the exact tool version + train params get recorded in REFERENCE_MANIFEST.md.
  </context>
  <options>
    <option id="path-b-pip">
      <name>Path B (RESEARCH-recommended): pip-install lightgbm, train the D-05 corpus, dump .txt + predict vectors, commit</name>
      <pros>pip `lightgbm` ships a prebuilt `lib_lightgbm` with `fmt` baked in, so its `.txt` IS the authoritative `version=v4` format with correct `%.17g`. Lowest fidelity risk. Produces real trained models exercising every Phase-3 path. No C++ build needed.</pros>
      <cons>Requires a network `pip install lightgbm` in the dev/capture environment. Pins an external tool version (recorded in the manifest). Capture step is not reproducible without that pip wheel (but fixtures are committed, so test time needs nothing).</cons>
    </option>
    <option id="path-a-transcribe">
      <name>Path A (fallback): verbatim transcription of SaveModelToString + a minimal train-from-fixed-grad stub</name>
      <pros>No external install; consistent with the Phase 1/2 header-only transcription precedent.</pros>
      <cons>Phase 3 has NO Rust trainer yet (Phase 5/6) and cannot build the C++ trainer (empty external_libs) — so a model `.txt` must still come from somewhere. Significantly more work; would require transcribing both the writer AND a tiny GBDT trainer. RESEARCH flags this as the heavier path.</cons>
    </option>
  </options>
  <resume-signal>Select: path-b-pip or path-a-transcribe. If path-b-pip, confirm `pip install lightgbm` is permitted in this environment and approve recording its version in REFERENCE_MANIFEST.md.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: xtask model-capture subcommand + committed model/predict golden corpus + manifest</name>
  <read_first>
    - `xtask/src/main.rs` `bin_capture()` (lines 186-318 — the verbatim subcommand-body analog: `workspace_root()` + `verify_toolchain()` → resolve fixture dir under the TRACKED crate → configure/build/run capture → assert each output written → `write_manifest()` refresh → idempotency reminder)
    - `xtask/src/main.rs` dispatch `match` (lines 53-63) + recorded constants (`MASTER_SEED`/`BIN_MASTER_SEED`/`LIGHTGBM_VERSION`/`LIGHTGBM_COMMIT`, lines 31-49)
    - `xtask/src/main.rs` `write_manifest()` (lines 386-560 — the `format!` manifest sections "Numeric Binning Golden Set"/"EFB Grouping Golden Set" to mirror)
    - `crates/lgbm-dataset/tests/fixtures/examples/` (the COPIED Phase-2 input matrices reused as prediction inputs — D-05/D-07)
    - 03-RESEARCH.md Open Q2 (lines 448-454) + § Tree::ToString / SaveModelToString section orders (lines 372-422) — what the captured `.txt` must contain
    - 03-PATTERNS.md `model-capture` subcommand (lines 355-385) + manifest-extend (lines 399-413)
  </read_first>
  <action>
    Implement the capture path chosen in Task 3 (default: path-b-pip).
    Add `fn model_capture() -> Result<()>` to `xtask/src/main.rs` mirroring `bin_capture()`'s body shape: `workspace_root()`, resolve `crates/lgbm-model/tests/fixtures/models/` (TRACKED crate dir — NEVER reference the untracked `LightGBM/` tree), `create_dir_all` the 5 corpus subdirs (`regression`, `binary`, `multiclass`, `categorical`, `subrange`).
    For path-b-pip: shell out to a documented one-off training step (pip `lightgbm`) that, with `deterministic=true force_row_wise=true num_threads=1` + a fixed `MODEL_TRAIN_SEED` constant (add it to the recorded-constants block alongside the existing seeds), trains each D-05 corpus on the reused Phase-2 input matrices (regression + binary + multiclass + a categorical-feature dataset), saves each `model.txt` via `Booster.save_model()`, and emits per-corpus predict-vector goldens: raw scores (PRD-01), transformed output (PRD-02 — sigmoid for binary, softmax for multiclass, identity for regression), leaf indices (PRD-03), and sub-range raw scores for representative `(start_iteration, num_iteration)` slices incl. `-1==all` (PRD-06). Write golden vectors in the `;`-separated / per-line format the test loaders parse (mirror `tests/golden/mod.rs` `parse_*_list` idiom). COPY/commit all outputs into the corpus subdirs.
    Also emit the small `format_golden.txt` battery for Task 2 (the fixed double battery formatted by C++ fmt `{:.17g}`/`{:g}`) so the formatter test asserts against a captured reference, not a hand-guess.
    Add `Some("model-capture") => model_capture(),` to the dispatch `match`; update the usage/`bail!` strings to list `model-capture`.
    Add `MODEL_TRAIN_SEED` (+ a `MODEL_LIGHTGBM_VERSION` constant capturing the pip version per Task 3) to the recorded-constants block.
    Extend `write_manifest()`'s `format!` with a new "Model / Predict Golden Set" section documenting the D-05 corpus, the exact `lightgbm` version + train params + deterministic flags, and the capture-path resolution note (path B pip vs path A transcription). Do NOT hand-edit the `.md` — `write_manifest()` owns it.
    If `xtask` needs new deps for the capture (e.g. invoking python), add them to `xtask/Cargo.toml`; prefer shelling out via `std::process::Command` (no new crate) to keep the dependency surface minimal.
  </action>
  <verify>
    <automated>cargo run -p xtask -- model-capture && git status --porcelain crates/lgbm-model/tests/fixtures/models crates/oracle-harness/fixtures/REFERENCE_MANIFEST.md | head && cargo run -p xtask -- model-capture && git diff --stat crates/lgbm-model/tests/fixtures/models crates/oracle-harness/fixtures/REFERENCE_MANIFEST.md</automated>
  </verify>
  <acceptance_criteria>
    - `cargo run -p xtask -- model-capture` exits 0 and writes a `model.txt` + the required golden vector files into each of the 5 corpus subdirs under `crates/lgbm-model/tests/fixtures/models/`.
    - Each emitted `model.txt` begins with `tree\nversion=v4\n` (assert by reading the first two lines — confirms the authoritative v4 envelope, RESEARCH line 398-399).
    - Re-running `cargo run -p xtask -- model-capture` leaves `git diff --stat` EMPTY for the fixtures dir + manifest (byte-idempotent capture — Phase 1/2 discipline).
    - `format_golden.txt` exists and Task 2's `cargo test -p lgbm-model format::` passes against it.
    - REFERENCE_MANIFEST.md contains a "Model / Predict Golden Set" section naming the lightgbm version + train params (assert via `grep -q "Model / Predict Golden Set" crates/oracle-harness/fixtures/REFERENCE_MANIFEST.md`).
    - No fixture path references the untracked `LightGBM/` tree (assert via `grep -rL "LightGBM/" crates/lgbm-model/tests/fixtures` — none should contain it).
  </acceptance_criteria>
  <done>The 5-corpus committed golden set + format battery exist, the capture is byte-idempotent, and the manifest records provenance — every later parity slice can now replay with no toolchain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture-tool → committed fixtures | The pip `lightgbm` (or transcription) output crosses into committed test fixtures; trusted-at-capture, but the human-approval checkpoint (Task 3) + manifest provenance + byte-idempotency are the integrity controls. |
| (deferred to 03-02+) model-file bytes → parser | The model-text parser is built in 03-02; the parsing-attack surface (OOB, overflow) is mitigated there. This plan only emits fixtures + the formatter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | committed model/format fixtures (capture provenance) | mitigate | Byte-idempotent capture (empty `git diff` on re-run) + REFERENCE_MANIFEST.md records exact lightgbm version/params + Task-3 human approval that fixtures are numerically identical to `lib_lightgbm`. |
| T-03-02 | Information Disclosure | format.rs on NaN/inf/subnormal | accept | Formatter handles 0.0/-0.0/subnormal/1e±300 defensively (no panic); NaN/inf cannot appear in valid model floats. Low risk, no PII. |
| T-03-SC | Tampering | pip `lightgbm` install (path B) | mitigate | `lightgbm` is the canonical PyPI package (verify at `pypi.org/project/lightgbm`); install is at CAPTURE time only, never at test time; version pinned in the manifest; gated behind the Task-3 blocking-human checkpoint. NOT a code dependency of the shipped crate. |
</threat_model>

<verification>
- `cargo build -p lgbm-model` compiles (Task 1).
- `cargo test -p lgbm-model format::` passes the full `%g` battery incl. round-trip (Task 2).
- `cargo run -p xtask -- model-capture` is byte-idempotent and produces all 5 corpora + format battery + manifest section (Task 4).
- `cargo test --workspace` green (no regression in core/dataset/oracle).
</verification>

<success_criteria>
- The `lgbm-model` crate exists, builds, and is a workspace member with a `thiserror` `ModelError`.
- The `%.17g`/`{:g}` formatter is proven byte-faithful to C++ fmt on the documented battery and round-trips bit-exact — the DAT-09 linchpin is de-risked.
- The committed 5-corpus golden set + format battery exist under the tracked fixtures dir, the capture is byte-idempotent, and provenance is recorded in REFERENCE_MANIFEST.md.
</success_criteria>

<output>
Create `.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-01-SUMMARY.md` when done.
</output>
