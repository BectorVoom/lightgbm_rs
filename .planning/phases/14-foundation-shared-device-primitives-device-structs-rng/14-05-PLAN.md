---
phase: 14-foundation-shared-device-primitives-device-structs-rng
plan: 05
type: execute
wave: 3
depends_on: ["14-03"]
files_modified:
  - crates/lgbm-compute/src/kernels/primitives.rs
  - crates/lgbm-compute/tests/primitives_self.rs
autonomous: true
requirements: [ODL-01]
must_haves:
  truths:
    - "Weighted and unweighted percentile exist as anchor-pinned skeletons (correct + tested), composed from the full-depth argsort + prefix-sum"
    - "Multi-block / global bitonic argsort exists as an anchor-pinned skeleton with a permutation matching the C++ reference"
    - "Per-segment ranking argsort (BitonicArgSortItems analog) exists as an anchor-pinned skeleton"
    - "ODL-01's 'all primitives exist' is satisfied; depth-hardening is explicitly deferred to the Phase 19 / 22 consumers (D-02)"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/primitives.rs"
      provides: "percentile (wtd/unwtd), multi-block argsort, items-sort skeletons"
      min_lines: 120
    - path: "crates/lgbm-compute/tests/primitives_self.rs"
      provides: "skeleton correctness tests (anchored, small inputs)"
  key_links:
    - from: "percentile skeleton"
      to: "full-depth argsort + prefix-sum (14-03)"
      via: "composition (sort values, weighted prefix-sum threshold)"
      pattern: "argsort|prefix"
    - from: "multi-block argsort skeleton"
      to: "single-block argsort (14-03)"
      via: "3-phase global wrapper structure"
      pattern: "argsort"
---

<objective>
Port the remaining ODL-01 primitives — weighted/unweighted percentile, multi-block / global argsort, and the per-query ranking item-sort (`BitonicArgSortItems` analog) — as anchor-pinned SKELETONS per D-02: correct and tested now, but finalized/depth-hardened by their first real consumer (percentile + items → Phase 19 objectives/ranking; multi-block argsort → Phase 19/22). This satisfies ODL-01's literal "all primitives exist" without front-loading YAGNI depth onto a foundation phase.

Purpose: Downstream subsystems need these primitives to EXIST with correct signatures + behavior so later phases extend rather than invent. They compose the full-depth primitives from 14-03.

Output: The three skeletons added to `primitives.rs` + small anchored correctness tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-CONTEXT.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-RESEARCH.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-PATTERNS.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Weighted + unweighted percentile skeleton</name>
  <files>crates/lgbm-compute/src/kernels/primitives.rs, crates/lgbm-compute/tests/primitives_self.rs</files>
  <read_first>
    - 14-03-SUMMARY.md (the argsort tie convention + prefix-sum entry points to compose)
    - LightGBM-release-4.6.0.99/src/cuda/cuda_algorithms.cu (PercentileDevice — weighted + unweighted reference semantics)
    - 14-RESEARCH.md D-02 (skeleton scope) + Pitfall 4 (percentile is a COLD Phase-19 consumer path; f64 is acceptable here, unlike the grow-loop hot primitives)
  </read_first>
  <action>
    Implement weighted and unweighted percentile by composing the 14-03 full-depth argsort (sort values) with a (weighted) prefix-sum threshold scan, mirroring the C++ `PercentileDevice` semantics. Because this is a cold Phase-19 consumer path (Pitfall 4), f64 device math is acceptable here. Mark the function clearly as a SKELETON in its doc — correct + tested now, to be finalized/hardened by the Phase-19 objectives consumer (D-02). Do NOT optimize for depth/perf. Anchor a small correctness test to a serial Rust percentile reference (cpu f64 anchor, never GPU-vs-GPU): e.g. unweighted median of a small array and a weighted percentile with non-uniform weights; assert bit-exact vs the serial ref for exact inputs, or a documented f64 ULP band where the reduction order cannot be matched (carry the 14-03 Open-Q2 policy).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test primitives_self percentile</automated>
  </verify>
  <acceptance_criteria>
    - Both weighted and unweighted variants present and correct on small inputs.
    - Documented as a skeleton with the Phase-19 hardening owner named.
    - Anchored to a serial f64 reference (no GPU-vs-GPU).
  </acceptance_criteria>
  <done>Weighted/unweighted percentile skeletons exist, composed from the full-depth primitives and anchor-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Multi-block / global argsort skeleton</name>
  <files>crates/lgbm-compute/src/kernels/primitives.rs, crates/lgbm-compute/tests/primitives_self.rs</files>
  <read_first>
    - 14-03-SUMMARY.md (single-block argsort comparator/tie convention)
    - LightGBM-release-4.6.0.99/src/cuda/cuda_algorithms.cu (BitonicArgSortGlobal / GlobalInclusiveArgPrefixSum — the multi-block structure)
    - 14-RESEARCH.md Pattern 3 (3-phase global structure, no cross-cube barrier) + D-02 (skeleton; multi-block hardening → Phase 19/22)
  </read_first>
  <action>
    Implement an index-only multi-block / global bitonic argsort skeleton extending the single-block sort from 14-03, following the global 3-phase wrapper structure (per-block sort → merge across blocks) with no cross-cube barrier assumption. Cap the input size to what the local APU exercises this phase (assume the small-input regime; note the large-input / recursive merge as the Phase-19/22 hardening task per D-02). Keep it index-only (values unmoved), comparator/tie order matching the C++ reference. Document it as a skeleton. Test the permutation BIT-EXACT vs a serial reference on an input spanning more than one block, including a tie-rich case.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test primitives_self argsort_global</automated>
  </verify>
  <acceptance_criteria>
    - Sorts an input larger than one single-block tile, index-only, permutation bit-exact vs serial ref.
    - No cross-cube barrier assumption; documented as skeleton with the hardening owner named.
  </acceptance_criteria>
  <done>A multi-block/global argsort skeleton exists with a C++-matched permutation on multi-block inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Per-segment ranking item-sort skeleton (BitonicArgSortItems analog)</name>
  <files>crates/lgbm-compute/src/kernels/primitives.rs, crates/lgbm-compute/tests/primitives_self.rs</files>
  <read_first>
    - 14-03-SUMMARY.md (argsort convention to reuse)
    - LightGBM-release-4.6.0.99/src/cuda/cuda_algorithms.cu (BitonicArgSortItems — per-query/segment sort)
    - 14-RESEARCH.md D-02 (items-sort skeleton → Phase 19 ranking consumer)
  </read_first>
  <action>
    Implement a per-segment (per-query) index-only argsort skeleton: given segment boundaries, sort the indices WITHIN each segment by key, never moving values, reusing the single-block comparator/tie convention from 14-03. This is the ranking-objective primitive consumed by Phase 19; document it as a skeleton with that hardening owner. Test on a small multi-segment input (two or three segments of differing lengths) and assert each segment's permutation bit-exact vs a serial per-segment reference, including a tie-rich segment.

    NOTE — C++ items-sort golden INTENTIONALLY DEFERRED this phase (do NOT add a new fixture): the C++ `BitonicArgSortItems` / `…Global` items-sort fixture is NOT captured in 14-02 and is not required here. The comparator/tie convention this skeleton depends on is ALREADY locked against C++ via the tie-rich single-block argsort fixture captured in 14-02 and asserted bit-exact in 14-03; this per-segment skeleton merely reuses that locked convention segment-by-segment. The skeleton is therefore anchored to the serial Rust per-segment permutation reference + that already-locked convention, NOT to a dedicated C++ items-sort golden. Capturing a dedicated items-sort fixture is the Phase-19 ranking consumer's hardening task — adding it now would expand 14-02/14-06 scope for no Phase-14 correctness gain.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test primitives_self items_sort</automated>
  </verify>
  <acceptance_criteria>
    - Sorts within each segment independently, index-only, permutation bit-exact vs serial ref.
    - Documented as a skeleton with the Phase-19 ranking consumer named.
  </acceptance_criteria>
  <done>A per-segment ranking item-sort skeleton exists and is anchor-tested on multi-segment input.</done>
</task>

</tasks>

<artifacts_produced>
New symbols in `crates/lgbm-compute/src/kernels/primitives.rs`: the weighted/unweighted percentile skeleton fn(s) + launcher, the multi-block/global argsort skeleton + launcher, the per-segment items-sort skeleton + launcher.
Extended `crates/lgbm-compute/tests/primitives_self.rs`: percentile / argsort_global / items_sort `#[test]` fns.
Note for plan-review source-grounding: all the above are NEW skeletons; the AMD-fork `cuda_algorithms.cu` is read-only reference.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launcher → device kernel | Host-controlled slices + segment boundaries; sizes host-known. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05-01 | Tampering / DoS | out-of-bounds on skeleton launches | mitigate | Skeletons use checked `::launch` (perf irrelevant per RESEARCH Pattern 6); V5 length/segment validation returns `ComputeError` before launch. |
| T-14-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (cubecl 0.10 vendored — Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --test primitives_self` (percentile, argsort_global, items_sort) green on the cpu anchor.
- `cargo build -p lgbm-compute` clean.
</verification>

<success_criteria>
ODL-01's "all primitives exist" is satisfied: percentile (wtd/unwtd), multi-block argsort, and per-segment items-sort exist as correct, anchor-tested skeletons with depth-hardening explicitly deferred to their Phase 19/22 consumers (D-02).
</success_criteria>

<output>
Create `.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-05-SUMMARY.md` when done, naming each skeleton's hardening owner.
</output>
