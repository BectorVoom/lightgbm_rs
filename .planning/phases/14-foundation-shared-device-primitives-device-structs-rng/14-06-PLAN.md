---
phase: 14-foundation-shared-device-primitives-device-structs-rng
plan: 06
type: execute
wave: 4
depends_on: ["14-02", "14-03", "14-04", "14-05"]
files_modified:
  - crates/oracle-harness/tests/primitive_parity.rs
  - crates/oracle-harness/tests/learner_parity.rs
  - crates/lgbm-compute/src/lib.rs
  - crates/lgbm-treelearner/src/learner.rs
autonomous: true
requirements: [ODL-01, ODL-02]
must_haves:
  truths:
    - "The Rust device primitives are cross-validated against the committed C++ goldens: int/permutation bit-exact, f64 bit-exact/ULP per the recorded policy, f32 ROCm ~1e-6"
    - "The on-device growth seam stays a strict no-op: on_device_growth_supported() == false, grow_tree_on_device() == Ok(None); both Slice-0 no-op tests remain green"
    - "The anchor-pinned tie-aware oracle is extended (never GPU-vs-GPU) and references the new foundation primitives/structs/RNG that Phase 21 will consume"
    - "LGBM_CUDA_ON_DEVICE is OFF by default; CPU / ROCm / existing-host-CUDA paths are byte-unchanged; the full workspace merge gate is green"
  artifacts:
    - path: "crates/oracle-harness/tests/primitive_parity.rs"
      provides: "fixture-replay parity tests (skip-if-absent) for prefix-sum/reductions/argsort/percentile vs C++ goldens"
      min_lines: 80
  key_links:
    - from: "crates/oracle-harness/tests/primitive_parity.rs"
      to: "crates/oracle-harness/fixtures/primitives (14-02)"
      via: "fixture loader + replay vs the Rust device primitives (14-03/14-05)"
      pattern: "fixtures/primitives"
    - from: "crates/oracle-harness/tests/learner_parity.rs"
      to: "cpu f64 anchor"
      via: "assert_on_device_tree_matches_cpu_anchor (tie-aware, never GPU-vs-GPU)"
      pattern: "assert_on_device_tree_matches_cpu_anchor"
---

<objective>
Close the phase: cross-validate the Rust device primitives against the committed C++ goldens (D-03/D-10), re-establish/extend the anchor-pinned tie-aware on-device seam + oracle WITHOUT flipping any discriminator (D-09), and prove the full merge gate is green with `LGBM_CUDA_ON_DEVICE` OFF and every existing path byte-unchanged (D-11).

Purpose: This is the integration + verification plan. It satisfies ROADMAP success criteria 3 (seam + oracle re-established/extended, never GPU-vs-GPU) and 4 (env OFF by default, byte-unchanged, merge gate green), and provides the numeric anchor proof for criterion 1.

Output: `primitive_parity.rs`, an extended oracle/seam doc-test surface, and a green `cargo test --workspace`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-CONTEXT.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-RESEARCH.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-PATTERNS.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-03-SUMMARY.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the no-op seam + anchor-pinned oracle (D-09 / D-10)</name>
  <files>crates/oracle-harness/tests/learner_parity.rs, crates/lgbm-compute/src/lib.rs, crates/lgbm-treelearner/src/learner.rs</files>
  <read_first>
    - crates/oracle-harness/tests/learner_parity.rs lines 2166-2222 (assert_on_device_tree_matches_cpu_anchor) + 2422 + 2452 (the two Slice-0 no-op seam tests that MUST stay green)
    - crates/lgbm-compute/src/lib.rs lines 1239 (on_device_growth_supported), 1262-1267 (the cubecl-0.10 gotcha checklist), 1272 (grow_tree_on_device trait default), 2207 (GpuBackend<R> override)
    - crates/lgbm-treelearner/src/learner.rs lines 443-444 (cuda_on_device_env), 488 + 705 (on_device_eligible wiring)
    - 14-PATTERNS.md "Seam Extension Scope" table (frozen invariants) + RESEARCH Pitfall 6
  </read_first>
  <action>
    Treat the seam discriminator as FROZEN (D-09): `on_device_growth_supported()` stays `false`, `grow_tree_on_device(..)` returns `Ok(None)`, no histogram/split/growth kernel is wired. Do NOT flip the discriminator or make the seam return `Some` (Pitfall 6 — that activates an unfinished path and fails the merge gate). EXTEND only the doc/test surface: add doc notes on `lib.rs` (near the seam) and `learner.rs` (near the env gate) pointing at the new foundation modules (`kernels::primitives`, `kernels::split_info`, `kernels::random`) as what Phase 21 will consume, and confirming `LGBM_CUDA_ON_DEVICE` remains an opt-in `"1"` AND-gate that leaves the host path byte-unchanged when unset. Extend `assert_on_device_tree_matches_cpu_anchor` (or its doc) to make explicit the anchor discipline: pin to the cubecl-cpu f64 fold, tie-aware `default_left`, NEVER compare two GPU f32 paths to each other (def-f8u-01, D-10). Keep both Slice-0 no-op tests (`..._noop_slice0`, `..._oracle_host_fallback_slice0`) passing unchanged. Make NO behavioral change to the discriminator, the env gate, or `LeafPartitionLayout`.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test learner_parity</automated>
  </verify>
  <acceptance_criteria>
    - `on_device_growth_supported()` is still `false` and `grow_tree_on_device()` still returns `Ok(None)` (grep/test confirmed).
    - Both Slice-0 no-op seam tests pass.
    - The oracle's anchor discipline (cpu f64, tie-aware, never GPU-vs-GPU) is explicit; doc notes reference the new foundation modules.
  </acceptance_criteria>
  <done>The seam is re-established/extended as a strict no-op with an explicit anchor-pinned tie-aware oracle; nothing behavioral changed.</done>
</task>

<task type="auto">
  <name>Task 2: Primitive fixture-parity replay vs C++ goldens (D-03 / D-10)</name>
  <files>crates/oracle-harness/tests/primitive_parity.rs</files>
  <read_first>
    - crates/oracle-harness/tests/rng_parity.rs lines 22-49 (fixture path + parse helpers), 53-61 (skip-if-fixture-absent so cargo test stays green pre-capture), 91-123 (bit / to_bits / ULP assertion discipline)
    - crates/oracle-harness/fixtures/primitives/* (the committed goldens from 14-02 — formats)
    - 14-03-SUMMARY.md (the per-reduction f64-order policy + argsort tie convention) and 14-05-SUMMARY.md (skeleton hardening owners)
    - the new primitive launchers in crates/lgbm-compute/src/kernels/primitives.rs (14-03/14-05)
    - 14-RESEARCH.md Validation Architecture (Req→Test map) + Pitfall 3 (f32 cross-backend tolerance)
  </read_first>
  <action>
    Author `primitive_parity.rs` mirroring `rng_parity.rs`: load each committed C++ golden from `crates/oracle-harness/fixtures/primitives/`, SKIP-if-absent (so `cargo test` stays green before capture), and replay the Rust device primitives (prefix-sum incl/excl, reductions sum/max/min/dotprod, single+multi-block argsort, weighted/unweighted percentile) over the same inputs, asserting per the recorded policy: integer prefix-sum + argsort permutation → BIT-EXACT (`assert_eq!`, including the tie-rich argsort case); f64 reductions/percentile → bit-exact where the reduction order matches the 14-03 policy, else the documented ULP band; f32 outputs on the ROCm leg (under `--features rocm`) → ~1e-6. Anchor every assertion to the cpu f64 fold / C++ fixture; NEVER compare two GPU f32 paths to each other (def-f8u-01, D-10). Use the same fixture-path + `;`-list + `key=value` parse helpers as `rng_parity.rs`.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test primitive_parity</automated>
  </verify>
  <acceptance_criteria>
    - Every numeric primitive has a fixture-replay assertion with the correct tolerance class (bit-exact int/perm, f64 policy, f32 ~1e-6).
    - The tie-rich argsort fixture asserts permutation bit-exact.
    - The test skips cleanly (green) when a fixture is absent and validates every committed case when present.
    - No GPU-vs-GPU comparison anywhere.
  </acceptance_criteria>
  <done>The Rust device primitives are cross-validated against the committed C++ goldens, closing the numeric anchor for ODL-01.</done>
</task>

<task type="auto">
  <name>Task 3: Full merge-gate verification (D-11)</name>
  <files>crates/oracle-harness/tests/primitive_parity.rs</files>
  <read_first>
    - 14-RESEARCH.md Validation Architecture "Full suite command" + the D-11 merge-gate list
    - 14-VALIDATION.md Sampling Rate (phase gate = cargo test --workspace green)
  </read_first>
  <action>
    Verify the phase merge gate with `LGBM_CUDA_ON_DEVICE` UNSET (OFF by default): run the full workspace test suite and confirm the existing gates are green and byte-unchanged — `raw_bin_train_matches_cpp_golden`, `learner_parity` (incl. both Slice-0 no-op tests), and the lgbm / treelearner / compute suites. Confirm the additive primitives/structs/RNG introduced no regression on the default CPU path and no behavioral change to the ROCm / existing-host-CUDA paths. (No source edits expected in this task beyond any final test tidy in `primitive_parity.rs`; if a regression surfaces it is fixed in the owning plan's file, not here.) Record the observed full-suite runtime in the SUMMARY to backfill 14-VALIDATION's TBD latency fields.
  </action>
  <verify>
    <automated>cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --workspace` is green with `LGBM_CUDA_ON_DEVICE` unset.
    - `raw_bin_train_matches_cpp_golden`, `learner_parity`, and the lgbm/treelearner/compute suites pass unchanged.
    - The SUMMARY records the full-suite runtime (fills the 14-VALIDATION TBD).
  </acceptance_criteria>
  <done>The full merge gate is green with the env flag off and all existing paths byte-unchanged (D-11 satisfied).</done>
</task>

</tasks>

<artifacts_produced>
New file: `crates/oracle-harness/tests/primitive_parity.rs` (fixture loader + per-primitive replay `#[test]` fns).
Extended: `crates/oracle-harness/tests/learner_parity.rs` (oracle doc/anchor-discipline extension — no behavioral change), `crates/lgbm-compute/src/lib.rs` + `crates/lgbm-treelearner/src/learner.rs` (doc-only notes referencing the new foundation modules; discriminator/env-gate FROZEN).
Note for plan-review source-grounding: `primitive_parity.rs` is NEW; the seam symbols already exist (frozen, extend-only).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed fixtures → test replay | Test-only; fixtures are committed text, replayed against host-controlled inputs. |
| env var LGBM_CUDA_ON_DEVICE → seam | Read-only opt-in flag; OFF by default leaves every existing path unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06-01 | Tampering | accidental discriminator flip activating an unfinished on-device path | mitigate | Seam frozen (D-09); both Slice-0 no-op tests + the full merge gate guard against any flip (Pitfall 6). |
| T-14-06-02 | Spoofing | fixture replay masking a real divergence | mitigate | Skip-if-absent is explicit + logged; committed fixtures are byte-idempotent (14-02); int/perm asserted bit-exact, no silent tolerance widening. |
| T-14-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (cubecl 0.10 vendored — Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test -p oracle-harness --test learner_parity` green (Slice-0 no-op tests intact).
- `cargo test -p oracle-harness --test primitive_parity` green (fixture replay; skips cleanly if absent).
- `cargo test --workspace` green with `LGBM_CUDA_ON_DEVICE` unset.
- (Optional, ROCm) `cargo test -p oracle-harness --features rocm` for the hip f32 ~1e-6 leg.
</verification>

<success_criteria>
The seam is a strict no-op with an explicit anchor-pinned tie-aware oracle; the Rust device primitives are cross-validated against the committed C++ goldens; and the full merge gate is green with `LGBM_CUDA_ON_DEVICE` off and all existing paths byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-06-SUMMARY.md` when done, recording the full-suite runtime + any tolerance-band decisions.
</output>
