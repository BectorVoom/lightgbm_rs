---
phase: 14-foundation-shared-device-primitives-device-structs-rng
plan: 04
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/lgbm-compute/src/kernels/split_info.rs
  - crates/lgbm-compute/src/kernels/random.rs
  - crates/lgbm-compute/tests/split_info.rs
  - crates/lgbm-compute/tests/cuda_random_parity.rs
autonomous: true
requirements: [ODL-02]
must_haves:
  truths:
    - "A CubeCL-safe SoA device split-record exists: one pre-allocated buffer per field sized [num_leaf_slots], allocated once in a constructor — NO per-split device allocation anywhere"
    - "Numeric fields (D-06) are allocated; categorical buffers are pre-allocated RESERVED so Phase 22 fills, not restructures them"
    - "A deep-copy slot a -> slot b operator (host-side this phase) copies every field buffer exactly"
    - "A CUDARandom #[cube] LCG produces a stream bit-identical to the host Random: RandInt16/RandInt32 i32-exact, NextFloat to_bits()-exact"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/split_info.rs"
      provides: "DeviceSplitInfo<R> SoA record + new(client, num_leaf_slots) + host slot-copy"
      min_lines: 120
    - path: "crates/lgbm-compute/src/kernels/random.rs"
      provides: "CUDARandom #[cube] LCG (advance/RandInt16/RandInt32/NextFloat) + host parity launcher"
      min_lines: 60
    - path: "crates/lgbm-compute/tests/cuda_random_parity.rs"
      provides: "device-stream vs host Random bit-exact parity test (D-04)"
      min_lines: 40
  key_links:
    - from: "crates/lgbm-compute/src/kernels/split_info.rs"
      to: "client.empty per field, once"
      via: "constructor allocates [num_leaf_slots] per numeric field + reserved cat slabs"
      pattern: "client.empty"
    - from: "crates/lgbm-compute/src/kernels/random.rs"
      to: "crates/lgbm-core/src/random.rs (host Random)"
      via: "device LCG x = x*214013 + 2531011 pinned bit-for-bit"
      pattern: "214013"
    - from: "crates/lgbm-compute/tests/cuda_random_parity.rs"
      to: "lgbm_core::random::Random"
      via: "draw-sequence comparison (to_bits / i32 equality)"
      pattern: "to_bits|Random::new"
---

<objective>
Deliver ODL-02: the CubeCL-safe pre-allocated device split-record (a `CUDASplitInfo` analog, SoA, NO per-split device alloc) and the `CUDARandom` LCG whose stream is bit-identical to the host `Random`. Both are foundation structures every later on-device subsystem consumes; neither wires on-device growth (D-09 stays no-op).

Purpose: §15 of the design doc — the resident split-record the future §8 argmax reductions copy between leaf slots, and the per-task seeded device RNG for extra-trees threshold selection / sampling parity. The split-record's whole-record deep-copy `operator=` is the leaf-slot copy; the RNG is pinned to the already-C++-bit-exact host `Random` (D-04 — no new C++ capture).

Output: `split_info.rs`, `random.rs`, and their parity/structural tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-CONTEXT.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-RESEARCH.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SoA device split-record (pre-allocated, numeric + reserved categorical)</name>
  <files>crates/lgbm-compute/src/kernels/split_info.rs, crates/lgbm-compute/tests/split_info.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/split.rs lines 797-836 (the `client.empty` "allocate only when the kernel writes every cell" pre-alloc idiom + the launch wrapper)
    - crates/lgbm-compute/src/kernels/histogram.rs lines 161-167 (the load-bearing empty-vs-zeroed contrast comment), 613-625 (the comptime-sized const convention to mirror for MAX_CAT_PER_SPLIT)
    - LightGBM-release-4.6.0.99/src/cuda/cuda_split_info.hpp (the CUDASplitInfo field list + deep-copy operator= semantics — the port reference)
    - /home/user/Documents/workspace/cubecl_manual/manual/cubecl/13_memory_preallocation.md (client.empty / empty_tensor once, off the hot loop, reused)
    - 14-RESEARCH.md Pattern 5 + D-05/D-06/D-07/D-08 + Open Question 3 (MAX_CAT_PER_SPLIT reserved width)
  </read_first>
  <action>
    Define `DeviceSplitInfo<R: Runtime>` as a Struct-of-Arrays: one `Handle` per field, each its own `client.empty(num_leaf_slots * size_of::<T>())`, allocated ONCE in a `new(client, num_leaf_slots)` constructor — never inside any (future) grow loop, NO per-split device allocation (D-08; the C++ `AllocateCatVectorsKernel` per-split alloc is exactly the anti-pattern eliminated). Numeric fields per D-06: `is_valid`, `leaf_index`, `gain`, `inner_feature_index`, `threshold` (u32), `default_left`, per-side `{left,right}_sum_gradients`/`_sum_hessians` (f64), `_count` (i32), `_gain` (f64), `_value` (f64), and the quantized `_sum_of_gradients_hessians` (i64 — store as a plain i64 buffer; the u64 two's-complement ATOMIC accumulation idiom is a Phase-16 concern, only the buffer is reserved here). Categorical buffers `num_cat_threshold` (i32 [num_leaf_slots]), `cat_threshold` (u32 [num_leaf_slots * MAX_CAT_PER_SPLIT]) and `cat_threshold_real` (i32, same slab) are pre-allocated RESERVED now so Phase 22 fills, not restructures (D-06). RESOLVE Open Q3 IN THIS PLAN: define `MAX_CAT_PER_SPLIT` as a documented plain `usize` const set to a conservative fixed cap (e.g. the C++ `config_->max_cat_threshold` default) and note it as Phase-22-tunable — the SoA layout is invariant to the cap, only the slab length changes. Implement the deep-copy `operator=` as a host-side "copy leaf-slot a -> leaf-slot b across every field buffer" (read the source slot scalar, write the dest slot) — sufficient for Phase-14 correctness; the device-kernel slot-copy + the 8/16-int readback packet are deferred to their §8/§17/§18 consumers (D-07, RESEARCH A6). Self-test: construct with N slots, write distinct values per field, copy slot a -> b, read back, assert every field at b equals a and the others are untouched; assert the constructor performs allocation exactly once (no per-slot alloc).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test split_info</automated>
  </verify>
  <acceptance_criteria>
    - One buffer per field, all sized off `num_leaf_slots`, allocated in `new(...)` only — grep confirms no allocation in any copy/index path.
    - Every D-06 numeric field present; categorical buffers present and reserved-sized via `MAX_CAT_PER_SPLIT`.
    - Slot a -> b deep-copy preserves all fields exactly and leaves other slots untouched.
    - `MAX_CAT_PER_SPLIT` documented as a Phase-22-tunable const (Open Q3 closed).
  </acceptance_criteria>
  <done>A pre-allocated SoA device split-record with numeric fields + reserved categorical slabs and a working host slot-copy exists, with zero per-split device alloc.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CUDARandom #[cube] LCG pinned bit-for-bit to host Random (D-04)</name>
  <files>crates/lgbm-compute/src/kernels/random.rs, crates/lgbm-compute/tests/cuda_random_parity.rs</files>
  <read_first>
    - crates/lgbm-core/src/random.rs lines 8-14 (the V6 non-crypto module doc to carry forward) + 44-77 (the EXACT recurrence + RandInt16/RandInt32/NextFloat draw methods — the parity oracle) + 158-167 (the host to_bits parity-test pattern)
    - crates/lgbm-compute/src/kernels/histogram.rs lines 176-189 (create_from_slice seeds -> launch -> read_one_unchecked -> from_bytes launcher shape)
    - LightGBM-release-4.6.0.99/include/LightGBM/cuda/cuda_random.hpp lines ~58-65 (the same recurrence on the device side — signature reference)
    - 14-RESEARCH.md "CUDARandom LCG" Code Example + Pitfall 2 (no wrapping_*, no Atomic<i64>) + Assumption A2
  </read_first>
  <behavior>
    - Device draw sequence from seed S equals host `Random::new(S)`'s sequence: RandInt16 i32-exact, RandInt32 i32-exact, NextFloat f32 to_bits()-exact.
    - Multiple independent seeded device tasks each reproduce their own host stream.
  </behavior>
  <action>
    Implement a `#[cube]` LCG mirroring the host `Random`: `cuda_rand_advance(state)` updates `state = state * 214013u32 + 2531011u32` using PLAIN `u32` `*`/`+` (native hardware two's-complement wrap) — NOT `wrapping_mul`/`wrapping_add` (those are not cube intrinsics) and NOT any i64 atomic (Pitfall 2). Provide `RandInt16` (`(advance >> 16) & 0x7FFF` as i32), `RandInt32` (`advance & 0x7FFFFFFF` as i32), and `NextFloat` (`RandInt16 as f32 / 32768.0f32` — divisor exactly 32768.0, f32 end-to-end). Add a host launcher that seeds N device tasks, draws K each, and reads them back (`create_from_slice` seeds -> `launch` -> `read_one_unchecked`). Carry forward the V6 negative control: a module doc stating this is a deterministic non-crypto PRNG that MUST NEVER be used as security entropy. Parity test: for several seeds, draw the device stream and assert bit-for-bit equality against `lgbm_core::random::Random::new(seed)` — RandInt16/RandInt32 via i32 equality, NextFloat via `f32::to_bits()` equality (mirror random.rs:158-167). This test IS the end-to-end verification of the "plain u32 wraps on device" assumption (A2).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test cuda_random_parity</automated>
  </verify>
  <acceptance_criteria>
    - Device RandInt16/RandInt32 match the host `Random` exactly (i32 equality) across multiple seeds.
    - Device NextFloat matches the host via `to_bits()` (no float tolerance — RNG draws are exact-bit).
    - The LCG uses plain u32 arithmetic (no `wrapping_*`, no i64 atomic) and the V6 non-crypto doc is present.
  </acceptance_criteria>
  <done>A CUDARandom device LCG with a bit-identical stream to the host Random exists and is parity-tested.</done>
</task>

</tasks>

<artifacts_produced>
New symbols in `crates/lgbm-compute/src/kernels/split_info.rs`: `DeviceSplitInfo<R>` struct + all field `Handle`s, `new(client, num_leaf_slots)`, the host slot-copy method, the `MAX_CAT_PER_SPLIT` const.
New symbols in `crates/lgbm-compute/src/kernels/random.rs`: `cuda_rand_advance`, `cuda_rand_int16`, `cuda_rand_int32`, `cuda_next_float` `#[cube]` fns + the host parity launcher.
New files: `crates/lgbm-compute/tests/split_info.rs`, `crates/lgbm-compute/tests/cuda_random_parity.rs`.
Note for plan-review source-grounding: all the above are NEW; host `Random` and the AMD-fork `cuda_split_info.hpp` / `cuda_random.hpp` are read-only references.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launcher → device kernel | Seeds + slot indices are host-controlled; sizes host-known. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Tampering / DoS | out-of-bounds slot index on the SoA buffers | mitigate | Host-side slot-index validation (< num_leaf_slots) before any read/write; copy path is host-side this phase (no unchecked device launch). |
| T-14-04-02 | Information disclosure | misuse of CUDARandom as security entropy | mitigate | V6 negative control: module doc forbids security use; only the seeded constructor is ported (inherited from `random.rs`). |
| T-14-04-03 | DoS | slab sizing `num_leaf_slots * MAX_CAT_PER_SPLIT` overflow | mitigate | Sizes computed in `usize`, validated at construction. |
| T-14-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (cubecl 0.10 vendored — Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --test split_info` green (alloc-once + slot-copy correctness).
- `cargo test -p lgbm-compute --test cuda_random_parity` green (bit-exact vs host Random).
- `cargo build -p lgbm-compute` clean; no `wrapping_*` / i64-atomic in the LCG.
</verification>

<success_criteria>
ODL-02 satisfied: a pre-allocated SoA device split-record with no per-split device alloc (numeric fields + reserved categorical slabs + host slot-copy) and a CUDARandom LCG with a bit-identical stream to the host Random both exist and are tested.
</success_criteria>

<output>
Create `.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-04-SUMMARY.md` when done, recording the chosen `MAX_CAT_PER_SPLIT` cap (Open Q3).
</output>
