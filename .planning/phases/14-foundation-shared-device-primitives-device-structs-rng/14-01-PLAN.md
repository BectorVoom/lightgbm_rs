---
phase: 14-foundation-shared-device-primitives-device-structs-rng
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/lgbm-compute/tests/plane_intrinsic_smoke.rs
  - crates/lgbm-compute/src/kernels/primitives.rs
  - crates/lgbm-compute/src/kernels/split_info.rs
  - crates/lgbm-compute/src/kernels/random.rs
  - crates/lgbm-compute/src/kernels/mod.rs
autonomous: true
requirements: [ODL-01]
must_haves:
  truths:
    - "A smoke test proves plane_inclusive_sum / plane_exclusive_sum / plane_max / plane_min lower and produce correct results on the cubecl-cpu f64 anchor (and on cubecl-hip under --features rocm)"
    - "A documented fallback exists if any intrinsic fails to lower (plane_shuffle_up manual scan)"
    - "The three new kernel modules (primitives, split_info, random) exist as compiling stubs wired into the kernels barrel"
    - "The default workspace build and existing compute tests are unchanged"
  artifacts:
    - path: "crates/lgbm-compute/tests/plane_intrinsic_smoke.rs"
      provides: "Open-Q1 per-intrinsic-per-backend plane-op smoke test (Pitfall 1 de-risk)"
      min_lines: 40
    - path: "crates/lgbm-compute/src/kernels/primitives.rs"
      provides: "Stub module for the shared device primitives (filled by 14-03 / 14-05)"
    - path: "crates/lgbm-compute/src/kernels/split_info.rs"
      provides: "Stub module for the SoA device split-record (filled by 14-04)"
    - path: "crates/lgbm-compute/src/kernels/random.rs"
      provides: "Stub module for the CUDARandom LCG (filled by 14-04)"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/mod.rs"
      to: "crates/lgbm-compute/src/kernels/{primitives,split_info,random}.rs"
      via: "pub mod declarations"
      pattern: "pub mod (primitives|split_info|random)"
    - from: "crates/lgbm-compute/tests/plane_intrinsic_smoke.rs"
      to: "cubecl-cpu f64 anchor"
      via: "serial reference comparison (never GPU-vs-GPU)"
      pattern: "cpu_client"
---

<objective>
De-risk the single MEDIUM unknown of this phase (RESEARCH Open Q1 / Pitfall 1: do the cubecl-0.10 plane scan/reduction intrinsics lower on every backend?) BEFORE any primitive is authored, and lay down the module scaffolding so the Wave-2 plans (14-03 primitives, 14-04 split-record + RNG) can each own exactly one new file with zero `mod.rs` contention.

Purpose: The shipped histogram path only ever exercised `plane_sum` / `plane_any` / `plane_ballot` / `plane_shuffle` on hip — the scan/max/min intrinsics are unproven in this repo. If a backend silently no-ops or fails to lower one, every downstream primitive built on it breaks. Front-loading a smoke test resolves the risk on the cheapest possible surface and records the `plane_shuffle_up` manual-scan fallback decision.

Output: A plane-intrinsic smoke test (cubecl-cpu anchor + optional hip) and three compiling stub modules wired into the kernels barrel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-CONTEXT.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-RESEARCH.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Plane-intrinsic per-backend smoke test (Open Q1 / Pitfall 1)</name>
  <files>crates/lgbm-compute/tests/plane_intrinsic_smoke.rs</files>
  <read_first>
    - crates/lgbm-compute/tests/rocm_plane_aggregate.rs (the GPU-vs-cpu-anchor smoke idiom: `probe_capabilities(&gc).has_plane` guard, pin GPU result to `cpu_client()` within tolerance, `#![cfg(feature = "rocm")]` for the hip half)
    - crates/lgbm-compute/src/runtime.rs (cpu_client, rocm_client, probe_capabilities, Capabilities, accumulate_type) — lines ~84-131
    - crates/lgbm-compute/src/kernels/histogram.rs lines ~435-558 (existing `plane_*` usage) and ~176-189 (the create_from_slice → launch → read_one_unchecked launcher shape)
    - 14-RESEARCH.md Pattern 1 + Pitfall 1 + Open Question 1
  </read_first>
  <action>
    Author a small test that exercises each of `plane_inclusive_sum`, `plane_exclusive_sum`, `plane_max`, `plane_min` inside a minimal `#[cube]` kernel over a single plane-width input, launched on the cubecl-cpu f64 anchor by default and, under `#[cfg(feature = "rocm")]`, on the hip client. For each intrinsic, compare the device result against a plain serial Rust reference fold (inclusive/exclusive prefix sums, running max, running min) — anchor to the cpu f64 fold / serial reference, NEVER GPU-vs-GPU (def-f8u-01, D-10). Integer/exactly-representable inputs assert bit-exact; if a hip f32 path is exercised use the existing ~1e-6 tolerance. If any intrinsic fails to lower (compile panic mentioning an unsupported Plane op, or a result equal to the input — the silent no-op warning sign), record the finding in the SUMMARY and switch the affected op to a `plane_shuffle_up`-based manual scan (RESEARCH Pattern 1 alternative); the smoke test then pins the manual-scan variant. Do NOT hard-code the plane width — read `PLANE_DIM` as the runtime builtin. This is the explicit Wave-0 de-risk gate (RESEARCH Open Q1); its outcome is an input to 14-03.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test plane_intrinsic_smoke</automated>
  </verify>
  <acceptance_criteria>
    - All four plane intrinsics produce results matching the serial reference on the cubecl-cpu anchor (bit-exact for integer/exact inputs).
    - The test compiles and passes on the default (cpu) build; the hip half is gated behind `--features rocm` and guarded by `probe_capabilities(...).has_plane`.
    - The SUMMARY records, per intrinsic, whether the 0.10 intrinsic lowered or the `plane_shuffle_up` fallback was needed.
  </acceptance_criteria>
  <done>The plane-intrinsic lowering question is resolved on both backends and the chosen path (intrinsic vs fallback) is recorded for 14-03 to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the three new kernel modules + wire the barrel</name>
  <files>crates/lgbm-compute/src/kernels/primitives.rs, crates/lgbm-compute/src/kernels/split_info.rs, crates/lgbm-compute/src/kernels/random.rs, crates/lgbm-compute/src/kernels/mod.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/mod.rs (the existing barrel: `pub mod histogram; pub mod partition; pub mod split; pub mod subtract;` and the `#[cfg(feature = "gpu")] pub mod autotune;` gate)
    - crates/lgbm-compute/src/kernels/histogram.rs lines 28-31 (the import preamble: `use cubecl::prelude::*; use crate::error::ComputeError; use crate::runtime::ActiveRuntime;`)
    - 14-PATTERNS.md "Recommended Project Structure" + the three NEW-file pattern assignments
  </read_first>
  <action>
    Create three new module files as compiling stubs, each with a module doc comment stating its purpose, the owning Wave-2 plan, and the relevant locked decisions: `primitives.rs` (shared device primitives — prefix-sum / reductions / bitonic argsort / skeletons; filled by 14-03 and 14-05; ODL-01, D-01/D-02), `split_info.rs` (SoA pre-allocated device split-record; filled by 14-04; ODL-02, D-05/D-06/D-08), `random.rs` (CUDARandom LCG; filled by 14-04; ODL-02, D-04). Each stub should carry the standard import preamble (commented or `#[allow(unused_imports)]`-guarded if needed so an empty module still compiles cleanly) and a brief reference to its analog file. Wire all three into `kernels/mod.rs` with `pub mod primitives; pub mod split_info; pub mod random;` placed alongside the existing module declarations. Mirror the existing barrel's ungated form (like `histogram`), NOT the `#[cfg(feature = "gpu")]` autotune gate, unless a stub genuinely needs gating to compile. The goal is purely to let 14-03 and 14-04 each edit one owned file without touching `mod.rs`.
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute && cargo test -p lgbm-compute --no-run</automated>
  </verify>
  <acceptance_criteria>
    - `crates/lgbm-compute/src/kernels/mod.rs` declares `primitives`, `split_info`, and `random` modules.
    - The crate builds with no warnings introduced by the stubs; existing tests still compile.
    - Each stub documents its owning plan + locked decisions so the Wave-2 executors have unambiguous scope.
  </acceptance_criteria>
  <done>The kernels barrel exposes the three new modules; Wave-2 plans can fill exactly one file each with no `mod.rs` conflict.</done>
</task>

</tasks>

<artifacts_produced>
New files: `crates/lgbm-compute/tests/plane_intrinsic_smoke.rs`, `crates/lgbm-compute/src/kernels/primitives.rs` (stub), `crates/lgbm-compute/src/kernels/split_info.rs` (stub), `crates/lgbm-compute/src/kernels/random.rs` (stub).
New symbols: the smoke-test `#[test]` fns; three new `pub mod` declarations in `kernels/mod.rs`; any small `#[cube]` probe kernels local to the smoke test.
Note for plan-review source-grounding: `primitives`, `split_info`, `random` are NEW modules created here; do not flag them as drift.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launcher → device kernel | Smoke-test inputs are host-controlled fixed slices; no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | smoke-test device launch | accept | Inputs are fixed test slices; lengths host-known. No `launch_unchecked` needed (use checked `::launch`); no out-of-bounds surface. |
| T-14-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs this plan (cubecl 0.10 already vendored — RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --test plane_intrinsic_smoke` green on the cpu anchor.
- `cargo build -p lgbm-compute` clean; the three modules resolve.
- (Optional, ROCm) `cargo test -p lgbm-compute --test plane_intrinsic_smoke --features rocm` on the local APU.
</verification>

<success_criteria>
The plane-intrinsic lowering risk is resolved and recorded; the three new kernel modules compile and are wired into the barrel, unblocking parallel Wave-2 work.
</success_criteria>

<output>
Create `.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-01-SUMMARY.md` when done, recording per-intrinsic lowering results (intrinsic vs fallback) for 14-03.
</output>
