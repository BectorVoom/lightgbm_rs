---
phase: 14-foundation-shared-device-primitives-device-structs-rng
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - crates/lgbm-compute/src/kernels/primitives.rs
  - crates/lgbm-compute/tests/primitives_self.rs
autonomous: true
requirements: [ODL-01]
must_haves:
  truths:
    - "Block + multi-kernel global prefix-sum (inclusive AND exclusive) exists as a reusable CubeCL kernel, correct on the cubecl-cpu f64 anchor"
    - "Shuffle reductions sum / max / min and dot-product exist as reusable CubeCL kernels"
    - "Single-block index-only bitonic argsort exists, never moving the value array, with a permutation that matches the C++ comparator/tie order"
    - "Each numeric primitive is anchored to a serial f64 reference (never GPU-vs-GPU); the f64 reduction-order decision (Open Q2) is recorded per primitive"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/primitives.rs"
      provides: "prefix_sum (block+global, incl/excl), reduce (sum/max/min/dotprod), single-block bitonic argsort"
      min_lines: 200
    - path: "crates/lgbm-compute/tests/primitives_self.rs"
      provides: "serial-f64-reference-anchored unit tests for each full-depth primitive"
      min_lines: 60
  key_links:
    - from: "crates/lgbm-compute/src/kernels/primitives.rs"
      to: "cubecl 0.10 plane intrinsics"
      via: "plane_inclusive_sum / plane_exclusive_sum / plane_sum / plane_max / plane_min (or the 14-01 fallback)"
      pattern: "plane_(inclusive_sum|exclusive_sum|sum|max|min|shuffle_up)"
    - from: "crates/lgbm-compute/src/kernels/primitives.rs"
      to: "global prefix-sum scratch"
      via: "client.empty(num_blocks) allocated once, reused across the 3 launches"
      pattern: "client.empty"
    - from: "crates/lgbm-compute/tests/primitives_self.rs"
      to: "serial f64 reference fold"
      via: "anchor comparison (never GPU-vs-GPU)"
      pattern: "cpu_client"
---

<objective>
Author the full-depth grow-loop primitive subset that Phases 15–18 actually consume (D-01): block + multi-kernel global prefix-sum (inclusive/exclusive), shuffle reductions (sum/max/min + dot-product), and single-block index-only bitonic argsort. Build them on the cubecl-0.10 plane intrinsics (or the 14-01 fallback), with LDS staging for the block/global levels, and anchor each to a serial f64 reference (never GPU-vs-GPU, D-10).

Purpose: These are the reusable kernels every later on-device subsystem builds on (ODL-01, §17 "port first"). Building only the consumed subset at full depth honors D-01 without front-loading YAGNI depth (skeletons follow in 14-05).

Output: The full-depth primitives in `primitives.rs` + serial-reference self-tests. The C++-fixture cross-validation lands in 14-06.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-CONTEXT.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-RESEARCH.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-PATTERNS.md
@.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Block + multi-kernel global prefix-sum (inclusive/exclusive)</name>
  <files>crates/lgbm-compute/src/kernels/primitives.rs, crates/lgbm-compute/tests/primitives_self.rs</files>
  <read_first>
    - 14-01-SUMMARY.md (whether plane_inclusive_sum/exclusive_sum lowered or the plane_shuffle_up fallback is in effect — USE the recorded path)
    - crates/lgbm-compute/src/kernels/histogram.rs lines 55-113 (single-source generic `#[cube]` body + thin per-type launch wrappers), 581-607 + 900-911 (safe `launch_unchecked` wrapper + SAFETY comment), 144-167 (V5 validate → Result<_, ComputeError> → upload), 792-835 (SharedMemory + sync_cube LDS staging), 613-625 (comptime-sized-to-max LDS const)
    - crates/lgbm-compute/src/runtime.rs lines 84-131 (accumulate_type f64/f32 gate, cpu_client, probe_capabilities)
    - 14-RESEARCH.md Pattern 2 (segmented LDS block-scan), Pattern 3 (3-kernel global scan, scratch sized num_blocks reused), Pattern 6 (launch_unchecked wrapper)
  </read_first>
  <behavior>
    - Inclusive block scan of [1,2,3,4] → [1,3,6,10]; exclusive → [0,1,3,6].
    - A block longer than one plane (cross-plane) scans correctly via LDS staging (no plane ever sums across more than PLANE_DIM lanes).
    - Global scan over an array spanning multiple blocks equals the serial running sum across the whole array.
    - Empty / single-element inputs handled without panic.
  </behavior>
  <action>
    Implement the prefix-sum primitive as a single-source generic `#[cube]` body with thin per-cell-type launch wrappers (f64 cpu-anchor / f32 hip-mirror, routed by `accumulate_type()`), mirroring the histogram.rs idiom. Within a plane use the 14-01-recorded path (`plane_inclusive_sum` / `plane_exclusive_sum`, or the `plane_shuffle_up` manual scan fallback). For blocks wider than a plane, compose a segmented `SharedMemory` block-scan (Pattern 2): per-plane scan, stage per-plane totals in a ≤PLANE_DIM-slot LDS buffer, exclusive-scan those, add the base back, with `sync_cube()` between stages (intra-cube barrier ONLY — there is no cross-cube barrier). For arrays spanning multiple blocks, implement the 3-launch global scan (Pattern 3): block-scan writing each block total into ONE `client.empty(num_blocks * size)` scratch buffer allocated once and reused, scan the block totals, then add-back. Provide both inclusive and exclusive entry points. Validate slice lengths at the V5 boundary returning `ComputeError` before any `launch_unchecked`; confine the unsafe launch behind the SAFETY-comment wrapper convention. Self-test against a plain serial Rust prefix-sum (integer/exact inputs → bit-exact) on the cpu_client anchor; assume num_blocks ≤ 1024 this phase (the recursive case is a Phase-15 concern — note it, do not build it).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test primitives_self prefix_sum</automated>
  </verify>
  <acceptance_criteria>
    - Inclusive and exclusive variants both present and correct for within-plane, cross-plane, and multi-block inputs.
    - Global scan uses exactly one reused `client.empty` scratch (no per-launch alloc).
    - No cross-cube barrier assumption; `sync_cube` used only intra-cube.
    - Integer/exact self-tests are bit-exact vs the serial reference on the cpu anchor.
  </acceptance_criteria>
  <done>A reusable, anchor-verified block+global inclusive/exclusive prefix-sum exists in primitives.rs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shuffle reductions (sum / max / min, dot-product)</name>
  <files>crates/lgbm-compute/src/kernels/primitives.rs, crates/lgbm-compute/tests/primitives_self.rs</files>
  <read_first>
    - 14-01-SUMMARY.md (plane_sum/plane_max/plane_min lowering status)
    - crates/lgbm-compute/src/kernels/histogram.rs lines 435-558 (existing plane usage) + the single-source body / thin-wrapper idiom (55-113)
    - 14-RESEARCH.md Pattern 1 (reductions map to plane_sum/plane_max/plane_min; dot-product = elementwise mul then plane_sum), Pitfall 3 (f32 cross-backend NOT bit-exact), Open Question 2 (f64 reduction-order decision)
  </read_first>
  <behavior>
    - sum([1,2,3,4]) == 10; max == 4; min == 1 (exact for integer/exact f64).
    - dot([1,2,3],[4,5,6]) == 32.
    - Cross-plane reduction (length > PLANE_DIM) folds correctly via LDS combine.
  </behavior>
  <action>
    Implement sum/max/min reductions and a dot-product reduction as single-source generic `#[cube]` bodies + thin per-type wrappers, using the 14-01-recorded plane path (`plane_sum`/`plane_max`/`plane_min`; dot-product = elementwise multiply then `plane_sum`). For lengths exceeding a plane, combine per-plane partials through the same `SharedMemory` + `sync_cube()` LDS staging as the prefix-sum (one plane reduces the per-plane partials). Route f64 (cpu anchor) vs f32 (hip mirror) via `accumulate_type()`. RESOLVE Open Q2 IN THIS PLAN and document the choice per reduction: assert f64 reductions BIT-EXACT against a serial Rust f64 fold that replicates the C++ pairwise/tree reduction order where feasible; where the GPU warp-tree order cannot be matched serially, assert a documented tight-ULP band instead and record why. Integer-input reductions remain bit-exact regardless. f32 hip reductions are held to ~1e-6 only (deferred to the ROCm leg in 14-06). Never compare two GPU paths to each other (def-f8u-01).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test primitives_self reduce</automated>
  </verify>
  <acceptance_criteria>
    - sum/max/min + dot-product all present, correct on within-plane and cross-plane inputs.
    - The f64 reduction-order decision (bit-exact-with-matched-order vs documented ULP band) is recorded per reduction in code/SUMMARY (Open Q2 closed).
    - Anchored to a serial f64 reference / integer-exact; no GPU-vs-GPU assertion.
  </acceptance_criteria>
  <done>Reusable, anchor-verified sum/max/min + dot-product reductions exist in primitives.rs with the f64-order policy recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Single-block index-only bitonic argsort</name>
  <files>crates/lgbm-compute/src/kernels/primitives.rs, crates/lgbm-compute/tests/primitives_self.rs</files>
  <read_first>
    - LightGBM-release-4.6.0.99/src/cuda/cuda_algorithms.cu (the BitonicArgSort_1024 comparator + ascending-segment + tie convention — match EXACTLY)
    - crates/lgbm-compute/src/kernels/histogram.rs lines 792-835 (sync_cube between network stages)
    - 14-RESEARCH.md Pattern 4 (bitonic network, comparator reads keys[indices[i]], only indices swapped, BITONIC depth 11), Pitfall 5 (tie-order divergence)
  </read_first>
  <behavior>
    - argsort of keys [3,1,2] (identity start) → permutation [1,2,0] (ascending).
    - The value/key array is never mutated — only the index array is reordered.
    - Tie-rich input ([2,1,2,1]) yields the SAME permutation the C++ comparator produces.
  </behavior>
  <action>
    Implement single-block bitonic argsort over an index array (identity or a provided permutation) keyed by a read-only `keys` array — the comparator reads `keys[indices[i]]` (indirection) and swaps ONLY `indices`, never the values (Pattern 4). Mirror the C++ `cuda_algorithms.cu` comparator EXACTLY: same strict/non-strict `>` vs `>=`, same ascending-segment parity `(i & k) == 0`, `sync_cube()` between network stages. Use `BITONIC_SORT_NUM_ELEMENTS=1024` / depth 11 as the single-block cap (multi-block extension is a 14-05 skeleton, D-02). Self-test the permutation BIT-EXACT against a serial Rust argsort that uses the identical comparator/tie convention, including a tie-rich input (locks the convention before 14-06's fixture cross-check). No float tolerance — it is an index array.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test primitives_self argsort</automated>
  </verify>
  <acceptance_criteria>
    - Index-only: the keys/values array is provably unmutated after the sort.
    - Permutation bit-exact vs the serial reference on distinct-key AND tie-rich inputs.
    - Comparator/tie convention matches the C++ reference (documented in code).
  </acceptance_criteria>
  <done>A single-block index-only bitonic argsort with C++-matched tie order exists in primitives.rs.</done>
</task>

</tasks>

<artifacts_produced>
New symbols in `crates/lgbm-compute/src/kernels/primitives.rs`: the generic prefix-sum `#[cube]` body + inclusive/exclusive launch wrappers (f64/f32) + the 3-kernel global-scan driver; the reduction `#[cube]` bodies + sum/max/min/dotprod launch wrappers; the single-block bitonic argsort kernel + launcher; any `BITONIC_*` / LDS-staging consts.
New file `crates/lgbm-compute/tests/primitives_self.rs`: serial-reference-anchored `#[test]` fns (prefix_sum, reduce, argsort).
Note for plan-review source-grounding: all symbols above are NEW; the AMD-fork `cuda_algorithms.cu` is read-only reference.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host launcher → device kernel | Host-controlled slices crossing into `launch_unchecked` device kernels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering / DoS | out-of-bounds device write via `launch_unchecked` | mitigate | V5 host-side length validation returns `ComputeError` BEFORE upload; every device index host-proven in range; unsafe confined to the launcher behind a SAFETY comment (CMP-01 / NRW-01). |
| T-14-03-02 | DoS | scratch sizing `num_blocks * size` overflow | mitigate | Compute sizes in `usize`, validate against input length at the V5 boundary. |
| T-14-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (cubecl 0.10 vendored — Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --test primitives_self` green on the cpu anchor (prefix_sum, reduce, argsort).
- `cargo build -p lgbm-compute` clean.
- No `Atomic` over an i64 cell and no `wrapping_*` in `#[cube]` (use the established u64/native-wrap idioms only where atomics are actually needed — prefix-sum/reduction/argsort need none).
</verification>

<success_criteria>
The full-depth grow-loop primitive subset (prefix-sum incl/excl block+global, reductions sum/max/min/dotprod, single-block index-only bitonic argsort) exists as reusable CubeCL kernels, each anchored to a serial f64 reference with the Open-Q2 f64-order policy recorded.
</success_criteria>

<output>
Create `.planning/phases/14-foundation-shared-device-primitives-device-structs-rng/14-03-SUMMARY.md` when done, recording the per-reduction f64-order policy (Open Q2) and the argsort tie convention.
</output>
