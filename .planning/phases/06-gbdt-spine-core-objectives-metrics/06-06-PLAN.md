---
phase: 06-gbdt-spine-core-objectives-metrics
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: false
requirements: [BST-07, OBJ-01, OBJ-03, BST-01, API-01]
files_modified:
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm-treelearner/src/learner.rs
  - crates/lgbm-model/src/tree.rs
  - crates/lgbm/src/booster.rs
  - crates/lgbm-boosting/src/early_stopping.rs
  - crates/lgbm-objective/src/regression.rs
  - xtask/src/main.rs
  - xtask/py/boosting_oracle_capture.py
  - .planning/phases/06-gbdt-spine-core-objectives-metrics/06-05-SUMMARY.md

must_haves:
  truths:
    # Derived goal-backward from the 3 PARTIAL success criteria (SC#1, SC#3, SC#4)
    - "SC#1 — A constant (num_leaves=1) tree's emitted model text is byte-for-byte equal to the C++ golden, including leaf_count=<num_data>; a test byte-compares emitted model text and FAILS if leaf_count diverges."
    - "SC#3 — regression_l1 + bagging leaf values equal the C++ median-residual leaves (within ORACLE_TOL / bit-exact where the algorithm permits); the matrix cell that exercises this FAILS if the leaf values are the raw Newton output."
    - "SC#3 — every D-07 matrix cell asserts numerically: no compare_within Result is discarded; a wrong value of any magnitude makes the matrix test FAIL."
    - "SC#3 — reg_sqrt=1 is exercised by a committed real-binary golden; grad/hess + ConvertOutput + final predictions are asserted within ORACLE_TOL and FAIL if the sqrt transform/inverse is wrong."
    - "SC#4 — the early-stop eval+decision runs every iteration when ES is on, independent of metric_freq; a metric_freq>1 + ES golden cell FAILS if the best_iteration / trailing-trim diverges from C++."
  artifacts:
    - path: "crates/lgbm-model/src/tree.rs"
      provides: "Tree::as_constant(constant_value, count) with leaf_count=vec![count]"
      contains: "fn as_constant"
    - path: "crates/lgbm-treelearner/src/learner.rs"
      provides: "train_on_subset_returning_partition (or subset partition exposure) for subset-path renewal"
      contains: "train_on_subset"
    - path: "crates/oracle-harness/tests/boosting_parity.rs"
      provides: "matrix cells with teeth (no .ok()), a constant-tree byte-compare, a reg_sqrt golden assertion, a metric_freq>1+ES assertion"
      contains: "compare_within"
    - path: "crates/oracle-harness/tests/fixtures/boosting/regression_sqrt_spine_model.txt"
      provides: "real lib_lightgbm 4.6 reg_sqrt=1 model golden"
    - path: "crates/oracle-harness/tests/fixtures/boosting/regression_mf2es_model.txt"
      provides: "real lib_lightgbm 4.6 metric_freq>1 + early_stopping golden"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs (subset branch)"
      to: "objective.renew_leaf_output via learner subset partition"
      via: "median-residual over in-bag rows mapped through in_bag[subset_row]"
      pattern: "is_renew_tree_output"
    - from: "crates/lgbm/src/booster.rs (ES loop)"
      to: "early.update"
      via: "valid-eval every iter when es_enabled, metric_freq gates history only"
      pattern: "early.update"
    - from: "crates/lgbm-boosting/src/gbdt.rs (as_constant call sites)"
      to: "Tree::as_constant"
      via: "thread self.num_data at all 3 call sites"
      pattern: "as_constant"
---

<objective>
Close the five verification gaps (A–E) found in Phase 6 (06-VERIFICATION.md, status gaps_found 3/5 SC). The phase's spine, score updater, six objectives, seven metrics, bagging RNG, and early-stop arithmetic are real and validated on the default-config paths, but three confirmed defects survived a green 426-test suite because the committed goldens never exercise their triggers. This plan fixes each defect AND adds the trigger that proves the fix has teeth.

Resolution order (per the verifier): WR-01 first (it masks the others), then CR-01 + WR-03, then CR-02, then the reg_sqrt verification gap.

Purpose: make SC#1 (bit-exact model text), SC#3 (l1+bagging + reg_sqrt + asserted matrix), and SC#4 (ES cadence) actually TRUE and actually VERIFIED.
Output: assertion-tightened matrix + new triggers, corrected `as_constant` signature + 3 call sites, subset-path median-residual renewal, decoupled ES cadence, two new real-binary goldens (reg_sqrt=1, metric_freq>1+ES), corrected SUMMARY/doc claims.

Scope guard: do NOT modify 06-01..06-05 PLAN.md. Do NOT replan the phase. Do NOT edit LightGBM/ or git-add it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-VERIFICATION.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-CONTEXT.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-RESEARCH.md
</context>

<artifacts_this_phase_produces>
New / changed symbols and files this gap-closure plan introduces (the executor MUST create exactly these):

- `Tree::as_constant(constant_value: f64, count: data_size_t) -> Tree` — NEW `count` parameter (was zero-arg); sets `leaf_count: vec![count]` instead of `vec![0]`. (crates/lgbm-model/src/tree.rs)
- Three updated `Tree::as_constant(const_val, self.num_data)` call sites in crates/lgbm-boosting/src/gbdt.rs (was `Tree::as_constant(const_val)` at the ~290, ~362, ~420 sites).
- A subset-path renewal capability on the learner: either `SerialTreeLearner::train_on_subset_returning_partition(...) -> Result<(Tree, DataPartition), TreeLearnerError>` (preferred — mirrors `train_returning_partition`) OR a documented mechanism that yields the subset DataPartition; the renewal in gbdt.rs maps subset leaf rows through `in_bag[subset_row]` to full-corpus rows. (crates/lgbm-treelearner/src/learner.rs + crates/lgbm-boosting/src/gbdt.rs)
- New test fn names in crates/oracle-harness/tests/boosting_parity.rs:
  - `constant_tree_model_text_byte_exact` — byte-compares emitted model text of a constant-tree golden.
  - `reg_sqrt_spine_matches_real_binary` — grad/hess + ConvertOutput + predictions for reg_sqrt=1.
  - `metric_freq_gt1_with_early_stopping_matches` — best_iteration/tree-count for metric_freq>1 + ES.
- New committed golden files under crates/oracle-harness/tests/fixtures/boosting/:
  - `regression_sqrt_spine_model.txt`, `regression_sqrt_spine_pred.txt`, `regression_sqrt_gh_iter1.txt`
  - `regression_mf2es_model.txt`, `regression_mf2es_pred.txt`, `regression_mf2es_best_iteration.txt`
- xtask capture additions: a `regression sqrt` cell and a `metric_freq=2 + early_stopping` cell added to `xtask/py/boosting_oracle_capture.py` and to the post-capture existence checklist in `xtask/src/main.rs` (`boosting_oracle_capture`).
- No new typed error variant is expected (preferred WR-03 outcome is correct leaves). If — and ONLY if — Task 2 study concludes faithful subset renewal is out of scope, the FALLBACK adds `BoostingError::UnsupportedConfig { what: String }` (or reuses the existing typed-rejection variant) for `regression_l1 + bagging`; this must be surfaced as a checkpoint:decision, not chosen silently.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1 (GAP A — WR-01): Give every D-07 matrix cell teeth (no discarded Results)</name>
  <files>crates/oracle-harness/tests/boosting_parity.rs, .planning/phases/06-gbdt-spine-core-objectives-metrics/06-05-SUMMARY.md</files>
  <read_first>
    - crates/oracle-harness/tests/boosting_parity.rs:880-1060 (run_d07_matrix: the cell loop, the two `.ok()` residual blocks at 980-986 and 1004-1011, the assert blocks at 1017-1041)
    - crates/oracle-harness/tests/boosting_parity.rs:37-45 (boosting_dir / ORACLE_TOL / SPINE_SEED constants)
    - .planning/phases/06-gbdt-spine-core-objectives-metrics/06-VERIFICATION.md (gap WR-01 frontmatter + Anti-Patterns row boosting_parity.rs:985,1010)
    - .planning/phases/06-gbdt-spine-core-objectives-metrics/06-05-SUMMARY.md (lines ~64,120 — the overstated "VALIDATED within ORACLE_TOL ... never silently dropped" claims to correct)
  </read_first>
  <action>
    In run_d07_matrix, replace BOTH `compare_within(...).ok();` calls (the `uniform_grad_residual` block ~980-986 and the `*num_class > 1 && es` block ~1004-1011) with `compare_within(...).unwrap_or_else(|m| panic!("{cell} tree {i} leaf_value not within RESIDUAL_TOL: {m:?}"))`. Each residual class of cells MUST assert against an EXPLICIT, named, documented tolerance constant, not ORACLE_TOL-by-accident and never a swallowed Result. Introduce two `const`s with a doc comment citing the residual cause: `MATRIX_RESIDUAL_TOL` for the documented f32-accumulation / softmax-exp-libm residual cells (set to the smallest power-of-ten bound that the CORRECTLY-renewed cells actually satisfy — determine empirically AFTER Task 2 lands by running the matrix and reading the max observed diff; do NOT pad it to hide a real regression). The `uniform_grad_residual` (regression_l1 bfa-off degenerate-split) and multiclass-es knife-edge families keep their overlapping-tree comparison BUT now assert it. Do NOT widen the tolerance to make WR-03's wrong leaves pass — Task 1 lands the assertion; Task 2 makes the regression_l1+bagging cells actually correct so they pass within MATRIX_RESIDUAL_TOL. After this task (before Task 2), the regression_l1 bagging cells are EXPECTED to fail — that is the proof the assertion has teeth; ordering Task 2 immediately after closes them. Also correct the in-code comments at ~937-998 and 06-05-SUMMARY.md lines 64,120 to state the true coverage: every cell asserts; list which cells use bit-exact vs MATRIX_RESIDUAL_TOL and WHY (do not claim coverage the code does not enforce).
  </action>
  <verify>
    <automated>cd crates/oracle-harness && grep -n '\.ok();' tests/boosting_parity.rs | grep -i compare_within; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'compare_within' crates/oracle-harness/tests/boosting_parity.rs | grep '\.ok()'` returns ZERO lines (exit 1) — no compare_within Result is discarded anywhere in the file.
    - `grep -c 'unwrap_or_else' crates/oracle-harness/tests/boosting_parity.rs` increases by at least 2 vs the pre-edit count (the two former `.ok()` sites now panic on mismatch).
    - The constants `MATRIX_RESIDUAL_TOL` exists with a doc comment naming its cause; `grep -n 'MATRIX_RESIDUAL_TOL' crates/oracle-harness/tests/boosting_parity.rs` returns >= 2 lines (definition + use).
    - TEETH PROOF: temporarily perturbing any matrix golden leaf value by 1.0 makes `cargo test -p oracle-harness run_d07_matrix` (or the wrapping test fn) PANIC; revert the perturbation. (Executor performs this check in-session and records the panic line in the SUMMARY.)
    - 06-05-SUMMARY.md no longer contains the unqualified string "never silently dropped" without an accompanying "(every cell now asserts; see 06-06)" correction; `grep -n 'never silently dropped' .planning/phases/06-gbdt-spine-core-objectives-metrics/06-05-SUMMARY.md` lines are each within 3 lines of a "06-06" correction note.
  </acceptance_criteria>
  <done>Every D-07 matrix cell asserts numerically; the matrix test fails on any wrong value; the overstated coverage claims are corrected. regression_l1+bagging cells are now (correctly) red, to be closed by Task 2.</done>
</task>

<task type="auto">
  <name>Task 2 (GAP B — WR-03): Apply median-residual RenewTreeOutput on the bagging/subset path</name>
  <files>crates/lgbm-treelearner/src/learner.rs, crates/lgbm-boosting/src/gbdt.rs</files>
  <read_first>
    - crates/lgbm-boosting/src/gbdt.rs:300-365 (the `if use_subset` branch; the EMPTY `if self.objective.is_renew_tree_output()` block at 314-322; the full-corpus renewal it must mirror at 372-397)
    - crates/lgbm-treelearner/src/learner.rs:305-358 (train_on_subset — does NOT return the partition; train_returning_partition — the pattern to copy)
    - crates/lgbm-treelearner/src/learner.rs:1424-1442 (renew_tree_output closure contract: `Fn(i32, &[u32]) -> f64` over leaf rows)
    - crates/lgbm-boosting/src/objective.rs:117-130 (is_renew_tree_output / renew_leaf_output)
    - LightGBM/src/boosting/gbdt.cpp:388-440 (TrainOneIter: the subset grad/hess copy into gradients_pointer_, then the SINGLE RenewTreeOutput call passing bag_data_indices.data()+bag_data_cnt for BOTH paths)
    - LightGBM/src/treelearner/serial_tree_learner.cpp:920-958 (RenewTreeOutput: bag_mapper = bag_indices when total_num_data != num_data_; index_mapper from data_partition_->GetIndexOnLeaf; new_output = obj->RenewTreeOutput(..., index_mapper, bag_mapper, cnt))
    - LightGBM/src/objective/regression_objective.hpp:253-285 (RegressionL1::RenewTreeOutput — `residual_getter(label_, bagging_mapper[index_mapper[i]])` i.e. PercentileFun over the FULL-corpus residual of each in-bag leaf row)
    - crates/oracle-harness/tests/fixtures/boosting/regression_l1_bag1_es0_bfa0_model.txt and regression_l1_bag1_es1_bfa1_model.txt (the l1+bagging goldens this must now match)
  </read_first>
  <action>
    Mirror C++ exactly: on the subset path, RenewTreeOutput uses the SUBSET data_partition (leaf membership in subset-row space) and maps each subset row through the bag index to the FULL-corpus row for the residual. (1) Add `SerialTreeLearner::train_on_subset_returning_partition(&mut self, in_bag, gradients, hessians, is_first_tree) -> Result<(Tree, DataPartition), TreeLearnerError>` mirroring `train_returning_partition` (swap in subset features as `train_on_subset` already does, call `train_inner`, return `(tree, part)`). (2) In gbdt.rs `use_subset` branch, call the new method instead of `train_on_subset`; remove the empty/comment-only `if is_renew_tree_output` block and replace it with the ACTIVE renewal performed BEFORE `tree.shrinkage(...)` (C++ order: RenewTreeOutput → Shrinkage → UpdateScore → AddBias). The renewal closure receives `(leaf, subset_rows: &[u32])`; for each subset row `sr` compute the full-corpus row `fr = in_bag[sr as usize]`, then residual `labels[fr] as f64 - train_score_pre[offset + fr]` (the SAME `train_score_pre` snapshot and `offset` the full-corpus branch uses — that is the C++ `score_ptr = train_score_updater_->score() + offset` over full-corpus rows). Pass the residual slice to `obj.renew_leaf_output(...)` (PercentileFun alpha=0.5 median). This is the bag_mapper path: `index_mapper[i]` = subset leaf row, `bag_mapper[index_mapper[i]]` = `in_bag[subset_row]` = full-corpus row. Do NOT renew when `tree.num_leaves <= 1` (matches the existing num_leaves>1 guard). Keep the predict-side OOB+in-bag scoring (add_tree_predict_path) UNCHANGED — only the leaf VALUES change, then both in-bag and OOB rows are scored with the renewed tree, matching C++ UpdateScore(subset path)=`AddScore(tree, cur_tree_id)` over the renewed leaves. If, after reading the C++ and attempting the above, the subset DataPartition leaf-row ordering cannot be made to agree with the C++ tmp_subset_ partition within MATRIX_RESIDUAL_TOL, STOP and raise the checkpoint:decision in Task 2b (fallback to typed rejection) rather than shipping wrong-but-passing leaves.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness run_d07_matrix 2>&1 | tail -5; cargo test -p lgbm-boosting 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'train_on_subset_returning_partition' crates/lgbm-treelearner/src/learner.rs` returns the new fn definition.
    - The gbdt.rs `use_subset` branch contains NO empty `if self.objective.is_renew_tree_output() {}` block: `grep -n 'is_renew_tree_output' crates/lgbm-boosting/src/gbdt.rs` shows the subset-path usage is followed (within the same block) by a call to `renew_leaf_output` / `renew_tree_output`, not a comment-only body.
    - `cargo test -p oracle-harness run_d07_matrix` exits 0 (the 4 regression_l1 bagging cells — bag1_es0_bfa0/bfa1, bag1_es1_bfa0/bfa1 — now satisfy their assertion within MATRIX_RESIDUAL_TOL or bit-exact).
    - TEETH PROOF: with the renewal reverted to the empty block, `cargo test -p oracle-harness run_d07_matrix` PANICS on a regression_l1 bag cell (recorded in SUMMARY); with the fix it exits 0.
    - `cargo test --workspace` exits 0 with no `.ok()`-swallowed compare in the matrix.
  </acceptance_criteria>
  <done>regression_l1 + bagging leaves equal the C++ median-residual leaves; the (now-asserting) matrix cells pass; reverting the fix re-reds them.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2b (GAP B fallback — ONLY if Task 2 subset renewal proves out of scope)</name>
  <files>crates/lgbm-boosting/src/gbdt.rs (only if fallback selected)</files>
  <decision>If faithful median-residual renewal on the subset path cannot be made to match C++ within MATRIX_RESIDUAL_TOL in Task 2, how should regression_l1 + bagging behave in Phase 6?</decision>
  <context>The verifier's preferred outcome is correct leaves verified against a real-binary golden (Task 2). The documented fallback is a typed BoostingError rejection (decision-backed) instead of a silent wrong-leaf no-op. This checkpoint fires ONLY if Task 2's `<action>` STOP condition triggers. If Task 2 succeeds, SKIP this task entirely.</context>
  <options>
    <option id="ship-correct">
      <name>Ship correct subset renewal (Task 2 succeeded)</name>
      <pros>SC#3 fully met; matrix l1+bagging cells assert real correctness.</pros>
      <cons>None if achievable.</cons>
    </option>
    <option id="typed-reject">
      <name>Typed-reject regression_l1 + bagging in Phase 6, defer faithful renewal</name>
      <pros>No silent wrong leaves; decision-backed; honest scope boundary.</pros>
      <cons>regression_l1+bagging unavailable until a later phase; the 4 matrix cells become rejection-asserting (assert the typed error) instead of leaf-value cells; ROADMAP/REQUIREMENTS note required.</cons>
    </option>
  </options>
  <resume-signal>Select: ship-correct or typed-reject (and which BoostingError variant / where the ROADMAP note goes).</resume-signal>
  <verify>
    <automated>echo "checkpoint: decision recorded in 06-06-SUMMARY; no automated check"</automated>
  </verify>
  <done>A decision (ship-correct or typed-reject) is recorded in the SUMMARY; if typed-reject, the BoostingError variant and ROADMAP/REQUIREMENTS note are in place and the 4 l1+bagging matrix cells assert the typed error.</done>
</task>

<task type="auto">
  <name>Task 3 (GAP C — CR-01): constant-tree leaf_count parity + byte-exact model-text assertion</name>
  <files>crates/lgbm-model/src/tree.rs, crates/lgbm-boosting/src/gbdt.rs, crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - crates/lgbm-model/src/tree.rs:647-687 (as_constant: the zero-arg signature + `leaf_count: vec![0]`)
    - crates/lgbm-model/src/tree.rs:363-387 (Tree::to_string — already ALWAYS emits leaf_count, matching tree.cpp:363; only the VALUE is wrong)
    - crates/lgbm-boosting/src/gbdt.rs:290,362,420 (the three `Tree::as_constant(const_val)` call sites)
    - LightGBM/include/LightGBM/tree.h:232-240 (AsConstantTree(double val, int count=0): `leaf_count_[0] = count`)
    - LightGBM/src/boosting/gbdt.cpp:430,433 (`new_tree->AsConstantTree(init_scores[cur_tree_id], num_data_)` and `AsConstantTree(0, num_data_)`)
    - crates/oracle-harness/tests/fixtures/boosting/regression_l1_bag1_es0_bfa0_model.txt (the committed golden with a constant Tree=0 carrying leaf_count=12)
    - crates/oracle-harness/tests/boosting_parity.rs:195-223 (assert_model_and_pred — leaf_value-ONLY comparison; the place to add the byte-compare)
    - crates/lgbm-model/src/model_text.rs:212-260 (save() — the byte-exact model-text serializer to call for the byte-compare)
  </read_first>
  <action>
    (1) Change the signature to `pub fn as_constant(constant_value: f64, count: data_size_t) -> Tree` (import/use the `data_size_t` alias already used in the crate — `i32`) and set `leaf_count: vec![count]` instead of `vec![0]`; update the doc comment to cite tree.h:232-240 + gbdt.cpp:430/433 and that ToString always emits leaf_count (tree.cpp:363, no single-leaf write-side early return). (2) Thread `self.num_data` at all THREE gbdt.rs call sites: the class_need_train constant push (~290), the subset no-split push (~362), and the full-corpus no-split push (~420) — each becomes `Tree::as_constant(const_val, self.num_data)`. Match C++: the value is `init_scores[cur_tree_id]` on the first iteration else `0.0`, count is ALWAYS `num_data_` (gbdt.cpp:430 uses num_data_ for BOTH the init and the extend-with-zeros case). (3) In boosting_parity.rs, ADD a byte-for-byte model-text assertion: introduce `fn assert_model_text_byte_exact(prefix, corpus)` (or extend a new test `constant_tree_model_text_byte_exact`) that builds+trains the cell whose golden contains a constant tree (regression_l1_bag1_es0_bfa0), calls `lgbm_model::model_text::save(booster.model())`, and compares the emitted Tree block(s) byte-for-byte against the committed golden's Tree block(s). Compare at least the constant `Tree=0` block's full text (or the whole tree section) — NOT leaf_value only. Use the raw golden string (read_golden) without re-parsing. If header/feature-name lines legitimately differ from the committed full model (non-tree envelope), restrict the byte-compare to the `Tree=` ... blocks (document the restriction in a comment), but the `leaf_count=` line MUST be in scope.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness constant_tree_model_text_byte_exact 2>&1 | tail -5; cargo test -p lgbm-model 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn as_constant' crates/lgbm-model/src/tree.rs` shows a two-arg signature taking a count; `grep -n 'leaf_count: vec!\[0\]' crates/lgbm-model/src/tree.rs` returns ZERO lines.
    - `grep -c 'as_constant(.*self.num_data' crates/lgbm-boosting/src/gbdt.rs` returns 3 (all three call sites thread num_data).
    - For the regression_l1_bag1_es0_bfa0 golden, the emitted constant Tree=0 block contains the literal substring `leaf_count=12` (matching the golden): the new byte-compare test exits 0.
    - TEETH PROOF: reverting `leaf_count: vec![count]` back to `vec![0]` makes `cargo test -p oracle-harness constant_tree_model_text_byte_exact` FAIL with a diff at the `leaf_count=` line (recorded in SUMMARY); restoring it passes.
    - `cargo test --workspace` exits 0.
  </acceptance_criteria>
  <done>Constant-tree model text is byte-exact vs C++ (leaf_count=num_data); a test byte-compares model text and fails if leaf_count diverges. SC#1's bit-exact-model-text contract is enforced.</done>
</task>

<task type="auto">
  <name>Task 4 (GAP D — CR-02): decouple early-stop eval+decision from metric_freq + add a metric_freq>1+ES golden</name>
  <files>crates/lgbm/src/booster.rs, crates/lgbm-boosting/src/early_stopping.rs, xtask/py/boosting_oracle_capture.py, xtask/src/main.rs, crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - crates/lgbm/src/booster.rs:490-553 (the ES loop: `do_eval = (it+1)%metric_freq==0 || last`; the valid-eval + `early.update` are INSIDE `if do_eval`)
    - crates/lgbm/src/builder.rs:173-199 (first_metric_only / metric_freq / build)
    - crates/lgbm/src/booster.rs:824-847 (metric_freq_thins_eval_history — the existing cadence test, must still pass: history thinning is independent of ES eval)
    - crates/lgbm-boosting/src/early_stopping.rs:1-40 (the doc comment claiming metric_freq gates eval — misdescribes C++)
    - LightGBM/src/boosting/gbdt.cpp:551-609 (OutputMetric: `need_output = (iter % metric_freq)==0` gates ONLY logging+training-metric+the `if(need_output)` valid LOG; the valid-eval + ES decision block runs when `need_output || early_stopping_round_ > 0` i.e. EVERY iter when ES on)
    - xtask/py/boosting_oracle_capture.py:395-520 (the matrix capture pattern — capture_matrix_cell — to extend with a metric_freq=2+ES cell)
  </read_first>
  <action>
    In booster.rs, split the cadence: `metric_freq` continues to gate ONLY the history PUSH + (eventual) logging (`train_eval_history`/`valid_eval_history`/`legacy_eval_history` appends and any log), but the valid-score eval used for the ES DECISION + the `early.update(...)` call MUST run EVERY iteration when `es_enabled`, regardless of `do_eval`. Concretely: compute the valid metric `row` whenever `valid_nd > 0 && (do_eval || es_enabled)`; push it to `valid_eval_history` ONLY when `do_eval` (so metric_freq still thins the recorded history per MET-02 and the existing metric_freq_thins_eval_history test stays green); call `early.update(it, ...)` whenever `es_enabled` (every iter). This mirrors gbdt.cpp:574 where the valid-metric+ES block is `if (need_output || early_stopping_round_ > 0)` and `need_output` only guards the `Log::Info`. Then fix the early_stopping.rs doc comment (lines ~15-16): metric_freq gates eval-HISTORY logging only; under ES the eval+decision runs every iteration (cite gbdt.cpp:574). Add the matrix-style golden+test: in boosting_oracle_capture.py add a `regression` cell with `metric_freq=2` + `early_stopping_round=2` on the plateau valid set (reuse matrix_valid_corpus), emitting `regression_mf2es_model.txt`, `regression_mf2es_pred.txt`, and `regression_mf2es_best_iteration.txt`. In boosting_parity.rs add `metric_freq_gt1_with_early_stopping_matches` that trains the same config and asserts the Rust best_iteration / final tree count equals the captured value (single-output → bit-exact leaf_value over the trimmed trees; best_iteration assert against the golden). Register the three new files in the xtask `boosting_oracle_capture` post-capture checklist.
  </action>
  <verify>
    <automated>cargo test -p lgbm metric_freq_thins_eval_history 2>&1 | tail -3; cargo test -p oracle-harness metric_freq_gt1_with_early_stopping_matches 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - In booster.rs the `early.update(` call is NOT inside the `if do_eval` block: `grep -n 'early.update' crates/lgbm/src/booster.rs` and confirm (by reading 3 lines of context) it is guarded by `es_enabled`, not `do_eval`.
    - `cargo test -p lgbm metric_freq_thins_eval_history` STILL exits 0 (history thinning to 3 rounds over 9 iters @ freq 3 is unchanged — metric_freq still gates history).
    - early_stopping.rs doc no longer claims metric_freq gates the ES eval: `grep -n 'metric_freq gates eval' crates/lgbm-boosting/src/early_stopping.rs` returns ZERO lines; a corrected note citing gbdt.cpp:574 is present.
    - New golden `crates/oracle-harness/tests/fixtures/boosting/regression_mf2es_best_iteration.txt` exists and `metric_freq_gt1_with_early_stopping_matches` exits 0 asserting that best_iteration.
    - TEETH PROOF: re-gating `early.update` behind `do_eval` makes `metric_freq_gt1_with_early_stopping_matches` FAIL (different best_iteration / tree count); recorded in SUMMARY.
  </acceptance_criteria>
  <done>ES eval+decision runs every iter when ES on, independent of metric_freq; a metric_freq>1+ES golden catches divergence; the doc comment is corrected; metric_freq still thins recorded history.</done>
</task>

<task type="auto">
  <name>Task 5 (GAP E — reg_sqrt): capture + assert a real-binary reg_sqrt=1 golden</name>
  <files>xtask/py/boosting_oracle_capture.py, xtask/src/main.rs, crates/oracle-harness/tests/boosting_parity.rs, crates/lgbm/src/builder.rs</files>
  <read_first>
    - crates/lgbm-objective/src/regression.rs:30-115 (Objective::Regression{sqrt}, parse, the sqrt label transform + ConvertOutput inverse; grad/hess path)
    - .planning/phases/06-gbdt-spine-core-objectives-metrics/06-VERIFICATION.md (gap reg_sqrt: regression.rs:42-63, all goldens [reg_sqrt: 0])
    - LightGBM/src/objective/regression_objective.hpp (RegressionL2loss with sqrt_: label pre-transform `Sign(label)*sqrt(|label|)`, ConvertOutput `Sign(x)*x*x`, the reg_sqrt flag plumbing through GetGradients/BoostFromScore)
    - LightGBM/include/LightGBM/meta.h (kEpsilon=1e-15f, score_t/label_t=float)
    - xtask/py/boosting_oracle_capture.py:86-112 (base_params), :308-352 (write_layered — L1-L5 golden writer), :354-365 (l2_gh)
    - crates/oracle-harness/tests/boosting_parity.rs:195-223 (assert_model_and_pred pattern), :40-45 (SPINE_SEED / boosting_dir)
    - xtask/src/main.rs:842-955 (boosting_oracle_capture — the existence checklist to extend)
  </read_first>
  <action>
    Add a `regression sqrt` (reg_sqrt=1) end-to-end cell to the capture + a parity assertion. In boosting_oracle_capture.py: add a `regression_sqrt` objective cell using the spine corpus with `reg_sqrt=true` (lightgbm param `reg_sqrt: True` with `objective: "regression"`), writing the layered goldens via the existing write_layered path with prefix `regression_sqrt`: `regression_sqrt_gh_iter1.txt` (iter-1 grad/hess from the sqrt-transformed labels — the gh_fn must apply the same `Sign(label)*sqrt(|label|)` pre-transform the C++ does before computing L2 grad/hess), `regression_sqrt_spine_model.txt`, and `regression_sqrt_spine_pred.txt` (predict() — the ConvertOutput inverse `Sign(x)*x*x` applied). Register all three in the xtask `boosting_oracle_capture` post-capture checklist (xtask/src/main.rs). In boosting_parity.rs add `reg_sqrt_spine_matches_real_binary` that: builds the cell via TrainingBuilder with the reg_sqrt setter (add a `.reg_sqrt(true)` builder method IF one does not exist — check builder.rs; if Config already carries reg_sqrt, route through it, never fork), trains over the spine corpus, asserts (a) iter-1 grad/hess within ORACLE_TOL of regression_sqrt_gh_iter1.txt, (b) emitted leaf_value bit-exact (compare_exact_f64_bits) or within ORACLE_TOL of regression_sqrt_spine_model.txt, and (c) predict() within ORACLE_TOL of regression_sqrt_spine_pred.txt (this exercises the ConvertOutput inverse). The capture step requires the real lib_lightgbm 4.6 wheel (LGBM_CAPTURE_PYTHON) — the test must SKIP gracefully (read_golden None → return) when the golden is absent on a fresh checkout, matching the existing spine-golden skip pattern, so `cargo test` without the wheel stays green while the committed golden enforces parity in CI.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness reg_sqrt_spine_matches_real_binary 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `xtask/py/boosting_oracle_capture.py` contains a reg_sqrt / `regression_sqrt` cell: `grep -n 'reg_sqrt\|regression_sqrt' xtask/py/boosting_oracle_capture.py` returns >= 2 lines.
    - The xtask checklist in `boosting_oracle_capture` (xtask/src/main.rs) lists `regression_sqrt_spine_model.txt`, `regression_sqrt_spine_pred.txt`, `regression_sqrt_gh_iter1.txt`: `grep -c 'regression_sqrt' xtask/src/main.rs` >= 3.
    - `reg_sqrt_spine_matches_real_binary` exists and asserts grad/hess AND model leaf_value AND predict (three compare calls, none `.ok()`).
    - TEETH PROOF: once the golden is committed, breaking the sqrt ConvertOutput inverse (e.g. removing the `Sign(x)*x*x`) makes the test's predict assertion FAIL; recorded in SUMMARY. When the golden is absent the test SKIPs (returns) without failing.
    - `cargo test --workspace` exits 0 (skip-on-missing-golden keeps the no-wheel path green).
  </acceptance_criteria>
  <done>reg_sqrt=1 is exercised by a committed real-binary golden; grad/hess + ConvertOutput + predictions are asserted within tolerance and fail if the sqrt transform/inverse is wrong. SC#3's reg_sqrt claim is verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture-time python ↔ committed golden | real lib_lightgbm 4.6 wheel produces the golden text; a wrong wheel version would commit divergent goldens |
| committed golden ↔ `cargo test` | the test trusts the golden as the C++ truth; a swallowed assertion (the WR-01 defect) lets a wrong value pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | boosting_parity.rs matrix cells | mitigate | Task 1 removes all `.ok()` so no cell can pass with a wrong value; teeth-proof perturbation check required |
| T-06-06-02 | Spoofing | reg_sqrt / mf2es goldens (capture wheel version) | mitigate | xtask `boosting_oracle_capture` already version-asserts `BOOSTING_ORACLE_LIGHTGBM_VERSION` before training; new cells reuse that gate |
| T-06-06-03 | Repudiation | 06-05-SUMMARY overstated claims | mitigate | Task 1 corrects the SUMMARY/comments to match enforced coverage |
| T-06-06-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; capture uses the already-pinned lightgbm==4.6.0 wheel; no new crates added |
</threat_model>

<verification>
- `cargo test --workspace` exits 0 (no swallowed matrix assertions; skip-on-missing-golden keeps no-wheel green).
- `grep -n 'compare_within' crates/oracle-harness/tests/boosting_parity.rs | grep '\.ok()'` returns ZERO lines.
- `grep -n 'leaf_count: vec!\[0\]' crates/lgbm-model/src/tree.rs` returns ZERO lines.
- `grep -c 'as_constant(.*self.num_data' crates/lgbm-boosting/src/gbdt.rs` returns 3.
- The gbdt.rs subset branch has no empty `if is_renew_tree_output {}` block (the true-branch calls renew).
- `early.update` is guarded by `es_enabled`, not `do_eval`, in booster.rs.
- New goldens present: `regression_sqrt_spine_model.txt`, `regression_mf2es_best_iteration.txt` (after a capture run; tests skip when absent).
- Each TEETH-PROOF check (Tasks 1–5) recorded in the SUMMARY with the observed panic/diff line.
</verification>

<success_criteria>
- SC#1 (model text): a constant tree emits `leaf_count=<num_data>` and a test byte-compares model text against the golden, failing on divergence.
- SC#3 (objectives): regression_l1+bagging leaves are the median-residual; the matrix asserts every cell; reg_sqrt=1 is verified against a real-binary golden.
- SC#4 (metrics/ES): ES eval+decision runs every iter when ES on; a metric_freq>1+ES golden catches divergence; the doc comment is corrected.
- No 06-01..06-05 PLAN.md modified. LightGBM/ untouched and not git-added.
</success_criteria>

<output>
Create `.planning/phases/06-gbdt-spine-core-objectives-metrics/06-06-SUMMARY.md` when done, recording: each gap (A–E) closed, the TEETH-PROOF panic/diff observed for each, the empirically-chosen MATRIX_RESIDUAL_TOL value + its justification, whether Task 2b fired (and the decision), and the new golden filenames + their captured best_iteration values.
</output>
