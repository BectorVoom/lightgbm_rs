---
phase: 06-gbdt-spine-core-objectives-metrics
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - crates/lgbm-objective/src/lib.rs
  - crates/lgbm-objective/src/regression.rs
  - crates/lgbm-metric/src/lib.rs
  - crates/lgbm-metric/src/regression.rs
  - crates/lgbm-boosting/src/lib.rs
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm-boosting/src/score_updater.rs
  - crates/lgbm/src/lib.rs
  - crates/lgbm/src/builder.rs
  - crates/lgbm/src/booster.rs
  - xtask/src/main.rs
  - xtask/py/boosting_oracle_capture.py
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md
autonomous: true
requirements: [BST-01, BST-02, OBJ-01, OBJ-03, MET-01, API-01]
must_haves:
  truths:
    - "A user can call the lgbm builder→build→train→predict on a regression corpus and get raw scores within ~1e-6 of the C++ reference"
    - "The GBDT loop mirrors TrainOneIter ordering: BoostFromAverage → GetGradients → per-class tree → Shrinkage → UpdateScore → AddBias, with boost_from_average=true (the C++ default)"
    - "score_ is a Vec<f64> accumulated via the learner's add_prediction_to_score per-leaf scatter (bit-exact, not per-row predict)"
    - "regression L2 GetGradients (grad=score-label, hess=1) and BoostFromScore (label mean) match the reference"
    - "l2 and rmse metrics match the per-round reference values"
    - "The spine end-to-end model text (%.17g) + predictions replay bit-exact / ~1e-6 vs the real lib_lightgbm 4.6 golden"
  artifacts:
    - path: "crates/lgbm-objective/src/regression.rs"
      provides: "regression L2 GetGradients/BoostFromScore (training side)"
      contains: "gradients"
    - path: "crates/lgbm-boosting/src/gbdt.rs"
      provides: "GBDT::train_one_iter loop (TrainOneIter mirror)"
      contains: "train_one_iter"
    - path: "crates/lgbm-boosting/src/score_updater.rs"
      provides: "f64 ScoreUpdater (AddScore + training-path scatter)"
      contains: "score"
    - path: "crates/lgbm/src/builder.rs"
      provides: "public training builder resolving to lgbm-core::Config"
      contains: "Config"
    - path: "crates/oracle-harness/tests/fixtures/boosting/regression_spine_model.txt"
      provides: "real-binary spine end-to-end model-text golden"
      contains: "Tree"
  key_links:
    - from: "crates/lgbm/src/builder.rs"
      to: "lgbm_core::Config"
      via: "from_config / per-param setters resolving into Config (D-02)"
      pattern: "Config"
    - from: "crates/lgbm-boosting/src/gbdt.rs"
      to: "SerialTreeLearner::train"
      via: "per-class tree growth driven by runtime grad/hess"
      pattern: "\\.train\\("
    - from: "crates/lgbm-boosting/src/score_updater.rs"
      to: "SerialTreeLearner::add_prediction_to_score"
      via: "training-path per-leaf f64 scatter"
      pattern: "add_prediction_to_score"
---

<objective>
Deliver the minimal end-to-end vertical spine (D-14/D-15): `regression`(L2) objective + `l2`/`rmse` metrics + the GBDT loop + the f64 score updater + the public builder/`Booster`/`train`/`predict`, INCLUDING `boost_from_average=true` (the C++ regression default). This is the first moment a full builder→Config→train→score-accumulate→predict→metric loop runs and is validated against the real `lib_lightgbm` 4.6 oracle across the layered L1–L5 goldens — on ONE objective, before any axis widens.

Purpose: Prove the entire spine (loop ordering, f64 accumulation, BoostFromScore init path, model-text + predict parity, the layered-golden capture pipeline) on the simplest single-output objective. Per RESEARCH it also RESOLVES the 3 Open Questions before scaling to 40 cells: (Q1) where ConvertOutput lives, (Q2) whether `predict(raw_score=True, k)` == internal `score_` bit-for-bit (verify on this cell), (Q3) is deferred to 06-03 (regression_l1 PercentileFun — not on this cell).
Output: A working `regression` train→predict slice; the spine L1–L5 goldens captured + replaying; the `boosting-oracle-capture` pipeline implemented for the spine; the three open questions resolved + recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-CONTEXT.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-RESEARCH.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-PATTERNS.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-VALIDATION.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: regression L2 objective + l2/rmse metrics (training-side g/h + reductions)</name>
  <files>crates/lgbm-objective/src/regression.rs, crates/lgbm-objective/src/lib.rs, crates/lgbm-metric/src/regression.rs, crates/lgbm-metric/src/lib.rs</files>
  <read_first>
    - `crates/lgbm-model/src/objective.rs` — existing `ObjectiveKind` enum (:44) + `convert_output` (:198, identity for regression) + `Sign` helper (:34); the C++-citation doc-header idiom (:1-31) to copy
    - `crates/lgbm-core/src/types.rs` — `ScoreT`/`LabelT` = f32, `K_EPSILON` (:31)
    - `crates/lgbm-objective/src/error.rs`, `crates/lgbm-metric/src/error.rs` — the 06-01 error enums to return
    - `crates/oracle-harness/src/comparator.rs` — `compare_within(.., ORACLE_TOL)` (:15) + `compare_exact_f64_bits` for the layer precision contract
    - `LightGBM/src/objective/regression_objective.hpp` — L2 `GetGradients` (:127-142: `grad=(score_t)(score-label)`, `hess=1.0f`; weighted inner f32 cast), `BoostFromScore` (:173-190: `suml=Σlabel`, return `suml/num_data`, `if(!deterministic_)` strips the OMP reduction — use ordered sum), `ConvertOutput` identity (:148), `IsConstantHessian=true`
    - `LightGBM/src/metric/regression_metric.hpp` — `l2` LossOnPoint `(score-label)^2` Σ/sumw (:142), `rmse` `sqrt(Σ/sumw)` (:123-130), `l1` `|score-label|` (:177); `factor_to_bigger_better=-1`
  </read_first>
  <behavior>
    - Test: L2 `get_gradients(score=&[f64], label=&[f32], grad, hess)` → `grad[i] = (score[i]-label[i] as f64) as f32`, `hess[i]=1.0f32` (no-weights path). Bit-exact f32 vs a hand-computed row.
    - Test: L2 `boost_from_score(label)` = ordered f64 sum of labels / num_data (label mean), `deterministic=true` ordered reduction.
    - Test: `l2` metric over (scores,labels) = `Σ(s-l)^2 / num_data`; `rmse` = `sqrt(l2)`, within ORACLE_TOL of hand-computed.
  </behavior>
  <action>
    In `lgbm-objective` add a `Regression { sqrt: bool }` variant to a new training-side `Objective` enum (mirroring the C++ string-keyed `CreateObjectiveFunction`; PATTERNS §enum-dispatch factory) with `get_gradients`, `boost_from_score`, and a `parse`/`from_config` constructor. Implement L2 per regression_objective.hpp:127-142 verbatim (no-weights + weighted with the inner f32 cast `(score_t)((score_t)(score-label)*w)`); `hess=1.0f32`; `boost_from_score` = ordered f64 label-mean (`Σlabel/num_data`, NO OpenMP — `deterministic=true`); `reg_sqrt` Init/ConvertOutput inversion per :116-122 (only if `sqrt=true`). Reuse `lgbm-model::ObjectiveKind::convert_output` for the predict-side transform (Open-Q1 recommendation: keep ConvertOutput in lgbm-model; lgbm-objective owns the training side only) — re-export or call it; do NOT re-port. In `lgbm-metric` add an `Metric` enum with `L2`, `Rmse`, `L1` variants (l1 included now — its formula is trivial and the spine metrics list is `l2`/`rmse`; l1 metric pairs with regression_l1 in 06-03) each with `eval(&self, scores: &[f64], labels: &[f32]) -> f64` (Σ/sumw reductions verbatim from regression_metric.hpp) + `factor_to_bigger_better()` (-1). Cite the exact C++ lines in every module doc-header (faithful-mirror discipline).
  </action>
  <verify>
    <automated>cargo test -p lgbm-objective gradients && cargo test -p lgbm-metric eval</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-objective gradients` exits 0: L2 g/h bit-exact f32 + BoostFromScore label-mean ordered sum.
    - `cargo test -p lgbm-metric eval` exits 0: l2/rmse/l1 reductions within `ORACLE_TOL` of hand-computed.
    - `grep -q 'regression_objective.hpp' crates/lgbm-objective/src/regression.rs` (C++ citation present).
    - No f64 score/label TYPE alias introduced (f32 contract); the score arg is a `&[f64]` accumulator buffer, grad/hess are f32 (per RESEARCH Pattern 2).
  </acceptance_criteria>
  <done>regression L2 training-side g/h + BoostFromScore and l2/rmse(+l1) metrics implemented, unit-tested against hand-computed values, citing the C++ source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GBDT loop + f64 ScoreUpdater + boost_from_average (BST-01/BST-02)</name>
  <files>crates/lgbm-boosting/src/gbdt.rs, crates/lgbm-boosting/src/score_updater.rs, crates/lgbm-boosting/src/lib.rs</files>
  <read_first>
    - `crates/lgbm-treelearner/src/learner.rs` — `train(grad,hess,is_first_tree)→Tree` (:265) + `add_prediction_to_score`/`renew_tree_output` (added 06-01); the `train`→`train_inner` driver-layering idiom (PATTERNS §gbdt.rs analog)
    - `crates/lgbm-model/src/tree.rs` — `Tree::shrinkage`/`add_bias` (added 06-01), `leaf_value: Vec<f64>`, `num_leaves`
    - `crates/lgbm-model/src/ensemble.rs` — `GbdtModel` (:35) container the loop fills + `predict_raw` (:90)
    - `crates/lgbm-core/src/types.rs` — `K_EPSILON` (:31) for the `|init|>kEpsilon` BoostFromAverage/AddBias gates
    - `LightGBM/src/boosting/gbdt.cpp` — `TrainOneIter` (:344-452, RESEARCH §Pattern 1 the exact order), `BoostFromAverage` (:319-342), `Boosting`/`GetGradients` (:220-235), `UpdateScore` (:491-520), the per-class loop (single class here, K=1)
    - `LightGBM/src/boosting/score_updater.hpp` — f64 `score_` (:27-128), `AddScore(double,class)` (:54-61) for BoostFromAverage, training-path delegate to learner scatter (:88-92)
    - 06-RESEARCH.md §Pattern 1 "Critical ordering note" + Pitfall 5 (init via BoostFromAverage→AddScore, AddBias model-text only — never double-add)
  </read_first>
  <behavior>
    - Test: with `boost_from_average=true`, after iter 0 the score updater's `score_[i]` == `init_score` for all rows before any tree adds (init enters via `AddScore`, NOT `AddBias`).
    - Test: `Shrinkage(lr)` is applied to the tree BEFORE `UpdateScore`; `AddBias(init)` is applied to tree-0 AFTER `UpdateScore` and does NOT change `score_`.
    - Test: a degenerate 1-leaf tree (no split) sets `should_continue=false` path correctly (no score change beyond the constant).
    - Test: `score_` is `Vec<f64>` and the training-path add routes through `add_prediction_to_score` (not a per-row predict).
  </behavior>
  <action>
    In `lgbm-boosting/src/score_updater.rs` implement `ScoreUpdater` holding `score_: Vec<f64>` (length `num_data * num_class`, class-major; init from Dataset `init_score` metadata else zero — score_updater.hpp:30-46), with `add_constant(val, cur_tree_id)` (AddScore over all rows, :54-61) and `add_tree_train_path(&learner, &tree, cur_tree_id)` delegating to `learner.add_prediction_to_score` over the class offset slice (the bit-exact hot path; RESEARCH Anti-Pattern: NOT a per-row walk). In `lgbm-boosting/src/gbdt.rs` implement `GBDT::train_one_iter` as the verbatim `TrainOneIter` mirror (RESEARCH Pattern 1): (1) `BoostFromAverage(class)` on iter 0 — if `models_` empty && !has_init_score && (boost_from_average || num_features==0): `init = objective.boost_from_score(class)`; if `|init|>K_EPSILON` add to TRAIN score updater (+ valid updaters); return init; (2) `objective.get_gradients(score_, grad, hess)`; (3) per `cur_tree_id`: `tree = learner.train(grad+offset, hess+offset, is_first_tree)`; if `num_leaves>1`: `learner.renew_tree_output(...)` (no-op for L2), `tree.shrinkage(learning_rate)`, `score_updater.add_tree_train_path(...)`, `if |init|>K_EPSILON { tree.add_bias(init) }`; push to `models_`. Apply the exact ordering note (Shrinkage before UpdateScore, AddBias after, init via BoostFromAverage not AddBias — Pitfall 5; do not double-add init). Use the `train`→`train_inner`/snapshot-returning-variant layering (PATTERNS) so the loop can emit per-iter score snapshots (L2 golden) and per-row g/h snapshots (L1). V5: validate grad/hess/score lengths up front, return `BoostingError::LengthMismatch`. K=1 (single-output) for this slice; per-class generalization is 06-04. Single-threaded deterministic core.
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting train_one_iter score_accumulation</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-boosting` exits 0 incl. the ordering tests (init via AddScore not AddBias; Shrinkage-before-UpdateScore; f64 score_).
    - `grep -q 'add_prediction_to_score' crates/lgbm-boosting/src/score_updater.rs` (training-path scatter wired, not a per-row predict).
    - `grep -q 'boost_from_score' crates/lgbm-boosting/src/gbdt.rs` (BoostFromAverage wired) and the init-score is added once.
    - `grep cubecl crates/lgbm-boosting/Cargo.toml` empty (CMP-01 preserved).
  </acceptance_criteria>
  <done>The GBDT loop reproduces TrainOneIter ordering with f64 accumulation + boost_from_average, driving the Phase-5 learner; per-iter score + per-row g/h snapshots available for the layered goldens.</done>
</task>

<task type="auto">
  <name>Task 3: Public builder/Booster/train/predict + spine L1–L5 capture + end-to-end golden replay (API-01, D-06/D-07 spine cell)</name>
  <files>crates/lgbm/src/{builder.rs,booster.rs,lib.rs}, xtask/src/main.rs, xtask/py/boosting_oracle_capture.py, crates/oracle-harness/tests/boosting_parity.rs, crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md</files>
  <read_first>
    - `crates/lgbm/src/builder.rs`, `crates/lgbm/src/booster.rs`, `crates/lgbm/src/lib.rs` — 06-01 stubs to fill
    - `crates/lgbm-core/src/config/` — the `Config` field reference + `from_params`/alias table + CHECK validation (D-02 single source of truth; the builder resolves INTO this, never forks defaults)
    - `crates/lgbm-model/src/ensemble.rs` — `GbdtModel` + `predict_raw` (:90) the Booster predict delegates to; `num_tree_per_iteration` stride (class-major)
    - `crates/lgbm-model/src/objective.rs` — `convert_output` (:198) for transformed predict (identity for regression)
    - `xtask/src/main.rs` — `learner_oracle_capture` (:739-810) skeleton + the `boosting-oracle-capture` stub from 06-01; `$LGBM_CAPTURE_PYTHON` resolve + wheel version-assert
    - `xtask/py/learner_oracle_capture.py` — the pip-wheel capture script to model `boosting_oracle_capture.py` on
    - `crates/oracle-harness/tests/boosting_parity.rs` — the 06-01 scaffold to fill (`spine_end_to_end`, `score_accumulation`)
    - 06-RESEARCH.md §Validation Architecture L1–L5 capture sources + Open Questions Q1/Q2 + Assumption A4 (verify `predict(raw_score,k)` == internal score_ on THIS cell)
    - PATTERNS §"No Analog Found" — the builder is the one net-new shape, bounded by Config (owned-builder idiom, `Self`-returning setters → `build()→Result`)
  </read_first>
  <action>
    Implement the D-01 builder in `lgbm/src/builder.rs` as a standard owned Rust builder (`Self`-returning per-param setters for the in-scope params: objective, num_iterations, learning_rate, num_leaves, metric, seeds, determinism flags, boost_from_average — D-03 full surface) PLUS a `from_config(Config)` escape hatch; every setter resolves into `lgbm-core::Config` via its `from_params`/setters (D-02 — NEVER fork defaults/aliases; route through the verbatim alias table + CHECK validation, returning `BoostingError`/typed `Result`, never panic — Security V5). Implement `Booster` (`lgbm/src/booster.rs`) wrapping the trained `GbdtModel` + the D-05 fields (`best_iteration: i32` + eval-history map, populated trivially here — full early-stop in 06-05) with `predict(&self, X) -> Vec<f32>` delegating to `GbdtModel::predict_raw` → `ObjectiveKind::convert_output` (Phase-3 path). Implement `train(...)`/`predict(...)` free fns + `Dataset` re-export in `lgbm/src/lib.rs` (facade). Implement the `boosting-oracle-capture` xtask body + `xtask/py/boosting_oracle_capture.py` for the SPINE cell only (regression, ~10-20 iters, low num_leaves, bagging off, early-stop off, bfa on — D-08/D-09): emit L1 `regression_gh_iter1.txt`/`regression_gh_iterN.txt` (per-row g/h at iter 1 + a later iter, D-10; score-derivation route per RESEARCH L1 recommendation), L2 `regression_scores.txt` (per-iteration accumulated raw score, D-11; `predict(raw_score=True, num_iteration=k)` per k), L3 `regression_metrics.txt` (`record_evaluation`/`evals_result`), L5 `regression_spine_model.txt` (`save_model`) + `regression_spine_pred.txt` (`predict`). Version-assert the wheel; write under tracked `crates/oracle-harness/tests/fixtures/boosting/`; idempotent (`git diff` empty on re-capture). Fill `boosting_parity.rs`: `spine_end_to_end` replays the model text bit-exact (`compare_exact_f64_bits` on leaf values) + predictions within `ORACLE_TOL`; `gradients` replays L1; `score_accumulation` replays L2. RESOLVE Open-Q2/A4: assert the Rust internal `score_` after k iters equals `predict(raw_score,k)` on this cell; record in the SUMMARY whether L2 is bit-exact (use for all cells) or ~1e-6 (lean on L5). NEVER `git add LightGBM/`.
  </action>
  <verify>
    <automated>cargo test -p lgbm public_api && cargo test -p oracle-harness --test boosting_parity spine_end_to_end score_accumulation gradients</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm public_api` exits 0: builder→Config→train→predict round-trips on the regression corpus.
    - `cargo test -p oracle-harness --test boosting_parity spine_end_to_end` exits 0: `regression_spine_model.txt` leaf values replay bit-exact (`compare_exact_f64_bits`) and `regression_spine_pred.txt` within `ORACLE_TOL`.
    - `score_accumulation` + `gradients` tests exit 0 (L2 + L1 replay).
    - `git diff --stat crates/oracle-harness/tests/fixtures/boosting/` is empty after a second `cargo run -p xtask -- boosting-oracle-capture` (idempotent).
    - The builder routes through `Config`: `grep -q 'Config' crates/lgbm/src/builder.rs` and no duplicated default/alias table in `lgbm` (D-02).
    - SUMMARY records the Open-Q1 (ConvertOutput location) + Open-Q2/A4 (L2 bit-exact vs ~1e-6) resolutions.
  </acceptance_criteria>
  <done>The full spine runs end-to-end through the public API and matches the real lib_lightgbm 4.6 spine golden across L1/L2/L5; the capture pipeline is idempotent; the 2 spine-relevant open questions are resolved.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (06-02) produces:
- `Objective::Regression { sqrt }` variant + training-side `get_gradients`/`boost_from_score` in `lgbm-objective`.
- `Metric::{L2, Rmse, L1}` variants + `eval`/`factor_to_bigger_better` in `lgbm-metric`.
- `GBDT` (`train_one_iter` TrainOneIter mirror), `ScoreUpdater` (f64 `score_`) in `lgbm-boosting`.
- Public `train`/`predict` fns, the training `builder` (→ `Config`), `Booster` (with `best_iteration` + eval-history fields) in `lgbm`.
- `boosting-oracle-capture` xtask body + `xtask/py/boosting_oracle_capture.py` (spine cell).
- Spine goldens: `regression_gh_iter1.txt`, `regression_gh_iterN.txt`, `regression_scores.txt`, `regression_metrics.txt`, `regression_spine_model.txt`, `regression_spine_pred.txt`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public builder/train/predict → engine | user Config params + training matrix/labels cross the API into the loop/objective/metric |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | builder params (e.g. learning_rate<=0, num_iterations<=0, num_leaves) | mitigate | Builder resolves through `lgbm-core::Config` CHECK validation (CFG-03, D-02); invalid combos return `BoostingError`/typed `Result`, never panic. |
| T-06-02-02 | Tampering | NaN/Inf labels or mismatched X/label dimensions at train | mitigate | V5 length/dimension checks at the `train` boundary before FP work; typed `Result`. |
| T-06-02-03 | Denial of Service | panic-as-DoS in the FFI-adjacent public surface | mitigate | No `unwrap`/`panic` on bad input in builder/train/predict; all error paths typed (mirrors project error-handling idiom). |
| T-06-02-SC | Tampering | pip wheel (`lightgbm==4.6.0`) used at capture | accept | Version-asserted at capture (`assert lightgbm.__version__=='4.6.0'`); test-time oracle only, not a shipped dependency (RESEARCH Package Legitimacy Audit: not applicable). Capture is human-gated/idempotent. |
</threat_model>

<verification>
- `cargo test --workspace` green (spine slice live; prior suites unregressed).
- Spine L1/L2/L5 goldens replay bit-exact / ~1e-6 on the cubecl-cpu anchor; capture idempotent.
- Open-Q1 + Open-Q2/A4 resolved + recorded.
- CMP-01 holds; `LightGBM/` never `git add`ed.
</verification>

<success_criteria>
- ROADMAP SC#1 (Rust-native API train+predict within ~1e-6) demonstrated for regression.
- SC#2 (loop ordering + f64 score updater + boost_from_average) demonstrated.
- SC#3 (regression GetGradients/ConvertOutput/BoostFromScore) + SC#4 (l2/rmse metrics) demonstrated for the spine objective.
</success_criteria>

<output>
Create `.planning/phases/06-gbdt-spine-core-objectives-metrics/06-02-SUMMARY.md` when done.
</output>
