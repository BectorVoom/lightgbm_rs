---
phase: 06-gbdt-spine-core-objectives-metrics
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - crates/lgbm-objective/src/lib.rs
  - crates/lgbm-objective/src/multiclass.rs
  - crates/lgbm-metric/src/lib.rs
  - crates/lgbm-metric/src/multiclass.rs
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm-boosting/src/score_updater.rs
  - crates/lgbm/src/builder.rs
  - crates/lgbm/src/booster.rs
  - xtask/py/boosting_oracle_capture.py
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md
autonomous: true
requirements: [OBJ-01, OBJ-03, MET-01, BST-01]
must_haves:
  truths:
    - "multiclass (softmax) GetGradients gathers rec[k]=score[num_data*k+i] across classes, applies Common::Softmax (max-subtraction), and produces per-class grad/hess matching the reference"
    - "multiclassova holds num_class independent binary objectives, each with is_pos=(label==i), matching the reference"
    - "the GBDT loop grows num_class trees per iteration with the class-major score/grad/hess layout (offset = num_data*cur_tree_id)"
    - "multi_logloss matches the per-round reference"
    - "class_need_train / degenerate constant trees are honored; tree count stays iter*num_class"
    - "multiclass/multiclassova end-to-end model text + predictions replay vs the real lib_lightgbm 4.6 golden"
  artifacts:
    - path: "crates/lgbm-objective/src/multiclass.rs"
      provides: "multiclass softmax + multiclassova per-class grad/hess + BoostFromScore"
      contains: "softmax"
    - path: "crates/lgbm-metric/src/multiclass.rs"
      provides: "multi_logloss"
      contains: "multi_logloss"
    - path: "crates/oracle-harness/tests/fixtures/boosting/multiclass_spine_model.txt"
      provides: "real-binary multiclass end-to-end model golden"
      contains: "Tree"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs"
      to: "per cur_tree_id in 0..num_class"
      via: "class-major offset = num_data*cur_tree_id"
      pattern: "num_data \\* cur_tree_id|offset"
    - from: "crates/lgbm-objective/src/multiclass.rs"
      to: "lgbm_model::ObjectiveKind::convert_output (softmax/ova)"
      via: "predict-side transform reuse"
      pattern: "convert_output|softmax"
---

<objective>
Add the per-class structural axis (D-16, D-17 step 2): `multiclass` (softmax) + `multiclassova` objectives growing `num_class` trees per iteration over the class-major score/grad/hess layout, plus `multi_logloss`, on the proven single-output loop. This is the one structural change to the loop — generalizing the K=1 per-class loop of 06-02 to K=num_class — done only after the single-output spine + breadth are locked.

Purpose: Prove the per-class tree layout and the strided softmax gather, which a row-major `[row][class]` layout would silently diverge on (RESEARCH Pattern 4). After this, all five core objectives (regression, regression_l1, binary, multiclass, multiclassova) are complete end-to-end, and the only remaining axes are bagging + early stopping (06-05).
Output: multiclass + multiclassova complete; multi_logloss; the loop generalized to num_class trees/iter with class-major layout + class_need_train; multiclass/ova L1–L5 goldens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-CONTEXT.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-RESEARCH.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-PATTERNS.md
@.planning/phases/06-gbdt-spine-core-objectives-metrics/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: multiclass (softmax) + multiclassova objectives + multi_logloss</name>
  <files>crates/lgbm-objective/src/{multiclass.rs,lib.rs}, crates/lgbm-metric/src/{multiclass.rs,lib.rs}</files>
  <read_first>
    - `crates/lgbm-objective/src/binary.rs` — multiclassova holds `num_class` binary objectives; reuse the 06-03 binary grad/hess
    - `crates/lgbm-objective/src/lib.rs` + `error.rs` — `LabelOutOfRange { label, num_class }` (Security V5) for the multiclass label range check
    - `crates/lgbm-model/src/objective.rs` — `ObjectiveKind::Multiclass`/`MulticlassOva` + `convert_output` softmax (:198) to reuse for predict + multi_logloss
    - `crates/lgbm-core/src/types.rs` — `K_EPSILON` (:31) for BoostFromScore + logloss floor
    - `LightGBM/src/objective/multiclass_objective.hpp` — softmax (:24-181: `factor_=num_class/(num_class-1)`, gather `rec[k]=score[num_data*k+i]`, `Common::Softmax(&rec)`, `grad=p-1 if label==k else p`, `hess=factor_*p*(1-p)`), `BoostFromScore(class_id)` (:155-157: `log(max(kEps, class_init_probs_[k]))`), `ConvertOutput` (:132), `NumModelPerIteration=num_class` (:149); multiclassova (:186-276: per-class BinaryLogloss with `is_pos=(label==i)`), label range check (:62 `Log::Fatal`)
    - `LightGBM/include/LightGBM/utils/common.h` — `Softmax` (:571-600: `wmax=max`, `exp(rec-wmax)`, normalize) VERBATIM
    - `LightGBM/src/metric/multiclass_metric.hpp` — `multi_logloss` (:167-174: `rec[label]>kEps ? -log(rec[label]) : -log(kEps)`, ConvertOutput first :74)
    - 06-RESEARCH.md Pattern 4 (class-major layout) + Pitfall 6 (class_need_train)
  </read_first>
  <behavior>
    - Test: `Common::Softmax([a,b,c])` matches the max-subtraction algorithm bit-exact f64 vs hand-computed.
    - Test: multiclass `get_gradients` over a 3-class row: gathers strided `rec[k]=score[num_data*k+i]`, softmaxes, `grad[idx]=p-1` if label==k else `p`, `hess[idx]=factor_*p*(1-p)`, f32-cast.
    - Test: multiclass `boost_from_score(k)` = `log(max(kEps, count(label==k)/num_data))`.
    - Test: multiclassova class i's g/h == binary objective with `is_pos=(label==i)`.
    - Test: multi_logloss = `Σ -log(rec[label]) / num_data` (ConvertOutput softmax/ova applied first).
    - Test: out-of-range multiclass label (`>= num_class`) → `ObjectiveError::LabelOutOfRange` (not panic).
  </behavior>
  <action>
    Add `multiclass.rs` to `lgbm-objective`: `Multiclass { num_class }` + `MulticlassOva { num_class, sigmoid }` variants. Port `Common::Softmax` verbatim (common.h:571-600, max-subtraction). multiclass `get_gradients` per multiclass_objective.hpp:24-181 with the STRIDED class-major gather (`rec[k]=score[num_data*k+i]`; Pattern 4 — a row-major layout diverges); `factor_=num_class/(num_class-1.0)`; f32-cast grad/hess. `boost_from_score(class_id)` per :155-157 (`class_init_probs_[k]=count/num_data`). Validate `label ∈ [0,num_class)` at Init → `ObjectiveError::LabelOutOfRange` (mirror :62 `Log::Fatal` as Result, never panic — Security V5). multiclassova: hold `num_class` binary objectives (reuse 06-03 `binary`), each `is_pos=(label==i)`, `get_gradients` calls each at `offset=num_data*i` (:228-233). Reuse `ObjectiveKind::convert_output` (softmax / per-class sigmoid) for predict. Add `multiclass.rs` to `lgbm-metric`: `MultiLogloss` per multiclass_metric.hpp:167-174 (ConvertOutput first). Set `num_model_per_iteration()==num_class` so the loop knows K. Cite all C++ lines.
  </action>
  <verify>
    <automated>cargo test -p lgbm-objective multiclass softmax && cargo test -p lgbm-metric multi_logloss</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-objective multiclass softmax` exits 0: strided softmax gather + per-class grad/hess + ova + BoostFromScore; out-of-range label → `LabelOutOfRange`.
    - `cargo test -p lgbm-metric multi_logloss` exits 0 within ORACLE_TOL.
    - `grep -q 'multiclass_objective.hpp' crates/lgbm-objective/src/multiclass.rs` (citation).
    - `grep -q 'num_data \* k\|num_data \* cur_tree' crates/lgbm-objective/src/multiclass.rs` (strided class-major gather present, not row-major).
  </acceptance_criteria>
  <done>multiclass softmax + multiclassova + multi_logloss implemented with the strided class-major layout and the label-range guard, unit-validated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize the GBDT loop to num_class trees/iter (class-major layout + class_need_train)</name>
  <files>crates/lgbm-boosting/src/{gbdt.rs,score_updater.rs}, crates/lgbm/src/{builder.rs,booster.rs}</files>
  <read_first>
    - `crates/lgbm-boosting/src/gbdt.rs` — the 06-02 K=1 per-class loop to generalize
    - `crates/lgbm-boosting/src/score_updater.rs` — `score_` is `num_data*num_class`; class offset `num_data*cur_tree_id`
    - `crates/lgbm-model/src/ensemble.rs` — `GbdtModel.num_tree_per_iteration` (class-major stride `trees[i*ntpi+k]`)
    - `crates/lgbm/src/booster.rs` — Booster predict must use the class-major stride for multiclass
    - `LightGBM/src/boosting/gbdt.cpp` — the per-class loop (:388 `offset=cur_tree_id*num_data`, :390 class_need_train gate, :419-434 degenerate constant tree), BoostFromAverage per class (:319-342)
    - 06-RESEARCH.md Pattern 4 + Pitfall 6 (class_need_train / tree count == iter*K)
  </read_first>
  <behavior>
    - Test: with num_class=3, one iteration pushes exactly 3 trees; `score_`/`grad`/`hess` are length `num_data*3` and each class k uses offset `num_data*k`.
    - Test: BoostFromAverage runs per class on iter 0 (3 init scores).
    - Test: a class with `class_need_train==false` pushes a constant (1-leaf) tree; tree count stays `iter*num_class` regardless.
    - Test: multiclass predict uses the class-major tree stride (`trees[i*num_class + k]`).
  </behavior>
  <action>
    Generalize `GBDT::train_one_iter` (06-02) so the per-class loop runs `cur_tree_id in 0..num_model_per_iteration` (=num_class for multiclass/ova, 1 otherwise), using `offset = num_data*cur_tree_id` for the grad/hess/score slices (class-major, Pattern 4). Run `BoostFromAverage(cur_tree_id)` per class on iter 0. Honor `class_need_train_[k] = objective.class_need_train(k)` — push a constant 1-leaf tree (via `Tree::as_constant`/equivalent + AddBias path, gbdt.cpp:419-434) when false; keep `models_` length == `iter*num_class` (Pitfall 6). Set `GbdtModel.num_tree_per_iteration = num_class` so the existing Phase-3 predict stride is correct; update `Booster::predict` to the class-major stride for multiclass output (delegating to `GbdtModel::predict_raw` + `convert_output` softmax/ova). The custom branch (06-03) generalizes too (closure returns `num_data*num_class`). Do not change the single-output behavior (num_class=1 path stays identical — regression/regression_l1/binary regression-tested). Wire `num_class`/`objective` through the builder→Config (D-02/D-03).
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting multiclass_loop && cargo test -p lgbm-boosting score_accumulation</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-boosting multiclass_loop` exits 0: 3 trees/iter, class-major offsets, per-class BoostFromAverage, class_need_train constant trees, tree count == iter*K.
    - `cargo test -p lgbm-boosting score_accumulation` (single-output) still green — no regression of the 06-02/06-03 path.
    - `grep -q 'num_data \* cur_tree_id\|num_data \* k' crates/lgbm-boosting/src/gbdt.rs` (class-major offset).
    - `grep cubecl crates/lgbm-boosting/Cargo.toml` empty (CMP-01).
  </acceptance_criteria>
  <done>The loop grows num_class trees/iter with the class-major layout, per-class BoostFromAverage, and class_need_train constant trees; single-output path unregressed.</done>
</task>

<task type="auto">
  <name>Task 3: Capture + replay multiclass / multiclassova layered goldens (L1–L5 spine cells)</name>
  <files>xtask/py/boosting_oracle_capture.py, crates/oracle-harness/tests/boosting_parity.rs, crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md</files>
  <read_first>
    - `xtask/py/boosting_oracle_capture.py` — the per-objective capture (06-02/06-03) to extend; multiclass needs class-major reshape for `predict(raw_score=True)`
    - `crates/oracle-harness/tests/boosting_parity.rs` — the parametrized per-objective replay to add multiclass/ova to
    - `crates/oracle-harness/src/comparator.rs` — comparators per layer
    - `crates/oracle-harness/tests/fixtures/boosting/REFERENCE_MANIFEST.md` — Phase-6 section
    - 06-CONTEXT.md D-06/D-08/D-09 (K-class synthetic corpus, ~10-20 iters, low num_leaves) + D-16
    - 06-RESEARCH.md §Validation Architecture (L2 class-major for multiclass)
  </read_first>
  <action>
    Extend the capture for `multiclass` and `multiclassova` spine cells (bagging off, early-stop off, bfa on — the D-07 spine cells) with a K-class synthetic corpus (D-08: integer labels in `[0,num_class)`; ~10-20 iters, low num_leaves per D-09): emit L1 `<obj>_gh_iter1.txt`/`<obj>_gh_iterN.txt` (per-row, per-class g/h, score-derivation route), L2 `<obj>_scores.txt` (`predict(raw_score=True, num_iteration=k)`, class-major reshape), L3 `<obj>_metrics.txt` (multi_logloss), L5 `<obj>_spine_model.txt` (num_class*iters trees) + `<obj>_spine_pred.txt` (softmax/ova probabilities). Parametrize `boosting_parity.rs` to replay `multiclass` and `multiclassova` per layer (model text bit-exact via `compare_exact_f64_bits`; predictions/g/h/metrics within ORACLE_TOL or bit-exact per the 06-02-resolved L2 contract). Assert the tree count == `iters*num_class` and the class-major stride. Version-assert wheel; idempotent regen; populate manifest. NEVER `git add LightGBM/`.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test boosting_parity multiclass multiclassova</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p oracle-harness --test boosting_parity multiclass multiclassova` exits 0 (L1 g/h, L2 class-major scores, L3 multi_logloss, L5 model+predict).
    - The replayed model has exactly `iters*num_class` trees in class-major order.
    - `git diff --stat crates/oracle-harness/tests/fixtures/boosting/` empty after a second capture (idempotent).
    - Manifest Phase-6 section documents the multiclass/ova cells.
  </acceptance_criteria>
  <done>multiclass + multiclassova pass the layered L1–L5 golden replay against the real binary; the class-major per-class tree layout is proven end-to-end; capture idempotent.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This plan (06-04) produces:
- `Objective::Multiclass { num_class }`, `Objective::MulticlassOva { num_class, sigmoid }` + `Common::Softmax` port in `lgbm-objective`.
- `Metric::MultiLogloss` in `lgbm-metric`.
- The generalized `GBDT::train_one_iter` (num_class trees/iter, class-major layout, per-class BoostFromAverage, class_need_train constant trees).
- `Booster::predict` class-major stride for multiclass; `GbdtModel.num_tree_per_iteration = num_class` wiring.
- Goldens: `multiclass_*`, `multiclassova_*` (L1/L2/L3/L5).
- All five core objectives (OBJ-01) now complete end-to-end.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training labels → multiclass objective | integer class labels index `class_init_probs_` / gather — OOB risk if out of range |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Tampering/DoS | multiclass label `>= num_class` or `< 0` → OOB index in softmax gather / class_init_probs | mitigate | Validate `label ∈ [0,num_class)` at objective Init → `ObjectiveError::LabelOutOfRange` (mirror multiclass_objective.hpp:62 `Log::Fatal`), never index OOB / panic (Security V5). |
| T-06-04-02 | Tampering | `num_class < 1` or grad/hess length != `num_data*num_class` | mitigate | Config CHECK (`num_class>=1`) + loop-boundary length validation → typed `Result`. |
| T-06-04-SC | Tampering | pip wheel at capture | accept | Version-asserted; test-time oracle only (as prior plans). |
</threat_model>

<verification>
- `cargo test --workspace` green (all 5 core objectives + custom + all core single-output/multiclass metrics live; single-output path unregressed).
- multiclass/ova layered goldens replay; tree count == iter*K; class-major proven.
- CMP-01 holds; `LightGBM/` never `git add`ed.
</verification>

<success_criteria>
- ROADMAP SC#3 complete for objectives: all five core objectives (regression, regression_l1, binary, multiclass, multiclassova) within ~1e-6.
- SC#4 advanced: multi_logloss matches the reference.
- SC#2 (per-class trees) demonstrated with the class-major layout.
</success_criteria>

<output>
Create `.planning/phases/06-gbdt-spine-core-objectives-metrics/06-04-SUMMARY.md` when done.
</output>
