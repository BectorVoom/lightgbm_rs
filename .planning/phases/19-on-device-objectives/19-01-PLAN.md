---
phase: 19-on-device-objectives
plan: 01
type: execute
wave: 2
depends_on: ["19-00"]
files_modified:
  - crates/lgbm-compute/src/kernels/objective_regression.rs
  - crates/oracle-harness/tests/objective_parity_regression.rs
autonomous: true
requirements: [ODL-05]
must_haves:
  truths:
    - "All SIX regression grad/hess kernels (L2, L1, Quantile, Huber, Fair, Poisson) run on the cpu f64 fold and are BIT-EXACT (compare_exact_u32 on f32 bits) to both the lgbm-objective f64 anchor and the real-lib_lightgbm goldens (regression_gh, regression_l1_gh, quantile_gh, huber_gh, fair_gh, poisson_gh) — per D-01/D-05"
    - "diff = score - label is the shared per-row form; the <USE_WEIGHT> template maps to a #[comptime] bool that compiles the weight branch out with no runtime cost (per D-07, Pattern 1)"
    - "BoostFromScore composes primitives ONLY (D-08): L2/Huber/Fair mean via reduce_sum_f64_on or dot_product_f64_on; L1/Quantile median via percentile_unweighted_f32_on; Poisson via reduce_sum_f64_on + reduce_min_f64_on label-check THEN safe_log(mean) — never a hand-rolled reduce"
    - "RenewTreeOutput (L1/Quantile per-leaf median) is a HOST-ORCHESTRATED loop over leaves calling percentile_{un,}weighted_f32_on per leaf — there is NO percentile_device per-leaf kernel (Discrepancy 1); parity-identical to the CUDA one-block-per-leaf result"
    - "ConvertOutput inverse-link is elementwise: sign(x)·x² when sqrt_, exp for Poisson, pass-through otherwise — anchored to the host ObjectiveKind::convert_output"
    - "weight-branch equivalence holds: use_weight=true with all-1.0 weights is bit-identical to use_weight=false; and twice-run determinism holds on cpu"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/objective_regression.rs"
      provides: "6 #[cube] regression grad/hess kernels + launchers, ConvertOutput, BoostFromScore composition, host-orchestrated RenewTreeOutput per-leaf median"
      contains: "get_gradients"
      min_lines: 120
    - path: "crates/oracle-harness/tests/objective_parity_regression.rs"
      provides: "regression + boost_from_score + renew_leaf + convert_regression parity cells"
      contains: "objective_common"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/objective_regression.rs"
      to: "crates/lgbm-compute/src/kernels/primitives.rs"
      via: "reduce_sum_f64_on / dot_product_f64_on / reduce_min_f64_on / percentile_unweighted_f32_on composition (D-08)"
      pattern: "percentile_unweighted_f32_on|reduce_sum_f64_on"
    - from: "crates/oracle-harness/tests/objective_parity_regression.rs"
      to: "crates/oracle-harness/tests/fixtures/boosting/regression_gh_iter1.txt"
      via: "read_boosting_golden + parse_gh + compare_exact_u32"
      pattern: "regression_gh"
---

<objective>
ODL-05: port the six CUDA regression-family objectives (§5.1) to standalone CubeCL kernels —
grad/hess (L2, L1, Quantile, Huber, Fair, Poisson), ConvertOutput inverse-link, BoostFromScore
init (mean via reduce, median via percentile, Poisson label-check), and RenewTreeOutput
(L1/Quantile per-leaf median) — anchor-pinned to the cpu f64 fold and the real-lib_lightgbm
goldens. Per D-02 these are STANDALONE (no GBDT wiring — Phase 21).

Purpose: eliminate the boosting layer's regression gradient round-trip (once wired in Phase 21).
Output: filled `objective_regression.rs` + `objective_parity_regression.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-on-device-objectives/19-CONTEXT.md
@.planning/phases/19-on-device-objectives/19-RESEARCH.md
@.planning/phases/19-on-device-objectives/19-PATTERNS.md
@.planning/phases/19-on-device-objectives/19-VALIDATION.md
@crates/lgbm-objective/src/regression.rs
@crates/lgbm-compute/src/kernels/primitives.rs
@crates/lgbm-compute/src/kernels/data_partition.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Six regression grad/hess #[cube] kernels + ConvertOutput</name>
  <files>crates/lgbm-compute/src/kernels/objective_regression.rs, crates/oracle-harness/tests/objective_parity_regression.rs</files>
  <read_first>
    - crates/lgbm-objective/src/regression.rs (get_gradients L380; per-objective match arms: L2 L407-411, L1 L413-423, Huber L425-440, Fair L442-454, Quantile L456-473, Poisson L489-501; convert_output link)
    - crates/lgbm-compute/src/kernels/data_partition.rs (route_to_left comptime-flag #[cube] + launcher, lines 61-76 — the `#[comptime] bool` fan-out template)
    - crates/lgbm-compute/src/kernels/primitives.rs (module header + host-launcher create→launch_unchecked→read pattern, lines 726-778)
    - 19-RESEARCH.md Pattern 1 (get_gradients_l2<USE_WEIGHT> shape) + Anti-Patterns (f64 per-row hot loop forbidden, D-07)
  </read_first>
  <behavior>
    - L2: diff = score - label; grad = use_weight ? diff*w : diff; hess = use_weight ? w : 1.0. Bit-exact vs regression_gh golden.
    - L1: grad = sign(diff) (·w); hess = w or 1.0. Bit-exact vs regression_l1_gh.
    - Quantile: grad = (diff>=0 ? alpha-1 : alpha)-style per regression.rs:456-473. Bit-exact vs quantile_gh.
    - Huber: grad clamps |diff| at delta; Fair: grad = c·diff/(|diff|+c). Bit-exact vs huber_gh / fair_gh.
    - Poisson: grad = exp(score)-label (with max_delta_step clamp per regression.rs:489-501); hess = exp(score+...). Bit-exact vs poisson_gh.
    - ConvertOutput: sign(x)·x² when sqrt_ set, exp for Poisson, pass-through otherwise; anchored to host convert_output.
  </behavior>
  <action>Transcribe the six per-objective grad/hess arms from regression.rs into `objective_regression.rs`. Per D-135/Discretion, implement ONE `#[cube]` grad/hess body generic over Float with a `#[comptime] objective_tag` (or an equivalent comptime dispatch) plus a `#[comptime] use_weight: bool` — mirroring the data_partition.rs comptime fan-out — so the `<USE_WEIGHT>` template folds at comptime with no runtime cost (D-07); OR six kernels if the enum-branch codegen bloats (parity-neutral). Keep the per-row math in f32 (NO f64 per-row hot loop — D-07/spike-052). Factor the shared `diff = score - label` and each link into `#[cube] fn` helpers. Add a per-objective host launcher `*_on<R: cubecl::Runtime>(client, scores, labels, weights, ...) -> Result<(Vec<f32>,Vec<f32>), ComputeError>` following the primitives.rs create→launch_unchecked→read pattern, validating slice lengths at the V5 boundary before launch. Add a `convert_output` elementwise kernel (sign·x² / exp / pass-through). In `objective_parity_regression.rs` add `mod objective_common;` and the `regression` + `convert_regression` cells: for each of the six objectives read the matching `*_gh_iter{1,N}.txt` via `read_boosting_golden`, `parse_gh`, and assert `compare_exact_u32` on f32 bits vs the device output; skip-pass if the golden is absent. Add the weight-branch equivalence property (all-1.0 weights == use_weight=false, bit-exact) and a twice-run determinism check.</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_regression regression && cargo test -p oracle-harness --test objective_parity_regression convert_regression</automated>
  </verify>
  <acceptance_criteria>
    - `objective_regression.rs` contains a `#[cube]` grad body whose per-row form is `diff = score - label` and six objective arms; each launcher returns `Result<_, ComputeError>` with a pre-launch length guard.
    - `cargo test -p oracle-harness --test objective_parity_regression regression` passes: all six objectives bit-exact (`compare_exact_u32`) vs their `*_gh` goldens (or skip-pass if absent).
    - Weight-branch equivalence + twice-run determinism assertions pass.
    - No f64 per-row loop in the grad/hess kernel bodies (f64 only in reductions/scalars).
  </acceptance_criteria>
  <done>Six regression grad/hess kernels + ConvertOutput are bit-exact to the f64 anchor and the real goldens.</done>
</task>

<task type="auto">
  <name>Task 2: BoostFromScore (mean / median / Poisson label-check) via primitive composition</name>
  <files>crates/lgbm-compute/src/kernels/objective_regression.rs, crates/oracle-harness/tests/objective_parity_regression.rs</files>
  <read_first>
    - crates/lgbm-objective/src/regression.rs (boost_from_score L544; mean L548-558, median L567-589, Poisson label-check + safe_log L595-605)
    - crates/lgbm-compute/src/kernels/primitives.rs (reduce_sum_f64_on L784, reduce_min_f64_on L806, dot_product_f64_on L818, percentile_unweighted_f32_on L1179, percentile_weighted_f32_on L1246)
    - 19-PATTERNS.md objective_regression.rs "BoostFromScore" bullets (lines 104-113) + Pitfall 6 (Poisson label-check)
  </read_first>
  <action>Add `boost_from_score` composition to `objective_regression.rs`: for L2/Huber/Fair compute the mean via `reduce_sum_f64_on(labels)/num_data` (or `dot_product_f64_on(weights, labels)/Σw` weighted) — COMPOSE, do not rebuild (D-08); for L1/Quantile compute the median via `percentile_unweighted_f32_on(residuals, 0.5)` / at the quantile alpha; for Poisson run the label non-negativity/finiteness check via `reduce_sum_f64_on` + `reduce_min_f64_on` (Pitfall 6 — the other objectives skip this) THEN `safe_log(mean)`. Mirror regression.rs:544-605 exactly. The scalar finalize math is legitimately f64 (D-07 allows f64 in scalar BoostFromScore). In `objective_parity_regression.rs` add the `boost_from_score` cell: assert the init scalar bit-exact vs the host `regression.rs::boost_from_score` f64 anchor for L2/Huber/Fair/L1/Quantile/Poisson (deterministic reduction order → bit-exact; use `compare_within(ORACLE_TOL)` only if a reduction is atomic on the target backend).</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_regression boost_from_score</automated>
  </verify>
  <acceptance_criteria>
    - `boost_from_score` uses `reduce_sum_f64_on`/`dot_product_f64_on`/`percentile_*` (no hand-rolled reduce) — grep confirms the primitive calls.
    - Poisson path calls `reduce_min_f64_on` for the label-check before the mean init.
    - `cargo test -p oracle-harness --test objective_parity_regression boost_from_score` passes vs the host f64 anchor.
  </acceptance_criteria>
  <done>Regression BoostFromScore init scalars match the f64 anchor for all six objectives, composing existing primitives.</done>
</task>

<task type="auto">
  <name>Task 3: RenewTreeOutput (L1/Quantile per-leaf median, host-orchestrated)</name>
  <files>crates/lgbm-compute/src/kernels/objective_regression.rs, crates/oracle-harness/tests/objective_parity_regression.rs</files>
  <read_first>
    - crates/lgbm-objective/src/regression.rs (renew_leaf_output L627-651: L1 = unweighted residual median α=0.5; Quantile = residual percentile at f32-rounded α; is_renew_tree_output L271)
    - crates/lgbm-compute/src/kernels/primitives.rs (percentile_unweighted_f32_on L1179, percentile_weighted_f32_on L1246)
    - 19-PATTERNS.md "No Analog Found / Discrepancies" (Discrepancy 1 — NO percentile_device kernel; host-loop over leaves) + RESEARCH Pitfall 1 + A2
    - crates/oracle-harness/tests/boosting_parity.rs (median-residual renew precedent ~L750)
  </read_first>
  <action>Implement `renew_tree_output` for L1/Quantile as a HOST-ORCHESTRATED loop over leaves: for each leaf, gather that leaf's residual subset (via the provided leaf row-index membership) and call `percentile_unweighted_f32_on(residuals, 0.5)` for L1 (or `percentile_weighted_f32_on` at the f32-rounded quantile α for Quantile), writing the leaf value. There is NO `percentile_device` per-leaf kernel (Discrepancy 1) — do NOT call one. This host-loop is parity-identical to the CUDA one-block-per-leaf result (same sort, same percentile index math — A2). Document the discrepancy in the module doc (per PATTERNS mandate). Mirror regression.rs:627-651. In `objective_parity_regression.rs` add the `renew_leaf` cell asserting bit-exact (deterministic percentile) vs the host `renew_leaf_output` anchor and, where available, the `regression_l1` renewed-leaf golden.</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_regression renew_leaf</automated>
  </verify>
  <acceptance_criteria>
    - `renew_tree_output` host-loops over leaves calling `percentile_{un,}weighted_f32_on` — grep confirms NO `percentile_device` symbol is referenced.
    - The module doc explicitly notes the "no per-leaf percentile device kernel; host-orchestrated loop is parity-identical" discrepancy.
    - `cargo test -p oracle-harness --test objective_parity_regression renew_leaf` passes bit-exact vs the host f64 anchor.
  </acceptance_criteria>
  <done>L1/Quantile RenewTreeOutput per-leaf medians match the f64 anchor via the host-orchestrated percentile loop.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `crates/lgbm-compute/src/kernels/objective_regression.rs`: six `#[cube]` grad/hess kernels (L2/L1/Quantile/Huber/Fair/Poisson) + per-objective `*_on` launchers, `convert_output` kernel, `boost_from_score` composition, `renew_tree_output` host-orchestrated per-leaf median.
- `crates/oracle-harness/tests/objective_parity_regression.rs`: `regression`, `convert_regression`, `boost_from_score`, `renew_leaf` parity cells + weight-branch/determinism properties.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→device kernel launch | Slice lengths/shape are validated at the V5 boundary before launch; no external/untrusted input, no privilege boundary. Only integrity concern is numerical correctness (anchor-parity gates). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | grad/hess kernel indexing | mitigate | `if i < len` guard in every `#[cube]`; host launcher validates score/label/weight slice lengths before launch (primitives.rs V5 pattern) |
| T-19-01-02 | Denial | Poisson label domain (NaN/Inf/negative) | mitigate | Poisson label non-negativity/finiteness check via reduce_sum + reduce_min before init (Pitfall 6, parity with C++ LaunchCheckLabelKernel) |
| T-19-01-03 | Denial | empty leaf passed to percentile (Renew) | mitigate | `percentile_*` rejects empty at the V5 boundary; host loop skips/guards empty leaves |
</threat_model>

<verification>
- `cargo test -p oracle-harness --test objective_parity_regression` passes all cells (or skip-passes absent goldens).
- `cargo test --workspace` green with `LGBM_CUDA_ON_DEVICE` unset (no regression to default path).
- Optional `cargo test -p oracle-harness --features rocm` ~1e-6 hip cross-check (never GPU-vs-GPU).
</verification>

<success_criteria>
- Six regression grad/hess kernels bit-exact to the f64 anchor and the real goldens.
- BoostFromScore (mean/median/Poisson-check) matches the anchor via primitive composition.
- RenewTreeOutput per-leaf median matches via the host-orchestrated percentile loop.
- ConvertOutput inverse-link matches the host convert_output.
</success_criteria>

<output>
Create `.planning/phases/19-on-device-objectives/19-01-SUMMARY.md` when done.
</output>
