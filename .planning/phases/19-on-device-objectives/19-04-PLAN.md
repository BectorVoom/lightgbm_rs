---
phase: 19-on-device-objectives
plan: 04
type: execute
wave: 2
depends_on: ["19-00"]
files_modified:
  - crates/lgbm-compute/src/kernels/objective_rank.rs
  - crates/oracle-harness/tests/objective_parity_rank.rs
autonomous: true
requirements: [ODL-08]
must_haves:
  truths:
    - "LambdaRank-NDCG grad/hess run block-per-query-group (NUM_QUERY_PER_BLOCK=10) with per-query DESCENDING-score bitonic ranking via bitonic_argsort_items_on, pairwise λ over truncation_level, atomicAdd_block accumulation and the norm rescale — BOTH the shared-memory AND the >2048 _Sorted variants are built (D-03), transcribed from rank.rs:274-366"
    - "RankXENDCG grad/hess build BOTH _SharedMemory and _GlobalMemory variants (D-03): per-query softmax→rho, params[i] = phi(label[i], rng.next_float()) where phi = pow2_int(label) - g (pow2_int is repeated-multiply, NOT powf), three-order λ terms + hessian, transcribed from rank.rs:490-540"
    - "The >2048 RankXENDCG _GlobalMemory path faithfully reproduces the CUDA buffer-aliasing: intermediates stashed in the hessian output buffer + a pre-allocated-once cuda_params_buffer (D-09), finalized to true hessians at the end (Pitfall 4); the phase18-wr01 swap-aliasing caution applies"
    - "Per-item RNG COMPOSES draw_next_float_on (bit-identical LCG, D-08) — NOT rebuilt; the draw ORDER is load-bearing: per query q, Random(seed + q) yields one NextFloat per row row-major (rank.rs:482-487); the RNG stream is asserted BIT-EXACT (compare_exact_u32) never GPU-vs-GPU"
    - "Rank objectives have NO Convert/Renew (base no-ops) — they are NOT synthesized"
    - "λ/hess (atomicAdd_block) are the documented atomic-order residual (D-05) → asserted compare_within(ORACLE_TOL); RankXENDCG transcendental cells → compare_within; anchored to the cpu f64 fold + the lambdarank_gh (from 19-00) and rank_xendcg_objseed5 goldens"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/objective_rank.rs"
      provides: "LambdaRank-NDCG {shared, >2048 _Sorted} + RankXENDCG {shared, global} grad/hess #[cube] + launchers, per-item RNG composition, block-per-query layout"
      contains: "bitonic_argsort_items_on"
      min_lines: 150
    - path: "crates/oracle-harness/tests/objective_parity_rank.rs"
      provides: "lambdarank + rank_xendcg parity cells + RNG-replay bit-exact cell"
      contains: "objective_common"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/objective_rank.rs"
      to: "crates/lgbm-compute/src/kernels/random.rs"
      via: "draw_next_float_on per-item RNG composition, seed = seed + q per query (D-08)"
      pattern: "draw_next_float_on"
    - from: "crates/lgbm-compute/src/kernels/objective_rank.rs"
      to: "crates/lgbm-compute/src/kernels/primitives.rs"
      via: "bitonic_argsort_items_on per-query item ranking (D-08)"
      pattern: "bitonic_argsort_items_on"
    - from: "crates/oracle-harness/tests/objective_parity_rank.rs"
      to: "crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iter1.txt"
      via: "read_rank_golden + parse_gh + compare_within(ORACLE_TOL)"
      pattern: "lambdarank_gh"
---

<objective>
ODL-08: port ranking (§5.4) to standalone CubeCL kernels — LambdaRank-NDCG (shared + >2048
`_Sorted`) and RankXENDCG (shared + `_GlobalMemory`), block-per-query-group, bitonic item
ranking, per-item RNG on the bit-identical LCG stream — anchor-pinned to the cpu f64 fold and
the real lambdarank_gh (captured in 19-00) + rank_xendcg_objseed5 goldens. STANDALONE per D-02.
Full §5.4 fidelity per D-03: both shared and >2048 global variants for BOTH objectives.

Purpose: on-device ranking gradient computation (wired in Phase 21).
Output: filled `objective_rank.rs` + `objective_parity_rank.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-on-device-objectives/19-CONTEXT.md
@.planning/phases/19-on-device-objectives/19-RESEARCH.md
@.planning/phases/19-on-device-objectives/19-PATTERNS.md
@.planning/phases/19-on-device-objectives/19-VALIDATION.md
@crates/lgbm-objective/src/rank.rs
@crates/lgbm-compute/src/kernels/primitives.rs
@crates/lgbm-compute/src/kernels/random.rs
@crates/oracle-harness/tests/rank_parity.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LambdaRank-NDCG grad/hess (shared + >2048 _Sorted)</name>
  <files>crates/lgbm-compute/src/kernels/objective_rank.rs, crates/oracle-harness/tests/objective_parity_rank.rs</files>
  <read_first>
    - crates/lgbm-objective/src/rank.rs (Lambdarank::get_gradients L239; gradients_for_one_query L274-366: per-query descending sort, pairwise λ over truncation_level, accumulation, norm rescale L359-365; query_boundaries L253-256)
    - crates/lgbm-compute/src/kernels/primitives.rs (bitonic_argsort_items_on L1361 per-segment, host-loops over segments)
    - 19-PATTERNS.md objective_rank.rs "LambdaRank-NDCG" bullet (lines 180-186) + RESEARCH §5.4
  </read_first>
  <behavior>
    - Per query group (block-per-query, NUM_QUERY_PER_BLOCK=10): descending-score item rank via bitonic_argsort_items_on (segment = query).
    - Pairwise λ over truncation_level; atomicAdd_block-style accumulation into lambdas/hessians; norm rescale.
    - λ/hess asserted compare_within(ORACLE_TOL) vs the lambdarank_gh golden (atomic-order residual, D-05).
  </behavior>
  <action>Transcribe rank.rs:274-366 (`gradients_for_one_query`) into `objective_rank.rs`: per-query DESCENDING-score sort via `bitonic_argsort_items_on` (segment boundaries = `query_boundaries`, the prefix-sum per rank.rs:253-256), pairwise λ over `truncation_level`, `atomicAdd_block`-style accumulation into the lambdas/hessians buffers, and the `norm` rescale (rank.rs:359-365). Use `NUM_QUERY_PER_BLOCK=10` block-per-query-group (start from the faithful C++ constant; autotune is deferred). Build BOTH the shared-memory variant AND the >2048 `_Sorted` (`<MAX_ITEM_GT_1024>`) variant (D-03) — map the template flag to a `#[comptime]` param. Compose the existing primitives (D-08), do not rebuild the argsort. Add a host launcher with a V5 length/query-boundary guard. In `objective_parity_rank.rs` add `mod objective_common;` and the `lambdarank` cell: read `lambdarank_gh_iter{1,N}.txt` (captured in 19-00) via `read_rank_golden`/`parse_gh` and assert `compare_within(ORACLE_TOL)` on λ/hess (atomic-order residual — NOT compare_exact); skip-pass if the golden is absent.</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_rank lambdarank</automated>
  </verify>
  <acceptance_criteria>
    - `objective_rank.rs` calls `bitonic_argsort_items_on` for per-query ranking (grep confirms) and builds both a shared and a `_Sorted`/`MAX_ITEM_GT_1024` variant.
    - The `lambdarank` cell asserts `compare_within(ORACLE_TOL)` (NOT compare_exact) vs the lambdarank_gh golden.
    - `cargo test -p oracle-harness --test objective_parity_rank lambdarank` passes (or skip-passes if golden absent).
  </acceptance_criteria>
  <done>LambdaRank-NDCG grad/hess (shared + >2048) match the f64 anchor and the lambdarank_gh golden within ORACLE_TOL.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RankXENDCG grad/hess (shared + global) + per-item RNG</name>
  <files>crates/lgbm-compute/src/kernels/objective_rank.rs, crates/oracle-harness/tests/objective_parity_rank.rs</files>
  <read_first>
    - crates/lgbm-objective/src/rank.rs (RankXendcg::get_gradients L441; make_rands L482-487; gradients_for_one_query L490-540; phi = pow2_int(label) - g L544-546; pow2_int L372 repeated-multiply)
    - crates/lgbm-compute/src/kernels/random.rs (draw_next_float_on L240-268, bit-identical LCG, single-owner)
    - 19-RESEARCH.md Pitfall 4 (>2048 global buffer-stashing in hessian buffer + cuda_params_buffer) + Pattern 4 (per-item RNG) + phase18-wr01 aliasing note
  </read_first>
  <behavior>
    - Per query: softmax(score) → rho; params[i] = phi(label[i], rng.next_float()); phi = pow2_int(label) - g.
    - Three-order λ terms + hessian per rank.rs:490-540.
    - Shared (≤2048) and _GlobalMemory (>2048) variants; global path stashes intermediates in the hessian buffer + cuda_params_buffer, finalized at the end.
    - RNG stream bit-exact (compare_exact_u32); grad/hess transcendental cells compare_within(ORACLE_TOL).
  </behavior>
  <action>Transcribe rank.rs:490-540 into `objective_rank.rs`: per-query `softmax(score) → rho`; `params[i] = phi(label[i], rng.next_float())` where `phi = pow2_int(label) - g` (rank.rs:544-546; `pow2_int` is repeated-multiply, NOT `powf`); three-order λ terms + hessian. Build BOTH `_SharedMemory<SHARED_MEMORY_SIZE>` and `_GlobalMemory` (D-03). For the >2048 `_GlobalMemory` path, faithfully reproduce the CUDA buffer-aliasing (Pitfall 4): stash intermediates in the hessian output buffer + a `cuda_params_buffer` pre-allocated ONCE outside the loop (D-09), finalized to true hessians at the end; heed the phase18-wr01 swap-aliasing caution. The per-query softmax is HOST-ORCHESTRATED (no per-segment softmax device kernel exists — Discrepancy 1) OR an in-block reduction; do NOT call a nonexistent `percentile_device`/per-segment-softmax primitive. Compose `draw_next_float_on` for the per-item randoms (D-08) — do NOT rebuild the RNG; the draw ORDER is load-bearing: per query `q`, `Random(seed + q)` yields one `NextFloat` per row row-major (rank.rs:482-487, proven by rank_parity.rs:277-289). Do NOT synthesize Convert/Renew (base no-ops). Add host launchers with V5 guards. In `objective_parity_rank.rs` add the `rank_xendcg` cell (read `rank_xendcg_objseed5.txt` / scores via `read_rank_golden`, assert `compare_within(ORACLE_TOL)` on grad/hess) AND an RNG-replay cell (copy rank_parity.rs:249-305): assert the device `draw_next_float_on` stream bit-exact (`compare_exact_u32` on `to_bits`) vs the host `Random(seed+q)` stream — never GPU-vs-GPU (D-05). Assert shared-path and global-path produce matching hessians (Pitfall 4 guard).</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_rank rank_xendcg</automated>
  </verify>
  <acceptance_criteria>
    - `objective_rank.rs` composes `draw_next_float_on` with `seed + q` per query (grep confirms); no re-implemented RNG.
    - Both `_SharedMemory` and `_GlobalMemory` RankXENDCG variants exist; the global path uses a pre-allocated-once params buffer and the hessian-buffer aliasing.
    - The RNG-replay cell asserts the stream bit-exact (`compare_exact_u32`); grad/hess cells use `compare_within(ORACLE_TOL)`.
    - No `percentile_device` / per-segment-softmax device symbol is referenced (Discrepancy 1).
    - `cargo test -p oracle-harness --test objective_parity_rank rank_xendcg` passes.
  </acceptance_criteria>
  <done>RankXENDCG grad/hess (shared + global) match the f64 anchor within ORACLE_TOL and the RNG stream is bit-exact; the >2048 aliasing is faithful.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `crates/lgbm-compute/src/kernels/objective_rank.rs`: LambdaRank-NDCG {shared, >2048 `_Sorted`} + RankXENDCG {shared, `_GlobalMemory`} grad/hess `#[cube]` + launchers, per-item RNG composition (`draw_next_float_on`), block-per-query-group layout, pre-allocated params buffer.
- `crates/oracle-harness/tests/objective_parity_rank.rs`: `lambdarank`, `rank_xendcg` parity cells + RNG-replay bit-exact cell + shared-vs-global hessian match.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→device kernel launch | Slice lengths + query boundaries validated at the V5 boundary before launch; no external/untrusted input, no privilege boundary. Only integrity concern is numerical correctness (anchor-parity gates). RNG is a deterministic LCG for parity, NOT a security RNG — do not substitute a CSPRNG (breaks the bit-identical-stream contract). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | RankXENDCG >2048 hessian-buffer aliasing | mitigate | Faithful documented aliasing + cuda_params_buffer pre-allocated once (D-09); shared-vs-global hessian-match assertion; phase18-wr01 swap-aliasing caution heeded |
| T-19-04-02 | Tampering | per-query index / empty query | mitigate | `if i < len` guard; host launcher validates query_boundaries; percentile/argsort reject empty at V5 boundary |
| T-19-04-03 | Spoofing/Repudiation | RNG stream substitution | mitigate | Compose the deterministic `draw_next_float_on` LCG only; RNG-replay cell pins the stream bit-exact (never a CSPRNG) |
</threat_model>

<verification>
- `cargo test -p oracle-harness --test objective_parity_rank` passes all cells (or skip-passes absent goldens).
- `cargo test --workspace` green with `LGBM_CUDA_ON_DEVICE` unset.
- Optional `--features rocm` ~1e-6 hip cross-check (never GPU-vs-GPU).
</verification>

<success_criteria>
- LambdaRank-NDCG (shared + >2048) grad/hess match the f64 anchor + lambdarank_gh golden within ORACLE_TOL.
- RankXENDCG (shared + global) grad/hess match within ORACLE_TOL; the RNG stream is bit-exact.
- The >2048 global buffer-aliasing is faithful (shared and global hessians match).
- No Convert/Renew synthesized for rank (base no-ops).
</success_criteria>

<output>
Create `.planning/phases/19-on-device-objectives/19-04-SUMMARY.md` when done.
</output>
