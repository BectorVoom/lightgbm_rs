---
phase: 19-on-device-objectives
plan: 03
type: execute
wave: 2
depends_on: ["19-00"]
files_modified:
  - crates/lgbm-compute/src/kernels/objective_multiclass.rs
  - crates/oracle-harness/tests/objective_parity_multiclass.rs
autonomous: true
requirements: [ODL-07]
must_haves:
  truths:
    - "Multiclass-softmax grad/hess run on the cpu f64 fold and are BIT-EXACT (compare_exact_u32) vs the lgbm-objective multiclass anchor and the class-major real multiclass_gh golden (12 rows × 3 classes = 36 values, GRAD then HESS) — per D-01"
    - "The class-major [k·num_data+i] stride is LOAD-BEARING: gather rec[k] = score[num_data*k + i]; write grad[num_data*k+i] = (label==k ? p-1 : p); hess[num_data*k+i] = factor·p·(1-p), factor = num_class/(num_class-1) (Pitfall 3, Pattern 3)"
    - "double* cuda_softmax_buffer (per-row length-K scratch) is pre-allocated ONCE outside the loop (D-09); softmax reuses the host softmax math, not a numerically-different re-implementation"
    - "MulticlassOVA reuses the binary response math per class at offset = num_data*i (parity-neutral, Discretion); asserted bit-exact vs the multiclassova_gh golden"
    - "Held-out invariant: Σ_k grad[k·N+i] ≈ 0 per row for softmax (a golden-independent sanity net)"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/objective_multiclass.rs"
      provides: "class-major softmax grad/hess #[cube] + launcher, softmax ConvertOutput, MulticlassOVA per-class path, pre-allocated softmax buffer"
      contains: "num_data"
      min_lines: 90
    - path: "crates/oracle-harness/tests/objective_parity_multiclass.rs"
      provides: "multiclass + multiclassova parity cells + class-major Σgrad≈0 invariant"
      contains: "objective_common"
  key_links:
    - from: "crates/oracle-harness/tests/objective_parity_multiclass.rs"
      to: "crates/oracle-harness/tests/fixtures/boosting/multiclass_gh_iter1.txt"
      via: "read_boosting_golden + parse_gh (class-major stride) + compare_exact_u32"
      pattern: "multiclass_gh"
    - from: "crates/lgbm-compute/src/kernels/objective_multiclass.rs"
      to: "crates/lgbm-objective/src/multiclass.rs"
      via: "class-major stride + factor = num_class/(num_class-1) transcription (multiclass.rs:138-180)"
      pattern: "num_data"
---

<objective>
ODL-07: port multiclass (§5.3) to standalone CubeCL kernels — softmax grad/hess in the
load-bearing class-major `[k·num_data+i]` layout, softmax ConvertOutput, and the one-vs-all
(MulticlassOVA) per-class path — anchor-pinned to the cpu f64 fold and the class-major real
multiclass_gh / multiclassova_gh goldens. STANDALONE per D-02 (no GBDT wiring — Phase 21).

Purpose: on-device multiclass gradient computation (wired in Phase 21).
Output: filled `objective_multiclass.rs` + `objective_parity_multiclass.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-on-device-objectives/19-CONTEXT.md
@.planning/phases/19-on-device-objectives/19-RESEARCH.md
@.planning/phases/19-on-device-objectives/19-PATTERNS.md
@.planning/phases/19-on-device-objectives/19-VALIDATION.md
@crates/lgbm-objective/src/multiclass.rs
@crates/lgbm-objective/src/binary.rs
@crates/lgbm-compute/src/kernels/primitives.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Class-major softmax grad/hess #[cube] + softmax ConvertOutput</name>
  <files>crates/lgbm-compute/src/kernels/objective_multiclass.rs, crates/oracle-harness/tests/objective_parity_multiclass.rs</files>
  <read_first>
    - crates/lgbm-objective/src/multiclass.rs (get_gradients L138-180; softmax import L43; factor L84; class_need_train L193)
    - crates/lgbm-compute/src/kernels/primitives.rs (module header + `#[cube]` body + launch-wrapper convention; reused-scratch note lines 8-11)
    - 19-RESEARCH.md Pattern 3 (class-major gather) + Pitfall 3 (stride correctness) + 19-PATTERNS.md objective_multiclass.rs block (lines 149-172)
    - crates/oracle-harness/tests/fixtures/boosting/multiclass_gh_iter1.txt (class-major 36-value layout)
  </read_first>
  <behavior>
    - Per row i: gather rec[k] = score[num_data*k + i] for k in 0..K; softmax(rec) → p_k.
    - grad[num_data*k + i] = (label[i]==k) ? (p_k - 1.0) : p_k.
    - hess[num_data*k + i] = factor·p_k·(1 - p_k), factor = num_class/(num_class-1).
    - Bit-exact vs class-major multiclass_gh golden (GRAD block then HESS block, class-blocked).
    - Σ_k grad[k·N+i] ≈ 0 per row (held-out invariant).
  </behavior>
  <action>Transcribe multiclass.rs:156-178 into a `#[cube]` softmax grad/hess body in `objective_multiclass.rs`, one thread per ROW, looping classes with the class-major `[k·num_data+i]` stride (gather `rec[k] = score[num_data*k+i]`, write `grad[num_data*k+i]`/`hess[num_data*k+i]`). Use `factor = num_class/(num_class-1)` (multiclass.rs:84). Port the SAME in-place softmax math the host uses (multiclass.rs:43 import) — do NOT introduce a numerically-different softmax. Allocate the `double* cuda_softmax_buffer` (per-row length-K scratch) ONCE via a reused `client.empty` handle outside the launch loop (D-09). Add a host launcher with a V5 length guard (num_data·num_class shape check). Add a `softmax_convert_output` per-row kernel. In `objective_parity_multiclass.rs` add `mod objective_common;` and the `multiclass` cell: read `multiclass_gh_iter{1,N}.txt` via `read_boosting_golden`/`parse_gh` (class-major stride) and assert `compare_exact_u32`. Add the class-major Σ_k grad ≈ 0 invariant (per-row sum within f32 tol) and a twice-run determinism check.</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_multiclass multiclass</automated>
  </verify>
  <acceptance_criteria>
    - The kernel gathers `score[num_data*k + i]` and writes `grad[num_data*k + i]` (class-major stride — grep confirms the `num_data*k` index form).
    - The softmax buffer handle is created once outside the launch loop (D-09).
    - `cargo test -p oracle-harness --test objective_parity_multiclass multiclass` passes bit-exact vs the class-major golden; the Σ_k grad ≈ 0 invariant holds.
  </acceptance_criteria>
  <done>Class-major softmax grad/hess + softmax ConvertOutput are bit-exact to the anchor and the class-major golden.</done>
</task>

<task type="auto">
  <name>Task 2: MulticlassOVA (reuse binary per class)</name>
  <files>crates/lgbm-compute/src/kernels/objective_multiclass.rs, crates/oracle-harness/tests/objective_parity_multiclass.rs</files>
  <read_first>
    - crates/lgbm-objective/src/multiclass.rs (MulticlassOva — K independent Binary at offset=num_data*i; boost_from_score L184)
    - crates/lgbm-objective/src/binary.rs (get_gradients L67-96 — the per-class response math)
    - 19-RESEARCH.md Alternatives (CUDAMulticlassOVA = binary-kernel-per-class) + 19-PATTERNS.md "MulticlassOVA" (line 167)
  </read_first>
  <action>Implement MulticlassOVA in `objective_multiclass.rs` as a thin per-class loop applying the binary-logloss response math (label == class ? +1 : -1, then the binary grad/hess) at `offset = num_data*i` for each class i — parity-neutral per Discretion (the host `MulticlassOva` holds K independent `Binary`). Transcribe the binary response math locally (mirroring binary.rs:67-96) so this plan stays self-contained with no cross-file dependency on 19-02. In `objective_parity_multiclass.rs` add the `multiclassova` cell: read `multiclassova_gh_iter{1,N}.txt` via `read_boosting_golden`/`parse_gh` (class-major) and assert `compare_exact_u32` vs the device output.</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_multiclass multiclassova</automated>
  </verify>
  <acceptance_criteria>
    - MulticlassOVA applies the binary response per class at `offset = num_data*i` (grep confirms the offset stride).
    - `cargo test -p oracle-harness --test objective_parity_multiclass multiclassova` passes bit-exact vs the multiclassova_gh golden (or skip-pass if absent).
  </acceptance_criteria>
  <done>MulticlassOVA per-class grad/hess is bit-exact to the multiclassova_gh golden.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `crates/lgbm-compute/src/kernels/objective_multiclass.rs`: class-major softmax grad/hess `#[cube]` + launcher, `softmax_convert_output`, pre-allocated softmax buffer, MulticlassOVA per-class path.
- `crates/oracle-harness/tests/objective_parity_multiclass.rs`: `multiclass`, `multiclassova` parity cells + class-major Σgrad≈0 invariant + determinism property.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→device kernel launch | num_data·num_class shape validated at the V5 boundary before launch; no external/untrusted input, no privilege boundary. Only integrity concern is numerical correctness (anchor-parity gates). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03-01 | Tampering | class-major stride index (k·num_data+i) | mitigate | `if i < num_data` guard; host launcher validates num_data·num_class buffer shape before launch |
| T-19-03-02 | Denial | label out of [0,K) range | mitigate | Multiclass LabelOutOfRange guard (parity with host MulticlassSoftmax::Init, STATE.md L237) |
</threat_model>

<verification>
- `cargo test -p oracle-harness --test objective_parity_multiclass` passes all cells (or skip-passes absent goldens).
- `cargo test --workspace` green with `LGBM_CUDA_ON_DEVICE` unset.
- Optional `--features rocm` ~1e-6 hip cross-check (never GPU-vs-GPU).
</verification>

<success_criteria>
- Class-major softmax grad/hess bit-exact to the anchor and the class-major golden.
- MulticlassOVA per-class grad/hess bit-exact to the multiclassova_gh golden.
- Softmax ConvertOutput matches the host anchor; Σ_k grad ≈ 0 invariant holds.
</success_criteria>

<output>
Create `.planning/phases/19-on-device-objectives/19-03-SUMMARY.md` when done.
</output>
