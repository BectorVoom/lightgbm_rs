---
phase: 19-on-device-objectives
plan: 02
type: execute
wave: 2
depends_on: ["19-00"]
files_modified:
  - crates/lgbm-compute/src/kernels/objective_binary.rs
  - crates/oracle-harness/tests/objective_parity_binary.rs
autonomous: true
requirements: [ODL-06]
must_haves:
  truths:
    - "Binary-logloss grad/hess (label=±1, response = -label·σ/(1+exp(label·σ·score)), hess = |response|·(σ-|response|)) run on the cpu f64 fold and are BIT-EXACT (compare_exact_u32) vs the lgbm-objective binary anchor and the real binary_gh golden (per D-01)"
    - "The <USE_LABEL_WEIGHT, USE_WEIGHT> templates map to two #[comptime] bool params folding both weight branches out at comptime (Pattern 1)"
    - "Two-stage BoostFromScore: kernel-1 reduce_sum_f64_on(is_pos)[+weight sum] on device; kernel-2 (<<<1,1>>> analog) pavg = clamp(Σ/N, ε, 1-ε), init = ln(pavg/(1-pavg))/σ as an f64 host scalar — matching binary.rs:102-117"
    - "The binary init scalar is the documented atomicAdd-order residual (D-05) → asserted with compare_within(ORACLE_TOL=1e-6), NOT bit-exact; the per-row grad/hess (no accumulation) IS bit-exact"
    - "The one-vs-all label reset (ResetOVACUDALabelKernel) is a per-row elementwise label == class ? +1 : -1 rewrite kernel"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/objective_binary.rs"
      provides: "binary grad/hess #[cube] + launcher, sigmoid ConvertOutput, two-stage BoostFromScore, OVA label-reset kernel"
      contains: "get_gradients"
      min_lines: 90
    - path: "crates/oracle-harness/tests/objective_parity_binary.rs"
      provides: "binary + binary_boost parity cells"
      contains: "objective_common"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/objective_binary.rs"
      to: "crates/lgbm-compute/src/kernels/primitives.rs"
      via: "reduce_sum_f64_on for the two-stage BoostFromScore label/weight sums (D-08)"
      pattern: "reduce_sum_f64_on"
    - from: "crates/oracle-harness/tests/objective_parity_binary.rs"
      to: "crates/oracle-harness/tests/fixtures/boosting/binary_gh_iter1.txt"
      via: "read_boosting_golden + parse_gh + compare_exact_u32"
      pattern: "binary_gh"
---

<objective>
ODL-06: port binary-logloss (§5.2) to standalone CubeCL kernels — grad/hess, two-stage
BoostFromScore (label-prior logit init), sigmoid ConvertOutput, and the one-vs-all label
reset — anchor-pinned to the cpu f64 fold and the real binary_gh golden. STANDALONE per D-02
(no GBDT wiring — Phase 21).

Purpose: on-device binary gradient computation (wired in Phase 21).
Output: filled `objective_binary.rs` + `objective_parity_binary.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-on-device-objectives/19-CONTEXT.md
@.planning/phases/19-on-device-objectives/19-RESEARCH.md
@.planning/phases/19-on-device-objectives/19-PATTERNS.md
@.planning/phases/19-on-device-objectives/19-VALIDATION.md
@crates/lgbm-objective/src/binary.rs
@crates/lgbm-compute/src/kernels/random.rs
@crates/lgbm-compute/src/kernels/primitives.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Binary grad/hess #[cube] kernel + sigmoid ConvertOutput</name>
  <files>crates/lgbm-compute/src/kernels/objective_binary.rs, crates/oracle-harness/tests/objective_parity_binary.rs</files>
  <read_first>
    - crates/lgbm-objective/src/binary.rs (get_gradients L67-96: is_pos, label_val ±1, response, grad, hess; class_need_train L124)
    - crates/lgbm-compute/src/kernels/random.rs (small `#[cube]`-helper → single-owner-kernel → host-launcher module shape, lines 55-79, 165-197)
    - 19-RESEARCH.md Pattern 1 (comptime template) + 19-PATTERNS.md objective_binary.rs block (lines 121-146)
  </read_first>
  <behavior>
    - grad/hess per row: is_pos = label > 0; label_val = is_pos ? 1 : -1; response = -label_val·σ/(1+exp(label_val·σ·score)); grad = response·label_weight (·w if use_weight); hess = |response|·(σ-|response|)·label_weight (·w). Bit-exact vs binary_gh golden.
    - sigmoid ConvertOutput: 1/(1+exp(-σ·score)); anchored to host convert_output.
  </behavior>
  <action>Transcribe binary.rs:67-96 verbatim into a `#[cube]` grad/hess body in `objective_binary.rs`, factoring the sigmoid/response math into a `#[cube] fn` helper (mirroring random.rs's small-helper decomposition) so the math exists once. Map `<USE_LABEL_WEIGHT, USE_WEIGHT>` to two `#[comptime] bool` params. Keep per-row math in f32 (D-07). Add a host launcher `get_gradients_on<R>(client, scores, labels, weights, sigmoid, ...) -> Result<(Vec<f32>,Vec<f32>), ComputeError>` with a V5 length guard. Add a `sigmoid_convert_output` elementwise kernel. In `objective_parity_binary.rs` add `mod objective_common;` and the `binary` cell: read `binary_gh_iter{1,N}.txt` via `read_boosting_golden`, `parse_gh`, assert `compare_exact_u32` on the per-row grad/hess (no accumulation → bit-exact). Add the weight-branch equivalence + twice-run determinism properties.</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_binary binary</automated>
  </verify>
  <acceptance_criteria>
    - `objective_binary.rs` contains a `#[cube]` grad/hess body with a shared response helper and two `#[comptime] bool` weight flags.
    - `cargo test -p oracle-harness --test objective_parity_binary binary` passes bit-exact (`compare_exact_u32`) vs the binary_gh golden (or skip-pass if absent).
    - Weight-branch equivalence + twice-run determinism pass.
  </acceptance_criteria>
  <done>Binary grad/hess + sigmoid ConvertOutput are bit-exact to the f64 anchor and the real golden.</done>
</task>

<task type="auto">
  <name>Task 2: Two-stage BoostFromScore (logit init) + OVA label reset</name>
  <files>crates/lgbm-compute/src/kernels/objective_binary.rs, crates/oracle-harness/tests/objective_parity_binary.rs</files>
  <read_first>
    - crates/lgbm-objective/src/binary.rs (boost_from_score L102-117: pavg clamp [ε,1-ε], init = ln(pavg/(1-pavg))/σ)
    - crates/lgbm-compute/src/kernels/primitives.rs (reduce_sum_f64_on L784, host-launcher pattern L726-778)
    - 19-RESEARCH.md Pattern 2 (device reduce + host scalar finalize) + Pitfall 5 (atomic-order residual → compare_within, not bit-exact) + D-05
  </read_first>
  <action>Add the two-stage BoostFromScore to `objective_binary.rs`: stage 1 = `reduce_sum_f64_on(is_pos as f64)` [+ weight sum] on device (COMPOSE, D-08); stage 2 (`<<<1,1>>>` analog) = `pavg = clamp(Σ/N, ε, 1-ε); init = ln(pavg/(1-pavg))/σ` as an f64 host scalar (D-07 allows f64 in scalar BoostFromScore), mirroring binary.rs:102-117 verbatim. Add the OVA label-reset kernel `reset_ova_label` — a per-row elementwise `label == class ? +1 : -1` rewrite (single-owner launcher shape like random.rs:165-197). In `objective_parity_binary.rs` add the `binary_boost` cell: assert the init scalar with `compare_within(ORACLE_TOL=1e-6)` (NOT `compare_exact` — the atomicAdd sum is the documented f32-vs-f64 residual, D-05/Pitfall 5) vs the host `binary.rs::boost_from_score` f64 anchor; assert the OVA label reset produces the expected ±1 vector bit-exact.</action>
  <verify>
    <automated>cargo test -p oracle-harness --test objective_parity_binary binary_boost</automated>
  </verify>
  <acceptance_criteria>
    - BoostFromScore stage 1 calls `reduce_sum_f64_on`; stage 2 computes `ln(pavg/(1-pavg))/σ` with the [ε,1-ε] clamp.
    - The `binary_boost` cell asserts the init scalar via `compare_within(ORACLE_TOL)` (grep confirms it does NOT use compare_exact for the init scalar).
    - The OVA label-reset kernel produces ±1 per row; `cargo test -p oracle-harness --test objective_parity_binary binary_boost` passes.
  </acceptance_criteria>
  <done>Two-stage binary BoostFromScore matches the f64 anchor within ORACLE_TOL; OVA label reset is bit-exact.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
- `crates/lgbm-compute/src/kernels/objective_binary.rs`: binary grad/hess `#[cube]` + `get_gradients_on` launcher, sigmoid ConvertOutput, two-stage `boost_from_score`, `reset_ova_label` kernel.
- `crates/oracle-harness/tests/objective_parity_binary.rs`: `binary`, `binary_boost` parity cells + weight-branch/determinism properties.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→device kernel launch | Slice lengths validated at the V5 boundary before launch; no external/untrusted input, no privilege boundary. Only integrity concern is numerical correctness (anchor-parity gates). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Tampering | grad/hess kernel indexing | mitigate | `if i < len` guard in every `#[cube]`; host launcher validates score/label/weight lengths before launch |
| T-19-02-02 | Denial | pavg = 0 or 1 (log blow-up) | mitigate | pavg clamped to [ε,1-ε] before the logit (parity with C++ binary BoostFromScore) |
</threat_model>

<verification>
- `cargo test -p oracle-harness --test objective_parity_binary` passes all cells (or skip-passes absent golden).
- `cargo test --workspace` green with `LGBM_CUDA_ON_DEVICE` unset.
- Optional `--features rocm` ~1e-6 hip cross-check (never GPU-vs-GPU).
</verification>

<success_criteria>
- Binary grad/hess bit-exact to the f64 anchor and the real binary_gh golden.
- Two-stage BoostFromScore logit init matches the anchor within ORACLE_TOL (documented atomic residual).
- Sigmoid ConvertOutput + OVA label reset match their anchors.
</success_criteria>

<output>
Create `.planning/phases/19-on-device-objectives/19-02-SUMMARY.md` when done.
</output>
