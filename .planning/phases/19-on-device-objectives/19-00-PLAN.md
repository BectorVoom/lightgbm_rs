---
phase: 19-on-device-objectives
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/lgbm-compute/src/kernels/mod.rs
  - crates/lgbm-compute/src/kernels/objective_regression.rs
  - crates/lgbm-compute/src/kernels/objective_binary.rs
  - crates/lgbm-compute/src/kernels/objective_multiclass.rs
  - crates/lgbm-compute/src/kernels/objective_rank.rs
  - crates/lgbm-compute/src/device_objective.rs
  - crates/lgbm-compute/src/lib.rs
  - crates/oracle-harness/tests/objective_common/mod.rs
  - xtask/py/rank_oracle_capture.py
  - crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iter1.txt
  - crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iterN.txt
autonomous: true
requirements: [ODL-05, ODL-06, ODL-07, ODL-08]
must_haves:
  truths:
    - "The four objective kernel modules exist as compiling stubs declared UNGATED (NOT #[cfg(feature = gpu)]) in kernels/mod.rs, so the default cpu f64 anchor exercises them (D-08) and each Wave-2 family plan fills exactly ONE owned stub file with no mod.rs contention"
    - "device_objective_supported(name) returns false for all SEVEN CUDA-unsupported objectives (mape, gamma/regression_gamma, gamma_deviance, tweedie, cross_entropy/xentropy, cross_entropy_lambda/xentlambda, map/rank_map) and true for the eleven supported — the SC #5 host-fallback discriminator (per D-02)"
    - "Backend::on_device_growth_supported() STILL returns false this phase (per D-02/D-06); the default host boosting path is byte-unchanged"
    - "objective_common/mod.rs provides parse_gh, read_boosting_golden and read_rank_golden helpers reused in shape from boosting_parity.rs; missing goldens return None so a fresh checkout builds and skip-passes"
    - "tests/fixtures/rank/lambdarank_gh_iter{1,N}.txt are captured from real lib_lightgbm 4.6 via the score-derivation route (GRAD/HESS u32-bits lines), matching the boosting/*_gh_iter*.txt golden format exactly (per D-01 fidelity cross-check)"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/objective_regression.rs"
      provides: "compiling stub for ODL-05 (filled by 19-01)"
    - path: "crates/lgbm-compute/src/kernels/objective_binary.rs"
      provides: "compiling stub for ODL-06 (filled by 19-02)"
    - path: "crates/lgbm-compute/src/kernels/objective_multiclass.rs"
      provides: "compiling stub for ODL-07 (filled by 19-03)"
    - path: "crates/lgbm-compute/src/kernels/objective_rank.rs"
      provides: "compiling stub for ODL-08 (filled by 19-04)"
    - path: "crates/lgbm-compute/src/device_objective.rs"
      provides: "DeviceObjectiveKind support-set + device_objective_supported() host-fallback discriminator (SC #5)"
      contains: "device_objective_supported"
    - path: "crates/oracle-harness/tests/objective_common/mod.rs"
      provides: "shared parity harness: parse_gh, read_boosting_golden, read_rank_golden"
      contains: "parse_gh"
    - path: "crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iter1.txt"
      provides: "lambdarank real-lib_lightgbm grad/hess golden (the one capture gap)"
      contains: "GRAD"
  key_links:
    - from: "crates/lgbm-compute/src/lib.rs"
      to: "crates/lgbm-compute/src/device_objective.rs"
      via: "pub mod device_objective; re-export of device_objective_supported"
      pattern: "device_objective"
    - from: "xtask/py/rank_oracle_capture.py"
      to: "crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iter1.txt"
      via: "score-derivation emit of GRAD/HESS u32-bits lines"
      pattern: "lambdarank_gh"
---

<objective>
Wave-1 foundation for Phase 19: stand up the greenfield `objective` kernel module surface,
the host-fallback discriminator that answers SC #5, the shared parity harness, and the one
missing real-`lib_lightgbm` golden — so the four per-family plans (19-01..04) run in parallel,
each filling exactly one owned kernel file + one owned parity test file with no contention.

Purpose: unblock every family plan (mod.rs + stubs declared here), and land the two Wave-0
validation gaps from 19-VALIDATION.md (the shared `objective_parity` harness + the
`lambdarank_gh` capture). Implements the ODL-05/06/07/08 scaffold and the SC #5 discriminator.
Output: 4 compiling stub kernel modules + mod.rs exports; `device_objective.rs` support-set;
`objective_common/mod.rs` shared harness; `lambdarank_gh_iter{1,N}.txt` fixtures.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-on-device-objectives/19-CONTEXT.md
@.planning/phases/19-on-device-objectives/19-RESEARCH.md
@.planning/phases/19-on-device-objectives/19-PATTERNS.md
@.planning/phases/19-on-device-objectives/19-VALIDATION.md
@crates/lgbm-compute/src/kernels/mod.rs
@crates/lgbm-compute/src/kernels/random.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Greenfield objective module stubs + mod.rs exports + host-fallback support-set (SC #5)</name>
  <files>crates/lgbm-compute/src/kernels/objective_regression.rs, crates/lgbm-compute/src/kernels/objective_binary.rs, crates/lgbm-compute/src/kernels/objective_multiclass.rs, crates/lgbm-compute/src/kernels/objective_rank.rs, crates/lgbm-compute/src/kernels/mod.rs, crates/lgbm-compute/src/device_objective.rs, crates/lgbm-compute/src/lib.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/mod.rs (the Phase-14..18 `pub mod` block, lines 15-70 — copy the ungated-block shape and doc-comment convention)
    - crates/lgbm-compute/src/kernels/random.rs (module header + `use cubecl::prelude::*; use crate::error::ComputeError;` convention, lines 42-56)
    - crates/lgbm-compute/src/lib.rs at on_device_growth_supported (line 1239) — confirm it stays false; find where to add `pub mod device_objective;`
    - crates/lgbm-objective/src/regression.rs (Mape variant ~line 87), crates/lgbm-objective/src/xentropy.rs (CrossEntropy/CrossEntropyLambda) — the objective name aliases the support-set must reject
    - 19-PATTERNS.md "crates/lgbm-compute/src/kernels/mod.rs (MODIFY)" block (lines 204-219)
  </read_first>
  <action>Create four empty-but-compiling stub files `objective_{regression,binary,multiclass,rank}.rs` in `crates/lgbm-compute/src/kernels/`, each opening with `use cubecl::prelude::*;` + `use crate::error::ComputeError;` and a docstring citing its C++ source (§5.1/§5.2/§5.3/§5.4 of docs/cuda-kernel-design.md), the host anchor file in lgbm-objective, the "cpu f64 fold is the anchor, never GPU-vs-GPU (D-05)" discipline, and a `// Filled by 19-0N` marker. Append a Phase-19 `pub mod objective_{regression,binary,multiclass,rank};` block to kernels/mod.rs in the identical UNGATED shape as the Phase-14..18 blocks (NOT `#[cfg(feature = "gpu")]`, per D-08) with a doc comment noting they are additive and OFF by default behind `LGBM_CUDA_ON_DEVICE` (D-06). Create `crates/lgbm-compute/src/device_objective.rs` with a `DeviceObjectiveKind` enum for the eleven CUDA-supported objectives (L2, L1, Quantile, Huber, Fair, Poisson, Binary, MulticlassSoftmax, MulticlassOva, Lambdarank, RankXENDCG) plus a free fn `device_objective_supported(name: &str) -> bool` returning true for those eleven objective aliases and FALSE for the seven CUDA-unsupported ones (mape, gamma/regression_gamma, gamma_deviance, tweedie, cross_entropy/xentropy, cross_entropy_lambda/xentlambda, map/rank_map) — the SC #5 host-fallback discriminator per D-02. Add a unit test in device_objective.rs asserting the false-set and the true-set. Register `pub mod device_objective;` and re-export `device_objective_supported` in lib.rs. Do NOT touch `on_device_growth_supported()` — it stays false (D-02/D-06). No numeric kernel bodies yet.</action>
  <verify>
    <automated>cargo build -p lgbm-compute && cargo test -p lgbm-compute device_objective</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p lgbm-compute` succeeds (all four stub modules + device_objective compile).
    - A unit test asserts `device_objective_supported` is false for each of mape, gamma, gamma_deviance, tweedie, cross_entropy, cross_entropy_lambda, rank_map, and true for regression/binary/multiclass/lambdarank/rank_xendcg — `cargo test -p lgbm-compute device_objective` passes.
    - The new Phase-19 block in mod.rs has zero `cfg(feature = "gpu")` occurrences (modules ungated).
    - lib.rs `on_device_growth_supported()` body is unchanged (still returns false).
  </acceptance_criteria>
  <done>Four ungated stub modules compile; the host-fallback support-set rejects all seven unsupported objectives; on_device_growth_supported stays false.</done>
</task>

<task type="auto">
  <name>Task 2: Shared objective parity harness (objective_common/mod.rs)</name>
  <files>crates/oracle-harness/tests/objective_common/mod.rs</files>
  <read_first>
    - crates/oracle-harness/tests/boosting_parity.rs (parse_gh / read_golden / fixture-dir helpers around lines 34-35, 315-336, 447-459)
    - crates/oracle-harness/tests/rank_parity.rs (RNG-replay + per-query score fixture read, lines 39-58, 249-305)
    - crates/oracle-harness/src/comparator.rs (pub `compare_exact_u32` L125, `compare_within` L92, `ORACLE_TOL` L15)
    - 19-PATTERNS.md "crates/oracle-harness/tests/objective_parity.rs" block (lines 223-271)
  </read_first>
  <action>Create `crates/oracle-harness/tests/objective_common/mod.rs` (a SUBDIR module — Cargo does not treat `tests/*/mod.rs` as its own test binary, so the four family test files include it via `mod objective_common;`). Expose: `parse_gh(text: &str) -> (Vec<f32>, Vec<f32>)` parsing `GRAD <u32 bits...>` / `HESS <u32 bits...>` lines via `f32::from_bits` (reused in shape from boosting_parity.rs:447-459); `boosting_dir()` / `rank_dir()` returning `PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("tests/fixtures/{boosting,rank}")`; `read_boosting_golden(name: &str) -> Option<String>` and `read_rank_golden(name: &str) -> Option<String>` returning `None` (with an eprintln SKIP note) when the fixture is absent so a fresh checkout skip-passes. Do NOT add any `#[test]` fn here — helper module only. Family tests pull comparators directly from `oracle_harness::comparator::{compare_exact_u32, compare_within, ORACLE_TOL}`.</action>
  <verify>
    <automated>cargo build -p oracle-harness --tests</automated>
  </verify>
  <acceptance_criteria>
    - `objective_common/mod.rs` contains `pub fn parse_gh`, `pub fn read_boosting_golden`, `pub fn read_rank_golden`.
    - `read_boosting_golden` returns `None` for a nonexistent fixture name (capture-gated skip-pass contract).
    - `cargo build -p oracle-harness --tests` succeeds.
  </acceptance_criteria>
  <done>The shared harness module compiles and exposes parse_gh + the two capture-gated golden readers.</done>
</task>

<task type="auto">
  <name>Task 3: Capture the lambdarank grad/hess golden (real lib_lightgbm 4.6)</name>
  <files>xtask/py/rank_oracle_capture.py, crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iter1.txt, crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iterN.txt</files>
  <read_first>
    - xtask/py/boosting_oracle_capture.py (the score-derivation route for `*_gh_iter{1,N}.txt`, lines 273-357; bit-helpers `f32_bits`/`f64_bits`)
    - xtask/py/rank_oracle_capture.py (existing raw-score + query_boundaries capture; the ported C++ Random LCG lines 73-83)
    - crates/lgbm-objective/src/rank.rs (Lambdarank::get_gradients L239, gradients_for_one_query L274-366 — the within-query λ math to reproduce; pow2_int L372, phi L544)
    - 19-RESEARCH.md Pitfall 2 + Open Question 1/A1 (score-derivation first, fobj fallback) + A4 (confirm venv)
  </read_first>
  <action>First confirm the uv `.venv` at repo root has `lightgbm==4.6` (Open Question A4): run the venv python to import lightgbm and print `lightgbm.__version__`; if absent/wrong, `pip install lightgbm==4.6` into that `.venv` (already-vendored/audited package — no new dependency, per RESEARCH Package Legitimacy Audit N/A). Extend `xtask/py/rank_oracle_capture.py` to emit `lambdarank_gh_iter1.txt` and `lambdarank_gh_iterN.txt` in the `GRAD <u32 bits...>` / `HESS <u32 bits...>` format (identical to boosting/*_gh_iter*.txt) via the score-derivation route: reconstruct grad/hess from the captured per-iteration raw scores + labels + query_boundaries + sigmoid σ + label-gains + inv-max-dcg using the lambdarank within-query λ math (mirroring rank.rs gradients_for_one_query semantics). Write GRAD then HESS lines per iter; iter1 = first boosting iter, iterN = a later iter (match boosting_oracle_capture.py's LATER_ITER convention). If the within-query λ accumulation does not derive cleanly from scores alone (A1), fall back to intercepting real grad/hess via a custom `fobj` in the capture run. Place both fixtures under `crates/oracle-harness/tests/fixtures/rank/`.</action>
  <verify>
    <automated>test -f crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iter1.txt && test -f crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iterN.txt && grep -q '^GRAD ' crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iter1.txt && grep -q '^HESS ' crates/oracle-harness/tests/fixtures/rank/lambdarank_gh_iter1.txt</automated>
  </verify>
  <acceptance_criteria>
    - Both `lambdarank_gh_iter1.txt` and `lambdarank_gh_iterN.txt` exist under `tests/fixtures/rank/`.
    - Each has a `GRAD ` line and a `HESS ` line of space-separated u32 decimals (parseable by `parse_gh`).
    - `rank_oracle_capture.py` contains a lambdarank_gh emit path; the uv `.venv` reports `lightgbm 4.6`.
  </acceptance_criteria>
  <done>The lambdarank real-lib_lightgbm grad/hess golden exists in the boosting `*_gh` bit format, closing the one capture gap (D-01).</done>
</task>

</tasks>

<artifacts_this_phase_produces>
This foundation plan produces:
- **Kernel module stubs (net-new files):** `crates/lgbm-compute/src/kernels/objective_regression.rs`, `objective_binary.rs`, `objective_multiclass.rs`, `objective_rank.rs` — empty compiling stubs, filled by 19-01..04.
- **mod.rs exports:** `pub mod objective_{regression,binary,multiclass,rank};` (ungated) in `kernels/mod.rs`.
- **Device-objective support-set (net-new):** `crates/lgbm-compute/src/device_objective.rs` — `DeviceObjectiveKind` enum + `device_objective_supported()` fn (SC #5 discriminator), re-exported from lib.rs.
- **Shared parity harness (net-new):** `crates/oracle-harness/tests/objective_common/mod.rs` — `parse_gh`, `read_boosting_golden`, `read_rank_golden`.
- **New fixtures:** `tests/fixtures/rank/lambdarank_gh_iter1.txt`, `lambdarank_gh_iterN.txt`.
- **Modified capture script:** `xtask/py/rank_oracle_capture.py` (lambdarank_gh emit path).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Numerical compute-kernel scaffold behind an OFF-by-default env flag (`LGBM_CUDA_ON_DEVICE`). No network, auth, session, PII, or untrusted-input surface. The only integrity concern is numerical correctness, covered by the anchor-parity gates in 19-01..04. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-00-01 | Tampering | device_objective_supported host-fallback set | mitigate | Unit test pins the 7-false / 11-true partition so an unsupported objective can never be silently routed on-device (SC #5) |
| T-19-00-SC | Tampering | pip install lightgbm==4.6 (capture fallback) | accept | Already-vendored/audited package (Phase-8 uv `.venv`); no new registry dependency (RESEARCH Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- `cargo build -p lgbm-compute` and `cargo build -p oracle-harness --tests` both succeed.
- `cargo test -p lgbm-compute device_objective` passes (support-set partition).
- `cargo test --workspace` stays green with `LGBM_CUDA_ON_DEVICE` unset (ODL-19 / D-06 — no regression to the byte-unchanged default path).
- `lambdarank_gh_iter{1,N}.txt` exist and parse as GRAD/HESS bit lines.
</verification>

<success_criteria>
- Four ungated objective kernel stub modules compile and are exported from mod.rs.
- `device_objective_supported` rejects all seven CUDA-unsupported objectives and accepts the eleven supported (SC #5 discriminator).
- `on_device_growth_supported()` still returns false; default host path byte-unchanged.
- Shared parity harness compiles with capture-gated skip-pass semantics.
- The lambdarank grad/hess golden is captured in the boosting `*_gh` format.
</success_criteria>

<output>
Create `.planning/phases/19-on-device-objectives/19-00-SUMMARY.md` when done.
</output>
