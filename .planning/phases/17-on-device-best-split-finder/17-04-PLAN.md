---
phase: 17-on-device-best-split-finder
plan: 04
type: execute
wave: 3
depends_on: ["17-03"]
files_modified:
  - crates/lgbm-compute/src/kernels/best_split.rs
autonomous: true
requirements: [ODL-11]
must_haves:
  truths:
    - "The _GlobalMemory stage-1 spill variant runs the SAME gain math over strided global loops for features with more bins than block threads (>256), bit-exact to the cpu f64 fold on the Phase-15 large-bin fixture (D-05)"
    - "The global-memory scratch buffers (feature_hist_grad/hess/stat + index) are pre-allocated ONCE outside the hot loop, never per-split (D-11)"
    - "The cpu single-owner fold handles >256 bins with the same serial body (no register/LDS cap, A4); the hip _GlobalMemory kernel is the net-new strided variant asserted against it"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/best_split.rs"
      provides: "split_eval_body_globalmem #[cube] strided variant + GlobalMemoryPrefixSum + pre-allocated scratch + globalmem launcher"
      contains: "globalmem"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/best_split.rs"
      to: "crates/lgbm-compute/src/kernels/split_info.rs"
      via: "scratch buffers pre-allocated once via the DeviceSplitInfo alloc-once idiom (device_allocations counter)"
      pattern: "client.empty|device_allocations"
---

<objective>
Build the `_GlobalMemory` stage-1 spill variant (D-05) for features with more bins than the
256-thread block can hold in registers/LDS. It runs the SAME gain math as 17-03's
`split_eval_body` but over strided global loops (`GlobalMemoryPrefixSum` + pre-allocated
global scratch), anchored by the Phase-15 synthetic large-bin / global-spill column (reuse
the Phase-16 D-04 fixture, num_bin > 256).

Purpose: SC#1 full >256-bin coverage. The user chose full coverage now (D-05) rather than the
shared-path-only shortcut. The cpu single-owner fold needs no separate implementation — its
serial body has no register/LDS cap, so it handles >256 bins by a larger loop bound (A4); the
hip `_GlobalMemory` strided kernel is the net-new part, asserted bit-exact against that fold.

Output: `split_eval_body_globalmem` + `GlobalMemoryPrefixSum` + the pre-allocated scratch
(`feature_hist_{grad,hess,stat}_buffer`, `feature_hist_index_buffer`, once per D-11) + the
globalmem launcher, turning the global-memory golden GREEN. Additive, env-gated (D-09); NO
f64 per-row hot loop (D-10). The discretized global-memory path is a C++ TODO and is out of
scope (v2/QGD-02).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-on-device-best-split-finder/17-CONTEXT.md
@.planning/phases/17-on-device-best-split-finder/17-RESEARCH.md
@.planning/phases/17-on-device-best-split-finder/17-PATTERNS.md
@.planning/phases/17-on-device-best-split-finder/17-03-SUMMARY.md
</context>

<artifacts_produced>
New symbols this plan creates (excluded from downstream drift verification):
- `best_split.rs`: `split_eval_body_globalmem::<N>` (the `#[cube]` strided >256-bin variant), a `GlobalMemoryPrefixSum` multi-pass scan helper, the pre-allocated scratch handles (`feature_hist_grad_buffer`, `feature_hist_hess_buffer`, `feature_hist_stat_buffer`, `feature_hist_index_buffer`) added to the stage-1 launcher's alloc-once constructor, and the globalmem launcher branch (`find_best_splits_stage1_on` dispatches to it when num_bin > threads).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: split_eval_body_globalmem — strided >256-bin variant + pre-allocated scratch (D-05/D-11)</name>
  <files>crates/lgbm-compute/src/kernels/best_split.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/best_split.rs (17-03: split_eval_body numerical core, the cpu single-owner fold, the f32 launcher, validate_stage1_inputs — the globalmem variant shares all the gain/count/guard math)
    - crates/lgbm-compute/src/kernels/split_info.rs (DeviceSplitInfo::new 267-354: the counted `alloc` closure at 289-293 + device_allocations counter at 369 — the alloc-once idiom to extend for the scratch buffers)
    - crates/lgbm-compute/src/kernels/primitives.rs (SharedMemory/sync_cube idiom; the multi-pass scan shape — GlobalMemoryPrefixSum is a global-scratch analog of the two-level scan)
    - LightGBM/src/treelearner/cuda/cuda_best_split_finder.cu (FindBestSplitsForLeafKernelInner_GlobalMemory + GlobalMemoryPrefixSum — the strided-loop variant; note the discretized _GlobalMemory is a C++ TODO and is out of scope)
    - 17-RESEARCH.md §"D-05 — _GlobalMemory spill variant (>256-bin blocks)" (strided loops, scratch buffers, cpu fold = same body with a larger bound, A4)
    - 17-PATTERNS.md §"Alloc-once, no per-split device alloc (D-11)" and §"No Analog Found" (the _GlobalMemory net-new row)
    - .planning/phases/16-on-device-histogram-constructor/16-CONTEXT.md (the D-04 large-bin / global-spill fixture this reuses)
  </read_first>
  <action>
    Add `split_eval_body_globalmem::<N: Numeric>` reusing 17-03's gain/count-recovery/guard/argmax logic verbatim (extract the shared inner math into a helper both bodies call, OR parameterize `split_eval_body` on a scan strategy — executor's discretion, but the gain/count/guard/default_left/argmax semantics MUST be identical so the anchor is one fold). The only difference is the scan: replace the in-block two-level scan with `GlobalMemoryPrefixSum` — a multi-pass inclusive scan writing partial sums to pre-allocated global scratch, with each thread processing bins `t, t+blockDim, t+2·blockDim, …` (strided). Complement stays `parent_total − scanned`; count recovery stays round-ties-even two-phase; default_left stays task.assume_out_default_left.
    Extend the stage-1 alloc-once constructor (the DeviceSplitInfo-style `new`) to pre-allocate `feature_hist_grad_buffer`, `feature_hist_hess_buffer`, `feature_hist_stat_buffer`, `feature_hist_index_buffer` ONCE, sized to `largest_feature_bin_count × num_concurrent_blocks`, via the counted `alloc` closure so `device_allocations` accounts for them (D-11 — never per-split). Add the size to `validate_stage1_inputs` (V5): reject a largest-bin-count that would overflow the scratch slab (`checked_mul`).
    Route the stage-1 launcher (`find_best_splits_stage1_on`) to dispatch: num_bin ≤ threads → the 17-03 shared/two-level path; num_bin > threads → the `_GlobalMemory` strided path. The cpu single-owner fold uses the SAME serial body with a larger loop bound (A4) — no separate cpu implementation.
    NO f64 per-row hot loop in the strided kernel (D-10); f32 mirror f32-pinned (WR-05).
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test best_split_parity best_split_parity_stage1_bit_exact_on_cpu</automated>
  </verify>
  <acceptance_criteria>
    - The global-memory golden (num_bin > 256) now passes compare_exact_f64_bits on the cpu fold — the `saw_globalmem` coverage boolean asserts true; the full best_split_parity_stage1 test is green for all 6 D-07 categories.
    - The scratch buffers are allocated in the alloc-once constructor: `device_allocations` reflects the +4 scratch allocations (unit test on the counter), and there is no `client.empty(` call inside any stage kernel body or per-split path (grep: `client.empty` appears only in the constructor).
    - `validate_stage1_inputs` rejects a largest-bin-count that overflows the scratch slab (unit test).
    - The gain/count/guard/argmax math is shared with 17-03 (grep: the globalmem body calls the same extracted helper OR the same gain fns — no duplicated divergent gain formula).
    - `grep -nE '\bf64\b' ` inside the f32 globalmem mirror body finds no un-pinned f64 (D-10).
  </acceptance_criteria>
  <done>The >256-bin spill variant is bit-exact to the cpu fold on the Phase-15 large-bin fixture; scratch pre-allocated once (D-11); all 6 stage-1 goldens green; gain math shared with 17-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host largest-bin-count → globalmem scratch sizing + strided launch | a host-provided bin count sizes the pre-allocated global scratch that the strided kernel indexes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering / DoS | _GlobalMemory launch with a largest-bin-count overflowing the scratch slab | mitigate | `validate_stage1_inputs` size-validates the spill scratch (`largest_bin_count × num_blocks` via `checked_mul`) and rejects overflow with a typed `ComputeError::Runtime` BEFORE the strided `launch_unchecked` (17-VALIDATION T-17-V5 globalmem row). |
| T-17-02 | Tampering | integer overflow in the strided index `t + k·blockDim` / scratch addressing | mitigate | Faithful u32/i32 widths; the scratch slab is pre-sized and checked; strided bounds derived from the validated bin count. |
| T-17-perf | DoS (perf) | f64 in the strided per-bin loop | mitigate | Grep audit (D-10): f64 only in scalar gain/count; strided loop f32-pinned. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (all internal). No install task exists. |
</threat_model>

<verification>
- `cargo test -p oracle-harness --test best_split_parity` — all 6 D-07 categories green including globalmem.
- `cargo test -p lgbm-compute --lib best_split` — scratch alloc-once counter + validate unit tests green.
- `cargo test --workspace` — merge gate green; default path byte-unchanged (D-09).
- `grep -c 'client.empty' crates/lgbm-compute/src/kernels/best_split.rs` — allocations confined to the constructor (no per-split alloc).
</verification>

<success_criteria>
- The `_GlobalMemory` >256-bin spill variant runs the same gain math over strided global loops, bit-exact to the cpu f64 fold on the Phase-15 large-bin fixture (D-05).
- Scratch buffers pre-allocated once (D-11); V5 size-validation guards the spill launch.
- The cpu fold reuses the same serial body (A4); the gain/count/guard math is shared with 17-03 (one anchor).
- No f64 per-row loop (D-10); default path byte-unchanged (D-09).
</success_criteria>

<output>
Create `.planning/phases/17-on-device-best-split-finder/17-04-SUMMARY.md` when done.
</output>
