---
phase: 17-on-device-best-split-finder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/lgbm-compute/src/kernels/best_split.rs
  - crates/lgbm-compute/src/kernels/mod.rs
  - crates/oracle-harness/tests/best_split_parity.rs
  - crates/oracle-harness/tests/fixtures/kernels/best_split.txt
autonomous: true
requirements: [ODL-11, ODL-12]
must_haves:
  truths:
    - "The round-ties-even count-recovery helper rounds k.5 to the nearest EVEN integer (not round-half-up) on the cpu f64 fold — per D-01"
    - "build_split_find_tasks reproduces the C++ assume_out_default_left task-gen table exactly; default_left ≠ reverse for MissingType::None / num_bin≤2 features"
    - "best_split_parity.rs harness parses best_split.txt bit-hex goldens, skips gracefully when the fixture is absent, and RED-fails against the stage stubs until Waves 2-4 land"
    - "The per-task RNG seed formula (extra_seed + task_index) is documented in the module from cuda_best_split_finder.cu:2220-2228 (Open Q1 locked)"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/best_split.rs"
      provides: "SplitFindTask struct, build_split_find_tasks host builder, round_ties_even helper, stage1/2/3 launcher stub signatures, module doc (RNG seed formula)"
      contains: "struct SplitFindTask"
      min_lines: 120
    - path: "crates/oracle-harness/tests/best_split_parity.rs"
      provides: "golden-anchor harness mirroring kernel_parity.rs (parse + skip-if-absent + coverage booleans)"
      contains: "fn best_split_parity"
      min_lines: 90
    - path: "crates/oracle-harness/tests/fixtures/kernels/best_split.txt"
      provides: "6-category golden fixture rows (default-template fwd+rev smaller+larger, USE_L1, USE_SMOOTHING, USE_RAND, empty/sparse-default-bin, global-memory spill)"
      contains: "COUNTS best_split="
  key_links:
    - from: "crates/lgbm-compute/src/kernels/mod.rs"
      to: "crates/lgbm-compute/src/kernels/best_split.rs"
      via: "pub mod best_split; declaration"
      pattern: "pub mod best_split"
    - from: "crates/oracle-harness/tests/best_split_parity.rs"
      to: "crates/oracle-harness/tests/fixtures/kernels/best_split.txt"
      via: "kernels_dir().join(\"best_split.txt\") skip-if-absent read"
      pattern: "best_split\\.txt"
---

<objective>
Stand up the Phase-17 Wave-0 test infrastructure and the pure-host scaffolding the stage
kernels consume, so every downstream task (Waves 2-4) has an automated verify BEFORE the
kernel is written. This plan front-loads the two riskiest parity landmines — the
round-ties-even count-recovery rounding (D-01) and the `assume_out_default_left` task-gen
table (Pitfall 3) — as fully testable pure-host units, and creates the golden-anchor harness
+ fixture matrix (D-07).

Purpose: Wave-0-first discipline (17-VALIDATION.md). The kernel plans that follow reference
`best_split.rs` stage-launcher signatures and drive `best_split_parity.rs`; both must exist
and compile first, RED-failing until the numerical core lands. It also locks Open Q1 (the
per-task RNG seed formula) in the module doc from the C++ reference.

Output: `best_split.rs` (module skeleton: `SplitFindTask`, `build_split_find_tasks`,
`round_ties_even`, stage1/2/3 launcher stubs), `best_split_parity.rs` (harness),
`best_split.txt` (fixtures), `mod.rs` registration. Additive, off by default
(`LGBM_CUDA_ON_DEVICE`); `on_device_growth_supported()` stays false; default path
byte-unchanged (D-09).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-on-device-best-split-finder/17-CONTEXT.md
@.planning/phases/17-on-device-best-split-finder/17-RESEARCH.md
@.planning/phases/17-on-device-best-split-finder/17-PATTERNS.md
@.planning/phases/17-on-device-best-split-finder/17-VALIDATION.md
</context>

<artifacts_produced>
New symbols this plan creates (excluded from downstream drift verification):
- `best_split.rs`: `struct SplitFindTask` (mirrors `cuda_best_split_finder.hpp:28-41`), `fn build_split_find_tasks`, `fn round_ties_even` (host f64 + the branch-free even-round identity), stage-1/2/3 launcher STUB signatures (`find_best_splits_stage1_on`, `sync_best_split_for_leaf_on`, `find_best_from_all_splits_on` — names at executor discretion, filled in Waves 2-4), module doc block recording the per-task RNG seed formula `extra_seed + task_index`.
- `best_split_parity.rs`: `fn best_split_parity_stage1_bit_exact_on_cpu` + parse helpers (`parse_best_split`, `field`, `parse_f64_bits` mirrors) + coverage booleans.
- `best_split.txt`: the 6-category golden fixture (D-07 matrix).
- `mod.rs`: `pub mod best_split;` line.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: best_split.rs skeleton — SplitFindTask, round-ties-even helper, stage-launcher stubs, RNG-seed doc</name>
  <files>crates/lgbm-compute/src/kernels/best_split.rs, crates/lgbm-compute/src/kernels/mod.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/split.rs (lines 1-115: module doc + `round_int` at 108-111 — the WRONG round-half-up we DIVERGE from; MLIR constraints doc 180-202)
    - crates/lgbm-compute/src/kernels/split_info.rs (lines 68-160: `SplitScalars` field list — the CUDASplitInfo analog this phase's records use)
    - crates/lgbm-compute/src/kernels/mod.rs (the `pub mod` block; add the new declaration alongside `split`/`split_info`/`random`)
    - LightGBM/src/treelearner/cuda/cuda_best_split_finder.hpp (SplitFindTask struct, lines 28-41: inner_feature_index, reverse, skip_default_bin, na_as_missing, assume_out_default_left, is_categorical, is_one_hot, hist_offset, mfb_offset, num_bin, default_bin, rand_threshold)
    - LightGBM/src/treelearner/cuda/cuda_best_split_finder.cu (lines 2220-2228 InitCUDARandomKernel: `cuda_randoms[task_index].SetSeed(seed + task_index)` — Open Q1 seed formula)
    - 17-RESEARCH.md §"Count Recovery" (the round-ties-even branch-free identity) and §"Common Pitfalls" Pitfall 1
    - 17-PATTERNS.md §"Count-recovery rounding — DIVERGE from the analog (D-01 landmine)"
  </read_first>
  <action>
    Create `crates/lgbm-compute/src/kernels/best_split.rs` and register `pub mod best_split;` in `mod.rs` (ungated, alongside `random`/`split`/`split_info` — NOT `#[cfg(feature = "gpu")]` — so the cpu f64 anchor exercises it, D-08). Write the module doc block: state it ports `cuda_best_split_finder.cu` §8 behind `LGBM_CUDA_ON_DEVICE` (D-09), cite the per-task RNG seed formula `extra_seed + task_index` from cu:2220-2228 (Open Q1 — locked, so the Wave-2 USE_RAND path has no open flag), and note `on_device_growth_supported()` stays false.
    Define `struct SplitFindTask` mirroring the C++ struct field-for-field (hpp:28-41) with faithful widths (`i32` inner_feature_index and rand_threshold = `data_size_t`; `u32` hist_offset/num_bin/default_bin; `u8` mfb_offset; `bool` for the six flags). Derive Debug/Clone/Copy/PartialEq for test assertions.
    Implement `round_ties_even(x: f64) -> i32` = round-to-nearest-ties-to-EVEN, faithful to CUDA `__double2int_rn` (NOT `split.rs::round_int`'s round-half-up). Primary body: `x.round_ties_even() as i32`. Because A1 (17-RESEARCH Assumptions) is unverified for cubecl-cpu `#[cube]` lowering, ALSO provide the branch-free even-round identity from 17-RESEARCH §"Count Recovery" as the fallback and leave a doc note pointing Wave 2 to use whichever cubecl-cpu lowers inside `#[cube]`. Hessian·cnt_factor ≥ 0, so the x≥0 identity is sufficient.
    Add stage-1/2/3 launcher STUB signatures (choose descriptive names, e.g. `find_best_splits_stage1_on` / `sync_best_split_for_leaf_on` / `find_best_from_all_splits_on`) generic over `R: cubecl::Runtime`, taking `&ComputeClient` + the pre-allocated `DeviceSplitInfo` handles + task scalars, returning the shapes the harness asserts. Stubs must COMPILE and return a sentinel (e.g. an all-`is_valid=false` record / empty export) so the Wave-0 harness RED-fails via assertion, not by failing to build. Do NOT implement the numerical core here (Waves 2-4). Follow the MLIR constraints doc (split.rs:180-202) in the stub signatures so Wave 2 does not have to reshape them.
    Add `#[cfg(test)]` unit tests: `count_recovery_ties_even` proving 0.5→0, 1.5→2, 2.5→2, 3.5→4 (ties-to-even) and 2.4→2, 2.6→3 (non-tie); contrast one case where round-half-up would give a different answer (2.5→3) to lock the divergence from `split.rs::round_int`.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --lib best_split::tests::count_recovery_ties_even</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p lgbm-compute` succeeds; `grep -q 'pub mod best_split' crates/lgbm-compute/src/kernels/mod.rs` passes.
    - `grep -q 'struct SplitFindTask' crates/lgbm-compute/src/kernels/best_split.rs` and all 12 C++ fields present (grep each field name).
    - `count_recovery_ties_even` unit test passes, including the 2.5→2 (even) and the round-half-up-would-give-3 contrast assertion.
    - Module doc contains the string `extra_seed + task_index` (grep) citing cu:2220-2228. To keep this literal out of any negative-grep gate, this is documentation prose only, not an acceptance-criterion negative-grep target.
    - No `#[cfg(feature = "gpu")]` guard on the `best_split` mod declaration (grep the mod.rs line).
  </acceptance_criteria>
  <done>SplitFindTask + round_ties_even + stage stubs compile; RNG seed locked in doc; ties-even test green; mod registered ungated.</done>
</task>

<task type="auto">
  <name>Task 2: build_split_find_tasks — the assume_out_default_left task-gen table (host)</name>
  <files>crates/lgbm-compute/src/kernels/best_split.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/best_split.rs (Task 1's SplitFindTask struct)
    - LightGBM/src/treelearner/cuda/cuda_best_split_finder.cpp (lines 137-227: the task-gen loop that sets reverse / skip_default_bin / na_as_missing / assume_out_default_left / is_categorical / is_one_hot per inner feature)
    - 17-RESEARCH.md §"Tie-Aware default_left" — the full 4-row task-gen table (the authoritative transcription)
    - 17-RESEARCH.md §"Common Pitfalls" Pitfall 3 (`default_left = assume_out_default_left`, NOT REVERSE)
  </read_first>
  <action>
    Implement `build_split_find_tasks(features: &[FeatureMeta]) -> Vec<SplitFindTask>` (define a small `FeatureMeta` input carrying num_bin, missing_type {None,Zero,NaN}, is_categorical, max_cat_to_onehot, hist_offset, mfb_offset, default_bin, rand_threshold source) reproducing the C++ table EXACTLY per 17-RESEARCH §"Tie-Aware default_left":
      • num_bin>2 && missing==Zero && !categorical → emit forward task (skip_default_bin, !na, assume_out_default_left=false) THEN reverse task (skip_default_bin, !na, assume_out_default_left=true).
      • num_bin>2 && missing==NaN && !categorical → emit forward (na_as_missing, assume=false) THEN reverse (na_as_missing, assume=true).
      • num_bin<=2 or missing==None, non-categorical → single reverse task, assume_out_default_left = (missing != NaN) ? true : false.
      • categorical → single forward task (is_one_hot = num_bin <= max_cat_to_onehot), assume_out_default_left = false (the Phase-22 dispatch seam per D-04 — wire the is_categorical/is_one_hot fields, do NOT implement categorical eval math).
    Emit tasks in the C++ order so task indices line up with the smaller/larger stream split the Wave-3 stage-2 reader expects (smaller task t ↔ [t], larger task t ↔ [t+num_tasks]).
    Add `#[cfg(test)]` unit test `assume_out_default_left_table` asserting all four rows, including the load-bearing divergence: a MissingType::None feature yields a single reverse task with `assume_out_default_left=false` while `reverse=true` (proving default_left ≠ reverse — Pitfall 3), and a num_bin=2 Zero-missing feature yields a single reverse task with assume=true.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --lib best_split::tests::assume_out_default_left_table</automated>
  </verify>
  <acceptance_criteria>
    - `assume_out_default_left_table` test passes for all four table rows.
    - Test explicitly asserts a case where `reverse == true && assume_out_default_left == false` (MissingType::None) and one where `reverse == true && assume_out_default_left == true` (Zero-missing num_bin>2 reverse task).
    - Categorical feature path emits exactly one forward task with is_categorical=true and the correct is_one_hot boolean; no categorical eval math is present (grep: no `BitonicArgSort`/`cat_threshold` compute in best_split.rs — dispatch seam only).
    - Task emission order matches the C++ forward-then-reverse pairing (test asserts task[0].reverse==false, task[1].reverse==true for a num_bin>2 Zero feature).
  </acceptance_criteria>
  <done>The task-gen table is bit-faithful to cuda_best_split_finder.cpp:137-227; default_left decoupled from reverse; categorical seam wired without eval math.</done>
</task>

<task type="auto">
  <name>Task 3: best_split_parity.rs harness + best_split.txt fixture matrix (D-07)</name>
  <files>crates/oracle-harness/tests/best_split_parity.rs, crates/oracle-harness/tests/fixtures/kernels/best_split.txt</files>
  <read_first>
    - crates/oracle-harness/tests/kernel_parity.rs (kernels_dir at 41; parse_f64_bits at 46; field at 64; parse_split at 285; kernel_parity_split_bit_exact_on_cpu at 511-522 skip-if-absent idiom; cpu_client/CpuBackend at 521-522; coverage-tracking asserts at 648-652)
    - crates/oracle-harness/tests/fixtures/kernels/split.txt (the bit-hex golden format: SCASE/SHIST/SWIN lines, all floats as u64/u32 decimal bit-patterns)
    - crates/lgbm-compute/src/kernels/best_split.rs (Task 1 stage-launcher stub signatures + SplitFindTask)
    - 17-RESEARCH.md §"D-07 — Fixture / golden matrix" (the 6-row matrix and the RNG-stream golden detail) and §"3-Stage Reduction & Export" (the 8-int buffer field layout)
    - 17-PATTERNS.md §"crates/oracle-harness/tests/best_split_parity.rs" (parse+assert+coverage shape to mirror)
  </read_first>
  <action>
    Create `best_split_parity.rs` mirroring `kernel_parity.rs`: copy the `kernels_dir()` + skip-if-absent read idiom verbatim (511-522) so an absent fixture SKIPs (never fails CI), and `assert!(!cases.is_empty(), ...)` when present. Reuse the bit-hex parse helpers (`parse_f64_bits` via `f64::from_bits`, `field(tokens,key)` keyed lookup, `parse_u32`) — import `oracle_harness::comparator::compare_exact_f64_bits`.
    Write the primary test `best_split_parity_stage1_bit_exact_on_cpu`: parse each SCASE (task scalars: num_bin, offset, default_bin, skip_default_bin, na_as_missing, use_l1, use_smoothing, use_rand, is_larger, min_data_in_leaf, min_sum_hessian_in_leaf, lambda_l1/l2, min_gain_to_split, path_smooth, parent_output, rng_seed) + the interleaved SHIST `[g0,h0,g1,h1,...]` + the expected `CUDASplitInfo` record (SWIN: is_valid, threshold, default_left, left/right count, left/right sum grad+hess, value, gain) + for stage3 cases the 8-int export (SEXPORT). Drive the `best_split.rs` stage stub via `cpu_client()`/`CpuBackend` and `compare_exact_f64_bits` field-by-field. Against the Wave-0 stubs this test RED-fails (sentinel record) — that is the intended Nyquist scaffold; Waves 2-4 turn it green.
    Add coverage-tracking booleans and assert each D-07 category was exercised: `saw_default_template`, `saw_use_l1`, `saw_smoothing`, `saw_rand`, `saw_empty`, `saw_globalmem`, `saw_default_left_tie`.
    Create `best_split.txt` with COUNTS header + one SCASE per D-07 row (17-RESEARCH §D-07 matrix): default-template (forward+reverse, smaller+larger — 4 records), USE_L1 (lambda_l1=0.1), USE_SMOOTHING (path_smooth=2.0 with parent_output set), USE_RAND (extra_trees seed + the drawn rand_threshold from CUDARandom.NextInt(0,num_bin-2)), empty/sparse-default-bin (all bins fail guards → is_valid=false; a skip_default_bin case), global-memory spill (num_bin>256, reuse the Phase-15/Phase-16 D-04 large-bin fixture shape). Golden values are hand-transcribed C++-core expected records (the histogram-golden precedent, 17-RESEARCH A5) stored as bit-hex; the exact numeric values may be finalized by the Wave-2 cpu fold and cross-checked, but the fixture STRUCTURE (all 6 categories, all fields) is complete now. PROVENANCE GUARD (anti-circularity): any Wave-2 "finalization" of these golden numeric values MUST be re-derived from the C++ `cuda_best_split_finder.cu` §8.1 accumulation (hand-transcription), NOT copied from the 17-03 cpu-fold output — copying the cpu fold's own result back into the golden it is checked against would make the anchor self-referential (a golden that always passes). The cpu fold and the golden must remain independent derivations of the same C++ reference. Cross-referenced in 17-03 Task 1.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test best_split_parity -- --list</automated>
  </verify>
  <acceptance_criteria>
    - `best_split_parity.rs` compiles; `cargo test -p oracle-harness --test best_split_parity -- --list` shows `best_split_parity_stage1_bit_exact_on_cpu`.
    - With the fixture present, the test does NOT skip; with it absent it SKIPs via the copied kernel_parity idiom (grep for the `SKIP` eprintln + early return).
    - `best_split.txt` contains `COUNTS best_split=` and one SCASE per D-07 category (grep each: use_l1=1, use_smoothing=1, use_rand=1, plus a num_bin>256 record and an is_valid=0 empty record).
    - The coverage booleans (`saw_use_l1`, `saw_smoothing`, `saw_rand`, `saw_globalmem`, `saw_empty`, `saw_default_left_tie`) are all asserted true at test end.
    - Test RED-fails against the Wave-0 stubs (expected) — confirm the failure is an assertion mismatch, not a compile/panic error.
  </acceptance_criteria>
  <done>Golden-anchor harness + 6-category fixture exist; skip-if-absent works; every downstream kernel task now has an automated verify; coverage of all D-07 flags asserted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host task-builder → SplitFindTask[] | host-constructed scalars (num_bin, mfb_offset, offsets) that Wave-2 kernels index device buffers with |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | build_split_find_tasks output feeding later launch bounds | mitigate | SplitFindTask carries faithful C++ widths (i32/u32/u8); Wave-2/3/4 launch-boundary validation (see 17-03/04/05 threat models) checks num_bin/num_tasks against the pre-allocated DeviceSplitInfo slabs before launch_unchecked. This plan produces only host structs + tests — no device launch. |
| T-17-perf | DoS (perf) | round_ties_even helper | accept | Scalar count-recovery math is inherently f64/double in §8.1 (D-10 permits f64 in scalar/gain math, never per-row); the helper is scalar, not a per-row hot loop. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (all internal reuse — 17-RESEARCH Standard Stack: "Package Legitimacy Audit N/A"). No install task exists. |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --lib best_split` — Task 1 + Task 2 unit tests green (ties-even, task-gen table).
- `cargo test -p oracle-harness --test best_split_parity -- --list` — harness compiles and is discoverable; RED against stubs is expected.
- `cargo build --workspace` — additive; default path byte-unchanged (D-09); `on_device_growth_supported()` still false.
- `grep -c 'pub mod best_split' crates/lgbm-compute/src/kernels/mod.rs` == 1.
</verification>

<success_criteria>
- SplitFindTask, round_ties_even, build_split_find_tasks land as tested pure-host units; the two riskiest landmines (count rounding, task-gen table) are locked before the kernel.
- The golden-anchor harness + 6-category fixture exist; skip-if-absent works; every Wave-2/3/4 task has a green-able automated verify.
- Open Q1 (RNG seed = extra_seed + task_index) documented from the C++ reference.
- Merge gate unaffected: `cargo test --workspace` still green (new tests are additive; the RED best_split_parity is expected until Wave 2 — mark it `#[ignore]` with a doc note OR keep it RED-tracked per the Nyquist scaffold; executor's discretion, but the workspace default-test run must not go red — prefer `#[ignore = "Wave-0 scaffold; un-ignore when 17-03 lands"]` on the stage1 parity test until the core exists).
</success_criteria>

<output>
Create `.planning/phases/17-on-device-best-split-finder/17-01-SUMMARY.md` when done.
</output>
