---
phase: 21-end-to-end-on-device-driver-integration-parity-gate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ODL-18, ODL-19, ODL-18H]

must_haves:
  truths:
    - "ODL-18 and ODL-19 are marked Complete and attributed to Phase 20 (where they were delivered) in REQUIREMENTS.md and its traceability + rollup tables"
    - "A new ODL-18H hardening requirement exists, mapped to Phase 21"
    - "The ROADMAP Phase 21 body reflects the hardening scope, not the stale driver-integration text"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ODL-18/ODL-19 marked Complete (Phase 20); new ODL-18H checklist + traceability + rollup rows"
      contains: "ODL-18H"
    - path: ".planning/ROADMAP.md"
      provides: "Re-cut Phase 21 body (Goal / Success Criteria / Notes) for the hardening scope"
      contains: "Harden"
  key_links:
    - from: ".planning/REQUIREMENTS.md (traceability table)"
      to: "Phase 20 VERIFICATION (ODL-18/19 delivered 6/6)"
      via: "ODL-18/ODL-19 rows updated to Phase 20 / Complete"
      pattern: "ODL-18 \\| Phase 20 \\| Complete"
---

<objective>
Reconcile the D-05 requirement/ROADMAP bookkeeping for the Phase 21 re-cut:
1. Mark ODL-18 and ODL-19 Complete (delivered + verified in Phase 20, 6/6) in
   REQUIREMENTS.md — the checklist, the traceability table (attribute to Phase 20), and the
   per-phase rollup.
2. Add a new ODL-18H hardening requirement (targeted STRUCTURE parity corpus + WR-01
   `HistArena::swap` free-slot fix confirmation + repro, cpu-f64-anchored, cubecl-cpu default
   lane), mapped to Phase 21.
3. Re-cut the STALE ROADMAP Phase 21 body (Goal / Success Criteria / Notes) to the hardening
   scope; record that the Notes open question (data→leaf `Handle` aliasing / batched
   `client.read`) does NOT affect the Phase-21 parity gate.

Purpose: the planning docs currently contradict Phase-20 reality (ODL-18/19 show pending
under Phase 21). This closes the audit trail honestly. Pure docs edit — no code, no behavior.
Output: consistent REQUIREMENTS.md + a re-cut ROADMAP Phase 21 body.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-end-to-end-on-device-driver-integration-parity-gate/21-RESEARCH.md
@.planning/phases/21-end-to-end-on-device-driver-integration-parity-gate/21-PATTERNS.md

# The two docs this plan edits + the proof ODL-18/19 were delivered
@.planning/REQUIREMENTS.md
@.planning/phases/20-on-device-score-updater-metrics/20-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md — mark ODL-18/19 Complete + add ODL-18H</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md: checklist ODL-18 (:50), ODL-19 (:51); traceability rows ODL-18 (:105), ODL-19 (:106); per-phase rollup "Phase 21 — Driver integration + parity gate | ODL-18, ODL-19 | 2" (:122); Phase 20 rollup row (:121); the coverage summary (:126-131).
    - .planning/phases/20-on-device-score-updater-metrics/20-VERIFICATION.md — proof ODL-18/19 delivered in Phase 20 (crit 5 STRUCTURE gate, crit 6 no-f64-per-row; requirements_verified includes ODL-18, ODL-19).
    - .planning/phases/21-end-to-end-on-device-driver-integration-parity-gate/21-RESEARCH.md §"D-05 Bookkeeping Mechanics (exact edit surface)" — the exact line edits.
    - .planning/phases/21-end-to-end-on-device-driver-integration-parity-gate/21-PATTERNS.md §REQUIREMENTS.md.
  </read_first>
  <action>Edit `.planning/REQUIREMENTS.md` with scoped edits only (do NOT rewrite the file):
    (a) Change the ODL-18 checklist box (:50) from pending to checked (`- [x]`); append a short parenthetical noting delivered + verified Phase 20 (VERIFICATION 6/6). Same for ODL-19 (:51).
    (b) Add a new checklist item near ODL-18/19 in the "Driver integration, feature coverage & parity" section: an unchecked `**ODL-18H**` entry describing "Hardening of the on-device driver — targeted STRUCTURE parity corpus (deep >2-live-leaf, no-split break, min-data/min-hessian-constrained), the WR-01 HistArena::swap free-slot fix confirmation + repro tests, all anchored to the cubecl-cpu f64 fold, cubecl-cpu default lane; additive + LGBM_CUDA_ON_DEVICE-gated. Mapped to Phase 21." Leave it unchecked (checked at phase completion).
    (c) Traceability table: change the ODL-18 row (:105) to `| ODL-18 | Phase 20 | Complete |`; the ODL-19 row (:106) to `| ODL-19 | Phase 20 | Complete |`; add a new row `| ODL-18H | Phase 21 | Pending |` in ODL order (after ODL-19).
    (d) Per-phase rollup (:122): re-cut the "Phase 21 — Driver integration + parity gate | ODL-18, ODL-19 | 2" row to "Phase 21 — On-device driver hardening + parity corpus | ODL-18H | 1". Update the Phase 20 rollup row (:121) so ODL-18/ODL-19 roll up there too (e.g. "Phase 20 — Score updater & metrics + driver integration | ODL-16, ODL-17, ODL-18, ODL-19 | 4") so the traceability and rollup tables stay consistent.
    (e) Update the coverage summary (:126-131): total v1.1 requirements becomes 23 (was 22, +ODL-18H); mapped 23 (100%); keep "each requirement mapped to exactly one phase" true (ODL-18/19 → Phase 20, ODL-18H → Phase 21). Fix any arithmetic so the rollup counts sum consistently.
  Do not touch unrelated requirements or reorder existing rows beyond the specified inserts.</action>
  <verify>
    <automated>bash -c 'grep -q "ODL-18H" .planning/REQUIREMENTS.md && grep -q "ODL-18 | Phase 20 | Complete" .planning/REQUIREMENTS.md && grep -q "ODL-19 | Phase 20 | Complete" .planning/REQUIREMENTS.md && echo BOOKKEEPING_OK'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ODL-18H" .planning/REQUIREMENTS.md` >= 3 (checklist item + traceability row + rollup row).
    - `grep -q "ODL-18 | Phase 20 | Complete" .planning/REQUIREMENTS.md` and `grep -q "ODL-19 | Phase 20 | Complete" .planning/REQUIREMENTS.md` both succeed.
    - The ODL-18 and ODL-19 checklist lines (:50-51) are `- [x]` (checked).
    - The Phase 21 rollup row references ODL-18H (not ODL-18/ODL-19); the coverage summary totals 23 requirements, 100% mapped, no orphans/duplicates.
  </acceptance_criteria>
  <done>REQUIREMENTS.md consistently records ODL-18/19 as Complete under Phase 20 and ODL-18H as the new Phase-21 hardening requirement; traceability, rollup, and coverage counts agree.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md — re-cut the stale Phase 21 body to the hardening scope</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md: the Phase 21 checklist line (:86, already says "Hardening/Slack ... Re-cut via /gsd-phase before planning"); the STALE Phase 21 body (:289-302 — Goal/Depends/Requirements/Success Criteria/Plans/Notes); the Phase 22 body (:304-317) as the format reference; the Phase 20 Requirements/Notes lines (:255, :287) that already record the D-01 pull-forward.
    - .planning/phases/21-end-to-end-on-device-driver-integration-parity-gate/21-RESEARCH.md §"D-05 Bookkeeping Mechanics > ROADMAP.md" + §"ROADMAP Notes open question" (data→leaf aliasing moot; batched read is Phase-23 perf).
    - .planning/phases/21-end-to-end-on-device-driver-integration-parity-gate/21-CONTEXT.md §domain (in-scope: WR-01 confirmation, targeted parity corpus, bookkeeping; out: categorical → Phase 22, perf/default-on → Phase 23).
  </read_first>
  <action>Rewrite ONLY the Phase 21 body (:289-302) via scoped edits (do NOT touch other phases). Update the `####` heading to reflect the hardening re-cut (e.g. "Phase 21: Harden the On-Device Driver (parity corpus + WR-01 confirmation + bookkeeping)"). Re-cut:
    - **Goal**: harden the just-landed on-device grow loop on the continuous-feature slice — confirm the WR-01 HistArena::swap free-slot fix, broaden STRUCTURE parity evidence with a small targeted set of risk cases (deep >2-live-leaf, no-split break, min-data/min-hessian-constrained) each pinned to the cpu f64 anchor, and reconcile the ODL-18/19 bookkeeping. No driver re-implementation (D-01: delivered by Phase 20).
    - **Requirements**: ODL-18H (note ODL-18/ODL-19 delivered in Phase 20 per D-01).
    - **Success Criteria** (what must be TRUE): (1) the three targeted STRUCTURE gates are bit-exact to the cpu f64 anchor (tie-aware default_left, leaves ~1e-5) on the cubecl-cpu default lane, non-vacuous under LGBM_CUDA_ON_DEVICE=1; (2) WR-01 confirmed closed (c9a7fd1) with repro tests green, documented as unit-test-locked (the live driver keeps per-leaf Vec<f64>, does not consume HistArena); (3) an additive grow_tree_on_device_driver_with_cfg exists with the trait seam untouched; (4) env-unset workspace stays byte-unchanged green; (5) REQUIREMENTS.md/ROADMAP bookkeeping reconciled.
    - **Notes**: explicitly out of scope — on-device categorical splits → Phase 22 (ODL-22); perf-validation / default-ON rollout DoD → Phase 23 (ODL-20/21). Record the resolved open question: data→leaf Handle aliasing is MOOT for the live driver (it carries host per-leaf rows and rebuilds LeafPartitionLayout at the end); batched client.read(vec![h]) is a Phase-23 perf concern — neither affects the Phase-21 parity gate.
    Keep the `**Plans**:` line updated (this plan set: 3 plans). Optionally tighten the :86 checklist line to drop "Re-cut via /gsd-phase before planning" now that the re-cut is done.</action>
  <verify>
    <automated>bash -c 'grep -qi "Harden" .planning/ROADMAP.md && grep -q "ODL-18H" .planning/ROADMAP.md && grep -qi "Phase 23" .planning/ROADMAP.md && echo ROADMAP_RECUT_OK'</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 21 `####` body Goal/Success Criteria/Notes reference the hardening scope (WR-01 confirmation, targeted parity corpus, bookkeeping) — NOT the stale "end-to-end driver orchestrates the full per-leaf grow loop" text.
    - The Phase 21 body lists Requirements as ODL-18H and notes ODL-18/19 delivered in Phase 20.
    - The Notes explicitly defer categorical → Phase 22 and perf/default-on → Phase 23, and record the data→leaf-aliasing / batched-read open question as not affecting the parity gate.
    - No other phase body (20, 22, 23) is modified beyond the Phase 21 section (and the optional :86 checklist tightening).
  </acceptance_criteria>
  <done>The ROADMAP Phase 21 body accurately describes the hardening re-cut, defers categorical/perf work to Phases 22/23, and resolves the stale Notes open question.</done>
</task>

</tasks>

<artifacts_produced>
Symbols/IDs this plan creates (bookkeeping):
- `ODL-18H` — new hardening requirement ID (REQUIREMENTS.md checklist + traceability + rollup), mapped to Phase 21.
- ODL-18 / ODL-19 — re-attributed to Phase 20, marked Complete.
- Re-cut ROADMAP Phase 21 body (Goal / Requirements / Success Criteria / Notes).
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Planning-doc (markdown) edits only. No code, no external input, network, auth, or executable surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Repudiation (inaccurate audit trail) | REQUIREMENTS.md / ROADMAP.md status | mitigate | Attribute ODL-18/19 to Phase 20 with the VERIFICATION 6/6 citation; add ODL-18H for the actual Phase-21 work; keep traceability, rollup, and coverage counts internally consistent (grep-verified). |
| T-21-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | No packages installed; docs-only edit. |
</threat_model>

<verification>
- `grep -q "ODL-18H" .planning/REQUIREMENTS.md` and the two `ODL-18/19 | Phase 20 | Complete` rows present.
- REQUIREMENTS coverage summary totals 23, 100% mapped, no orphans/duplicates.
- ROADMAP Phase 21 body re-cut to hardening scope; categorical/perf deferred to Phases 22/23.
- No unrelated phase bodies or requirements modified.
</verification>

<success_criteria>
- ODL-18/19 Complete under Phase 20; ODL-18H added and mapped to Phase 21.
- ROADMAP Phase 21 body reflects the hardening re-cut with the open question resolved.
- Bookkeeping tables are internally consistent.
</success_criteria>

<output>
Create `.planning/phases/21-end-to-end-on-device-driver-integration-parity-gate/21-03-SUMMARY.md` when done.
</output>
