---
phase: 16-on-device-histogram-constructor
plan: 05
type: execute
wave: 3
depends_on: [16-04]
files_modified: []
autonomous: false
requirements: [ODL-09, ODL-10]
must_haves:
  truths:
    - "The full workspace suite is green with default features and LGBM_CUDA_ON_DEVICE unset (the hard merge gate, ODL-19)"
    - "No new build/fix kernel introduces an f64 per-row hot loop (grep-verified) and no kernel uses Atomic<i64>"
    - "CPU / ROCm / existing-host-CUDA paths are byte-unchanged (the shipped per-feature build kernel diff is empty)"
    - "On the physical ROCm backend, the on-device build/fix/subtract are within ~1e-6 of the cpu f64 anchor (human-verified; never GPU-vs-GPU)"
  artifacts:
    - path: ".planning/phases/16-on-device-histogram-constructor/16-05-SUMMARY.md"
      provides: "merge-gate sign-off record (workspace green, byte-unchanged default, ROCm parity attested)"
  key_links:
    - from: "the merge gate"
      to: "all Phase 16 kernels"
      via: "cargo test --workspace + grep gates + ROCm parity human-verify"
      pattern: "cargo test"
---

<objective>
Wave 3: the hard merge gate (D-07, ODL-19). Prove the whole phase is additive and parity-clean:
the full workspace suite is green with default features and `LGBM_CUDA_ON_DEVICE` unset; no new
kernel introduced an f64 per-row hot loop or `Atomic<i64>`; the shipped per-feature ROCm build is
byte-unchanged; and on the physical ROCm backend the on-device build/fix/subtract are within ~1e-6
of the cpu f64 anchor (the one manual-only check per VALIDATION.md — requires the GPU backend,
never GPU-vs-GPU).

Purpose: this is the non-negotiable contract — Phase 16 must not weaken the bit-exact cpu f64
merge gate nor change any default path.

Output: the gate run + a human-verified ROCm parity attestation, recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-on-device-histogram-constructor/16-CONTEXT.md
@.planning/phases/16-on-device-histogram-constructor/16-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hard merge gate — workspace green, no f64 hot loop, no Atomic<i64>, default byte-unchanged (D-07/D-08/ODL-19)</name>
  <read_first>
    - .planning/phases/16-on-device-histogram-constructor/16-VALIDATION.md (Sampling Rate: phase gate = cargo test --workspace GREEN before verify-work; D-07 byte-unchanged default)
    - crates/lgbm-compute/src/kernels/histogram.rs (the new construct_leaf_hist_partition_u64 + fix_histogram_mfb — the grep targets for f64-hot-loop / Atomic<i64>)
    - .planning/phases/16-on-device-histogram-constructor/16-CONTEXT.md (D-07/D-08; the merge gate is the hard contract)
  </read_first>
  <action>
    Run the full merge gate with default features and `LGBM_CUDA_ON_DEVICE` unset: `cargo test`
    (workspace) must be GREEN. Verify D-08 by grep: the new build/fix kernels
    (`construct_leaf_hist_partition_u64`, `fix_histogram_mfb`) contain NO f64 accumulation in their
    per-row hot loops (f64 only in the post-merge de-quant and scalar/gain math) and NO `Atomic<i64>`
    anywhere (use a comment-stripping grep, e.g. `grep -vE '^\s*//'` before counting, to avoid
    matching doc prose). Verify the shipped per-feature `construct_leaf_hist_resident_lds_kernel_u64`
    is byte-unchanged (git diff against its pre-phase state is empty for that function body). Confirm
    no existing CPU/ROCm/host-CUDA default path changed behavior (the on-device entry is reachable only
    via the OFF-by-default seam). Record each gate result (workspace pass count, grep results, the
    byte-unchanged confirmation) in the SUMMARY.
  </action>
  <verify>
    <automated>cargo test 2>&1 | tail -15 && grep -vE '^\s*//' crates/lgbm-compute/src/kernels/histogram.rs | grep -c 'Atomic<i64>'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` (default features, LGBM_CUDA_ON_DEVICE unset) is GREEN across the workspace.
    - The `Atomic<i64>` grep (comment-stripped) returns 0 over histogram.rs.
    - No f64 add appears inside the per-row scatter of `construct_leaf_hist_partition_u64` or the fold of `fix_histogram_mfb` beyond the documented de-quant/scalar math.
    - `git diff` shows the shipped `construct_leaf_hist_resident_lds_kernel_u64` body is unchanged.
  </acceptance_criteria>
  <done>The full workspace suite is green with the default path byte-unchanged; no new f64 per-row hot loop and no Atomic<i64>; results recorded in the SUMMARY.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The on-device histogram build (dense+sparse × shared+global spill), FixHistogram (most-freq-bin
    repair), and SubtractHistogram (larger = parent − smaller via hist_t** rotation) — all behind
    LGBM_CUDA_ON_DEVICE, anchored to the cpu f64 fold. The cpu-anchor parity runs automatically in
    Task 1; this checkpoint covers the ROCm f32 parity that requires the physical GPU backend
    (VALIDATION.md manual-only).
  </what-built>
  <how-to-verify>
    1. On the ROCm host, run: `LGBM_CUDA_ON_DEVICE=1 cargo test -p lgbm-compute --features rocm --test rocm_cuda_mirror`
    2. Confirm every build/fix/subtract case passes within the ABS=5e-6 / REL=1e-5 envelope vs the cpu
       f64 anchor (the assert_close scaffold). These are hip-vs-cpu-anchor asserts — confirm NONE compare
       two GPU outputs to each other (def-f8u-01).
    3. Spot-check the large-bin/global-spill case and the most_freq_bin!=0 fix case are among those run
       (not skipped/ignored on the ROCm path).
    4. Note: the local GPU is a SPOOFED 8-CU APU — parity is valid, but ignore any perf numbers (this
       phase is parity-only).
  </how-to-verify>
  <resume-signal>Type "approved" if all ROCm parity cases pass within ~1e-6 of the cpu anchor, or describe the failing case.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → built artifacts | The gate only runs tests and greps source; no new device launch surface beyond what 16-02..16-04 already validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05-01 | Tampering | regression of the byte-unchanged default path | mitigate | The merge gate (cargo test workspace, default features) + the git-diff byte-unchanged check catch any accidental change to CPU/ROCm/host-CUDA behavior. |
| T-16-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (gate-only plan; RESEARCH Package Legitimacy Audit: zero new deps). |
</threat_model>

<verification>
- `cargo test` (default features, LGBM_CUDA_ON_DEVICE unset) green across the workspace.
- Comment-stripped grep: 0 `Atomic<i64>` in histogram.rs; no f64 per-row hot loop in the new kernels.
- ROCm parity human-verify checkpoint approved (within ~1e-6 of the cpu f64 anchor, never GPU-vs-GPU).
</verification>

<success_criteria>
The hard merge gate is satisfied: full workspace green with the default path byte-unchanged (D-07,
ODL-19), no f64 per-row hot loop / no Atomic<i64> (D-08), and ROCm f32 parity within ~1e-6 of the
cpu f64 anchor attested on the physical backend (never GPU-vs-GPU).
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 16-05)
- No source symbols. Produces the merge-gate sign-off record in `16-05-SUMMARY.md`: workspace pass count, the Atomic<i64> / f64-hot-loop grep results, the shipped-kernel byte-unchanged confirmation, and the ROCm parity human-verify attestation.
</artifacts>

<output>
Create `.planning/phases/16-on-device-histogram-constructor/16-05-SUMMARY.md` when done.
</output>
