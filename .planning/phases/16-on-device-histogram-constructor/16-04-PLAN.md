---
phase: 16-on-device-histogram-constructor
plan: 04
type: execute
wave: 2
depends_on: [16-02, 16-03]
files_modified:
  - crates/lgbm-compute/src/kernels/histogram.rs
  - crates/lgbm-compute/src/kernels/subtract.rs
  - crates/lgbm-compute/src/lib.rs
autonomous: true
requirements: [ODL-10]
must_haves:
  truths:
    - "FixHistogram repairs the omitted most-frequent bin in the hist_t float domain: feat_hist[mfb·2] = leaf_total_grad − Σ, feat_hist[mfb·2+1] = leaf_total_hess − Σ, only when mfb>0 && mfb<num_bin"
    - "The cpu anchor Fix folds in ascending (load-bearing) order with no reorder; the hip Fix uses plane_sum/ShuffleReduceSum over num_bin_aligned"
    - "The compact step is DROPPED (§7 is build→fix→subtract only; compaction is a CPU-learner artifact)"
    - "SubtractHistogram derives larger = parent − smaller via subtract.rs through the HistArena rotation (larger in parent buffer in-place, smaller fresh slot), no bulk copy, only after the parent is fully built+synced"
    - "ConstructHistogramForLeaf entry runs build→de-quant→fix→subtract behind LGBM_CUDA_ON_DEVICE (OFF by default); on_device_growth_supported() stays false"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/histogram.rs"
      provides: "fix_histogram_mfb kernel (hist_t domain, drop compact) + the construct_histogram_for_leaf entry"
      contains: "fix_histogram"
    - path: "crates/lgbm-compute/src/lib.rs"
      provides: "LGBM_CUDA_ON_DEVICE-gated entry hook; on_device_growth_supported stays false"
      contains: "on_device_growth_supported"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/histogram.rs (construct_histogram_for_leaf)"
      to: "crates/lgbm-compute/src/kernels/subtract.rs (subtract_hist_kernel)"
      via: "subtract larger -= smaller through HistArena handles after build fully synced"
      pattern: "subtract_hist_kernel"
    - from: "crates/lgbm-compute/src/kernels/histogram.rs (construct_histogram_for_leaf)"
      to: "crates/lgbm-compute/src/kernels/histogram_arena.rs (HistArena)"
      via: "rotation supplies {parent, smaller, larger} handles"
      pattern: "HistArena"
---

<objective>
Wave 2: the subtraction trick on device (ODL-10) — FixHistogram (most-freq-bin omit-and-repair),
SubtractHistogram (larger = parent − smaller via `hist_t**` rotation, no bulk copy), and the
`ConstructHistogramForLeaf` entry that sequences build → de-quant → fix → subtract behind
`LGBM_CUDA_ON_DEVICE` (OFF by default). Fix + Subtract operate in the hist_t FLOAT domain (D-01);
the u64 fixed-point stays confined to BUILD. The subtraction trick is a CORRECTNESS requirement —
building the larger child directly takes a different f32 rounding path (§17).

Purpose: complete the build→fix→subtract hot path, anchor-pinned to the cpu f64 fold, with the
build-fully-synced-before-subtract ordering invariant (8aed100 guard) enforced, while keeping
CPU/ROCm/host-CUDA byte-unchanged and `on_device_growth_supported()` false (Phase 16 demonstrates
the rotation in isolation; the whole-tree pool SWAP is Phase 18).

Output: `fix_histogram_mfb` kernel + the subtract wiring + the gated entry in histogram.rs/lib.rs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-on-device-histogram-constructor/16-CONTEXT.md
@.planning/phases/16-on-device-histogram-constructor/16-RESEARCH.md
@.planning/phases/16-on-device-histogram-constructor/16-PATTERNS.md
@.planning/phases/16-on-device-histogram-constructor/16-VALIDATION.md
@.claude/skills/spike-findings-lightgbm_rs/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FixHistogram most-freq-bin repair in the hist_t float domain, DROP compact (D-01/D-06)</name>
  <read_first>
    - crates/lgbm-compute/src/kernels/histogram.rs:2331-2398 (fix_compact_kernel: the de-quant first pass + the Fix repair fold; do_fix = mfb>0 && mfb<nb; ASCENDING serial fold; seed with raw leaf totals)
    - crates/lgbm-compute/src/kernels/histogram.rs:2400-2427 (the compact block `if off > 0 {...}` — DO NOT port this, Pitfall 5)
    - crates/lgbm-compute/src/kernels/primitives.rs:412-424 (reduce_sum_body single-owner ascending fold) and primitives.rs:1456 (plane_reduce_sum_f32_on — the hip ShuffleReduceSum analog over num_bin_aligned)
    - crates/lgbm-compute/src/kernels/column_data.rs:32 (ColumnFeatureMeta: most_freq_bin, num_bin, offset)
    - docs/cuda-kernel-design.md §7.5 (FixHistogramKernel: one block per feature with mfb≠0, reduce Σ over num_bin_aligned, write leaf_total − Σ)
    - .planning/phases/16-on-device-histogram-constructor/16-RESEARCH.md (Pitfall 4 mfb guard; DEF-07-02 is a *compaction* issue the §7 path sidesteps)
  </read_first>
  <behavior>
    - Fix runs only when `mfb > 0 && mfb < num_bin`; mfb==0 and out-of-range are skipped (Pitfall 4).
    - The repaired value = leaf_total − Σ(all other bins) in ASCENDING order, seeded with the RAW host f64 leaf totals (sum_gradient, sum_hessian); written to feat_hist[mfb·2] (grad) and feat_hist[mfb·2+1] (hess).
    - Fix operates on the already-de-quanted hist_t (D-01) — NOT on the raw u64; it never re-quantizes.
    - cpu anchor folds ascending (load-bearing, never reorder); hip uses plane_sum/ShuffleReduceSum over num_bin_aligned (power-of-two, host-precomputed). Same #[cube] generic, comptime/runtime-split (D-06).
    - The compact (offset-shift) step is absent — the histogram shape matches the uncompacted §7 reference.
  </behavior>
  <action>
    Add `fix_histogram_mfb` to histogram.rs, reusing the `fix_compact_kernel` Fix repair logic
    (do_fix guard, ascending branchless `select` fold seeded with raw leaf totals, write
    `hist[mfb·2]=leaf_total_grad−Σ`, `hist[mfb·2+1]=leaf_total_hess−Σ`) but operating on the
    de-quanted hist_t produced by 16-03 — DROP the de-quant first pass duplication (Fix consumes
    hist_t) AND DROP the compact block at 2400-2427 (Pitfall 5; §7 lists only Fix + Subtract). Keep
    ONE #[cube] generic, comptime/runtime-split (D-06): cpu anchor = the ascending serial fold
    (bit-exact); hip = `plane_reduce_sum_f32_on`/ShuffleReduceSum over `num_bin_aligned` (within
    ~1e-6; if num_bin_aligned > plane width, fall to a multi-plane reduce or the single-owner fold per
    research A2). Mirror the `fix_compact_f64_on` V5 launcher checks (num_bin==0, 2*num_bin overflow,
    slot_off + 2*num_bin > raw.len(), empty feats → Ok no launch). Anchor to the Wave 0
    `most_freq_bin != 0` fixture; assert the repaired default-bin value equals the golden
    `leaf_total − scanned Σ`. Pin to the cpu f64 fold, never GPU-vs-GPU.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --features gpu 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `fix_histogram_mfb` only repairs when `mfb>0 && mfb<num_bin`; an mfb==0 fixture leaves the histogram untouched (no fix).
    - On the Wave 0 mfb!=0 fixture, the repaired feat_hist[mfb·2]/[mfb·2+1] equal the precomputed golden `leaf_total − Σ(ascending)` bit-exact on the cpu f64 anchor (within ~1e-6 on hip).
    - `grep -n 'if off > 0' crates/lgbm-compute/src/kernels/histogram.rs` shows the compact block belongs only to the legacy `fix_compact_kernel`, not to `fix_histogram_mfb` (no compaction in the §7 path).
    - Fix consumes hist_t and does not re-quantize (no 2^30 scale appears inside `fix_histogram_mfb`).
  </acceptance_criteria>
  <done>FixHistogram repairs the omitted most-freq bin in the hist_t float domain (ascending anchor fold; hip plane reduce), drops the compact step, and matches the Wave 0 mfb!=0 golden repaired value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SubtractHistogram via HistArena rotation + build-synced-before-subtract ordering (D-01/D-02)</name>
  <read_first>
    - crates/lgbm-compute/src/kernels/subtract.rs:43-106 (subtract_hist_kernel f64/f32/_vec — REUSE verbatim; out = parent − child, element-wise, no atomics, bit-exact per cell)
    - crates/lgbm-compute/src/kernels/histogram_arena.rs (16-02 output: HistArena rotation + parent_handle/smaller_handle/larger_handle)
    - crates/lgbm-compute/tests/rocm_cuda_mirror.rs (16-01 subtract_ordering harness + assert_close)
    - .claude/skills/spike-findings-lightgbm_rs/SKILL.md (the 8aed100 bug: subtract ran before the fused smaller histogram was built; debug fix)
    - docs/cuda-kernel-design.md §7.5 (SubtractHistogram: one thread per element, guarded larger.leaf_index ≥ 0) and §17 (subtraction trick correctness)
  </read_first>
  <behavior>
    - SubtractHistogram computes larger_hist[i] = parent_hist[i] − smaller_hist[i] per cell, reusing subtract.rs (f64 anchor / f32 hip), guarded by larger.leaf_index ≥ 0.
    - The larger child is derived IN-PLACE in the parent's buffer via the HistArena rotation (larger handle == old parent slot); the smaller occupies a fresh slot — no bulk copy.
    - The subtract launch happens ONLY after the parent (smaller) build + de-quant + fix are fully synced (kernel boundary = the sync; there is no grid barrier). The ordering harness fails loudly if subtract reads the parent before the build completes (8aed100 guard).
    - The derived larger child is anchor-pinned: bit-exact on the cpu f64 anchor, within ~1e-6 on hip (never GPU-vs-GPU).
  </behavior>
  <action>
    Add a `subtract_histogram_on_device` wiring fn that takes a `HistArena` (16-02), performs
    `rotate()` to set larger ← parent slot (in-place) and smaller ← fresh slot, then launches the
    REUSED `subtract_hist_kernel` (f64 cpu anchor / f32 hip) to compute `larger = parent − smaller`
    over the arena handles — guarded by `larger.leaf_index ≥ 0`. Enforce the ordering invariant:
    the subtract launch must be sequenced AFTER the build/de-quant/fix of the smaller-leaf parent
    histogram are fully synced (kernel boundary). Use the Wave 0 `subtract_ordering` harness to prove
    both the correct sequence (bit-exact larger = parent − smaller on the f64 anchor) and the negative
    control (mis-ordered subtract is detected). Add the anchored subtract case (larger child vs the cpu
    f64 anchor on the dense corpora). Do not add a new subtract kernel — subtract.rs is reused
    verbatim (Don't-Hand-Roll).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --features gpu subtract 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `subtract_histogram_on_device` reuses `subtract_hist_kernel` (no new subtract kernel added; git diff to subtract.rs is reuse/wiring only).
    - After rotation, the larger child is written into the old parent slot (in-place); the smaller is in a distinct slot (HistArena no-alias invariant holds).
    - The correctly-ordered build→fix→subtract reproduces larger = parent − smaller bit-exact on the cpu f64 anchor (within ~1e-6 on hip); the mis-ordered control is detected by the 8aed100 guard.
    - The subtract is skipped (or no-ops) when larger.leaf_index < 0.
  </acceptance_criteria>
  <done>SubtractHistogram derives the larger child in-place from the parent via HistArena rotation reusing subtract.rs, no bulk copy, with the build-synced-before-subtract ordering invariant enforced and anchor-pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ConstructHistogramForLeaf entry behind LGBM_CUDA_ON_DEVICE; on_device_growth_supported stays false (D-07/D-08)</name>
  <read_first>
    - crates/lgbm-compute/src/lib.rs:1239 (on_device_growth_supported — stays false this phase) and 1282 (grow_tree_on_device seam) and 2212-2217 (the default trait seam comment)
    - crates/lgbm-compute/src/kernels/mod.rs (the OFF-by-default LGBM_CUDA_ON_DEVICE seam convention from Phase 14/15)
    - crates/lgbm-compute/src/kernels/histogram.rs (the 16-03 build launcher + 16-04 fix + the subtract wiring this entry sequences)
    - docs/cuda-kernel-design.md §7.0 (ConstructHistogramForLeaf entry: early-return if BOTH children fail min_data/min_hess; only the smaller leaf is built from data)
    - .planning/phases/16-on-device-histogram-constructor/16-CONTEXT.md (D-07 OFF by default, byte-unchanged; on_device_growth_supported stays false; §9 pool SWAP → Phase 18)
  </read_first>
  <behavior>
    - construct_histogram_for_leaf(smaller, larger) sequences: build smaller (16-03) → de-quant once → fix smaller (16-04 T1) → rotate + subtract to derive larger (16-04 T2); early-returns if BOTH children fail min_data/min_sum_hessian.
    - The entry is reached ONLY when LGBM_CUDA_ON_DEVICE is set; with it unset, CPU/ROCm/host-CUDA paths are byte-unchanged (D-07).
    - on_device_growth_supported() still returns false (Phase 16 demonstrates the histogram path in isolation; the tree-growth driver is Phase 18/21).
    - The end-to-end entry produces hist_in_leaf for BOTH the smaller (built+fixed) and larger (subtract-derived) children, anchor-pinned to the cpu f64 fold.
  </behavior>
  <action>
    Add the `construct_histogram_for_leaf` entry (gpu-gated, reached only via the OFF-by-default
    `LGBM_CUDA_ON_DEVICE` seam) that orchestrates build → de-quant → fix → rotate → subtract using the
    16-03 launcher, the 16-04 T1 fix, the HistArena (16-02), and the 16-04 T2 subtract wiring. Mirror
    the §7.0 early-return (skip when both children fail min_data/min_sum_hessian; build ONLY the smaller
    leaf from data). Keep `on_device_growth_supported()` returning false in lib.rs (do NOT wire a
    tree-growth driver — Phase 18/21). Confirm the default path (LGBM_CUDA_ON_DEVICE unset) is
    byte-unchanged: no existing CPU/ROCm/host-CUDA code path changes behavior. Add the end-to-end anchor
    test producing both children's hist_in_leaf and asserting both against the cpu f64 fold (never
    GPU-vs-GPU). Document that hist_in_leaf is consumed by Phase 17 (best-split) and the rotation
    contract by Phase 18 (pool swap).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --features gpu 2>&1 | tail -25 && cargo test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `on_device_growth_supported()` returns false (test-asserted); no tree-growth driver is wired.
    - With `LGBM_CUDA_ON_DEVICE` unset, the default-feature `cargo test` is green and no existing path changes (the entry is unreachable).
    - The end-to-end `construct_histogram_for_leaf` produces both children's histograms; both match the cpu f64 anchor (bit-exact structure; within ~1e-6 on hip).
    - The §7.0 early-return is exercised: a leaf pair failing both min_data/min_sum_hessian builds nothing.
  </acceptance_criteria>
  <done>The build→de-quant→fix→subtract entry runs behind the OFF-by-default LGBM_CUDA_ON_DEVICE seam, produces both children anchor-pinned to the cpu f64 fold, keeps on_device_growth_supported false, and leaves the default path byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host orchestration → device fix/subtract | Fix/subtract launch args (slot offsets, num_bin, arena handles) are host-computed. Realistic threat surface: OOB device writes from bad offsets/arena indices and parent/smaller aliasing in the rotation — no network/auth/untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | Tampering | OOB write in fix_histogram_mfb launcher | mitigate | V5 checks before launch_unchecked (num_bin>0, 2*num_bin overflow, slot_off+2*num_bin ≤ raw.len(), empty feats → Ok), mirroring fix_compact_f64_on:2445-2448. |
| T-16-04-02 | Tampering | parent/smaller slot aliasing during in-place subtract | mitigate | Rely on the HistArena no-alias invariant (T-16-02-02); the in-place subtract into the parent slot is safe only because smaller is a distinct slot — assert the invariant before the subtract launch. |
| T-16-04-03 | Tampering | subtract reads parent before build sync (8aed100 class) | mitigate | Enforce build/de-quant/fix fully synced (kernel boundary) before the subtract launch; the Wave 0 ordering harness guards it. |
| T-16-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs (extends existing crate; RESEARCH Package Legitimacy Audit: zero new deps). |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --features gpu` passes Fix (mfb!=0), subtract (ordering + anchored), and the end-to-end entry within ~1e-6 of the cpu f64 anchor.
- `cargo test` (default features, LGBM_CUDA_ON_DEVICE unset) stays green; default path byte-unchanged (D-07).
- `on_device_growth_supported()` returns false (test-asserted).
- No new subtract kernel added; the compact block is absent from the §7 fix path.
</verification>

<success_criteria>
ODL-10 subtraction trick: build-smaller-only → FixHistogram (most-freq-bin repair in hist_t float
domain, leaf_total − Σ) → SubtractHistogram (larger = parent − smaller via hist_t** rotation,
in-place, no bulk copy), sequenced behind the OFF-by-default LGBM_CUDA_ON_DEVICE entry with the
build-synced-before-subtract ordering invariant enforced, both children anchor-pinned to the cpu
f64 fold, on_device_growth_supported false, default path byte-unchanged.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 16-04)
- `fix_histogram_mfb` — most-freq-bin repair #[cube] generic in the hist_t float domain (cpu ascending fold / hip plane reduce), compact step dropped; + its V5-checked launcher.
- `subtract_histogram_on_device` — subtract wiring reusing subtract.rs over HistArena handles (rotate → larger in-place, smaller fresh slot), ordering-guarded.
- `construct_histogram_for_leaf` — the build→de-quant→fix→rotate→subtract entry behind the OFF-by-default LGBM_CUDA_ON_DEVICE seam, with §7.0 early-return.
- `on_device_growth_supported()` confirmed still false in lib.rs (no new symbol; seam unchanged).
</artifacts>

<output>
Create `.planning/phases/16-on-device-histogram-constructor/16-04-SUMMARY.md` when done.
</output>
