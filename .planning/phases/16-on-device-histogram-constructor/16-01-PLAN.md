---
phase: 16-on-device-histogram-constructor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/oracle-harness/tests/fixtures/kernels/
  - crates/oracle-harness/tests/kernel_parity.rs
  - crates/lgbm-compute/tests/rocm_cuda_mirror.rs
autonomous: true
requirements: [ODL-09, ODL-10]
must_haves:
  truths:
    - "A synthetic sparse column fixture exists forcing row_ptr_type {16,32,64}"
    - "A synthetic large-bin/global-spill column fixture exists (NumLargeBinPartition() > 0)"
    - "A most_freq_bin != 0 column fixture exists with the anchored repaired default-bin value (leaf_total - scanned sum)"
    - "An ordering-invariant harness exists asserting the parent is fully built/synced before any child subtract reads it (8aed100-class)"
    - "A [2b]/[2b+1] interleave assert helper exists (grad at 2b, hess at 2b+1)"
    - "The rocm_cuda_mirror cpu_anchor/assert_close scaffold is extended for the two-tier + sparse + spill cases"
  artifacts:
    - path: "crates/oracle-harness/tests/fixtures/kernels/"
      provides: "Synthetic sparse / large-bin / mfb!=0 fixture data + the dense corpora build anchor"
    - path: "crates/oracle-harness/tests/kernel_parity.rs"
      provides: "Golden-replay harness extension points for build/fix/subtract"
      contains: "fix_mfb_nonzero"
    - path: "crates/lgbm-compute/tests/rocm_cuda_mirror.rs"
      provides: "cpu_anchor + assert_close(tol) scaffold extended with §13-aware cases"
      contains: "assert_close"
  key_links:
    - from: "crates/lgbm-compute/tests/rocm_cuda_mirror.rs"
      to: "crates/lgbm-compute/src/kernels/histogram.rs"
      via: "cpu_anchor compares hip kernel output to construct_histograms_f64_on (never GPU-vs-GPU)"
      pattern: "construct_histograms"
---

<objective>
Wave 0: land every test fixture + anchor-harness extension Phase 16's build/fix/subtract
tasks verify against, BEFORE any kernel is written. This closes the VALIDATION.md Wave 0
gaps and operationalizes D-05 so the build/fix/subtract plans (16-03, 16-04) have a green
anchor target on the first commit.

Purpose: every numeric output in this phase is anchor-pinned to the cubecl-cpu f64 fold
(bit-exact structure; ROCm/CUDA f32 within ~1e-6) — never GPU-vs-GPU (def-f8u-01, D-06).
The fixtures here are the *exact* places parity historically broke (sparse row_ptr widths,
global-memory spill, the most_freq_bin omit-and-repair path, build-before-subtract ordering,
the [2b]/[2b+1] interleave). They must exist first so Nyquist sampling has a target.

Output: synthetic fixtures + harness helpers; NO production kernel code in this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-on-device-histogram-constructor/16-CONTEXT.md
@.planning/phases/16-on-device-histogram-constructor/16-RESEARCH.md
@.planning/phases/16-on-device-histogram-constructor/16-PATTERNS.md
@.planning/phases/16-on-device-histogram-constructor/16-VALIDATION.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synthetic sparse + large-bin/global-spill + mfb!=0 column fixtures (D-05)</name>
  <read_first>
    - crates/oracle-harness/tests/kernel_parity.rs (the golden-replay home; `kernel_parity_histogram_bit_exact_on_cpu` at ~line 162)
    - crates/oracle-harness/tests/device_dataset_parity.rs AND crates/lgbm-compute/tests/device_dataset_parity.rs (Phase-15 synthetic sparse + large-bin column fixtures to REUSE / extend — research A3)
    - crates/lgbm-compute/src/kernels/row_data.rs:43-64 (FeaturePartitionLayout: feature_partition_column_index_offsets, column_hist_offsets, partition_hist_offsets, num_large_bin_partition, shared_hist_size, max_num_column_per_partition)
    - crates/lgbm-compute/src/kernels/column_data.rs:32 (ColumnFeatureMeta: most_freq_bin, num_bin, offset)
    - docs/cuda-kernel-design.md §13 (feature-partition layout) and §7.2 (_GlobalMemory variant, large-bin partition)
  </read_first>
  <action>
    Add fixtures under crates/oracle-harness/tests/fixtures/kernels/ (and/or a shared
    `#[cfg(test)]` fixture builder module reachable from both oracle-harness and lgbm-compute
    tests). Reuse the Phase-15 synthetic sparse column (forces `row_ptr_type` selection across
    {16,32,64} per §13 CSR re-lay) and the Phase-15 synthetic large-bin column (forces
    `NumLargeBinPartition() > 0` so the partition routes to the `_GlobalMemory` spill path, D-04).
    Add a NEW purpose-built `most_freq_bin != 0` column whose distribution makes the
    most-frequent bin a non-zero, in-range bin (`mfb > 0 && mfb < num_bin`) so it exercises
    FixHistogram's omit-and-repair path (Pitfall 4, DEF-07-02 class). Each fixture must carry
    the ordered per-row grad/hess (f32) AND the raw f64 leaf totals (sum_gradient, sum_hessian)
    the Fix anchor needs (research A3). Compute and store the EXPECTED anchored repaired
    default-bin value `leaf_total - scanned_sum_ascending` as a golden constant (do not let a
    test recompute it from the same kernel under test). Anchor all expected histograms to the
    cpu f64 fold via `construct_histograms_f64_on` (CubeDim::new_1d(1)); never store a GPU output
    as a golden. Document the fixture invariants in a header comment. Do NOT add the offset
    `compact` shape to any expected histogram — §7 is build->fix->subtract only (Pitfall 5).
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test kernel_parity 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p oracle-harness --tests` exits 0 and the fixtures load.
    - A test asserts the sparse fixture selects each of row_ptr widths {16,32,64} across its
      three configured columns (assert on the resolved `row_ptr_type` / re-lay width).
    - A test asserts the large-bin fixture yields `num_large_bin_partition > 0` from
      `divide_cuda_feature_groups(num_bin_per_column, shared_hist_size)`.
    - The mfb!=0 fixture's stored golden default-bin value equals `leaf_total - Σ(other bins ascending)`
      computed independently (e.g. a plain Rust f64 loop in the test), not from any device kernel.
    - No fixture stores a compacted (offset-shifted) histogram shape.
  </acceptance_criteria>
  <done>Sparse {16,32,64}, large-bin/global-spill, and mfb!=0 fixtures exist with f64-anchored goldens; oracle-harness tests compile and the fixture-shape assertions pass.</done>
</task>

<task type="auto">
  <name>Task 2: Ordering-invariant harness + [2b]/[2b+1] interleave assert helper (D-05)</name>
  <read_first>
    - crates/lgbm-compute/tests/rocm_cuda_mirror.rs:40-109 (cpu_anchor + assert_close(tol) scaffold; ABS=5e-6, REL=1e-5 envelope)
    - crates/lgbm-compute/src/kernels/subtract.rs:43-106 (subtract_hist_kernel f64/f32/_vec — the SubtractHistogram math the ordering harness drives)
    - .claude/skills/spike-findings-lightgbm_rs/SKILL.md (the 8aed100 ordering bug: Phase-12 co-pack scan deferral ran subtract before the fused smaller histogram was built)
    - docs/cuda-kernel-design.md §7.5 (FixHistogram + SubtractHistogram; the [2b] grad / [2b+1] hess interleave) and §17 (subtraction-trick correctness, atomic-ordering nondeterminism)
  </read_first>
  <action>
    Add an `interleave_layout` assert helper that, given a per-feature histogram slice, asserts
    grad lives at cell `bin*2` and hess at `bin*2+1` for every bin — a reusable checker the
    build/fix tests call. Add a `subtract_ordering` harness that drives a deliberate
    build-fully-synced-then-subtract sequence and a NEGATIVE control that would read the parent
    before the build is synced; assert the correct sequence reproduces `parent - smaller`
    bit-exactly on the cpu anchor and that the harness FAILS loudly if subtract is invoked before
    the parent buffer is populated (guard the 8aed100 class). The "sync" with no grid barrier is
    the kernel boundary between build and subtract — model it as an explicit ordering assertion at
    the host/test level (parent handle fully written before the subtract launch reads it).
    Extend `rocm_cuda_mirror.rs`'s `cpu_anchor`/`assert_close` scaffold with §13-partition-aware
    case entry points (sparse, large-bin spill) so 16-03/16-04 only add the kernel call — keep the
    ABS=5e-6/REL=1e-5 envelope and pin to `construct_histograms_f64_on`; NEVER assert hip-vs-hip or
    hip-vs-cuda (def-f8u-01).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test rocm_cuda_mirror 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `interleave_layout` helper exists and a unit test feeds it a known [2b]/[2b+1] slice that passes and a swapped slice that fails.
    - `subtract_ordering` harness: the correctly-ordered run reproduces `parent - smaller` to bit-exact on the f64 anchor; the mis-ordered control is detected (test asserts the guard fires).
    - `rocm_cuda_mirror.rs` compiles with the new partition-aware case entry points (may be `#[ignore]` until the kernels land, but must reference the cpu f64 anchor, not a GPU golden).
    - No assertion in the harness compares two GPU outputs to each other.
  </acceptance_criteria>
  <done>Ordering-invariant harness + interleave assert helper exist and pass on the cpu anchor; the rocm mirror scaffold is extended with partition-aware, anchor-pinned case stubs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → fixtures | In-process synthetic data; no external/untrusted input. Only memory-safety of fixture indexing matters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-01 | Tampering | fixture array indexing (row_ptr / bin offsets) | mitigate | Fixture builders assert `slot_off + 2*num_bin <= raw.len()` and row_ptr monotonicity before use; goldens computed in plain Rust f64, not from the kernel under test. |
| T-16-01-02 | Information disclosure | golden value provenance | accept | Internal compute library, no PII; goldens are synthetic numbers. |
| T-16-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (test fixtures only); RESEARCH Package Legitimacy Audit confirms zero new deps. |
</threat_model>

<verification>
- `cargo test -p oracle-harness --test kernel_parity` compiles and fixture-shape assertions pass.
- `cargo test -p lgbm-compute --test rocm_cuda_mirror` compiles; anchor-pinned case stubs present.
- `cargo test` (full workspace) stays green — this plan is additive test-only code.
</verification>

<success_criteria>
All six VALIDATION.md Wave 0 gaps are closed: sparse {16,32,64}, large-bin/global-spill,
mfb!=0 with anchored repaired value, build-before-subtract ordering harness, [2b]/[2b+1]
interleave helper, and the extended cpu_anchor/assert_close scaffold — all pinned to the cpu
f64 fold, none GPU-vs-GPU.
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 16-01)
- Fixture data + builders under `crates/oracle-harness/tests/fixtures/kernels/` (or a shared `#[cfg(test)]` fixture module): synthetic sparse column (row_ptr {16,32,64}), synthetic large-bin/global-spill column, purpose-built `most_freq_bin != 0` column with golden repaired default-bin value.
- `kernel_parity.rs` extension points: `fix_mfb_nonzero`-keyed golden entries (test names referenced by 16-04).
- `rocm_cuda_mirror.rs` helpers: `interleave_layout(...)` assert helper, `subtract_ordering` harness, partition-aware `cpu_anchor`/`assert_close` case stubs (sparse, large-bin spill).
</artifacts>

<output>
Create `.planning/phases/16-on-device-histogram-constructor/16-01-SUMMARY.md` when done.
</output>
