---
phase: 02-dataset-binning-determinism-root
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [DAT-01, DAT-07]
files_modified:
  - crates/lgbm-dataset/src/ingest.rs
  - crates/lgbm-dataset/src/bin_mapper.rs
  - crates/lgbm-dataset/tests/default_config_ingest_parity.rs
  - xtask/cpp/bin_capture.cpp
  - xtask/src/main.rs

must_haves:
  truths:
    - "build_mapper feeds the SCALED filter_cnt = (min_data_in_leaf * total_sample_cnt) / num_rows (C++ integer truncation) as min_split_data, not raw min_data_in_leaf — so is_trivial_ matches C++ on the default config (SC#1)."
    - "A single source-of-truth helper computes filter_cnt; both ingest.rs::build_mapper and bin_mapper.rs::find_bin_from_column route through it (no divergent second copy)."
    - "A default-config (feature_pre_filter=true, min_data_in_leaf=20, sample_cnt < num_rows) ingest parity test exists, FAILS before the fix and PASSES after, asserting identical is_trivial_ / num_bin_ / per-row bin assignment vs a C++ golden captured through the same scaled-filter_cnt path (SC#5 / ORA-03 bin stage)."
  artifacts:
    - path: "crates/lgbm-dataset/tests/default_config_ingest_parity.rs"
      provides: "Default feature_pre_filter=true ingest parity replay against the scaled-filter_cnt C++ golden"
      min_lines: 40
    - path: "crates/lgbm-dataset/tests/fixtures/default_config_ingest.txt"
      provides: "C++ golden captured through ConstructFromSampleData scaled-filter_cnt path (pre_filter=true, sample_cnt < num_rows)"
      contains: "is_trivial="
    - path: "crates/lgbm-dataset/src/bin_mapper.rs"
      provides: "filter_cnt source-of-truth helper (e.g. scaled_filter_cnt) + find_bin_from_column routed through it"
      contains: "scaled_filter_cnt"
  key_links:
    - from: "crates/lgbm-dataset/src/ingest.rs"
      to: "crates/lgbm-dataset/src/bin_mapper.rs"
      via: "build_mapper calls the shared scaled_filter_cnt helper before find_bin_numeric"
      pattern: "scaled_filter_cnt"
    - from: "crates/lgbm-dataset/tests/default_config_ingest_parity.rs"
      to: "crates/lgbm-dataset/tests/fixtures/default_config_ingest.txt"
      via: "test loads the golden and replays it against from_mat default-config ingest"
      pattern: "default_config_ingest"
---

<objective>
Close the two blocking phase-02 verification gaps, which share one root cause: the
default-configuration in-memory ingest path forwards the RAW `min_data_in_leaf` as the
pre-filter threshold, whereas C++ forwards the SCALED
`filter_cnt = (min_data_in_leaf * total_sample_size) / num_dist_data`. With
`feature_pre_filter=true` (default) and sampling active (`sample_cnt < num_rows`), the
Rust threshold (e.g. 20) differs from C++ (`filter_cnt < 20`), so `is_trivial_` — and
every downstream feature store / EFB membership / split — diverges. The green suite
hides this because EVERY ingest parity test (and the C++ example golden) forces
`feature_pre_filter=false`, so the divergent default path ships with zero coverage.

Purpose: restore the bit-identical-on-default-configuration contract at the determinism
root (the phase goal) and prove it with a golden that fails-before / passes-after.
Output:
- A C++ golden through the scaled-`filter_cnt` `ConstructFromSampleData` path.
- A new Rust parity test replaying it (fails before the fix, passes after).
- The `filter_cnt` derivation fixed in a SINGLE source-of-truth helper used by both
  divergent sites (`ingest.rs::build_mapper` + `bin_mapper.rs::find_bin_from_column`).

Closes GAP-1 (CR-01/IN-02, SC#1, DAT-01) and GAP-2 (CR-02, SC#5 / ORA-03 bin stage, DAT-07).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dataset-binning-determinism-root/02-VERIFICATION.md
@.planning/phases/02-dataset-binning-determinism-root/02-CONTEXT.md
@.planning/phases/02-dataset-binning-determinism-root/02-RESEARCH.md
@.planning/phases/02-dataset-binning-determinism-root/02-01-SUMMARY.md
@.planning/phases/02-dataset-binning-determinism-root/02-04-SUMMARY.md
@CLAUDE.md
</context>

<repo_constraints>
Honor project memory + CLAUDE.md exactly:
- NEVER `git add -A` / `git add .`. Stage explicitly by path.
- NEVER commit `LightGBM/`, `.serena/`, `AGENTS.md`, or `.planning/config.json`.
- The C++ capture is HEADER-ONLY verbatim transcription compiled against
  `-I LightGBM/include`; do NOT build `lib_lightgbm` or the CLI (external_libs unvendored).
  This matches the precedent of 02-01..02-05 — reuse the existing `bin_capture` cmake target.
- Binning goldens are compared BIT-EXACT (f64-bit equality / exact integer equality),
  never within the ~1e-6 oracle tolerance.
</repo_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Capture the scaled-filter_cnt C++ golden + add the failing default-config parity test</name>
  <files>crates/lgbm-dataset/tests/default_config_ingest_parity.rs, crates/lgbm-dataset/tests/fixtures/default_config_ingest.txt, xtask/cpp/bin_capture.cpp, xtask/src/main.rs</files>
  <read_first>
    - xtask/cpp/bin_capture.cpp (EmitExampleDataset ~1814; F64Bits / FindBinNumeric helpers; main() argv dispatch ~2101-2294 — note example golden uses argv[8], examples start at argv[9])
    - xtask/src/main.rs (bin_capture() ~183-300: fixture paths, cmd.arg ordering, idempotence post-write checks)
    - LightGBM/src/io/dataset_loader.cpp:623-624 (filter_cnt = static_cast<data_size_t>(static_cast<double>(min_data_in_leaf * total_sample_size) / num_dist_data)) and :645-648 (FindBin call)
    - LightGBM/src/io/bin.cpp:54-62 (NeedFilter) and :486 (NeedFilter call site setting is_trivial_)
    - LightGBM/src/c_api.cpp:1360-1374 (dense path: total_sample_size = sample_cnt, num_dist_data = total_nrow)
    - crates/lgbm-dataset/tests/example_dataset_parity.rs (golden parse + per-feature/per-row assert pattern to mirror)
    - crates/lgbm-dataset/tests/ingest_equivalence.rs (cfg helper + from_mat usage)
    - crates/lgbm-dataset/src/ingest.rs (from_mat signature; build_mapper; FinishedDataset accessors num_data/num_groups/feature_group)
    - crates/lgbm-dataset/src/bin_mapper.rs (BinMapper fields: is_trivial_, num_bin_, bin_upper_bound_; ValueToBin)
  </read_first>
  <action>
    Extend the C++ harness and add a Rust parity test that exercises the DEFAULT scaled-filter_cnt path and FAILS against current code.

    C++ side (xtask/cpp/bin_capture.cpp):
    - Add a new emitter `EmitDefaultConfigIngest(std::ofstream& out, int master_seed)` that builds a small deterministic synthetic dense matrix (master-seed-derived, header-only Random — same precedent as the EFB/example emitters; no wall-clock entropy). Choose dimensions so the divergence is TRIGGERED: `num_rows` large enough and `bin_construct_sample_cnt` SMALL so `k = sample_cnt < num_rows` (e.g. num_rows = 200, sample_cnt = 50), with default `min_data_in_leaf = 20`. This yields filter_cnt = (20 * 50) / 200 = 5 (integer truncation) != 20, so the raw-vs-scaled difference changes is_trivial_ on at least one feature. Include at least one feature engineered so its bin-count split crosses the threshold between filter_cnt=5 and min_data_in_leaf=20 (so is_trivial_ flips). For each feature: replicate the C-API sampler (Random(seed).Sample(num_rows, k) -> gather), compute `filter_cnt = static_cast<data_size_t>(static_cast<double>(20 * k) / num_rows)` EXACTLY as dataset_loader.cpp:623-624 (total_sample_size = k for the dense in-memory path per c_api.cpp:1368-1374, num_dist_data = num_rows), then call the existing verbatim `FindBinNumeric(sampled, max_bin, min_data_in_bin, /*min_split_data=*/filter_cnt, /*pre_filter=*/true, use_missing=true, zero_as_missing=false, sampled.size(), {})`. Emit per feature a line carrying `num_bin=`, `is_trivial=`, `default_bin=`, `most_freq_bin=`, `missing_type=`, and `upper=` (semicolon-joined F64Bits), then an `ASSIGN f=<f>` line of `ValueToBin(col[r])` over ALL rows (mirror EmitExampleDataset's exact token format so the Rust parser can be reused/adapted). Also emit a `DATASET` header line carrying num_rows / num_features / max_bin / min_data_in_bin / min_data_in_leaf / bin_construct_sample_cnt / seed so the Rust test configures an IDENTICAL Config.
    - Wire it into main() at a NEW trailing argv slot (argv[10]) so ALL existing argv slots (1..9) and every committed golden stay byte-unchanged. Guard with `if (argc >= 11)`; open the output, write a `# ...` header + `MASTER_SEED <master_seed>`, then call the emitter. Do NOT alter the per-dataset seed math of the example block.

    xtask/src/main.rs (bin_capture()):
    - Add `let default_cfg_fixture_path = fixtures_dir.join("default_config_ingest.txt");`, append it as the final `cmd.arg(...)` AFTER the example inputs, add it to the post-run `is_file()` existence check loop, and add it to the final "done. Wrote ..." eprintln list. Keep the function idempotent (pure function of recorded constants).
    - Run `cargo run -p xtask -- bin-capture` to generate crates/lgbm-dataset/tests/fixtures/default_config_ingest.txt. If the C++ capture build is unavailable in this environment, transcribe the golden BY HAND from the verbatim C++ logic above (the values are a pure deterministic function of the recorded seed/dims) and record in the SUMMARY that capture was hand-derived — never fabricate values that do not follow the C++ math.

    Rust side (crates/lgbm-dataset/tests/default_config_ingest_parity.rs — NEW):
    - Parse default_config_ingest.txt (reuse the example_dataset_parity.rs parse style: DATASET header, FEATURE/num_bin/is_trivial/upper lines, ASSIGN per-row lines). Reconstruct the SAME synthetic matrix the C++ emitter used (regenerate it in Rust from the same seed/dims, OR emit the raw matrix into the golden and read it back — choose one and document it; the matrix MUST be identical to C++'s).
    - Build a Config with feature_pre_filter = TRUE (do NOT reuse the pre_filter=false cfg() helper), min_data_in_leaf from the header (default 20), bin_construct_sample_cnt from the header (the small value), max_bin / min_data_in_bin / data_random_seed from the header.
    - Call from_mat, then for each feature assert EXACT equality of is_trivial_, num_bin_, bin_upper_bound_ (f64-bit equality), and the per-row stored bin index vector against the golden. Use the project's exact comparators (no tolerance).
    - This test MUST FAIL against the current build_mapper (which forwards raw min_data_in_leaf=20 instead of filter_cnt=5), proving CR-01/CR-02 are real. Confirm: `cargo test -p lgbm-dataset --test default_config_ingest_parity` reports a FAILURE (is_trivial_ / assignment mismatch) before Task 2.
  </action>
  <verify>
    <automated>cargo test -p lgbm-dataset --test default_config_ingest_parity 2>&1 | grep -E 'FAILED|test result: FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - `xtask/cpp/bin_capture.cpp` contains `EmitDefaultConfigIngest` and an `if (argc >= 11)` block writing the new golden; `grep -c 'EmitExampleDataset' xtask/cpp/bin_capture.cpp` is unchanged (existing emitters untouched).
    - `crates/lgbm-dataset/tests/fixtures/default_config_ingest.txt` exists, contains `is_trivial=` tokens, a `DATASET` header line with `bin_construct_sample_cnt` < `num_rows`, and `min_data_in_leaf=20`.
    - The golden encodes a feature whose `is_trivial` value differs between the scaled threshold (filter_cnt=5) and the raw threshold (20) — i.e. the divergence is genuinely triggered (note the feature index in the SUMMARY).
    - `cargo test -p lgbm-dataset --test default_config_ingest_parity` FAILS on the current (unfixed) build_mapper with an is_trivial_ or per-row ASSIGN mismatch. Capture the failing assertion text in the SUMMARY.
    - Every other committed golden under crates/lgbm-dataset/tests/fixtures/*.txt is byte-unchanged (`git diff --stat` shows only the new default_config_ingest.txt added).
  </acceptance_criteria>
  <done>The scaled-filter_cnt golden exists and the new default-config parity test exists and FAILS against current code, proving GAP-1 + GAP-2 are real and reproducible.</done>
</task>

<task type="auto">
  <name>Task 2: Fix filter_cnt derivation in a single source-of-truth helper used by both sites</name>
  <files>crates/lgbm-dataset/src/bin_mapper.rs, crates/lgbm-dataset/src/ingest.rs</files>
  <read_first>
    - crates/lgbm-dataset/src/ingest.rs:81-101 (build_mapper — the CR-01 site at line ~94 forwarding cfg.min_data_in_leaf raw)
    - crates/lgbm-dataset/src/bin_mapper.rs:627-662 (find_bin_from_column — the IN-02 duplicate forwarding min_split_data raw)
    - LightGBM/src/io/dataset_loader.cpp:623-624 (exact integer-truncation formula) and :645-648 (FindBin call)
    - LightGBM/src/c_api.cpp:1360-1374 (DENSE: total_sample_size=sample_cnt, num_dist_data=total_nrow) — AND scan the CSR (LGBM_DatasetCreateFromCSR) + CSC (LGBM_DatasetCreateFromCSC) sample paths in c_api.cpp to confirm total_sample_size / num_dist_data definitions match the dense convention before locking (the verification missing: item requires this).
    - crates/lgbm-dataset/src/ingest.rs (from_csr / from_csc — confirm they funnel through finish_from_columns -> build_mapper, so the single fix covers all three representations)
  </read_first>
  <action>
    Introduce ONE source-of-truth helper that computes the scaled pre-filter threshold and collapse both raw-forwarding copies onto it.

    - Add a helper (e.g. `pub(crate) fn scaled_filter_cnt(min_data_in_leaf: i32, total_sample_cnt: i32, num_rows: i32) -> i32`) in bin_mapper.rs next to need_filter. Compute it with EXACT C++ integer-truncation semantics: `((min_data_in_leaf as i64 * total_sample_cnt as i64) / num_rows as i64) as i32`, mirroring `static_cast<data_size_t>(static_cast<double>(min_data_in_leaf * total_sample_size) / num_dist_data)`. NOTE the C++ does the divide in DOUBLE then truncates to int; for the in-range integer magnitudes here the i64 integer divide is identical, but document this and guard num_rows == 0 (return min_data_in_leaf to avoid divide-by-zero on the empty-matrix edge; an empty dataset has no rows to filter). Cite D-?? / dataset_loader.cpp:623-624 in the doc comment.
    - In ingest.rs::build_mapper: compute `let filter_cnt = scaled_filter_cnt(cfg.min_data_in_leaf, total_sample_cnt as i32, total_nrow);` and pass `filter_cnt` (NOT cfg.min_data_in_leaf) as the min_split_data argument to find_bin_numeric. Here total_sample_cnt = k (the sampled count) and num_rows = total_nrow, matching the dense c_api convention.
    - Collapse the duplicate: route bin_mapper.rs::find_bin_from_column's min_split_data through the SAME helper, OR (preferred) have build_mapper delegate to find_bin_from_column so there is exactly one sampling+find_bin code path and one filter_cnt computation. Whichever is chosen, after this task there must be NO second place that forwards a raw min_data_in_leaf / min_split_data as the pre-filter threshold without the scaling. Confirm from_csr / from_csc inherit the fix via finish_from_columns.
    - Do NOT change the find_bin_numeric kernel, need_filter, or any committed golden; this is purely the threshold-derivation seam.
  </action>
  <verify>
    <automated>test "$(grep -c 'scaled_filter_cnt' crates/lgbm-dataset/src/bin_mapper.rs)" -ge 1 && cargo build -p lgbm-dataset</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'scaled_filter_cnt' crates/lgbm-dataset/src/bin_mapper.rs crates/lgbm-dataset/src/ingest.rs` shows the helper defined once and called from build_mapper (and find_bin_from_column routes through it / is the single path).
    - `grep -n 'min_data_in_leaf' crates/lgbm-dataset/src/ingest.rs` shows it is consumed ONLY via scaled_filter_cnt, never passed raw to find_bin_numeric.
    - The integer-truncation formula in the helper matches dataset_loader.cpp:623-624 (i64 multiply then divide then cast to i32), verified by a unit test asserting e.g. scaled_filter_cnt(20, 50, 200) == 5 and scaled_filter_cnt(20, 200, 200) == 20.
    - The CSR/CSC total_sample_size / num_dist_data convention was verified against c_api.cpp and the finding (matches dense, or any divergence handled) is recorded in the SUMMARY.
    - `cargo build -p lgbm-dataset` succeeds with no new warnings on the touched lines.
  </acceptance_criteria>
  <done>Both former raw-forwarding sites compute the scaled filter_cnt through one shared helper with exact C++ integer-truncation semantics; CSR/CSC inherit the fix.</done>
</task>

<task type="auto">
  <name>Task 3: Verify — parity test passes, full suite green, capture idempotent, SC#1/SC#5 re-affirmed</name>
  <files>crates/lgbm-dataset/tests/default_config_ingest_parity.rs, .planning/phases/02-dataset-binning-determinism-root/02-06-SUMMARY.md</files>
  <read_first>
    - crates/lgbm-dataset/tests/default_config_ingest_parity.rs (the test from Task 1)
    - crates/lgbm-dataset/src/ingest.rs + crates/lgbm-dataset/src/bin_mapper.rs (the fix from Task 2)
    - xtask/src/main.rs (bin_capture idempotence)
    - .planning/phases/02-dataset-binning-determinism-root/02-VERIFICATION.md (SC#1 and SC#5 success-criteria wording to re-affirm)
  </read_first>
  <action>
    Prove the fix closes both gaps and breaks nothing:
    - Run `cargo test -p lgbm-dataset --test default_config_ingest_parity` and confirm it now PASSES (the same test that failed in Task 1 — fails-before / passes-after property satisfied).
    - Run `cargo test --workspace` and confirm fully green (lgbm-dataset unit + all integration suites, core, oracle-harness; 0 failed). In particular ingest_equivalence.rs and example_dataset_parity.rs (pre_filter=false paths) must remain green — the fix only changes the pre_filter=true threshold.
    - Re-run `cargo run -p xtask -- bin-capture` (if the capture build is available) and confirm it is idempotent: `git diff --stat crates/lgbm-dataset/tests/fixtures/` shows no changes to any golden (the new golden is a pure function of the recorded seed/dims). If the capture build is unavailable, assert idempotence by re-deriving by the documented hand method and confirming byte-identity.
    - In the SUMMARY, re-affirm SC#1 ("is_trivial_ matches C++ on the default in-memory ingest path") and SC#5 / ORA-03 bin stage ("a per-stage bin parity test now covers the DEFAULT configuration"), and note DAT-01 / DAT-07 move from PARTIAL toward SATISFIED for the default-config divergence.
  </action>
  <verify>
    <automated>cargo test -p lgbm-dataset --test default_config_ingest_parity && cargo test --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p lgbm-dataset --test default_config_ingest_parity` PASSES.
    - `cargo test --workspace` reports 0 failed across all suites; ingest_equivalence and example_dataset_parity still pass.
    - `cargo run -p xtask -- bin-capture` is idempotent: re-running produces a byte-identical default_config_ingest.txt (`git diff --quiet crates/lgbm-dataset/tests/fixtures/default_config_ingest.txt` after a second run) — or the documented hand-derivation reproduces byte-identically.
    - SUMMARY explicitly re-affirms SC#1 and SC#5 with the passing test as evidence and records the fails-before/passes-after transition (the failing assertion text from Task 1 + the now-passing run).
  </acceptance_criteria>
  <done>The default-config parity test passes, the full workspace suite is green, the capture is idempotent, and SC#1 + SC#5 (DAT-01, DAT-07) are re-affirmed with evidence.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
New / changed symbols and files:
- `crates/lgbm-dataset/src/bin_mapper.rs` — NEW `scaled_filter_cnt(min_data_in_leaf, total_sample_cnt, num_rows) -> i32` helper (single source of truth); `find_bin_from_column` routed through it.
- `crates/lgbm-dataset/src/ingest.rs` — `build_mapper` now computes & passes `filter_cnt` via `scaled_filter_cnt` instead of raw `cfg.min_data_in_leaf`.
- `crates/lgbm-dataset/tests/default_config_ingest_parity.rs` — NEW parity test (default feature_pre_filter=true, sample_cnt < num_rows).
- `crates/lgbm-dataset/tests/fixtures/default_config_ingest.txt` — NEW C++ golden (scaled-filter_cnt ConstructFromSampleData path).
- `xtask/cpp/bin_capture.cpp` — NEW `EmitDefaultConfigIngest` emitter + `argc >= 11` argv[10] dispatch block.
- `xtask/src/main.rs` — `bin_capture()` writes/asserts the new `default_config_ingest.txt` fixture.
</artifacts_this_plan_produces>

<verification>
- The new default-config parity test FAILS before Task 2 and PASSES after (recorded in SUMMARY).
- `cargo test --workspace` fully green; pre_filter=false suites (ingest_equivalence, example_dataset_parity) unchanged and passing.
- `scaled_filter_cnt` unit test: (20,50,200)==5, (20,200,200)==20 (exact C++ integer truncation).
- No raw `min_data_in_leaf` reaches `find_bin_numeric` as the pre-filter threshold from any ingest site.
- `cargo run -p xtask -- bin-capture` idempotent; only the new golden is added, no existing golden mutated.
- Repo constraints honored: explicit staging, no LightGBM/ or .serena/ or AGENTS.md or .planning/config.json committed; C++ capture header-only.
</verification>

<success_criteria>
GAP-1 (CR-01/IN-02, SC#1, DAT-01) closed: the default in-memory ingest path computes the
scaled filter_cnt in a single source-of-truth helper used by both former duplicate sites,
so is_trivial_ / num_bin_ / per-row bin assignment match C++ on the default configuration.

GAP-2 (CR-02, SC#5 / ORA-03 bin stage, DAT-07) closed: a default feature_pre_filter=true,
sample_cnt < num_rows ingest parity test exists, fails-before/passes-after, replaying a C++
golden captured through the same scaled-filter_cnt path — the default path is now covered.

Full `cargo test --workspace` green; capture idempotent.
</success_criteria>

<output>
Create `.planning/phases/02-dataset-binning-determinism-root/02-06-SUMMARY.md` when done,
recording: the failing-then-passing assertion text for default_config_ingest_parity, the
feature index whose is_trivial_ flips between filter_cnt=5 and raw 20, the CSR/CSC
total_sample_size/num_dist_data verification finding, and re-affirmation of SC#1 + SC#5.
</output>
