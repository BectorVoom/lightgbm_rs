---
phase: 14-scaffold-oracle-slice-0
plan: 03
type: execute
wave: 3
depends_on: ["14-01", "14-02"]
files_modified:
  - crates/oracle-harness/tests/learner_parity.rs
autonomous: true
requirements: [ODL-02, ODL-01]
must_haves:
  truths:
    - "assert_on_device_tree_matches_cpu_anchor compares tree STRUCTURE bit-exact to the cpu f64 anchor, with tie-aware default_left (bit1 of decision_type, mask=2) per internal node and leaf values within ~1e-5 (ROCM_LEAF_VALUE_TOL)."
    - "The tie-aware oracle runs LIVE (D-01): the on-device tree is obtained via backend.grow_tree_on_device(..)?.map(|(t,_)| t).unwrap_or_else(host_grow) and passes against cpu_anchor_tree before any kernel exists."
    - "A seam-reachability test proves grow_tree_on_device returns Ok(None) (no-op) on CpuBackend and GpuBackend and on_device_growth_supported() is false on both — the default route is provably untouched (SC#2)."
    - "The full merge gate is green AND byte-unchanged with LGBM_CUDA_ON_DEVICE unset."
  artifacts:
    - path: "crates/oracle-harness/tests/learner_parity.rs"
      provides: "assert_on_device_tree_matches_cpu_anchor (tie-aware), the D-01 host-fallback oracle test (ODL-02/SC#3), and the SC#2 seam no-op test (ODL-01)"
      contains: "assert_on_device_tree_matches_cpu_anchor"
  key_links:
    - from: "crates/oracle-harness/tests/learner_parity.rs"
      to: "crates/lgbm-compute/src/lib.rs"
      via: "oracle test calls backend.grow_tree_on_device(..); SC#2 test asserts on_device_growth_supported() == false"
      pattern: "grow_tree_on_device|on_device_growth_supported"
    - from: "crates/oracle-harness/tests/learner_parity.rs"
      to: "crates/lgbm-model/src/tree.rs"
      via: "comparator decodes default_left as bit1 of decision_type (DEFAULT_LEFT_MASK = 2)"
      pattern: "decision_type"
---

<objective>
Build the anchor-pinned tie-aware oracle (ODL-02/SC#3) and the seam-reachability proof
(ODL-01/SC#2). The oracle pins on-device tree STRUCTURE bit-exact to the cpu f64 anchor
with a tie-aware `default_left` acceptance and a ~1e-5 leaf envelope — present BEFORE any
kernel, never comparing two GPU paths to each other (def-f8u-01).

Purpose: Generalize the existing `assert_gpu_tree_matches_cpu_anchor` to be tie-aware
(decode `default_left` = bit1 of `decision_type`, mask=2; lift the kernel_parity.rs:1597
near-tie acceptance to per-internal-node), run it LIVE via D-01's host-fallback, and prove
the Plan 01 seam is a provable no-op so the default route is untouched.
Output: `assert_on_device_tree_matches_cpu_anchor`, the D-01 oracle test, the SC#2 test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-scaffold-oracle-slice-0/14-CONTEXT.md
@.planning/phases/14-scaffold-oracle-slice-0/14-RESEARCH.md
@.planning/phases/14-scaffold-oracle-slice-0/14-PATTERNS.md
@.planning/phases/14-scaffold-oracle-slice-0/14-VALIDATION.md
@.planning/phases/14-scaffold-oracle-slice-0/14-01-PLAN.md
@.planning/phases/14-scaffold-oracle-slice-0/14-02-PLAN.md
</context>

<artifacts_this_phase_produces>
This plan creates: `assert_on_device_tree_matches_cpu_anchor` (the tie-aware comparator),
the D-01 host-fallback oracle test (ODL-02/SC#3), and the SC#2 seam no-op test (ODL-01).
(Consumes Plan 01's `grow_tree_on_device` + `on_device_growth_supported`; reuses the
existing `cpu_anchor_tree`, `ROCM_LEAF_VALUE_TOL`, and the kernel_parity near-tie logic.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Generalize the comparator into tie-aware assert_on_device_tree_matches_cpu_anchor</name>
  <files>crates/oracle-harness/tests/learner_parity.rs</files>
  <read_first>
    - crates/oracle-harness/tests/learner_parity.rs:2041 (ROCM_LEAF_VALUE_TOL = 1e-5 — reuse)
    - crates/oracle-harness/tests/learner_parity.rs:2046-2079 (assert_gpu_tree_matches_cpu_anchor — the 8 bit-exact structural asserts + the per-leaf 1e-5 envelope loop to generalize)
    - crates/oracle-harness/tests/kernel_parity.rs:1597-1629 (the per-SplitInfo near-tie default_left acceptance to lift to per-internal-node)
    - crates/lgbm-model/src/tree.rs:24-26,47-48,53-55,135 (decision_type packing: DEFAULT_LEFT_MASK = 2 is bit1; categorical = bit0; missing_type = bits2-3; get_decision_type accessor)
    - .planning/phases/14-scaffold-oracle-slice-0/14-CONTEXT.md §D-04
    - .planning/phases/14-scaffold-oracle-slice-0/14-PATTERNS.md §"crates/oracle-harness/tests/learner_parity.rs"
  </read_first>
  <action>
    Add `fn assert_on_device_tree_matches_cpu_anchor(on_device: &lgbm_model::Tree, anchor: &lgbm_model::Tree, label: &str)`
    as a tie-aware generalization reused by both (do NOT duplicate the structural asserts —
    factor the shared 8-field + leaf-envelope body so the existing
    assert_gpu_tree_matches_cpu_anchor can delegate, or extract a common helper). Keep
    bit-exact `assert_eq!` on num_leaves, split_feature, threshold, left_child, right_child,
    leaf_count, internal_count, and the per-leaf `abs_diff <= ROCM_LEAF_VALUE_TOL` envelope.

    Change ONLY the decision_type comparison (the D-04 tie-aware branch):
    1. Per node, compare `decision_type & !DEFAULT_LEFT_MASK` (i.e. `& !2`) STRICTLY —
       categorical (bit0) and missing_type (bits2-3) must stay exactly equal.
    2. For bit1 (`default_left`), apply the kernel_parity.rs:1597 near-tie acceptance PER
       INTERNAL NODE: accept a flip ONLY when that node's `threshold` (exact f64) AND its
       child row counts match; otherwise hard-fail with a message naming a wrong-direction
       (non-tie) divergence. At Tree level the "net gain tie" reduces to threshold+count
       equality since split_gain is predict-irrelevant metadata (PATTERNS §near-tie).
    This is the only non-trivial code in the phase (per-SplitInfo logic → per-node index).
    The tie branch is dormant — no kernel yet produces a flip — but must compile and be
    reachable. Define a local DEFAULT_LEFT_MASK const (= 2) or reuse the lgbm_model one.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test learner_parity 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'fn assert_on_device_tree_matches_cpu_anchor' crates/oracle-harness/tests/learner_parity.rs` matches.
    - The comparator masks bit1 for the strict decision_type compare (grep shows `& !2` or `& !DEFAULT_LEFT_MASK`) and applies a per-internal-node tie acceptance gated on threshold + child-count equality.
    - The 8 structural fields are still asserted bit-exact and leaves use `ROCM_LEAF_VALUE_TOL`; the shared body is factored (assert_gpu_tree_matches_cpu_anchor still compiles, no duplicated 8-field block).
    - `cargo test -p oracle-harness --test learner_parity` compiles and is green.
  </acceptance_criteria>
  <done>assert_on_device_tree_matches_cpu_anchor exists: structure bit-exact + ~1e-5 leaf envelope + tie-aware default_left (bit1) per internal node; compiles, reachable, green; structural body shared not duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: Add the LIVE D-01 host-fallback oracle test (ODL-02 / SC#3)</name>
  <files>crates/oracle-harness/tests/learner_parity.rs</files>
  <read_first>
    - crates/oracle-harness/tests/learner_parity.rs:2083-2096 (cpu_anchor_tree — reuse as-is to build the f64 anchor)
    - crates/oracle-harness/tests/learner_parity.rs:2099-2141 (learner_parity_resident_equals_host_tree_on_hip — the reference test shape: backend + client + SerialTreeLearner::new + train)
    - crates/lgbm-compute/src/lib.rs (grow_tree_on_device seam signature from Plan 01 — gradients, hessians, num_leaves, max_depth → Result<Option<(Tree, LeafPartitionLayout)>>)
    - .planning/phases/14-scaffold-oracle-slice-0/14-CONTEXT.md §D-01, §D-02
    - .planning/phases/14-scaffold-oracle-slice-0/14-RESEARCH.md §"Oracle test exercising the seam via host-fallback"
  </read_first>
  <action>
    Add a `#[test]` that exercises the seam end-to-end LIVE (D-01) before any kernel exists.
    Build a small fixture (reuse the features/g/h construction from the existing parity
    tests). Obtain the on-device tree stand-in:
    `let tree = backend.grow_tree_on_device(&g, &h, num_leaves, max_depth)?.map(|(t, _payload)| t).unwrap_or_else(|| host_grow(&features, &g, &h, num_leaves, max_depth));`
    where `host_grow` builds via `SerialTreeLearner::new(&cpu_backend, &cpu_client, cfg(), num_leaves, max_depth).with_features(..).train(&g, &h, true)` (same construction as
    cpu_anchor_tree — factor a shared helper if convenient). Because the seam returns
    Ok(None) in Slice 0, the unwrap_or_else supplies the host stand-in. Then:
    `let anchor = cpu_anchor_tree(&features, &g, &h, num_leaves, max_depth);` and
    `assert_on_device_tree_matches_cpu_anchor(&tree, &anchor, "slice0-host-fallback");`.
    Both trees are cpu-grown so the comparator passes; this proves the comparator + seam
    signature + plumbing are exercised and GREEN. CRITICAL (D-02): the `unwrap_or_else`
    host-fallback lives in THIS TEST only — it is the stand-in for the not-yet-existent
    on-device tree, never production behavior. NEVER compare two GPU f32 paths (def-f8u-01):
    the anchor is always the cpu f64 tree.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test learner_parity 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - A new `#[test]` calls `backend.grow_tree_on_device(..)` and `.map(|(t,_)| t).unwrap_or_else(..)` to obtain the tree (grep shows the call + unwrap_or_else in the test).
    - The test asserts via `assert_on_device_tree_matches_cpu_anchor(.., &cpu_anchor_tree(..), ..)` — anchor is the cpu f64 tree (never a second GPU path).
    - The test is GREEN and named to reflect the slice-0 host-fallback oracle.
    - `cargo test -p oracle-harness --test learner_parity` green.
  </acceptance_criteria>
  <done>A live #[test] obtains the tree via the seam + host-fallback and passes the tie-aware oracle against the cpu f64 anchor — the comparator and seam plumbing are exercised GREEN before any kernel (SC#3).</done>
</task>

<task type="auto">
  <name>Task 3: Add the SC#2 seam no-op / default-route-untouched test (ODL-01)</name>
  <files>crates/oracle-harness/tests/learner_parity.rs</files>
  <read_first>
    - crates/oracle-harness/tests/learner_parity.rs:2099-2141 (the with_resident(true/false) + assert!(backend.<discriminator>()) reference shape)
    - crates/lgbm-compute/src/lib.rs (on_device_growth_supported default-false + grow_tree_on_device Ok(None) default + GpuBackend<R> override from Plan 01)
    - .planning/phases/14-scaffold-oracle-slice-0/14-RESEARCH.md §"SC#2 (seam reachable, default untouched)"
    - .planning/phases/14-scaffold-oracle-slice-0/14-CONTEXT.md §specifics (GpuBackend<R> override must return Ok(None))
  </read_first>
  <action>
    Add a `#[test]` proving the seam is a provable no-op so the default route is untouched
    (SC#2). Assert on CpuBackend AND a GpuBackend (the existing rocm/cpu test client
    construction): (a) `on_device_growth_supported()` returns `false` on both (Slice-0
    discriminator stays false — Pitfall 2, no kernel yet); (b) a direct call
    `backend.grow_tree_on_device(&g, &h, num_leaves, max_depth)` returns `Ok(None)` on both
    — the GpuBackend<R> override no-op is the proof the default route is provably untouched.
    This is the faithful Slice-0 observable (no real backend has the discriminator true, so
    the production fork's Some-branch is genuinely unexercised until Slice 1). OPTIONAL
    (executor discretion, only if a delegating test-only Backend wrapper is cheap): also
    exercise the train_inner fork by forcing on_device_eligible via a wrapper backend whose
    on_device_growth_supported() returns true + LGBM_CUDA_ON_DEVICE=1, asserting the fork
    falls through (seam Ok(None)) to the host tree equal to cpu_anchor_tree. If the wrapper
    is not cheap, the direct no-op proof above is sufficient for SC#2. If the test sets the
    env var, wrap it in the existing serialized env-lock guard pattern so it cannot leak
    into other tests.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test learner_parity 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - A new `#[test]` asserts `on_device_growth_supported() == false` on CpuBackend and GpuBackend (grep shows both asserts).
    - The test asserts `grow_tree_on_device(..)` returns `Ok(None)` (matches!(.., Ok(None)) or assert) on both backends.
    - If the env var is set anywhere in the test, it is inside a serialized/guarded scope (grep shows the lock guard).
    - `cargo test -p oracle-harness --test learner_parity` green.
  </acceptance_criteria>
  <done>A #[test] proves the discriminator is false and the seam returns Ok(None) (no-op) on Cpu + Gpu backends — the default route is provably untouched (SC#2); green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → test harness | a SC#2 test MAY set `LGBM_CUDA_ON_DEVICE`; must be guarded so it cannot leak across tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | test-set `LGBM_CUDA_ON_DEVICE` leaking into sibling tests | mitigate | If any test sets the env var, wrap it in the existing serialized env-lock guard (the FORCE_ENV_LOCK pattern in learner_parity) and restore/unset after — prevents a forced-on toggle bleeding into the byte-unchanged merge-gate tests. |
| T-14-05 | Spoofing/Tampering | oracle comparing two nondeterministic GPU f32 paths (def-f8u-01) | mitigate | The oracle always pins to the cpu f64 anchor (cpu_anchor_tree); the comparator never takes two GPU trees. This is the whole reason ODL-02 exists. |
| T-14-SC | Tampering | cargo installs | accept | Zero external packages added (test-only code on the built-in harness). |
</threat_model>

<verification>
- `cargo test -p oracle-harness --test learner_parity` green (new oracle + SC#2 tests pass; 63 existing parity tests unregressed).
- Full merge gate green AND byte-unchanged with LGBM_CUDA_ON_DEVICE unset:
  `cargo test -p oracle-harness --test raw_bin_train_parity` (raw_bin_train_matches_cpp_golden),
  `cargo test -p oracle-harness --test kernel_parity`, `cargo test --workspace`.
</verification>

<success_criteria>
- assert_on_device_tree_matches_cpu_anchor exists: structure bit-exact + ~1e-5 leaf envelope + tie-aware default_left per internal node (ODL-02).
- The live D-01 host-fallback oracle test passes against the cpu f64 anchor before any kernel (SC#3).
- The SC#2 test proves the seam is a no-op (Ok(None)) and the discriminator false on Cpu + Gpu backends (default route untouched).
- Full merge gate green and byte-unchanged with the env unset (SC#1).
</success_criteria>

<output>
Create `.planning/phases/14-scaffold-oracle-slice-0/14-03-SUMMARY.md` when done.
</output>
