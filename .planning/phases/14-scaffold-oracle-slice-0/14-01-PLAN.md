---
phase: 14-scaffold-oracle-slice-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/lgbm-dataset/src/dataset.rs
  - crates/lgbm-dataset/src/lib.rs
  - crates/lgbm-compute/Cargo.toml
  - crates/lgbm-compute/src/lib.rs
autonomous: true
requirements: [ODL-01]
must_haves:
  truths:
    - "A lower-crate partition payload type LeafPartitionLayout exists in lgbm-dataset and is publicly nameable from lgbm-compute (no crate cycle)."
    - "Backend::on_device_growth_supported() exists, defaults false, and CpuBackend + GpuBackend<R> both report false in Slice 0."
    - "Backend::grow_tree_on_device(..) exists, defaults to Ok(None), and the GpuBackend<R> override also returns Ok(None) (no-op)."
    - "The workspace still builds and the full merge gate is green with LGBM_CUDA_ON_DEVICE unset."
  artifacts:
    - path: "crates/lgbm-dataset/src/dataset.rs"
      provides: "LeafPartitionLayout POD struct (num_data, indices, leaf_begin, leaf_count)"
      contains: "pub struct LeafPartitionLayout"
    - path: "crates/lgbm-compute/Cargo.toml"
      provides: "lgbm-model path dependency so Tree is nameable on the Backend seam"
      contains: "lgbm-model"
    - path: "crates/lgbm-compute/src/lib.rs"
      provides: "on_device_growth_supported() discriminator + grow_tree_on_device() seam on trait Backend + GpuBackend<R> no-op override"
      contains: "fn grow_tree_on_device"
  key_links:
    - from: "crates/lgbm-compute/src/lib.rs"
      to: "crates/lgbm-dataset/src/dataset.rs"
      via: "grow_tree_on_device return type names LeafPartitionLayout"
      pattern: "LeafPartitionLayout"
    - from: "crates/lgbm-compute/src/lib.rs"
      to: "crates/lgbm-model/src/tree.rs"
      via: "grow_tree_on_device return type names lgbm_model::Tree (acyclic dep added)"
      pattern: "Tree"
---

<objective>
Stand up the lower-crate partition payload type and the additive `Backend` seam +
discriminator (ODL-01), with the seam a provable no-op (`Ok(None)`) on every backend.
This unblocks the learner fork (Plan 02) and the oracle (Plan 03) by naming the concrete
`P` type and the seam signature.

Purpose: Resolve D-03 via Option A — name a lower-crate `P` (`LeafPartitionLayout` in
lgbm-dataset) so the `(Tree, P)` seam can live on `Backend` (lgbm-compute) without the
`DataPartition` crate cycle, and add `lgbm-model` to lgbm-compute so `Tree` is nameable.
Output: `LeafPartitionLayout`, `Backend::on_device_growth_supported()`,
`Backend::grow_tree_on_device(..) -> Result<Option<(Tree, LeafPartitionLayout)>, ComputeError>`,
and the `GpuBackend<R>` no-op override. Zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-scaffold-oracle-slice-0/14-CONTEXT.md
@.planning/phases/14-scaffold-oracle-slice-0/14-RESEARCH.md
@.planning/phases/14-scaffold-oracle-slice-0/14-PATTERNS.md
@.planning/phases/14-scaffold-oracle-slice-0/14-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates: `LeafPartitionLayout` (lgbm-dataset), the `lgbm-model` dep edge on
lgbm-compute, `Backend::on_device_growth_supported()`, `Backend::grow_tree_on_device()`,
and the `GpuBackend<R>` no-op overrides of both. (Downstream: Plan 02 adds
`cuda_on_device_env`, `on_device_eligible`, `DataPartition::from_payload`, the train_inner
fork; Plan 03 adds `assert_on_device_tree_matches_cpu_anchor`. The runtime input
`LGBM_CUDA_ON_DEVICE` is read in Plan 02.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add LeafPartitionLayout payload struct to lgbm-dataset</name>
  <files>crates/lgbm-dataset/src/dataset.rs, crates/lgbm-dataset/src/lib.rs</files>
  <read_first>
    - crates/lgbm-dataset/src/dataset.rs:58-70 (EfbSamples — the exact POD struct shape to mirror: `#[derive(Debug, Clone)]`, public Vec + scalar fields, no behavior)
    - crates/lgbm-treelearner/src/data_partition.rs:33-42 (DataPartition — the four fields the payload must carry: num_data: i32, indices: Vec<u32>, leaf_begin: Vec<i32>, leaf_count: Vec<i32>)
    - crates/lgbm-dataset/src/lib.rs (the pub re-export list — add LeafPartitionLayout next to EfbSamples)
    - .planning/phases/14-scaffold-oracle-slice-0/14-PATTERNS.md §"Partition payload `P` (CREATE)"
  </read_first>
  <action>
    Add `pub struct LeafPartitionLayout` to crates/lgbm-dataset/src/dataset.rs beside
    `EfbSamples`, deriving `#[derive(Debug, Clone)]`, with exactly the four public fields
    that `DataPartition` (data_partition.rs:33) wraps — `pub num_data: i32`,
    `pub indices: Vec<u32>`, `pub leaf_begin: Vec<i32>`, `pub leaf_count: Vec<i32>`. This
    is the D-03-RESOLVED Option A "lower-crate partition payload P": a pure POD container,
    NO methods, NO dependency on lgbm-treelearner or lgbm-compute. Doc-comment it as the
    raw leaf-row index layout a device-grown tree returns (ODL-01), that lgbm-treelearner
    reconstructs `DataPartition` from in Plan 02's fork. Export it from
    crates/lgbm-dataset/src/lib.rs the same way `EfbSamples` is exported so lgbm-compute
    can name `lgbm_dataset::LeafPartitionLayout`.
  </action>
  <verify>
    <automated>cargo build -p lgbm-dataset 2>&1 | tail -5; cargo test -p lgbm-dataset 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'pub struct LeafPartitionLayout' crates/lgbm-dataset/src/dataset.rs` matches.
    - The struct has exactly the four fields `num_data: i32`, `indices: Vec<u32>`, `leaf_begin: Vec<i32>`, `leaf_count: Vec<i32>`, all `pub`, and derives `Debug, Clone`.
    - `grep -n 'LeafPartitionLayout' crates/lgbm-dataset/src/lib.rs` shows it re-exported.
    - No `use lgbm_treelearner` or `use lgbm_compute` appears in lgbm-dataset (grep -L confirms; would be a cycle).
    - `cargo build -p lgbm-dataset` exits 0.
  </acceptance_criteria>
  <done>LeafPartitionLayout exists in lgbm-dataset, is publicly re-exported, builds clean, and introduces no upward crate dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Add the Backend discriminator + grow_tree_on_device seam (no-op) and the lgbm-model dep</name>
  <files>crates/lgbm-compute/Cargo.toml, crates/lgbm-compute/src/lib.rs</files>
  <read_first>
    - crates/lgbm-compute/src/lib.rs:931-937 (resident_pool_supported — the default-false discriminator to copy verbatim in shape)
    - crates/lgbm-compute/src/lib.rs:898-900 (prefers_host_partition — sibling additive-bool default-false idiom)
    - crates/lgbm-compute/src/lib.rs:495 (trait Backend), :1239,:1242 (CpuBackend inherits defaults), :2037,:2126 (GpuBackend<R> impl where the override goes), :2110,:2116,:2123 (Rocm/Cuda/Wgpu type aliases of GpuBackend<R>)
    - crates/lgbm-compute/Cargo.toml:7-10 (the [dependencies] block — lgbm-core + lgbm-dataset present; add lgbm-model here)
    - crates/lgbm-model/Cargo.toml (proves lgbm-model deps are only lgbm-core + lgbm-dataset + thiserror — the lgbm-compute→lgbm-model edge is acyclic)
    - crates/lgbm-model/src/tree.rs:72 (pub struct Tree — the seam's tree return type)
    - .planning/phases/14-scaffold-oracle-slice-0/14-RESEARCH.md §"D-03 Feasibility" and §"Code Examples"
    - .planning/phases/14-scaffold-oracle-slice-0/14-PATTERNS.md §"crates/lgbm-compute/src/lib.rs"
  </read_first>
  <action>
    (a) In crates/lgbm-compute/Cargo.toml add `lgbm-model = { path = "../lgbm-model" }`
    to `[dependencies]` (alongside lgbm-core/lgbm-dataset). VERIFIED acyclic — lgbm-model
    does NOT depend on lgbm-compute.

    (b) On `trait Backend` (lib.rs:495), add `fn on_device_growth_supported(&self) -> bool { false }`
    mirroring `resident_pool_supported` (:935) verbatim in shape. Keep it `false` for
    `GpuBackend<R>` in Slice 0 — do NOT override to true: `GpuBackend<R>` is ONE generic
    impl shared by ROCm/CUDA/WGPU (Pitfall 2), so a true here hits all three; no kernel
    exists yet (activation is Slice 1). CpuBackend inherits the default.

    (c) On `trait Backend`, add the seam:
    `fn grow_tree_on_device(&self, gradients: &[f32], hessians: &[f32], num_leaves: i32, max_depth: i32) -> Result<Option<(lgbm_model::Tree, lgbm_dataset::LeafPartitionLayout)>, ComputeError> { Ok(None) }`.
    `Ok(None)` = "I did not grow it" (NOT a typed `Err(NotSupported)` — D-03 keeps the
    default path error-noise-free). The args are the forward-looking minimum the learner
    has in train_inner; richer feature/bin inputs arrive in Slice 1 (additive). Use ONLY
    lgbm-compute-nameable types (lgbm-core / lgbm-dataset / lgbm-model / cubecl) — NEVER
    `use lgbm_treelearner::...` (the DataPartition cycle warning sign, Pitfall 1).
    Bake the cubecl-0.10 checklist into the seam's doc-comment for Slice 1: no global
    barrier; Atomic<i64> broken; wrapping_add not an intrinsic; plane-sum <= plane width;
    launch_unchecked is unsafe.

    (d) On the `impl Backend for GpuBackend<R>` block (:2126), add an explicit override of
    `grow_tree_on_device` that also returns `Ok(None)` (no-op) — this is the SC#2 proof
    that the default route is provably untouched.
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute 2>&1 | tail -5; cargo test -p lgbm-compute 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'lgbm-model' crates/lgbm-compute/Cargo.toml` matches the path dep.
    - `grep -n 'fn on_device_growth_supported' crates/lgbm-compute/src/lib.rs` returns the default-false method on the trait.
    - `grep -n 'fn grow_tree_on_device' crates/lgbm-compute/src/lib.rs` returns BOTH the trait default and the GpuBackend<R> override (2 sites).
    - The seam return type is `Result<Option<(lgbm_model::Tree, lgbm_dataset::LeafPartitionLayout)>, ComputeError>` (grep the signature).
    - `grep -c 'use lgbm_treelearner' crates/lgbm-compute/src/lib.rs` returns 0 (no cycle).
    - The seam doc-comment contains the cubecl-0.10 gotcha checklist (grep for `launch_unchecked` and `Atomic`).
    - `cargo build -p lgbm-compute` exits 0; `cargo test -p lgbm-compute` green.
  </acceptance_criteria>
  <done>Both Backend methods exist (discriminator default-false; seam default Ok(None)); GpuBackend<R> overrides the seam to Ok(None); lgbm-model dep added; lgbm-compute builds and tests green with no treelearner import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env → library | (introduced in Plan 02) the `LGBM_CUDA_ON_DEVICE` toggle; this plan adds only the off-by-default seam it gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering/Repudiation | `grow_tree_on_device` / `on_device_growth_supported` seam | mitigate | Discriminator defaults false on `Backend`; `GpuBackend<R>` override returns `Ok(None)` (no-op). With no kernel and the discriminator false, the seam can never alter CPU/ROCm trees. Enforced by the full byte-unchanged merge gate (SC#1). |
| T-14-SC | Tampering | cargo installs | accept | This plan installs ZERO external packages; the only dep added is the internal path edge `lgbm-compute → lgbm-model` (no registry, no slopsquat surface). |
</threat_model>

<verification>
- `cargo build --workspace` exits 0 (no crate cycle from the lgbm-model edge).
- `cargo test -p lgbm-dataset && cargo test -p lgbm-compute` green.
- Full merge gate green AND byte-unchanged with LGBM_CUDA_ON_DEVICE unset:
  `cargo test -p oracle-harness --test raw_bin_train_parity` (raw_bin_train_matches_cpp_golden),
  `cargo test -p oracle-harness --test learner_parity`, `cargo test --workspace`.
</verification>

<success_criteria>
- LeafPartitionLayout exists in lgbm-dataset (the named D-03 Option A payload P), exported.
- Backend gains a default-false `on_device_growth_supported()` and a default-`Ok(None)` `grow_tree_on_device(..)` returning `Result<Option<(Tree, LeafPartitionLayout)>, ComputeError>`.
- GpuBackend<R> overrides the seam to a no-op `Ok(None)` (SC#2 proof).
- No crate cycle; full merge gate green and byte-unchanged with the env unset (SC#1).
</success_criteria>

<output>
Create `.planning/phases/14-scaffold-oracle-slice-0/14-01-SUMMARY.md` when done.
</output>
