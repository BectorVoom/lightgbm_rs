---
phase: 15-on-device-device-dataset-row-subset-gather
plan: 04
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - crates/lgbm-compute/src/kernels/copy_subrow.rs
autonomous: true
requirements: [ODL-04]

must_haves:
  truths:
    - "CopySubrow gathers the compacted subset == host BinColumn::gather(used_indices) across all 3 widths (dense + row-major span)"
    - "The on-device bagging draw stream + route is bit-for-bit equal to the host bag_data_indices over >=2 BAGGING_RAND_BLOCK blocks"
    - "The gather works for ANY host-supplied index set (GOSS-shaped), not just bagging (D-06)"
    - "Every used_indices[i] is validated in [0, num_data) BEFORE the unsafe launch, returning ComputeError"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/copy_subrow.rs"
      provides: "copy_subrow_kernel<B: Int> + copy_subrow_on launcher (D-07) + bagging_draw_on wrapper over draw_next_float_on (D-05)"
      min_lines: 120
      contains: "copy_subrow_kernel"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/copy_subrow.rs"
      to: "crates/lgbm-compute/src/kernels/random.rs (draw_next_float_on)"
      via: "bagging_draw_on reuses the Phase-14 launcher, one seed per BAGGING_RAND_BLOCK block"
      pattern: "draw_next_float_on"
    - from: "crates/lgbm-compute/src/kernels/copy_subrow.rs"
      to: "crates/lgbm-compute/tests/copy_subrow_parity.rs"
      via: "gather_matches_host_all_widths / bagging_draw_matches_host / gather_arbitrary_indices pass against host truth"
      pattern: "copy_subrow_on|bagging_draw_on"
  prohibitions:
    - "NEVER compare two GPU paths — anchor the draw to the inline host bag_data_indices reference (D-05/D-08, def-f8u-01)."
    - "NEVER widen the gather output — same width in/out (D-07)."
    - "NEVER compare the bagging route in f32 — promote the f32 NextFloat to f64 before `< bagging_fraction` (Pitfall 6)."
    - "NEVER import lgbm-boosting (crate cycle) — BAGGING_RAND_BLOCK=1024 is a local const."
    - "NEVER skip the used_indices in-range validation before the unsafe launch (T-15-IDX)."
---

<objective>
Implement the `CopySubrow` row-subset gather + the on-device bagging draw in `copy_subrow.rs`
(ODL-04): a per-row, per-column-width-dispatched gather (`in[used_indices[local]] →
out[local]`) that compacts the binned subset for ANY host-supplied index set, and a bagging
draw wrapper that reproduces the host `BAGGING_RAND_BLOCK` per-block RNG stream bit-for-bit
via the Phase-14 `draw_next_float_on` launcher.

Purpose: ODL-04 — build the bagging/GOSS subset dataset on device, anchor-pinned to the host
subset-selection draw sequence (never GPU-vs-GPU). The gather is index-set-agnostic (GOSS
subsets gather now from host indices, D-06); only the plain-bagging draw is ported this phase.
Output: `copy_subrow.rs` filled; the three ODL-04 tests in `copy_subrow_parity.rs` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-RESEARCH.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-PATTERNS.md
@crates/lgbm-compute/src/kernels/copy_subrow.rs
@crates/lgbm-compute/tests/copy_subrow_parity.rs
</context>

## Artifacts this plan produces
`copy_subrow.rs` symbols: `copy_subrow_kernel<B: Int>` (`#[cube(launch)]`, the D-07 skeleton),
`copy_subrow_on` (host launcher, per-column width dispatch + V5 validation), `bagging_draw_on`
(the on-device bagging-draw wrapper over `draw_next_float_on`), `COPY_SUBROW_BLOCK_SIZE=1024`,
local `BAGGING_RAND_BLOCK=1024`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CopySubrow gather kernel + launcher (per-column width dispatch + V5 validation)</name>
  <files>crates/lgbm-compute/src/kernels/copy_subrow.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/partition.rs:44-100 (the `#[cube(launch)] <B: Int>` kernel + `u32::cast_from` index read) and :360-400 (`launch_native!` per-width match + the SAFETY-comment confined-unsafe block)
    - crates/lgbm-compute/src/kernels/partition.rs:210-241 (the V5 per-index boundary validation + `ComputeError::BinIndexOutOfRange` precedent to mirror for `used_indices`)
    - crates/lgbm-compute/src/lib.rs:134 (`BinColumn::gather` — the host oracle) + :51-188 (variants)
  </read_first>
  <behavior>
    - `copy_subrow_on(client, &BinColumn, used_indices, num_data)` returns a `BinColumn` of the SAME width whose row `local` == `in[used_indices[local]]`, equal to `BinColumn::gather(used_indices)`.
    - Works for dense columns and the row-major span; index set may be arbitrary/unsorted (GOSS-shaped).
    - A `used_indices[i] >= num_data` (or negative) returns `Err(ComputeError)` before any launch.
  </behavior>
  <action>
    Keep/confirm the `copy_subrow_kernel<B: Int>` D-07 skeleton (`local = ABSOLUTE_POS; if local < num_used { out_col[local] = in_col[used_indices[local] as u32]; }` — same width in/out, no widen). Implement `copy_subrow_on`: VALIDATE every `used_indices[i] ∈ [0, num_data)` BEFORE the unsafe launch, returning `ComputeError` (mirror the `partition.rs:233-241` per-index loop — T-15-IDX; the C++ raw-pointer table has no bounds check, the Rust port adds one). `checked_mul` on the `num_used * elem_width` output sizing (T-15-PART). Dispatch the monomorph on the `BinColumn` width via the `launch_native!`/`match BinColumn` idiom; `COPY_SUBROW_BLOCK_SIZE = 1024`, `cube_count = num_used.div_ceil(1024)`. Read back into a same-width `BinColumn`. Confine all cubecl unsafe to the launcher with a SAFETY comment (CMP-01). Host oracle for the test is `BinColumn::gather`.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test copy_subrow_parity gather_matches_host_all_widths gather_arbitrary_indices</automated>
  </verify>
  <acceptance_criteria>
    `gather_matches_host_all_widths` and `gather_arbitrary_indices` pass: the device subset equals `BinColumn::gather(used_indices)` for all three widths and for an arbitrary unsorted index set; same width in/out. An out-of-range index returns `ComputeError` before launch. clippy clean on the touched file.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: On-device bagging draw — reuse draw_next_float_on, anchor to host bag_data_indices</name>
  <files>crates/lgbm-compute/src/kernels/copy_subrow.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/random.rs:230-275 (`draw_next_float_on(client, seeds, k) -> Result<Vec<f32>, ComputeError>`: one task per seed, k draws, row-major output) and :77-88 (`cuda_next_float`, divisor exactly 32768.0f32)
    - .planning/phases/15-…/15-RESEARCH.md Pattern 6 + Pitfalls 5 & 6 (block-seed convention `bagging_seed + block` for the iteration-0 anchor; f32→f64 route promotion)
    - crates/lgbm-compute/tests/copy_subrow_parity.rs (the inline `host_bag_data_indices` reference Wave 0 authored — the anchor)
  </read_first>
  <behavior>
    - `bagging_draw_on(client, num_data, bagging_seed, bagging_fraction)` produces, for `num_data > 1024` (≥2 blocks), a draw stream where row `i`'s NextFloat is `draws[i]` (block `b = i/1024`), and a host-side route `(draws[i] as f64) < bagging_fraction` yielding the `[in-bag asc] ++ [OOB desc]` index layout.
    - The full index layout is bit-for-bit equal to the inline host reference `host_bag_data_indices(num_data, bagging_seed, bagging_fraction)`.
  </behavior>
  <action>
    Implement `bagging_draw_on`: `n_blocks = num_data.div_ceil(BAGGING_RAND_BLOCK)` (local `const BAGGING_RAND_BLOCK: i32 = 1024` — do NOT import from lgbm-boosting, crate cycle); `seeds[b] = (bagging_seed + b) as u32` (iteration-0 anchor convention, Pitfall 5 — document that multi-iteration continuity needs host block-state supply, a Phase-21 seam; keep the seed-supply seam explicit). Call `draw_next_float_on(client, &seeds, BAGGING_RAND_BLOCK as u32)`; row `i`'s draw is `draws[i]` (`b*1024 + i%1024 == i`). Compute the route host-side from the readback (Open Question 2 recommendation — simplest, fully anchored): for each row promote the f32 draw to f64 (Pitfall 6) and compare `< bagging_fraction`, filling in-bag left (ascending) / OOB right, then reverse the OOB tail — producing `[in-bag asc] ++ [OOB desc]`. Return the index layout (and the realized in-bag count). `checked_mul` already guarded inside `draw_next_float_on` (`validate_draw_inputs`).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test copy_subrow_parity bagging_draw_matches_host</automated>
  </verify>
  <acceptance_criteria>
    `bagging_draw_matches_host` passes over `num_data > 1024` (≥2 blocks): the device draw stream (`f32::to_bits()`-exact) and the resulting `[in-bag asc] ++ [OOB desc]` index layout are bit-for-bit equal to the inline host `host_bag_data_indices` reference. The route compare promotes f32→f64. No `lgbm-boosting` import. clippy clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host `used_indices` → device gather | untrusted indices cross into an unsafe device read |
| draw output sizing | `n_blocks * BAGGING_RAND_BLOCK` length must not overflow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-IDX | Tampering / Info-disclosure | `copy_subrow_on` `used_indices` | mitigate | Host-validate every `used_indices[i] ∈ [0, num_data)` before the unsafe launch; return `ComputeError` (the `partition.rs:233-241` precedent). |
| T-15-PART | Denial of Service / memory corruption | gather/draw length sizing | mitigate | `checked_mul` on `num_used * width`; `draw_next_float_on` already `checked_mul`s `n_blocks * BLOCK` (`validate_draw_inputs`, `random.rs:146`). |
| T-15-CMP | Memory corruption | `from_raw_parts` in the gather launcher | mitigate | Confine cubecl unsafe to the launcher with a SAFETY comment proving handle sizing + lifetime (CMP-01). |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --test copy_subrow_parity` (all 3 tests) green.
- `cargo clippy -p lgbm-compute --tests` clean.
- No `lgbm-boosting` import; route promoted f32→f64; same width in/out.
</verification>

<success_criteria>
`CopySubrow` compacts the subset bit-identically to `BinColumn::gather` across all widths and
arbitrary index sets, and the on-device bagging draw + route is bit-for-bit anchored to the
host `bag_data_indices` over ≥2 blocks — never GPU-vs-GPU.
</success_criteria>

<output>
Create `.planning/phases/15-on-device-device-dataset-row-subset-gather/15-04-SUMMARY.md` when done
</output>
