---
phase: 15-on-device-device-dataset-row-subset-gather
plan: 05
type: execute
wave: 2
depends_on: ["15-02", "15-03", "15-04"]
files_modified:
  - .planning/phases/15-on-device-device-dataset-row-subset-gather/15-05-SUMMARY.md
autonomous: true
requirements: [ODL-03, ODL-04]

must_haves:
  truths:
    - "The full merge gate is green: cargo test --workspace passes with LGBM_CUDA_ON_DEVICE unset (D-10)"
    - "raw_bin_train_matches_cpp_golden and learner_parity are byte-unchanged (the cpu f64 anchor / hard merge gate)"
    - "on_device_growth_supported() still returns false (this phase mirrors binning resident; it does not grow)"
    - "The new device-dataset modules are reachable only behind LGBM_CUDA_ON_DEVICE; the default cpu/ROCm/host-CUDA paths are byte-unchanged"
  artifacts:
    - path: ".planning/phases/15-on-device-device-dataset-row-subset-gather/15-05-SUMMARY.md"
      provides: "merge-gate evidence: full-suite result + the D-10 byte-unchanged confirmation"
  key_links:
    - from: "the Phase-15 additive modules"
      to: "the default training path"
      via: "LGBM_CUDA_ON_DEVICE OFF by default — no default-path edge introduced"
      pattern: "LGBM_CUDA_ON_DEVICE"
  prohibitions:
    - "NEVER edit source to make the gate pass — if a default-path suite regresses, that is a real D-10 violation to fix in the owning plan, not to paper over here."
    - "on_device_growth_supported() stays false."
---

<objective>
Phase-15 merge gate (D-10): prove the device-dataset additions are fully additive — the
entire workspace test suite is green with `LGBM_CUDA_ON_DEVICE` unset, and the cpu f64
anchor suites (`raw_bin_train_matches_cpp_golden`, `learner_parity`) plus the
lgbm/treelearner/compute suites are byte-unchanged.

Purpose: The hard merge gate. v1.1 is additive and off by default; this plan is the
goal-backward verification that Success Criterion 4 ("CPU / ROCm / existing-host-CUDA paths
byte-unchanged; merge gate green") holds before `/gsd-verify-work`.
Output: a SUMMARY recording the full-suite result + the D-10 confirmation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-VALIDATION.md
</context>

## Artifacts this plan produces
Verification evidence only — no source changes. Confirms the §13/§3 stores + `CopySubrow` +
bagging draw landed additively behind the OFF-by-default `LGBM_CUDA_ON_DEVICE` seam.

<tasks>

<task type="auto">
  <name>Task 1: Run the full merge gate + confirm D-10 byte-unchanged default path</name>
  <files>.planning/phases/15-on-device-device-dataset-row-subset-gather/15-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/15-…/15-VALIDATION.md (the Phase gate row: `cargo test --workspace` green, merge gate D-10 unchanged)
    - crates/lgbm-compute/src/kernels/mod.rs (confirm the 3 new modules are registered ungated and nothing else changed)
  </read_first>
  <action>
    With `LGBM_CUDA_ON_DEVICE` UNSET (the default), run the full workspace suite and the named anchor suites, and confirm clippy is clean on the new code:
    - `cargo test --workspace` — must be green.
    - `cargo test -p oracle-harness` — esp. `raw_bin_train_matches_cpp_golden` (the cpu f64 bit-exact anchor) and `learner_parity` — byte-unchanged.
    - `cargo test -p lgbm-compute` + `cargo test -p lgbm-treelearner --lib` + `cargo test -p lgbm` — green.
    - `cargo clippy -p lgbm-compute --tests` — clean.
    - Confirm `on_device_growth_supported()` still returns `false` (grep the source; this phase mirrors binning resident, it does not grow).
    Record the pass/fail of each in the SUMMARY. If ANY default-path suite regresses, STOP and report it as a D-10 violation for the owning Wave-1 plan to fix — do NOT edit source here to mask it.
  </action>
  <verify>
    <automated>cargo test --workspace && cargo test -p oracle-harness raw_bin_train_matches_cpp_golden && cargo clippy -p lgbm-compute --tests</automated>
  </verify>
  <acceptance_criteria>
    `cargo test --workspace` exits 0; `raw_bin_train_matches_cpp_golden` and `learner_parity` pass byte-unchanged; `lgbm-compute` / `lgbm-treelearner` / `lgbm` suites green; clippy clean; `on_device_growth_supported()` still false. The SUMMARY records each result and the D-10 confirmation.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| additive modules → default path | the new code must introduce no edge reachable with `LGBM_CUDA_ON_DEVICE` unset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-DEFAULT | Tampering | default training path | mitigate | The full merge gate with `LGBM_CUDA_ON_DEVICE` unset proves the default cpu/ROCm/host-CUDA paths are byte-unchanged (D-10); any regression blocks the phase. |
</threat_model>

<verification>
- `cargo test --workspace` green with `LGBM_CUDA_ON_DEVICE` unset.
- cpu f64 anchor suites byte-unchanged.
- `on_device_growth_supported()` still false.
</verification>

<success_criteria>
The phase is additive and the hard merge gate is green — the device-dataset + gather
additions break nothing on the default path.
</success_criteria>

<output>
Create `.planning/phases/15-on-device-device-dataset-row-subset-gather/15-05-SUMMARY.md` when done
</output>
