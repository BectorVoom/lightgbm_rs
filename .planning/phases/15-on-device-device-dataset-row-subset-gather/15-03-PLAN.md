---
phase: 15-on-device-device-dataset-row-subset-gather
plan: 03
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - crates/lgbm-compute/src/kernels/column_data.rs
autonomous: true
requirements: [ODL-03]

must_haves:
  truths:
    - "The §3 column store reproduces host binned values per-column across all 3 bit widths"
    - "The numeric per-feature meta (bit_type, feature_min/max_bin, offset, most_freq_bin, default_bin) matches the host"
    - "The store is uploaded once (D-09) and has NO consumer wired this phase (Phase-18 prediction is the consumer)"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/column_data.rs"
      provides: "§3 CUDAColumnData: per-column native-width device buffers + ColumnFeatureMeta numeric meta, uploaded once"
      min_lines: 120
      contains: "ColumnFeatureMeta"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/column_data.rs"
      to: "crates/lgbm-compute/tests/device_dataset_parity.rs"
      via: "column_store_parity passes against host BinColumn values + numeric meta"
      pattern: "column_store_parity"
    - from: "crates/lgbm-compute/src/kernels/column_data.rs"
      to: "crates/lgbm-compute/src/lib.rs (BinColumn)"
      via: "per-column native-width upload (create_from_slice(<w>::as_bytes))"
      pattern: "BinColumn::U8|BinColumn::U16|BinColumn::U32"
  prohibitions:
    - "NEVER wire a consumer for the column store this phase — build + parity-test only (Phase 18 is the prediction consumer)."
    - "NEVER carry categorical-bitset meta this phase — numeric meta only; leave a documented TODO(Phase 22) marker (Open Question 1)."
    - "NEVER compare two GPU paths — anchor to host BinColumn / host meta (D-08)."
    - "NEVER re-upload per tree or per-call client.empty in a loop (D-09)."
---

<objective>
Implement the §3 `CUDAColumnData` column store in `column_data.rs` (ODL-03): the
column-major per-column native-width device buffers plus the numeric per-feature meta
(`bit_type`, `feature_{min,max}_bin`, `offset`, `most_freq_bin`, `default_bin`, missing
flags, `feature_to_column`), uploaded once and parity-tested against host `BinColumn` values.

Purpose: This is the prediction-side mirror (Phase-18 tree-walk predict consumer). D-01
requires it built now to match the full C++ dual representation up front; it has NO consumer
this phase — build + parity-test, do not wire.
Output: `column_data.rs` filled; `column_store_parity` in `device_dataset_parity.rs` green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-RESEARCH.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-PATTERNS.md
@crates/lgbm-compute/src/kernels/column_data.rs
@crates/lgbm-compute/tests/device_dataset_parity.rs
</context>

## Artifacts this plan produces
`column_data.rs` symbols: `CudaColumnData<R>` (per-column device handles), `ColumnFeatureMeta`
(numeric per-feature meta), a constructor that uploads once, and a per-column read-back
accessor for parity. Categorical-bitset meta deferred to Phase 22 (documented TODO).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CUDAColumnData per-column buffers + ColumnFeatureMeta numeric meta (upload once)</name>
  <files>crates/lgbm-compute/src/kernels/column_data.rs</files>
  <read_first>
    - docs/cuda-kernel-design.md §3 + §14 (CUDAColumnData column-major layout + per-feature meta fields + host-class buffer-ownership role)
    - crates/lgbm-compute/src/lib.rs:51-188 (`BinColumn` — the per-column storage primitive: `new`, `bin`, `to_u32_vec`) and :2360-2395 (native-width upload shape)
    - crates/lgbm-compute/src/kernels/split_info.rs:267-300 (alloc-once-in-new template, D-09)
    - .planning/phases/15-…/15-RESEARCH.md Pattern 4 (§3 vs §13 — two coexisting layouts) + Open Question 1 (numeric meta now, categorical deferred)
  </read_first>
  <behavior>
    - Build `CudaColumnData` from a host feature set: one native-width device buffer per column (u8/u16/u32 per `BinColumn` variant), uploaded ONCE.
    - `ColumnFeatureMeta` per feature carries numeric meta: `bit_type`, `feature_min_bin`, `feature_max_bin`, `offset`, `most_freq_bin`, `default_bin`, missing flags, `feature_to_column`.
    - A per-column read-back returns the binned values widened to u32, equal to the host `BinColumn::to_u32_vec()`.
  </behavior>
  <action>
    Implement `CudaColumnData::new(client, &[&BinColumn], &[ColumnFeatureMeta]) -> Result<Self, ComputeError>`: upload each column at native width via `create_from_slice(<w>::as_bytes(..))` (the `upload_resident_bins` per-width shape), storing the handles column-major; allocate once (D-09 — no per-call `client.empty` loop beyond the one-time construction). Populate `ColumnFeatureMeta` numeric fields from the host feature metadata. Add a `// TODO(Phase 22): categorical bitset meta` marker (Open Question 1 — numeric only this phase). Provide a per-column read-back accessor (`read_one_unchecked` + per-width decode to u32) for the parity test. `checked_mul` on length sizing (T-15-PART); confine cubecl unsafe to the upload/read launcher with a SAFETY comment (CMP-01).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test device_dataset_parity column_store_parity</automated>
  </verify>
  <acceptance_criteria>
    `column_store_parity` passes: each column's read-back equals the host `BinColumn::to_u32_vec()` across all three widths, and the numeric `ColumnFeatureMeta` (`bit_type`, `feature_min_bin`, `feature_max_bin`, `offset`, `most_freq_bin`, `default_bin`) matches the host. The categorical-meta TODO marker is present. No consumer is wired. clippy clean on the touched file.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host columns → device upload | per-column buffer length sizing must not overflow before alloc |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-PART | Denial of Service / memory corruption | per-column buffer sizing | mitigate | `checked_mul` on every `len*width` product before `client.empty`/`create_from_slice` (the `random.rs:146` precedent). |
| T-15-CMP | Memory corruption | `from_raw_parts` in upload/read-back launcher | mitigate | Confine cubecl unsafe to the launcher with a SAFETY comment proving handle sizing + lifetime (CMP-01). |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --test device_dataset_parity column_store_parity` green.
- `cargo clippy -p lgbm-compute --tests` clean.
- No consumer wired; categorical TODO documented.
</verification>

<success_criteria>
The §3 column store reproduces host binned values + numeric per-feature meta exactly across
all three widths, uploaded once, with no consumer wired and categorical meta deferred.
</success_criteria>

<output>
Create `.planning/phases/15-on-device-device-dataset-row-subset-gather/15-03-SUMMARY.md` when done
</output>
