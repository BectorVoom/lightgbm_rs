---
phase: 15-on-device-device-dataset-row-subset-gather
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/lgbm-compute/src/kernels/mod.rs
  - crates/lgbm-compute/src/kernels/row_data.rs
  - crates/lgbm-compute/src/kernels/column_data.rs
  - crates/lgbm-compute/src/kernels/copy_subrow.rs
  - crates/lgbm-compute/tests/device_dataset_parity.rs
  - crates/lgbm-compute/tests/copy_subrow_parity.rs
autonomous: true
requirements: [ODL-03, ODL-04]

must_haves:
  truths:
    - "cargo build -p lgbm-compute succeeds with the 3 new modules registered ungated"
    - "Both new test files compile and run (failing at todo!() bodies until Waves 1 fill them)"
    - "The in-test sparse synthesizer forces each row_ptr_type{16,32,64} and a large-bin spill (D-04)"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/mod.rs"
      provides: "ungated registration of row_data, column_data, copy_subrow (mirrors random/split, NOT #[cfg(feature=gpu)])"
      contains: "pub mod row_data"
    - path: "crates/lgbm-compute/src/kernels/row_data.rs"
      provides: "§13 CUDARowData public API stubs (DivideCUDAFeatureGroups, accessors, dense/sparse re-lay signatures)"
      min_lines: 40
    - path: "crates/lgbm-compute/src/kernels/column_data.rs"
      provides: "§3 CUDAColumnData public API stubs (per-column buffers + numeric per-feature meta)"
      min_lines: 30
    - path: "crates/lgbm-compute/src/kernels/copy_subrow.rs"
      provides: "CopySubrow kernel + bagging-draw wrapper public API stubs"
      min_lines: 30
    - path: "crates/lgbm-compute/tests/device_dataset_parity.rs"
      provides: "ODL-03 host-vs-device parity tests + sparse synthesizer helper"
      contains: "cpu_client"
    - path: "crates/lgbm-compute/tests/copy_subrow_parity.rs"
      provides: "ODL-04 gather + bagging-draw + arbitrary-index tests; inline host bagging reference"
      contains: "Random"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/mod.rs"
      to: "crates/lgbm-compute/src/kernels/row_data.rs"
      via: "pub mod row_data; (ungated, like random/split)"
      pattern: "pub mod row_data"
    - from: "crates/lgbm-compute/tests/copy_subrow_parity.rs"
      to: "lgbm_core::random::Random"
      via: "inline host bagging reference (NO lgbm-boosting dev-dep — crate cycle, see action)"
      pattern: "use lgbm_core::random::Random"
  prohibitions:
    - "NEVER add lgbm-boosting as a dev-dependency of lgbm-compute (lgbm-boosting -> lgbm-compute is an existing edge; the reverse dev-dep is a crate cycle). Reproduce the host bagging draw inline via lgbm_core::random::Random."
    - "NEVER gate the 3 new modules behind #[cfg(feature=gpu)] — they must run on the default cpu f64 anchor (D-08); register them ungated like random/split."
    - "on_device_growth_supported() stays false this phase (do not touch it)."
---

<objective>
Wave 0 scaffolding for Phase 15. Register the three new `lgbm-compute` kernel modules
(`row_data`, `column_data`, `copy_subrow`) ungated, stub their public API surface so the
crate compiles, and author the two Nyquist parity test files (plus the D-04 sparse-column
synthesizer) that Waves 1 implement against.

Purpose: Establish a compiling, test-anchored skeleton so the three Wave-1 implementation
plans each own exactly one source file with zero `mod.rs`/test-file contention (the proven
Phase-14 pattern — see the existing `kernels/mod.rs` comment block).
Output: `mod.rs` registration + 3 stub source modules + 2 test files; `cargo build
-p lgbm-compute` green, tests compile and run (failing at `todo!()` until Waves 1).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-CONTEXT.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-RESEARCH.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-PATTERNS.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-VALIDATION.md
</context>

## Artifacts this phase produces (full phase manifest — Wave 0 stubs the surface)

New modules (`crates/lgbm-compute/src/kernels/`): `row_data.rs` (§13 `CUDARowData`),
`column_data.rs` (§3 `CUDAColumnData`), `copy_subrow.rs` (`CopySubrow` + bagging draw).
New symbols (filled across Waves 1): `CUDARowData` / `CUDAColumnData` device structs,
`DivideCUDAFeatureGroups`, `GetDenseDataPartitioned`, `GetSparseDataPartitioned`,
`copy_subrow_kernel`, the on-device bagging-draw wrapper, the `LGBM_CUDA_ON_DEVICE` env
gate (read-only; already exists from Phase 14), accessor methods (`bit_type`,
`row_ptr_bit_type`, `NumLargeBinPartition`, the offset-table accessors
`feature_partition_column_index_offsets`, `column_hist_offsets`, `partition_hist_offsets`,
`max_num_column_per_partition`, `num_feature_partitions`, `shared_hist_size`).
New tests: `tests/device_dataset_parity.rs`, `tests/copy_subrow_parity.rs`.

<tasks>

<task type="auto">
  <name>Task 1: Register modules + stub the public API surface of all three new kernel modules</name>
  <files>crates/lgbm-compute/src/kernels/mod.rs, crates/lgbm-compute/src/kernels/row_data.rs, crates/lgbm-compute/src/kernels/column_data.rs, crates/lgbm-compute/src/kernels/copy_subrow.rs</files>
  <read_first>
    - crates/lgbm-compute/src/kernels/mod.rs (the existing ungated `pub mod random; pub mod split; pub mod split_info;` block + the Phase-14 comment explaining why ungated)
    - crates/lgbm-compute/src/kernels/partition.rs:44-100 (the `#[cube(launch)] data_partition_kernel<B: Int>` shape) and :360-400 (`launch_native!` macro + `match BinColumn`)
    - crates/lgbm-compute/src/kernels/random.rs:230-275 (`draw_next_float_on(client, seeds, k) -> Result<Vec<f32>, ComputeError>`)
    - crates/lgbm-compute/src/lib.rs:51-188 (`BinColumn` enum: U8/U16/U32, `new`, `bin`, `gather`, `to_u32_vec`, `len`)
    - crates/lgbm-compute/src/error.rs:19-72 (`ComputeError::{BinIndexOutOfRange, Runtime, LengthMismatch}`)
    - docs/cuda-kernel-design.md §3 and §13 (the field semantics for CUDAColumnData / CUDARowData — accessor table, DivideCUDAFeatureGroups, GetSparseDataPartitioned)
  </read_first>
  <action>
    Append `pub mod row_data;`, `pub mod column_data;`, `pub mod copy_subrow;` to the EXISTING ungated block in `kernels/mod.rs` (immediately after `pub mod subtract;`), mirroring `random`/`split` — NOT behind `#[cfg(feature="gpu")]` (per D-08 the cpu f64 anchor must run them). Add a one-line comment noting they are the Phase-15 additive device-dataset modules behind the OFF-by-default `LGBM_CUDA_ON_DEVICE` seam.

    Create the three source files as COMPILING STUBS that declare the full public API the Wave-1 plans fill, with `todo!("15-NN")` bodies (so the crate builds and the tests compile/link):
    - `row_data.rs`: a `pub struct CudaRowData<R: cubecl::Runtime>` (device-buffer handles + the host layout tables) and a host layout type `pub struct FeaturePartitionLayout` carrying `feature_partition_column_index_offsets: Vec<usize>`, `column_hist_offsets: Vec<usize>`, `partition_hist_offsets: Vec<usize>`, `max_num_column_per_partition: usize`, `num_feature_partitions: usize`, `num_large_bin_partition: usize`, `shared_hist_size: usize`. Stub `pub fn divide_cuda_feature_groups(num_bin_per_column: &[usize], shared_hist_size: usize) -> FeaturePartitionLayout`, plus accessor stubs `bit_type(&self) -> u32`, `row_ptr_bit_type(&self) -> u32`, `num_large_bin_partition(&self) -> usize`, and the offset-table getters. Stub `pub fn get_dense_data_partitioned(...) -> ...` and `pub fn get_sparse_data_partitioned(...) -> Result<_, ComputeError>` signatures (exact param lists are Plan 15-02/refinement scope — keep them minimal but nameable from the tests).
    - `column_data.rs`: `pub struct CudaColumnData<R>` (per-column device handles) + `pub struct ColumnFeatureMeta` (numeric meta: `bit_type: u32`, `feature_min_bin: u32`, `feature_max_bin: u32`, `offset: u32`, `most_freq_bin: u32`, `default_bin: u32`, `missing_is_zero`/`missing_is_na` flags as bool, `feature_to_column: usize`). Leave a `// TODO(Phase 22): categorical bitset meta` marker (Open Question 1 recommendation). Stub a constructor + a per-column read-back accessor for parity.
    - `copy_subrow.rs`: the `#[cube(launch)] pub fn copy_subrow_kernel<B: Int>(in_col: &Array<B>, out_col: &mut Array<B>, used_indices: &Array<i32>, num_used: u32)` (real kernel body is trivial — `let local = ABSOLUTE_POS; if local < num_used { out_col[local] = in_col[used_indices[local] as u32]; }` — you MAY write the real kernel now since it is the D-07 skeleton), a host launcher stub `pub fn copy_subrow_on<R>(client, &BinColumn, used_indices: &[i32], num_data: usize) -> Result<BinColumn, ComputeError>` returning `todo!()`, and a bagging-draw wrapper stub `pub fn bagging_draw_on<R>(client, num_data: i32, bagging_seed: i32, bagging_fraction: f64) -> Result<Vec<i32>, ComputeError>` returning `todo!()`.

    Use `const COPY_SUBROW_BLOCK_SIZE: u32 = 1024;` and reference `BAGGING_RAND_BLOCK = 1024` as a local `const` (do NOT import it from lgbm-boosting — that crate depends on lgbm-compute; importing it back is a cycle). Each stub file gets a module doc-comment citing its design-doc section and the layout-difference warning (Pitfall 3: §13 row store is row-major partition-local `data[idx*ncol+tx]`, NOT the feature-major `resident_bins` buffer).
  </action>
  <verify>
    <automated>cargo build -p lgbm-compute && grep -c "pub mod row_data\|pub mod column_data\|pub mod copy_subrow" crates/lgbm-compute/src/kernels/mod.rs</automated>
  </verify>
  <acceptance_criteria>
    `cargo build -p lgbm-compute` exits 0. The three modules are registered ungated in `mod.rs` (grep returns 3). Each source file exposes the named public API as `todo!()` stubs except `copy_subrow_kernel` whose body is the real D-07 skeleton. No `#[cfg(feature="gpu")]` on the new modules. `mod.rs` is the only registration site touched.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Author device_dataset_parity.rs (ODL-03) + the D-04 sparse-column synthesizer</name>
  <files>crates/lgbm-compute/tests/device_dataset_parity.rs</files>
  <read_first>
    - crates/lgbm-compute/tests/cuda_random_parity.rs:1-70 (the host-vs-device anchor shape: `cpu_client()`, exact-bit `assert_eq!`, `f32::to_bits()`, the inline reference recurrence convention)
    - crates/lgbm-compute/src/lib.rs:51-188 (`BinColumn` — the host binned-value oracle: `new`, `bin`, `to_u32_vec`)
    - .planning/phases/15-…/15-RESEARCH.md "Validation Architecture" → Phase Requirements → Test Map (the four ODL-03 test names) and Pattern 2/3 (the `max_num_bin_per_partition = shared_hist_size/2` budget, the `partition_hist_start` subtraction)
    - docs/cuda-kernel-design.md §13 (DivideCUDAFeatureGroups, GetSparseDataPartitioned semantics)
  </read_first>
  <action>
    Create `tests/device_dataset_parity.rs` with `use lgbm_compute::runtime::cpu_client;` and the four ODL-03 parity tests named EXACTLY (the validation map keys on these):
    - `dense_bin_parity_all_widths`: build host `BinColumn`s at each width (a u8 column with num_bin<=256, a u16 with 256<num_bin<=65536, a u32 with num_bin>65536), run `get_dense_data_partitioned` re-lay + read-back, assert every (row, column) device bin equals the host `BinColumn::bin(row)` exactly.
    - `feature_partition_layout`: call `divide_cuda_feature_groups` on a hand-chosen `num_bin_per_column` set with `shared_hist_size = 6144` (budget = 3072), assert the partition count, `feature_partition_column_index_offsets`, `column_hist_offsets` (partition-local), and `partition_hist_offsets` against hand-computed expectations; include a column whose own bin count exceeds 3072 and assert `num_large_bin_partition() == 1` (Pitfall 1).
    - `sparse_relay_3x3_and_partition_local`: drive `get_sparse_data_partitioned` over each `(bit_type, row_ptr_type)` cell reachable from the synthesizer; assert (a) each `row_ptr_type{16,32,64}` cell is exercised, (b) a 2-partition case re-lays each bin to `global_bin - partition_hist_offsets[partition]` (Pitfall 4), (c) an unsupported width returns `ComputeError` (not a silent widen — Pitfall 2).
    - `column_store_parity`: build `CudaColumnData`, read back per-column binned values + assert numeric `ColumnFeatureMeta` (`bit_type`, `feature_min_bin`, `feature_max_bin`, `offset`, `most_freq_bin`, `default_bin`) match the host.

    Add a `mod synth { … }` (or top-of-file fns) D-04 sparse-column synthesizer: generate sparse columns whose nnz crosses 2^16 and 2^32 so each `row_ptr_type` width is forced, plus a column over the `shared_hist_size/2` budget for the large-bin spill. The synthesizer's host binned values ARE the anchor (the v1.0 Rust binning is already C++-bit-exact — same reasoning as Phase-14 D-04). Every assert runs on `cpu_client()` (D-08, never GPU-vs-GPU). Tests will panic at the `todo!()` stub bodies until Plans 15-02/15-03 land — that is expected this wave.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test device_dataset_parity --no-run</automated>
  </verify>
  <acceptance_criteria>
    `device_dataset_parity.rs` compiles and links (`--no-run` exits 0). The four named tests exist and the sparse synthesizer is present and forces all three `row_ptr_type` widths + the large-bin spill. Every assert uses `cpu_client()` against host `BinColumn` truth. No `#[ignore]` (tests are allowed to fail at `todo!()` this wave; they must not be silently skipped).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Author copy_subrow_parity.rs (ODL-04) with the inline host bagging reference</name>
  <files>crates/lgbm-compute/tests/copy_subrow_parity.rs</files>
  <read_first>
    - crates/lgbm-compute/tests/cuda_random_parity.rs:1-70 (the inline `ref_rand_int32` reference precedent — reproduce host truth inline, do NOT depend on a higher crate)
    - crates/lgbm-boosting/src/sample_strategy.rs:45-46, 142-165, 239-285, 370-382 (READ ONLY for the algorithm: `BAGGING_RAND_BLOCK=1024`, per-block `Random::new(bagging_seed + block)` constructed once, the `next_float() as f64 < bagging_fraction` route, the `[in-bag asc] ++ [OOB desc]` layout with the OOB-tail reverse, `bag_data_indices()`)
    - crates/lgbm-compute/src/lib.rs:134 (`BinColumn::gather(rows)` — the host gather oracle)
    - crates/lgbm-core/src/random.rs (`Random::new`, `next_float`) — the shared host RNG (already a lgbm-compute dep)
  </read_first>
  <action>
    Create `tests/copy_subrow_parity.rs` with `use lgbm_compute::runtime::cpu_client;` and `use lgbm_core::random::Random;`. CRITICAL: do NOT add or import `lgbm-boosting` (it depends on `lgbm-compute`; a dev-dep back is a crate cycle). Reproduce the host bagging reference INLINE as a local `fn host_bag_data_indices(num_data: i32, bagging_seed: i32, bagging_fraction: f64) -> (Vec<i32>, i32)` that mirrors `sample_strategy.rs:239-285` exactly: per-block `Random::new(bagging_seed + block)` (block = i/1024), `draw = next_float() as f64`, route `draw < bagging_fraction` → in-bag (left, ascending) else OOB (right), then reverse the OOB tail. This inline fn IS the anchor (it uses the same `lgbm_core::random::Random` recurrence the host strategy uses — the `cuda_random_parity.rs::ref_rand_int32` precedent).

    Write the three ODL-04 tests named EXACTLY:
    - `gather_matches_host_all_widths`: for each `BinColumn` width, pick a `used_indices` set, run `copy_subrow_on`, assert the compacted device subset equals `BinColumn::gather(used_indices)` element-for-element; cover dense and the row-major span case.
    - `bagging_draw_matches_host`: choose `num_data > 1024` (spans >=2 blocks — the block-structure parity surface, D-05), run `bagging_draw_on`, and assert the device draw stream + host-side route decisions are bit-for-bit equal to `host_bag_data_indices(...)` (NextFloat via `f32::to_bits()`, route via exact set+order). Promote the f32 draw to f64 before the `< bagging_fraction` compare (Pitfall 6).
    - `gather_arbitrary_indices`: an arbitrary GOSS-shaped (non-contiguous, unsorted) host-supplied index set gathers correctly (D-06).

    Also assert the V5 boundary (T-15-IDX): a `used_indices` entry `>= num_data` makes `copy_subrow_on` return `Err(ComputeError::…)` BEFORE any launch. Every assert runs on `cpu_client()` (D-08). Tests panic at `todo!()` until Plan 15-04 lands — expected this wave.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test copy_subrow_parity --no-run && ! grep -q "lgbm.boosting\|lgbm_boosting" crates/lgbm-compute/tests/copy_subrow_parity.rs</automated>
  </verify>
  <acceptance_criteria>
    `copy_subrow_parity.rs` compiles and links (`--no-run` exits 0). The inline `host_bag_data_indices` reference is present and there is NO reference to `lgbm-boosting`/`lgbm_boosting` (grep returns nothing → the `!`/negation passes). The three named tests + the `bagging_draw_matches_host` test spans >1024 rows. The OOB-index rejection assert is present.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → kernel launchers | scaffolding only; the real launch-boundary validation lands in Waves 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-SCAFFOLD | Tampering | new module wiring | mitigate | Modules registered ungated but inert (`todo!()` bodies / trivial kernel); no behavior reachable until Waves 1. The `copy_subrow_parity.rs` negative-grep guards against the lgbm-boosting crate-cycle import. |

No package-manager installs in this phase → no supply-chain (T-15-SC) checkpoint required (RESEARCH "Package Legitimacy Audit: Not applicable").
</threat_model>

<verification>
- `cargo build -p lgbm-compute` green (3 modules registered, stubs compile).
- Both test files compile and link (`--no-run`).
- No `lgbm-boosting` import in `lgbm-compute` (no crate cycle).
- Default path untouched: no edits outside the 6 listed files.
</verification>

<success_criteria>
The Phase-15 skeleton compiles; the two Nyquist parity test files exist, compile, and run
(failing at `todo!()` until Waves 1); the three Wave-1 plans can each fill exactly one
owned source file with no shared-file contention.
</success_criteria>

<output>
Create `.planning/phases/15-on-device-device-dataset-row-subset-gather/15-01-SUMMARY.md` when done
</output>
