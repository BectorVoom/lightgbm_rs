---
phase: 15-on-device-device-dataset-row-subset-gather
plan: 02
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - crates/lgbm-compute/src/kernels/row_data.rs
autonomous: true
requirements: [ODL-03]

must_haves:
  truths:
    - "The §13 row store reproduces host binned values exactly per-column across all 3 bit widths (dense)"
    - "max_num_bin_per_partition = shared_hist_size / 2 (grad+hess pair per entry) — ported literally"
    - "DivideCUDAFeatureGroups partition count + offset tables match hand-computed expectation; an over-budget column becomes its own large-bin partition (NumLargeBinPartition += 1)"
    - "GetSparseDataPartitioned subtracts partition_hist_start so each partition's bins are partition-local"
    - "the 3x3 InitSparseData<BIN,PTR> dispatch reaches all 9 cells; an unsupported width returns ComputeError (never a silent widen)"
  artifacts:
    - path: "crates/lgbm-compute/src/kernels/row_data.rs"
      provides: "§13 CUDARowData: DivideCUDAFeatureGroups + offset tables + GetDense/SparseDataPartitioned + bit_type/row_ptr_bit_type/NumLargeBinPartition accessors, uploaded once"
      min_lines: 200
      contains: "divide_cuda_feature_groups"
  key_links:
    - from: "crates/lgbm-compute/src/kernels/row_data.rs"
      to: "crates/lgbm-compute/tests/device_dataset_parity.rs"
      via: "dense_bin_parity_all_widths / feature_partition_layout / sparse_relay_3x3_and_partition_local pass against host BinColumn truth"
      pattern: "get_dense_data_partitioned|get_sparse_data_partitioned"
    - from: "crates/lgbm-compute/src/kernels/row_data.rs"
      to: "crates/lgbm-compute/src/lib.rs (BinColumn)"
      via: "host binned-value source + native-width upload (create_from_slice(<w>::as_bytes))"
      pattern: "BinColumn::U8|BinColumn::U16|BinColumn::U32"
  prohibitions:
    - "NEVER reuse the feature-major resident_bins buffer (lib.rs:2360, f*num_data+row) for the §13 row store — §13 is row-major partition-local (data[idx*ncol+tx]). Build a NEW buffer (Pitfall 3)."
    - "NEVER silently widen an unsupported {bit_type}x{row_ptr_type} width — return ComputeError (the Log::Fatal analog, Pitfall 2)."
    - "NEVER compare two GPU paths — anchor every value to the cpu f64 fold / host BinColumn (D-08, def-f8u-01)."
    - "NEVER re-upload the dataset per tree — upload once (D-09); allocate device buffers once in a new()-style constructor (split_info.rs:267 template)."
---

<objective>
Implement the §13 `CUDARowData` row + feature-partition store in `row_data.rs` (ODL-03):
the host-side `DivideCUDAFeatureGroups` layout math + offset tables, the dense row-major
partition-local re-lay (`GetDenseDataPartitioned`) across all three bin widths, the sparse
CSR partition-local re-lay (`GetSparseDataPartitioned`) across the 3×3
`{bit_type}×{row_ptr_type}` dispatch, and the width-dispatched read-back accessors — all
uploaded once and anchored bit-exact to host `BinColumn` values.

Purpose: This is the new load-bearing artifact of the phase — the direct Phase-16 histogram
input layout (`blockIdx.x` = partition, `threadIdx.x` = column). The layout math is pure
host `usize` arithmetic with zero parity surface beyond producing correct offsets (§17), so
it is unit-testable against hand-computed expectations.
Output: `row_data.rs` filled; the three ODL-03 row-store tests in `device_dataset_parity.rs`
green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-RESEARCH.md
@.planning/phases/15-on-device-device-dataset-row-subset-gather/15-PATTERNS.md
@crates/lgbm-compute/src/kernels/row_data.rs
@crates/lgbm-compute/tests/device_dataset_parity.rs
</context>

## Artifacts this plan produces
`row_data.rs` symbols: `CudaRowData<R>`, `FeaturePartitionLayout`,
`divide_cuda_feature_groups`, `get_dense_data_partitioned`, `get_sparse_data_partitioned`,
accessors `bit_type` / `row_ptr_bit_type` / `num_large_bin_partition` / the offset-table
getters (`feature_partition_column_index_offsets`, `column_hist_offsets`,
`partition_hist_offsets`, `max_num_column_per_partition`, `num_feature_partitions`,
`shared_hist_size`). All host-side `usize` layout math + once-uploaded device buffers.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DivideCUDAFeatureGroups + offset tables + accessors</name>
  <files>crates/lgbm-compute/src/kernels/row_data.rs</files>
  <read_first>
    - docs/cuda-kernel-design.md §13 (the accessor table + DivideCUDAFeatureGroups packing rule; §7.0 for the shared_hist_size constants)
    - .planning/phases/15-…/15-RESEARCH.md Pattern 2 (the parity-load-bearing formula + the offset-table meanings) and Assumptions Log A1 (DP_SHARED_HIST_SIZE=6144)
    - crates/lgbm-compute/src/error.rs:19-72 (ComputeError::Runtime for the overflow/width errors)
  </read_first>
  <behavior>
    - `divide_cuda_feature_groups(&[num_bin per column], shared_hist_size=6144)` → budget `max_num_bin_per_partition = 6144/2 = 3072`; packs columns in order until adding a column would exceed the budget, then opens a new partition.
    - A single column with bin count > 3072 becomes its OWN partition and increments `num_large_bin_partition`.
    - `feature_partition_column_index_offsets` is a prefix array: partition i owns columns `[off[i], off[i+1])`.
    - `column_hist_offsets[c]` is partition-LOCAL (resets to 0 at each partition start); `partition_hist_offsets[i]` is the global bin offset where partition i begins.
    - `num_feature_partitions` and `max_num_column_per_partition` match hand-computed values.
  </behavior>
  <action>
    Implement `divide_cuda_feature_groups` per D-02: `max_num_bin_per_partition = shared_hist_size / 2` (each histogram entry is a grad+hess pair — the `/2` is the parity-load-bearing detail, Pitfall 1). Walk the per-column bin counts in order, accumulating into the current partition until the next column would exceed the budget; then close the partition and start a new one. A column whose own bin count exceeds the budget is its own large-bin partition (`num_large_bin_partition += 1`). Build all five offset/size tables exactly as the §13 accessor table defines them (see `<behavior>`). Add the `bit_type` / `row_ptr_bit_type` / `num_large_bin_partition` + offset-table getter accessors. Use `shared_hist_size = 6144` as the DP constant (A1; leave a doc-comment noting it is the design-doc value, no parity impact per §17 — only grouping). Overflow-check every length product with `checked_mul` at the boundary, returning `ComputeError::Runtime` (T-15-PART). This task is host `usize` math only — no device launch.
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test device_dataset_parity feature_partition_layout</automated>
  </verify>
  <acceptance_criteria>
    `feature_partition_layout` passes: partition count, `feature_partition_column_index_offsets`, partition-local `column_hist_offsets`, and `partition_hist_offsets` equal the hand-computed expectations; the over-budget column yields `num_large_bin_partition() == 1`. `cargo clippy -p lgbm-compute --tests` clean on the touched file.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GetDenseDataPartitioned — row-major partition-local buffer + upload-once</name>
  <files>crates/lgbm-compute/src/kernels/row_data.rs</files>
  <read_first>
    - crates/lgbm-compute/src/lib.rs:2360-2395 (`upload_resident_bins`: narrowest-uniform-width concat + `client.create_from_slice(<w>::as_bytes(&concat))` native-width upload — REUSE the upload shape, NOT the feature-major layout)
    - crates/lgbm-compute/src/kernels/split_info.rs:267-300 (the single-`client.empty`-in-`new` alloc-once template, D-09)
    - crates/lgbm-compute/src/kernels/partition.rs:360-400 (`launch_native!` width-match for the read-back accessor)
  </read_first>
  <behavior>
    - For a host feature set, `get_dense_data_partitioned` produces a NEW row-major partition-local buffer indexed `data[row*ncol + tx]` (NOT feature-major `f*num_data+row`).
    - Read-back of cell (row, partition-column) equals the host `BinColumn::bin(row)` for that column, across u8/u16/u32 store widths.
    - The buffer is uploaded ONCE (cached), not per call.
  </behavior>
  <action>
    Implement `get_dense_data_partitioned`: assemble the row-major partition-local dense buffer at the store `bit_type` width (the max column width in the partition; widen narrower columns losslessly into the store width). Upload once via `create_from_slice(<w>::as_bytes(&buf))` at native width (the `upload_resident_bins` shape, but ROW-MAJOR — Pitfall 3: do NOT reuse the feature-major `resident_bins` buffer). Cache the device handle in the `CudaRowData` struct (allocate-once, D-09 — the `split_info.rs` template; no per-call `client.empty` in a loop). Provide a width-dispatched read-back accessor (generic-over-`Int` via the `launch_native!`/`u32::cast_from` idiom, or a direct `read_one_unchecked` + per-width decode) used by the parity test. All `cubecl` unsafe confined to the launcher with a SAFETY comment proving handle sizing + lifetime (CMP-01).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test device_dataset_parity dense_bin_parity_all_widths</automated>
  </verify>
  <acceptance_criteria>
    `dense_bin_parity_all_widths` passes for all three widths: every read-back cell equals the host `BinColumn::bin(row)`. The buffer is row-major partition-local (verified by the test's column/row indexing). Upload happens once (no `client.empty`/`create_from_slice` inside a per-call loop). clippy clean.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GetSparseDataPartitioned — 3×3 dispatch + partition_hist_start subtraction</name>
  <files>crates/lgbm-compute/src/kernels/row_data.rs</files>
  <read_first>
    - docs/cuda-kernel-design.md §13 (`GetSparseDataPartitioned`, the `InitSparseData<BIN_TYPE,PTR_TYPE>` 3×3, `row_ptr_type` selection by max nnz)
    - .planning/phases/15-…/15-RESEARCH.md Pattern 3 (the partition-local CSR re-lay crux) + Pitfall 2 & 4
    - crates/lgbm-compute/src/kernels/partition.rs:360-400 (nested width-match precedent for the 3×3)
  </read_first>
  <behavior>
    - Each partition gets its own CSR (`row_ptr` + bin values); each bin index is made partition-local by subtracting `partition_hist_start` (= `partition_hist_offsets[i]`).
    - `row_ptr_type` is chosen by actual max nnz: `<= 2^16 → u16`, `<= 2^32 → u32`, else `u64` (narrowest fitting).
    - All 9 `{bit_type 8/16/32} × {row_ptr_type 16/32/64}` cells are reachable; a width outside the 3×3 returns `ComputeError`.
  </behavior>
  <action>
    Implement `get_sparse_data_partitioned`: build per-partition CSR (`row_ptr` at the narrowest fitting `row_ptr_type` for that partition's max nnz; assert the chosen width fits BEFORE upload — T-15-PTR) and SUBTRACT `partition_hist_offsets[i]` from each stored bin so the partition's bins are partition-local (Pitfall 4 — the single most error-prone line; assert the re-lay'd bin == `global_bin - partition_hist_start` in the 2-partition test). Dispatch the `(bit_type, row_ptr_type)` 3×3 via a nested host `match` (the `launch_native!` precedent); any width outside `{8,16,32}×{16,32,64}` returns `ComputeError::Runtime` — NEVER a catch-all silent widen (Pitfall 2). `checked_mul` on every nnz×width length sizing (T-15-PART). All unsafe confined to the launcher (CMP-01).
  </action>
  <verify>
    <automated>cargo test -p lgbm-compute --test device_dataset_parity sparse_relay_3x3_and_partition_local</automated>
  </verify>
  <acceptance_criteria>
    `sparse_relay_3x3_and_partition_local` passes: each `row_ptr_type{16,32,64}` cell is exercised (the D-04 synthesizer forces them), the 2-partition re-lay'd bins equal `global_bin - partition_hist_offsets[partition]`, and an unsupported width returns `ComputeError`. clippy clean.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host layout math → device buffer sizing | length products (`nnz*width`, `rows*ncol`) must not overflow before alloc |
| host bins → device upload | `row_ptr_type` width must fit the actual max nnz, else truncated offsets → OOB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-PART | Denial of Service / memory corruption | `divide_cuda_feature_groups` / re-lay length sizing | mitigate | `checked_mul` on every length product at the V5 boundary before any `client.empty`/`create_from_slice` (the `random.rs:146` precedent). |
| T-15-PTR | Tampering | sparse `row_ptr` width selection | mitigate | Pick `row_ptr_type` by actual max nnz; assert the chosen width fits before upload; unsupported width → `ComputeError` (no silent widen, Pitfall 2). |
| T-15-CMP | Memory corruption | `from_raw_parts` in the read-back launcher | mitigate | Confine all cubecl unsafe to the launcher fn with a SAFETY comment proving handle sizing + lifetime (CMP-01). |
</threat_model>

<verification>
- `cargo test -p lgbm-compute --test device_dataset_parity` (the 3 row-store tests) green.
- `cargo clippy -p lgbm-compute --tests` clean.
- Buffer is row-major partition-local (NOT the feature-major resident buffer).
- Upload-once / alloc-once (no per-call device alloc in a loop).
</verification>

<success_criteria>
The §13 row store reproduces host binned values exactly across all three widths, the
partition layout + offset tables match hand-computed expectations (including the large-bin
spill), and the sparse 3×3 re-lay is partition-local with every width-cell reachable and
unsupported widths rejected.
</success_criteria>

<output>
Create `.planning/phases/15-on-device-device-dataset-row-subset-gather/15-02-SUMMARY.md` when done
</output>
