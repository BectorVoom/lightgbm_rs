---
phase: 07-parity-completing-variants
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-core/src/config/alias.rs
  - crates/lgbm-objective/src/regression.rs
  - crates/lgbm-objective/src/lib.rs
  - crates/lgbm-boosting/src/objective.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/boosting_parity.rs
autonomous: false
requirements: ["OBJ-04"]
must_haves:
  truths:
    - "A user can select huber/fair/quantile/mape objectives via the builder and train a model whose grad/hess, per-iter scores, and final model match real lib_lightgbm 4.6"
    - "huber clips the gradient to ±alpha; quantile/mape are is_renew_tree_output==true and reuse the percentile renewal"
    - "quantile and mape renew on bagged subsets only under the D-05-settled posture from 07-01"
  artifacts:
    - path: "crates/lgbm-objective/src/regression.rs"
      provides: "huber/fair/quantile/mape grad/hess + boost_from_score + renew, mirroring regression_objective.hpp"
      contains: "Huber"
    - path: "crates/oracle-harness/tests/boosting_parity.rs"
      provides: "layered L1-L5 parity cells for the 4 objectives across {bag×es×bfa} + objective-specific params"
  key_links:
    - from: "crates/lgbm/src/builder.rs"
      to: "crates/lgbm-objective/src/regression.rs"
      via: "objective string routed through Config::from_params -> Objective::parse"
      pattern: "huber|fair|quantile|mape"
    - from: "crates/lgbm-boosting/src/objective.rs"
      to: "crates/lgbm-objective::Objective"
      via: "BoostObjective variant dispatch"
      pattern: "BoostObjective"
---

<objective>
W1 (OBJ-04, regression family A): add the L1-like / clip-style regression objectives `huber`, `fair`, `quantile`, `mape` as thin additions on the proven Phase-6 objective seam. Each is a vertical slice: Config param (+CHECK) → factory wiring → faithful C++-ported grad/hess/renew → builder setter → real-binary layered golden.

Purpose: lowest-structural-risk breadth on the locked loop (D-02 step 2). `huber`/`fair` are clip/robust regressions; `quantile`/`mape` are renew-tree-output objectives that reuse the Phase-6 percentile machinery — their bagged-subset renew cells inherit the 07-01 D-05 decision.
Output: 4 new objectives wired end-to-end with layered real-lib_lightgbm-4.6 goldens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config params + huber/fair/quantile/mape objective math + factory wiring</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-core/src/config/alias.rs, crates/lgbm-objective/src/regression.rs, crates/lgbm-objective/src/lib.rs, crates/lgbm-boosting/src/objective.rs</files>
  <read_first>
    - crates/lgbm-objective/src/regression.rs (the enum Objective + parse + flag methods regression.rs:40-133; get_gradients V5 gate + f64-compute/single-f32-cast regression.rs:174-221; BoostFromScore ordered fold regression.rs:229-252; the existing RegressionL1 renew path)
    - crates/lgbm-objective/src/percentile.rs (PercentileFun/WeightedPercentileFun, already ported Phase-6 for L1 — quantile/mape reuse)
    - crates/lgbm-boosting/src/objective.rs (BoostObjective enum — add the 4 variants)
    - crates/lgbm-core/src/config/set.rs (typed getters + try_finalize block set.rs:440-483; re-grep for alpha/fair_c — per RESEARCH A2 do NOT trust the present/ADD table)
    - crates/lgbm-core/src/config/alias.rs (resolve_alias :192)
    - C++ refs (authoritative): LightGBM/src/objective/regression_objective.hpp:293 (huber, grad clipped ±alpha), :351 (fair, fair_c), :481 (quantile, alpha, RenewTreeOutput percentile), :579 (mape, subclasses L1, label-magnitude weighting); percentile macros :18-88; LightGBM/include/LightGBM/config.h:958 (alpha — SHARED huber δ + quantile α), :963 (fair_c)
  </read_first>
  <behavior>
    - huber: gradient = clamp(score-label, -alpha, +alpha) in f64 then cast f32; hess = 1.0 (constant); is_renew_tree_output==false
    - fair: grad = c*residual/(|residual|+c), hess = c*c/(|residual|+c)^2 (per regression_objective.hpp:351); fair_c default 1.0, CHECK >0
    - quantile: grad = alpha-1 if residual<0 else alpha (sign-style); is_renew_tree_output==true; renew uses WeightedPercentile/Percentile at alpha
    - mape: subclasses L1; grad = sign(residual)/max(|label|, 1) weighting; is_renew_tree_output==true
    - parse aliases: huber, fair, quantile, mape (+ any config_auto.cpp aliases); unknown name -> typed Unsupported error
  </behavior>
  <action>
    Re-grep `set.rs` for `alpha`/`fair_c` FIRST. Add any missing params with the EXACT name/default/CHECK from config.h: `alpha` (default 0.9, CHECK >0, used by huber δ AND quantile α), `fair_c` (default 1.0, CHECK >0). Add aliases via alias.rs if config_auto.cpp defines any. Add `Huber`/`Fair`/`Quantile`/`Mape` to `enum Objective` (regression.rs) with a parse alias arm and arms in every flag method (`is_constant_hessian` true for huber/fair, false for quantile/mape; `is_renew_tree_output` true for quantile/mape like RegressionL1, false for huber/fair). Port grad/hess per the `<behavior>` block from regression_objective.hpp file:line above — arithmetic in f64, narrow to f32 exactly once at the write; use `lgbm_core::types::K_EPSILON`, never a fresh literal. quantile/mape `renew_leaf_output` reuses `percentile.rs`. Add the 4 `BoostObjective` variants + dispatch in lgbm-boosting/src/objective.rs. Re-export in lgbm-objective/src/lib.rs.
  </action>
  <verify>
    <automated>cargo test -p lgbm-objective && cargo test -p lgbm-core</automated>
  </verify>
  <acceptance_criteria>
    - `Objective::Huber`, `Objective::Fair`, `Objective::Quantile`, `Objective::Mape` exist with parse arms (grep regression.rs)
    - `is_renew_tree_output()` returns true for Quantile/Mape and false for Huber/Fair (unit test)
    - `alpha` and `fair_c` resolve with config.h defaults (0.9 / 1.0) and reject out-of-range via typed Result (unit test asserts CHECK)
    - huber grad is clamped to ±alpha (unit test: residual far beyond alpha yields exactly ±alpha as f32)
    - `cargo test -p lgbm-objective` and `cargo test -p lgbm-core` GREEN
  </acceptance_criteria>
  <done>The 4 objectives compute faithful grad/hess/renew, are factory-wired, and unit tests prove the clip + renew flags + CHECK constraints.</done>
</task>

<task type="auto">
  <name>Task 2: Builder setters + layered oracle capture subcommand + capture-gated parity cells</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - crates/lgbm/src/builder.rs (objective setter builder.rs:52-55; reg_sqrt builder.rs:121-124; bagging_fraction builder.rs:152-156 — add alpha/fair_c setters in this shape)
    - xtask/src/main.rs (boosting_oracle_capture registered :115; version-pin :84-97)
    - crates/oracle-harness/tests/boosting_parity.rs (cell_builder cross-product :44-176; assert_model_and_pred/assert_scores/assert_gradients :199-301; read_golden skip-pass :316-330)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Per-Subsystem Oracle Axis Matrix → Objectives" (huber 8 loop cells × alpha∈{0.9,0.5}; fair 8 × fair_c∈{1.0,2.0}; quantile 8 × alpha∈{0.9,0.1}, bag cells gate on D-05; mape 8, bag cells gate on D-05)
  </read_first>
  <action>
    Add builder setters `alpha(f64)` and `fair_c(f64)` mirroring builder.rs:121-124 (insert into self.params). Extend the boosting oracle capture xtask to emit layered L1-L5 goldens (per-row grad/hess, per-iter scores, per-round metric, final model text, predict) for huber/fair/quantile/mape across `{bag on/off} × {es on/off} × {bfa on/off}` plus the objective-specific param axis from the matrix; version-pin 4.6.0. Add capture-gated parity cells in boosting_parity.rs using the existing cell_builder + layered asserts; quantile/mape BAGGED cells must reference the 07-01 D-05 posture (assert parity if faithful-fix, or the bounded-cap if bounded-divergence). Document any collapsed cell as provably byte-identical (never silent truncation). Tests skip-pass until the wheel capture (Task 3 of 07-01 covers the install; per-subsystem capture is Task 3 here).
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test boosting_parity huber</automated>
  </verify>
  <acceptance_criteria>
    - `builder.alpha(..)` and `builder.fair_c(..)` exist and route into Config (unit/integration test)
    - boosting_parity.rs has cells `huber_*`, `fair_*`, `quantile_*`, `mape_*` covering {bag×es×bfa} + the objective param axis; each skip-passes cleanly absent the golden
    - quantile/mape bagged cells cite the 07-01 D-05 decision in a comment and assert accordingly
    - any collapsed matrix cell has an in-code comment proving byte-identity
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>Builder setters exist, the capture emitter covers the full relevant axis matrix, and capture-gated cells skip-pass; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Capture the huber/fair/quantile/mape real-binary goldens</name>
  <what-built>The objective math (Task 1), builder setters + capture emitter + capture-gated parity cells (Task 2). Capture is human-gated because `lightgbm==4.6.0` is not installed (RESEARCH A5), the Phase-5/6 posture.</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed (`pip install lightgbm==4.6.0`).
    2. Run the per-objective capture subcommand for huber/fair/quantile/mape; confirm it version-asserts 4.6.0 and writes fixtures under `crates/oracle-harness/tests/fixtures/`.
    3. Re-run to confirm byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test boosting_parity huber fair quantile mape` and confirm the cells flip from skip to GREEN (bit-exact on CPU f64-fold where the algorithm permits; ORACLE_TOL where renew/percentile horizon applies).
    5. Confirm quantile/mape bagged cells behave per the 07-01 D-05 decision.
  </how-to-verify>
  <resume-signal>Type "approved" once captures are committed and the 4 objective cells are green, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline numerical objectives over developer-supplied config + binned dataset. No network/auth/session/PII. Inputs cross the crate boundary as typed slices.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering→NaN | objective get_gradients with malformed length / out-of-range params | mitigate | V5 length-validation gate before any per-row write (regression.rs:181-199 pattern); alpha/fair_c CHECK >0 typed-reject mirroring C++ Log::Fatal |
| T-07-02-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask before emitting; blocking-human checkpoint; LightGBM/ never git-added |

No exp/log objective here (those are 07-03), so no LabelOutOfRange guard needed in this slice.
</threat_model>

<verification>
- `cargo test -p lgbm-objective`, `cargo test -p lgbm-core` GREEN
- `cargo test --workspace` GREEN (parity cells skip-pass until capture)
- After capture: huber/fair/quantile/mape cells green, byte-idempotent regen
</verification>

<success_criteria>
huber/fair/quantile/mape train via the builder and match real lib_lightgbm 4.6 across the relevant {bag×es×bfa}+param axes; quantile/mape bagged renew respects the 07-01 D-05 posture (OBJ-04 partial — exp/log objectives in 07-03).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-02)

- `Objective::Huber`, `Objective::Fair`, `Objective::Quantile`, `Objective::Mape` enum arms + parse arms (lgbm-objective/src/regression.rs)
- `BoostObjective` variants for the 4 objectives (lgbm-boosting/src/objective.rs)
- Config params: `alpha` (if not present), `fair_c` (+ any aliases) in lgbm-core/src/config/set.rs / alias.rs
- Builder setters `alpha`, `fair_c` (crates/lgbm/src/builder.rs)
- xtask capture subcommand cells for huber/fair/quantile/mape
- boosting_parity.rs cells: `huber_*`, `fair_*`, `quantile_*`, `mape_*` + fixtures under `tests/fixtures/`
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-02-SUMMARY.md` when done
</output>
