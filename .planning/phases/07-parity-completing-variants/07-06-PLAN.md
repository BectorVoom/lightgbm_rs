---
phase: 07-parity-completing-variants
plan: 06
type: execute
wave: 6
depends_on: ["07-05"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm-model/src/ensemble.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/boosting_parity.rs
  - crates/oracle-harness/tests/fixtures/dart/.gitkeep
autonomous: false
requirements: ["BST-05"]
must_haves:
  truths:
    - "A user can select boosting=dart and train a model whose dropped trees per iter, normalized tree weights, and predictions match real lib_lightgbm 4.6"
    - "DART drop selection uses a single advancing Random(drop_seed) with the exact draw order: draw #0 skip_drop, then per-tree drop_rate*tree_weight*inv_avg (or drop_rate if uniform_drop)"
    - "All 4 normalize branches (uniform_drop × xgboost_dart_mode) are covered"
  artifacts:
    - path: "crates/lgbm-boosting/src/gbdt.rs"
      provides: "BoostingVariant::Dart branch + DroppingTrees + Normalize, mirroring dart.hpp"
      contains: "Dart"
    - path: "crates/oracle-harness/tests/fixtures/dart/.gitkeep"
      provides: "dart RNG-replay + parity fixture dir"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs (BoostingVariant::Dart)"
      to: "crates/lgbm-model/src/ensemble.rs (tree-weight normalize on predict)"
      via: "DART predict-side normalize of kept+dropped tree weights"
      pattern: "tree_weight|normalize"
---

<objective>
W5 (BST-05): add DART boosting as a `BoostingVariant::Dart` enum field on `Gbdt` (RESEARCH Pattern 1 — enum field, not trait objects). Vertical slice: Config params → DroppingTrees (drop_seed RNG, exact draw order) + Normalize (4 branches) → predict-side tree-weight normalize → builder → real-binary parity + RNG-replay drop golden.

Purpose: DART subclasses GBDT in C++ and overrides TrainOneIter/GetTrainingScore + tree-weight normalize; depends only on the locked spine. Ordered after 07-05 because both edit gbdt.rs/builder.rs/set.rs/boosting_parity.rs.
Output: DART wired end-to-end with the drop RNG-replay golden + all-4-normalize-branch parity cells.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DART params + BoostingVariant::Dart (DroppingTrees + Normalize) + drop RNG-replay golden</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-boosting/src/gbdt.rs, crates/oracle-harness/tests/boosting_parity.rs, crates/oracle-harness/tests/fixtures/dart/.gitkeep</files>
  <read_first>
    - crates/lgbm-boosting/src/gbdt.rs (Gbdt::train_one_iter :209+; constructor family new/with_objective/with_bagging :98-159; with_bagging chained-config :150-159 — add with_variant the same way)
    - crates/lgbm-boosting/src/sample_strategy.rs (the single-advancing-Random RNG-replay test shape :272-340; build-once discipline — DART uses one advancing Random(drop_seed))
    - crates/lgbm-core/src/config/set.rs (re-grep drop_rate/max_drop/skip_drop/xgboost_dart_mode/uniform_drop/drop_seed — RESEARCH marks several ADD(verify))
    - C++ refs (authoritative): LightGBM/src/boosting/dart.hpp:58-71 (TrainOneIter calls GBDT::TrainOneIter then Normalize then push tree_weight_), :78-86 (GetTrainingScore triggers DroppingTrees once/iter), :97-128 (DroppingTrees: random_for_drop_=Random(drop_seed); draw #0 NextFloat()<skip_drop; per-tree NextFloat()<drop_rate*tree_weight*inv_avg [uniform_drop=false] or <drop_rate [uniform]), :158-197 (Normalize 3-step with the xgboost_dart_mode branch — 4 branches via uniform_drop×xgboost_dart_mode); config.h drop_rate:440 max_drop:445 skip_drop:451 xgboost_dart_mode:455 uniform_drop:459 drop_seed:463
  </read_first>
  <behavior>
    - DroppingTrees: random_for_drop_ = Random(drop_seed) advanced (not re-seeded per iter); draw #0 NextFloat()<skip_drop -> skip all drops; else per-tree i in 0..iter: NextFloat()<drop_rate*tree_weight[i]*inv_avg (uniform_drop=false) or <drop_rate (uniform_drop=true) -> drop tree (num_init_iteration_+i); respect max_drop
    - Normalize 3-step per dart.hpp:158-197, 4 branches (uniform_drop × xgboost_dart_mode) — each rescales kept + newly-added tree weights differently
    - TrainOneIter: GBDT::TrainOneIter then Normalize then push tree_weight_
    - drop RNG-replay golden dart_drop_seed{S}_iter{N}.txt asserts dropped tree indices per iteration bit-exact
  </behavior>
  <action>
    Re-grep set.rs FIRST; add any missing params with exact config.h values: max_drop (50), skip_drop (0.5, [0,1]), xgboost_dart_mode (false), uniform_drop (false), drop_seed (4) — drop_rate (0.1) likely present. Add `BoostingVariant {Gbdt, Dart, Rf}` enum + a `variant` field on Gbdt (RESEARCH Pattern 1) with a `with_variant` chained ctor (:150-159 shape). Branch in train_one_iter/get_training_score for Dart: port DroppingTrees (single advancing Random(drop_seed), exact draw order) + Normalize (all 4 branches) per `<behavior>` from dart.hpp:97-197. Add the dart fixture dir + the drop RNG-replay golden `dart_drop_seed{S}_iter{N}.txt` asserting dropped tree indices per iter bit-exact.
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting && cargo test -p oracle-harness --test boosting_parity dart_drop</automated>
  </verify>
  <acceptance_criteria>
    - `BoostingVariant::Dart` field + branch exist; selected via boosting=dart (grep)
    - DroppingTrees uses a single advancing Random(drop_seed) with draw #0 = skip_drop, then per-tree draws (grep / unit test on draw order)
    - all 4 Normalize branches (uniform_drop×xgboost_dart_mode) are implemented (unit test exercising each combination)
    - max_drop/skip_drop/xgboost_dart_mode/uniform_drop/drop_seed resolve with config.h defaults (unit test)
    - the dart drop RNG-replay golden asserts dropped indices per iter bit-exact; skip-passes absent the fixture
    - `cargo test -p lgbm-boosting` GREEN
  </acceptance_criteria>
  <done>BoostingVariant::Dart faithfully drops+normalizes with the exact drop_seed draw order and all 4 normalize branches; drop RNG-replay golden in place.</done>
</task>

<task type="auto">
  <name>Task 2: Predict-side tree-weight normalize + builder + capture emitter + capture-gated DART parity cells</name>
  <files>crates/lgbm-model/src/ensemble.rs, crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - crates/lgbm-model/src/ensemble.rs (GbdtModel tree accumulation — DART weights each tree by tree_weight_ on predict; find the per-tree score accumulation site)
    - crates/lgbm/src/builder.rs (bool setters reg_sqrt/boost_from_average :108-124; chained setters :152-156)
    - xtask/src/main.rs (boosting_oracle_capture :115; version-pin :84-97)
    - crates/oracle-harness/tests/boosting_parity.rs (cell_builder :44-176; layered asserts :199-301; read_golden :316-330)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Oracle Axis Matrix → Boosting variants → DART" (drop_rate∈{0.1,0.3} × max_drop∈{50,2} × skip_drop∈{0.5,0.0} × uniform_drop{T,F} × xgboost_dart_mode{T,F}; × {bag on/off}; DART overrides EvalAndCheckEarlyStopping; the "all 4 normalize branches" note)
  </read_first>
  <action>
    Wire the DART per-tree weight into the predict-side accumulation in ensemble.rs so predictions apply the normalized tree_weight_ (mirror dart.hpp predict normalize). Add builder setters: `drop_rate`/`max_drop`/`skip_drop`/`uniform_drop(bool)`/`xgboost_dart_mode(bool)`/`drop_seed` + `boosting("dart")` selection. Extend the capture xtask to emit layered goldens for DART across the defining-param cross-product (ensuring all 4 normalize branches via uniform_drop×xgboost_dart_mode) × {bag}; version-pin 4.6.0. Add capture-gated `dart_*` parity cells. Document any collapsed cell. Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test boosting_parity dart</automated>
  </verify>
  <acceptance_criteria>
    - predict applies the normalized DART tree_weight_ (integration test: a dart model's predict matches sum of weighted tree outputs)
    - builder setters drop_rate/max_drop/skip_drop/uniform_drop/xgboost_dart_mode/drop_seed route into Config (test)
    - boosting_parity.rs has `dart_*` cells covering all 4 normalize branches × {bag}; each skip-passes absent the golden
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>DART predict-side normalize + builder setters + capture emitter cover all 4 normalize branches; cells skip-pass; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Capture the DART real-binary goldens (parity + drop RNG-replay)</name>
  <what-built>BoostingVariant::Dart drop+normalize + drop RNG-replay (Task 1) + predict-normalize/builder/capture/cells (Task 2). Human-gated capture (wheel absent).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the DART capture subcommand; confirm version-assert + fixtures (parity cells covering all 4 normalize branches + the drop RNG-replay golden) under `crates/oracle-harness/tests/fixtures/dart/`.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test boosting_parity dart` — confirm parity cells + drop RNG-replay flip to GREEN (dropped indices bit-exact; normalized weights/predictions within parity).
  </how-to-verify>
  <resume-signal>Type "approved" once captures are committed and DART cells + drop RNG-replay are green, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline boosting variant over developer config + the in-memory ensemble. No network/auth/session/PII. RNG is the deterministic FND-01 LCG.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06-01 | Tampering | drop_rate/skip_drop out of [0,1] | mitigate | config.h CHECK [0,1] surfaced as typed Result |
| T-07-06-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-boosting` GREEN
- `cargo test --workspace` GREEN (dart cells + drop RNG-replay skip-pass until capture)
- After capture: dart parity (all 4 normalize branches) + drop RNG-replay green; byte-idempotent
</verification>

<success_criteria>
DART (BST-05) trains via boosting=dart and matches real lib_lightgbm 4.6 across the defining params (all 4 normalize branches) × {bag}; the drop RNG-replay golden proves dropped tree indices bit-exact.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-06)

- `BoostingVariant {Gbdt, Dart, Rf}` enum + `variant` field on Gbdt + `with_variant` ctor (crates/lgbm-boosting/src/gbdt.rs)
- DART DroppingTrees + Normalize (4 branches) in the Dart branch
- Config params max_drop/skip_drop/xgboost_dart_mode/uniform_drop/drop_seed (lgbm-core/src/config/set.rs)
- predict-side DART tree-weight normalize (crates/lgbm-model/src/ensemble.rs)
- Builder setters drop_rate/max_drop/skip_drop/uniform_drop/xgboost_dart_mode/drop_seed (crates/lgbm/src/builder.rs)
- drop RNG-replay golden `dart_drop_seed{S}_iter{N}.txt` + `dart_*` parity cells + `tests/fixtures/dart/`
- xtask DART capture subcommand
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-06-SUMMARY.md` when done
</output>
