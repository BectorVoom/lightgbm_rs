---
phase: 07-parity-completing-variants
plan: 04
type: execute
wave: 4
depends_on: ["07-02", "07-03"]
files_modified:
  - crates/lgbm-metric/src/regression.rs
  - crates/lgbm-metric/src/xentropy.rs
  - crates/lgbm-metric/src/multiclass.rs
  - crates/lgbm-metric/src/lib.rs
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/metric_parity.rs
autonomous: false
requirements: ["MET-03"]
must_haves:
  truths:
    - "A user can request the extended regression/xentropy/multiclass metrics and get values matching real lib_lightgbm 4.6"
    - "Prob-space metrics call ObjectiveKind::convert_output before scoring (the transform is not re-ported)"
    - "Each new metric has the correct factor_to_bigger_better sign (-1 for losses, +1 for AP/auc_mu)"
  artifacts:
    - path: "crates/lgbm-metric/src/regression.rs"
      provides: "quantile/huber/fair/poisson/mape/gamma/gamma_deviance/tweedie metric arms"
      contains: "GammaDeviance"
    - path: "crates/oracle-harness/tests/metric_parity.rs"
      provides: "new metric parity test file with capture-gated cells per metric"
  key_links:
    - from: "crates/lgbm-metric/src/regression.rs"
      to: "crates/lgbm-model::ObjectiveKind::convert_output"
      via: "prob-space metric transform before eval"
      pattern: "convert_output"
---

<objective>
W3 (MET-03): add the extended metrics — regression/xentropy: `quantile`/`huber`/`fair`/`poisson`/`mape`/`gamma`/`gamma_deviance`/`tweedie`/`cross_entropy`/`cross_entropy_lambda`/`kullback_leibler`; multiclass: `multi_error`/`auc_mu`/`average_precision` — as thin additions on the Phase-6 `Metric::Eval` seam. (Ranking metrics ndcg/map are MET-04, in 07-09 with the ranking stack.)

Purpose: metrics need objectives to score against, so this runs after the objective waves (D-02). New `metric_parity.rs` file (a Wave-0 gap per VALIDATION).
Output: extended metrics wired behind the existing enum-dispatch + capture-gated parity in a new `metric_parity.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extended regression/xentropy/multiclass metric math + factory wiring</name>
  <files>crates/lgbm-metric/src/regression.rs, crates/lgbm-metric/src/xentropy.rs, crates/lgbm-metric/src/multiclass.rs, crates/lgbm-metric/src/lib.rs, crates/lgbm-core/src/config/set.rs</files>
  <read_first>
    - crates/lgbm-metric/src/regression.rs (enum Metric + parse + name + factor_to_bigger_better + eval ordered fold regression.rs:27-111; factor sign -1 for losses / +1 for AUC :65-69)
    - crates/lgbm-metric/src/binary.rs (BinaryMetric — xentropy metric models on this)
    - crates/lgbm-metric/src/multiclass.rs (MultiLogloss — multi_error/auc_mu/average_precision model on this)
    - crates/lgbm-metric/src/lib.rs (the convert_output call before prob-space metrics lib.rs:11-14 — do NOT re-port the transform)
    - C++ refs (authoritative): LightGBM/src/metric/regression_metric.hpp (quantile/huber/fair/poisson/mape/gamma/gamma_deviance/tweedie), LightGBM/src/metric/xentropy_metric.hpp (cross_entropy/cross_entropy_lambda/kullback_leibler), LightGBM/src/metric/multiclass_metric.hpp (multi_error/auc_mu/average_precision)
  </read_first>
  <behavior>
    - regression metrics (quantile/huber/fair/poisson/mape/gamma/gamma_deviance/tweedie): each is a LossOnPoint arm in the ordered f64 fold; factor_to_bigger_better = -1; eval = sum_loss/sum_weights (tweedie/poisson may need the deviance form per metric.hpp)
    - xentropy metrics (cross_entropy/cross_entropy_lambda/kullback_leibler): prob-space, call convert_output first; factor -1
    - multiclass: multi_error (argmax != label rate, factor -1); auc_mu (factor +1); average_precision (factor +1)
    - parse: alias arm per metric (multi-metric lists already supported by MET-02 infra); unknown -> typed Unsupported
  </behavior>
  <action>
    Re-grep set.rs for any metric-specific params (most reuse objective params alpha/fair_c/tweedie_variance_power already added in 07-02/07-03 — confirm they are visible to the metric layer). Add the regression metric arms to enum Metric (regression.rs): a variant + parse alias + name + factor_to_bigger_better + a LossOnPoint/AverageLoss arm per the `<behavior>` block, ported from regression_metric.hpp. Add xentropy metric arms (xentropy.rs) modeled on binary.rs, calling `lgbm_model::ObjectiveKind::convert_output` before scoring (lib.rs:11-14 pattern). Add multiclass arms (multiclass.rs). Re-export in lib.rs. Each loss uses the ordered sequential f64 fold (deterministic anchor) and K_EPSILON via types.
  </action>
  <verify>
    <automated>cargo test -p lgbm-metric</automated>
  </verify>
  <acceptance_criteria>
    - new Metric arms exist with parse aliases: quantile/huber/fair/poisson/mape/gamma/gamma_deviance/tweedie/cross_entropy/cross_entropy_lambda/kullback_leibler/multi_error/auc_mu/average_precision (grep)
    - `factor_to_bigger_better()` returns -1 for losses and +1 for auc_mu/average_precision (unit test)
    - prob-space metrics call convert_output before eval (grep: convert_output in xentropy.rs metric path)
    - eval uses the ordered f64 fold (no parallel reduction); K_EPSILON via types, no fresh literal
    - `cargo test -p lgbm-metric` GREEN
  </acceptance_criteria>
  <done>The extended metrics compute faithful losses behind the enum factory with correct bigger-better signs; unit tests prove parse + sign + transform routing.</done>
</task>

<task type="auto">
  <name>Task 2: metric builder routing + capture emitter + new metric_parity.rs (capture-gated)</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/metric_parity.rs</files>
  <read_first>
    - crates/lgbm/src/builder.rs (the existing metric routing through params; MET-02 multi-metric list wiring)
    - xtask/src/main.rs (boosting_oracle_capture :115; version-pin :84-97 — model a metric capture or reuse the boosting capture to also emit per-round metric values)
    - crates/oracle-harness/tests/boosting_parity.rs (the capture-gated read_golden skip-pass :316-330 and layered assert shape to mirror in the new file)
    - crates/oracle-harness/tests/comparator.rs (ORACLE_TOL, compare_within)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Phase Requirements → Test Map" (MET-03 → metric_parity new file)
  </read_first>
  <action>
    Create the NEW `crates/oracle-harness/tests/metric_parity.rs` (a VALIDATION Wave-0 gap), modeled on boosting_parity.rs's capture-gated read_golden + layered assert shape. Extend the capture xtask to emit per-round metric goldens for each new metric over a small corpus (train with a compatible objective, capture the real-binary metric value per round at %.17g). Add capture-gated cells per metric comparing the Rust `Metric::eval` to the captured value via `compare_within(ORACLE_TOL)` (bit-exact where the algorithm permits). Confirm builder metric-name routing accepts the new names (reuse MET-02 multi-metric list). Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test metric_parity</automated>
  </verify>
  <acceptance_criteria>
    - `crates/oracle-harness/tests/metric_parity.rs` exists with capture-gated cells per new metric, each skip-passing absent the golden
    - the builder accepts each new metric name into the multi-metric list (integration test)
    - capture emitter writes per-round metric fixtures (version-pinned 4.6.0)
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>metric_parity.rs exists, the capture emitter covers the new metrics, builder routes the names; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Capture the extended-metric real-binary goldens</name>
  <what-built>Extended metric math (Task 1) + metric_parity.rs + capture emitter (Task 2). Human-gated capture (wheel absent, RESEARCH A5).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the metric capture subcommand; confirm version-assert + per-round metric fixtures written.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test metric_parity` — confirm cells flip to GREEN within ORACLE_TOL (bit-exact where the algorithm permits).
  </how-to-verify>
  <resume-signal>Type "approved" once captures are committed and metric_parity cells are green, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline metric eval over developer config + scores/labels. No network/auth/session/PII.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Tampering→NaN | metric eval with mismatched score/label lengths or out-of-range prob inputs | mitigate | V5 length-validation gate (regression.rs:82-88 pattern) typed MetricError::LengthMismatch before any fold; K_EPSILON clamp for log metrics |
| T-07-04-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-metric` GREEN
- `cargo test --workspace` GREEN (metric_parity skip-passes until capture)
- After capture: metric_parity cells green within ORACLE_TOL; byte-idempotent
</verification>

<success_criteria>
The extended regression/xentropy/multiclass metrics (MET-03) match real lib_lightgbm 4.6 within parity, with correct bigger-better signs and prob-space transform routing. (Ranking metrics MET-04 land with the ranking stack in 07-09.)
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-04)

- enum Metric arms: quantile/huber/fair/poisson/mape/gamma/gamma_deviance/tweedie (lgbm-metric/src/regression.rs)
- xentropy metric arms: cross_entropy/cross_entropy_lambda/kullback_leibler (lgbm-metric/src/xentropy.rs)
- multiclass metric arms: multi_error/auc_mu/average_precision (lgbm-metric/src/multiclass.rs)
- `crates/oracle-harness/tests/metric_parity.rs` (new test file)
- xtask metric capture cells + fixtures
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-04-SUMMARY.md` when done
</output>
