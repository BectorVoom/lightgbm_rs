---
phase: 07-parity-completing-variants
plan: 12
type: execute
wave: 12
depends_on: ["07-07", "07-10"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm-model/src/ensemble.rs
  - crates/lgbm-model/src/model_text.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/advanced_parity.rs
  - crates/oracle-harness/tests/fixtures/advanced/.gitkeep
autonomous: false
requirements: ["ADV-06", "ADV-07"]
must_haves:
  truths:
    - "A user can refit a model's leaf outputs (refit_decay_rate: leaf = decay*old + (1-decay)*new) and continue training from an input_model, matching real lib_lightgbm 4.6"
    - "Feature importance reports split-count and gain-sum per saved_feature_importance_type, with the split_gain>0 guard"
    - "Refit reuses Phase-3 model load + num_init_iteration accounting; importance reuses the GbdtModel split-count path"
  artifacts:
    - path: "crates/lgbm-boosting/src/gbdt.rs"
      provides: "RefitTree (leaf decay) + input_model continue-training"
      contains: "refit"
    - path: "crates/lgbm-model/src/ensemble.rs"
      provides: "gain-based feature importance + saved_feature_importance_type"
      contains: "feature_importance"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs (continue-training)"
      to: "crates/lgbm-model/src/model_text.rs (Phase-3 model load)"
      via: "input_model loaded then num_init_iteration accounting continues"
      pattern: "input_model|num_init_iteration"
---

<objective>
W11 (ADV-06 + ADV-07): the final additive features — refit/continue training (ADV-06) and feature importance reporting (ADV-07) — both on the locked model/boosting layer. Two refit slices per RESEARCH Open Q3: `refit_decay_rate` leaf-refit and `input_model` continue-training. Importance adds gain-based reporting alongside the existing split-count.

Purpose: leaf-node DAG features touching lgbm-model/lgbm-boosting (D-02 step 7). Last plan before the single end-of-phase verification gate (D-01). Ordered after 07-07 (RF) and 07-10 (predict) which also touch gbdt.rs/ensemble.rs.
Output: refit (both flavors) + gain importance wired with a new advanced_parity.rs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
@.planning/phases/03-tree-model-model-text-i-o-predict-parity/03-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refit (refit_decay_rate leaf-refit + input_model continue-training)</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-boosting/src/gbdt.rs, crates/lgbm-model/src/model_text.rs</files>
  <read_first>
    - crates/lgbm-boosting/src/gbdt.rs (Gbdt::into_model :557; pop_trailing_trees :546; with_objective continue-from-model; the num_init_iteration accounting DART/RF already reference)
    - crates/lgbm-model/src/model_text.rs (load — Phase-3 model load reused for continue-training + leaf-refit)
    - crates/lgbm-model/src/tree.rs (leaf_output set/get — leaf-refit decays it)
    - crates/lgbm-core/src/config/set.rs (re-grep refit_decay_rate — RESEARCH marks ADD)
    - C++ refs (authoritative): LightGBM/src/boosting/gbdt.cpp RefitTree + continue-training (input_model, refit_decay_rate); LightGBM/src/c_api.cpp LGBM_BoosterRefit; config.h:551/:553 refit_decay_rate ([0,1]); the RESEARCH Open Q3 two-slice boundary (continue reuses num_init_iteration; leaf-refit reuses Phase-3 load)
  </read_first>
  <behavior>
    - leaf-refit: load model -> route data to leaves -> per leaf new_output = decay*old_output + (1-decay)*newton_leaf(grad,hess over routed rows); refit_decay_rate default 0.9
    - continue-training: load input_model -> set num_init_iteration to the loaded tree count -> subsequent TrainOneIter appends, accounting for num_init_iteration (as RF/DART already do)
  </behavior>
  <action>
    Re-grep set.rs FIRST; add refit_decay_rate (0.9, CHECK [0,1]). Add a RefitTree path on Gbdt (leaf-refit) per `<behavior>` from gbdt.cpp RefitTree: load the model (Phase-3 model_text load), route training rows to leaves, compute new leaf outputs as decay*old + (1-decay)*new, write them back. Add continue-training: load input_model and set num_init_iteration to the loaded count so subsequent train appends (reuse the existing num_init_iteration accounting). Keep both flavors as distinct entry points (RESEARCH Open Q3 two slices).
  </action>
  <verify>
    <automated>cargo test -p lgbm-boosting && cargo test -p lgbm-model && cargo test -p lgbm-core</automated>
  </verify>
  <acceptance_criteria>
    - refit_decay_rate resolves (0.9) and rejects out-of-[0,1] via typed Result (unit test)
    - leaf-refit sets new_output = decay*old + (1-decay)*new (unit test on a known leaf: decay=0 -> all-new, decay=1 -> unchanged)
    - continue-training from an input_model sets num_init_iteration to the loaded tree count and appends (unit test: tree count grows from the loaded base)
    - `cargo test -p lgbm-boosting`, `cargo test -p lgbm-model`, `cargo test -p lgbm-core` GREEN
  </acceptance_criteria>
  <done>Both refit flavors work: leaf decay-refit and input_model continue-training, reusing Phase-3 load + num_init_iteration accounting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gain-based feature importance + saved_feature_importance_type (ADV-07)</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-model/src/ensemble.rs, crates/lgbm-model/src/model_text.rs</files>
  <read_first>
    - crates/lgbm-model/src/ensemble.rs (GbdtModel::feature_importance_split_count :108 — add a gain-based sibling)
    - crates/lgbm-model/src/model_text.rs (the feature_importances: block recompute :264-276 — extend for saved_feature_importance_type)
    - crates/lgbm-model/src/tree.rs (split_gain per node — gain importance sums it; the Phase-3 CR-02 split_gain>0 guard follow-up)
    - crates/lgbm-core/src/config/set.rs (re-grep saved_feature_importance_type — RESEARCH marks ADD)
    - C++ refs (authoritative): LightGBM/src/io/tree.cpp + gbdt.cpp FeatureImportance (split vs gain, saved_feature_importance_type); config.h:615 saved_feature_importance_type (0=split,1=gain); the CR-02 split_gain>0 guard (Phase-3 03-REVIEW follow-up)
  </read_first>
  <behavior>
    - split importance: per-feature count of splits using that feature (existing path)
    - gain importance: per-feature sum of split_gain over splits using that feature, ONLY where split_gain>0 (CR-02 guard)
    - saved_feature_importance_type ∈ {0=split,1=gain} selects which is written into the model-text feature_importances: block
  </behavior>
  <action>
    Re-grep set.rs FIRST; add saved_feature_importance_type (0, the split/gain selector). Add a gain-based `feature_importance_gain` to GbdtModel (ensemble.rs) summing split_gain per feature with the split_gain>0 guard (the Phase-3 CR-02 follow-up). Extend the model_text feature_importances: block (:264-276) to write split-count or gain-sum per saved_feature_importance_type.
  </action>
  <verify>
    <automated>cargo test -p lgbm-model && cargo test -p lgbm-core</automated>
  </verify>
  <acceptance_criteria>
    - `feature_importance_gain` exists and applies the split_gain>0 guard (unit test: a split with gain<=0 contributes 0)
    - saved_feature_importance_type (0) resolves and selects split vs gain in the model-text block (unit test)
    - `cargo test -p lgbm-model`, `cargo test -p lgbm-core` GREEN
  </acceptance_criteria>
  <done>Gain-based importance (with the split_gain>0 guard) + saved_feature_importance_type selection are wired into the importance reporting + model-text.</done>
</task>

<task type="auto">
  <name>Task 3: Builder + advanced capture + new advanced_parity.rs (refit + importance, capture-gated)</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/advanced_parity.rs, crates/oracle-harness/tests/fixtures/advanced/.gitkeep</files>
  <read_first>
    - crates/lgbm/src/builder.rs (chained setters; how a model load / refit entry point is exposed)
    - xtask/src/main.rs (model/boosting capture; version-pin :84-97)
    - crates/oracle-harness/tests/boosting_parity.rs (capture-gated read_golden :316-330 shape to mirror)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Oracle Axis Matrix → Refit / Importance" (refit_decay_rate∈{0.9,0.0} leaf-refit; input_model continue; importance saved_feature_importance_type∈{0=split,1=gain} per-feature counts/gains, CR-02 guard)
  </read_first>
  <action>
    Add builder setters: refit_decay_rate + a refit/continue entry point (input_model) + saved_feature_importance_type. Create the NEW `crates/oracle-harness/tests/advanced_parity.rs`, modeled on the capture-gated shape. Extend the xtask capture to emit real-binary goldens: refit leaf outputs (refit_decay_rate∈{0.9,0.0}), continue-training tree growth from an input_model, and per-feature split-count + gain-sum importance (saved_feature_importance_type∈{0,1}); version-pin 4.6.0. Add capture-gated `refit_*` and `importance_*` cells. Tests skip-pass until capture.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test advanced_parity</automated>
  </verify>
  <acceptance_criteria>
    - builder setters refit_decay_rate/input_model/saved_feature_importance_type route into Config / the refit entry (test)
    - `crates/oracle-harness/tests/advanced_parity.rs` exists with `refit_*` (decay∈{0.9,0.0} + continue) + `importance_*` (split/gain) cells; each skip-passes absent the golden
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>Builder + advanced capture + advanced_parity.rs cells are in place; cells skip-pass; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Capture the refit + importance real-binary goldens</name>
  <what-built>Refit (Task 1), gain importance (Task 2), builder/capture/advanced_parity.rs (Task 3). Human-gated capture (wheel absent).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the advanced capture subcommand; confirm version-assert + fixtures (refit leaf outputs decay∈{0.9,0.0}, continue-training, split/gain importance) under `crates/oracle-harness/tests/fixtures/advanced/`.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test advanced_parity` — confirm refit + importance cells flip to GREEN.
  </how-to-verify>
  <resume-signal>Type "approved" once refit + importance cells are green, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline model ops over developer config + a loaded model. **ADV-06 `input_model` deserialization parses untrusted model-text** — a real Tampering surface (the Phase-3-validated parse path, extended here for continue/refit).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12-01 | Tampering | malformed input_model text on refit/continue-load (OOB node/leaf indices, bad cat bitset) | mitigate | reuse the Phase-3 model_text load bounds-validation (typed ModelError, never panic); confirm cat-tree fields validated for the 07-08 grown-tree schema |
| T-07-12-02 | Tampering | refit_decay_rate out of [0,1] | mitigate | config.h CHECK [0,1] surfaced as typed Result |
| T-07-12-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-boosting`, `cargo test -p lgbm-model`, `cargo test -p lgbm-core` GREEN
- `cargo test --workspace` GREEN (advanced_parity skip-passes until capture)
- After capture: refit (both flavors) + importance (split/gain) cells green; byte-idempotent
</verification>

<success_criteria>
Refit/continue training (ADV-06: refit_decay_rate leaf-refit + input_model continue) and feature importance reporting (ADV-07: split/gain per saved_feature_importance_type, with the split_gain>0 guard) reproduce real lib_lightgbm 4.6, reusing Phase-3 model I/O + num_init_iteration accounting. This is the final additive plan before the single end-of-phase verification gate (D-01).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-12)

- RefitTree (leaf decay-refit) + input_model continue-training on Gbdt (crates/lgbm-boosting/src/gbdt.rs)
- `feature_importance_gain` (split_gain>0 guard) on GbdtModel (crates/lgbm-model/src/ensemble.rs)
- saved_feature_importance_type selection in the model-text feature_importances: block (crates/lgbm-model/src/model_text.rs)
- Config params refit_decay_rate/saved_feature_importance_type (lgbm-core/src/config/set.rs)
- Builder setters refit_decay_rate/input_model/saved_feature_importance_type (crates/lgbm/src/builder.rs)
- `crates/oracle-harness/tests/advanced_parity.rs` (new file) — `refit_*`/`importance_*` cells + `tests/fixtures/advanced/`
- xtask advanced capture subcommand
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-12-SUMMARY.md` when done
</output>
