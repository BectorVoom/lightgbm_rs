---
phase: 07-parity-completing-variants
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - crates/lgbm-core/src/config/set.rs
  - crates/lgbm-core/src/config/alias.rs
  - crates/lgbm-objective/src/regression.rs
  - crates/lgbm-objective/src/xentropy.rs
  - crates/lgbm-objective/src/lib.rs
  - crates/lgbm-boosting/src/objective.rs
  - crates/lgbm/src/builder.rs
  - xtask/src/main.rs
  - crates/oracle-harness/tests/boosting_parity.rs
autonomous: false
requirements: ["OBJ-04", "OBJ-05"]
must_haves:
  truths:
    - "A user can select poisson/gamma/tweedie (OBJ-04) and cross_entropy/cross_entropy_lambda (OBJ-05) objectives via the builder and train within parity of real lib_lightgbm 4.6"
    - "exp/log objectives use f64 exp/log narrowed to f32 once, with BoostFromScore via SafeLog (poisson/tweedie) and a documented horizon cap where exp-libm flips a knife-edge"
    - "Out-of-range labels feeding exp/log objectives are typed-rejected mirroring the C++ LabelOutOfRange Init guards (poisson/gamma label>=0, xentropy label in [0,1])"
  artifacts:
    - path: "crates/lgbm-objective/src/regression.rs"
      provides: "poisson/gamma/tweedie grad/hess + SafeLog BoostFromScore + LabelOutOfRange guard"
      contains: "Poisson"
    - path: "crates/lgbm-objective/src/xentropy.rs"
      provides: "cross_entropy/cross_entropy_lambda grad/hess + sigmoid Convert"
  key_links:
    - from: "crates/lgbm/src/builder.rs"
      to: "crates/lgbm-objective"
      via: "objective string -> Objective::parse / Xentropy::parse"
      pattern: "poisson|gamma|tweedie|cross_entropy"
---

<objective>
W2 (OBJ-04 exp/log family + OBJ-05 cross-entropy): add `poisson`, `gamma`, `tweedie` (OBJ-04, completing OBJ-04 begun in 07-02) and `cross_entropy`, `cross_entropy_lambda` (OBJ-05). These are exp/log objectives carrying the carried multiclass exp-libm horizon caveat (Pitfall 5).

Purpose: complete the objective breadth (D-02 step 2); independent of 07-01 except via the shared loop. Ordered after 07-02 because both edit `regression.rs`/`objective.rs`/`builder.rs` (no same-wave file conflict).
Output: 5 objectives wired end-to-end with layered real-binary goldens captured at a bit-exact horizon.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-parity-completing-variants/07-RESEARCH.md
@.planning/phases/07-parity-completing-variants/07-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Config params + poisson/gamma/tweedie + cross_entropy/cross_entropy_lambda math + LabelOutOfRange guards</name>
  <files>crates/lgbm-core/src/config/set.rs, crates/lgbm-core/src/config/alias.rs, crates/lgbm-objective/src/regression.rs, crates/lgbm-objective/src/xentropy.rs, crates/lgbm-objective/src/lib.rs, crates/lgbm-boosting/src/objective.rs</files>
  <read_first>
    - crates/lgbm-objective/src/regression.rs (enum Objective + parse + flags regression.rs:40-133; BoostFromScore ordered fold :229-252)
    - crates/lgbm-objective/src/binary.rs (sigmoid f64→f32 pattern :38-96; clamp to [kEpsilon,1-kEpsilon] :112-116; typed-reject ctor :45-52 — xentropy models on this)
    - crates/lgbm-objective/src/multiclass.rs (the exp/softmax + LabelOutOfRange Init guard, carried Phase-6 multiclass exp-libm horizon deviation)
    - crates/lgbm-core/src/config/set.rs (re-grep poisson_max_delta_step/tweedie_variance_power — RESEARCH A2)
    - C++ refs (authoritative): LightGBM/src/objective/regression_objective.hpp:398 (poisson grad=exp(score)-label, hess=exp(score)*max_delta_step, BoostFromScore=SafeLog :469), :680 (gamma subclasses Poisson), tweedie (tweedie_variance_power); LightGBM/src/objective/xentropy_objective.hpp (cross_entropy/cross_entropy_lambda label in [0,1], BoostFromScore + sigmoid Convert); config.h:968 (poisson_max_delta_step), :976 (tweedie_variance_power range [1,2))
  </read_first>
  <behavior>
    - poisson: grad = exp(score)-label (f64 exp, cast f32); hess = exp(score)*poisson_max_delta_step; BoostFromScore = SafeLog(L2 boost); label>=0 else LabelOutOfRange typed error
    - gamma: subclasses poisson grad shape per regression_objective.hpp:680; label>=0 guard
    - tweedie: grad/hess parameterized by tweedie_variance_power in [1,2); exp-based; label>=0
    - cross_entropy: label in [0,1] guard; grad/hess + sigmoid Convert per xentropy_objective.hpp
    - cross_entropy_lambda: per xentropy_objective.hpp variant
    - all exp/log: f64 arithmetic, single f32 cast; K_EPSILON via types, never literal
  </behavior>
  <action>
    Re-grep set.rs FIRST. Add missing params with exact config.h values: `poisson_max_delta_step` (default 0.7, CHECK >0), `tweedie_variance_power` (default 1.5, CHECK in [1,2)). Add `Poisson`/`Gamma`/`Tweedie` to enum Objective (regression.rs) with parse arms + flag arms (is_constant_hessian false — hessian depends on exp(score)). Create/extend `crates/lgbm-objective/src/xentropy.rs` with `cross_entropy`/`cross_entropy_lambda` structs mirroring binary.rs's sigmoid shape; add to lib.rs re-exports. Port grad/hess + BoostFromScore (SafeLog for poisson/tweedie) per `<behavior>` from the cited C++ file:line. Add LabelOutOfRange typed guards in each objective's Init/ctor (poisson/gamma/tweedie label>=0; xentropy label in [0,1]) as typed errors, never panics. Wire the 5 BoostObjective variants + dispatch in lgbm-boosting/src/objective.rs.
  </action>
  <verify>
    <automated>cargo test -p lgbm-objective && cargo test -p lgbm-core</automated>
  </verify>
  <acceptance_criteria>
    - `Objective::Poisson`/`Gamma`/`Tweedie` + xentropy `cross_entropy`/`cross_entropy_lambda` parse arms exist (grep)
    - `poisson_max_delta_step` (0.7) and `tweedie_variance_power` (1.5, rejects 2.0 and 0.9) resolve/CHECK via typed Result (unit test)
    - A negative label fed to poisson/gamma/tweedie returns a typed LabelOutOfRange error (unit test), and a label outside [0,1] fed to cross_entropy returns a typed error
    - exp arithmetic done in f64 then cast f32 once (grep: no `as f32` mid-expression before exp)
    - `cargo test -p lgbm-objective` + `cargo test -p lgbm-core` GREEN
  </acceptance_criteria>
  <done>The 5 exp/log objectives compute faithful grad/hess with SafeLog boost + typed label guards; unit tests prove CHECKs and guards.</done>
</task>

<task type="auto">
  <name>Task 2: Builder setters + capture emitter + capture-gated horizon-capped parity cells</name>
  <files>crates/lgbm/src/builder.rs, xtask/src/main.rs, crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - crates/lgbm/src/builder.rs (objective/reg_sqrt/bagging_fraction setters :52-55,121-124,152-156)
    - xtask/src/main.rs (boosting_oracle_capture :115; version-pin :84-97)
    - crates/oracle-harness/tests/boosting_parity.rs (cell_builder :44-176; layered asserts :199-301; read_golden :316-330; the existing multiclass 5-iter horizon-cap precedent)
    - .planning/phases/07-parity-completing-variants/07-RESEARCH.md §"Pitfall 5" (exp-libm horizon: capture at a horizon where trees stay bit-exact; ORACLE_TOL after) + §"Oracle Axis Matrix → Objectives" (poisson 8 × max_delta_step∈{0.7,0.1}; tweedie 8 × power∈{1.5,1.1,1.9}; gamma 8; cross_entropy 8; cross_entropy_lambda 8)
  </read_first>
  <action>
    Add builder setters `poisson_max_delta_step(f64)` and `tweedie_variance_power(f64)`. Extend the capture xtask to emit layered L1-L5 goldens for poisson/gamma/tweedie/cross_entropy/cross_entropy_lambda across the matrix axes; CAP the iteration horizon where trees stay bit-exact (mirror the Phase-6 multiclass 5-iter precedent) and document the cap in-fixture/in-comment — do NOT weaken the assertion, cap the horizon. Add capture-gated parity cells; bit-exact on the bit-exact-horizon trees, ORACLE_TOL on the predict-side Convert (one exp/row). Document any collapsed cell.
  </action>
  <verify>
    <automated>cargo build -p xtask && cargo test -p oracle-harness --test boosting_parity poisson</automated>
  </verify>
  <acceptance_criteria>
    - `builder.poisson_max_delta_step(..)` and `builder.tweedie_variance_power(..)` route into Config (test)
    - boosting_parity.rs has `poisson_*`/`gamma_*`/`tweedie_*`/`cross_entropy_*`/`cross_entropy_lambda_*` cells covering the matrix; each skip-passes absent the golden
    - each exp/log cell documents its bit-exact horizon cap in-code (no weakened tolerance disguising a knife-edge flip)
    - `cargo build -p xtask` succeeds; `cargo test --workspace` GREEN
  </acceptance_criteria>
  <done>Builder setters exist, capture emitter covers the matrix with documented horizon caps, cells skip-pass; workspace green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Capture the poisson/gamma/tweedie/cross_entropy(_lambda) real-binary goldens</name>
  <what-built>The exp/log objective math (Task 1) + builder setters/capture emitter/capture-gated cells (Task 2). Human-gated capture (wheel absent, RESEARCH A5).</what-built>
  <how-to-verify>
    1. Ensure `lightgbm==4.6.0` is installed.
    2. Run the per-objective capture subcommand for poisson/gamma/tweedie/cross_entropy/cross_entropy_lambda; confirm version-assert + fixtures written.
    3. Re-run for byte-idempotent regen.
    4. Run `cargo test -p oracle-harness --test boosting_parity poisson gamma tweedie cross_entropy` — confirm cells flip to GREEN (bit-exact at the capped horizon; ORACLE_TOL on predict Convert).
    5. Confirm the horizon cap holds (trees bit-exact within the captured horizon; no silent tolerance widening).
  </how-to-verify>
  <resume-signal>Type "approved" once captures are committed and the 5 objective cells are green at their documented horizon, or describe issues.</resume-signal>
  <action>Human-gated capture step (no autonomous code action): perform the install + xtask capture + golden commit described in <how-to-verify>. This task gates the enforce-parity moment; the wheel (`lightgbm==4.6.0`) is not installed in this environment, so it is a blocking-human checkpoint per the Phase-5/6 capture posture. LightGBM/ is never git-added.</action>
  <verify><human-check>The per-subsystem replay cells flip from skip-pass to GREEN after the fixtures are committed; re-running the capture is byte-idempotent.</human-check></verify>
  <done>The real-lib_lightgbm-4.6 fixtures are committed and the subsystem replay cells are green (or the issue is described to the operator).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Offline objectives over developer config + binned labels. No network/auth/session/PII.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering→NaN | poisson/gamma/tweedie/xentropy fed out-of-range labels into exp/log | mitigate | LabelOutOfRange typed Init guards (poisson/gamma/tweedie label>=0; xentropy label in [0,1]) as typed errors, never panics |
| T-07-03-02 | Tampering | tweedie_variance_power / poisson_max_delta_step out-of-range | mitigate | config.h CHECK ([1,2) / >0) surfaced as typed Result |
| T-07-03-SC | Tampering | `pip install lightgbm==4.6.0` capture | mitigate | version-asserted in xtask; blocking-human checkpoint; LightGBM/ never git-added |
</threat_model>

<verification>
- `cargo test -p lgbm-objective` + `cargo test -p lgbm-core` GREEN
- `cargo test --workspace` GREEN (cells skip-pass until capture)
- After capture: 5 objective cells green at documented horizon; byte-idempotent
</verification>

<success_criteria>
poisson/gamma/tweedie (completing OBJ-04) + cross_entropy/cross_entropy_lambda (OBJ-05) train via the builder and match real lib_lightgbm 4.6 within parity at the documented bit-exact horizon, with typed LabelOutOfRange guards.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (Plan 07-03)

- `Objective::Poisson`/`Gamma`/`Tweedie` enum + parse arms (lgbm-objective/src/regression.rs)
- `crates/lgbm-objective/src/xentropy.rs` (new file) — `CrossEntropy`/`CrossEntropyLambda` structs + parse
- `BoostObjective` variants for the 5 objectives (lgbm-boosting/src/objective.rs)
- Config params `poisson_max_delta_step`, `tweedie_variance_power` (lgbm-core/src/config/set.rs)
- Builder setters `poisson_max_delta_step`, `tweedie_variance_power` (crates/lgbm/src/builder.rs)
- xtask capture cells + boosting_parity.rs cells `poisson_*`/`gamma_*`/`tweedie_*`/`cross_entropy_*`/`cross_entropy_lambda_*` + fixtures
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-03-SUMMARY.md` when done
</output>
