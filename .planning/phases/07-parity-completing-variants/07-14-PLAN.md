---
phase: 07-parity-completing-variants
plan: 14
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/lgbm-boosting/src/gbdt.rs
  - crates/lgbm/src/booster.rs
  - crates/oracle-harness/tests/boosting_parity.rs
  - .planning/phases/07-parity-completing-variants/deferred-items.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "On a NON-FIRST bagged iteration that yields no positive-gain split (`num_leaves <= 1`), the Rust GBDT boosting loop replicates C++ `GBDT::TrainOneIter` (gbdt.cpp:406-447): it POPS the would-be 1-leaf constant tree, does NOT advance the iteration counter, and re-bags on the next boost round — instead of APPENDING a `Tree::as_constant` and advancing"
    - "`quantile_loop_matrix` (bagged sub-cell `quantile_bag1_es0_bfa0`) grows 10 trees `[1,2,3,3,3,3,3,3,2,3]` not 12 `[1,2,3,3,1,3,3,3,1,3,2,3]`, and wheel tree4 ≡ Rust tree5 bit-exact; the cell is un-#[ignore]d and asserts real-lib_lightgbm-4.6 parity"
    - "The FIRST-iteration no-split constant baseline is PRESERVED in both Rust and C++: `regression_l1_bag1` Tree=0 stays a constant unchanged; `class_need_train_false_pushes_constant_tree` and the gbdt unit tests stay green"
    - "Every bagged/randomized variant stays bit-exact with NO weakened assertion / capped horizon: DART (`dart_parity_matrix`, `dart_drop_rng_replay`), GOSS (`goss_parity_matrix`, `goss_rng_replay`), RF (`rf_single_parity`, `rf_multi_parity`), all bagging cells (`bagging_rng`, every `*_bag1_*`), `subset_determinism_diagnostic`; ALL cells closed by 07-13 (Family A) and by the remaining-ignored-cells session (Family B: monotone_mixed/forced_nested/extra_trees_seed6/9) stay green; full `cargo test --workspace` 0-failed (kernel_parity 4/4, learner_parity 29/29, all `*_spine`/`*_gradients`)"
    - "DEF-07-13-01 is cleared in deferred-items.md (the LAST ignored cell closed); DEF-07-02/03 and DEF-07-11 remain recorded as already-RESOLVED (untouched)"
  artifacts:
    - path: "crates/lgbm-boosting/src/gbdt.rs"
      provides: "C++-faithful no-split bagged-iteration pop/skip + iter-non-advance + re-bag-retry in the GBDT boosting loop / train-one-iter path"
    - path: "crates/oracle-harness/tests/boosting_parity.rs"
      provides: "quantile_loop_matrix un-#[ignore]d asserting real-binary parity (10-tree structure, wheel tree4 ≡ Rust tree5)"
    - path: ".planning/phases/07-parity-completing-variants/deferred-items.md"
      provides: "DEF-07-13-01 cleared (last ignored cell closed); DEF-07-02/03 + DEF-07-11 RESOLVED records retained"
  key_links:
    - from: "crates/lgbm-boosting/src/gbdt.rs (train-one-iter no-split branch ~758-829 + train() loop ~1199-1210)"
      to: "crates/lgbm-boosting/src/sample_strategy.rs (per-iteration bagging draw / RNG advance / need_re_bagging)"
      via: "the iteration counter + re-bag retry: a non-advanced iter must re-draw the bag on the next round with the C++-faithful RNG timing"
      pattern: "self\\.iter|bag\\.bagging|need_re_bagging|as_constant"
    - from: "crates/oracle-harness/tests/boosting_parity.rs (quantile_loop_matrix)"
      to: "real lib_lightgbm 4.6 quantile bagged golden (10-tree wheel model)"
      via: "replay of the committed quantile bagged golden / the loop matrix tree[i]-vs-golden tree[i] assertion"
      pattern: "quantile_loop_matrix|quantile_bag1_es0_bfa0"
---

<objective>
Close the LAST remaining ignored cell, DEF-07-13-01 — the quantile bagged-renew
structural divergence (`quantile_loop_matrix` / sub-cell `quantile_bag1_es0_bfa0`).
This is a focused FIX plan within the otherwise-complete phase 07; it is NOT a
re-plan of phase 07. The root cause is already PROVEN — the resolved debug session
(`.planning/debug/remaining-ignored-cells.md`, Group 3) IS the research, so there
is no research task.

Root cause (verified, Python-wheel oracle — the CLI cannot reproduce this golden):
the divergence is NOT a bagging-draw or quantile-RenewTreeOutput bug — both are
verified bit-exact (Rust's per-iteration bagged subset matches the C++
`bagging.hpp` algorithm bit-for-bit: iter4 in_bag=[0,1,4,5,6,7,8,9,10],
oob=[2,3,11] in both; Rust's scores through tree 3 are bit-exact to the wheel). The
divergence is the GBDT no-split bagged-tree EMISSION policy. On a NON-FIRST bagged
iteration that yields no positive-gain split (uniform gradients on the subset —
quantile alpha=0.9 hits this mid-training), C++ `GBDT::TrainOneIter`
(gbdt.cpp:406-447) POPS the would-be 1-leaf constant tree (the `!should_continue`
path, "Stopped training because there are no more leaves" warning), does NOT
advance `iter_`, and re-bags on the next boost round. Rust (`gbdt.rs:765,828`)
instead APPENDS a 1-leaf `Tree::as_constant` and advances. Result: Rust grows 12
trees `[1,2,3,3,1,3,3,3,1,3,2,3]` vs the wheel's 10 `[1,2,3,3,3,3,3,3,2,3]`; the 2
spurious constants (iters 4,8) shift every later tree (wheel tree4 ≡ Rust tree5,
bit-exact). The FIRST-iteration no-split constant baseline is KEPT in BOTH (e.g.
`regression_l1_bag1` Tree=0), so the divergence is isolated to LATER no-split
bagged iterations.

NOTE (oracle nuance — encode in comments): the deterministic source-built CLI
`GBDT::Train` treats `!should_continue` as `is_finished` (gbdt.cpp:447 `return
true`) and STOPS entirely, so it produces only 1 tree for quantile+bfa-off. The
10-tree golden comes from the Python `lgb.train` driver, which calls
`TrainOneIter` per boost round and CONTINUES past the no-split signal: because the
no-split round does NOT advance `iter_`, the next round re-bags (fresh RNG draw)
and grows a real tree. The Rust fix must reproduce the wheel/`lgb.train` driver
semantics (continue + re-bag), not the CLI early-stop.

Purpose: close DEF-07-13-01 — the last `#[ignore]`d cell — so phase 07 has zero
deferred parity cells, WITHOUT regressing the bit-exact CPU f64-fold merge gate or
any bagged/randomized variant.
Output: a C++-faithful no-split bagged-iteration pop/skip + iter-non-advance +
re-bag-retry in the GBDT boosting loop, `quantile_loop_matrix` un-#[ignore]d
asserting real-binary parity, and DEF-07-13-01 cleared.

Scope is the shared GBDT boosting-loop tree-emission contract ONLY. Do NOT touch
the tree learner, the histogram representation, the bagging draw, or
RenewTreeOutput — all verified bit-exact. Do NOT re-open DEF-07-02/03 (Family A)
or DEF-07-11 (Family B) — both already RESOLVED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/remaining-ignored-cells.md
@.planning/phases/07-parity-completing-variants/deferred-items.md
@.planning/phases/07-parity-completing-variants/07-13-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the C++ gbdt.cpp:406-447 no-split bagged semantics; encode a failing-then-passing assertion that quantile_bag1_es0_bfa0 grows 10 trees not 12</name>
  <files>crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - .planning/debug/remaining-ignored-cells.md (the Group 3 / DEF-07-13-01 section: the CONFIRMED root cause — bagging-draw + RenewTreeOutput are bit-exact; the EMISSION-policy divergence; the verified iter4 in_bag/oob; "wheel tree4 == Rust tree5"; the 12-tree [1,2,3,3,1,3,3,3,1,3,2,3] vs 10-tree [1,2,3,3,3,3,3,3,2,3] structure; the CLI-cannot-reproduce / Python-wheel-oracle note; the Group 3 ROOT CAUSE Evidence + Eliminated entries — DO NOT re-chase the bagging draw or quantile RenewTreeOutput)
    - crates/oracle-harness/tests/boosting_parity.rs (the `quantile_loop_matrix` cell at :2540 and its current DEF-07-13-01 #[ignore] reason at :2539; the bagged sub-cell `quantile_bag1_es0_bfa0`; the replay/assert helpers the loop matrix already uses; the existing `regression_l1_bag1_es0_bfa0` constant-Tree=0 handling at :463-478 and the DEF-06-01 commentary at :1352-1361 as the model for asserting a constant-baseline-preserving bagged structure; `subset_determinism_diagnostic` at :1996 as the tree-count diagnostic pattern)
    - crates/lgbm-boosting/src/gbdt.rs (the two no-split push sites — the use_subset bagged branch at :758-766 and the full-corpus branch at :816-829; `no_split_constant_value` at :459; the `train()` loop at :1199-1210 that runs exactly `num_iterations` rounds and always pushes one tree/round + advances `self.iter` at :845; the bagging draw at :588-590 and `use_subset` plumbing)
    - LightGBM/src/boosting/gbdt.cpp (TrainOneIter :344-451 — the `should_continue` flag :386/:407, the `num_leaves()>1` vs else branch :406-434, the `!should_continue` POP + `return true` :440-448, the `++iter_; return false` :450-451; read-only, NEVER git-add LightGBM/)
  </read_first>
  <action>
    Add a focused diagnostic test (e.g. `quantile_bagged_no_split_emission_contract`)
    in boosting_parity.rs that drives the `quantile_bag1_es0_bfa0` reproduction and
    asserts the C++ no-split bagged EMISSION ground truth from the Python-wheel
    oracle: the booster grows EXACTLY 10 trees with the structure
    `[1,2,3,3,3,3,3,3,2,3]` (NOT 12 `[1,2,3,3,1,3,3,3,1,3,2,3]`), and the surviving
    trees match the wheel (in particular wheel tree4 ≡ the current-Rust tree5
    bit-exact — i.e. once the 2 spurious constants at iters 4,8 are skipped, every
    later tree aligns). Pin the exact C++ contract this test encodes IN COMMENTS:
    on a NON-FIRST bagged iteration with `num_leaves <= 1` (no positive-gain split),
    C++ `GBDT::TrainOneIter` (gbdt.cpp:406-447) sets `should_continue=false`, POPS
    the would-be 1-leaf constant, does NOT advance `iter_`, and the wheel/`lgb.train`
    driver re-bags next round; the FIRST-iteration no-split constant baseline
    (gbdt.cpp:419-434, `models_.size() < num_tree_per_iteration_`) is KEPT. Also
    note the oracle nuance: the deterministic CLI early-stops (gbdt.cpp:447 `return
    true`) so it cannot produce this 10-tree golden — the golden is the wheel's.
    This test MUST FAIL on the current append-constant model (capturing the 12-tree
    divergence) and is the structural target Task 2 turns green. Use the committed
    quantile bagged goldens already present (the loop-matrix fixtures the existing
    `quantile_loop_matrix` cell replays) so no new fixture capture is required — the
    debug session already validated the wheel reproduces them. Do NOT weaken or alter
    any existing assertion; this is a NEW diagnostic alongside the matrix. Leave the
    `quantile_loop_matrix` cell #[ignore]d until Task 3. Leave every currently-green
    cell untouched.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test boosting_parity quantile_bagged_no_split_emission_contract -- --include-ignored 2>&1 | tail -30</automated>
  </verify>
  <done>The diagnostic exists, encodes the C++ gbdt.cpp:406-447 no-split bagged emission contract (pop/skip + iter-non-advance + re-bag + first-iter-constant-kept) from the wheel oracle, and FAILS on the current append-constant model (12 trees, structure [1,2,3,3,1,3,3,3,1,3,2,3]) — pinning the 10-tree structural target. All previously-green cells remain green.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the C++-faithful no-split bagged pop/skip + iter-non-advance + re-bag-retry in the GBDT boosting loop</name>
  <files>crates/lgbm-boosting/src/gbdt.rs, crates/lgbm/src/booster.rs</files>
  <read_first>
    - crates/lgbm-boosting/src/gbdt.rs (train_one_iter :498+; the bagging/GOSS draw + `use_subset` at :588-610; the per-class tree loop :628+; the use_subset no-split push at :758-766; the full-corpus no-split push at :816-829; the `class_need_train` constant-push gate at :628-643; `is_first_tree`/`is_first_iter` (trees.len() < k) at :630/:650; `self.iter += 1` at :845; `no_split_constant_value` :459; the `train()` driver loop :1199-1210 running exactly `num_iterations` rounds; reload/iter-derivation at :1557/:1577 for invariants)
    - crates/lgbm/src/booster.rs (the REAL boosting driver the parity cells exercise — the internal `train()` / `train_with_*` loop ~:1098-1187: `for it in 0..total_iters { gbdt.train_one_iter(...) }`; this is the driver the matrix cells actually run, NOT gbdt.rs::train() ~:1207 which is used only by gbdt.rs unit tests. The popped/no-split round perturbs this loop's iteration-coupled bookkeeping: `iter_scores.push(snap.score)` would push a DUPLICATE score on a popped round, and `best_iteration = gbdt.num_iteration()` ~:1186 returns the (now-10) tree count. The `prev_tree_count` skip at ~:1112/:1119 is already robust to "no new tree this round", but the `snap.score` duplicate-push and the `num_iteration()`-derived `best_iteration` must be confirmed/handled so a popped round does not corrupt the per-iteration score accumulation or the reported best_iteration.)
    - crates/lgbm-boosting/src/sample_strategy.rs (`should_bag` :220-227 and `bagging` :239+ — `need_re_bagging` is set at construction so iter 0 always bags, and a re-bag happens when `should_bag(iter)` fires; confirm WHERE/HOW the next round re-draws so a non-advanced iter re-bags with the C++-faithful RNG timing. C++ `bagging.hpp:33`: `(bag_data_cnt_ < num_data_ && iter % bagging_freq == 0) || need_re_bagging_`. The bag is drawn BEFORE the no-split is detected — the RNG already advanced for the discarded round; the next round draws AGAIN. Reproduce that draw cadence.)
    - LightGBM/src/boosting/gbdt.cpp (TrainOneIter :386-451 — `should_continue`; the `num_leaves()>1` true-branch :406-417 (Renew/Shrinkage/UpdateScore/AddBias) vs else-branch :419-434 (first-iter AsConstantTree(init) / later AsConstantTree(0)); `models_.push_back` :437; the `!should_continue` POP-this-iteration's-trees + `return true` :440-448; `++iter_; return false` :450-451. Read-only, NEVER git-add LightGBM/. Distinguish the CLI early-stop (return true) from the wheel/lgb.train continue-and-re-bag the golden actually exercises.)
    - LightGBM/src/boosting/sample_strategy.cpp + src/boosting/bagging.hpp (the per-iteration Bagging draw + the LCG/Random advance per re-bag; read-only — confirm the RNG advances once per bagged round so a re-bagged retry round draws the NEXT bag, not the same one)
  </read_first>
  <action>
    Replicate the C++ `GBDT::TrainOneIter` no-split bagged EMISSION semantics
    (gbdt.cpp:406-447) in the Rust boosting loop so the Task-1 diagnostic passes.
    The proven divergence is that Rust APPENDS a 1-leaf `Tree::as_constant` and
    advances `self.iter` on EVERY no-split iteration (gbdt.rs:758-766, :816-829),
    while C++ POPS the would-be constant on a NON-FIRST no-split iteration, does NOT
    advance `iter_`, and re-bags next round. Bring Rust into agreement: on a no-split
    iteration (`num_leaves <= 1`) that is NOT the first iteration (i.e. when the
    first-iteration constant baseline has already been emitted — guard exactly as
    C++ does via `models_.size() < num_tree_per_iteration_`, the Rust
    `trees.len() < k` / `is_first_iter` predicate), do NOT push a constant and do NOT
    advance the iteration counter; instead skip tree emission for this round and let
    the loop re-bag and retry on the next round. PRESERVE the first-iteration no-split
    constant baseline exactly (C++ gbdt.cpp:419-434: first iter pushes
    `AsConstantTree(init)` — the `regression_l1_bag1` Tree=0 path must stay
    byte-unchanged). Whatever mechanism you choose (e.g. signal "no tree emitted this
    round" out of train_one_iter and have the `train()` driver loop keep iterating
    until the target tree count is reached, mirroring how `lgb.train` drives
    `TrainOneIter` per round; OR an internal retry within the loop), the binding
    constraints are: (1) the iteration counter / `self.iter` is NOT advanced on a
    skipped no-split round, so the next round RE-BAGS with a fresh RNG draw and the
    C++-faithful draw cadence (the discarded round's bag draw still advanced the RNG
    — match that, do not double-draw or skip a draw); (2) the final tree count is 10
    for `quantile_bag1_es0_bfa0` with structure `[1,2,3,3,3,3,3,3,2,3]` and wheel
    tree4 ≡ the surviving Rust tree; (3) the change is the GBDT-loop tree-emission
    contract ONLY — do NOT touch the tree learner, the histogram representation, the
    bagging draw algorithm, or RenewTreeOutput (all verified bit-exact). Touch ONLY
    the LATER no-split bagged path; the first-iteration constant, the full-corpus
    (non-bagged) path, the `class_need_train` constant-push, DART/GOSS/RF emission,
    and every `num_leaves > 1` path must be byte-untouched. Do NOT introduce any new
    tolerance, cap, special-case for the quantile cell, or capped horizon.

    CRITICAL — the real driver is `crates/lgbm/src/booster.rs`, NOT gbdt.rs::train():
    the parity matrix cells run booster.rs's internal `train()`/`train_with_*` loop
    (~:1098-1187: `for it in 0..total_iters { gbdt.train_one_iter(...) }`), whereas
    gbdt.rs::train() (~:1207) is exercised only by gbdt.rs's own unit tests. A popped
    / no-new-tree round perturbs booster.rs's iteration-coupled bookkeeping, so when
    train_one_iter can emit zero trees for a round you MUST verify/handle it there:
    (a) the `prev_tree_count` skip at ~:1112/:1119 is already robust to "no new tree
    this round" — confirm it still short-circuits correctly so no stale/duplicate
    work runs; (b) `iter_scores.push(snap.score)` MUST NOT push a DUPLICATE score on
    a popped round (a no-tree round produced no score change — either skip the push or
    otherwise keep the per-iteration score-accumulation vector aligned with emitted
    trees); (c) `best_iteration = gbdt.num_iteration()` (~:1186) now returns the
    (10) tree count rather than the boost-round count — confirm this is harmless for
    the matrix cell AND for EVERY `*_score_accumulation` / `best_iteration` assertion
    named in the Task-4 no-regression gate (if any such assertion expects a
    round-count rather than a tree-count, that is a regression to surface at Task 4,
    not to paper over). Make the minimal booster.rs change needed to keep the popped
    round harmless; do not alter early-stopping semantics, the eval cadence, or any
    other variant's driver path.

    If the `lgb.train`-style continue-and-re-bag cannot be reproduced without an
    architectural change to the `train()` driver, make that change minimally and
    faithfully (it is the sanctioned Rule-4 fix this plan exists to land) — but never
    weaken an assertion to pass.
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test boosting_parity quantile_bagged_no_split_emission_contract && cargo test -p lgbm-boosting</automated>
  </verify>
  <done>The Task-1 diagnostic passes (quantile_bag1_es0_bfa0 grows 10 trees [1,2,3,3,3,3,3,3,2,3]; wheel tree4 ≡ Rust surviving tree, bit-exact); the iteration counter is not advanced on a skipped no-split round and the next round re-bags with the C++-faithful RNG cadence; the first-iter constant baseline is byte-unchanged; booster.rs's popped-round bookkeeping (prev_tree_count skip, no duplicate snap.score push, best_iteration) is confirmed harmless for the matrix cell and every named score_accumulation/best_iteration assertion; lgbm-boosting unit suite GREEN; only the later no-split bagged path changed.</done>
</task>

<task type="auto">
  <name>Task 3: Un-#[ignore] quantile_loop_matrix asserting real-binary parity</name>
  <files>crates/oracle-harness/tests/boosting_parity.rs</files>
  <read_first>
    - crates/oracle-harness/tests/boosting_parity.rs (the `#[ignore = "DEF-07-13-01: ..."]` attribute at :2539 and the `quantile_loop_matrix` cell body at :2540 — it already calls the right replay/assert helper; only the #[ignore] attribute + its now-stale comment must be removed. Confirm the cell covers the bagged sub-cell `quantile_bag1_es0_bfa0` and the already-green non-bagged/alpha cells. NOTE: the `quantile_loop_matrix` helper (`replay_family_a_loop_cell`, ~:2370) compares only `min(rust.len, golden.len)` OVERLAPPING trees and does NOT assert tree-COUNT equality — so this cell is NOT the 10-tree count guard; the exact 10-tree structural guarantee is owned by the Task-1 diagnostic `quantile_bagged_no_split_emission_contract`. Do not mistake the min-length loop cell for the count guard.)
    - .planning/phases/07-parity-completing-variants/deferred-items.md (the DEF-07-13-01 entry :163-198 — to cross-check that quantile_loop_matrix is the ONLY cell this attribute gates, and that quantile_alpha_axis + the quantile SPINE are already green)
  </read_first>
  <action>
    Remove the `#[ignore = "DEF-07-13-01: ..."]` attribute (and its now-stale
    comment) from `quantile_loop_matrix` so it runs and asserts
    real-lib_lightgbm-4.6 parity via its existing helper — including the
    `quantile_bag1_es0_bfa0` bagged sub-cell. Do NOT weaken any
    assertion or change any helper — only the #[ignore] attribute and its stale
    comment are removed. Do NOT touch any other cell. NOTE: the exact 10-tree
    structural guarantee is owned by the Task-1 diagnostic
    (`quantile_bagged_no_split_emission_contract`), NOT this cell — its helper
    (`replay_family_a_loop_cell`, ~:2370) only compares `min(rust.len, golden.len)`
    overlapping trees and does not assert tree-count equality, so it confirms the
    surviving trees are bit-exact but relies on Task 1 for the count. If the cell
    still fails after the Task-2 fix, STOP — do not weaken the assertion; surface it
    as an unresolved cell at the Task-4 checkpoint (which then triggers a
    REVERT-and-reconsider of Task 2).
  </action>
  <verify>
    <automated>cargo test -p oracle-harness --test boosting_parity quantile_loop_matrix 2>&1 | tail -20</automated>
  </verify>
  <done>quantile_loop_matrix runs (no longer ignored) and passes real-binary parity (overlapping trees bit-exact); the 10-tree count guarantee remains owned by the Task-1 diagnostic; no other cell touched; no assertion weakened.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Full cross-variant no-regression merge-gate suite (hard gate — shared boosting-loop tree-emission contract)</name>
  <what-built>
    The C++-faithful no-split bagged pop/skip + iter-non-advance + re-bag-retry in
    the GBDT boosting loop (Task 2) and the un-#[ignore]d quantile_loop_matrix
    (Task 3). This edit touches the SHARED boosting-loop tree-emission contract
    (tree count, the `Tree::as_constant` push path, per-iteration RNG-advance /
    re-bag timing) and the CPU f64-fold path that is the project's HARD merge gate,
    so a human checkpoint around the full cross-variant suite is required (mirroring
    07-13's blocking-human posture, broadened to all bagged/randomized variants).
  </what-built>
  <how-to-verify>
    1. Run the full workspace suite with the capture python on PATH:
       `LGBM_CAPTURE_PYTHON=/tmp/lgbm-capture-venv/bin/python cargo test --workspace 2>&1 | tail -80`
    2. Confirm ALL of these stay GREEN and bit-exact, with NO weakened tolerance
       and NO capped horizon (if ANY regresses, REVERT the Task-2 edit and
       reconsider — never weaken an assertion, cap a horizon, or special-case the
       quantile cell):
       - Every bagged/randomized variant: DART (`dart_parity_matrix`,
         `dart_drop_rng_replay`), GOSS (`goss_parity_matrix`, `goss_rng_replay`),
         RF (`rf_single_parity`, `rf_multi_parity`), all bagging cells
         (`bagging_rng`, every `*_bag1_*`), `subset_determinism_diagnostic`
       - The FIRST-iteration no-split constant baseline PRESERVED:
         `regression_l1_bag1` Tree=0 unchanged;
         `class_need_train_false_pushes_constant_tree` + the gbdt unit tests green
       - ALL cells closed by 07-13 (Family A: fair/gamma/tweedie/quantile_alpha_axis)
         and by the remaining-ignored-cells session (Family B: monotone_mixed,
         forced_nested, extra_trees_seed6, extra_trees_seed9) stay green
       - `kernel_parity` 4/4 bit-exact; `learner_parity` 29/29; all `*_spine` /
         `*_gradients` cells green; the new `quantile_loop_matrix` GREEN
       - Every `*_score_accumulation` / `best_iteration` assertion stays green
         (booster.rs's popped-round bookkeeping must not have perturbed per-iteration
         score accumulation or the reported best_iteration)
       - Full `cargo test --workspace` 0-failed, 0 ignored (was 1 ignored)
    3. Confirm `git status --porcelain LightGBM/` is empty (LightGBM/ never
       git-added; any `/tmp/LightGBM` or wheel-oracle instrumentation reverted).
  </how-to-verify>
  <resume-signal>Type "approved" once the full workspace suite is green (0 failed, 0 ignored) with every bagged/randomized variant + the first-iter constant baseline + all 07-13/Family-B cells bit-exact and no assertion weakened, or describe the regressed cell (which then triggers a REVERT-and-reconsider of Task 2 rather than a tolerance change).</resume-signal>
  <action>Human-gated verification of the shared boosting-loop tree-emission contract and the bit-exact merge gate (no autonomous code action). A regression in any bagged/randomized variant or the first-iter constant baseline must be reverted, never papered over with a weakened assertion, capped horizon, or quantile special-case. LightGBM/ is never git-added.</action>
  <verify><automated>LGBM_CAPTURE_PYTHON=/tmp/lgbm-capture-venv/bin/python cargo test --workspace 2>&1 | tail -80</automated></verify>
  <done>The full `cargo test --workspace` is GREEN (0 failed, 0 ignored); every bagged/randomized variant (DART/GOSS/RF/all bagging) + the first-iter constant baseline + all 07-13 Family-A + Family-B cells + kernel_parity 4/4 + learner_parity 29/29 + all *_spine/*_gradients + every *_score_accumulation/best_iteration remain bit-exact; quantile_loop_matrix passes; no tolerance weakened, no horizon capped, no quantile special-case; LightGBM/ never git-added.</done>
</task>

<task type="auto">
  <name>Task 5: Clear DEF-07-13-01 in deferred-items.md — HARD-GATED: do NOT run until Task 4's blocking-human checkpoint is APPROVED and `cargo test --workspace` is GREEN (0 failed, 0 ignored)</name>
  <files>.planning/phases/07-parity-completing-variants/deferred-items.md</files>
  <read_first>
    - .planning/phases/07-parity-completing-variants/deferred-items.md (the DEF-07-13-01 section :163-198 to flip to RESOLVED; the DEF-07-02/03 :9-159 and DEF-07-11 :202-240 sections which are ALREADY RESOLVED and MUST be left untouched)
    - Task 4 (this plan) — its `checkpoint:human-verify` (gate="blocking-human") cross-variant no-regression gate is a HARD precondition: confirm it has been explicitly APPROVED ("approved") and the full `cargo test --workspace` is GREEN (0 failed, 0 ignored) before reading/editing deferred-items.md
  </read_first>
  <action>
    HARD PRECONDITION (do NOT proceed otherwise): this task MUST NOT run until
    Task 4's `checkpoint:human-verify` (gate="blocking-human") cross-variant
    no-regression gate has been explicitly APPROVED ("approved") AND the full
    `LGBM_CAPTURE_PYTHON=/tmp/lgbm-capture-venv/bin/python cargo test --workspace`
    is GREEN with 0 failed AND 0 ignored, every bagged/randomized variant +
    first-iter constant baseline + all 07-13/Family-B cells bit-exact, and no
    assertion weakened / horizon capped / quantile special-cased. DEF-07-13-01 may
    be marked RESOLVED ONLY after the no-regression gate passes — NEVER before. If
    Task 4 is not yet APPROVED, or the workspace suite is not green / not 0-ignored
    (e.g. a regressed variant triggered the REVERT-and-reconsider path), STOP and do
    NOT edit deferred-items.md — DEF-07-13-01 remains OPEN.

    Once that precondition is satisfied:
    Mark DEF-07-13-01 as CLOSED/RESOLVED in deferred-items.md: flip the STATUS to
    RESOLVED, note the closing plan (07-14) and the root cause (the GBDT no-split
    bagged-tree emission policy — C++ pops the would-be constant on a non-first
    no-split bagged iteration, does not advance iter_, and re-bags, per
    gbdt.cpp:406-447 and `.planning/debug/remaining-ignored-cells.md` Group 3), and
    record that `quantile_loop_matrix` now asserts real-binary parity GREEN
    (10-tree structure, wheel tree4 ≡ Rust tree5) with no tolerance weakened. State
    that this was the LAST ignored cell — phase 07 now has zero deferred parity
    cells. Do NOT delete the historical evidence (keep the section as a closed
    record). Leave the DEF-07-02/03 and DEF-07-11 sections ENTIRELY untouched —
    they remain RESOLVED.
  </action>
  <verify>
    <automated>grep -n "DEF-07-13-01\|RESOLVED\|DEF-07-02\|DEF-07-11" .planning/phases/07-parity-completing-variants/deferred-items.md | head -20</automated>
  </verify>
  <done>DEF-07-13-01 is recorded as RESOLVED (closed by 07-14) ONLY after Task 4's blocking-human gate is APPROVED and `cargo test --workspace` is GREEN (0 failed, 0 ignored), with the root cause + GREEN-parity note + "last ignored cell" statement; DEF-07-02/03 and DEF-07-11 are unchanged and still RESOLVED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is an offline numerical-correctness fix over the in-repo Rust crates and the
read-only C++ reference. No network, auth, session, PII, or untrusted-input
surface is introduced. Inputs are developer-supplied training config and committed
deterministic goldens.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-14-01 | Tampering | the shared GBDT boosting-loop tree-emission contract (tree count, as_constant push path, per-iter RNG/re-bag timing) | mitigate | The Task-4 blocking-human checkpoint asserts every bagged/randomized variant (DART/GOSS/RF/all bagging) + the first-iter constant baseline stay bit-exact; a regression triggers REVERT-and-reconsider, never a weakened assertion or capped horizon |
| T-07-14-02 | Tampering | the bit-exact CPU f64-fold merge gate (kernel_parity / learner_parity / *_spine / *_gradients) | mitigate | The Task-4 gate re-runs the full `cargo test --workspace`; any merge-gate regression reverts Task 2; `LightGBM/` is NEVER git-added (memory: lightgbm-ref-tree-untracked); wheel/CLI oracle instrumentation reverted after capture |

No other security-relevant surface: this plan edits a boosting-loop emission
policy, a test attribute, and a deferral record; it parses no untrusted input.
</threat_model>

<verification>
- `cargo test -p oracle-harness --test boosting_parity quantile_bagged_no_split_emission_contract` — FAILS at Task 1 (pins the 12-tree divergence), GREEN after Task 2
- `cargo test -p lgbm-boosting` GREEN after Task 2
- `cargo test -p oracle-harness --test boosting_parity quantile_loop_matrix` — GREEN after Task 3 (no longer ignored, bagged sub-cell grows 10 trees)
- `LGBM_CAPTURE_PYTHON=/tmp/lgbm-capture-venv/bin/python cargo test --workspace` GREEN at Task 4 (0 failed, 0 ignored) with DART/GOSS/RF/all-bagging + first-iter constant baseline + 07-13 Family-A + Family-B cells + kernel_parity 4/4 + learner_parity 29/29 + all *_spine/*_gradients bit-exact
- `deferred-items.md`: DEF-07-13-01 RESOLVED; DEF-07-02/03 + DEF-07-11 retained RESOLVED
</verification>

<success_criteria>
DEF-07-13-01 — the LAST ignored parity cell — is closed: the Rust GBDT boosting
loop replicates the C++ no-split bagged-tree emission policy (pop/skip +
iter-non-advance + re-bag-retry, gbdt.cpp:406-447), `quantile_loop_matrix`
(bagged sub-cell `quantile_bag1_es0_bfa0`) grows 10 trees and asserts real-binary
parity GREEN (wheel tree4 ≡ Rust tree5), and the bit-exact CPU f64-fold merge gate
plus every bagged/randomized variant and the first-iteration constant baseline are
preserved with NO weakened tolerance, NO capped horizon, and NO quantile
special-case. Phase 07 now has zero deferred parity cells. DEF-07-02/03 and
DEF-07-11 remain RESOLVED and untouched.
</success_criteria>

<artifacts_produced>
## Artifacts this plan produces (Plan 07-14)

- `crates/lgbm-boosting/src/gbdt.rs` — C++-faithful no-split bagged-iteration pop/skip + iter-non-advance + re-bag-retry in the GBDT boosting loop / train-one-iter path (first-iter constant baseline preserved; full-corpus / DART / GOSS / RF paths byte-unchanged)
- `crates/lgbm/src/booster.rs` — popped/no-new-tree round bookkeeping confirmed harmless in the real parity driver loop (~:1098-1187): prev_tree_count skip robust to no-new-tree, no duplicate `snap.score` push on a popped round, `best_iteration`/`num_iteration()` harmless for the matrix cell and every named score_accumulation/best_iteration assertion
- `crates/oracle-harness/tests/boosting_parity.rs` — new `quantile_bagged_no_split_emission_contract` diagnostic (owns the exact 10-tree count guarantee) + `quantile_loop_matrix` un-#[ignore]d asserting real-binary parity (overlapping trees bit-exact)
- `.planning/phases/07-parity-completing-variants/deferred-items.md` — DEF-07-13-01 marked RESOLVED (last ignored cell closed); DEF-07-02/03 + DEF-07-11 retained RESOLVED
</artifacts_produced>

<output>
Create `.planning/phases/07-parity-completing-variants/07-14-SUMMARY.md` when done
</output>
</content>
